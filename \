
#define _XOPEN_SOURCE 600
#include "runtime3.h"
#include <limits.h>
#include <stdatomic.h>
#include <stdio.h>
#include <stdlib.h>

// TODO: these are only needed until core.toc codegen is complete
Value *noImpl1(FnArity *arity, Value *str_0) { abort(); }

Value *noImpl2(FnArity *arity, Value *str_0, Value *arg1) { abort(); }

Value *noImpl3(FnArity *arity, Value *str_0, Value *arg1, Value *arg2) {
  abort();
}
Term glblFn1;
void glblCFn0(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  Term result = NUL;
  if (arityArgs.count == 1) {
    Term x_1 = arityArgs.args[0];
    if (term_tag(x_1) == I60) {
      result = term_val((Term)stringValue("Integer"));
    } else {
      char *name = typeName(((Value *)x_1)->type);
      result = term_val((Term)stringValue(name));
    };
    moveStore(port(2, term_loc(args)), result);
  }
  return;
}
Term glblFn1 = new_ref(glblCFn0);
Term glbltype_name6;
void glblglbl5(Term ref, Term args) {
  Term apps, lams, V;
  Location vLoc;
  Term seq_5 = pair_make(LAM, 0, SUB, NUL);
  Location r_seq_5 = port(2, term_loc(seq_5));
  Term x_4 = term_new(VAR, 0, port(1, term_loc(seq_5)));
  // allocate args at core: 38
  // call default-type-name at core: 40
  Term Rslt_7Args = pair_make(APP, 0, x_4, SUB);
  Term Rslt_7 = term_new(VAR, 0, port(2, term_loc(Rslt_7Args)));
#ifdef STRICT
  store_redex(Rslt_7Args, glblFn1);
#else
  swapStore(term_loc(Rslt_7), pair_make(LAZ, 0, Rslt_7Args, glblFn1));
#endif

  // link args to body
  swapStore(r_seq_5, Rslt_7);

  Term Rslt_7V = get(term_loc(Rslt_7));
  apps = take(port(1, term_loc(Rslt_7V)));
  lams = take(port(2, term_loc(Rslt_7V)));

  vLoc = port(2, term_loc(apps));
  swapStore(term_loc(Rslt_7), pair_make(LAZ, 0, apps, lams));
  store_redex(args, seq_5);
  return;
}
Term glbltype_name6 = new_ref(glblglbl5);
Term glblProto3;
Term glblFn8;
void glblCFn7(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  Term result = NUL;
  if (arityArgs.count == 1) {
    Term x_1 = arityArgs.args[0];

    switch (term_tag(x_1)) {
    case I60:
      result = new_i60(IntegerType);
      break;

    case F60:
      result = new_i60(FloatType);
      break;

    case REF:
      BOOM("too tire");
      break;

    default:
      result = new_i60(((Value *)x_1)->type);
      dec_and_free(x_1, 1);
    }
    moveStore(port(2, term_loc(args)), result);
  }
  return;
}
Term glblFn8 = new_ref(glblCFn7);
Term glblProto14;
void glblSome16(Term ref, Term args) {
  Term callArgs = pair_make(APP, 0, new_i60(1), args);
  callArgs = pair_make(APP, 0, new_i60(43), callArgs);
  interact(callArgs, construct);
  return;
}
void glblNone11(Term ref, Term args) {
  Term callArgs = pair_make(APP, 0, new_i60(0), args);
  callArgs = pair_make(APP, 0, new_i60(42), callArgs);
  interact(callArgs, construct);
  return;
}
Term glblVal13 = new_ref(glblNone11);
Term glblVal13;
Term glblRslt12;
Term glblVal18 = new_ref(glblSome16);
Term glblVal18;
Term glblFld17;
void glblFldFn21(Term ref, Term args) {
  args = pair_make(APP, 0, new_i60(0), args);
  store_redex(args, accessField);
  return;
}
Term glblFld17 = new_ref(glblFldFn21);
Term glblProto19;
Term glblProto22;
Term glblProto24;
Term glblProto26;
Term glblProto28;
Term glblProto30;
Term glblProto32;
Term glblProto34;
Term glblProto36;
Term glblProto38;
Term glblProto40;
Term glblProto42;
Term glblProto44;
Term glblProto46;
Term glblProto48;
Term glblProto50;
Term glblProto52;
Term glblProto54;
Term glblProto56;
Term glblProto58;
Term glblProto60;
Term glblProto62;
Term glblProto64;
Term glblProto66;
Term glblProto68;
Term glblProto70;
Term glblProto72;
Term glblProto74;
Term glblProto76;
Term glblProto78;
Term glblProto80;
Term glblProto82;
Term glblProto84;
Term glblProto86;
Term glblProto88;
Term glblProto90;
Term glblProto92;
Term glblProto94;
Term glblProto96;
Term glblProto98;
Term glblProto100;
Term glblProto102;
Term glblProto104;
Term glblProto106;
Term glblProto108;
Term glblFn111;
void glblCFn110(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  Term result = NUL;
  if (arityArgs.count == 1) {
    Term str_1 = arityArgs.args[0];

    if (term_tag(str_1) != VAL) {
      fprintf(stderr, "\ninvalid type for 'abort': Integer or Float\n");
      abort();
    }
    Value *s = (Value *)((u64)str_1 & ~7);
    if (s->type == StringBufferType) {
      fprintf(stderr, "%-.*s", (int)((String *)s)->len, ((String *)s)->buffer);
    } else {
      fprintf(stderr, "\ninvalid type for 'abort': %ld\n", s->type);
      abort();
    }
    abort();
    moveStore(port(2, term_loc(args)), result);
  }
  return;
}
Term glblFn111 = new_ref(glblCFn110);
Term glblFn114;
void glblCFn113(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  Term result = NUL;
  if (arityArgs.count == 1) {
    Term str_1 = arityArgs.args[0];

    if (term_tag(str_1) != VAL) {
      fprintf(stderr, "\ninvalid type for 'pr*': Integer or Float\n");
      abort();
    }
    Value *s = (Value *)((u64)str_1 & ~7);
    if (s->type == StringBufferType) {
      fprintf(stdout, "%-.*s", (int)((String *)s)->len, ((String *)s)->buffer);
    } else if (s->type == SubStringType) {
      ReifiedVal *ss = (ReifiedVal *)str_1;
      String *parent = (String *)ss->impls[0];
      long start = get_i60(ss->impls[1]);
      int len = (int)get_i60(ss->impls[2]);
      fprintf(stdout, "%-.*s", len, &parent->buffer[start]);
    } else {
      fprintf(stdout, "\ninvalid type for 'pr*': %ld\n", s->type);
      abort();
    }
    dec_and_free(str_1, 1);
    result = new_i60(0);
    moveStore(port(2, term_loc(args)), result);
  }
  return;
}
Term glblFn114 = new_ref(glblCFn113);
Term glblFn117;
void glblCFn116(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 2, &arityArgs);
  Term result = NUL;
  if (arityArgs.count == 2) {
    Term x_1 = arityArgs.args[0];
    Term y_2 = arityArgs.args[1];

    if (term_tag(x_1) != I60 && term_tag(y_2) != I60) {
      BOOM("Invalid inputs to '+'");
    }
    result = new_i60(get_i60(x_1) + get_i60(y_2));

    moveStore(port(2, term_loc(args)), result);
  }
  return;
}
Term glblFn117 = new_ref(glblCFn116);
Term glblFn120;
void glblCFn119(Term ref, Term args) {
  Term apps, lams, V;
  Location vLoc;
  Term seq_2 = pair_make(LAM, 0, SUB, NUL);
  Location r_seq_2 = port(2, term_loc(seq_2));
  Term x_1 = term_new(VAR, 0, port(1, term_loc(seq_2)));
  // allocate args at core: 262
  Term Rslt_4 = make_op(OP_ADD, new_i60(1), x_1);
  // link args to body
  swapStore(r_seq_2, Rslt_4);

  Term Rslt_4V = get(term_loc(Rslt_4));
  apps = take(port(1, term_loc(Rslt_4V)));
  lams = take(port(2, term_loc(Rslt_4V)));

  vLoc = port(2, term_loc(apps));
  swapStore(term_loc(Rslt_4), pair_make(LAZ, 0, apps, lams));
  store_redex(args, seq_2);
  return;
}
Term glblFn120 = new_ref(glblCFn119);
Term glblFn123;
void glblCFn122(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 2, &arityArgs);
  Term result = NUL;
  if (arityArgs.count == 2) {
    Term x_1 = arityArgs.args[0];
    Term y_2 = arityArgs.args[1];

    fprintf(stderr, "'-' should be optimized away\n");
    abort();

    moveStore(port(2, term_loc(args)), result);
  }
  return;
}
Term glblFn123 = new_ref(glblCFn122);
Term glblFn126;
void glblCFn125(Term ref, Term args) {
  Term apps, lams, V;
  Location vLoc;
  Term seq_2 = pair_make(LAM, 0, SUB, NUL);
  Location r_seq_2 = port(2, term_loc(seq_2));
  Term x_1 = term_new(VAR, 0, port(1, term_loc(seq_2)));
  // allocate args at core: 274
  Term Rslt_4 = make_op(OP_SUB, x_1, new_i60(1));
  // link args to body
  swapStore(r_seq_2, Rslt_4);

  Term Rslt_4V = get(term_loc(Rslt_4));
  apps = take(port(1, term_loc(Rslt_4V)));
  lams = take(port(2, term_loc(Rslt_4V)));

  vLoc = port(2, term_loc(apps));
  swapStore(term_loc(Rslt_4), pair_make(LAZ, 0, apps, lams));
  store_redex(args, seq_2);
  return;
}
Term glblFn126 = new_ref(glblCFn125);
Term glblFn129;
void glblCFn128(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 2, &arityArgs);
  Term result = NUL;
  if (arityArgs.count == 2) {
    Term x_1 = arityArgs.args[0];
    Term y_2 = arityArgs.args[1];

    fprintf(stderr, "'*' should be optimized away\n");
    abort();

    moveStore(port(2, term_loc(args)), result);
  }
  return;
}
Term glblFn129 = new_ref(glblCFn128);
Term glblVal130 = (Term)&empty_vect_struct;
Term glblVal130;
Term glblFn133;
void glblCFn132(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 2, &arityArgs);
  Term result = NUL;
  if (arityArgs.count == 2) {
    Term vect_1 = arityArgs.args[0];
    Term v_2 = arityArgs.args[1];

    result = term_val((Term)vectConj((Vector *)vect_1, v_2));
    moveStore(port(2, term_loc(args)), result);
  }
  return;
}
Term glblFn133 = new_ref(glblCFn132);
struct {
  TYPE_SIZE type;
  REFS_SIZE refs;
  int64_t *hash;
  Value *parent;
  int64_t len;
  char buffer[5];
} str0 = {StringBufferType, REFS_STATIC, 0, 0, 4, "core"};
Term glblStr149 = term_new_(VAL, (Term)&str0);
Term glbl_EQ_148;
void glbl_EQ_147(Term ref, Term args) {
  Term apps, lams, V;
  Location vLoc;
  Term seq_8 = pair_make(LAM, 0, SUB, NUL);
  Location r_seq_8 = port(2, term_loc(seq_8));
  Term y_7 = term_new(VAR, 0, port(1, term_loc(seq_8)));
  seq_8 = pair_make(LAM, 0, SUB, seq_8);
  Term x_6 = term_new(VAR, 0, port(1, term_loc(seq_8)));
  // allocate args at core: 293
  // call type-num at core: 294
  Term Rslt_10Args = pair_make(APP, 0, x_6, SUB);
  Term Rslt_10 = term_new(VAR, 0, port(2, term_loc(Rslt_10Args)));
#ifdef STRICT
  store_redex(Rslt_10Args, glblFn8);
#else
  swapStore(term_loc(Rslt_10), pair_make(LAZ, 0, Rslt_10Args, glblFn8));
#endif

  // call type-num at core: 294
  Term Rslt_11Args = pair_make(APP, 0, y_7, SUB);
  Term Rslt_11 = term_new(VAR, 0, port(2, term_loc(Rslt_11Args)));
#ifdef STRICT
  store_redex(Rslt_11Args, glblFn8);
#else
  swapStore(term_loc(Rslt_11), pair_make(LAZ, 0, Rslt_11Args, glblFn8));
#endif

  // call = at core: 294
  Term Rslt_12Args = pair_make(APP, 0, Rslt_11, SUB);
  Term Rslt_12 = term_new(VAR, 0, port(2, term_loc(Rslt_12Args)));
  Rslt_12Args = pair_make(APP, 0, Rslt_10, Rslt_12Args);
  Rslt_12Args = pair_make(APP, 0, new_i60(294), Rslt_12Args);
  Rslt_12Args = pair_make(APP, 0, glblStr149, Rslt_12Args);
#ifdef STRICT
  store_redex(Rslt_12Args, glblProto19);
#else
  swapStore(term_loc(Rslt_12), pair_make(LAZ, 0, Rslt_12Args, glblProto19));
#endif
  // link args to body
  swapStore(r_seq_8, Rslt_12);

  Term Rslt_12V = get(term_loc(Rslt_12));
  apps = take(port(1, term_loc(Rslt_12V)));
  lams = take(port(2, term_loc(Rslt_12V)));

  vLoc = port(2, term_loc(apps));
  swapStore(term_loc(Rslt_12), pair_make(LAZ, 0, apps, lams));
  store_redex(args, seq_8);
  return;
}
Term glbl_EQ_148 = new_ref(glbl_EQ_147);
Term glblrecurse146;
void glblrecurse145(Term ref, Term args) {
  Term apps, lams, V;
  Location vLoc;
  Term seq_16 = pair_make(LAM, 0, SUB, NUL);
  Location r_seq_16 = port(2, term_loc(seq_16));
  Term f_15 = term_new(VAR, 0, port(1, term_loc(seq_16)));
  seq_16 = pair_make(LAM, 0, SUB, seq_16);
  Term v_14 = term_new(VAR, 0, port(1, term_loc(seq_16)));
  // allocate args at core: 295
  swapStore(term_loc(f_15), ERA);
  // link args to body
  swapStore(r_seq_16, v_14);

  V = get(r_seq_16);
  apps = pair_make(APP, 0, V, SUB);
  lams = pair_make(LAM, 0, SUB, NUL);
  swapStore(port(2, term_loc(lams)), term_new(VAR, 0, port(1, term_loc(lams))));
  vLoc = port(2, term_loc(apps));
  swapStore(vLoc, pair_make(LAZ, 0, apps, lams));
  swapStore(r_seq_16, term_new(VAR, 0, vLoc));
  store_redex(args, seq_16);
  return;
}
Term glblrecurse146 = new_ref(glblrecurse145);
Term glbleither144;
void glbleither143(Term ref, Term args) {
  Term apps, lams, V;
  Location vLoc;
  Term seq_21 = pair_make(LAM, 0, SUB, NUL);
  Location r_seq_21 = port(2, term_loc(seq_21));
  Term cont_20 = term_new(VAR, 0, port(1, term_loc(seq_21)));
  seq_21 = pair_make(LAM, 0, SUB, seq_21);
  Term v_19 = term_new(VAR, 0, port(1, term_loc(seq_21)));
  // allocate args at core: 297
  swapStore(term_loc(v_19), ERA);
  // link args to body
  swapStore(r_seq_21, cont_20);

  V = get(r_seq_21);
  apps = pair_make(APP, 0, V, SUB);
  lams = pair_make(LAM, 0, SUB, NUL);
  swapStore(port(2, term_loc(lams)), term_new(VAR, 0, port(1, term_loc(lams))));
  vLoc = port(2, term_loc(apps));
  swapStore(vLoc, pair_make(LAZ, 0, apps, lams));
  swapStore(r_seq_21, term_new(VAR, 0, vLoc));
  store_redex(args, seq_21);
  return;
}
Term glbleither144 = new_ref(glbleither143);
Term glblcond142;
void glblcond141(Term ref, Term args) {
  Term apps, lams, V;
  Location vLoc;
  Term seq_27 = pair_make(LAM, 0, SUB, NUL);
  Location r_seq_27 = port(2, term_loc(seq_27));
  Term cont_26 = term_new(VAR, 0, port(1, term_loc(seq_27)));
  seq_27 = pair_make(LAM, 0, SUB, seq_27);
  Term x_25 = term_new(VAR, 0, port(1, term_loc(seq_27)));
  seq_27 = pair_make(LAM, 0, SUB, seq_27);
  Term v_24 = term_new(VAR, 0, port(1, term_loc(seq_27)));
  // allocate args at core: 299
  swapStore(term_loc(v_24), ERA);
  swapStore(term_loc(x_25), ERA);
  // link args to body
  swapStore(r_seq_27, cont_26);

  V = get(r_seq_27);
  apps = pair_make(APP, 0, V, SUB);
  lams = pair_make(LAM, 0, SUB, NUL);
  swapStore(port(2, term_loc(lams)), term_new(VAR, 0, port(1, term_loc(lams))));
  vLoc = port(2, term_loc(apps));
  swapStore(vLoc, pair_make(LAZ, 0, apps, lams));
  swapStore(r_seq_27, term_new(VAR, 0, vLoc));
  store_redex(args, seq_27);
  return;
}
Term glblcond142 = new_ref(glblcond141);
Term glbland140;
void glbland139(Term ref, Term args) {
  Term apps, lams, V;
  Location vLoc;
  Term seq_32 = pair_make(LAM, 0, SUB, NUL);
  Location r_seq_32 = port(2, term_loc(seq_32));
  Term arg_31 = term_new(VAR, 0, port(1, term_loc(seq_32)));
  seq_32 = pair_make(LAM, 0, SUB, seq_32);
  Term x_30 = term_new(VAR, 0, port(1, term_loc(seq_32)));
  // allocate args at core: 301
  swapStore(term_loc(arg_31), ERA);
  // link args to body
  swapStore(r_seq_32, x_30);

  V = get(r_seq_32);
  apps = pair_make(APP, 0, V, SUB);
  lams = pair_make(LAM, 0, SUB, NUL);
  swapStore(port(2, term_loc(lams)), term_new(VAR, 0, port(1, term_loc(lams))));
  vLoc = port(2, term_loc(apps));
  swapStore(vLoc, pair_make(LAZ, 0, apps, lams));
  swapStore(r_seq_32, term_new(VAR, 0, vLoc));
  store_redex(args, seq_32);
  return;
}
Term glbland140 = new_ref(glbland139);
Term glblor138;
void glblor137(Term ref, Term args) {
  Term apps, lams, V;
  Location vLoc;
  Term seq_37 = pair_make(LAM, 0, SUB, NUL);
  Location r_seq_37 = port(2, term_loc(seq_37));
  Term y_36 = term_new(VAR, 0, port(1, term_loc(seq_37)));
  seq_37 = pair_make(LAM, 0, SUB, seq_37);
  Term arg_35 = term_new(VAR, 0, port(1, term_loc(seq_37)));
  // allocate args at core: 303
  swapStore(term_loc(arg_35), ERA);
  // link args to body
  swapStore(r_seq_37, y_36);

  V = get(r_seq_37);
  apps = pair_make(APP, 0, V, SUB);
  lams = pair_make(LAM, 0, SUB, NUL);
  swapStore(port(2, term_loc(lams)), term_new(VAR, 0, port(1, term_loc(lams))));
  vLoc = port(2, term_loc(apps));
  swapStore(vLoc, pair_make(LAZ, 0, apps, lams));
  swapStore(r_seq_37, term_new(VAR, 0, vLoc));
  store_redex(args, seq_37);
  return;
}
Term glblor138 = new_ref(glblor137);
Term glblmap136;
void glblmap135(Term ref, Term args) {
  Term apps, lams, V;
  Location vLoc;
  Term seq_42 = pair_make(LAM, 0, SUB, NUL);
  Location r_seq_42 = port(2, term_loc(seq_42));
  Term f_41 = term_new(VAR, 0, port(1, term_loc(seq_42)));
  seq_42 = pair_make(LAM, 0, SUB, seq_42);
  Term x_40 = term_new(VAR, 0, port(1, term_loc(seq_42)));
  // allocate args at core: 305
  swapStore(term_loc(f_41), ERA);
  // link args to body
  swapStore(r_seq_42, x_40);

  V = get(r_seq_42);
  apps = pair_make(APP, 0, V, SUB);
  lams = pair_make(LAM, 0, SUB, NUL);
  swapStore(port(2, term_loc(lams)), term_new(VAR, 0, port(1, term_loc(lams))));
  vLoc = port(2, term_loc(apps));
  swapStore(vLoc, pair_make(LAZ, 0, apps, lams));
  swapStore(r_seq_42, term_new(VAR, 0, vLoc));
  store_redex(args, seq_42);
  return;
}
Term glblmap136 = new_ref(glblmap135);
struct {
  TYPE_SIZE type;
  REFS_SIZE refs;
  int64_t *hash;
  Value *parent;
  int64_t len;
  char buffer[2];
} str1 = {StringBufferType, REFS_STATIC, 0, 0, 1, "\n"};
Term glblStr159 = term_new_(VAL, (Term)&str1);
struct {
  TYPE_SIZE type;
  REFS_SIZE refs;
  int64_t *hash;
  Value *parent;
  int64_t len;
  char buffer[2];
} str2 = {StringBufferType, REFS_STATIC, 0, 0, 1, " "};
Term glblStr160 = term_new_(VAL, (Term)&str2);
Term glblc162;
void glblc161(Term ref, Term args) {
  Term apps, lams, V;
  Location vLoc;
  Term seq_15 = pair_make(LAM, 0, SUB, NUL);
  Location r_seq_15 = port(2, term_loc(seq_15));
  Term s_14 = term_new(VAR, 0, port(1, term_loc(seq_15)));
  seq_15 = pair_make(LAM, 0, SUB, seq_15);
  Term v_13 = term_new(VAR, 0, port(1, term_loc(seq_15)));
  // allocate args at core: 313
  // call pr* at core: 314
  Term Rslt_17Args = pair_make(APP, 0, s_14, SUB);
  Term Rslt_17 = term_new(VAR, 0, port(2, term_loc(Rslt_17Args)));
#ifdef STRICT
  store_redex(Rslt_17Args, glblFn114);
#else
  swapStore(term_loc(Rslt_17), pair_make(LAZ, 0, Rslt_17Args, glblFn114));
#endif

  // call conj at core: 314
  Term Rslt_18Args = pair_make(APP, 0, Rslt_17, SUB);
  Term Rslt_18 = term_new(VAR, 0, port(2, term_loc(Rslt_18Args)));
  Rslt_18Args = pair_make(APP, 0, v_13, Rslt_18Args);
  Rslt_18Args = pair_make(APP, 0, new_i60(314), Rslt_18Args);
  Rslt_18Args = pair_make(APP, 0, glblStr149, Rslt_18Args);
#ifdef STRICT
  store_redex(Rslt_18Args, glblProto64);
#else
  swapStore(term_loc(Rslt_18), pair_make(LAZ, 0, Rslt_18Args, glblProto64));
#endif
  // link args to body
  swapStore(r_seq_15, Rslt_18);

  Term Rslt_18V = get(term_loc(Rslt_18));
  apps = take(port(1, term_loc(Rslt_18V)));
  lams = take(port(2, term_loc(Rslt_18V)));

  vLoc = port(2, term_loc(apps));
  swapStore(term_loc(Rslt_18), pair_make(LAZ, 0, apps, lams));
  store_redex(args, seq_15);
  return;
}
Term glblc162 = new_ref(glblc161);
Term glblc164;
void glblc163(Term ref, Term args) {
  Term apps, lams, V;
  Location vLoc;
  Term seq_8 = pair_make(LAM, 0, SUB, NUL);
  Location r_seq_8 = port(2, term_loc(seq_8));
  Term val_7 = term_new(VAR, 0, port(1, term_loc(seq_8)));
  seq_8 = pair_make(LAM, 0, SUB, seq_8);
  Term v_6 = term_new(VAR, 0, port(1, term_loc(seq_8)));
  // allocate args at core: 311
  Term glblVal130_2;
  glblVal130 = dupeArg(glblVal130, &glblVal130_2, 0);

  // call str-vect at core: 312
  Term Rslt_10Args = pair_make(APP, 0, val_7, SUB);
  Term Rslt_10 = term_new(VAR, 0, port(2, term_loc(Rslt_10Args)));
  Rslt_10Args = pair_make(APP, 0, new_i60(312), Rslt_10Args);
  Rslt_10Args = pair_make(APP, 0, glblStr149, Rslt_10Args);
#ifdef STRICT
  store_redex(Rslt_10Args, glblProto106);
#else
  swapStore(term_loc(Rslt_10), pair_make(LAZ, 0, Rslt_10Args, glblProto106));
#endif
  // call conj at core: 312
  Term Rslt_11Args = pair_make(APP, 0, glblStr160, SUB);
  Term Rslt_11 = term_new(VAR, 0, port(2, term_loc(Rslt_11Args)));
  Rslt_11Args = pair_make(APP, 0, Rslt_10, Rslt_11Args);
  Rslt_11Args = pair_make(APP, 0, new_i60(312), Rslt_11Args);
  Rslt_11Args = pair_make(APP, 0, glblStr149, Rslt_11Args);
#ifdef STRICT
  store_redex(Rslt_11Args, glblProto64);
#else
  swapStore(term_loc(Rslt_11), pair_make(LAZ, 0, Rslt_11Args, glblProto64));
#endif
  // call reduce at core: 312
  Term Rslt_19Args = pair_make(APP, 0, glblc162, SUB);
  Term Rslt_19 = term_new(VAR, 0, port(2, term_loc(Rslt_19Args)));
  Rslt_19Args = pair_make(APP, 0, glblVal130_2, Rslt_19Args);
  Rslt_19Args = pair_make(APP, 0, Rslt_11, Rslt_19Args);
  Rslt_19Args = pair_make(APP, 0, new_i60(312), Rslt_19Args);
  Rslt_19Args = pair_make(APP, 0, glblStr149, Rslt_19Args);
#ifdef STRICT
  store_redex(Rslt_19Args, glblProto68);
#else
  swapStore(term_loc(Rslt_19), pair_make(LAZ, 0, Rslt_19Args, glblProto68));
#endif
  // call conj at core: 312
  Term Rslt_20Args = pair_make(APP, 0, Rslt_19, SUB);
  Term Rslt_20 = term_new(VAR, 0, port(2, term_loc(Rslt_20Args)));
  Rslt_20Args = pair_make(APP, 0, v_6, Rslt_20Args);
  Rslt_20Args = pair_make(APP, 0, new_i60(312), Rslt_20Args);
  Rslt_20Args = pair_make(APP, 0, glblStr149, Rslt_20Args);
#ifdef STRICT
  store_redex(Rslt_20Args, glblProto64);
#else
  swapStore(term_loc(Rslt_20), pair_make(LAZ, 0, Rslt_20Args, glblProto64));
#endif
  // link args to body
  swapStore(r_seq_8, Rslt_20);

  Term Rslt_20V = get(term_loc(Rslt_20));
  apps = take(port(1, term_loc(Rslt_20V)));
  lams = take(port(2, term_loc(Rslt_20V)));

  vLoc = port(2, term_loc(apps));
  swapStore(term_loc(Rslt_20), pair_make(LAZ, 0, apps, lams));
  store_redex(args, seq_8);
  return;
}
Term glblc164 = new_ref(glblc163);
Term glblFn158;
void glblCFn157(Term ref, Term args) {
  Term apps, lams, V;
  Location vLoc;
  Term seq_2 = pair_make(LAM, 0, SUB, NUL);
  Location r_seq_2 = port(2, term_loc(seq_2));
  Term vs_1 = term_new(VAR, 0, port(1, term_loc(seq_2)));
  // allocate args at core: 308
  Term glblVal130_1;
  glblVal130 = dupeArg(glblVal130, &glblVal130_1, 0);

  // call conj at core: 310
  Term Rslt_4Args = pair_make(APP, 0, glblStr159, SUB);
  Term Rslt_4 = term_new(VAR, 0, port(2, term_loc(Rslt_4Args)));
  Rslt_4Args = pair_make(APP, 0, vs_1, Rslt_4Args);
  Rslt_4Args = pair_make(APP, 0, new_i60(310), Rslt_4Args);
  Rslt_4Args = pair_make(APP, 0, glblStr149, Rslt_4Args);
#ifdef STRICT
  store_redex(Rslt_4Args, glblProto64);
#else
  swapStore(term_loc(Rslt_4), pair_make(LAZ, 0, Rslt_4Args, glblProto64));
#endif
  // call reduce at core: 310
  Term Rslt_21Args = pair_make(APP, 0, glblc164, SUB);
  Term Rslt_21 = term_new(VAR, 0, port(2, term_loc(Rslt_21Args)));
  Rslt_21Args = pair_make(APP, 0, glblVal130_1, Rslt_21Args);
  Rslt_21Args = pair_make(APP, 0, Rslt_4, Rslt_21Args);
  Rslt_21Args = pair_make(APP, 0, new_i60(310), Rslt_21Args);
  Rslt_21Args = pair_make(APP, 0, glblStr149, Rslt_21Args);
#ifdef STRICT
  store_redex(Rslt_21Args, glblProto68);
#else
  swapStore(term_loc(Rslt_21), pair_make(LAZ, 0, Rslt_21Args, glblProto68));
#endif
  // link args to body
  swapStore(r_seq_2, Rslt_21);

  Term Rslt_21V = get(term_loc(Rslt_21));
  apps = take(port(1, term_loc(Rslt_21V)));
  lams = take(port(2, term_loc(Rslt_21V)));

  vLoc = port(2, term_loc(apps));
  swapStore(term_loc(Rslt_21), pair_make(LAZ, 0, apps, lams));
  store_redex(args, seq_2);
  return;
}
Term glblFn158 = new_ref(glblCFn157);
struct {
  TYPE_SIZE type;
  REFS_SIZE refs;
  int64_t *hash;
  Value *parent;
  int64_t len;
  char buffer[7];
} str3 = {StringBufferType, REFS_STATIC, 0, 0, 6, "(Some "};
Term glblStr198 = term_new_(VAL, (Term)&str3);
struct {
  TYPE_SIZE type;
  REFS_SIZE refs;
  int64_t *hash;
  Value *parent;
  int64_t len;
  char buffer[2];
} str4 = {StringBufferType, REFS_STATIC, 0, 0, 1, ")"};
Term glblStr197 = term_new_(VAL, (Term)&str4);
Term glblstr_vect194;
void glblstr_vect193(Term ref, Term args) {
  Term apps, lams, V;
  Location vLoc;
  Term seq_24 = pair_make(LAM, 0, SUB, NUL);
  Location r_seq_24 = port(2, term_loc(seq_24));
  Term s_23 = term_new(VAR, 0, port(1, term_loc(seq_24)));
  // allocate args at core: 317
  Term glblVal130_3;
  glblVal130 = dupeArg(glblVal130, &glblVal130_3, 0);

  // call vect-conj at core: 318
  Term Rslt_26Args = pair_make(APP, 0, glblStr198, SUB);
  Term Rslt_26 = term_new(VAR, 0, port(2, term_loc(Rslt_26Args)));
  Rslt_26Args = pair_make(APP, 0, glblVal130_3, Rslt_26Args);
#ifdef STRICT
  store_redex(Rslt_26Args, glblFn133);
#else
  swapStore(term_loc(Rslt_26), pair_make(LAZ, 0, Rslt_26Args, glblFn133));
#endif

  // call .x at core: 319
  Term Rslt_27Args = pair_make(APP, 0, s_23, SUB);
  Term Rslt_27 = term_new(VAR, 0, port(2, term_loc(Rslt_27Args)));
  Rslt_27Args = pair_make(APP, 0, new_i60(319), Rslt_27Args);
  Rslt_27Args = pair_make(APP, 0, glblStr149, Rslt_27Args);
#ifdef STRICT
  store_redex(Rslt_27Args, glblProto14);
#else
  swapStore(term_loc(Rslt_27), pair_make(LAZ, 0, Rslt_27Args, glblProto14));
#endif
  // call str-vect at core: 319
  Term Rslt_28Args = pair_make(APP, 0, Rslt_27, SUB);
  Term Rslt_28 = term_new(VAR, 0, port(2, term_loc(Rslt_28Args)));
  Rslt_28Args = pair_make(APP, 0, new_i60(319), Rslt_28Args);
  Rslt_28Args = pair_make(APP, 0, glblStr149, Rslt_28Args);
#ifdef STRICT
  store_redex(Rslt_28Args, glblProto106);
#else
  swapStore(term_loc(Rslt_28), pair_make(LAZ, 0, Rslt_28Args, glblProto106));
#endif
  // call comp at core: 319
  Term Rslt_29Args = pair_make(APP, 0, Rslt_28, SUB);
  Term Rslt_29 = term_new(VAR, 0, port(2, term_loc(Rslt_29Args)));
  Rslt_29Args = pair_make(APP, 0, Rslt_26, Rslt_29Args);
  Rslt_29Args = pair_make(APP, 0, new_i60(319), Rslt_29Args);
  Rslt_29Args = pair_make(APP, 0, glblStr149, Rslt_29Args);
#ifdef STRICT
  store_redex(Rslt_29Args, glblProto34);
#else
  swapStore(term_loc(Rslt_29), pair_make(LAZ, 0, Rslt_29Args, glblProto34));
#endif
  // call vect-conj at core: 320
  Term Rslt_30Args = pair_make(APP, 0, glblStr197, SUB);
  Term Rslt_30 = term_new(VAR, 0, port(2, term_loc(Rslt_30Args)));
  Rslt_30Args = pair_make(APP, 0, Rslt_29, Rslt_30Args);
#ifdef STRICT
  store_redex(Rslt_30Args, glblFn133);
#else
  swapStore(term_loc(Rslt_30), pair_make(LAZ, 0, Rslt_30Args, glblFn133));
#endif

  // link args to body
  swapStore(r_seq_24, Rslt_30);

  Term Rslt_30V = get(term_loc(Rslt_30));
  apps = take(port(1, term_loc(Rslt_30V)));
  lams = take(port(2, term_loc(Rslt_30V)));

  vLoc = port(2, term_loc(apps));
  swapStore(term_loc(Rslt_30), pair_make(LAZ, 0, apps, lams));
  store_redex(args, seq_24);
  return;
}
Term glblstr_vect194 = new_ref(glblstr_vect193);
Term glbl_EQ_192;
void glbl_EQ_191(Term ref, Term args) {
  Term apps, lams, V;
  Location vLoc;
  Term seq_34 = pair_make(LAM, 0, SUB, NUL);
  Location r_seq_34 = port(2, term_loc(seq_34));
  Term y_33 = term_new(VAR, 0, port(1, term_loc(seq_34)));
  seq_34 = pair_make(LAM, 0, SUB, seq_34);
  Term x_32 = term_new(VAR, 0, port(1, term_loc(seq_34)));
  // allocate args at core: 321

  Term x_32_1;
  x_32 = dupeArg(x_32, &x_32_1, 0);

  Term y_33_1;
  y_33 = dupeArg(y_33, &y_33_1, 0);
  // call type-num at core: 322
  Term Rslt_36Args = pair_make(APP, 0, x_32, SUB);
  Term Rslt_36 = term_new(VAR, 0, port(2, term_loc(Rslt_36Args)));
#ifdef STRICT
  store_redex(Rslt_36Args, glblFn8);
#else
  swapStore(term_loc(Rslt_36), pair_make(LAZ, 0, Rslt_36Args, glblFn8));
#endif

  // call type-num at core: 322
  Term Rslt_37Args = pair_make(APP, 0, y_33, SUB);
  Term Rslt_37 = term_new(VAR, 0, port(2, term_loc(Rslt_37Args)));
#ifdef STRICT
  store_redex(Rslt_37Args, glblFn8);
#else
  swapStore(term_loc(Rslt_37), pair_make(LAZ, 0, Rslt_37Args, glblFn8));
#endif

  // call = at core: 322
  Term Rslt_38Args = pair_make(APP, 0, Rslt_37, SUB);
  Term Rslt_38 = term_new(VAR, 0, port(2, term_loc(Rslt_38Args)));
  Rslt_38Args = pair_make(APP, 0, Rslt_36, Rslt_38Args);
  Rslt_38Args = pair_make(APP, 0, new_i60(322), Rslt_38Args);
  Rslt_38Args = pair_make(APP, 0, glblStr149, Rslt_38Args);
#ifdef STRICT
  store_redex(Rslt_38Args, glblProto19);
#else
  swapStore(term_loc(Rslt_38), pair_make(LAZ, 0, Rslt_38Args, glblProto19));
#endif
  // call .x at core: 323
  Term Rslt_39Args = pair_make(APP, 0, x_32_1, SUB);
  Term Rslt_39 = term_new(VAR, 0, port(2, term_loc(Rslt_39Args)));
  Rslt_39Args = pair_make(APP, 0, new_i60(323), Rslt_39Args);
  Rslt_39Args = pair_make(APP, 0, glblStr149, Rslt_39Args);
#ifdef STRICT
  store_redex(Rslt_39Args, glblProto14);
#else
  swapStore(term_loc(Rslt_39), pair_make(LAZ, 0, Rslt_39Args, glblProto14));
#endif
  // call .x at core: 323
  Term Rslt_40Args = pair_make(APP, 0, y_33_1, SUB);
  Term Rslt_40 = term_new(VAR, 0, port(2, term_loc(Rslt_40Args)));
  Rslt_40Args = pair_make(APP, 0, new_i60(323), Rslt_40Args);
  Rslt_40Args = pair_make(APP, 0, glblStr149, Rslt_40Args);
#ifdef STRICT
  store_redex(Rslt_40Args, glblProto14);
#else
  swapStore(term_loc(Rslt_40), pair_make(LAZ, 0, Rslt_40Args, glblProto14));
#endif
  // call = at core: 323
  Term Rslt_41Args = pair_make(APP, 0, Rslt_40, SUB);
  Term Rslt_41 = term_new(VAR, 0, port(2, term_loc(Rslt_41Args)));
  Rslt_41Args = pair_make(APP, 0, Rslt_39, Rslt_41Args);
  Rslt_41Args = pair_make(APP, 0, new_i60(323), Rslt_41Args);
  Rslt_41Args = pair_make(APP, 0, glblStr149, Rslt_41Args);
#ifdef STRICT
  store_redex(Rslt_41Args, glblProto19);
#else
  swapStore(term_loc(Rslt_41), pair_make(LAZ, 0, Rslt_41Args, glblProto19));
#endif
  // call and at core: 322
  Term Rslt_42Args = pair_make(APP, 0, Rslt_41, SUB);
  Term Rslt_42 = term_new(VAR, 0, port(2, term_loc(Rslt_42Args)));
  Rslt_42Args = pair_make(APP, 0, Rslt_38, Rslt_42Args);
  Rslt_42Args = pair_make(APP, 0, new_i60(322), Rslt_42Args);
  Rslt_42Args = pair_make(APP, 0, glblStr149, Rslt_42Args);
#ifdef STRICT
  store_redex(Rslt_42Args, glblProto24);
#else
  swapStore(term_loc(Rslt_42), pair_make(LAZ, 0, Rslt_42Args, glblProto24));
#endif
  // link args to body
  swapStore(r_seq_34, Rslt_42);

  Term Rslt_42V = get(term_loc(Rslt_42));
  apps = take(port(1, term_loc(Rslt_42V)));
  lams = take(port(2, term_loc(Rslt_42V)));

  vLoc = port(2, term_loc(apps));
  swapStore(term_loc(Rslt_42), pair_make(LAZ, 0, apps, lams));
  store_redex(args, seq_34);
  return;
}
Term glbl_EQ_192 = new_ref(glbl_EQ_191);
Term glblextract190;
void glblextract189(Term ref, Term args) {
  Term apps, lams, V;
  Location vLoc;
  Term seq_45 = pair_make(LAM, 0, SUB, NUL);
  Location r_seq_45 = port(2, term_loc(seq_45));
  Term v_44 = term_new(VAR, 0, port(1, term_loc(seq_45)));
  // allocate args at core: 324
  // call .x at core: 325
  Term Rslt_47Args = pair_make(APP, 0, v_44, SUB);
  Term Rslt_47 = term_new(VAR, 0, port(2, term_loc(Rslt_47Args)));
  Rslt_47Args = pair_make(APP, 0, new_i60(325), Rslt_47Args);
  Rslt_47Args = pair_make(APP, 0, glblStr149, Rslt_47Args);
#ifdef STRICT
  store_redex(Rslt_47Args, glblProto14);
#else
  swapStore(term_loc(Rslt_47), pair_make(LAZ, 0, Rslt_47Args, glblProto14));
#endif
  // link args to body
  swapStore(r_seq_45, Rslt_47);

  Term Rslt_47V = get(term_loc(Rslt_47));
  apps = take(port(1, term_loc(Rslt_47V)));
  lams = take(port(2, term_loc(Rslt_47V)));

  vLoc = port(2, term_loc(apps));
  swapStore(term_loc(Rslt_47), pair_make(LAZ, 0, apps, lams));
  store_redex(args, seq_45);
  return;
}
Term glblextract190 = new_ref(glblextract189);
Term glblrecurse188;
void glblrecurse187(Term ref, Term args) {
  Term apps, lams, V;
  Location vLoc;
  Term seq_51 = pair_make(LAM, 0, SUB, NUL);
  Location r_seq_51 = port(2, term_loc(seq_51));
  Term f_50 = term_new(VAR, 0, port(1, term_loc(seq_51)));
  seq_51 = pair_make(LAM, 0, SUB, seq_51);
  Term v_49 = term_new(VAR, 0, port(1, term_loc(seq_51)));
  // allocate args at core: 326
  Term glblVal18_1;
  glblVal18 = dupeArg(glblVal18, &glblVal18_1, 0);

  // call .x at core: 327
  Term Rslt_53Args = pair_make(APP, 0, v_49, SUB);
  Term Rslt_53 = term_new(VAR, 0, port(2, term_loc(Rslt_53Args)));
  Rslt_53Args = pair_make(APP, 0, new_i60(327), Rslt_53Args);
  Rslt_53Args = pair_make(APP, 0, glblStr149, Rslt_53Args);
#ifdef STRICT
  store_redex(Rslt_53Args, glblProto14);
#else
  swapStore(term_loc(Rslt_53), pair_make(LAZ, 0, Rslt_53Args, glblProto14));
#endif
  // call f at core: 326
  Term Rslt_54Args = pair_make(APP, 0, Rslt_53, SUB);
  Term Rslt_54 = term_new(VAR, 0, port(2, term_loc(Rslt_54Args)));
#ifdef STRICT
  store_redex(Rslt_54Args, f_50);
#else
  swapStore(term_loc(Rslt_54), pair_make(LAZ, 0, Rslt_54Args, f_50));
#endif

  // call Some at core: 327
  Term Rslt_55Args = pair_make(APP, 0, Rslt_54, SUB);
  Term Rslt_55 = term_new(VAR, 0, port(2, term_loc(Rslt_55Args)));
#ifdef STRICT
  store_redex(Rslt_55Args, glblVal18_1);
#else
  swapStore(term_loc(Rslt_55), pair_make(LAZ, 0, Rslt_55Args, glblVal18_1));
#endif

  // link args to body
  swapStore(r_seq_51, Rslt_55);

  Term Rslt_55V = get(term_loc(Rslt_55));
  apps = take(port(1, term_loc(Rslt_55V)));
  lams = take(port(2, term_loc(Rslt_55V)));

  vLoc = port(2, term_loc(apps));
  swapStore(term_loc(Rslt_55), pair_make(LAZ, 0, apps, lams));
  store_redex(args, seq_51);
  return;
}
Term glblrecurse188 = new_ref(glblrecurse187);
Term glbleither186;
void glbleither185(Term ref, Term args) {
  Term apps, lams, V;
  Location vLoc;
  Term seq_59 = pair_make(LAM, 0, SUB, NUL);
  Location r_seq_59 = port(2, term_loc(seq_59));
  Term arg_58 = term_new(VAR, 0, port(1, term_loc(seq_59)));
  seq_59 = pair_make(LAM, 0, SUB, seq_59);
  Term v_57 = term_new(VAR, 0, port(1, term_loc(seq_59)));
  // allocate args at core: 328
  swapStore(term_loc(arg_58), ERA);
  // call .x at core: 329
  Term Rslt_61Args = pair_make(APP, 0, v_57, SUB);
  Term Rslt_61 = term_new(VAR, 0, port(2, term_loc(Rslt_61Args)));
  Rslt_61Args = pair_make(APP, 0, new_i60(329), Rslt_61Args);
  Rslt_61Args = pair_make(APP, 0, glblStr149, Rslt_61Args);
#ifdef STRICT
  store_redex(Rslt_61Args, glblProto14);
#else
  swapStore(term_loc(Rslt_61), pair_make(LAZ, 0, Rslt_61Args, glblProto14));
#endif
  // link args to body
  swapStore(r_seq_59, Rslt_61);

  Term Rslt_61V = get(term_loc(Rslt_61));
  apps = take(port(1, term_loc(Rslt_61V)));
  lams = take(port(2, term_loc(Rslt_61V)));

  vLoc = port(2, term_loc(apps));
  swapStore(term_loc(Rslt_61), pair_make(LAZ, 0, apps, lams));
  store_redex(args, seq_59);
  return;
}
Term glbleither186 = new_ref(glbleither185);
Term glblcond184;
void glblcond183(Term ref, Term args) {
  Term apps, lams, V;
  Location vLoc;
  Term seq_66 = pair_make(LAM, 0, SUB, NUL);
  Location r_seq_66 = port(2, term_loc(seq_66));
  Term arg_65 = term_new(VAR, 0, port(1, term_loc(seq_66)));
  seq_66 = pair_make(LAM, 0, SUB, seq_66);
  Term clause_64 = term_new(VAR, 0, port(1, term_loc(seq_66)));
  seq_66 = pair_make(LAM, 0, SUB, seq_66);
  Term v_63 = term_new(VAR, 0, port(1, term_loc(seq_66)));
  // allocate args at core: 330
  swapStore(term_loc(v_63), ERA);
  swapStore(term_loc(arg_65), ERA);
  // link args to body
  swapStore(r_seq_66, clause_64);

  V = get(r_seq_66);
  apps = pair_make(APP, 0, V, SUB);
  lams = pair_make(LAM, 0, SUB, NUL);
  swapStore(port(2, term_loc(lams)), term_new(VAR, 0, port(1, term_loc(lams))));
  vLoc = port(2, term_loc(apps));
  swapStore(vLoc, pair_make(LAZ, 0, apps, lams));
  swapStore(r_seq_66, term_new(VAR, 0, vLoc));
  store_redex(args, seq_66);
  return;
}
Term glblcond184 = new_ref(glblcond183);
Term glbland182;
void glbland181(Term ref, Term args) {
  Term apps, lams, V;
  Location vLoc;
  Term seq_71 = pair_make(LAM, 0, SUB, NUL);
  Location r_seq_71 = port(2, term_loc(seq_71));
  Term y_70 = term_new(VAR, 0, port(1, term_loc(seq_71)));
  seq_71 = pair_make(LAM, 0, SUB, seq_71);
  Term arg_69 = term_new(VAR, 0, port(1, term_loc(seq_71)));
  // allocate args at core: 332
  swapStore(term_loc(arg_69), ERA);
  // link args to body
  swapStore(r_seq_71, y_70);

  V = get(r_seq_71);
  apps = pair_make(APP, 0, V, SUB);
  lams = pair_make(LAM, 0, SUB, NUL);
  swapStore(port(2, term_loc(lams)), term_new(VAR, 0, port(1, term_loc(lams))));
  vLoc = port(2, term_loc(apps));
  swapStore(vLoc, pair_make(LAZ, 0, apps, lams));
  swapStore(r_seq_71, term_new(VAR, 0, vLoc));
  store_redex(args, seq_71);
  return;
}
Term glbland182 = new_ref(glbland181);
Term glblor180;
void glblor179(Term ref, Term args) {
  Term apps, lams, V;
  Location vLoc;
  Term seq_76 = pair_make(LAM, 0, SUB, NUL);
  Location r_seq_76 = port(2, term_loc(seq_76));
  Term arg_75 = term_new(VAR, 0, port(1, term_loc(seq_76)));
  seq_76 = pair_make(LAM, 0, SUB, seq_76);
  Term q_74 = term_new(VAR, 0, port(1, term_loc(seq_76)));
  // allocate args at core: 334
  swapStore(term_loc(arg_75), ERA);
  // link args to body
  swapStore(r_seq_76, q_74);

  V = get(r_seq_76);
  apps = pair_make(APP, 0, V, SUB);
  lams = pair_make(LAM, 0, SUB, NUL);
  swapStore(port(2, term_loc(lams)), term_new(VAR, 0, port(1, term_loc(lams))));
  vLoc = port(2, term_loc(apps));
  swapStore(vLoc, pair_make(LAZ, 0, apps, lams));
  swapStore(r_seq_76, term_new(VAR, 0, vLoc));
  store_redex(args, seq_76);
  return;
}
Term glblor180 = new_ref(glblor179);
Term glblmap178;
void glblmap177(Term ref, Term args) {
  Term apps, lams, V;
  Location vLoc;
  Term seq_81 = pair_make(LAM, 0, SUB, NUL);
  Location r_seq_81 = port(2, term_loc(seq_81));
  Term f_80 = term_new(VAR, 0, port(1, term_loc(seq_81)));
  seq_81 = pair_make(LAM, 0, SUB, seq_81);
  Term v_79 = term_new(VAR, 0, port(1, term_loc(seq_81)));
  // allocate args at core: 336
  Term glblVal18_2;
  glblVal18 = dupeArg(glblVal18, &glblVal18_2, 0);

  // call .x at core: 337
  Term Rslt_83Args = pair_make(APP, 0, v_79, SUB);
  Term Rslt_83 = term_new(VAR, 0, port(2, term_loc(Rslt_83Args)));
  Rslt_83Args = pair_make(APP, 0, new_i60(337), Rslt_83Args);
  Rslt_83Args = pair_make(APP, 0, glblStr149, Rslt_83Args);
#ifdef STRICT
  store_redex(Rslt_83Args, glblProto14);
#else
  swapStore(term_loc(Rslt_83), pair_make(LAZ, 0, Rslt_83Args, glblProto14));
#endif
  // call f at core: 336
  Term Rslt_84Args = pair_make(APP, 0, Rslt_83, SUB);
  Term Rslt_84 = term_new(VAR, 0, port(2, term_loc(Rslt_84Args)));
#ifdef STRICT
  store_redex(Rslt_84Args, f_80);
#else
  swapStore(term_loc(Rslt_84), pair_make(LAZ, 0, Rslt_84Args, f_80));
#endif

  // call Some at core: 337
  Term Rslt_85Args = pair_make(APP, 0, Rslt_84, SUB);
  Term Rslt_85 = term_new(VAR, 0, port(2, term_loc(Rslt_85Args)));
#ifdef STRICT
  store_redex(Rslt_85Args, glblVal18_2);
#else
  swapStore(term_loc(Rslt_85), pair_make(LAZ, 0, Rslt_85Args, glblVal18_2));
#endif

  // link args to body
  swapStore(r_seq_81, Rslt_85);

  Term Rslt_85V = get(term_loc(Rslt_85));
  apps = take(port(1, term_loc(Rslt_85V)));
  lams = take(port(2, term_loc(Rslt_85V)));

  vLoc = port(2, term_loc(apps));
  swapStore(term_loc(Rslt_85), pair_make(LAZ, 0, apps, lams));
  store_redex(args, seq_81);
  return;
}
Term glblmap178 = new_ref(glblmap177);
Term glblflat_map176;
void glblflat_map175(Term ref, Term args) {
  Term apps, lams, V;
  Location vLoc;
  Term seq_89 = pair_make(LAM, 0, SUB, NUL);
  Location r_seq_89 = port(2, term_loc(seq_89));
  Term f_88 = term_new(VAR, 0, port(1, term_loc(seq_89)));
  seq_89 = pair_make(LAM, 0, SUB, seq_89);
  Term v_87 = term_new(VAR, 0, port(1, term_loc(seq_89)));
  // allocate args at core: 338
  // call .x at core: 339
  Term Rslt_91Args = pair_make(APP, 0, v_87, SUB);
  Term Rslt_91 = term_new(VAR, 0, port(2, term_loc(Rslt_91Args)));
  Rslt_91Args = pair_make(APP, 0, new_i60(339), Rslt_91Args);
  Rslt_91Args = pair_make(APP, 0, glblStr149, Rslt_91Args);
#ifdef STRICT
  store_redex(Rslt_91Args, glblProto14);
#else
  swapStore(term_loc(Rslt_91), pair_make(LAZ, 0, Rslt_91Args, glblProto14));
#endif
  // call f at core: 338
  Term Rslt_92Args = pair_make(APP, 0, Rslt_91, SUB);
  Term Rslt_92 = term_new(VAR, 0, port(2, term_loc(Rslt_92Args)));
#ifdef STRICT
  store_redex(Rslt_92Args, f_88);
#else
  swapStore(term_loc(Rslt_92), pair_make(LAZ, 0, Rslt_92Args, f_88));
#endif

  // link args to body
  swapStore(r_seq_89, Rslt_92);

  Term Rslt_92V = get(term_loc(Rslt_92));
  apps = take(port(1, term_loc(Rslt_92V)));
  lams = take(port(2, term_loc(Rslt_92V)));

  vLoc = port(2, term_loc(apps));
  swapStore(term_loc(Rslt_92), pair_make(LAZ, 0, apps, lams));
  store_redex(args, seq_89);
  return;
}
Term glblflat_map176 = new_ref(glblflat_map175);
Term glblFn196;
void glblCFn195(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 2, &arityArgs);
  Term result = NUL;
  if (arityArgs.count == 2) {
    Term x_1 = arityArgs.args[0];
    Term y_2 = arityArgs.args[1];

    result = term_val(integer_EQ(x_1, y_2));
    moveStore(port(2, term_loc(args)), result);
  }
  return;
}
Term glblFn196 = new_ref(glblCFn195);
Term glblFn200;
void glblCFn199(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 2, &arityArgs);
  Term result = NUL;
  if (arityArgs.count == 2) {
    Term x_1 = arityArgs.args[0];
    Term y_2 = arityArgs.args[1];

    result = term_val(integer_LT(x_1, y_2));
    moveStore(port(2, term_loc(args)), result);
  }
  return;
}
Term glblFn200 = new_ref(glblCFn199);
Term glblFn202;
void glblCFn201(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  Term result = NUL;
  if (arityArgs.count == 1) {
    Term n_1 = arityArgs.args[0];

    if (term_tag(n_1) == VAL) {
      fprintf(stderr, "\ninvalid type for 'number-str': Value\n");
      abort();
    }
    result = number_str(n_1);

    moveStore(port(2, term_loc(args)), result);
  }
  return;
}
Term glblFn202 = new_ref(glblCFn201);
Term glblFn204;
void glblCFn203(Term ref, Term args) {
  Term apps, lams, V;
  Location vLoc;
  Term seq_3 = pair_make(LAM, 0, SUB, NUL);
  Location r_seq_3 = port(2, term_loc(seq_3));
  Term y_2 = term_new(VAR, 0, port(1, term_loc(seq_3)));
  seq_3 = pair_make(LAM, 0, SUB, seq_3);
  Term x_1 = term_new(VAR, 0, port(1, term_loc(seq_3)));
  // allocate args at core: 363

  Term x_1_1;
  x_1 = dupeArg(x_1, &x_1_1, 0);

  Term y_2_1;
  y_2 = dupeArg(y_2, &y_2_1, 0);
  // call int-< at core: 364
  Term Rslt_5Args = pair_make(APP, 0, y_2, SUB);
  Term Rslt_5 = term_new(VAR, 0, port(2, term_loc(Rslt_5Args)));
  Rslt_5Args = pair_make(APP, 0, x_1, Rslt_5Args);
#ifdef STRICT
  store_redex(Rslt_5Args, glblFn200);
#else
  swapStore(term_loc(Rslt_5), pair_make(LAZ, 0, Rslt_5Args, glblFn200));
#endif

  // call int-= at core: 365
  Term Rslt_6Args = pair_make(APP, 0, y_2_1, SUB);
  Term Rslt_6 = term_new(VAR, 0, port(2, term_loc(Rslt_6Args)));
  Rslt_6Args = pair_make(APP, 0, x_1_1, Rslt_6Args);
#ifdef STRICT
  store_redex(Rslt_6Args, glblFn196);
#else
  swapStore(term_loc(Rslt_6), pair_make(LAZ, 0, Rslt_6Args, glblFn196));
#endif

  // call or at core: 364
  Term Rslt_7Args = pair_make(APP, 0, Rslt_6, SUB);
  Term Rslt_7 = term_new(VAR, 0, port(2, term_loc(Rslt_7Args)));
  Rslt_7Args = pair_make(APP, 0, Rslt_5, Rslt_7Args);
  Rslt_7Args = pair_make(APP, 0, new_i60(364), Rslt_7Args);
  Rslt_7Args = pair_make(APP, 0, glblStr149, Rslt_7Args);
#ifdef STRICT
  store_redex(Rslt_7Args, glblProto26);
#else
  swapStore(term_loc(Rslt_7), pair_make(LAZ, 0, Rslt_7Args, glblProto26));
#endif
  // link args to body
  swapStore(r_seq_3, Rslt_7);

  Term Rslt_7V = get(term_loc(Rslt_7));
  apps = take(port(1, term_loc(Rslt_7V)));
  lams = take(port(2, term_loc(Rslt_7V)));

  vLoc = port(2, term_loc(apps));
  swapStore(term_loc(Rslt_7), pair_make(LAZ, 0, apps, lams));
  store_redex(args, seq_3);
  return;
}
Term glblFn204 = new_ref(glblCFn203);
struct {
  TYPE_SIZE type;
  REFS_SIZE refs;
  int64_t *hash;
  Value *parent;
  int64_t len;
  char buffer[8];
} str5 = {StringBufferType, REFS_STATIC, 0, 0, 7, "Integer"};
Term glblStr241 = term_new_(VAL, (Term)&str5);
Term glbltype_name240;
void glbltype_name238(Term ref, Term args) {
  Term apps, lams, V;
  Location vLoc;
  Term seq_10 = pair_make(LAM, 0, SUB, NUL);
  Location r_seq_10 = port(2, term_loc(seq_10));
  Term arg_9 = term_new(VAR, 0, port(1, term_loc(seq_10)));
  // allocate args at core: 368
  swapStore(term_loc(arg_9), ERA);
  // link args to body
  swapStore(r_seq_10, glblStr241);

  V = get(r_seq_10);
  apps = pair_make(APP, 0, V, SUB);
  lams = pair_make(LAM, 0, SUB, NUL);
  swapStore(port(2, term_loc(lams)), term_new(VAR, 0, port(1, term_loc(lams))));
  vLoc = port(2, term_loc(apps));
  swapStore(vLoc, pair_make(LAZ, 0, apps, lams));
  swapStore(r_seq_10, term_new(VAR, 0, vLoc));
  store_redex(args, seq_10);
  return;
}
Term glbltype_name240 = new_ref(glbltype_name238);
Term glblstr_vect236;
void glblstr_vect235(Term ref, Term args) {
  Term apps, lams, V;
  Location vLoc;
  Term seq_14 = pair_make(LAM, 0, SUB, NUL);
  Location r_seq_14 = port(2, term_loc(seq_14));
  Term n_13 = term_new(VAR, 0, port(1, term_loc(seq_14)));
  // allocate args at core: 371
  Term glblVal130_4;
  glblVal130 = dupeArg(glblVal130, &glblVal130_4, 0);

  // call number-str at core: 372
  Term Rslt_16Args = pair_make(APP, 0, n_13, SUB);
  Term Rslt_16 = term_new(VAR, 0, port(2, term_loc(Rslt_16Args)));
#ifdef STRICT
  store_redex(Rslt_16Args, glblFn202);
#else
  swapStore(term_loc(Rslt_16), pair_make(LAZ, 0, Rslt_16Args, glblFn202));
#endif

  // call vect-conj at core: 372
  Term Rslt_17Args = pair_make(APP, 0, Rslt_16, SUB);
  Term Rslt_17 = term_new(VAR, 0, port(2, term_loc(Rslt_17Args)));
  Rslt_17Args = pair_make(APP, 0, glblVal130_4, Rslt_17Args);
#ifdef STRICT
  store_redex(Rslt_17Args, glblFn133);
#else
  swapStore(term_loc(Rslt_17), pair_make(LAZ, 0, Rslt_17Args, glblFn133));
#endif

  // link args to body
  swapStore(r_seq_14, Rslt_17);

  Term Rslt_17V = get(term_loc(Rslt_17));
  apps = take(port(1, term_loc(Rslt_17V)));
  lams = take(port(2, term_loc(Rslt_17V)));

  vLoc = port(2, term_loc(apps));
  swapStore(term_loc(Rslt_17), pair_make(LAZ, 0, apps, lams));
  store_redex(args, seq_14);
  return;
}
Term glblstr_vect236 = new_ref(glblstr_vect235);
Term glbl_EQ_234;
void glbl_EQ_233(Term ref, Term args) {
  Term apps, lams, V;
  Location vLoc;
  Term seq_21 = pair_make(LAM, 0, SUB, NUL);
  Location r_seq_21 = port(2, term_loc(seq_21));
  Term y_20 = term_new(VAR, 0, port(1, term_loc(seq_21)));
  seq_21 = pair_make(LAM, 0, SUB, seq_21);
  Term x_19 = term_new(VAR, 0, port(1, term_loc(seq_21)));
  // allocate args at core: 374
  // call int-= at core: 375
  Term Rslt_23Args = pair_make(APP, 0, y_20, SUB);
  Term Rslt_23 = term_new(VAR, 0, port(2, term_loc(Rslt_23Args)));
  Rslt_23Args = pair_make(APP, 0, x_19, Rslt_23Args);
#ifdef STRICT
  store_redex(Rslt_23Args, glblFn196);
#else
  swapStore(term_loc(Rslt_23), pair_make(LAZ, 0, Rslt_23Args, glblFn196));
#endif

  // link args to body
  swapStore(r_seq_21, Rslt_23);

  Term Rslt_23V = get(term_loc(Rslt_23));
  apps = take(port(1, term_loc(Rslt_23V)));
  lams = take(port(2, term_loc(Rslt_23V)));

  vLoc = port(2, term_loc(apps));
  swapStore(term_loc(Rslt_23), pair_make(LAZ, 0, apps, lams));
  store_redex(args, seq_21);
  return;
}
Term glbl_EQ_234 = new_ref(glbl_EQ_233);
Term glbl_LT_232;
void glbl_LT_231(Term ref, Term args) {
  Term apps, lams, V;
  Location vLoc;
  Term seq_27 = pair_make(LAM, 0, SUB, NUL);
  Location r_seq_27 = port(2, term_loc(seq_27));
  Term y_26 = term_new(VAR, 0, port(1, term_loc(seq_27)));
  seq_27 = pair_make(LAM, 0, SUB, seq_27);
  Term x_25 = term_new(VAR, 0, port(1, term_loc(seq_27)));
  // allocate args at core: 377
  // call int-< at core: 378
  Term Rslt_29Args = pair_make(APP, 0, y_26, SUB);
  Term Rslt_29 = term_new(VAR, 0, port(2, term_loc(Rslt_29Args)));
  Rslt_29Args = pair_make(APP, 0, x_25, Rslt_29Args);
#ifdef STRICT
  store_redex(Rslt_29Args, glblFn200);
#else
  swapStore(term_loc(Rslt_29), pair_make(LAZ, 0, Rslt_29Args, glblFn200));
#endif

  // link args to body
  swapStore(r_seq_27, Rslt_29);

  Term Rslt_29V = get(term_loc(Rslt_29));
  apps = take(port(1, term_loc(Rslt_29V)));
  lams = take(port(2, term_loc(Rslt_29V)));

  vLoc = port(2, term_loc(apps));
  swapStore(term_loc(Rslt_29), pair_make(LAZ, 0, apps, lams));
  store_redex(args, seq_27);
  return;
}
Term glbl_LT_232 = new_ref(glbl_LT_231);
Term glblFn243;
void glblCFn242(Term ref, Term args) {
  Term apps, lams, V;
  Location vLoc;
  Term seq_3 = pair_make(LAM, 0, SUB, NUL);
  Location r_seq_3 = port(2, term_loc(seq_3));
  Term y_2 = term_new(VAR, 0, port(1, term_loc(seq_3)));
  seq_3 = pair_make(LAM, 0, SUB, seq_3);
  Term x_1 = term_new(VAR, 0, port(1, term_loc(seq_3)));
  // allocate args at core: 384

  Term y_2_1;
  y_2 = dupeArg(y_2, &y_2_1, 0);
  // call < at core: 388
  Term Rslt_5Args = pair_make(APP, 0, y_2, SUB);
  Term Rslt_5 = term_new(VAR, 0, port(2, term_loc(Rslt_5Args)));
  Rslt_5Args = pair_make(APP, 0, x_1, Rslt_5Args);
  Rslt_5Args = pair_make(APP, 0, new_i60(388), Rslt_5Args);
  Rslt_5Args = pair_make(APP, 0, glblStr149, Rslt_5Args);
#ifdef STRICT
  store_redex(Rslt_5Args, glblProto22);
#else
  swapStore(term_loc(Rslt_5), pair_make(LAZ, 0, Rslt_5Args, glblProto22));
#endif
  // call either at core: 388
  Term Rslt_6Args = pair_make(APP, 0, y_2_1, SUB);
  Term Rslt_6 = term_new(VAR, 0, port(2, term_loc(Rslt_6Args)));
  Rslt_6Args = pair_make(APP, 0, Rslt_5, Rslt_6Args);
  Rslt_6Args = pair_make(APP, 0, new_i60(388), Rslt_6Args);
  Rslt_6Args = pair_make(APP, 0, glblStr149, Rslt_6Args);
#ifdef STRICT
  store_redex(Rslt_6Args, glblProto28);
#else
  swapStore(term_loc(Rslt_6), pair_make(LAZ, 0, Rslt_6Args, glblProto28));
#endif
  // link args to body
  swapStore(r_seq_3, Rslt_6);

  Term Rslt_6V = get(term_loc(Rslt_6));
  apps = take(port(1, term_loc(Rslt_6V)));
  lams = take(port(2, term_loc(Rslt_6V)));

  vLoc = port(2, term_loc(apps));
  swapStore(term_loc(Rslt_6), pair_make(LAZ, 0, apps, lams));
  store_redex(args, seq_3);
  return;
}
Term glblFn243 = new_ref(glblCFn242);
Term glblFn245;
void glblCFn244(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  Term result = NUL;
  if (arityArgs.count == 1) {
    Term v_1 = arityArgs.args[0];

    Vector *v = (Vector *)((u64)v_1 & ~7);
    result = new_i60(v->count);
    dec_and_free((Term)v, 1);
    moveStore(port(2, term_loc(args)), result);
  }
  return;
}
Term glblFn245 = new_ref(glblCFn244);
Term glblFn251;
void glblCFn249(Term ref, Term args) {
  Term apps, lams, V;
  Location vLoc;
  Term seq_2 = pair_make(LAM, 0, SUB, NUL);
  Location r_seq_2 = port(2, term_loc(seq_2));
  Term x_1 = term_new(VAR, 0, port(1, term_loc(seq_2)));
  // allocate args at core: 398
  // link args to body
  swapStore(r_seq_2, x_1);

  V = get(r_seq_2);
  apps = pair_make(APP, 0, V, SUB);
  lams = pair_make(LAM, 0, SUB, NUL);
  swapStore(port(2, term_loc(lams)), term_new(VAR, 0, port(1, term_loc(lams))));
  vLoc = port(2, term_loc(apps));
  swapStore(vLoc, pair_make(LAZ, 0, apps, lams));
  swapStore(r_seq_2, term_new(VAR, 0, vLoc));
  store_redex(args, seq_2);
  return;
}
Term glblFn251 = new_ref(glblCFn249);
Term glblFn258;
void glblCFn257(Term ref, Term args) {
  Term apps, lams, V;
  Location vLoc;
  Term seq_3 = pair_make(LAM, 0, SUB, NUL);
  Location r_seq_3 = port(2, term_loc(seq_3));
  Term f_2 = term_new(VAR, 0, port(1, term_loc(seq_3)));
  seq_3 = pair_make(LAM, 0, SUB, seq_3);
  Term x_1 = term_new(VAR, 0, port(1, term_loc(seq_3)));
  // allocate args at core: 402

  Term f_2_1;
  f_2 = dupeArg(f_2, &f_2_1, 0);
  Term seq_7 = pair_make(LAM, 0, SUB, NUL);
  Location r_seq_7 = port(2, term_loc(seq_7));
  Term v_6 = term_new(VAR, 0, port(1, term_loc(seq_7)));
  // allocate args at core: 403
  // call fold at core: 402
  Term Rslt_9Args = pair_make(APP, 0, f_2_1, SUB);
  Term Rslt_9 = term_new(VAR, 0, port(2, term_loc(Rslt_9Args)));
  Rslt_9Args = pair_make(APP, 0, v_6, Rslt_9Args);
#ifdef STRICT
  store_redex(Rslt_9Args, glblFn258);
#else
  swapStore(term_loc(Rslt_9), pair_make(LAZ, 0, Rslt_9Args, glblFn258));
#endif

  // link args to body
  swapStore(r_seq_7, Rslt_9);

  Term Rslt_9V = get(term_loc(Rslt_9));
  apps = take(port(1, term_loc(Rslt_9V)));
  lams = take(port(2, term_loc(Rslt_9V)));

  vLoc = port(2, term_loc(apps));
  swapStore(term_loc(Rslt_9), pair_make(LAZ, 0, apps, lams));
  // call recurse at core: 403
  Term Rslt_10Args = pair_make(APP, 0, seq_7, SUB);
  Term Rslt_10 = term_new(VAR, 0, port(2, term_loc(Rslt_10Args)));
  Rslt_10Args = pair_make(APP, 0, x_1, Rslt_10Args);
  Rslt_10Args = pair_make(APP, 0, new_i60(403), Rslt_10Args);
  Rslt_10Args = pair_make(APP, 0, glblStr149, Rslt_10Args);
#ifdef STRICT
  store_redex(Rslt_10Args, glblProto36);
#else
  swapStore(term_loc(Rslt_10), pair_make(LAZ, 0, Rslt_10Args, glblProto36));
#endif
  // call f at core: 402
  Term Rslt_12Args = pair_make(APP, 0, Rslt_10, SUB);
  Term Rslt_12 = term_new(VAR, 0, port(2, term_loc(Rslt_12Args)));
#ifdef STRICT
  store_redex(Rslt_12Args, f_2);
#else
  swapStore(term_loc(Rslt_12), pair_make(LAZ, 0, Rslt_12Args, f_2));
#endif

  // link args to body
  swapStore(r_seq_3, Rslt_12);

  Term Rslt_12V = get(term_loc(Rslt_12));
  apps = take(port(1, term_loc(Rslt_12V)));
  lams = take(port(2, term_loc(Rslt_12V)));

  vLoc = port(2, term_loc(apps));
  swapStore(term_loc(Rslt_12), pair_make(LAZ, 0, apps, lams));
  store_redex(args, seq_3);
  return;
}
Term glblFn258 = new_ref(glblCFn257);
Term glblFn262;
void glblCFn261(Term ref, Term args) {
  Term apps, lams, V;
  Location vLoc;
  Term seq_3 = pair_make(LAM, 0, SUB, NUL);
  Location r_seq_3 = port(2, term_loc(seq_3));
  Term f_2 = term_new(VAR, 0, port(1, term_loc(seq_3)));
  seq_3 = pair_make(LAM, 0, SUB, seq_3);
  Term x_1 = term_new(VAR, 0, port(1, term_loc(seq_3)));
  // allocate args at core: 412

  Term f_2_1;
  f_2 = dupeArg(f_2, &f_2_1, 0);
  Term seq_8 = pair_make(LAM, 0, SUB, NUL);
  Location r_seq_8 = port(2, term_loc(seq_8));
  Term v_7 = term_new(VAR, 0, port(1, term_loc(seq_8)));
  // allocate args at core: 413
  // call f at core: 412
  Term Rslt_5Args = pair_make(APP, 0, x_1, SUB);
  Term Rslt_5 = term_new(VAR, 0, port(2, term_loc(Rslt_5Args)));
#ifdef STRICT
  store_redex(Rslt_5Args, f_2);
#else
  swapStore(term_loc(Rslt_5), pair_make(LAZ, 0, Rslt_5Args, f_2));
#endif

  // call unfold at core: 412
  Term Rslt_10Args = pair_make(APP, 0, f_2_1, SUB);
  Term Rslt_10 = term_new(VAR, 0, port(2, term_loc(Rslt_10Args)));
  Rslt_10Args = pair_make(APP, 0, v_7, Rslt_10Args);
#ifdef STRICT
  store_redex(Rslt_10Args, glblFn262);
#else
  swapStore(term_loc(Rslt_10), pair_make(LAZ, 0, Rslt_10Args, glblFn262));
#endif

  // link args to body
  swapStore(r_seq_8, Rslt_10);

  Term Rslt_10V = get(term_loc(Rslt_10));
  apps = take(port(1, term_loc(Rslt_10V)));
  lams = take(port(2, term_loc(Rslt_10V)));

  vLoc = port(2, term_loc(apps));
  swapStore(term_loc(Rslt_10), pair_make(LAZ, 0, apps, lams));
  // call recurse at core: 413
  Term Rslt_11Args = pair_make(APP, 0, seq_8, SUB);
  Term Rslt_11 = term_new(VAR, 0, port(2, term_loc(Rslt_11Args)));
  Rslt_11Args = pair_make(APP, 0, Rslt_5, Rslt_11Args);
  Rslt_11Args = pair_make(APP, 0, new_i60(413), Rslt_11Args);
  Rslt_11Args = pair_make(APP, 0, glblStr149, Rslt_11Args);
#ifdef STRICT
  store_redex(Rslt_11Args, glblProto36);
#else
  swapStore(term_loc(Rslt_11), pair_make(LAZ, 0, Rslt_11Args, glblProto36));
#endif
  // link args to body
  swapStore(r_seq_3, Rslt_11);

  Term Rslt_11V = get(term_loc(Rslt_11));
  apps = take(port(1, term_loc(Rslt_11V)));
  lams = take(port(2, term_loc(Rslt_11V)));

  vLoc = port(2, term_loc(apps));
  swapStore(term_loc(Rslt_11), pair_make(LAZ, 0, apps, lams));
  store_redex(args, seq_3);
  return;
}
Term glblFn262 = new_ref(glblCFn261);
void glblLeaf265(Term ref, Term args) {
  Term callArgs = pair_make(APP, 0, new_i60(0), args);
  callArgs = pair_make(APP, 0, new_i60(44), callArgs);
  interact(callArgs, construct);
  return;
}
Term glblVal267 = new_ref(glblLeaf265);
Term glblVal267;
Term glblRslt266;
Term glblFn264;
void glblCFn263(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  Term result = NUL;
  if (arityArgs.count == 1) {
    Term s_1 = arityArgs.args[0];

    result = strCount(s_1);
    dec_and_free(s_1, 1);
    moveStore(port(2, term_loc(args)), result);
  }
  return;
}
Term glblFn264 = new_ref(glblCFn263);
Term glblFn271;
void glblCFn270(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  Term result = NUL;
  if (arityArgs.count == 1) {
    Term len_1 = arityArgs.args[0];

    String *strVal = malloc_string(get_i60(len_1));
    strVal->len = 0;
    strVal->buffer[0] = 0;
    dec_and_free(len_1, 1);
    result = term_val((Term)strVal);

    moveStore(port(2, term_loc(args)), result);
  }
  return;
}
Term glblFn271 = new_ref(glblCFn270);
Term glblFn275;
void glblCFn274(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 2, &arityArgs);
  Term result = NUL;
  if (arityArgs.count == 2) {
    Term dest_1 = arityArgs.args[0];
    Term src_2 = arityArgs.args[1];

    String *s_1 = (String *)dest_1;
    String *s_2 = (String *)src_2;
    strncat(s_1->buffer, s_2->buffer, s_2->len);
    s_1->len += s_2->len;
    dec_and_free(src_2, 1);
    result = term_val(dest_1);
    moveStore(port(2, term_loc(args)), result);
  }
  return;
}
Term glblFn275 = new_ref(glblCFn274);
Term glblProto276;
Term glblFn280;
void glblCFn279(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 4, &arityArgs);
  Term result = NUL;
  if (arityArgs.count == 4) {
    Term s_1 = arityArgs.args[0];
    Term start_2 = arityArgs.args[1];
    Term len_3 = arityArgs.args[2];
    Term tgt_4 = arityArgs.args[3];
    result = strEQ(s_1, start_2, len_3, tgt_4);
    moveStore(port(2, term_loc(args)), result);
  }
  return;
}
Term glblFn280 = new_ref(glblCFn279);
Term glblProto286;
Term glblProto284;
Term glblProto282;
void glblSubString296(Term ref, Term args) {
  Term callArgs = pair_make(APP, 0, new_i60(3), args);
  callArgs = pair_make(APP, 0, new_i60(5), callArgs);
  interact(callArgs, construct);
  return;
}
Term glblVal300 = new_ref(glblSubString296);
Term glblVal300;
Term glblstr_vect289;
void glblstr_vect288(Term ref, Term args) {
  Term apps, lams, V;
  Location vLoc;
  Term seq_2 = pair_make(LAM, 0, SUB, NUL);
  Location r_seq_2 = port(2, term_loc(seq_2));
  Term s_1 = term_new(VAR, 0, port(1, term_loc(seq_2)));
  // allocate args at core: 465
  Term s_1Args = pair_make(APP, 0, s_1, SUB);
  Term Rslt_4 = term_new(VAR, 0, port(2, term_loc(s_1Args)));
  s_1Args = pair_make(APP, 0, new_i60(0), s_1Args);
  store_redex(s_1Args, accessField);
  Term glblVal130_5;
  glblVal130 = dupeArg(glblVal130, &glblVal130_5, 0);

  // call vect-conj at core: 465
  Term Rslt_5Args = pair_make(APP, 0, Rslt_4, SUB);
  Term Rslt_5 = term_new(VAR, 0, port(2, term_loc(Rslt_5Args)));
  Rslt_5Args = pair_make(APP, 0, glblVal130_5, Rslt_5Args);
#ifdef STRICT
  store_redex(Rslt_5Args, glblFn133);
#else
  swapStore(term_loc(Rslt_5), pair_make(LAZ, 0, Rslt_5Args, glblFn133));
#endif

  // link args to body
  swapStore(r_seq_2, Rslt_5);

  Term Rslt_5V = get(term_loc(Rslt_5));
  apps = take(port(1, term_loc(Rslt_5V)));
  lams = take(port(2, term_loc(Rslt_5V)));

  vLoc = port(2, term_loc(apps));
  swapStore(term_loc(Rslt_5), pair_make(LAZ, 0, apps, lams));
  store_redex(args, seq_2);
  return;
}
Term glblstr_vect289 = new_ref(glblstr_vect288);
Term glblcount291;
void glblcount290(Term ref, Term args) {
  Term apps, lams, V;
  Location vLoc;
  Term seq_8 = pair_make(LAM, 0, SUB, NUL);
  Location r_seq_8 = port(2, term_loc(seq_8));
  Term arg_7 = term_new(VAR, 0, port(1, term_loc(seq_8)));
  // allocate args at core: 466
  Term arg_7Args = pair_make(APP, 0, arg_7, SUB);
  Term Rslt_10 = term_new(VAR, 0, port(2, term_loc(arg_7Args)));
  arg_7Args = pair_make(APP, 0, new_i60(2), arg_7Args);
  store_redex(arg_7Args, accessField);
  // link args to body
  swapStore(r_seq_8, Rslt_10);

  V = get(r_seq_8);
  apps = pair_make(APP, 0, V, SUB);
  lams = pair_make(LAM, 0, SUB, NUL);
  swapStore(port(2, term_loc(lams)), term_new(VAR, 0, port(1, term_loc(lams))));
  vLoc = port(2, term_loc(apps));
  swapStore(vLoc, pair_make(LAZ, 0, apps, lams));
  swapStore(r_seq_8, term_new(VAR, 0, vLoc));
  store_redex(args, seq_8);
  return;
}
Term glblcount291 = new_ref(glblcount290);
Term glbl_EQ_293;
void glbl_EQ_292(Term ref, Term args) {
  Term apps, lams, V;
  Location vLoc;
  Term seq_14 = pair_make(LAM, 0, SUB, NUL);
  Location r_seq_14 = port(2, term_loc(seq_14));
  Term y_13 = term_new(VAR, 0, port(1, term_loc(seq_14)));
  seq_14 = pair_make(LAM, 0, SUB, seq_14);
  Term x_12 = term_new(VAR, 0, port(1, term_loc(seq_14)));
  // allocate args at core: 468

  Term x_12_2;
  x_12 = dupeArg(x_12, &x_12_2, 0);

  Term x_12_1;
  x_12 = dupeArg(x_12, &x_12_1, 0);
  Term x_12_2Args = pair_make(APP, 0, x_12_2, SUB);
  Term Rslt_18 = term_new(VAR, 0, port(2, term_loc(x_12_2Args)));
  x_12_2Args = pair_make(APP, 0, new_i60(2), x_12_2Args);
  store_redex(x_12_2Args, accessField);
  Term x_12_1Args = pair_make(APP, 0, x_12_1, SUB);
  Term Rslt_17 = term_new(VAR, 0, port(2, term_loc(x_12_1Args)));
  x_12_1Args = pair_make(APP, 0, new_i60(1), x_12_1Args);
  store_redex(x_12_1Args, accessField);
  Term x_12Args = pair_make(APP, 0, x_12, SUB);
  Term Rslt_16 = term_new(VAR, 0, port(2, term_loc(x_12Args)));
  x_12Args = pair_make(APP, 0, new_i60(0), x_12Args);
  store_redex(x_12Args, accessField);
  // call str= at core: 469
  Term Rslt_19Args = pair_make(APP, 0, y_13, SUB);
  Term Rslt_19 = term_new(VAR, 0, port(2, term_loc(Rslt_19Args)));
  Rslt_19Args = pair_make(APP, 0, Rslt_18, Rslt_19Args);
  Rslt_19Args = pair_make(APP, 0, Rslt_17, Rslt_19Args);
  Rslt_19Args = pair_make(APP, 0, Rslt_16, Rslt_19Args);
#ifdef STRICT
  store_redex(Rslt_19Args, glblFn280);
#else
  swapStore(term_loc(Rslt_19), pair_make(LAZ, 0, Rslt_19Args, glblFn280));
#endif

  // link args to body
  swapStore(r_seq_14, Rslt_19);

  Term Rslt_19V = get(term_loc(Rslt_19));
  apps = take(port(1, term_loc(Rslt_19V)));
  lams = take(port(2, term_loc(Rslt_19V)));

  vLoc = port(2, term_loc(apps));
  swapStore(term_loc(Rslt_19), pair_make(LAZ, 0, apps, lams));
  store_redex(args, seq_14);
  return;
}
Term glbl_EQ_293 = new_ref(glbl_EQ_292);
struct {
  TYPE_SIZE type;
  REFS_SIZE refs;
  int64_t *hash;
  Value *parent;
  int64_t len;
  char buffer[1];
} str6 = {StringBufferType, REFS_STATIC, 0, 0, 0, ""};
Term glblStr301 = term_new_(VAL, (Term)&str6);
Term glblsubs295;
void glblsubs294(Term ref, Term args) {
  Term apps, lams, V;
  Location vLoc;
  Term seq_24 = pair_make(LAM, 0, SUB, NUL);
  Location r_seq_24 = port(2, term_loc(seq_24));
  Term new_len_23 = term_new(VAR, 0, port(1, term_loc(seq_24)));
  seq_24 = pair_make(LAM, 0, SUB, seq_24);
  Term new_start_22 = term_new(VAR, 0, port(1, term_loc(seq_24)));
  seq_24 = pair_make(LAM, 0, SUB, seq_24);
  Term s_21 = term_new(VAR, 0, port(1, term_loc(seq_24)));
  // allocate args at core: 471

  Term s_21_4;
  s_21 = dupeArg(s_21, &s_21_4, 0);

  Term s_21_3;
  s_21 = dupeArg(s_21, &s_21_3, 0);

  Term s_21_2;
  s_21 = dupeArg(s_21, &s_21_2, 0);

  Term s_21_1;
  s_21 = dupeArg(s_21, &s_21_1, 0);
  Term s_21_4Args = pair_make(APP, 0, s_21_4, SUB);
  Term Rslt_39 = term_new(VAR, 0, port(2, term_loc(s_21_4Args)));
  s_21_4Args = pair_make(APP, 0, new_i60(0), s_21_4Args);
  store_redex(s_21_4Args, accessField);
  Term s_21_3Args = pair_make(APP, 0, s_21_3, SUB);
  Term Rslt_37 = term_new(VAR, 0, port(2, term_loc(s_21_3Args)));
  s_21_3Args = pair_make(APP, 0, new_i60(2), s_21_3Args);
  store_redex(s_21_3Args, accessField);
  Term s_21_2Args = pair_make(APP, 0, s_21_2, SUB);
  Term Rslt_36 = term_new(VAR, 0, port(2, term_loc(s_21_2Args)));
  s_21_2Args = pair_make(APP, 0, new_i60(1), s_21_2Args);
  store_redex(s_21_2Args, accessField);
  Term seq_28 = pair_make(LAM, 0, SUB, NUL);
  Location r_seq_28 = port(2, term_loc(seq_28));
  Term final_len_27 = term_new(VAR, 0, port(1, term_loc(seq_28)));
  // allocate args at core: 472

  Term final_len_27_1;
  final_len_27 = dupeArg(final_len_27, &final_len_27_1, 0);
  Term s_21_1Args = pair_make(APP, 0, s_21_1, SUB);
  Term Rslt_32 = term_new(VAR, 0, port(2, term_loc(s_21_1Args)));
  s_21_1Args = pair_make(APP, 0, new_i60(1), s_21_1Args);
  store_redex(s_21_1Args, accessField);
  Term s_21Args = pair_make(APP, 0, s_21, SUB);
  Term Rslt_31 = term_new(VAR, 0, port(2, term_loc(s_21Args)));
  s_21Args = pair_make(APP, 0, new_i60(0), s_21Args);
  store_redex(s_21Args, accessField);
  Term glblVal300_1;
  glblVal300 = dupeArg(glblVal300, &glblVal300_1, 0);

  // call <= at core: 473
  Term Rslt_30Args = pair_make(APP, 0, new_i60(0), SUB);
  Term Rslt_30 = term_new(VAR, 0, port(2, term_loc(Rslt_30Args)));
  Rslt_30Args = pair_make(APP, 0, final_len_27, Rslt_30Args);
#ifdef STRICT
  store_redex(Rslt_30Args, glblFn204);
#else
  swapStore(term_loc(Rslt_30), pair_make(LAZ, 0, Rslt_30Args, glblFn204));
#endif

  Term Rslt_33 = make_op(OP_ADD, Rslt_32, new_start_22);
  // call SubString at core: 475
  Term Rslt_34Args = pair_make(APP, 0, final_len_27_1, SUB);
  Term Rslt_34 = term_new(VAR, 0, port(2, term_loc(Rslt_34Args)));
  Rslt_34Args = pair_make(APP, 0, Rslt_33, Rslt_34Args);
  Rslt_34Args = pair_make(APP, 0, Rslt_31, Rslt_34Args);
#ifdef STRICT
  store_redex(Rslt_34Args, glblVal300_1);
#else
  swapStore(term_loc(Rslt_34), pair_make(LAZ, 0, Rslt_34Args, glblVal300_1));
#endif

  // call cond at core: 473
  Term Rslt_35Args = pair_make(APP, 0, Rslt_34, SUB);
  Term Rslt_35 = term_new(VAR, 0, port(2, term_loc(Rslt_35Args)));
  Rslt_35Args = pair_make(APP, 0, glblStr301, Rslt_35Args);
  Rslt_35Args = pair_make(APP, 0, Rslt_30, Rslt_35Args);
  Rslt_35Args = pair_make(APP, 0, new_i60(473), Rslt_35Args);
  Rslt_35Args = pair_make(APP, 0, glblStr149, Rslt_35Args);
#ifdef STRICT
  store_redex(Rslt_35Args, glblProto30);
#else
  swapStore(term_loc(Rslt_35), pair_make(LAZ, 0, Rslt_35Args, glblProto30));
#endif
  // link args to body
  swapStore(r_seq_28, Rslt_35);

  Term Rslt_35V = get(term_loc(Rslt_35));
  apps = take(port(1, term_loc(Rslt_35V)));
  lams = take(port(2, term_loc(Rslt_35V)));

  vLoc = port(2, term_loc(apps));
  swapStore(term_loc(Rslt_35), pair_make(LAZ, 0, apps, lams));
  Term Rslt_38 = make_op(OP_ADD, Rslt_36, Rslt_37);
  // call count at core: 472
  Term Rslt_40Args = pair_make(APP, 0, Rslt_39, SUB);
  Term Rslt_40 = term_new(VAR, 0, port(2, term_loc(Rslt_40Args)));
  Rslt_40Args = pair_make(APP, 0, new_i60(472), Rslt_40Args);
  Rslt_40Args = pair_make(APP, 0, glblStr149, Rslt_40Args);
#ifdef STRICT
  store_redex(Rslt_40Args, glblProto60);
#else
  swapStore(term_loc(Rslt_40), pair_make(LAZ, 0, Rslt_40Args, glblProto60));
#endif
  Term Rslt_41 = make_op(OP_SUB, Rslt_38, Rslt_40);
  // call min at core: 472
  Term Rslt_42Args = pair_make(APP, 0, Rslt_41, SUB);
  Term Rslt_42 = term_new(VAR, 0, port(2, term_loc(Rslt_42Args)));
  Rslt_42Args = pair_make(APP, 0, new_len_23, Rslt_42Args);
#ifdef STRICT
  store_redex(Rslt_42Args, glblFn243);
#else
  swapStore(term_loc(Rslt_42), pair_make(LAZ, 0, Rslt_42Args, glblFn243));
#endif

  // call  at core: 472
  Term Rslt_43Args = pair_make(APP, 0, Rslt_42, SUB);
  Term Rslt_43 = term_new(VAR, 0, port(2, term_loc(Rslt_43Args)));
#ifdef STRICT
  store_redex(Rslt_43Args, seq_28);
#else
  swapStore(term_loc(Rslt_43), pair_make(LAZ, 0, Rslt_43Args, seq_28));
#endif

  // link args to body
  swapStore(r_seq_24, Rslt_43);

  Term Rslt_43V = get(term_loc(Rslt_43));
  apps = take(port(1, term_loc(Rslt_43V)));
  lams = take(port(2, term_loc(Rslt_43V)));

  vLoc = port(2, term_loc(apps));
  swapStore(term_loc(Rslt_43), pair_make(LAZ, 0, apps, lams));
  store_redex(args, seq_24);
  return;
}
Term glblsubs295 = new_ref(glblsubs294);
Term glblFld297;
void glblFldFn308(Term ref, Term args) {
  args = pair_make(APP, 0, new_i60(0), args);
  store_redex(args, accessField);
  return;
}
Term glblFld297 = new_ref(glblFldFn308);
Term glblFld298;
void glblFldFn309(Term ref, Term args) {
  args = pair_make(APP, 0, new_i60(1), args);
  store_redex(args, accessField);
  return;
}
Term glblFld298 = new_ref(glblFldFn309);
Term glblFld299;
void glblFldFn310(Term ref, Term args) {
  args = pair_make(APP, 0, new_i60(2), args);
  store_redex(args, accessField);
  return;
}
Term glblFld299 = new_ref(glblFldFn310);
Term glblstr_vect318;
void glblstr_vect317(Term ref, Term args) {
  Term apps, lams, V;
  Location vLoc;
  Term seq_46 = pair_make(LAM, 0, SUB, NUL);
  Location r_seq_46 = port(2, term_loc(seq_46));
  Term s_45 = term_new(VAR, 0, port(1, term_loc(seq_46)));
  // allocate args at core: 478
  Term glblVal130_6;
  glblVal130 = dupeArg(glblVal130, &glblVal130_6, 0);

  // call vect-conj at core: 479
  Term Rslt_48Args = pair_make(APP, 0, s_45, SUB);
  Term Rslt_48 = term_new(VAR, 0, port(2, term_loc(Rslt_48Args)));
  Rslt_48Args = pair_make(APP, 0, glblVal130_6, Rslt_48Args);
#ifdef STRICT
  store_redex(Rslt_48Args, glblFn133);
#else
  swapStore(term_loc(Rslt_48), pair_make(LAZ, 0, Rslt_48Args, glblFn133));
#endif

  // link args to body
  swapStore(r_seq_46, Rslt_48);

  Term Rslt_48V = get(term_loc(Rslt_48));
  apps = take(port(1, term_loc(Rslt_48V)));
  lams = take(port(2, term_loc(Rslt_48V)));

  vLoc = port(2, term_loc(apps));
  swapStore(term_loc(Rslt_48), pair_make(LAZ, 0, apps, lams));
  store_redex(args, seq_46);
  return;
}
Term glblstr_vect318 = new_ref(glblstr_vect317);
Term glblcount316;
void glblcount315(Term ref, Term args) {
  Term apps, lams, V;
  Location vLoc;
  Term seq_51 = pair_make(LAM, 0, SUB, NUL);
  Location r_seq_51 = port(2, term_loc(seq_51));
  Term s_50 = term_new(VAR, 0, port(1, term_loc(seq_51)));
  // allocate args at core: 481
  // call str-count at core: 482
  Term Rslt_53Args = pair_make(APP, 0, s_50, SUB);
  Term Rslt_53 = term_new(VAR, 0, port(2, term_loc(Rslt_53Args)));
#ifdef STRICT
  store_redex(Rslt_53Args, glblFn264);
#else
  swapStore(term_loc(Rslt_53), pair_make(LAZ, 0, Rslt_53Args, glblFn264));
#endif

  // link args to body
  swapStore(r_seq_51, Rslt_53);

  Term Rslt_53V = get(term_loc(Rslt_53));
  apps = take(port(1, term_loc(Rslt_53V)));
  lams = take(port(2, term_loc(Rslt_53V)));

  vLoc = port(2, term_loc(apps));
  swapStore(term_loc(Rslt_53), pair_make(LAZ, 0, apps, lams));
  store_redex(args, seq_51);
  return;
}
Term glblcount316 = new_ref(glblcount315);
Term glbl_EQ_314;
void glbl_EQ_313(Term ref, Term args) {
  Term apps, lams, V;
  Location vLoc;
  Term seq_57 = pair_make(LAM, 0, SUB, NUL);
  Location r_seq_57 = port(2, term_loc(seq_57));
  Term y_56 = term_new(VAR, 0, port(1, term_loc(seq_57)));
  seq_57 = pair_make(LAM, 0, SUB, seq_57);
  Term x_55 = term_new(VAR, 0, port(1, term_loc(seq_57)));
  // allocate args at core: 484

  Term x_55_1;
  x_55 = dupeArg(x_55, &x_55_1, 0);
  // call str-count at core: 485
  Term Rslt_59Args = pair_make(APP, 0, x_55_1, SUB);
  Term Rslt_59 = term_new(VAR, 0, port(2, term_loc(Rslt_59Args)));
#ifdef STRICT
  store_redex(Rslt_59Args, glblFn264);
#else
  swapStore(term_loc(Rslt_59), pair_make(LAZ, 0, Rslt_59Args, glblFn264));
#endif

  // call str= at core: 485
  Term Rslt_60Args = pair_make(APP, 0, y_56, SUB);
  Term Rslt_60 = term_new(VAR, 0, port(2, term_loc(Rslt_60Args)));
  Rslt_60Args = pair_make(APP, 0, Rslt_59, Rslt_60Args);
  Rslt_60Args = pair_make(APP, 0, new_i60(0), Rslt_60Args);
  Rslt_60Args = pair_make(APP, 0, x_55, Rslt_60Args);
#ifdef STRICT
  store_redex(Rslt_60Args, glblFn280);
#else
  swapStore(term_loc(Rslt_60), pair_make(LAZ, 0, Rslt_60Args, glblFn280));
#endif

  // link args to body
  swapStore(r_seq_57, Rslt_60);

  Term Rslt_60V = get(term_loc(Rslt_60));
  apps = take(port(1, term_loc(Rslt_60V)));
  lams = take(port(2, term_loc(Rslt_60V)));

  vLoc = port(2, term_loc(apps));
  swapStore(term_loc(Rslt_60), pair_make(LAZ, 0, apps, lams));
  store_redex(args, seq_57);
  return;
}
Term glbl_EQ_314 = new_ref(glbl_EQ_313);
Term glblsubs312;
void glblsubs311(Term ref, Term args) {
  Term apps, lams, V;
  Location vLoc;
  Term seq_65 = pair_make(LAM, 0, SUB, NUL);
  Location r_seq_65 = port(2, term_loc(seq_65));
  Term len_64 = term_new(VAR, 0, port(1, term_loc(seq_65)));
  seq_65 = pair_make(LAM, 0, SUB, seq_65);
  Term start_63 = term_new(VAR, 0, port(1, term_loc(seq_65)));
  seq_65 = pair_make(LAM, 0, SUB, seq_65);
  Term s_62 = term_new(VAR, 0, port(1, term_loc(seq_65)));
  // allocate args at core: 487

  Term s_62_1;
  s_62 = dupeArg(s_62, &s_62_1, 0);

  Term start_63_1;
  start_63 = dupeArg(start_63, &start_63_1, 0);
  Term seq_69 = pair_make(LAM, 0, SUB, NUL);
  Location r_seq_69 = port(2, term_loc(seq_69));
  Term final_len_68 = term_new(VAR, 0, port(1, term_loc(seq_69)));
  // allocate args at core: 488

  Term final_len_68_1;
  final_len_68 = dupeArg(final_len_68, &final_len_68_1, 0);
  Term glblVal300_2;
  glblVal300 = dupeArg(glblVal300, &glblVal300_2, 0);

  // call <= at core: 489
  Term Rslt_71Args = pair_make(APP, 0, new_i60(0), SUB);
  Term Rslt_71 = term_new(VAR, 0, port(2, term_loc(Rslt_71Args)));
  Rslt_71Args = pair_make(APP, 0, final_len_68, Rslt_71Args);
#ifdef STRICT
  store_redex(Rslt_71Args, glblFn204);
#else
  swapStore(term_loc(Rslt_71), pair_make(LAZ, 0, Rslt_71Args, glblFn204));
#endif

  // call SubString at core: 491
  Term Rslt_72Args = pair_make(APP, 0, final_len_68_1, SUB);
  Term Rslt_72 = term_new(VAR, 0, port(2, term_loc(Rslt_72Args)));
  Rslt_72Args = pair_make(APP, 0, start_63, Rslt_72Args);
  Rslt_72Args = pair_make(APP, 0, s_62, Rslt_72Args);
#ifdef STRICT
  store_redex(Rslt_72Args, glblVal300_2);
#else
  swapStore(term_loc(Rslt_72), pair_make(LAZ, 0, Rslt_72Args, glblVal300_2));
#endif

  // call cond at core: 489
  Term Rslt_73Args = pair_make(APP, 0, Rslt_72, SUB);
  Term Rslt_73 = term_new(VAR, 0, port(2, term_loc(Rslt_73Args)));
  Rslt_73Args = pair_make(APP, 0, glblStr301, Rslt_73Args);
  Rslt_73Args = pair_make(APP, 0, Rslt_71, Rslt_73Args);
  Rslt_73Args = pair_make(APP, 0, new_i60(489), Rslt_73Args);
  Rslt_73Args = pair_make(APP, 0, glblStr149, Rslt_73Args);
#ifdef STRICT
  store_redex(Rslt_73Args, glblProto30);
#else
  swapStore(term_loc(Rslt_73), pair_make(LAZ, 0, Rslt_73Args, glblProto30));
#endif
  // link args to body
  swapStore(r_seq_69, Rslt_73);

  Term Rslt_73V = get(term_loc(Rslt_73));
  apps = take(port(1, term_loc(Rslt_73V)));
  lams = take(port(2, term_loc(Rslt_73V)));

  vLoc = port(2, term_loc(apps));
  swapStore(term_loc(Rslt_73), pair_make(LAZ, 0, apps, lams));
  // call count at core: 488
  Term Rslt_74Args = pair_make(APP, 0, s_62_1, SUB);
  Term Rslt_74 = term_new(VAR, 0, port(2, term_loc(Rslt_74Args)));
  Rslt_74Args = pair_make(APP, 0, new_i60(488), Rslt_74Args);
  Rslt_74Args = pair_make(APP, 0, glblStr149, Rslt_74Args);
#ifdef STRICT
  store_redex(Rslt_74Args, glblProto60);
#else
  swapStore(term_loc(Rslt_74), pair_make(LAZ, 0, Rslt_74Args, glblProto60));
#endif
  Term Rslt_75 = make_op(OP_SUB, Rslt_74, start_63_1);
  // call min at core: 488
  Term Rslt_76Args = pair_make(APP, 0, len_64, SUB);
  Term Rslt_76 = term_new(VAR, 0, port(2, term_loc(Rslt_76Args)));
  Rslt_76Args = pair_make(APP, 0, Rslt_75, Rslt_76Args);
#ifdef STRICT
  store_redex(Rslt_76Args, glblFn243);
#else
  swapStore(term_loc(Rslt_76), pair_make(LAZ, 0, Rslt_76Args, glblFn243));
#endif

  // call  at core: 488
  Term Rslt_77Args = pair_make(APP, 0, Rslt_76, SUB);
  Term Rslt_77 = term_new(VAR, 0, port(2, term_loc(Rslt_77Args)));
#ifdef STRICT
  store_redex(Rslt_77Args, seq_69);
#else
  swapStore(term_loc(Rslt_77), pair_make(LAZ, 0, Rslt_77Args, seq_69));
#endif

  // link args to body
  swapStore(r_seq_65, Rslt_77);

  Term Rslt_77V = get(term_loc(Rslt_77));
  apps = take(port(1, term_loc(Rslt_77V)));
  lams = take(port(2, term_loc(Rslt_77V)));

  vLoc = port(2, term_loc(apps));
  swapStore(term_loc(Rslt_77), pair_make(LAZ, 0, apps, lams));
  store_redex(args, seq_65);
  return;
}
Term glblsubs312 = new_ref(glblsubs311);
struct {
  TYPE_SIZE type;
  REFS_SIZE refs;
  int64_t *hash;
  Value *parent;
  int64_t len;
  char buffer[7];
} str7 = {StringBufferType, REFS_STATIC, 0, 0, 6, "String"};
Term glblStr338 = term_new_(VAL, (Term)&str7);
Term glbltype_name335;
void glbltype_name334(Term ref, Term args) {
  Term apps, lams, V;
  Location vLoc;
  Term seq_80 = pair_make(LAM, 0, SUB, NUL);
  Location r_seq_80 = port(2, term_loc(seq_80));
  Term arg_79 = term_new(VAR, 0, port(1, term_loc(seq_80)));
  // allocate args at core: 502
  swapStore(term_loc(arg_79), ERA);
  // link args to body
  swapStore(r_seq_80, glblStr338);

  V = get(r_seq_80);
  apps = pair_make(APP, 0, V, SUB);
  lams = pair_make(LAM, 0, SUB, NUL);
  swapStore(port(2, term_loc(lams)), term_new(VAR, 0, port(1, term_loc(lams))));
  vLoc = port(2, term_loc(apps));
  swapStore(vLoc, pair_make(LAZ, 0, apps, lams));
  swapStore(r_seq_80, term_new(VAR, 0, vLoc));
  store_redex(args, seq_80);
  return;
}
Term glbltype_name335 = new_ref(glbltype_name334);
Term glbltype_name337;
void glbltype_name336(Term ref, Term args) {
  Term apps, lams, V;
  Location vLoc;
  Term seq_84 = pair_make(LAM, 0, SUB, NUL);
  Location r_seq_84 = port(2, term_loc(seq_84));
  Term arg_83 = term_new(VAR, 0, port(1, term_loc(seq_84)));
  // allocate args at core: 502
  swapStore(term_loc(arg_83), ERA);
  // link args to body
  swapStore(r_seq_84, glblStr338);

  V = get(r_seq_84);
  apps = pair_make(APP, 0, V, SUB);
  lams = pair_make(LAM, 0, SUB, NUL);
  swapStore(port(2, term_loc(lams)), term_new(VAR, 0, port(1, term_loc(lams))));
  vLoc = port(2, term_loc(apps));
  swapStore(vLoc, pair_make(LAZ, 0, apps, lams));
  swapStore(r_seq_84, term_new(VAR, 0, vLoc));
  store_redex(args, seq_84);
  return;
}
Term glbltype_name337 = new_ref(glbltype_name336);
Term glblstr_vect331;
void glblstr_vect330(Term ref, Term args) {
  Term apps, lams, V;
  Location vLoc;
  Term seq_88 = pair_make(LAM, 0, SUB, NUL);
  Location r_seq_88 = port(2, term_loc(seq_88));
  Term s_87 = term_new(VAR, 0, port(1, term_loc(seq_88)));
  // allocate args at core: 505
  Term glblVal130_7;
  glblVal130 = dupeArg(glblVal130, &glblVal130_7, 0);

  // call vect-conj at core: 506
  Term Rslt_90Args = pair_make(APP, 0, s_87, SUB);
  Term Rslt_90 = term_new(VAR, 0, port(2, term_loc(Rslt_90Args)));
  Rslt_90Args = pair_make(APP, 0, glblVal130_7, Rslt_90Args);
#ifdef STRICT
  store_redex(Rslt_90Args, glblFn133);
#else
  swapStore(term_loc(Rslt_90), pair_make(LAZ, 0, Rslt_90Args, glblFn133));
#endif

  // link args to body
  swapStore(r_seq_88, Rslt_90);

  Term Rslt_90V = get(term_loc(Rslt_90));
  apps = take(port(1, term_loc(Rslt_90V)));
  lams = take(port(2, term_loc(Rslt_90V)));

  vLoc = port(2, term_loc(apps));
  swapStore(term_loc(Rslt_90), pair_make(LAZ, 0, apps, lams));
  store_redex(args, seq_88);
  return;
}
Term glblstr_vect331 = new_ref(glblstr_vect330);
Term glblstr_vect333;
void glblstr_vect332(Term ref, Term args) {
  Term apps, lams, V;
  Location vLoc;
  Term seq_93 = pair_make(LAM, 0, SUB, NUL);
  Location r_seq_93 = port(2, term_loc(seq_93));
  Term s_92 = term_new(VAR, 0, port(1, term_loc(seq_93)));
  // allocate args at core: 505
  Term glblVal130_8;
  glblVal130 = dupeArg(glblVal130, &glblVal130_8, 0);

  // call vect-conj at core: 506
  Term Rslt_95Args = pair_make(APP, 0, s_92, SUB);
  Term Rslt_95 = term_new(VAR, 0, port(2, term_loc(Rslt_95Args)));
  Rslt_95Args = pair_make(APP, 0, glblVal130_8, Rslt_95Args);
#ifdef STRICT
  store_redex(Rslt_95Args, glblFn133);
#else
  swapStore(term_loc(Rslt_95), pair_make(LAZ, 0, Rslt_95Args, glblFn133));
#endif

  // link args to body
  swapStore(r_seq_93, Rslt_95);

  Term Rslt_95V = get(term_loc(Rslt_95));
  apps = take(port(1, term_loc(Rslt_95V)));
  lams = take(port(2, term_loc(Rslt_95V)));

  vLoc = port(2, term_loc(apps));
  swapStore(term_loc(Rslt_95), pair_make(LAZ, 0, apps, lams));
  store_redex(args, seq_93);
  return;
}
Term glblstr_vect333 = new_ref(glblstr_vect332);
Term glblrecurse327;
void glblrecurse326(Term ref, Term args) {
  Term apps, lams, V;
  Location vLoc;
  Term seq_99 = pair_make(LAM, 0, SUB, NUL);
  Location r_seq_99 = port(2, term_loc(seq_99));
  Term f_98 = term_new(VAR, 0, port(1, term_loc(seq_99)));
  seq_99 = pair_make(LAM, 0, SUB, seq_99);
  Term s_97 = term_new(VAR, 0, port(1, term_loc(seq_99)));
  // allocate args at core: 508
  swapStore(term_loc(f_98), ERA);
  // link args to body
  swapStore(r_seq_99, s_97);

  V = get(r_seq_99);
  apps = pair_make(APP, 0, V, SUB);
  lams = pair_make(LAM, 0, SUB, NUL);
  swapStore(port(2, term_loc(lams)), term_new(VAR, 0, port(1, term_loc(lams))));
  vLoc = port(2, term_loc(apps));
  swapStore(vLoc, pair_make(LAZ, 0, apps, lams));
  swapStore(r_seq_99, term_new(VAR, 0, vLoc));
  store_redex(args, seq_99);
  return;
}
Term glblrecurse327 = new_ref(glblrecurse326);
Term glblrecurse329;
void glblrecurse328(Term ref, Term args) {
  Term apps, lams, V;
  Location vLoc;
  Term seq_104 = pair_make(LAM, 0, SUB, NUL);
  Location r_seq_104 = port(2, term_loc(seq_104));
  Term f_103 = term_new(VAR, 0, port(1, term_loc(seq_104)));
  seq_104 = pair_make(LAM, 0, SUB, seq_104);
  Term s_102 = term_new(VAR, 0, port(1, term_loc(seq_104)));
  // allocate args at core: 508
  swapStore(term_loc(f_103), ERA);
  // link args to body
  swapStore(r_seq_104, s_102);

  V = get(r_seq_104);
  apps = pair_make(APP, 0, V, SUB);
  lams = pair_make(LAM, 0, SUB, NUL);
  swapStore(port(2, term_loc(lams)), term_new(VAR, 0, port(1, term_loc(lams))));
  vLoc = port(2, term_loc(apps));
  swapStore(vLoc, pair_make(LAZ, 0, apps, lams));
  swapStore(r_seq_104, term_new(VAR, 0, vLoc));
  store_redex(args, seq_104);
  return;
}
Term glblrecurse329 = new_ref(glblrecurse328);
Term glblFn341;
void glblCFn340(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 2, &arityArgs);
  Term result = NUL;
  if (arityArgs.count == 2) {
    Term v_1 = arityArgs.args[0];
    Term n_2 = arityArgs.args[1];

    result = term_val((Term)vectorGet(v_1, n_2));
    moveStore(port(2, term_loc(args)), result);
  }
  return;
}
Term glblFn341 = new_ref(glblCFn340);
Term glblFn350;
void glblCFn349(Term ref, Term args) {
  Term apps, lams, V;
  Location vLoc;
  Term seq_4 = pair_make(LAM, 0, SUB, NUL);
  Location r_seq_4 = port(2, term_loc(seq_4));
  Term x_3 = term_new(VAR, 0, port(1, term_loc(seq_4)));
  seq_4 = pair_make(LAM, 0, SUB, seq_4);
  Term n_2 = term_new(VAR, 0, port(1, term_loc(seq_4)));
  seq_4 = pair_make(LAM, 0, SUB, seq_4);
  Term v_1 = term_new(VAR, 0, port(1, term_loc(seq_4)));
  // allocate args at core: 607

  Term v_1_1;
  v_1 = dupeArg(v_1, &v_1_1, 0);

  Term n_2_1;
  n_2 = dupeArg(n_2, &n_2_1, 0);

  Term x_3_2;
  x_3 = dupeArg(x_3, &x_3_2, 0);

  Term x_3_1;
  x_3 = dupeArg(x_3, &x_3_1, 0);
  // call = at core: 612
  Term Rslt_6Args = pair_make(APP, 0, x_3, SUB);
  Term Rslt_6 = term_new(VAR, 0, port(2, term_loc(Rslt_6Args)));
  Rslt_6Args = pair_make(APP, 0, n_2, Rslt_6Args);
  Rslt_6Args = pair_make(APP, 0, new_i60(612), Rslt_6Args);
  Rslt_6Args = pair_make(APP, 0, glblStr149, Rslt_6Args);
#ifdef STRICT
  store_redex(Rslt_6Args, glblProto19);
#else
  swapStore(term_loc(Rslt_6), pair_make(LAZ, 0, Rslt_6Args, glblProto19));
#endif
  // call conj at core: 614
  Term Rslt_7Args = pair_make(APP, 0, x_3_1, SUB);
  Term Rslt_7 = term_new(VAR, 0, port(2, term_loc(Rslt_7Args)));
  Rslt_7Args = pair_make(APP, 0, v_1_1, Rslt_7Args);
  Rslt_7Args = pair_make(APP, 0, new_i60(614), Rslt_7Args);
  Rslt_7Args = pair_make(APP, 0, glblStr149, Rslt_7Args);
#ifdef STRICT
  store_redex(Rslt_7Args, glblProto64);
#else
  swapStore(term_loc(Rslt_7), pair_make(LAZ, 0, Rslt_7Args, glblProto64));
#endif
  // call inc at core: 614
  Term Rslt_8Args = pair_make(APP, 0, x_3_2, SUB);
  Term Rslt_8 = term_new(VAR, 0, port(2, term_loc(Rslt_8Args)));
#ifdef STRICT
  store_redex(Rslt_8Args, glblFn120);
#else
  swapStore(term_loc(Rslt_8), pair_make(LAZ, 0, Rslt_8Args, glblFn120));
#endif

  // call range* at core: 607
  Term Rslt_9Args = pair_make(APP, 0, Rslt_8, SUB);
  Term Rslt_9 = term_new(VAR, 0, port(2, term_loc(Rslt_9Args)));
  Rslt_9Args = pair_make(APP, 0, n_2_1, Rslt_9Args);
  Rslt_9Args = pair_make(APP, 0, Rslt_7, Rslt_9Args);
#ifdef STRICT
  store_redex(Rslt_9Args, glblFn350);
#else
  swapStore(term_loc(Rslt_9), pair_make(LAZ, 0, Rslt_9Args, glblFn350));
#endif

  // call cond at core: 612
  Term Rslt_10Args = pair_make(APP, 0, Rslt_9, SUB);
  Term Rslt_10 = term_new(VAR, 0, port(2, term_loc(Rslt_10Args)));
  Rslt_10Args = pair_make(APP, 0, v_1, Rslt_10Args);
  Rslt_10Args = pair_make(APP, 0, Rslt_6, Rslt_10Args);
  Rslt_10Args = pair_make(APP, 0, new_i60(612), Rslt_10Args);
  Rslt_10Args = pair_make(APP, 0, glblStr149, Rslt_10Args);
#ifdef STRICT
  store_redex(Rslt_10Args, glblProto30);
#else
  swapStore(term_loc(Rslt_10), pair_make(LAZ, 0, Rslt_10Args, glblProto30));
#endif
  // link args to body
  swapStore(r_seq_4, Rslt_10);

  Term Rslt_10V = get(term_loc(Rslt_10));
  apps = take(port(1, term_loc(Rslt_10V)));
  lams = take(port(2, term_loc(Rslt_10V)));

  vLoc = port(2, term_loc(apps));
  swapStore(term_loc(Rslt_10), pair_make(LAZ, 0, apps, lams));
  store_redex(args, seq_4);
  return;
}
Term glblFn350 = new_ref(glblCFn349);
Term glblFn356;
void glblCFn355(Term ref, Term args) {
  Term apps, lams, V;
  Location vLoc;
  Term seq_2 = pair_make(LAM, 0, SUB, NUL);
  Location r_seq_2 = port(2, term_loc(seq_2));
  Term n_1 = term_new(VAR, 0, port(1, term_loc(seq_2)));
  // allocate args at core: 616
  Term glblVal130_9;
  glblVal130 = dupeArg(glblVal130, &glblVal130_9, 0);

  // call range* at core: 619
  Term Rslt_4Args = pair_make(APP, 0, new_i60(0), SUB);
  Term Rslt_4 = term_new(VAR, 0, port(2, term_loc(Rslt_4Args)));
  Rslt_4Args = pair_make(APP, 0, n_1, Rslt_4Args);
  Rslt_4Args = pair_make(APP, 0, glblVal130_9, Rslt_4Args);
#ifdef STRICT
  store_redex(Rslt_4Args, glblFn350);
#else
  swapStore(term_loc(Rslt_4), pair_make(LAZ, 0, Rslt_4Args, glblFn350));
#endif

  // link args to body
  swapStore(r_seq_2, Rslt_4);

  Term Rslt_4V = get(term_loc(Rslt_4));
  apps = take(port(1, term_loc(Rslt_4V)));
  lams = take(port(2, term_loc(Rslt_4V)));

  vLoc = port(2, term_loc(apps));
  swapStore(term_loc(Rslt_4), pair_make(LAZ, 0, apps, lams));
  store_redex(args, seq_2);
  return;
}
Term glblFn356 = new_ref(glblCFn355);
Term glblProto358;
Term glblvect_reduce361;
void glblvect_reduce360(Term ref, Term args) {
  Term apps, lams, V;
  Location vLoc;
  Term seq_13 = pair_make(LAM, 0, SUB, NUL);
  Location r_seq_13 = port(2, term_loc(seq_13));
  Term f_12 = term_new(VAR, 0, port(1, term_loc(seq_13)));
  seq_13 = pair_make(LAM, 0, SUB, seq_13);
  Term result_11 = term_new(VAR, 0, port(1, term_loc(seq_13)));
  seq_13 = pair_make(LAM, 0, SUB, seq_13);
  Term n_10 = term_new(VAR, 0, port(1, term_loc(seq_13)));
  seq_13 = pair_make(LAM, 0, SUB, seq_13);
  Term vect_9 = term_new(VAR, 0, port(1, term_loc(seq_13)));
  seq_13 = pair_make(LAM, 0, SUB, seq_13);
  Term el_8 = term_new(VAR, 0, port(1, term_loc(seq_13)));
  // allocate args at core: 630
  swapStore(term_loc(el_8), ERA);
  swapStore(term_loc(vect_9), ERA);
  swapStore(term_loc(n_10), ERA);
  swapStore(term_loc(f_12), ERA);
  // link args to body
  swapStore(r_seq_13, result_11);

  V = get(r_seq_13);
  apps = pair_make(APP, 0, V, SUB);
  lams = pair_make(LAM, 0, SUB, NUL);
  swapStore(port(2, term_loc(lams)), term_new(VAR, 0, port(1, term_loc(lams))));
  vLoc = port(2, term_loc(apps));
  swapStore(vLoc, pair_make(LAZ, 0, apps, lams));
  swapStore(r_seq_13, term_new(VAR, 0, vLoc));
  store_redex(args, seq_13);
  return;
}
Term glblvect_reduce361 = new_ref(glblvect_reduce360);
Term glblvect_reduce364;
void glblvect_reduce363(Term ref, Term args) {
  Term apps, lams, V;
  Location vLoc;
  Term seq_21 = pair_make(LAM, 0, SUB, NUL);
  Location r_seq_21 = port(2, term_loc(seq_21));
  Term f_20 = term_new(VAR, 0, port(1, term_loc(seq_21)));
  seq_21 = pair_make(LAM, 0, SUB, seq_21);
  Term result_19 = term_new(VAR, 0, port(1, term_loc(seq_21)));
  seq_21 = pair_make(LAM, 0, SUB, seq_21);
  Term n_18 = term_new(VAR, 0, port(1, term_loc(seq_21)));
  seq_21 = pair_make(LAM, 0, SUB, seq_21);
  Term vect_17 = term_new(VAR, 0, port(1, term_loc(seq_21)));
  seq_21 = pair_make(LAM, 0, SUB, seq_21);
  Term el_16 = term_new(VAR, 0, port(1, term_loc(seq_21)));
  // allocate args at core: 633

  Term vect_17_1;
  vect_17 = dupeArg(vect_17, &vect_17_1, 0);

  Term n_18_1;
  n_18 = dupeArg(n_18, &n_18_1, 0);

  Term f_20_1;
  f_20 = dupeArg(f_20, &f_20_1, 0);
  // call get at core: 634
  Term Rslt_23Args = pair_make(APP, 0, n_18, SUB);
  Term Rslt_23 = term_new(VAR, 0, port(2, term_loc(Rslt_23Args)));
  Rslt_23Args = pair_make(APP, 0, vect_17, Rslt_23Args);
  Rslt_23Args = pair_make(APP, 0, new_i60(634), Rslt_23Args);
  Rslt_23Args = pair_make(APP, 0, glblStr149, Rslt_23Args);
#ifdef STRICT
  store_redex(Rslt_23Args, glblProto100);
#else
  swapStore(term_loc(Rslt_23), pair_make(LAZ, 0, Rslt_23Args, glblProto100));
#endif
  // call inc at core: 634
  Term Rslt_24Args = pair_make(APP, 0, n_18_1, SUB);
  Term Rslt_24 = term_new(VAR, 0, port(2, term_loc(Rslt_24Args)));
#ifdef STRICT
  store_redex(Rslt_24Args, glblFn120);
#else
  swapStore(term_loc(Rslt_24), pair_make(LAZ, 0, Rslt_24Args, glblFn120));
#endif

  // call .x at core: 634
  Term Rslt_25Args = pair_make(APP, 0, el_16, SUB);
  Term Rslt_25 = term_new(VAR, 0, port(2, term_loc(Rslt_25Args)));
  Rslt_25Args = pair_make(APP, 0, new_i60(634), Rslt_25Args);
  Rslt_25Args = pair_make(APP, 0, glblStr149, Rslt_25Args);
#ifdef STRICT
  store_redex(Rslt_25Args, glblProto14);
#else
  swapStore(term_loc(Rslt_25), pair_make(LAZ, 0, Rslt_25Args, glblProto14));
#endif
  // call f at core: 633
  Term Rslt_26Args = pair_make(APP, 0, Rslt_25, SUB);
  Term Rslt_26 = term_new(VAR, 0, port(2, term_loc(Rslt_26Args)));
  Rslt_26Args = pair_make(APP, 0, result_19, Rslt_26Args);
#ifdef STRICT
  store_redex(Rslt_26Args, f_20);
#else
  swapStore(term_loc(Rslt_26), pair_make(LAZ, 0, Rslt_26Args, f_20));
#endif

  // call vect-reduce at core: 634
  Term Rslt_27Args = pair_make(APP, 0, f_20_1, SUB);
  Term Rslt_27 = term_new(VAR, 0, port(2, term_loc(Rslt_27Args)));
  Rslt_27Args = pair_make(APP, 0, Rslt_26, Rslt_27Args);
  Rslt_27Args = pair_make(APP, 0, Rslt_24, Rslt_27Args);
  Rslt_27Args = pair_make(APP, 0, vect_17_1, Rslt_27Args);
  Rslt_27Args = pair_make(APP, 0, Rslt_23, Rslt_27Args);
  Rslt_27Args = pair_make(APP, 0, new_i60(634), Rslt_27Args);
  Rslt_27Args = pair_make(APP, 0, glblStr149, Rslt_27Args);
#ifdef STRICT
  store_redex(Rslt_27Args, glblProto358);
#else
  swapStore(term_loc(Rslt_27), pair_make(LAZ, 0, Rslt_27Args, glblProto358));
#endif
  // link args to body
  swapStore(r_seq_21, Rslt_27);

  Term Rslt_27V = get(term_loc(Rslt_27));
  apps = take(port(1, term_loc(Rslt_27V)));
  lams = take(port(2, term_loc(Rslt_27V)));

  vLoc = port(2, term_loc(apps));
  swapStore(term_loc(Rslt_27), pair_make(LAZ, 0, apps, lams));
  store_redex(args, seq_21);
  return;
}
Term glblvect_reduce364 = new_ref(glblvect_reduce363);
Term glblProto365;
Term glblvect_reverse372;
void glblvect_reverse371(Term ref, Term args) {
  Term apps, lams, V;
  Location vLoc;
  Term seq_11 = pair_make(LAM, 0, SUB, NUL);
  Location r_seq_11 = port(2, term_loc(seq_11));
  Term result_10 = term_new(VAR, 0, port(1, term_loc(seq_11)));
  seq_11 = pair_make(LAM, 0, SUB, seq_11);
  Term n_9 = term_new(VAR, 0, port(1, term_loc(seq_11)));
  seq_11 = pair_make(LAM, 0, SUB, seq_11);
  Term vect_8 = term_new(VAR, 0, port(1, term_loc(seq_11)));
  seq_11 = pair_make(LAM, 0, SUB, seq_11);
  Term el_7 = term_new(VAR, 0, port(1, term_loc(seq_11)));
  // allocate args at core: 644
  swapStore(term_loc(el_7), ERA);
  swapStore(term_loc(vect_8), ERA);
  swapStore(term_loc(n_9), ERA);
  // link args to body
  swapStore(r_seq_11, result_10);

  V = get(r_seq_11);
  apps = pair_make(APP, 0, V, SUB);
  lams = pair_make(LAM, 0, SUB, NUL);
  swapStore(port(2, term_loc(lams)), term_new(VAR, 0, port(1, term_loc(lams))));
  vLoc = port(2, term_loc(apps));
  swapStore(vLoc, pair_make(LAZ, 0, apps, lams));
  swapStore(r_seq_11, term_new(VAR, 0, vLoc));
  store_redex(args, seq_11);
  return;
}
Term glblvect_reverse372 = new_ref(glblvect_reverse371);
Term glblvect_reverse375;
void glblvect_reverse374(Term ref, Term args) {
  Term apps, lams, V;
  Location vLoc;
  Term seq_18 = pair_make(LAM, 0, SUB, NUL);
  Location r_seq_18 = port(2, term_loc(seq_18));
  Term result_17 = term_new(VAR, 0, port(1, term_loc(seq_18)));
  seq_18 = pair_make(LAM, 0, SUB, seq_18);
  Term n_16 = term_new(VAR, 0, port(1, term_loc(seq_18)));
  seq_18 = pair_make(LAM, 0, SUB, seq_18);
  Term vect_15 = term_new(VAR, 0, port(1, term_loc(seq_18)));
  seq_18 = pair_make(LAM, 0, SUB, seq_18);
  Term el_14 = term_new(VAR, 0, port(1, term_loc(seq_18)));
  // allocate args at core: 647

  Term vect_15_1;
  vect_15 = dupeArg(vect_15, &vect_15_1, 0);

  Term n_16_2;
  n_16 = dupeArg(n_16, &n_16_2, 0);

  Term n_16_1;
  n_16 = dupeArg(n_16, &n_16_1, 0);
  // call <= at core: 648
  Term Rslt_20Args = pair_make(APP, 0, n_16, SUB);
  Term Rslt_20 = term_new(VAR, 0, port(2, term_loc(Rslt_20Args)));
  Rslt_20Args = pair_make(APP, 0, new_i60(0), Rslt_20Args);
#ifdef STRICT
  store_redex(Rslt_20Args, glblFn204);
#else
  swapStore(term_loc(Rslt_20), pair_make(LAZ, 0, Rslt_20Args, glblFn204));
#endif

  // call get at core: 648
  Term Rslt_21Args = pair_make(APP, 0, n_16_1, SUB);
  Term Rslt_21 = term_new(VAR, 0, port(2, term_loc(Rslt_21Args)));
  Rslt_21Args = pair_make(APP, 0, vect_15, Rslt_21Args);
  Rslt_21Args = pair_make(APP, 0, new_i60(648), Rslt_21Args);
  Rslt_21Args = pair_make(APP, 0, glblStr149, Rslt_21Args);
#ifdef STRICT
  store_redex(Rslt_21Args, glblProto100);
#else
  swapStore(term_loc(Rslt_21), pair_make(LAZ, 0, Rslt_21Args, glblProto100));
#endif
  // call and at core: 648
  Term Rslt_22Args = pair_make(APP, 0, Rslt_21, SUB);
  Term Rslt_22 = term_new(VAR, 0, port(2, term_loc(Rslt_22Args)));
  Rslt_22Args = pair_make(APP, 0, Rslt_20, Rslt_22Args);
  Rslt_22Args = pair_make(APP, 0, new_i60(648), Rslt_22Args);
  Rslt_22Args = pair_make(APP, 0, glblStr149, Rslt_22Args);
#ifdef STRICT
  store_redex(Rslt_22Args, glblProto24);
#else
  swapStore(term_loc(Rslt_22), pair_make(LAZ, 0, Rslt_22Args, glblProto24));
#endif
  // call dec at core: 649
  Term Rslt_23Args = pair_make(APP, 0, n_16_2, SUB);
  Term Rslt_23 = term_new(VAR, 0, port(2, term_loc(Rslt_23Args)));
#ifdef STRICT
  store_redex(Rslt_23Args, glblFn126);
#else
  swapStore(term_loc(Rslt_23), pair_make(LAZ, 0, Rslt_23Args, glblFn126));
#endif

  // call .x at core: 649
  Term Rslt_24Args = pair_make(APP, 0, el_14, SUB);
  Term Rslt_24 = term_new(VAR, 0, port(2, term_loc(Rslt_24Args)));
  Rslt_24Args = pair_make(APP, 0, new_i60(649), Rslt_24Args);
  Rslt_24Args = pair_make(APP, 0, glblStr149, Rslt_24Args);
#ifdef STRICT
  store_redex(Rslt_24Args, glblProto14);
#else
  swapStore(term_loc(Rslt_24), pair_make(LAZ, 0, Rslt_24Args, glblProto14));
#endif
  // call conj at core: 649
  Term Rslt_25Args = pair_make(APP, 0, Rslt_24, SUB);
  Term Rslt_25 = term_new(VAR, 0, port(2, term_loc(Rslt_25Args)));
  Rslt_25Args = pair_make(APP, 0, result_17, Rslt_25Args);
  Rslt_25Args = pair_make(APP, 0, new_i60(649), Rslt_25Args);
  Rslt_25Args = pair_make(APP, 0, glblStr149, Rslt_25Args);
#ifdef STRICT
  store_redex(Rslt_25Args, glblProto64);
#else
  swapStore(term_loc(Rslt_25), pair_make(LAZ, 0, Rslt_25Args, glblProto64));
#endif
  // call vect-reverse at core: 648
  Term Rslt_26Args = pair_make(APP, 0, Rslt_25, SUB);
  Term Rslt_26 = term_new(VAR, 0, port(2, term_loc(Rslt_26Args)));
  Rslt_26Args = pair_make(APP, 0, Rslt_23, Rslt_26Args);
  Rslt_26Args = pair_make(APP, 0, vect_15_1, Rslt_26Args);
  Rslt_26Args = pair_make(APP, 0, Rslt_22, Rslt_26Args);
  Rslt_26Args = pair_make(APP, 0, new_i60(648), Rslt_26Args);
  Rslt_26Args = pair_make(APP, 0, glblStr149, Rslt_26Args);
#ifdef STRICT
  store_redex(Rslt_26Args, glblProto365);
#else
  swapStore(term_loc(Rslt_26), pair_make(LAZ, 0, Rslt_26Args, glblProto365));
#endif
  // link args to body
  swapStore(r_seq_18, Rslt_26);

  Term Rslt_26V = get(term_loc(Rslt_26));
  apps = take(port(1, term_loc(Rslt_26V)));
  lams = take(port(2, term_loc(Rslt_26V)));

  vLoc = port(2, term_loc(apps));
  swapStore(term_loc(Rslt_26), pair_make(LAZ, 0, apps, lams));
  store_redex(args, seq_18);
  return;
}
Term glblvect_reverse375 = new_ref(glblvect_reverse374);
Term glblProto376;
Term glblvect_sub385;
void glblvect_sub384(Term ref, Term args) {
  Term apps, lams, V;
  Location vLoc;
  Term seq_13 = pair_make(LAM, 0, SUB, NUL);
  Location r_seq_13 = port(2, term_loc(seq_13));
  Term result_12 = term_new(VAR, 0, port(1, term_loc(seq_13)));
  seq_13 = pair_make(LAM, 0, SUB, seq_13);
  Term max_n_11 = term_new(VAR, 0, port(1, term_loc(seq_13)));
  seq_13 = pair_make(LAM, 0, SUB, seq_13);
  Term n_10 = term_new(VAR, 0, port(1, term_loc(seq_13)));
  seq_13 = pair_make(LAM, 0, SUB, seq_13);
  Term vect_9 = term_new(VAR, 0, port(1, term_loc(seq_13)));
  seq_13 = pair_make(LAM, 0, SUB, seq_13);
  Term el_8 = term_new(VAR, 0, port(1, term_loc(seq_13)));
  // allocate args at core: 658
  swapStore(term_loc(el_8), ERA);
  swapStore(term_loc(vect_9), ERA);
  swapStore(term_loc(n_10), ERA);
  swapStore(term_loc(max_n_11), ERA);
  // link args to body
  swapStore(r_seq_13, result_12);

  V = get(r_seq_13);
  apps = pair_make(APP, 0, V, SUB);
  lams = pair_make(LAM, 0, SUB, NUL);
  swapStore(port(2, term_loc(lams)), term_new(VAR, 0, port(1, term_loc(lams))));
  vLoc = port(2, term_loc(apps));
  swapStore(vLoc, pair_make(LAZ, 0, apps, lams));
  swapStore(r_seq_13, term_new(VAR, 0, vLoc));
  store_redex(args, seq_13);
  return;
}
Term glblvect_sub385 = new_ref(glblvect_sub384);
Term glblvect_sub388;
void glblvect_sub387(Term ref, Term args) {
  Term apps, lams, V;
  Location vLoc;
  Term seq_21 = pair_make(LAM, 0, SUB, NUL);
  Location r_seq_21 = port(2, term_loc(seq_21));
  Term result_20 = term_new(VAR, 0, port(1, term_loc(seq_21)));
  seq_21 = pair_make(LAM, 0, SUB, seq_21);
  Term max_n_19 = term_new(VAR, 0, port(1, term_loc(seq_21)));
  seq_21 = pair_make(LAM, 0, SUB, seq_21);
  Term n_18 = term_new(VAR, 0, port(1, term_loc(seq_21)));
  seq_21 = pair_make(LAM, 0, SUB, seq_21);
  Term vect_17 = term_new(VAR, 0, port(1, term_loc(seq_21)));
  seq_21 = pair_make(LAM, 0, SUB, seq_21);
  Term el_16 = term_new(VAR, 0, port(1, term_loc(seq_21)));
  // allocate args at core: 661

  Term vect_17_1;
  vect_17 = dupeArg(vect_17, &vect_17_1, 0);

  Term n_18_2;
  n_18 = dupeArg(n_18, &n_18_2, 0);

  Term n_18_1;
  n_18 = dupeArg(n_18, &n_18_1, 0);

  Term max_n_19_1;
  max_n_19 = dupeArg(max_n_19, &max_n_19_1, 0);
  // call < at core: 662
  Term Rslt_23Args = pair_make(APP, 0, max_n_19, SUB);
  Term Rslt_23 = term_new(VAR, 0, port(2, term_loc(Rslt_23Args)));
  Rslt_23Args = pair_make(APP, 0, n_18, Rslt_23Args);
  Rslt_23Args = pair_make(APP, 0, new_i60(662), Rslt_23Args);
  Rslt_23Args = pair_make(APP, 0, glblStr149, Rslt_23Args);
#ifdef STRICT
  store_redex(Rslt_23Args, glblProto22);
#else
  swapStore(term_loc(Rslt_23), pair_make(LAZ, 0, Rslt_23Args, glblProto22));
#endif
  // call get at core: 662
  Term Rslt_24Args = pair_make(APP, 0, n_18_1, SUB);
  Term Rslt_24 = term_new(VAR, 0, port(2, term_loc(Rslt_24Args)));
  Rslt_24Args = pair_make(APP, 0, vect_17, Rslt_24Args);
  Rslt_24Args = pair_make(APP, 0, new_i60(662), Rslt_24Args);
  Rslt_24Args = pair_make(APP, 0, glblStr149, Rslt_24Args);
#ifdef STRICT
  store_redex(Rslt_24Args, glblProto100);
#else
  swapStore(term_loc(Rslt_24), pair_make(LAZ, 0, Rslt_24Args, glblProto100));
#endif
  // call and at core: 662
  Term Rslt_25Args = pair_make(APP, 0, Rslt_24, SUB);
  Term Rslt_25 = term_new(VAR, 0, port(2, term_loc(Rslt_25Args)));
  Rslt_25Args = pair_make(APP, 0, Rslt_23, Rslt_25Args);
  Rslt_25Args = pair_make(APP, 0, new_i60(662), Rslt_25Args);
  Rslt_25Args = pair_make(APP, 0, glblStr149, Rslt_25Args);
#ifdef STRICT
  store_redex(Rslt_25Args, glblProto24);
#else
  swapStore(term_loc(Rslt_25), pair_make(LAZ, 0, Rslt_25Args, glblProto24));
#endif
  // call inc at core: 663
  Term Rslt_26Args = pair_make(APP, 0, n_18_2, SUB);
  Term Rslt_26 = term_new(VAR, 0, port(2, term_loc(Rslt_26Args)));
#ifdef STRICT
  store_redex(Rslt_26Args, glblFn120);
#else
  swapStore(term_loc(Rslt_26), pair_make(LAZ, 0, Rslt_26Args, glblFn120));
#endif

  // call .x at core: 663
  Term Rslt_27Args = pair_make(APP, 0, el_16, SUB);
  Term Rslt_27 = term_new(VAR, 0, port(2, term_loc(Rslt_27Args)));
  Rslt_27Args = pair_make(APP, 0, new_i60(663), Rslt_27Args);
  Rslt_27Args = pair_make(APP, 0, glblStr149, Rslt_27Args);
#ifdef STRICT
  store_redex(Rslt_27Args, glblProto14);
#else
  swapStore(term_loc(Rslt_27), pair_make(LAZ, 0, Rslt_27Args, glblProto14));
#endif
  // call conj at core: 663
  Term Rslt_28Args = pair_make(APP, 0, Rslt_27, SUB);
  Term Rslt_28 = term_new(VAR, 0, port(2, term_loc(Rslt_28Args)));
  Rslt_28Args = pair_make(APP, 0, result_20, Rslt_28Args);
  Rslt_28Args = pair_make(APP, 0, new_i60(663), Rslt_28Args);
  Rslt_28Args = pair_make(APP, 0, glblStr149, Rslt_28Args);
#ifdef STRICT
  store_redex(Rslt_28Args, glblProto64);
#else
  swapStore(term_loc(Rslt_28), pair_make(LAZ, 0, Rslt_28Args, glblProto64));
#endif
  // call vect-sub at core: 662
  Term Rslt_29Args = pair_make(APP, 0, Rslt_28, SUB);
  Term Rslt_29 = term_new(VAR, 0, port(2, term_loc(Rslt_29Args)));
  Rslt_29Args = pair_make(APP, 0, max_n_19_1, Rslt_29Args);
  Rslt_29Args = pair_make(APP, 0, Rslt_26, Rslt_29Args);
  Rslt_29Args = pair_make(APP, 0, vect_17_1, Rslt_29Args);
  Rslt_29Args = pair_make(APP, 0, Rslt_25, Rslt_29Args);
  Rslt_29Args = pair_make(APP, 0, new_i60(662), Rslt_29Args);
  Rslt_29Args = pair_make(APP, 0, glblStr149, Rslt_29Args);
#ifdef STRICT
  store_redex(Rslt_29Args, glblProto376);
#else
  swapStore(term_loc(Rslt_29), pair_make(LAZ, 0, Rslt_29Args, glblProto376));
#endif
  // link args to body
  swapStore(r_seq_21, Rslt_29);

  Term Rslt_29V = get(term_loc(Rslt_29));
  apps = take(port(1, term_loc(Rslt_29V)));
  lams = take(port(2, term_loc(Rslt_29V)));

  vLoc = port(2, term_loc(apps));
  swapStore(term_loc(Rslt_29), pair_make(LAZ, 0, apps, lams));
  store_redex(args, seq_21);
  return;
}
Term glblvect_sub388 = new_ref(glblvect_sub387);
Term glblFn397;
void glblCFn396(Term ref, Term args) {
  Term apps, lams, V;
  Location vLoc;
  Term seq_4 = pair_make(LAM, 0, SUB, NUL);
  Location r_seq_4 = port(2, term_loc(seq_4));
  Term len_3 = term_new(VAR, 0, port(1, term_loc(seq_4)));
  seq_4 = pair_make(LAM, 0, SUB, seq_4);
  Term start_2 = term_new(VAR, 0, port(1, term_loc(seq_4)));
  seq_4 = pair_make(LAM, 0, SUB, seq_4);
  Term v_1 = term_new(VAR, 0, port(1, term_loc(seq_4)));
  // allocate args at core: 665

  Term v_1_1;
  v_1 = dupeArg(v_1, &v_1_1, 0);

  Term start_2_2;
  start_2 = dupeArg(start_2, &start_2_2, 0);

  Term start_2_1;
  start_2 = dupeArg(start_2, &start_2_1, 0);
  Term glblVal130_10;
  glblVal130 = dupeArg(glblVal130, &glblVal130_10, 0);

  // call vect-get at core: 667
  Term Rslt_6Args = pair_make(APP, 0, start_2, SUB);
  Term Rslt_6 = term_new(VAR, 0, port(2, term_loc(Rslt_6Args)));
  Rslt_6Args = pair_make(APP, 0, v_1, Rslt_6Args);
#ifdef STRICT
  store_redex(Rslt_6Args, glblFn341);
#else
  swapStore(term_loc(Rslt_6), pair_make(LAZ, 0, Rslt_6Args, glblFn341));
#endif

  // call inc at core: 667
  Term Rslt_7Args = pair_make(APP, 0, start_2_1, SUB);
  Term Rslt_7 = term_new(VAR, 0, port(2, term_loc(Rslt_7Args)));
#ifdef STRICT
  store_redex(Rslt_7Args, glblFn120);
#else
  swapStore(term_loc(Rslt_7), pair_make(LAZ, 0, Rslt_7Args, glblFn120));
#endif

  Term Rslt_8 = make_op(OP_ADD, start_2_2, len_3);
  // call vect-sub at core: 667
  Term Rslt_9Args = pair_make(APP, 0, glblVal130_10, SUB);
  Term Rslt_9 = term_new(VAR, 0, port(2, term_loc(Rslt_9Args)));
  Rslt_9Args = pair_make(APP, 0, Rslt_8, Rslt_9Args);
  Rslt_9Args = pair_make(APP, 0, Rslt_7, Rslt_9Args);
  Rslt_9Args = pair_make(APP, 0, v_1_1, Rslt_9Args);
  Rslt_9Args = pair_make(APP, 0, Rslt_6, Rslt_9Args);
  Rslt_9Args = pair_make(APP, 0, new_i60(667), Rslt_9Args);
  Rslt_9Args = pair_make(APP, 0, glblStr149, Rslt_9Args);
#ifdef STRICT
  store_redex(Rslt_9Args, glblProto376);
#else
  swapStore(term_loc(Rslt_9), pair_make(LAZ, 0, Rslt_9Args, glblProto376));
#endif
  // link args to body
  swapStore(r_seq_4, Rslt_9);

  Term Rslt_9V = get(term_loc(Rslt_9));
  apps = take(port(1, term_loc(Rslt_9V)));
  lams = take(port(2, term_loc(Rslt_9V)));

  vLoc = port(2, term_loc(apps));
  swapStore(term_loc(Rslt_9), pair_make(LAZ, 0, apps, lams));
  store_redex(args, seq_4);
  return;
}
Term glblFn397 = new_ref(glblCFn396);
Term glblVal398 = new_ref(vectMap);
Term glblVal398;
Term glblc404;
void glblc403(Term ref, Term args) {
  Term apps, lams, V;
  Location vLoc;
  Term seq_19 = pair_make(LAM, 0, SUB, NUL);
  Location r_seq_19 = port(2, term_loc(seq_19));
  Term s_18 = term_new(VAR, 0, port(1, term_loc(seq_19)));
  seq_19 = pair_make(LAM, 0, SUB, seq_19);
  Term len_17 = term_new(VAR, 0, port(1, term_loc(seq_19)));
  // allocate args at core: 674
  // call count at core: 675
  Term Rslt_21Args = pair_make(APP, 0, s_18, SUB);
  Term Rslt_21 = term_new(VAR, 0, port(2, term_loc(Rslt_21Args)));
  Rslt_21Args = pair_make(APP, 0, new_i60(675), Rslt_21Args);
  Rslt_21Args = pair_make(APP, 0, glblStr149, Rslt_21Args);
#ifdef STRICT
  store_redex(Rslt_21Args, glblProto60);
#else
  swapStore(term_loc(Rslt_21), pair_make(LAZ, 0, Rslt_21Args, glblProto60));
#endif
  Term Rslt_22 = make_op(OP_ADD, len_17, Rslt_21);
  // link args to body
  swapStore(r_seq_19, Rslt_22);

  Term Rslt_22V = get(term_loc(Rslt_22));
  apps = take(port(1, term_loc(Rslt_22V)));
  lams = take(port(2, term_loc(Rslt_22V)));

  vLoc = port(2, term_loc(apps));
  swapStore(term_loc(Rslt_22), pair_make(LAZ, 0, apps, lams));
  store_redex(args, seq_19);
  return;
}
Term glblc404 = new_ref(glblc403);
void glblProto106_412(Term ref, Term args) {
  args = pair_make(APP, 0, new_i60(672), args);
  args = pair_make(APP, 0, glblStr149, args);
  interact(args, glblProto106);
}
Term glblFn402;
void glblCFn401(Term ref, Term args) {
  Term apps, lams, V;
  Location vLoc;
  Term seq_2 = pair_make(LAM, 0, SUB, NUL);
  Location r_seq_2 = port(2, term_loc(seq_2));
  Term vect_1 = term_new(VAR, 0, port(1, term_loc(seq_2)));
  // allocate args at core: 671
  Term seq_6 = pair_make(LAM, 0, SUB, NUL);
  Location r_seq_6 = port(2, term_loc(seq_6));
  Term ss_vect_5 = term_new(VAR, 0, port(1, term_loc(seq_6)));
  // allocate args at core: 672

  Term ss_vect_5_3;
  ss_vect_5 = dupeArg(ss_vect_5, &ss_vect_5_3, 0);

  Term ss_vect_5_2;
  ss_vect_5 = dupeArg(ss_vect_5, &ss_vect_5_2, 0);

  Term ss_vect_5_1;
  ss_vect_5 = dupeArg(ss_vect_5, &ss_vect_5_1, 0);
  Term seq_10 = pair_make(LAM, 0, SUB, NUL);
  Location r_seq_10 = port(2, term_loc(seq_10));
  Term new_len_9 = term_new(VAR, 0, port(1, term_loc(seq_10)));
  // allocate args at core: 673
  // call first at core: 676
  Term Rslt_12Args = pair_make(APP, 0, ss_vect_5, SUB);
  Term Rslt_12 = term_new(VAR, 0, port(2, term_loc(Rslt_12Args)));
  Rslt_12Args = pair_make(APP, 0, new_i60(676), Rslt_12Args);
  Rslt_12Args = pair_make(APP, 0, glblStr149, Rslt_12Args);
#ifdef STRICT
  store_redex(Rslt_12Args, glblProto72);
#else
  swapStore(term_loc(Rslt_12), pair_make(LAZ, 0, Rslt_12Args, glblProto72));
#endif
  // call str-malloc at core: 676
  Term Rslt_13Args = pair_make(APP, 0, new_len_9, SUB);
  Term Rslt_13 = term_new(VAR, 0, port(2, term_loc(Rslt_13Args)));
#ifdef STRICT
  store_redex(Rslt_13Args, glblFn271);
#else
  swapStore(term_loc(Rslt_13), pair_make(LAZ, 0, Rslt_13Args, glblFn271));
#endif

  // call vect-reduce at core: 676
  Term Rslt_14Args = pair_make(APP, 0, glblFn275, SUB);
  Term Rslt_14 = term_new(VAR, 0, port(2, term_loc(Rslt_14Args)));
  Rslt_14Args = pair_make(APP, 0, Rslt_13, Rslt_14Args);
  Rslt_14Args = pair_make(APP, 0, new_i60(1), Rslt_14Args);
  Rslt_14Args = pair_make(APP, 0, ss_vect_5_1, Rslt_14Args);
  Rslt_14Args = pair_make(APP, 0, Rslt_12, Rslt_14Args);
  Rslt_14Args = pair_make(APP, 0, new_i60(676), Rslt_14Args);
  Rslt_14Args = pair_make(APP, 0, glblStr149, Rslt_14Args);
#ifdef STRICT
  store_redex(Rslt_14Args, glblProto358);
#else
  swapStore(term_loc(Rslt_14), pair_make(LAZ, 0, Rslt_14Args, glblProto358));
#endif
  // link args to body
  swapStore(r_seq_10, Rslt_14);

  Term Rslt_14V = get(term_loc(Rslt_14));
  apps = take(port(1, term_loc(Rslt_14V)));
  lams = take(port(2, term_loc(Rslt_14V)));

  vLoc = port(2, term_loc(apps));
  swapStore(term_loc(Rslt_14), pair_make(LAZ, 0, apps, lams));
  // call first at core: 673
  Term Rslt_15Args = pair_make(APP, 0, ss_vect_5_2, SUB);
  Term Rslt_15 = term_new(VAR, 0, port(2, term_loc(Rslt_15Args)));
  Rslt_15Args = pair_make(APP, 0, new_i60(673), Rslt_15Args);
  Rslt_15Args = pair_make(APP, 0, glblStr149, Rslt_15Args);
#ifdef STRICT
  store_redex(Rslt_15Args, glblProto72);
#else
  swapStore(term_loc(Rslt_15), pair_make(LAZ, 0, Rslt_15Args, glblProto72));
#endif
  // call vect-reduce at core: 673
  Term Rslt_23Args = pair_make(APP, 0, glblc404, SUB);
  Term Rslt_23 = term_new(VAR, 0, port(2, term_loc(Rslt_23Args)));
  Rslt_23Args = pair_make(APP, 0, new_i60(0), Rslt_23Args);
  Rslt_23Args = pair_make(APP, 0, new_i60(1), Rslt_23Args);
  Rslt_23Args = pair_make(APP, 0, ss_vect_5_3, Rslt_23Args);
  Rslt_23Args = pair_make(APP, 0, Rslt_15, Rslt_23Args);
  Rslt_23Args = pair_make(APP, 0, new_i60(673), Rslt_23Args);
  Rslt_23Args = pair_make(APP, 0, glblStr149, Rslt_23Args);
#ifdef STRICT
  store_redex(Rslt_23Args, glblProto358);
#else
  swapStore(term_loc(Rslt_23), pair_make(LAZ, 0, Rslt_23Args, glblProto358));
#endif
  // call  at core: 673
  Term Rslt_24Args = pair_make(APP, 0, Rslt_23, SUB);
  Term Rslt_24 = term_new(VAR, 0, port(2, term_loc(Rslt_24Args)));
#ifdef STRICT
  store_redex(Rslt_24Args, seq_10);
#else
  swapStore(term_loc(Rslt_24), pair_make(LAZ, 0, Rslt_24Args, seq_10));
#endif

  // link args to body
  swapStore(r_seq_6, Rslt_24);

  Term Rslt_24V = get(term_loc(Rslt_24));
  apps = take(port(1, term_loc(Rslt_24V)));
  lams = take(port(2, term_loc(Rslt_24V)));

  vLoc = port(2, term_loc(apps));
  swapStore(term_loc(Rslt_24), pair_make(LAZ, 0, apps, lams));
  // call flat-map at core: 672
  Term Rslt_25Args = pair_make(APP, 0, new_ref(glblProto106_412), SUB);
  Term Rslt_25 = term_new(VAR, 0, port(2, term_loc(Rslt_25Args)));
  Rslt_25Args = pair_make(APP, 0, vect_1, Rslt_25Args);
  Rslt_25Args = pair_make(APP, 0, new_i60(672), Rslt_25Args);
  Rslt_25Args = pair_make(APP, 0, glblStr149, Rslt_25Args);
#ifdef STRICT
  store_redex(Rslt_25Args, glblProto42);
#else
  swapStore(term_loc(Rslt_25), pair_make(LAZ, 0, Rslt_25Args, glblProto42));
#endif
  // call  at core: 672
  Term Rslt_26Args = pair_make(APP, 0, Rslt_25, SUB);
  Term Rslt_26 = term_new(VAR, 0, port(2, term_loc(Rslt_26Args)));
#ifdef STRICT
  store_redex(Rslt_26Args, seq_6);
#else
  swapStore(term_loc(Rslt_26), pair_make(LAZ, 0, Rslt_26Args, seq_6));
#endif

  // link args to body
  swapStore(r_seq_2, Rslt_26);

  Term Rslt_26V = get(term_loc(Rslt_26));
  apps = take(port(1, term_loc(Rslt_26V)));
  lams = take(port(2, term_loc(Rslt_26V)));

  vLoc = port(2, term_loc(apps));
  swapStore(term_loc(Rslt_26), pair_make(LAZ, 0, apps, lams));
  store_redex(args, seq_2);
  return;
}
Term glblFn402 = new_ref(glblCFn401);
Term glblFn416;
void glblCFn415(Term ref, Term args) {
  Term apps, lams, V;
  Location vLoc;
  Term seq_4 = pair_make(LAM, 0, SUB, NUL);
  Location r_seq_4 = port(2, term_loc(seq_4));
  Term start_3 = term_new(VAR, 0, port(1, term_loc(seq_4)));
  seq_4 = pair_make(LAM, 0, SUB, seq_4);
  Term y_2 = term_new(VAR, 0, port(1, term_loc(seq_4)));
  seq_4 = pair_make(LAM, 0, SUB, seq_4);
  Term x_1 = term_new(VAR, 0, port(1, term_loc(seq_4)));
  // allocate args at core: 678

  Term x_1_2;
  x_1 = dupeArg(x_1, &x_1_2, 0);

  Term x_1_1;
  x_1 = dupeArg(x_1, &x_1_1, 0);

  Term y_2_1;
  y_2 = dupeArg(y_2, &y_2_1, 0);

  Term start_3_3;
  start_3 = dupeArg(start_3, &start_3_3, 0);

  Term start_3_2;
  start_3 = dupeArg(start_3, &start_3_2, 0);

  Term start_3_1;
  start_3 = dupeArg(start_3, &start_3_1, 0);
  Term glblVal18_3;
  glblVal18 = dupeArg(glblVal18, &glblVal18_3, 0);

  // call < at core: 691
  Term Rslt_6Args = pair_make(APP, 0, new_i60(0), SUB);
  Term Rslt_6 = term_new(VAR, 0, port(2, term_loc(Rslt_6Args)));
  Rslt_6Args = pair_make(APP, 0, start_3, Rslt_6Args);
  Rslt_6Args = pair_make(APP, 0, new_i60(691), Rslt_6Args);
  Rslt_6Args = pair_make(APP, 0, glblStr149, Rslt_6Args);
#ifdef STRICT
  store_redex(Rslt_6Args, glblProto22);
#else
  swapStore(term_loc(Rslt_6), pair_make(LAZ, 0, Rslt_6Args, glblProto22));
#endif
  // call Some at core: 692
  Term Rslt_7Args = pair_make(APP, 0, x_1, SUB);
  Term Rslt_7 = term_new(VAR, 0, port(2, term_loc(Rslt_7Args)));
#ifdef STRICT
  store_redex(Rslt_7Args, glblVal18_3);
#else
  swapStore(term_loc(Rslt_7), pair_make(LAZ, 0, Rslt_7Args, glblVal18_3));
#endif

  // call nth at core: 693
  Term Rslt_8Args = pair_make(APP, 0, start_3_1, SUB);
  Term Rslt_8 = term_new(VAR, 0, port(2, term_loc(Rslt_8Args)));
  Rslt_8Args = pair_make(APP, 0, x_1_1, Rslt_8Args);
  Rslt_8Args = pair_make(APP, 0, new_i60(693), Rslt_8Args);
  Rslt_8Args = pair_make(APP, 0, glblStr149, Rslt_8Args);
#ifdef STRICT
  store_redex(Rslt_8Args, glblProto94);
#else
  swapStore(term_loc(Rslt_8), pair_make(LAZ, 0, Rslt_8Args, glblProto94));
#endif
  // call .x at core: 693
  Term Rslt_9Args = pair_make(APP, 0, Rslt_8, SUB);
  Term Rslt_9 = term_new(VAR, 0, port(2, term_loc(Rslt_9Args)));
  Rslt_9Args = pair_make(APP, 0, new_i60(693), Rslt_9Args);
  Rslt_9Args = pair_make(APP, 0, glblStr149, Rslt_9Args);
#ifdef STRICT
  store_redex(Rslt_9Args, glblProto14);
#else
  swapStore(term_loc(Rslt_9), pair_make(LAZ, 0, Rslt_9Args, glblProto14));
#endif
  // call nth at core: 693
  Term Rslt_10Args = pair_make(APP, 0, start_3_2, SUB);
  Term Rslt_10 = term_new(VAR, 0, port(2, term_loc(Rslt_10Args)));
  Rslt_10Args = pair_make(APP, 0, y_2, Rslt_10Args);
  Rslt_10Args = pair_make(APP, 0, new_i60(693), Rslt_10Args);
  Rslt_10Args = pair_make(APP, 0, glblStr149, Rslt_10Args);
#ifdef STRICT
  store_redex(Rslt_10Args, glblProto94);
#else
  swapStore(term_loc(Rslt_10), pair_make(LAZ, 0, Rslt_10Args, glblProto94));
#endif
  // call .x at core: 693
  Term Rslt_11Args = pair_make(APP, 0, Rslt_10, SUB);
  Term Rslt_11 = term_new(VAR, 0, port(2, term_loc(Rslt_11Args)));
  Rslt_11Args = pair_make(APP, 0, new_i60(693), Rslt_11Args);
  Rslt_11Args = pair_make(APP, 0, glblStr149, Rslt_11Args);
#ifdef STRICT
  store_redex(Rslt_11Args, glblProto14);
#else
  swapStore(term_loc(Rslt_11), pair_make(LAZ, 0, Rslt_11Args, glblProto14));
#endif
  // call = at core: 693
  Term Rslt_12Args = pair_make(APP, 0, Rslt_11, SUB);
  Term Rslt_12 = term_new(VAR, 0, port(2, term_loc(Rslt_12Args)));
  Rslt_12Args = pair_make(APP, 0, Rslt_9, Rslt_12Args);
  Rslt_12Args = pair_make(APP, 0, new_i60(693), Rslt_12Args);
  Rslt_12Args = pair_make(APP, 0, glblStr149, Rslt_12Args);
#ifdef STRICT
  store_redex(Rslt_12Args, glblProto19);
#else
  swapStore(term_loc(Rslt_12), pair_make(LAZ, 0, Rslt_12Args, glblProto19));
#endif
  // call dec at core: 694
  Term Rslt_13Args = pair_make(APP, 0, start_3_3, SUB);
  Term Rslt_13 = term_new(VAR, 0, port(2, term_loc(Rslt_13Args)));
#ifdef STRICT
  store_redex(Rslt_13Args, glblFn126);
#else
  swapStore(term_loc(Rslt_13), pair_make(LAZ, 0, Rslt_13Args, glblFn126));
#endif

  // call vect-= at core: 678
  Term Rslt_14Args = pair_make(APP, 0, Rslt_13, SUB);
  Term Rslt_14 = term_new(VAR, 0, port(2, term_loc(Rslt_14Args)));
  Rslt_14Args = pair_make(APP, 0, y_2_1, Rslt_14Args);
  Rslt_14Args = pair_make(APP, 0, x_1_2, Rslt_14Args);
#ifdef STRICT
  store_redex(Rslt_14Args, glblFn416);
#else
  swapStore(term_loc(Rslt_14), pair_make(LAZ, 0, Rslt_14Args, glblFn416));
#endif

  // call and at core: 693
  Term Rslt_15Args = pair_make(APP, 0, Rslt_14, SUB);
  Term Rslt_15 = term_new(VAR, 0, port(2, term_loc(Rslt_15Args)));
  Rslt_15Args = pair_make(APP, 0, Rslt_12, Rslt_15Args);
  Rslt_15Args = pair_make(APP, 0, new_i60(693), Rslt_15Args);
  Rslt_15Args = pair_make(APP, 0, glblStr149, Rslt_15Args);
#ifdef STRICT
  store_redex(Rslt_15Args, glblProto24);
#else
  swapStore(term_loc(Rslt_15), pair_make(LAZ, 0, Rslt_15Args, glblProto24));
#endif
  // call cond at core: 691
  Term Rslt_16Args = pair_make(APP, 0, Rslt_15, SUB);
  Term Rslt_16 = term_new(VAR, 0, port(2, term_loc(Rslt_16Args)));
  Rslt_16Args = pair_make(APP, 0, Rslt_7, Rslt_16Args);
  Rslt_16Args = pair_make(APP, 0, Rslt_6, Rslt_16Args);
  Rslt_16Args = pair_make(APP, 0, new_i60(691), Rslt_16Args);
  Rslt_16Args = pair_make(APP, 0, glblStr149, Rslt_16Args);
#ifdef STRICT
  store_redex(Rslt_16Args, glblProto30);
#else
  swapStore(term_loc(Rslt_16), pair_make(LAZ, 0, Rslt_16Args, glblProto30));
#endif
  // link args to body
  swapStore(r_seq_4, Rslt_16);

  Term Rslt_16V = get(term_loc(Rslt_16));
  apps = take(port(1, term_loc(Rslt_16V)));
  lams = take(port(2, term_loc(Rslt_16V)));

  vLoc = port(2, term_loc(apps));
  swapStore(term_loc(Rslt_16), pair_make(LAZ, 0, apps, lams));
  store_redex(args, seq_4);
  return;
}
Term glblFn416 = new_ref(glblCFn415);
Term glblFn418;
void glblCFn417(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 3, &arityArgs);
  Term result = NUL;
  if (arityArgs.count == 3) {
    Term v_1 = arityArgs.args[0];
    Term n_2 = arityArgs.args[1];
    Term x_3 = arityArgs.args[2];

    result = vectStore((Vector *)v_1, get_i60(n_2), (Term)x_3);
    dec_and_free(n_2, 1);
    moveStore(port(2, term_loc(args)), result);
  }
  return;
}
Term glblFn418 = new_ref(glblCFn417);
Term glbl_EQ_472;
void glbl_EQ_471(Term ref, Term args) {
  Term apps, lams, V;
  Location vLoc;
  Term seq_9 = pair_make(LAM, 0, SUB, NUL);
  Location r_seq_9 = port(2, term_loc(seq_9));
  Term y_8 = term_new(VAR, 0, port(1, term_loc(seq_9)));
  seq_9 = pair_make(LAM, 0, SUB, seq_9);
  Term x_7 = term_new(VAR, 0, port(1, term_loc(seq_9)));
  // allocate args at core: 706

  Term x_7_3;
  x_7 = dupeArg(x_7, &x_7_3, 0);

  Term x_7_2;
  x_7 = dupeArg(x_7, &x_7_2, 0);

  Term x_7_1;
  x_7 = dupeArg(x_7, &x_7_1, 0);

  Term y_8_2;
  y_8 = dupeArg(y_8, &y_8_2, 0);

  Term y_8_1;
  y_8 = dupeArg(y_8, &y_8_1, 0);
  // call type-num at core: 708
  Term Rslt_11Args = pair_make(APP, 0, x_7, SUB);
  Term Rslt_11 = term_new(VAR, 0, port(2, term_loc(Rslt_11Args)));
#ifdef STRICT
  store_redex(Rslt_11Args, glblFn8);
#else
  swapStore(term_loc(Rslt_11), pair_make(LAZ, 0, Rslt_11Args, glblFn8));
#endif

  // call type-num at core: 708
  Term Rslt_12Args = pair_make(APP, 0, y_8, SUB);
  Term Rslt_12 = term_new(VAR, 0, port(2, term_loc(Rslt_12Args)));
#ifdef STRICT
  store_redex(Rslt_12Args, glblFn8);
#else
  swapStore(term_loc(Rslt_12), pair_make(LAZ, 0, Rslt_12Args, glblFn8));
#endif

  // call = at core: 708
  Term Rslt_13Args = pair_make(APP, 0, Rslt_12, SUB);
  Term Rslt_13 = term_new(VAR, 0, port(2, term_loc(Rslt_13Args)));
  Rslt_13Args = pair_make(APP, 0, Rslt_11, Rslt_13Args);
  Rslt_13Args = pair_make(APP, 0, new_i60(708), Rslt_13Args);
  Rslt_13Args = pair_make(APP, 0, glblStr149, Rslt_13Args);
#ifdef STRICT
  store_redex(Rslt_13Args, glblProto19);
#else
  swapStore(term_loc(Rslt_13), pair_make(LAZ, 0, Rslt_13Args, glblProto19));
#endif
  // call vect-count at core: 709
  Term Rslt_14Args = pair_make(APP, 0, x_7_1, SUB);
  Term Rslt_14 = term_new(VAR, 0, port(2, term_loc(Rslt_14Args)));
#ifdef STRICT
  store_redex(Rslt_14Args, glblFn245);
#else
  swapStore(term_loc(Rslt_14), pair_make(LAZ, 0, Rslt_14Args, glblFn245));
#endif

  // call vect-count at core: 709
  Term Rslt_15Args = pair_make(APP, 0, y_8_1, SUB);
  Term Rslt_15 = term_new(VAR, 0, port(2, term_loc(Rslt_15Args)));
#ifdef STRICT
  store_redex(Rslt_15Args, glblFn245);
#else
  swapStore(term_loc(Rslt_15), pair_make(LAZ, 0, Rslt_15Args, glblFn245));
#endif

  // call = at core: 709
  Term Rslt_16Args = pair_make(APP, 0, Rslt_15, SUB);
  Term Rslt_16 = term_new(VAR, 0, port(2, term_loc(Rslt_16Args)));
  Rslt_16Args = pair_make(APP, 0, Rslt_14, Rslt_16Args);
  Rslt_16Args = pair_make(APP, 0, new_i60(709), Rslt_16Args);
  Rslt_16Args = pair_make(APP, 0, glblStr149, Rslt_16Args);
#ifdef STRICT
  store_redex(Rslt_16Args, glblProto19);
#else
  swapStore(term_loc(Rslt_16), pair_make(LAZ, 0, Rslt_16Args, glblProto19));
#endif
  // call count at core: 710
  Term Rslt_17Args = pair_make(APP, 0, x_7_3, SUB);
  Term Rslt_17 = term_new(VAR, 0, port(2, term_loc(Rslt_17Args)));
  Rslt_17Args = pair_make(APP, 0, new_i60(710), Rslt_17Args);
  Rslt_17Args = pair_make(APP, 0, glblStr149, Rslt_17Args);
#ifdef STRICT
  store_redex(Rslt_17Args, glblProto60);
#else
  swapStore(term_loc(Rslt_17), pair_make(LAZ, 0, Rslt_17Args, glblProto60));
#endif
  // call dec at core: 710
  Term Rslt_18Args = pair_make(APP, 0, Rslt_17, SUB);
  Term Rslt_18 = term_new(VAR, 0, port(2, term_loc(Rslt_18Args)));
#ifdef STRICT
  store_redex(Rslt_18Args, glblFn126);
#else
  swapStore(term_loc(Rslt_18), pair_make(LAZ, 0, Rslt_18Args, glblFn126));
#endif

  // call vect-= at core: 710
  Term Rslt_19Args = pair_make(APP, 0, Rslt_18, SUB);
  Term Rslt_19 = term_new(VAR, 0, port(2, term_loc(Rslt_19Args)));
  Rslt_19Args = pair_make(APP, 0, y_8_2, Rslt_19Args);
  Rslt_19Args = pair_make(APP, 0, x_7_2, Rslt_19Args);
#ifdef STRICT
  store_redex(Rslt_19Args, glblFn416);
#else
  swapStore(term_loc(Rslt_19), pair_make(LAZ, 0, Rslt_19Args, glblFn416));
#endif

  // call and at core: 709
  Term Rslt_20Args = pair_make(APP, 0, Rslt_19, SUB);
  Term Rslt_20 = term_new(VAR, 0, port(2, term_loc(Rslt_20Args)));
  Rslt_20Args = pair_make(APP, 0, Rslt_16, Rslt_20Args);
  Rslt_20Args = pair_make(APP, 0, new_i60(709), Rslt_20Args);
  Rslt_20Args = pair_make(APP, 0, glblStr149, Rslt_20Args);
#ifdef STRICT
  store_redex(Rslt_20Args, glblProto24);
#else
  swapStore(term_loc(Rslt_20), pair_make(LAZ, 0, Rslt_20Args, glblProto24));
#endif
  // call and at core: 708
  Term Rslt_21Args = pair_make(APP, 0, Rslt_20, SUB);
  Term Rslt_21 = term_new(VAR, 0, port(2, term_loc(Rslt_21Args)));
  Rslt_21Args = pair_make(APP, 0, Rslt_13, Rslt_21Args);
  Rslt_21Args = pair_make(APP, 0, new_i60(708), Rslt_21Args);
  Rslt_21Args = pair_make(APP, 0, glblStr149, Rslt_21Args);
#ifdef STRICT
  store_redex(Rslt_21Args, glblProto24);
#else
  swapStore(term_loc(Rslt_21), pair_make(LAZ, 0, Rslt_21Args, glblProto24));
#endif
  // link args to body
  swapStore(r_seq_9, Rslt_21);

  Term Rslt_21V = get(term_loc(Rslt_21));
  apps = take(port(1, term_loc(Rslt_21V)));
  lams = take(port(2, term_loc(Rslt_21V)));

  vLoc = port(2, term_loc(apps));
  swapStore(term_loc(Rslt_21), pair_make(LAZ, 0, apps, lams));
  store_redex(args, seq_9);
  return;
}
Term glbl_EQ_472 = new_ref(glbl_EQ_471);
struct {
  TYPE_SIZE type;
  REFS_SIZE refs;
  int64_t *hash;
  Value *parent;
  int64_t len;
  char buffer[7];
} str8 = {StringBufferType, REFS_STATIC, 0, 0, 6, "Vector"};
Term glblStr473 = term_new_(VAL, (Term)&str8);
Term glbltype_name470;
void glbltype_name469(Term ref, Term args) {
  Term apps, lams, V;
  Location vLoc;
  Term seq_24 = pair_make(LAM, 0, SUB, NUL);
  Location r_seq_24 = port(2, term_loc(seq_24));
  Term arg_23 = term_new(VAR, 0, port(1, term_loc(seq_24)));
  // allocate args at core: 712
  swapStore(term_loc(arg_23), ERA);
  // link args to body
  swapStore(r_seq_24, glblStr473);

  V = get(r_seq_24);
  apps = pair_make(APP, 0, V, SUB);
  lams = pair_make(LAM, 0, SUB, NUL);
  swapStore(port(2, term_loc(lams)), term_new(VAR, 0, port(1, term_loc(lams))));
  vLoc = port(2, term_loc(apps));
  swapStore(vLoc, pair_make(LAZ, 0, apps, lams));
  swapStore(r_seq_24, term_new(VAR, 0, vLoc));
  store_redex(args, seq_24);
  return;
}
Term glbltype_name470 = new_ref(glbltype_name469);
struct {
  TYPE_SIZE type;
  REFS_SIZE refs;
  int64_t *hash;
  Value *parent;
  int64_t len;
  char buffer[2];
} str9 = {StringBufferType, REFS_STATIC, 0, 0, 1, "["};
Term glblStr475 = term_new_(VAL, (Term)&str9);
struct {
  TYPE_SIZE type;
  REFS_SIZE refs;
  int64_t *hash;
  Value *parent;
  int64_t len;
  char buffer[2];
} str10 = {StringBufferType, REFS_STATIC, 0, 0, 1, "]"};
Term glblStr474 = term_new_(VAL, (Term)&str10);
Term glblstr_vect468;
void glblstr_vect467(Term ref, Term args) {
  Term apps, lams, V;
  Location vLoc;
  Term seq_28 = pair_make(LAM, 0, SUB, NUL);
  Location r_seq_28 = port(2, term_loc(seq_28));
  Term v_27 = term_new(VAR, 0, port(1, term_loc(seq_28)));
  // allocate args at core: 715
  Term glblVal130_11;
  glblVal130 = dupeArg(glblVal130, &glblVal130_11, 0);

  // call vect-conj at core: 716
  Term Rslt_30Args = pair_make(APP, 0, glblStr475, SUB);
  Term Rslt_30 = term_new(VAR, 0, port(2, term_loc(Rslt_30Args)));
  Rslt_30Args = pair_make(APP, 0, glblVal130_11, Rslt_30Args);
#ifdef STRICT
  store_redex(Rslt_30Args, glblFn133);
#else
  swapStore(term_loc(Rslt_30), pair_make(LAZ, 0, Rslt_30Args, glblFn133));
#endif

  // call interpose at core: 716
  Term Rslt_31Args = pair_make(APP, 0, glblStr160, SUB);
  Term Rslt_31 = term_new(VAR, 0, port(2, term_loc(Rslt_31Args)));
  Rslt_31Args = pair_make(APP, 0, v_27, Rslt_31Args);
  Rslt_31Args = pair_make(APP, 0, new_i60(716), Rslt_31Args);
  Rslt_31Args = pair_make(APP, 0, glblStr149, Rslt_31Args);
#ifdef STRICT
  store_redex(Rslt_31Args, glblProto56);
#else
  swapStore(term_loc(Rslt_31), pair_make(LAZ, 0, Rslt_31Args, glblProto56));
#endif
  // call to-str at core: 716
  Term Rslt_32Args = pair_make(APP, 0, Rslt_31, SUB);
  Term Rslt_32 = term_new(VAR, 0, port(2, term_loc(Rslt_32Args)));
#ifdef STRICT
  store_redex(Rslt_32Args, glblFn402);
#else
  swapStore(term_loc(Rslt_32), pair_make(LAZ, 0, Rslt_32Args, glblFn402));
#endif

  // call vect-conj at core: 716
  Term Rslt_33Args = pair_make(APP, 0, Rslt_32, SUB);
  Term Rslt_33 = term_new(VAR, 0, port(2, term_loc(Rslt_33Args)));
  Rslt_33Args = pair_make(APP, 0, Rslt_30, Rslt_33Args);
#ifdef STRICT
  store_redex(Rslt_33Args, glblFn133);
#else
  swapStore(term_loc(Rslt_33), pair_make(LAZ, 0, Rslt_33Args, glblFn133));
#endif

  // call vect-conj at core: 716
  Term Rslt_34Args = pair_make(APP, 0, glblStr474, SUB);
  Term Rslt_34 = term_new(VAR, 0, port(2, term_loc(Rslt_34Args)));
  Rslt_34Args = pair_make(APP, 0, Rslt_33, Rslt_34Args);
#ifdef STRICT
  store_redex(Rslt_34Args, glblFn133);
#else
  swapStore(term_loc(Rslt_34), pair_make(LAZ, 0, Rslt_34Args, glblFn133));
#endif

  // link args to body
  swapStore(r_seq_28, Rslt_34);

  Term Rslt_34V = get(term_loc(Rslt_34));
  apps = take(port(1, term_loc(Rslt_34V)));
  lams = take(port(2, term_loc(Rslt_34V)));

  vLoc = port(2, term_loc(apps));
  swapStore(term_loc(Rslt_34), pair_make(LAZ, 0, apps, lams));
  store_redex(args, seq_28);
  return;
}
Term glblstr_vect468 = new_ref(glblstr_vect467);
Term glblcount466;
void glblcount465(Term ref, Term args) {
  Term apps, lams, V;
  Location vLoc;
  Term seq_37 = pair_make(LAM, 0, SUB, NUL);
  Location r_seq_37 = port(2, term_loc(seq_37));
  Term l_36 = term_new(VAR, 0, port(1, term_loc(seq_37)));
  // allocate args at core: 718
  // call vect-count at core: 719
  Term Rslt_39Args = pair_make(APP, 0, l_36, SUB);
  Term Rslt_39 = term_new(VAR, 0, port(2, term_loc(Rslt_39Args)));
#ifdef STRICT
  store_redex(Rslt_39Args, glblFn245);
#else
  swapStore(term_loc(Rslt_39), pair_make(LAZ, 0, Rslt_39Args, glblFn245));
#endif

  // link args to body
  swapStore(r_seq_37, Rslt_39);

  Term Rslt_39V = get(term_loc(Rslt_39));
  apps = take(port(1, term_loc(Rslt_39V)));
  lams = take(port(2, term_loc(Rslt_39V)));

  vLoc = port(2, term_loc(apps));
  swapStore(term_loc(Rslt_39), pair_make(LAZ, 0, apps, lams));
  store_redex(args, seq_37);
  return;
}
Term glblcount466 = new_ref(glblcount465);
Term glblget464;
void glblget463(Term ref, Term args) {
  Term apps, lams, V;
  Location vLoc;
  Term seq_43 = pair_make(LAM, 0, SUB, NUL);
  Location r_seq_43 = port(2, term_loc(seq_43));
  Term n_42 = term_new(VAR, 0, port(1, term_loc(seq_43)));
  seq_43 = pair_make(LAM, 0, SUB, seq_43);
  Term v_41 = term_new(VAR, 0, port(1, term_loc(seq_43)));
  // allocate args at core: 721
  // call vect-get at core: 722
  Term Rslt_45Args = pair_make(APP, 0, n_42, SUB);
  Term Rslt_45 = term_new(VAR, 0, port(2, term_loc(Rslt_45Args)));
  Rslt_45Args = pair_make(APP, 0, v_41, Rslt_45Args);
#ifdef STRICT
  store_redex(Rslt_45Args, glblFn341);
#else
  swapStore(term_loc(Rslt_45), pair_make(LAZ, 0, Rslt_45Args, glblFn341));
#endif

  // link args to body
  swapStore(r_seq_43, Rslt_45);

  Term Rslt_45V = get(term_loc(Rslt_45));
  apps = take(port(1, term_loc(Rslt_45V)));
  lams = take(port(2, term_loc(Rslt_45V)));

  vLoc = port(2, term_loc(apps));
  swapStore(term_loc(Rslt_45), pair_make(LAZ, 0, apps, lams));
  store_redex(args, seq_43);
  return;
}
Term glblget464 = new_ref(glblget463);
Term glblnth462;
void glblnth461(Term ref, Term args) {
  Term apps, lams, V;
  Location vLoc;
  Term seq_49 = pair_make(LAM, 0, SUB, NUL);
  Location r_seq_49 = port(2, term_loc(seq_49));
  Term n_48 = term_new(VAR, 0, port(1, term_loc(seq_49)));
  seq_49 = pair_make(LAM, 0, SUB, seq_49);
  Term v_47 = term_new(VAR, 0, port(1, term_loc(seq_49)));
  // allocate args at core: 724
  // call vect-get at core: 725
  Term Rslt_51Args = pair_make(APP, 0, n_48, SUB);
  Term Rslt_51 = term_new(VAR, 0, port(2, term_loc(Rslt_51Args)));
  Rslt_51Args = pair_make(APP, 0, v_47, Rslt_51Args);
#ifdef STRICT
  store_redex(Rslt_51Args, glblFn341);
#else
  swapStore(term_loc(Rslt_51), pair_make(LAZ, 0, Rslt_51Args, glblFn341));
#endif

  // link args to body
  swapStore(r_seq_49, Rslt_51);

  Term Rslt_51V = get(term_loc(Rslt_51));
  apps = take(port(1, term_loc(Rslt_51V)));
  lams = take(port(2, term_loc(Rslt_51V)));

  vLoc = port(2, term_loc(apps));
  swapStore(term_loc(Rslt_51), pair_make(LAZ, 0, apps, lams));
  store_redex(args, seq_49);
  return;
}
Term glblnth462 = new_ref(glblnth461);
Term glblempty_QM_460;
void glblempty_QM_459(Term ref, Term args) {
  Term apps, lams, V;
  Location vLoc;
  Term seq_54 = pair_make(LAM, 0, SUB, NUL);
  Location r_seq_54 = port(2, term_loc(seq_54));
  Term v_53 = term_new(VAR, 0, port(1, term_loc(seq_54)));
  // allocate args at core: 727
  Term glblVal130_12;
  glblVal130 = dupeArg(glblVal130, &glblVal130_12, 0);

  Term glblVal18_4;
  glblVal18 = dupeArg(glblVal18, &glblVal18_4, 0);

  // call count at core: 728
  Term Rslt_56Args = pair_make(APP, 0, v_53, SUB);
  Term Rslt_56 = term_new(VAR, 0, port(2, term_loc(Rslt_56Args)));
  Rslt_56Args = pair_make(APP, 0, new_i60(728), Rslt_56Args);
  Rslt_56Args = pair_make(APP, 0, glblStr149, Rslt_56Args);
#ifdef STRICT
  store_redex(Rslt_56Args, glblProto60);
#else
  swapStore(term_loc(Rslt_56), pair_make(LAZ, 0, Rslt_56Args, glblProto60));
#endif
  // call int-= at core: 728
  Term Rslt_57Args = pair_make(APP, 0, Rslt_56, SUB);
  Term Rslt_57 = term_new(VAR, 0, port(2, term_loc(Rslt_57Args)));
  Rslt_57Args = pair_make(APP, 0, new_i60(0), Rslt_57Args);
#ifdef STRICT
  store_redex(Rslt_57Args, glblFn196);
#else
  swapStore(term_loc(Rslt_57), pair_make(LAZ, 0, Rslt_57Args, glblFn196));
#endif

  // call Some at core: 729
  Term Rslt_58Args = pair_make(APP, 0, glblVal130_12, SUB);
  Term Rslt_58 = term_new(VAR, 0, port(2, term_loc(Rslt_58Args)));
#ifdef STRICT
  store_redex(Rslt_58Args, glblVal18_4);
#else
  swapStore(term_loc(Rslt_58), pair_make(LAZ, 0, Rslt_58Args, glblVal18_4));
#endif

  // call and at core: 728
  Term Rslt_59Args = pair_make(APP, 0, Rslt_58, SUB);
  Term Rslt_59 = term_new(VAR, 0, port(2, term_loc(Rslt_59Args)));
  Rslt_59Args = pair_make(APP, 0, Rslt_57, Rslt_59Args);
  Rslt_59Args = pair_make(APP, 0, new_i60(728), Rslt_59Args);
  Rslt_59Args = pair_make(APP, 0, glblStr149, Rslt_59Args);
#ifdef STRICT
  store_redex(Rslt_59Args, glblProto24);
#else
  swapStore(term_loc(Rslt_59), pair_make(LAZ, 0, Rslt_59Args, glblProto24));
#endif
  // link args to body
  swapStore(r_seq_54, Rslt_59);

  Term Rslt_59V = get(term_loc(Rslt_59));
  apps = take(port(1, term_loc(Rslt_59V)));
  lams = take(port(2, term_loc(Rslt_59V)));

  vLoc = port(2, term_loc(apps));
  swapStore(term_loc(Rslt_59), pair_make(LAZ, 0, apps, lams));
  store_redex(args, seq_54);
  return;
}
Term glblempty_QM_460 = new_ref(glblempty_QM_459);
Term glblempty458;
void glblempty457(Term ref, Term args) {
  Term apps, lams, V;
  Location vLoc;
  Term seq_62 = pair_make(LAM, 0, SUB, NUL);
  Location r_seq_62 = port(2, term_loc(seq_62));
  Term coll_61 = term_new(VAR, 0, port(1, term_loc(seq_62)));
  // allocate args at core: 731
  swapStore(term_loc(coll_61), ERA);
  Term glblVal130_13;
  glblVal130 = dupeArg(glblVal130, &glblVal130_13, 0);

  // link args to body
  swapStore(r_seq_62, glblVal130_13);

  V = get(r_seq_62);
  apps = pair_make(APP, 0, V, SUB);
  lams = pair_make(LAM, 0, SUB, NUL);
  swapStore(port(2, term_loc(lams)), term_new(VAR, 0, port(1, term_loc(lams))));
  vLoc = port(2, term_loc(apps));
  swapStore(vLoc, pair_make(LAZ, 0, apps, lams));
  swapStore(r_seq_62, term_new(VAR, 0, vLoc));
  store_redex(args, seq_62);
  return;
}
Term glblempty458 = new_ref(glblempty457);
Term glblcomp456;
void glblcomp455(Term ref, Term args) {
  Term apps, lams, V;
  Location vLoc;
  Term seq_67 = pair_make(LAM, 0, SUB, NUL);
  Location r_seq_67 = port(2, term_loc(seq_67));
  Term v2_66 = term_new(VAR, 0, port(1, term_loc(seq_67)));
  seq_67 = pair_make(LAM, 0, SUB, seq_67);
  Term v1_65 = term_new(VAR, 0, port(1, term_loc(seq_67)));
  // allocate args at core: 734
  // call reduce at core: 735
  Term Rslt_69Args = pair_make(APP, 0, glblFn133, SUB);
  Term Rslt_69 = term_new(VAR, 0, port(2, term_loc(Rslt_69Args)));
  Rslt_69Args = pair_make(APP, 0, v1_65, Rslt_69Args);
  Rslt_69Args = pair_make(APP, 0, v2_66, Rslt_69Args);
  Rslt_69Args = pair_make(APP, 0, new_i60(735), Rslt_69Args);
  Rslt_69Args = pair_make(APP, 0, glblStr149, Rslt_69Args);
#ifdef STRICT
  store_redex(Rslt_69Args, glblProto68);
#else
  swapStore(term_loc(Rslt_69), pair_make(LAZ, 0, Rslt_69Args, glblProto68));
#endif
  // link args to body
  swapStore(r_seq_67, Rslt_69);

  Term Rslt_69V = get(term_loc(Rslt_69));
  apps = take(port(1, term_loc(Rslt_69V)));
  lams = take(port(2, term_loc(Rslt_69V)));

  vLoc = port(2, term_loc(apps));
  swapStore(term_loc(Rslt_69), pair_make(LAZ, 0, apps, lams));
  store_redex(args, seq_67);
  return;
}
Term glblcomp456 = new_ref(glblcomp455);
Term glblfirst454;
void glblfirst453(Term ref, Term args) {
  Term apps, lams, V;
  Location vLoc;
  Term seq_72 = pair_make(LAM, 0, SUB, NUL);
  Location r_seq_72 = port(2, term_loc(seq_72));
  Term v_71 = term_new(VAR, 0, port(1, term_loc(seq_72)));
  // allocate args at core: 737
  // call get at core: 738
  Term Rslt_74Args = pair_make(APP, 0, new_i60(0), SUB);
  Term Rslt_74 = term_new(VAR, 0, port(2, term_loc(Rslt_74Args)));
  Rslt_74Args = pair_make(APP, 0, v_71, Rslt_74Args);
  Rslt_74Args = pair_make(APP, 0, new_i60(738), Rslt_74Args);
  Rslt_74Args = pair_make(APP, 0, glblStr149, Rslt_74Args);
#ifdef STRICT
  store_redex(Rslt_74Args, glblProto100);
#else
  swapStore(term_loc(Rslt_74), pair_make(LAZ, 0, Rslt_74Args, glblProto100));
#endif
  // link args to body
  swapStore(r_seq_72, Rslt_74);

  Term Rslt_74V = get(term_loc(Rslt_74));
  apps = take(port(1, term_loc(Rslt_74V)));
  lams = take(port(2, term_loc(Rslt_74V)));

  vLoc = port(2, term_loc(apps));
  swapStore(term_loc(Rslt_74), pair_make(LAZ, 0, apps, lams));
  store_redex(args, seq_72);
  return;
}
Term glblfirst454 = new_ref(glblfirst453);
Term glbllast452;
void glbllast451(Term ref, Term args) {
  Term apps, lams, V;
  Location vLoc;
  Term seq_77 = pair_make(LAM, 0, SUB, NUL);
  Location r_seq_77 = port(2, term_loc(seq_77));
  Term v_76 = term_new(VAR, 0, port(1, term_loc(seq_77)));
  // allocate args at core: 740

  Term v_76_1;
  v_76 = dupeArg(v_76, &v_76_1, 0);
  // call count at core: 741
  Term Rslt_79Args = pair_make(APP, 0, v_76_1, SUB);
  Term Rslt_79 = term_new(VAR, 0, port(2, term_loc(Rslt_79Args)));
  Rslt_79Args = pair_make(APP, 0, new_i60(741), Rslt_79Args);
  Rslt_79Args = pair_make(APP, 0, glblStr149, Rslt_79Args);
#ifdef STRICT
  store_redex(Rslt_79Args, glblProto60);
#else
  swapStore(term_loc(Rslt_79), pair_make(LAZ, 0, Rslt_79Args, glblProto60));
#endif
  // call dec at core: 741
  Term Rslt_80Args = pair_make(APP, 0, Rslt_79, SUB);
  Term Rslt_80 = term_new(VAR, 0, port(2, term_loc(Rslt_80Args)));
#ifdef STRICT
  store_redex(Rslt_80Args, glblFn126);
#else
  swapStore(term_loc(Rslt_80), pair_make(LAZ, 0, Rslt_80Args, glblFn126));
#endif

  // call get at core: 741
  Term Rslt_81Args = pair_make(APP, 0, Rslt_80, SUB);
  Term Rslt_81 = term_new(VAR, 0, port(2, term_loc(Rslt_81Args)));
  Rslt_81Args = pair_make(APP, 0, v_76, Rslt_81Args);
  Rslt_81Args = pair_make(APP, 0, new_i60(741), Rslt_81Args);
  Rslt_81Args = pair_make(APP, 0, glblStr149, Rslt_81Args);
#ifdef STRICT
  store_redex(Rslt_81Args, glblProto100);
#else
  swapStore(term_loc(Rslt_81), pair_make(LAZ, 0, Rslt_81Args, glblProto100));
#endif
  // link args to body
  swapStore(r_seq_77, Rslt_81);

  Term Rslt_81V = get(term_loc(Rslt_81));
  apps = take(port(1, term_loc(Rslt_81V)));
  lams = take(port(2, term_loc(Rslt_81V)));

  vLoc = port(2, term_loc(apps));
  swapStore(term_loc(Rslt_81), pair_make(LAZ, 0, apps, lams));
  store_redex(args, seq_77);
  return;
}
Term glbllast452 = new_ref(glbllast451);
Term glblrest450;
void glblrest449(Term ref, Term args) {
  Term apps, lams, V;
  Location vLoc;
  Term seq_84 = pair_make(LAM, 0, SUB, NUL);
  Location r_seq_84 = port(2, term_loc(seq_84));
  Term v_83 = term_new(VAR, 0, port(1, term_loc(seq_84)));
  // allocate args at core: 743

  Term v_83_1;
  v_83 = dupeArg(v_83, &v_83_1, 0);
  // call count at core: 744
  Term Rslt_86Args = pair_make(APP, 0, v_83_1, SUB);
  Term Rslt_86 = term_new(VAR, 0, port(2, term_loc(Rslt_86Args)));
  Rslt_86Args = pair_make(APP, 0, new_i60(744), Rslt_86Args);
  Rslt_86Args = pair_make(APP, 0, glblStr149, Rslt_86Args);
#ifdef STRICT
  store_redex(Rslt_86Args, glblProto60);
#else
  swapStore(term_loc(Rslt_86), pair_make(LAZ, 0, Rslt_86Args, glblProto60));
#endif
  // call dec at core: 744
  Term Rslt_87Args = pair_make(APP, 0, Rslt_86, SUB);
  Term Rslt_87 = term_new(VAR, 0, port(2, term_loc(Rslt_87Args)));
#ifdef STRICT
  store_redex(Rslt_87Args, glblFn126);
#else
  swapStore(term_loc(Rslt_87), pair_make(LAZ, 0, Rslt_87Args, glblFn126));
#endif

  // call subvec at core: 744
  Term Rslt_88Args = pair_make(APP, 0, Rslt_87, SUB);
  Term Rslt_88 = term_new(VAR, 0, port(2, term_loc(Rslt_88Args)));
  Rslt_88Args = pair_make(APP, 0, new_i60(1), Rslt_88Args);
  Rslt_88Args = pair_make(APP, 0, v_83, Rslt_88Args);
#ifdef STRICT
  store_redex(Rslt_88Args, glblFn397);
#else
  swapStore(term_loc(Rslt_88), pair_make(LAZ, 0, Rslt_88Args, glblFn397));
#endif

  // link args to body
  swapStore(r_seq_84, Rslt_88);

  Term Rslt_88V = get(term_loc(Rslt_88));
  apps = take(port(1, term_loc(Rslt_88V)));
  lams = take(port(2, term_loc(Rslt_88V)));

  vLoc = port(2, term_loc(apps));
  swapStore(term_loc(Rslt_88), pair_make(LAZ, 0, apps, lams));
  store_redex(args, seq_84);
  return;
}
Term glblrest450 = new_ref(glblrest449);
Term glblconj448;
void glblconj447(Term ref, Term args) {
  Term apps, lams, V;
  Location vLoc;
  Term seq_92 = pair_make(LAM, 0, SUB, NUL);
  Location r_seq_92 = port(2, term_loc(seq_92));
  Term v_91 = term_new(VAR, 0, port(1, term_loc(seq_92)));
  seq_92 = pair_make(LAM, 0, SUB, seq_92);
  Term vect_90 = term_new(VAR, 0, port(1, term_loc(seq_92)));
  // allocate args at core: 746
  // call vect-conj at core: 747
  Term Rslt_94Args = pair_make(APP, 0, v_91, SUB);
  Term Rslt_94 = term_new(VAR, 0, port(2, term_loc(Rslt_94Args)));
  Rslt_94Args = pair_make(APP, 0, vect_90, Rslt_94Args);
#ifdef STRICT
  store_redex(Rslt_94Args, glblFn133);
#else
  swapStore(term_loc(Rslt_94), pair_make(LAZ, 0, Rslt_94Args, glblFn133));
#endif

  // link args to body
  swapStore(r_seq_92, Rslt_94);

  Term Rslt_94V = get(term_loc(Rslt_94));
  apps = take(port(1, term_loc(Rslt_94V)));
  lams = take(port(2, term_loc(Rslt_94V)));

  vLoc = port(2, term_loc(apps));
  swapStore(term_loc(Rslt_94), pair_make(LAZ, 0, apps, lams));
  store_redex(args, seq_92);
  return;
}
Term glblconj448 = new_ref(glblconj447);
Term glblstore446;
void glblstore445(Term ref, Term args) {
  Term apps, lams, V;
  Location vLoc;
  Term seq_99 = pair_make(LAM, 0, SUB, NUL);
  Location r_seq_99 = port(2, term_loc(seq_99));
  Term v_98 = term_new(VAR, 0, port(1, term_loc(seq_99)));
  seq_99 = pair_make(LAM, 0, SUB, seq_99);
  Term n_97 = term_new(VAR, 0, port(1, term_loc(seq_99)));
  seq_99 = pair_make(LAM, 0, SUB, seq_99);
  Term vect_96 = term_new(VAR, 0, port(1, term_loc(seq_99)));
  // allocate args at core: 749
  // call vect-store at core: 750
  Term Rslt_101Args = pair_make(APP, 0, v_98, SUB);
  Term Rslt_101 = term_new(VAR, 0, port(2, term_loc(Rslt_101Args)));
  Rslt_101Args = pair_make(APP, 0, n_97, Rslt_101Args);
  Rslt_101Args = pair_make(APP, 0, vect_96, Rslt_101Args);
#ifdef STRICT
  store_redex(Rslt_101Args, glblFn418);
#else
  swapStore(term_loc(Rslt_101), pair_make(LAZ, 0, Rslt_101Args, glblFn418));
#endif

  // link args to body
  swapStore(r_seq_99, Rslt_101);

  Term Rslt_101V = get(term_loc(Rslt_101));
  apps = take(port(1, term_loc(Rslt_101V)));
  lams = take(port(2, term_loc(Rslt_101V)));

  vLoc = port(2, term_loc(apps));
  swapStore(term_loc(Rslt_101), pair_make(LAZ, 0, apps, lams));
  store_redex(args, seq_99);
  return;
}
Term glblstore446 = new_ref(glblstore445);
Term glblreduce444;
void glblreduce443(Term ref, Term args) {
  Term apps, lams, V;
  Location vLoc;
  Term seq_106 = pair_make(LAM, 0, SUB, NUL);
  Location r_seq_106 = port(2, term_loc(seq_106));
  Term f_105 = term_new(VAR, 0, port(1, term_loc(seq_106)));
  seq_106 = pair_make(LAM, 0, SUB, seq_106);
  Term result_104 = term_new(VAR, 0, port(1, term_loc(seq_106)));
  seq_106 = pair_make(LAM, 0, SUB, seq_106);
  Term v_103 = term_new(VAR, 0, port(1, term_loc(seq_106)));
  // allocate args at core: 752

  Term v_103_1;
  v_103 = dupeArg(v_103, &v_103_1, 0);
  // call first at core: 753
  Term Rslt_108Args = pair_make(APP, 0, v_103, SUB);
  Term Rslt_108 = term_new(VAR, 0, port(2, term_loc(Rslt_108Args)));
  Rslt_108Args = pair_make(APP, 0, new_i60(753), Rslt_108Args);
  Rslt_108Args = pair_make(APP, 0, glblStr149, Rslt_108Args);
#ifdef STRICT
  store_redex(Rslt_108Args, glblProto72);
#else
  swapStore(term_loc(Rslt_108), pair_make(LAZ, 0, Rslt_108Args, glblProto72));
#endif
  // call vect-reduce at core: 753
  Term Rslt_109Args = pair_make(APP, 0, f_105, SUB);
  Term Rslt_109 = term_new(VAR, 0, port(2, term_loc(Rslt_109Args)));
  Rslt_109Args = pair_make(APP, 0, result_104, Rslt_109Args);
  Rslt_109Args = pair_make(APP, 0, new_i60(1), Rslt_109Args);
  Rslt_109Args = pair_make(APP, 0, v_103_1, Rslt_109Args);
  Rslt_109Args = pair_make(APP, 0, Rslt_108, Rslt_109Args);
  Rslt_109Args = pair_make(APP, 0, new_i60(753), Rslt_109Args);
  Rslt_109Args = pair_make(APP, 0, glblStr149, Rslt_109Args);
#ifdef STRICT
  store_redex(Rslt_109Args, glblProto358);
#else
  swapStore(term_loc(Rslt_109), pair_make(LAZ, 0, Rslt_109Args, glblProto358));
#endif
  // link args to body
  swapStore(r_seq_106, Rslt_109);

  Term Rslt_109V = get(term_loc(Rslt_109));
  apps = take(port(1, term_loc(Rslt_109V)));
  lams = take(port(2, term_loc(Rslt_109V)));

  vLoc = port(2, term_loc(apps));
  swapStore(term_loc(Rslt_109), pair_make(LAZ, 0, apps, lams));
  store_redex(args, seq_106);
  return;
}
Term glblreduce444 = new_ref(glblreduce443);
Term glblreverse442;
void glblreverse441(Term ref, Term args) {
  Term apps, lams, V;
  Location vLoc;
  Term seq_112 = pair_make(LAM, 0, SUB, NUL);
  Location r_seq_112 = port(2, term_loc(seq_112));
  Term v_111 = term_new(VAR, 0, port(1, term_loc(seq_112)));
  // allocate args at core: 755

  Term v_111_2;
  v_111 = dupeArg(v_111, &v_111_2, 0);

  Term v_111_1;
  v_111 = dupeArg(v_111, &v_111_1, 0);
  Term glblVal130_14;
  glblVal130 = dupeArg(glblVal130, &glblVal130_14, 0);

  // call last at core: 756
  Term Rslt_114Args = pair_make(APP, 0, v_111, SUB);
  Term Rslt_114 = term_new(VAR, 0, port(2, term_loc(Rslt_114Args)));
  Rslt_114Args = pair_make(APP, 0, new_i60(756), Rslt_114Args);
  Rslt_114Args = pair_make(APP, 0, glblStr149, Rslt_114Args);
#ifdef STRICT
  store_redex(Rslt_114Args, glblProto76);
#else
  swapStore(term_loc(Rslt_114), pair_make(LAZ, 0, Rslt_114Args, glblProto76));
#endif
  // call count at core: 756
  Term Rslt_115Args = pair_make(APP, 0, v_111_2, SUB);
  Term Rslt_115 = term_new(VAR, 0, port(2, term_loc(Rslt_115Args)));
  Rslt_115Args = pair_make(APP, 0, new_i60(756), Rslt_115Args);
  Rslt_115Args = pair_make(APP, 0, glblStr149, Rslt_115Args);
#ifdef STRICT
  store_redex(Rslt_115Args, glblProto60);
#else
  swapStore(term_loc(Rslt_115), pair_make(LAZ, 0, Rslt_115Args, glblProto60));
#endif
  Term Rslt_116 = make_op(OP_SUB, Rslt_115, new_i60(2));
  // call vect-reverse at core: 756
  Term Rslt_117Args = pair_make(APP, 0, glblVal130_14, SUB);
  Term Rslt_117 = term_new(VAR, 0, port(2, term_loc(Rslt_117Args)));
  Rslt_117Args = pair_make(APP, 0, Rslt_116, Rslt_117Args);
  Rslt_117Args = pair_make(APP, 0, v_111_1, Rslt_117Args);
  Rslt_117Args = pair_make(APP, 0, Rslt_114, Rslt_117Args);
  Rslt_117Args = pair_make(APP, 0, new_i60(756), Rslt_117Args);
  Rslt_117Args = pair_make(APP, 0, glblStr149, Rslt_117Args);
#ifdef STRICT
  store_redex(Rslt_117Args, glblProto365);
#else
  swapStore(term_loc(Rslt_117), pair_make(LAZ, 0, Rslt_117Args, glblProto365));
#endif
  // link args to body
  swapStore(r_seq_112, Rslt_117);

  Term Rslt_117V = get(term_loc(Rslt_117));
  apps = take(port(1, term_loc(Rslt_117V)));
  lams = take(port(2, term_loc(Rslt_117V)));

  vLoc = port(2, term_loc(apps));
  swapStore(term_loc(Rslt_117), pair_make(LAZ, 0, apps, lams));
  store_redex(args, seq_112);
  return;
}
Term glblreverse442 = new_ref(glblreverse441);
Term glblmap440;
void glblmap439(Term ref, Term args) {
  Term apps, lams, V;
  Location vLoc;
  Term seq_121 = pair_make(LAM, 0, SUB, NUL);
  Location r_seq_121 = port(2, term_loc(seq_121));
  Term f_120 = term_new(VAR, 0, port(1, term_loc(seq_121)));
  seq_121 = pair_make(LAM, 0, SUB, seq_121);
  Term v_119 = term_new(VAR, 0, port(1, term_loc(seq_121)));
  // allocate args at core: 758
  Term glblVal398_1;
  glblVal398 = dupeArg(glblVal398, &glblVal398_1, 0);

  // call vect-map at core: 760
  Term Rslt_123Args = pair_make(APP, 0, f_120, SUB);
  Term Rslt_123 = term_new(VAR, 0, port(2, term_loc(Rslt_123Args)));
  Rslt_123Args = pair_make(APP, 0, v_119, Rslt_123Args);
#ifdef STRICT
  store_redex(Rslt_123Args, glblVal398_1);
#else
  swapStore(term_loc(Rslt_123), pair_make(LAZ, 0, Rslt_123Args, glblVal398_1));
#endif

  // link args to body
  swapStore(r_seq_121, Rslt_123);

  Term Rslt_123V = get(term_loc(Rslt_123));
  apps = take(port(1, term_loc(Rslt_123V)));
  lams = take(port(2, term_loc(Rslt_123V)));

  vLoc = port(2, term_loc(apps));
  swapStore(term_loc(Rslt_123), pair_make(LAZ, 0, apps, lams));
  store_redex(args, seq_121);
  return;
}
Term glblmap440 = new_ref(glblmap439);
Term glblwrap438;
void glblwrap437(Term ref, Term args) {
  Term apps, lams, V;
  Location vLoc;
  Term seq_127 = pair_make(LAM, 0, SUB, NUL);
  Location r_seq_127 = port(2, term_loc(seq_127));
  Term v_126 = term_new(VAR, 0, port(1, term_loc(seq_127)));
  seq_127 = pair_make(LAM, 0, SUB, seq_127);
  Term arg_125 = term_new(VAR, 0, port(1, term_loc(seq_127)));
  // allocate args at core: 762
  swapStore(term_loc(arg_125), ERA);
  Term glblVal130_15;
  glblVal130 = dupeArg(glblVal130, &glblVal130_15, 0);

  // call vect-conj at core: 763
  Term Rslt_129Args = pair_make(APP, 0, v_126, SUB);
  Term Rslt_129 = term_new(VAR, 0, port(2, term_loc(Rslt_129Args)));
  Rslt_129Args = pair_make(APP, 0, glblVal130_15, Rslt_129Args);
#ifdef STRICT
  store_redex(Rslt_129Args, glblFn133);
#else
  swapStore(term_loc(Rslt_129), pair_make(LAZ, 0, Rslt_129Args, glblFn133));
#endif

  // link args to body
  swapStore(r_seq_127, Rslt_129);

  Term Rslt_129V = get(term_loc(Rslt_129));
  apps = take(port(1, term_loc(Rslt_129V)));
  lams = take(port(2, term_loc(Rslt_129V)));

  vLoc = port(2, term_loc(apps));
  swapStore(term_loc(Rslt_129), pair_make(LAZ, 0, apps, lams));
  store_redex(args, seq_127);
  return;
}
Term glblwrap438 = new_ref(glblwrap437);
Term glblflat_map436;
void glblflat_map435(Term ref, Term args) {
  Term apps, lams, V;
  Location vLoc;
  Term seq_133 = pair_make(LAM, 0, SUB, NUL);
  Location r_seq_133 = port(2, term_loc(seq_133));
  Term f_132 = term_new(VAR, 0, port(1, term_loc(seq_133)));
  seq_133 = pair_make(LAM, 0, SUB, seq_133);
  Term v_131 = term_new(VAR, 0, port(1, term_loc(seq_133)));
  // allocate args at core: 765
  Term glblVal398_2;
  glblVal398 = dupeArg(glblVal398, &glblVal398_2, 0);

  // call vect-map at core: 766
  Term Rslt_135Args = pair_make(APP, 0, f_132, SUB);
  Term Rslt_135 = term_new(VAR, 0, port(2, term_loc(Rslt_135Args)));
  Rslt_135Args = pair_make(APP, 0, v_131, Rslt_135Args);
#ifdef STRICT
  store_redex(Rslt_135Args, glblVal398_2);
#else
  swapStore(term_loc(Rslt_135), pair_make(LAZ, 0, Rslt_135Args, glblVal398_2));
#endif

  // call flatten at core: 766
  Term Rslt_136Args = pair_make(APP, 0, Rslt_135, SUB);
  Term Rslt_136 = term_new(VAR, 0, port(2, term_loc(Rslt_136Args)));
  Rslt_136Args = pair_make(APP, 0, new_i60(766), Rslt_136Args);
  Rslt_136Args = pair_make(APP, 0, glblStr149, Rslt_136Args);
#ifdef STRICT
  store_redex(Rslt_136Args, glblProto44);
#else
  swapStore(term_loc(Rslt_136), pair_make(LAZ, 0, Rslt_136Args, glblProto44));
#endif
  // link args to body
  swapStore(r_seq_133, Rslt_136);

  Term Rslt_136V = get(term_loc(Rslt_136));
  apps = take(port(1, term_loc(Rslt_136V)));
  lams = take(port(2, term_loc(Rslt_136V)));

  vLoc = port(2, term_loc(apps));
  swapStore(term_loc(Rslt_136), pair_make(LAZ, 0, apps, lams));
  store_redex(args, seq_133);
  return;
}
Term glblflat_map436 = new_ref(glblflat_map435);
void glblProto34_541(Term ref, Term args) {
  args = pair_make(APP, 0, new_i60(769), args);
  args = pair_make(APP, 0, glblStr149, args);
  interact(args, glblProto34);
}
Term glblflatten434;
void glblflatten433(Term ref, Term args) {
  Term apps, lams, V;
  Location vLoc;
  Term seq_139 = pair_make(LAM, 0, SUB, NUL);
  Location r_seq_139 = port(2, term_loc(seq_139));
  Term v_138 = term_new(VAR, 0, port(1, term_loc(seq_139)));
  // allocate args at core: 768

  Term v_138_1;
  v_138 = dupeArg(v_138, &v_138_1, 0);
  Term glblVal130_16;
  glblVal130 = dupeArg(glblVal130, &glblVal130_16, 0);

  // call first at core: 769
  Term Rslt_141Args = pair_make(APP, 0, v_138, SUB);
  Term Rslt_141 = term_new(VAR, 0, port(2, term_loc(Rslt_141Args)));
  Rslt_141Args = pair_make(APP, 0, new_i60(769), Rslt_141Args);
  Rslt_141Args = pair_make(APP, 0, glblStr149, Rslt_141Args);
#ifdef STRICT
  store_redex(Rslt_141Args, glblProto72);
#else
  swapStore(term_loc(Rslt_141), pair_make(LAZ, 0, Rslt_141Args, glblProto72));
#endif
  // call vect-reduce at core: 769
  Term Rslt_142Args = pair_make(APP, 0, new_ref(glblProto34_541), SUB);
  Term Rslt_142 = term_new(VAR, 0, port(2, term_loc(Rslt_142Args)));
  Rslt_142Args = pair_make(APP, 0, glblVal130_16, Rslt_142Args);
  Rslt_142Args = pair_make(APP, 0, new_i60(1), Rslt_142Args);
  Rslt_142Args = pair_make(APP, 0, v_138_1, Rslt_142Args);
  Rslt_142Args = pair_make(APP, 0, Rslt_141, Rslt_142Args);
  Rslt_142Args = pair_make(APP, 0, new_i60(769), Rslt_142Args);
  Rslt_142Args = pair_make(APP, 0, glblStr149, Rslt_142Args);
#ifdef STRICT
  store_redex(Rslt_142Args, glblProto358);
#else
  swapStore(term_loc(Rslt_142), pair_make(LAZ, 0, Rslt_142Args, glblProto358));
#endif
  // link args to body
  swapStore(r_seq_139, Rslt_142);

  Term Rslt_142V = get(term_loc(Rslt_142));
  apps = take(port(1, term_loc(Rslt_142V)));
  lams = take(port(2, term_loc(Rslt_142V)));

  vLoc = port(2, term_loc(apps));
  swapStore(term_loc(Rslt_142), pair_make(LAZ, 0, apps, lams));
  store_redex(args, seq_139);
  return;
}
Term glblflatten434 = new_ref(glblflatten433);
Term glblProto286;
Term glblProto502;
Term glblProto499;
void glblCons512(Term ref, Term args) {
  Term callArgs = pair_make(APP, 0, new_i60(3), args);
  callArgs = pair_make(APP, 0, new_i60(45), callArgs);
  interact(callArgs, construct);
  return;
}
void glblEndOfList486(Term ref, Term args) {
  Term callArgs = pair_make(APP, 0, new_i60(0), args);
  callArgs = pair_make(APP, 0, new_i60(46), callArgs);
  interact(callArgs, construct);
  return;
}
Term glblfilter432;
void glblfilter431(Term ref, Term args) {
  Term apps, lams, V;
  Location vLoc;
  Term seq_146 = pair_make(LAM, 0, SUB, NUL);
  Location r_seq_146 = port(2, term_loc(seq_146));
  Term pred_145 = term_new(VAR, 0, port(1, term_loc(seq_146)));
  seq_146 = pair_make(LAM, 0, SUB, seq_146);
  Term v_144 = term_new(VAR, 0, port(1, term_loc(seq_146)));
  // allocate args at core: 771

  Term v_144_1;
  v_144 = dupeArg(v_144, &v_144_1, 0);
  Term seq_152 = pair_make(LAM, 0, SUB, NUL);
  Location r_seq_152 = port(2, term_loc(seq_152));
  Term x_151 = term_new(VAR, 0, port(1, term_loc(seq_152)));
  seq_152 = pair_make(LAM, 0, SUB, seq_152);
  Term result_150 = term_new(VAR, 0, port(1, term_loc(seq_152)));
  // allocate args at core: 772

  Term result_150_1;
  result_150 = dupeArg(result_150, &result_150_1, 6);

  Term x_151_1;
  x_151 = dupeArg(x_151, &x_151_1, 6);
  Term glblVal130_17;
  glblVal130 = dupeArg(glblVal130, &glblVal130_17, 0);

  // call first at core: 772
  Term Rslt_148Args = pair_make(APP, 0, v_144, SUB);
  Term Rslt_148 = term_new(VAR, 0, port(2, term_loc(Rslt_148Args)));
  Rslt_148Args = pair_make(APP, 0, new_i60(772), Rslt_148Args);
  Rslt_148Args = pair_make(APP, 0, glblStr149, Rslt_148Args);
#ifdef STRICT
  store_redex(Rslt_148Args, glblProto72);
#else
  swapStore(term_loc(Rslt_148), pair_make(LAZ, 0, Rslt_148Args, glblProto72));
#endif
  // call pred at core: 771
  Term Rslt_154Args = pair_make(APP, 0, x_151, SUB);
  Term Rslt_154 = term_new(VAR, 0, port(2, term_loc(Rslt_154Args)));
#ifdef STRICT
  store_redex(Rslt_154Args, pred_145);
#else
  swapStore(term_loc(Rslt_154), pair_make(LAZ, 0, Rslt_154Args, pred_145));
#endif

  // call conj at core: 774
  Term Rslt_155Args = pair_make(APP, 0, x_151_1, SUB);
  Term Rslt_155 = term_new(VAR, 0, port(2, term_loc(Rslt_155Args)));
  Rslt_155Args = pair_make(APP, 0, result_150, Rslt_155Args);
  Rslt_155Args = pair_make(APP, 0, new_i60(774), Rslt_155Args);
  Rslt_155Args = pair_make(APP, 0, glblStr149, Rslt_155Args);
#ifdef STRICT
  store_redex(Rslt_155Args, glblProto64);
#else
  swapStore(term_loc(Rslt_155), pair_make(LAZ, 0, Rslt_155Args, glblProto64));
#endif
  // call cond at core: 773
  Term Rslt_156Args = pair_make(APP, 0, result_150_1, SUB);
  Term Rslt_156 = term_new(VAR, 0, port(2, term_loc(Rslt_156Args)));
  Rslt_156Args = pair_make(APP, 0, Rslt_155, Rslt_156Args);
  Rslt_156Args = pair_make(APP, 0, Rslt_154, Rslt_156Args);
  Rslt_156Args = pair_make(APP, 0, new_i60(773), Rslt_156Args);
  Rslt_156Args = pair_make(APP, 0, glblStr149, Rslt_156Args);
#ifdef STRICT
  store_redex(Rslt_156Args, glblProto30);
#else
  swapStore(term_loc(Rslt_156), pair_make(LAZ, 0, Rslt_156Args, glblProto30));
#endif
  // link args to body
  swapStore(r_seq_152, Rslt_156);

  Term Rslt_156V = get(term_loc(Rslt_156));
  apps = take(port(1, term_loc(Rslt_156V)));
  lams = take(port(2, term_loc(Rslt_156V)));

  vLoc = port(2, term_loc(apps));
  swapStore(term_loc(Rslt_156), pair_make(LAZ, 0, apps, lams));
  // call vect-reduce at core: 772
  Term Rslt_157Args = pair_make(APP, 0, seq_152, SUB);
  Term Rslt_157 = term_new(VAR, 0, port(2, term_loc(Rslt_157Args)));
  Rslt_157Args = pair_make(APP, 0, glblVal130_17, Rslt_157Args);
  Rslt_157Args = pair_make(APP, 0, new_i60(1), Rslt_157Args);
  Rslt_157Args = pair_make(APP, 0, v_144_1, Rslt_157Args);
  Rslt_157Args = pair_make(APP, 0, Rslt_148, Rslt_157Args);
  Rslt_157Args = pair_make(APP, 0, new_i60(772), Rslt_157Args);
  Rslt_157Args = pair_make(APP, 0, glblStr149, Rslt_157Args);
#ifdef STRICT
  store_redex(Rslt_157Args, glblProto358);
#else
  swapStore(term_loc(Rslt_157), pair_make(LAZ, 0, Rslt_157Args, glblProto358));
#endif
  // link args to body
  swapStore(r_seq_146, Rslt_157);

  Term Rslt_157V = get(term_loc(Rslt_157));
  apps = take(port(1, term_loc(Rslt_157V)));
  lams = take(port(2, term_loc(Rslt_157V)));

  vLoc = port(2, term_loc(apps));
  swapStore(term_loc(Rslt_157), pair_make(LAZ, 0, apps, lams));
  store_redex(args, seq_146);
  return;
}
Term glblfilter432 = new_ref(glblfilter431);
Term glblinterpose430;
void glblinterpose429(Term ref, Term args) {
  Term apps, lams, V;
  Location vLoc;
  Term seq_162 = pair_make(LAM, 0, SUB, NUL);
  Location r_seq_162 = port(2, term_loc(seq_162));
  Term sep_161 = term_new(VAR, 0, port(1, term_loc(seq_162)));
  seq_162 = pair_make(LAM, 0, SUB, seq_162);
  Term coll_160 = term_new(VAR, 0, port(1, term_loc(seq_162)));
  // allocate args at core: 777
  Term seq_166 = pair_make(LAM, 0, SUB, NUL);
  Location r_seq_166 = port(2, term_loc(seq_166));
  Term x_165 = term_new(VAR, 0, port(1, term_loc(seq_166)));
  // allocate args at core: 780
  Term glblVal130_18;
  glblVal130 = dupeArg(glblVal130, &glblVal130_18, 0);

  // call vect-conj at core: 780
  Term Rslt_168Args = pair_make(APP, 0, sep_161, SUB);
  Term Rslt_168 = term_new(VAR, 0, port(2, term_loc(Rslt_168Args)));
  Rslt_168Args = pair_make(APP, 0, glblVal130_18, Rslt_168Args);
#ifdef STRICT
  store_redex(Rslt_168Args, glblFn133);
#else
  swapStore(term_loc(Rslt_168), pair_make(LAZ, 0, Rslt_168Args, glblFn133));
#endif

  // call vect-conj at core: 780
  Term Rslt_169Args = pair_make(APP, 0, x_165, SUB);
  Term Rslt_169 = term_new(VAR, 0, port(2, term_loc(Rslt_169Args)));
  Rslt_169Args = pair_make(APP, 0, Rslt_168, Rslt_169Args);
#ifdef STRICT
  store_redex(Rslt_169Args, glblFn133);
#else
  swapStore(term_loc(Rslt_169), pair_make(LAZ, 0, Rslt_169Args, glblFn133));
#endif

  // link args to body
  swapStore(r_seq_166, Rslt_169);

  Term Rslt_169V = get(term_loc(Rslt_169));
  apps = take(port(1, term_loc(Rslt_169V)));
  lams = take(port(2, term_loc(Rslt_169V)));

  vLoc = port(2, term_loc(apps));
  swapStore(term_loc(Rslt_169), pair_make(LAZ, 0, apps, lams));
  // call flat-map at core: 780
  Term Rslt_170Args = pair_make(APP, 0, seq_166, SUB);
  Term Rslt_170 = term_new(VAR, 0, port(2, term_loc(Rslt_170Args)));
  Rslt_170Args = pair_make(APP, 0, coll_160, Rslt_170Args);
  Rslt_170Args = pair_make(APP, 0, new_i60(780), Rslt_170Args);
  Rslt_170Args = pair_make(APP, 0, glblStr149, Rslt_170Args);
#ifdef STRICT
  store_redex(Rslt_170Args, glblProto42);
#else
  swapStore(term_loc(Rslt_170), pair_make(LAZ, 0, Rslt_170Args, glblProto42));
#endif
  // call rest at core: 781
  Term Rslt_172Args = pair_make(APP, 0, Rslt_170, SUB);
  Term Rslt_172 = term_new(VAR, 0, port(2, term_loc(Rslt_172Args)));
  Rslt_172Args = pair_make(APP, 0, new_i60(781), Rslt_172Args);
  Rslt_172Args = pair_make(APP, 0, glblStr149, Rslt_172Args);
#ifdef STRICT
  store_redex(Rslt_172Args, glblProto74);
#else
  swapStore(term_loc(Rslt_172), pair_make(LAZ, 0, Rslt_172Args, glblProto74));
#endif
  // link args to body
  swapStore(r_seq_162, Rslt_172);

  Term Rslt_172V = get(term_loc(Rslt_172));
  apps = take(port(1, term_loc(Rslt_172V)));
  lams = take(port(2, term_loc(Rslt_172V)));

  vLoc = port(2, term_loc(apps));
  swapStore(term_loc(Rslt_172), pair_make(LAZ, 0, apps, lams));
  store_redex(args, seq_162);
  return;
}
Term glblinterpose430 = new_ref(glblinterpose429);
Term glblVal489 = new_ref(glblEndOfList486);
Term glblVal489;
Term glblRslt487;
Term glblrecurse483;
void glblrecurse481(Term ref, Term args) {
  Term apps, lams, V;
  Location vLoc;
  Term seq_3 = pair_make(LAM, 0, SUB, NUL);
  Location r_seq_3 = port(2, term_loc(seq_3));
  Term f_2 = term_new(VAR, 0, port(1, term_loc(seq_3)));
  seq_3 = pair_make(LAM, 0, SUB, seq_3);
  Term l_1 = term_new(VAR, 0, port(1, term_loc(seq_3)));
  // allocate args at core: 791
  swapStore(term_loc(f_2), ERA);
  // link args to body
  swapStore(r_seq_3, l_1);

  V = get(r_seq_3);
  apps = pair_make(APP, 0, V, SUB);
  lams = pair_make(LAM, 0, SUB, NUL);
  swapStore(port(2, term_loc(lams)), term_new(VAR, 0, port(1, term_loc(lams))));
  vLoc = port(2, term_loc(apps));
  swapStore(vLoc, pair_make(LAZ, 0, apps, lams));
  swapStore(r_seq_3, term_new(VAR, 0, vLoc));
  store_redex(args, seq_3);
  return;
}
Term glblrecurse483 = new_ref(glblrecurse481);
Term glblcount485;
void glblcount484(Term ref, Term args) {
  Term apps, lams, V;
  Location vLoc;
  Term seq_7 = pair_make(LAM, 0, SUB, NUL);
  Location r_seq_7 = port(2, term_loc(seq_7));
  Term l_6 = term_new(VAR, 0, port(1, term_loc(seq_7)));
  // allocate args at core: 797
  swapStore(term_loc(l_6), ERA);
  // link args to body
  swapStore(r_seq_7, new_i60(0));

  V = get(r_seq_7);
  apps = pair_make(APP, 0, V, SUB);
  lams = pair_make(LAM, 0, SUB, NUL);
  swapStore(port(2, term_loc(lams)), term_new(VAR, 0, port(1, term_loc(lams))));
  vLoc = port(2, term_loc(apps));
  swapStore(vLoc, pair_make(LAZ, 0, apps, lams));
  swapStore(r_seq_7, term_new(VAR, 0, vLoc));
  store_redex(args, seq_7);
  return;
}
Term glblcount485 = new_ref(glblcount484);
Term glblVal519 = new_ref(glblCons512);
Term glblVal519;
Term glblrecurse508;
void glblrecurse506(Term ref, Term args) {
  Term apps, lams, V;
  Location vLoc;
  Term seq_3 = pair_make(LAM, 0, SUB, NUL);
  Location r_seq_3 = port(2, term_loc(seq_3));
  Term f_2 = term_new(VAR, 0, port(1, term_loc(seq_3)));
  seq_3 = pair_make(LAM, 0, SUB, seq_3);
  Term arg_1 = term_new(VAR, 0, port(1, term_loc(seq_3)));
  // allocate args at core: 804

  Term arg_1_2;
  arg_1 = dupeArg(arg_1, &arg_1_2, 0);

  Term arg_1_1;
  arg_1 = dupeArg(arg_1, &arg_1_1, 0);
  Term arg_1_2Args = pair_make(APP, 0, arg_1_2, SUB);
  Term Rslt_8 = term_new(VAR, 0, port(2, term_loc(arg_1_2Args)));
  arg_1_2Args = pair_make(APP, 0, new_i60(2), arg_1_2Args);
  store_redex(arg_1_2Args, accessField);
  Term arg_1_1Args = pair_make(APP, 0, arg_1_1, SUB);
  Term Rslt_6 = term_new(VAR, 0, port(2, term_loc(arg_1_1Args)));
  arg_1_1Args = pair_make(APP, 0, new_i60(1), arg_1_1Args);
  store_redex(arg_1_1Args, accessField);
  Term arg_1Args = pair_make(APP, 0, arg_1, SUB);
  Term Rslt_5 = term_new(VAR, 0, port(2, term_loc(arg_1Args)));
  arg_1Args = pair_make(APP, 0, new_i60(0), arg_1Args);
  store_redex(arg_1Args, accessField);
  Term glblVal519_1;
  glblVal519 = dupeArg(glblVal519, &glblVal519_1, 0);

  // call f at core: 804
  Term Rslt_7Args = pair_make(APP, 0, Rslt_6, SUB);
  Term Rslt_7 = term_new(VAR, 0, port(2, term_loc(Rslt_7Args)));
#ifdef STRICT
  store_redex(Rslt_7Args, f_2);
#else
  swapStore(term_loc(Rslt_7), pair_make(LAZ, 0, Rslt_7Args, f_2));
#endif

  // call Cons at core: 805
  Term Rslt_9Args = pair_make(APP, 0, Rslt_8, SUB);
  Term Rslt_9 = term_new(VAR, 0, port(2, term_loc(Rslt_9Args)));
  Rslt_9Args = pair_make(APP, 0, Rslt_7, Rslt_9Args);
  Rslt_9Args = pair_make(APP, 0, Rslt_5, Rslt_9Args);
#ifdef STRICT
  store_redex(Rslt_9Args, glblVal519_1);
#else
  swapStore(term_loc(Rslt_9), pair_make(LAZ, 0, Rslt_9Args, glblVal519_1));
#endif

  // link args to body
  swapStore(r_seq_3, Rslt_9);

  Term Rslt_9V = get(term_loc(Rslt_9));
  apps = take(port(1, term_loc(Rslt_9V)));
  lams = take(port(2, term_loc(Rslt_9V)));

  vLoc = port(2, term_loc(apps));
  swapStore(term_loc(Rslt_9), pair_make(LAZ, 0, apps, lams));
  store_redex(args, seq_3);
  return;
}
Term glblrecurse508 = new_ref(glblrecurse506);
Term glblcount511;
void glblcount509(Term ref, Term args) {
  Term apps, lams, V;
  Location vLoc;
  Term seq_12 = pair_make(LAM, 0, SUB, NUL);
  Location r_seq_12 = port(2, term_loc(seq_12));
  Term l_11 = term_new(VAR, 0, port(1, term_loc(seq_12)));
  // allocate args at core: 810
  // call .len at core: 811
  Term Rslt_14Args = pair_make(APP, 0, l_11, SUB);
  Term Rslt_14 = term_new(VAR, 0, port(2, term_loc(Rslt_14Args)));
  Rslt_14Args = pair_make(APP, 0, new_i60(811), Rslt_14Args);
  Rslt_14Args = pair_make(APP, 0, glblStr149, Rslt_14Args);
#ifdef STRICT
  store_redex(Rslt_14Args, glblProto286);
#else
  swapStore(term_loc(Rslt_14), pair_make(LAZ, 0, Rslt_14Args, glblProto286));
#endif
  // link args to body
  swapStore(r_seq_12, Rslt_14);

  Term Rslt_14V = get(term_loc(Rslt_14));
  apps = take(port(1, term_loc(Rslt_14V)));
  lams = take(port(2, term_loc(Rslt_14V)));

  vLoc = port(2, term_loc(apps));
  swapStore(term_loc(Rslt_14), pair_make(LAZ, 0, apps, lams));
  store_redex(args, seq_12);
  return;
}
Term glblcount511 = new_ref(glblcount509);
Term glblFld514;
void glblFldFn557(Term ref, Term args) {
  args = pair_make(APP, 0, new_i60(0), args);
  store_redex(args, accessField);
  return;
}
Term glblFld514 = new_ref(glblFldFn557);
Term glblFld515;
void glblFldFn558(Term ref, Term args) {
  args = pair_make(APP, 0, new_i60(1), args);
  store_redex(args, accessField);
  return;
}
Term glblFld515 = new_ref(glblFldFn558);
Term glblFld516;
void glblFldFn559(Term ref, Term args) {
  args = pair_make(APP, 0, new_i60(2), args);
  store_redex(args, accessField);
  return;
}
Term glblFld516 = new_ref(glblFldFn559);
Term glblFn478;
void glblCFn477(Term ref, Term args) {
  Term apps, lams, V;
  Location vLoc;
  Term seq_3 = pair_make(LAM, 0, SUB, NUL);
  Location r_seq_3 = port(2, term_loc(seq_3));
  Term l_2 = term_new(VAR, 0, port(1, term_loc(seq_3)));
  seq_3 = pair_make(LAM, 0, SUB, seq_3);
  Term v_1 = term_new(VAR, 0, port(1, term_loc(seq_3)));
  // allocate args at core: 813

  Term l_2_1;
  l_2 = dupeArg(l_2, &l_2_1, 0);
  Term glblVal519_2;
  glblVal519 = dupeArg(glblVal519, &glblVal519_2, 0);

  // call count at core: 815
  Term Rslt_5Args = pair_make(APP, 0, l_2_1, SUB);
  Term Rslt_5 = term_new(VAR, 0, port(2, term_loc(Rslt_5Args)));
  Rslt_5Args = pair_make(APP, 0, new_i60(815), Rslt_5Args);
  Rslt_5Args = pair_make(APP, 0, glblStr149, Rslt_5Args);
#ifdef STRICT
  store_redex(Rslt_5Args, glblProto60);
#else
  swapStore(term_loc(Rslt_5), pair_make(LAZ, 0, Rslt_5Args, glblProto60));
#endif
  // call inc at core: 815
  Term Rslt_6Args = pair_make(APP, 0, Rslt_5, SUB);
  Term Rslt_6 = term_new(VAR, 0, port(2, term_loc(Rslt_6Args)));
#ifdef STRICT
  store_redex(Rslt_6Args, glblFn120);
#else
  swapStore(term_loc(Rslt_6), pair_make(LAZ, 0, Rslt_6Args, glblFn120));
#endif

  // call Cons at core: 815
  Term Rslt_7Args = pair_make(APP, 0, Rslt_6, SUB);
  Term Rslt_7 = term_new(VAR, 0, port(2, term_loc(Rslt_7Args)));
  Rslt_7Args = pair_make(APP, 0, l_2, Rslt_7Args);
  Rslt_7Args = pair_make(APP, 0, v_1, Rslt_7Args);
#ifdef STRICT
  store_redex(Rslt_7Args, glblVal519_2);
#else
  swapStore(term_loc(Rslt_7), pair_make(LAZ, 0, Rslt_7Args, glblVal519_2));
#endif

  // link args to body
  swapStore(r_seq_3, Rslt_7);

  Term Rslt_7V = get(term_loc(Rslt_7));
  apps = take(port(1, term_loc(Rslt_7V)));
  lams = take(port(2, term_loc(Rslt_7V)));

  vLoc = port(2, term_loc(apps));
  swapStore(term_loc(Rslt_7), pair_make(LAZ, 0, apps, lams));
  store_redex(args, seq_3);
  return;
}
Term glblFn478 = new_ref(glblCFn477);
Term glblFn480;
void glblCFn479(Term ref, Term args) {
  Term apps, lams, V;
  Location vLoc;
  Term seq_2 = pair_make(LAM, 0, SUB, NUL);
  Location r_seq_2 = port(2, term_loc(seq_2));
  Term vs_1 = term_new(VAR, 0, port(1, term_loc(seq_2)));
  // allocate args at core: 817
  // call to-str at core: 821
  Term Rslt_4Args = pair_make(APP, 0, vs_1, SUB);
  Term Rslt_4 = term_new(VAR, 0, port(2, term_loc(Rslt_4Args)));
#ifdef STRICT
  store_redex(Rslt_4Args, glblFn402);
#else
  swapStore(term_loc(Rslt_4), pair_make(LAZ, 0, Rslt_4Args, glblFn402));
#endif

  // link args to body
  swapStore(r_seq_2, Rslt_4);

  Term Rslt_4V = get(term_loc(Rslt_4));
  apps = take(port(1, term_loc(Rslt_4V)));
  lams = take(port(2, term_loc(Rslt_4V)));

  vLoc = port(2, term_loc(apps));
  swapStore(term_loc(Rslt_4), pair_make(LAZ, 0, apps, lams));
  store_redex(args, seq_2);
  return;
}
Term glblFn480 = new_ref(glblCFn479);
Value *(*type_name)(FnArity *, Value *) = noImpl1;
Value *(*zero)(FnArity *, Value *) = noImpl1;
Value *(*invoke0Args)(FnArity *, Value *) = noImpl1;
Value *(*invoke1Arg)(FnArity *, Value *, Value *) = noImpl2;
Value *(*invoke2Args)(FnArity *, Value *, Value *, Value *) = noImpl3;
Value *(*equalSTAR)(FnArity *, Value *, Value *) = noImpl2;
Value *(*hashSeq)(FnArity *, Value *, Value *) = noImpl2;
Value *(*count)(FnArity *, Value *) = noImpl1;
Value *(*vals)(FnArity *, Value *) = noImpl1;
Value *(*first)(FnArity *, Value *) = noImpl1;
Value *(*rest)(FnArity *, Value *) = noImpl1;
Value *(*seq)(FnArity *, Value *) = noImpl1;
Value *(*sha1)(FnArity *, Value *) = noImpl1;
Value *(*hasField)(FnArity *, Value *, Value *) = noImpl2;
Value *(*showFn)(FnArity *, Value *) = noImpl1;
Value *(*newHashSet)(FnArity *, Value *) = noImpl1;
Value *(*fn_apply)(FnArity *, Value *, Value *) = noImpl2;
Value *(*prValue)(FnArity *, Value *) = noImpl1;
Term glblFn564;
void glblCFn563(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 2, &arityArgs);
  Term result = NUL;
  if (arityArgs.count == 2) {
    Term x_1 = arityArgs.args[0];
    Term y_2 = arityArgs.args[1];

    unsigned x = (unsigned)get_i60(x_1);
    unsigned y = (unsigned)get_i60(y_2);
    result = new_i60(x & y);

    moveStore(port(2, term_loc(args)), result);
  }
  return;
}
Term glblFn564 = new_ref(glblCFn563);
Term glblFn566;
void glblCFn565(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 2, &arityArgs);
  Term result = NUL;
  if (arityArgs.count == 2) {
    Term x_1 = arityArgs.args[0];
    Term n_2 = arityArgs.args[1];

    unsigned x = (unsigned)get_i60(x_1);
    unsigned n = (unsigned)get_i60(n_2);
    result = new_i60(x >> n);

    moveStore(port(2, term_loc(args)), result);
  }
  return;
}
Term glblFn566 = new_ref(glblCFn565);
Term glblVal568;
Term glblVal570;
struct {
  TYPE_SIZE type;
  REFS_SIZE refs;
  int64_t *hash;
  Value *parent;
  int64_t len;
  char buffer[2];
} str11 = {StringBufferType, REFS_STATIC, 0, 0, 1, "0"};
Term glblStr573 = term_new_(VAL, (Term)&str11);
Term glblVal568;
void glblCFn571(Term ref, Term args) {
  Term apps, lams, V;
  Location vLoc;
  Term seq_2 = pair_make(LAM, 0, SUB, NUL);
  Location r_seq_2 = port(2, term_loc(seq_2));
  Term p_1 = term_new(VAR, 0, port(1, term_loc(seq_2)));
  // allocate args at sidequest.toc: 20
  Term glblVal570_1;
  glblVal570 = dupeArg(glblVal570, &glblVal570_1, 0);

  // call pr* at sidequest.toc: 21
  Term Rslt_4Args = pair_make(APP, 0, glblStr573, SUB);
  Term Rslt_4 = term_new(VAR, 0, port(2, term_loc(Rslt_4Args)));
#ifdef STRICT
  store_redex(Rslt_4Args, glblFn114);
#else
  swapStore(term_loc(Rslt_4), pair_make(LAZ, 0, Rslt_4Args, glblFn114));
#endif

  // call p at sidequest.toc: 20
  Term Rslt_5Args = pair_make(APP, 0, glblFn251, SUB);
  Term Rslt_5 = term_new(VAR, 0, port(2, term_loc(Rslt_5Args)));
  Rslt_5Args = pair_make(APP, 0, glblVal570_1, Rslt_5Args);
  Rslt_5Args = pair_make(APP, 0, glblVal568, Rslt_5Args);
#ifdef STRICT
  store_redex(Rslt_5Args, p_1);
#else
  swapStore(term_loc(Rslt_5), pair_make(LAZ, 0, Rslt_5Args, p_1));
#endif

  // link args to body
  swapStore(r_seq_2, Rslt_5);

  Term Rslt_5V = get(term_loc(Rslt_5));
  apps = take(port(1, term_loc(Rslt_5V)));
  lams = take(port(2, term_loc(Rslt_5V)));

  vLoc = port(2, term_loc(apps));
  apps = pair_make(APP, 0, Rslt_4, apps);
  lams = pair_make(LAM, 0, sideEffects, lams);

  swapStore(term_loc(Rslt_5), pair_make(LAZ, 0, apps, lams));
  store_redex(args, seq_2);
  return;
}
Term glblVal568 = new_ref(glblCFn571);
struct {
  TYPE_SIZE type;
  REFS_SIZE refs;
  int64_t *hash;
  Value *parent;
  int64_t len;
  char buffer[2];
} str12 = {StringBufferType, REFS_STATIC, 0, 0, 1, "1"};
Term glblStr577 = term_new_(VAL, (Term)&str12);
Term glblVal570;
void glblCFn575(Term ref, Term args) {
  Term apps, lams, V;
  Location vLoc;
  Term seq_2 = pair_make(LAM, 0, SUB, NUL);
  Location r_seq_2 = port(2, term_loc(seq_2));
  Term p_1 = term_new(VAR, 0, port(1, term_loc(seq_2)));
  // allocate args at sidequest.toc: 24
  // call pr* at sidequest.toc: 25
  Term Rslt_4Args = pair_make(APP, 0, glblStr577, SUB);
  Term Rslt_4 = term_new(VAR, 0, port(2, term_loc(Rslt_4Args)));
#ifdef STRICT
  store_redex(Rslt_4Args, glblFn114);
#else
  swapStore(term_loc(Rslt_4), pair_make(LAZ, 0, Rslt_4Args, glblFn114));
#endif

  // call p at sidequest.toc: 24
  Term Rslt_5Args = pair_make(APP, 0, glblFn251, SUB);
  Term Rslt_5 = term_new(VAR, 0, port(2, term_loc(Rslt_5Args)));
  Rslt_5Args = pair_make(APP, 0, glblVal570, Rslt_5Args);
  Rslt_5Args = pair_make(APP, 0, glblVal568, Rslt_5Args);
#ifdef STRICT
  store_redex(Rslt_5Args, p_1);
#else
  swapStore(term_loc(Rslt_5), pair_make(LAZ, 0, Rslt_5Args, p_1));
#endif

  // link args to body
  swapStore(r_seq_2, Rslt_5);

  Term Rslt_5V = get(term_loc(Rslt_5));
  apps = take(port(1, term_loc(Rslt_5V)));
  lams = take(port(2, term_loc(Rslt_5V)));

  vLoc = port(2, term_loc(apps));
  apps = pair_make(APP, 0, Rslt_4, apps);
  lams = pair_make(LAM, 0, sideEffects, lams);

  swapStore(term_loc(Rslt_5), pair_make(LAZ, 0, apps, lams));
  store_redex(args, seq_2);
  return;
}
Term glblVal570 = new_ref(glblCFn575);
Term glblFn580;
void glblCFn579(Term ref, Term args) {
  Term apps, lams, V;
  Location vLoc;
  Term seq_2 = pair_make(LAM, 0, SUB, NUL);
  Location r_seq_2 = port(2, term_loc(seq_2));
  Term bits_1 = term_new(VAR, 0, port(1, term_loc(seq_2)));
  // allocate args at sidequest.toc: 28
  // call pr* at sidequest.toc: 29
  Term Rslt_4Args = pair_make(APP, 0, glblStr159, SUB);
  Term Rslt_4 = term_new(VAR, 0, port(2, term_loc(Rslt_4Args)));
#ifdef STRICT
  store_redex(Rslt_4Args, glblFn114);
#else
  swapStore(term_loc(Rslt_4), pair_make(LAZ, 0, Rslt_4Args, glblFn114));
#endif

  // call bits at sidequest.toc: 28
  Term Rslt_5Args = pair_make(APP, 0, glblVal568, SUB);
  Term Rslt_5 = term_new(VAR, 0, port(2, term_loc(Rslt_5Args)));
#ifdef STRICT
  store_redex(Rslt_5Args, bits_1);
#else
  swapStore(term_loc(Rslt_5), pair_make(LAZ, 0, Rslt_5Args, bits_1));
#endif

  // call  at sidequest.toc: 30
  Term Rslt_6Args = pair_make(APP, 0, glblVal570, SUB);
  Term Rslt_6 = term_new(VAR, 0, port(2, term_loc(Rslt_6Args)));
#ifdef STRICT
  store_redex(Rslt_6Args, Rslt_5);
#else
  swapStore(term_loc(Rslt_6), pair_make(LAZ, 0, Rslt_6Args, Rslt_5));
#endif

  // call  at sidequest.toc: 30
  Term Rslt_7Args = pair_make(APP, 0, glblFn251, SUB);
  Term Rslt_7 = term_new(VAR, 0, port(2, term_loc(Rslt_7Args)));
#ifdef STRICT
  store_redex(Rslt_7Args, Rslt_6);
#else
  swapStore(term_loc(Rslt_7), pair_make(LAZ, 0, Rslt_7Args, Rslt_6));
#endif

  // link args to body
  swapStore(r_seq_2, Rslt_7);

  Term Rslt_7V = get(term_loc(Rslt_7));
  apps = take(port(1, term_loc(Rslt_7V)));
  lams = take(port(2, term_loc(Rslt_7V)));

  vLoc = port(2, term_loc(apps));
  apps = pair_make(APP, 0, Rslt_4, apps);
  lams = pair_make(LAM, 0, sideEffects, lams);

  swapStore(term_loc(Rslt_7), pair_make(LAZ, 0, apps, lams));
  store_redex(args, seq_2);
  return;
}
Term glblFn580 = new_ref(glblCFn579);
struct {
  TYPE_SIZE type;
  REFS_SIZE refs;
  int64_t *hash;
  Value *parent;
  int64_t len;
  char buffer[14];
} str13 = {StringBufferType, REFS_STATIC, 0, 0, 13, "sidequest.toc"};
Term glblStr588 = term_new_(VAL, (Term)&str13);
Term glblc587;
void glblc586(Term ref, Term args) {
  Term apps, lams, V;
  Location vLoc;
  Term seq_8 = pair_make(LAM, 0, SUB, NUL);
  Location r_seq_8 = port(2, term_loc(seq_8));
  Term o_7 = term_new(VAR, 0, port(1, term_loc(seq_8)));
  // allocate args at sidequest.toc: 56
  swapStore(term_loc(o_7), ERA);
  Term seq_13 = pair_make(LAM, 0, SUB, NUL);
  Location r_seq_13 = port(2, term_loc(seq_13));
  Term e_12 = term_new(VAR, 0, port(1, term_loc(seq_13)));
  seq_13 = pair_make(LAM, 0, SUB, seq_13);
  Term i_11 = term_new(VAR, 0, port(1, term_loc(seq_13)));
  // allocate args at sidequest.toc: 57
  swapStore(term_loc(i_11), ERA);
  // link args to body
  swapStore(r_seq_13, e_12);

  V = get(r_seq_13);
  apps = pair_make(APP, 0, V, SUB);
  lams = pair_make(LAM, 0, SUB, NUL);
  swapStore(port(2, term_loc(lams)), term_new(VAR, 0, port(1, term_loc(lams))));
  vLoc = port(2, term_loc(apps));
  swapStore(vLoc, pair_make(LAZ, 0, apps, lams));
  swapStore(r_seq_13, term_new(VAR, 0, vLoc));
  // link args to body
  swapStore(r_seq_8, seq_13);

  V = get(r_seq_8);
  apps = pair_make(APP, 0, V, SUB);
  lams = pair_make(LAM, 0, SUB, NUL);
  swapStore(port(2, term_loc(lams)), term_new(VAR, 0, port(1, term_loc(lams))));
  vLoc = port(2, term_loc(apps));
  swapStore(vLoc, pair_make(LAZ, 0, apps, lams));
  swapStore(r_seq_8, term_new(VAR, 0, vLoc));
  store_redex(args, seq_8);
  return;
}
Term glblc587 = new_ref(glblc586);
Term glblFn584;
void glblCFn583(Term ref, Term args) {
  Term apps, lams, V;
  Location vLoc;
  Term seq_3 = pair_make(LAM, 0, SUB, NUL);
  Location r_seq_3 = port(2, term_loc(seq_3));
  Term n_2 = term_new(VAR, 0, port(1, term_loc(seq_3)));
  seq_3 = pair_make(LAM, 0, SUB, seq_3);
  Term bits_1 = term_new(VAR, 0, port(1, term_loc(seq_3)));
  // allocate args at sidequest.toc: 54

  Term bits_1_2;
  bits_1 = dupeArg(bits_1, &bits_1_2, 0);

  Term bits_1_1;
  bits_1 = dupeArg(bits_1, &bits_1_1, 0);

  Term n_2_2;
  n_2 = dupeArg(n_2, &n_2_2, 0);

  Term n_2_1;
  n_2 = dupeArg(n_2, &n_2_1, 0);
  Term seq_32 = pair_make(LAM, 0, SUB, NUL);
  Location r_seq_32 = port(2, term_loc(seq_32));
  Term o_31 = term_new(VAR, 0, port(1, term_loc(seq_32)));
  // allocate args at sidequest.toc: 67
  Term seq_37 = pair_make(LAM, 0, SUB, NUL);
  Location r_seq_37 = port(2, term_loc(seq_37));
  Term e_36 = term_new(VAR, 0, port(1, term_loc(seq_37)));
  seq_37 = pair_make(LAM, 0, SUB, seq_37);
  Term i_35 = term_new(VAR, 0, port(1, term_loc(seq_37)));
  // allocate args at sidequest.toc: 68
  swapStore(term_loc(i_35), ERA);
  swapStore(term_loc(e_36), ERA);
  Term seq_19 = pair_make(LAM, 0, SUB, NUL);
  Location r_seq_19 = port(2, term_loc(seq_19));
  Term o_18 = term_new(VAR, 0, port(1, term_loc(seq_19)));
  // allocate args at sidequest.toc: 62
  swapStore(term_loc(o_18), ERA);
  Term seq_24 = pair_make(LAM, 0, SUB, NUL);
  Location r_seq_24 = port(2, term_loc(seq_24));
  Term e_23 = term_new(VAR, 0, port(1, term_loc(seq_24)));
  seq_24 = pair_make(LAM, 0, SUB, seq_24);
  Term i_22 = term_new(VAR, 0, port(1, term_loc(seq_24)));
  // allocate args at sidequest.toc: 63
  swapStore(term_loc(e_23), ERA);
  // call = at sidequest.toc: 55
  Term Rslt_5Args = pair_make(APP, 0, bits_1, SUB);
  Term Rslt_5 = term_new(VAR, 0, port(2, term_loc(Rslt_5Args)));
  Rslt_5Args = pair_make(APP, 0, new_i60(0), Rslt_5Args);
  Rslt_5Args = pair_make(APP, 0, new_i60(55), Rslt_5Args);
  Rslt_5Args = pair_make(APP, 0, glblStr588, Rslt_5Args);
#ifdef STRICT
  store_redex(Rslt_5Args, glblProto19);
#else
  swapStore(term_loc(Rslt_5), pair_make(LAZ, 0, Rslt_5Args, glblProto19));
#endif
  // call bit-and at sidequest.toc: 60
  Term Rslt_15Args = pair_make(APP, 0, new_i60(1), SUB);
  Term Rslt_15 = term_new(VAR, 0, port(2, term_loc(Rslt_15Args)));
  Rslt_15Args = pair_make(APP, 0, n_2, Rslt_15Args);
#ifdef STRICT
  store_redex(Rslt_15Args, glblFn564);
#else
  swapStore(term_loc(Rslt_15), pair_make(LAZ, 0, Rslt_15Args, glblFn564));
#endif

  // call = at sidequest.toc: 60
  Term Rslt_16Args = pair_make(APP, 0, Rslt_15, SUB);
  Term Rslt_16 = term_new(VAR, 0, port(2, term_loc(Rslt_16Args)));
  Rslt_16Args = pair_make(APP, 0, new_i60(1), Rslt_16Args);
  Rslt_16Args = pair_make(APP, 0, new_i60(60), Rslt_16Args);
  Rslt_16Args = pair_make(APP, 0, glblStr588, Rslt_16Args);
#ifdef STRICT
  store_redex(Rslt_16Args, glblProto19);
#else
  swapStore(term_loc(Rslt_16), pair_make(LAZ, 0, Rslt_16Args, glblProto19));
#endif
  // call dec at sidequest.toc: 64
  Term Rslt_26Args = pair_make(APP, 0, bits_1_1, SUB);
  Term Rslt_26 = term_new(VAR, 0, port(2, term_loc(Rslt_26Args)));
#ifdef STRICT
  store_redex(Rslt_26Args, glblFn126);
#else
  swapStore(term_loc(Rslt_26), pair_make(LAZ, 0, Rslt_26Args, glblFn126));
#endif

  // call >> at sidequest.toc: 64
  Term Rslt_27Args = pair_make(APP, 0, new_i60(1), SUB);
  Term Rslt_27 = term_new(VAR, 0, port(2, term_loc(Rslt_27Args)));
  Rslt_27Args = pair_make(APP, 0, n_2_1, Rslt_27Args);
#ifdef STRICT
  store_redex(Rslt_27Args, glblFn566);
#else
  swapStore(term_loc(Rslt_27), pair_make(LAZ, 0, Rslt_27Args, glblFn566));
#endif

  // call bin at sidequest.toc: 54
  Term Rslt_28Args = pair_make(APP, 0, Rslt_27, SUB);
  Term Rslt_28 = term_new(VAR, 0, port(2, term_loc(Rslt_28Args)));
  Rslt_28Args = pair_make(APP, 0, Rslt_26, Rslt_28Args);
#ifdef STRICT
  store_redex(Rslt_28Args, glblFn584);
#else
  swapStore(term_loc(Rslt_28), pair_make(LAZ, 0, Rslt_28Args, glblFn584));
#endif

  // call i at sidequest.toc: 63
  Term Rslt_29Args = pair_make(APP, 0, Rslt_28, SUB);
  Term Rslt_29 = term_new(VAR, 0, port(2, term_loc(Rslt_29Args)));
#ifdef STRICT
  store_redex(Rslt_29Args, i_22);
#else
  swapStore(term_loc(Rslt_29), pair_make(LAZ, 0, Rslt_29Args, i_22));
#endif

  // link args to body
  swapStore(r_seq_24, Rslt_29);

  Term Rslt_29V = get(term_loc(Rslt_29));
  apps = take(port(1, term_loc(Rslt_29V)));
  lams = take(port(2, term_loc(Rslt_29V)));

  vLoc = port(2, term_loc(apps));
  swapStore(term_loc(Rslt_29), pair_make(LAZ, 0, apps, lams));
  // link args to body
  swapStore(r_seq_19, seq_24);

  V = get(r_seq_19);
  apps = pair_make(APP, 0, V, SUB);
  lams = pair_make(LAM, 0, SUB, NUL);
  swapStore(port(2, term_loc(lams)), term_new(VAR, 0, port(1, term_loc(lams))));
  vLoc = port(2, term_loc(apps));
  swapStore(vLoc, pair_make(LAZ, 0, apps, lams));
  swapStore(r_seq_19, term_new(VAR, 0, vLoc));
  // call dec at sidequest.toc: 69
  Term Rslt_39Args = pair_make(APP, 0, bits_1_2, SUB);
  Term Rslt_39 = term_new(VAR, 0, port(2, term_loc(Rslt_39Args)));
#ifdef STRICT
  store_redex(Rslt_39Args, glblFn126);
#else
  swapStore(term_loc(Rslt_39), pair_make(LAZ, 0, Rslt_39Args, glblFn126));
#endif

  // call >> at sidequest.toc: 69
  Term Rslt_40Args = pair_make(APP, 0, new_i60(1), SUB);
  Term Rslt_40 = term_new(VAR, 0, port(2, term_loc(Rslt_40Args)));
  Rslt_40Args = pair_make(APP, 0, n_2_2, Rslt_40Args);
#ifdef STRICT
  store_redex(Rslt_40Args, glblFn566);
#else
  swapStore(term_loc(Rslt_40), pair_make(LAZ, 0, Rslt_40Args, glblFn566));
#endif

  // call bin at sidequest.toc: 54
  Term Rslt_41Args = pair_make(APP, 0, Rslt_40, SUB);
  Term Rslt_41 = term_new(VAR, 0, port(2, term_loc(Rslt_41Args)));
  Rslt_41Args = pair_make(APP, 0, Rslt_39, Rslt_41Args);
#ifdef STRICT
  store_redex(Rslt_41Args, glblFn584);
#else
  swapStore(term_loc(Rslt_41), pair_make(LAZ, 0, Rslt_41Args, glblFn584));
#endif

  // call o at sidequest.toc: 67
  Term Rslt_42Args = pair_make(APP, 0, Rslt_41, SUB);
  Term Rslt_42 = term_new(VAR, 0, port(2, term_loc(Rslt_42Args)));
#ifdef STRICT
  store_redex(Rslt_42Args, o_31);
#else
  swapStore(term_loc(Rslt_42), pair_make(LAZ, 0, Rslt_42Args, o_31));
#endif

  // link args to body
  swapStore(r_seq_37, Rslt_42);

  Term Rslt_42V = get(term_loc(Rslt_42));
  apps = take(port(1, term_loc(Rslt_42V)));
  lams = take(port(2, term_loc(Rslt_42V)));

  vLoc = port(2, term_loc(apps));
  swapStore(term_loc(Rslt_42), pair_make(LAZ, 0, apps, lams));
  // link args to body
  swapStore(r_seq_32, seq_37);

  V = get(r_seq_32);
  apps = pair_make(APP, 0, V, SUB);
  lams = pair_make(LAM, 0, SUB, NUL);
  swapStore(port(2, term_loc(lams)), term_new(VAR, 0, port(1, term_loc(lams))));
  vLoc = port(2, term_loc(apps));
  swapStore(vLoc, pair_make(LAZ, 0, apps, lams));
  swapStore(r_seq_32, term_new(VAR, 0, vLoc));
  // call cond at sidequest.toc: 60
  Term Rslt_43Args = pair_make(APP, 0, seq_32, SUB);
  Term Rslt_43 = term_new(VAR, 0, port(2, term_loc(Rslt_43Args)));
  Rslt_43Args = pair_make(APP, 0, seq_19, Rslt_43Args);
  Rslt_43Args = pair_make(APP, 0, Rslt_16, Rslt_43Args);
  Rslt_43Args = pair_make(APP, 0, new_i60(60), Rslt_43Args);
  Rslt_43Args = pair_make(APP, 0, glblStr588, Rslt_43Args);
#ifdef STRICT
  store_redex(Rslt_43Args, glblProto30);
#else
  swapStore(term_loc(Rslt_43), pair_make(LAZ, 0, Rslt_43Args, glblProto30));
#endif
  // call cond at sidequest.toc: 55
  Term Rslt_46Args = pair_make(APP, 0, Rslt_43, SUB);
  Term Rslt_46 = term_new(VAR, 0, port(2, term_loc(Rslt_46Args)));
  Rslt_46Args = pair_make(APP, 0, glblc587, Rslt_46Args);
  Rslt_46Args = pair_make(APP, 0, Rslt_5, Rslt_46Args);
  Rslt_46Args = pair_make(APP, 0, new_i60(55), Rslt_46Args);
  Rslt_46Args = pair_make(APP, 0, glblStr588, Rslt_46Args);
#ifdef STRICT
  store_redex(Rslt_46Args, glblProto30);
#else
  swapStore(term_loc(Rslt_46), pair_make(LAZ, 0, Rslt_46Args, glblProto30));
#endif
  // link args to body
  swapStore(r_seq_3, Rslt_46);

  Term Rslt_46V = get(term_loc(Rslt_46));
  apps = take(port(1, term_loc(Rslt_46V)));
  lams = take(port(2, term_loc(Rslt_46V)));

  vLoc = port(2, term_loc(apps));
  swapStore(term_loc(Rslt_46), pair_make(LAZ, 0, apps, lams));
  store_redex(args, seq_3);
  return;
}
Term glblFn584 = new_ref(glblCFn583);
Term glblVal585;
Term glblc598;
void glblc597(Term ref, Term args) {
  Term apps, lams, V;
  Location vLoc;
  Term seq_20 = pair_make(LAM, 0, SUB, NUL);
  Location r_seq_20 = port(2, term_loc(seq_20));
  Term f_19 = term_new(VAR, 0, port(1, term_loc(seq_20)));
  // allocate args at sidequest.toc: 89
  swapStore(term_loc(f_19), ERA);
  // link args to body
  swapStore(r_seq_20, glblFn251);

  V = get(r_seq_20);
  apps = pair_make(APP, 0, V, SUB);
  lams = pair_make(LAM, 0, SUB, NUL);
  swapStore(port(2, term_loc(lams)), term_new(VAR, 0, port(1, term_loc(lams))));
  vLoc = port(2, term_loc(apps));
  swapStore(vLoc, pair_make(LAZ, 0, apps, lams));
  swapStore(r_seq_20, term_new(VAR, 0, vLoc));
  store_redex(args, seq_20);
  return;
}
Term glblc598 = new_ref(glblc597);
Term glblc600;
void glblc599(Term ref, Term args) {
  Term apps, lams, V;
  Location vLoc;
  Term seq_25 = pair_make(LAM, 0, SUB, NUL);
  Location r_seq_25 = port(2, term_loc(seq_25));
  Term p_24 = term_new(VAR, 0, port(1, term_loc(seq_25)));
  // allocate args at sidequest.toc: 84
  Term seq_29 = pair_make(LAM, 0, SUB, NUL);
  Location r_seq_29 = port(2, term_loc(seq_29));
  Term f_28 = term_new(VAR, 0, port(1, term_loc(seq_29)));
  // allocate args at sidequest.toc: 85

  Term f_28_2;
  f_28 = dupeArg(f_28, &f_28_2, 0);

  Term f_28_1;
  f_28 = dupeArg(f_28, &f_28_1, 0);
  Term seq_33 = pair_make(LAM, 0, SUB, NUL);
  Location r_seq_33 = port(2, term_loc(seq_33));
  Term x_32 = term_new(VAR, 0, port(1, term_loc(seq_33)));
  // allocate args at sidequest.toc: 86
  Term seq_38 = pair_make(LAM, 0, SUB, NUL);
  Location r_seq_38 = port(2, term_loc(seq_38));
  Term k_37 = term_new(VAR, 0, port(1, term_loc(seq_38)));
  // allocate args at sidequest.toc: 87
  // call rep at sidequest.toc: 78
  Term Rslt_35Args = pair_make(APP, 0, p_24, SUB);
  Term Rslt_35 = term_new(VAR, 0, port(2, term_loc(Rslt_35Args)));
#ifdef STRICT
  store_redex(Rslt_35Args, glblVal585);
#else
  swapStore(term_loc(Rslt_35), pair_make(LAZ, 0, Rslt_35Args, glblVal585));
#endif

  // call f at sidequest.toc: 85
  Term Rslt_40Args = pair_make(APP, 0, k_37, SUB);
  Term Rslt_40 = term_new(VAR, 0, port(2, term_loc(Rslt_40Args)));
#ifdef STRICT
  store_redex(Rslt_40Args, f_28_1);
#else
  swapStore(term_loc(Rslt_40), pair_make(LAZ, 0, Rslt_40Args, f_28_1));
#endif

  // call f at sidequest.toc: 85
  Term Rslt_41Args = pair_make(APP, 0, Rslt_40, SUB);
  Term Rslt_41 = term_new(VAR, 0, port(2, term_loc(Rslt_41Args)));
#ifdef STRICT
  store_redex(Rslt_41Args, f_28);
#else
  swapStore(term_loc(Rslt_41), pair_make(LAZ, 0, Rslt_41Args, f_28));
#endif

  // link args to body
  swapStore(r_seq_38, Rslt_41);

  Term Rslt_41V = get(term_loc(Rslt_41));
  apps = take(port(1, term_loc(Rslt_41V)));
  lams = take(port(2, term_loc(Rslt_41V)));

  vLoc = port(2, term_loc(apps));
  swapStore(term_loc(Rslt_41), pair_make(LAZ, 0, apps, lams));
  // call  at sidequest.toc: 87
  Term Rslt_42Args = pair_make(APP, 0, seq_38, SUB);
  Term Rslt_42 = term_new(VAR, 0, port(2, term_loc(Rslt_42Args)));
#ifdef STRICT
  store_redex(Rslt_42Args, Rslt_35);
#else
  swapStore(term_loc(Rslt_42), pair_make(LAZ, 0, Rslt_42Args, Rslt_35));
#endif

  // call f at sidequest.toc: 85
  Term Rslt_44Args = pair_make(APP, 0, x_32, SUB);
  Term Rslt_44 = term_new(VAR, 0, port(2, term_loc(Rslt_44Args)));
#ifdef STRICT
  store_redex(Rslt_44Args, f_28_2);
#else
  swapStore(term_loc(Rslt_44), pair_make(LAZ, 0, Rslt_44Args, f_28_2));
#endif

  // call  at sidequest.toc: 85
  Term Rslt_45Args = pair_make(APP, 0, Rslt_44, SUB);
  Term Rslt_45 = term_new(VAR, 0, port(2, term_loc(Rslt_45Args)));
#ifdef STRICT
  store_redex(Rslt_45Args, Rslt_42);
#else
  swapStore(term_loc(Rslt_45), pair_make(LAZ, 0, Rslt_45Args, Rslt_42));
#endif

  // link args to body
  swapStore(r_seq_33, Rslt_45);

  Term Rslt_45V = get(term_loc(Rslt_45));
  apps = take(port(1, term_loc(Rslt_45V)));
  lams = take(port(2, term_loc(Rslt_45V)));

  vLoc = port(2, term_loc(apps));
  swapStore(term_loc(Rslt_45), pair_make(LAZ, 0, apps, lams));
  // link args to body
  swapStore(r_seq_29, seq_33);

  V = get(r_seq_29);
  apps = pair_make(APP, 0, V, SUB);
  lams = pair_make(LAM, 0, SUB, NUL);
  swapStore(port(2, term_loc(lams)), term_new(VAR, 0, port(1, term_loc(lams))));
  vLoc = port(2, term_loc(apps));
  swapStore(vLoc, pair_make(LAZ, 0, apps, lams));
  swapStore(r_seq_29, term_new(VAR, 0, vLoc));
  // link args to body
  swapStore(r_seq_25, seq_29);

  V = get(r_seq_25);
  apps = pair_make(APP, 0, V, SUB);
  lams = pair_make(LAM, 0, SUB, NUL);
  swapStore(port(2, term_loc(lams)), term_new(VAR, 0, port(1, term_loc(lams))));
  vLoc = port(2, term_loc(apps));
  swapStore(vLoc, pair_make(LAZ, 0, apps, lams));
  swapStore(r_seq_25, term_new(VAR, 0, vLoc));
  store_redex(args, seq_25);
  return;
}
Term glblc600 = new_ref(glblc599);
Term glblc602;
void glblc601(Term ref, Term args) {
  Term apps, lams, V;
  Location vLoc;
  Term seq_49 = pair_make(LAM, 0, SUB, NUL);
  Location r_seq_49 = port(2, term_loc(seq_49));
  Term p_48 = term_new(VAR, 0, port(1, term_loc(seq_49)));
  // allocate args at sidequest.toc: 79
  Term seq_53 = pair_make(LAM, 0, SUB, NUL);
  Location r_seq_53 = port(2, term_loc(seq_53));
  Term f_52 = term_new(VAR, 0, port(1, term_loc(seq_53)));
  // allocate args at sidequest.toc: 80

  Term f_52_1;
  f_52 = dupeArg(f_52, &f_52_1, 0);
  Term seq_57 = pair_make(LAM, 0, SUB, NUL);
  Location r_seq_57 = port(2, term_loc(seq_57));
  Term x_56 = term_new(VAR, 0, port(1, term_loc(seq_57)));
  // allocate args at sidequest.toc: 81
  Term seq_62 = pair_make(LAM, 0, SUB, NUL);
  Location r_seq_62 = port(2, term_loc(seq_62));
  Term k_61 = term_new(VAR, 0, port(1, term_loc(seq_62)));
  // allocate args at sidequest.toc: 82
  // call rep at sidequest.toc: 78
  Term Rslt_59Args = pair_make(APP, 0, p_48, SUB);
  Term Rslt_59 = term_new(VAR, 0, port(2, term_loc(Rslt_59Args)));
#ifdef STRICT
  store_redex(Rslt_59Args, glblVal585);
#else
  swapStore(term_loc(Rslt_59), pair_make(LAZ, 0, Rslt_59Args, glblVal585));
#endif

  // call f at sidequest.toc: 80
  Term Rslt_64Args = pair_make(APP, 0, k_61, SUB);
  Term Rslt_64 = term_new(VAR, 0, port(2, term_loc(Rslt_64Args)));
#ifdef STRICT
  store_redex(Rslt_64Args, f_52_1);
#else
  swapStore(term_loc(Rslt_64), pair_make(LAZ, 0, Rslt_64Args, f_52_1));
#endif

  // call f at sidequest.toc: 80
  Term Rslt_65Args = pair_make(APP, 0, Rslt_64, SUB);
  Term Rslt_65 = term_new(VAR, 0, port(2, term_loc(Rslt_65Args)));
#ifdef STRICT
  store_redex(Rslt_65Args, f_52);
#else
  swapStore(term_loc(Rslt_65), pair_make(LAZ, 0, Rslt_65Args, f_52));
#endif

  // link args to body
  swapStore(r_seq_62, Rslt_65);

  Term Rslt_65V = get(term_loc(Rslt_65));
  apps = take(port(1, term_loc(Rslt_65V)));
  lams = take(port(2, term_loc(Rslt_65V)));

  vLoc = port(2, term_loc(apps));
  swapStore(term_loc(Rslt_65), pair_make(LAZ, 0, apps, lams));
  // call  at sidequest.toc: 82
  Term Rslt_66Args = pair_make(APP, 0, seq_62, SUB);
  Term Rslt_66 = term_new(VAR, 0, port(2, term_loc(Rslt_66Args)));
#ifdef STRICT
  store_redex(Rslt_66Args, Rslt_59);
#else
  swapStore(term_loc(Rslt_66), pair_make(LAZ, 0, Rslt_66Args, Rslt_59));
#endif

  // call  at sidequest.toc: 81
  Term Rslt_68Args = pair_make(APP, 0, x_56, SUB);
  Term Rslt_68 = term_new(VAR, 0, port(2, term_loc(Rslt_68Args)));
#ifdef STRICT
  store_redex(Rslt_68Args, Rslt_66);
#else
  swapStore(term_loc(Rslt_68), pair_make(LAZ, 0, Rslt_68Args, Rslt_66));
#endif

  // link args to body
  swapStore(r_seq_57, Rslt_68);

  Term Rslt_68V = get(term_loc(Rslt_68));
  apps = take(port(1, term_loc(Rslt_68V)));
  lams = take(port(2, term_loc(Rslt_68V)));

  vLoc = port(2, term_loc(apps));
  swapStore(term_loc(Rslt_68), pair_make(LAZ, 0, apps, lams));
  // link args to body
  swapStore(r_seq_53, seq_57);

  V = get(r_seq_53);
  apps = pair_make(APP, 0, V, SUB);
  lams = pair_make(LAM, 0, SUB, NUL);
  swapStore(port(2, term_loc(lams)), term_new(VAR, 0, port(1, term_loc(lams))));
  vLoc = port(2, term_loc(apps));
  swapStore(vLoc, pair_make(LAZ, 0, apps, lams));
  swapStore(r_seq_53, term_new(VAR, 0, vLoc));
  // link args to body
  swapStore(r_seq_49, seq_53);

  V = get(r_seq_49);
  apps = pair_make(APP, 0, V, SUB);
  lams = pair_make(LAM, 0, SUB, NUL);
  swapStore(port(2, term_loc(lams)), term_new(VAR, 0, port(1, term_loc(lams))));
  vLoc = port(2, term_loc(apps));
  swapStore(vLoc, pair_make(LAZ, 0, apps, lams));
  swapStore(r_seq_49, term_new(VAR, 0, vLoc));
  store_redex(args, seq_49);
  return;
}
Term glblc602 = new_ref(glblc601);
Term glblVal585;
void glblCFn594(Term ref, Term args) {
  Term apps, lams, V;
  Location vLoc;
  Term seq_2 = pair_make(LAM, 0, SUB, NUL);
  Location r_seq_2 = port(2, term_loc(seq_2));
  Term xs_1 = term_new(VAR, 0, port(1, term_loc(seq_2)));
  // allocate args at sidequest.toc: 78
  Term seq_6 = pair_make(LAM, 0, SUB, NUL);
  Location r_seq_6 = port(2, term_loc(seq_6));
  Term O_5 = term_new(VAR, 0, port(1, term_loc(seq_6)));
  // allocate args at sidequest.toc: 79
  Term seq_10 = pair_make(LAM, 0, SUB, NUL);
  Location r_seq_10 = port(2, term_loc(seq_10));
  Term I_9 = term_new(VAR, 0, port(1, term_loc(seq_10)));
  // allocate args at sidequest.toc: 84
  Term seq_14 = pair_make(LAM, 0, SUB, NUL);
  Location r_seq_14 = port(2, term_loc(seq_14));
  Term E_13 = term_new(VAR, 0, port(1, term_loc(seq_14)));
  // allocate args at sidequest.toc: 89
  // call xs at sidequest.toc: 78
  Term Rslt_16Args = pair_make(APP, 0, O_5, SUB);
  Term Rslt_16 = term_new(VAR, 0, port(2, term_loc(Rslt_16Args)));
#ifdef STRICT
  store_redex(Rslt_16Args, xs_1);
#else
  swapStore(term_loc(Rslt_16), pair_make(LAZ, 0, Rslt_16Args, xs_1));
#endif

  // call  at sidequest.toc: 84
  Term Rslt_17Args = pair_make(APP, 0, E_13, SUB);
  Term Rslt_17 = term_new(VAR, 0, port(2, term_loc(Rslt_17Args)));
  Rslt_17Args = pair_make(APP, 0, I_9, Rslt_17Args);
#ifdef STRICT
  store_redex(Rslt_17Args, Rslt_16);
#else
  swapStore(term_loc(Rslt_17), pair_make(LAZ, 0, Rslt_17Args, Rslt_16));
#endif

  // link args to body
  swapStore(r_seq_14, Rslt_17);

  Term Rslt_17V = get(term_loc(Rslt_17));
  apps = take(port(1, term_loc(Rslt_17V)));
  lams = take(port(2, term_loc(Rslt_17V)));

  vLoc = port(2, term_loc(apps));
  swapStore(term_loc(Rslt_17), pair_make(LAZ, 0, apps, lams));
  // call  at : 0
  Term Rslt_22Args = pair_make(APP, 0, glblc598, SUB);
  Term Rslt_22 = term_new(VAR, 0, port(2, term_loc(Rslt_22Args)));
#ifdef STRICT
  store_redex(Rslt_22Args, seq_14);
#else
  swapStore(term_loc(Rslt_22), pair_make(LAZ, 0, Rslt_22Args, seq_14));
#endif

  // link args to body
  swapStore(r_seq_10, Rslt_22);

  Term Rslt_22V = get(term_loc(Rslt_22));
  apps = take(port(1, term_loc(Rslt_22V)));
  lams = take(port(2, term_loc(Rslt_22V)));

  vLoc = port(2, term_loc(apps));
  swapStore(term_loc(Rslt_22), pair_make(LAZ, 0, apps, lams));
  // call  at : 0
  Term Rslt_46Args = pair_make(APP, 0, glblc600, SUB);
  Term Rslt_46 = term_new(VAR, 0, port(2, term_loc(Rslt_46Args)));
#ifdef STRICT
  store_redex(Rslt_46Args, seq_10);
#else
  swapStore(term_loc(Rslt_46), pair_make(LAZ, 0, Rslt_46Args, seq_10));
#endif

  // link args to body
  swapStore(r_seq_6, Rslt_46);

  Term Rslt_46V = get(term_loc(Rslt_46));
  apps = take(port(1, term_loc(Rslt_46V)));
  lams = take(port(2, term_loc(Rslt_46V)));

  vLoc = port(2, term_loc(apps));
  swapStore(term_loc(Rslt_46), pair_make(LAZ, 0, apps, lams));
  // call  at : 0
  Term Rslt_69Args = pair_make(APP, 0, glblc602, SUB);
  Term Rslt_69 = term_new(VAR, 0, port(2, term_loc(Rslt_69Args)));
#ifdef STRICT
  store_redex(Rslt_69Args, seq_6);
#else
  swapStore(term_loc(Rslt_69), pair_make(LAZ, 0, Rslt_69Args, seq_6));
#endif

  // link args to body
  swapStore(r_seq_2, Rslt_69);

  Term Rslt_69V = get(term_loc(Rslt_69));
  apps = take(port(1, term_loc(Rslt_69V)));
  lams = take(port(2, term_loc(Rslt_69V)));

  vLoc = port(2, term_loc(apps));
  swapStore(term_loc(Rslt_69), pair_make(LAZ, 0, apps, lams));
  store_redex(args, seq_2);
  return;
}
Term glblVal585 = new_ref(glblCFn594);
Term glblVal596;
struct {
  TYPE_SIZE type;
  REFS_SIZE refs;
  int64_t *hash;
  Value *parent;
  int64_t len;
  char buffer[5];
} str14 = {StringBufferType, REFS_STATIC, 0, 0, 4, "clr\n"};
Term glblStr607 = term_new_(VAL, (Term)&str14);
Term glblVal596;
void glblCFn603(Term ref, Term args) {
  Term apps, lams, V;
  Location vLoc;
  Term seq_2 = pair_make(LAM, 0, SUB, NUL);
  Location r_seq_2 = port(2, term_loc(seq_2));
  Term x_1 = term_new(VAR, 0, port(1, term_loc(seq_2)));
  // allocate args at sidequest.toc: 109
  Term seq_7 = pair_make(LAM, 0, SUB, NUL);
  Location r_seq_7 = port(2, term_loc(seq_7));
  Term o_6 = term_new(VAR, 0, port(1, term_loc(seq_7)));
  // allocate args at sidequest.toc: 111

  Term o_6_1;
  o_6 = dupeArg(o_6, &o_6_1, 0);
  Term seq_12 = pair_make(LAM, 0, SUB, NUL);
  Location r_seq_12 = port(2, term_loc(seq_12));
  Term e_11 = term_new(VAR, 0, port(1, term_loc(seq_12)));
  seq_12 = pair_make(LAM, 0, SUB, seq_12);
  Term i_10 = term_new(VAR, 0, port(1, term_loc(seq_12)));
  // allocate args at sidequest.toc: 112
  swapStore(term_loc(i_10), ERA);
  Term seq_24 = pair_make(LAM, 0, SUB, NUL);
  Location r_seq_24 = port(2, term_loc(seq_24));
  Term p_23 = term_new(VAR, 0, port(1, term_loc(seq_24)));
  // allocate args at sidequest.toc: 114
  Term seq_16 = pair_make(LAM, 0, SUB, NUL);
  Location r_seq_16 = port(2, term_loc(seq_16));
  Term p_15 = term_new(VAR, 0, port(1, term_loc(seq_16)));
  // allocate args at sidequest.toc: 113
  // call pr* at sidequest.toc: 110
  Term Rslt_4Args = pair_make(APP, 0, glblStr607, SUB);
  Term Rslt_4 = term_new(VAR, 0, port(2, term_loc(Rslt_4Args)));
#ifdef STRICT
  store_redex(Rslt_4Args, glblFn114);
#else
  swapStore(term_loc(Rslt_4), pair_make(LAZ, 0, Rslt_4Args, glblFn114));
#endif

  // call clr at sidequest.toc: 109
  Term Rslt_18Args = pair_make(APP, 0, p_15, SUB);
  Term Rslt_18 = term_new(VAR, 0, port(2, term_loc(Rslt_18Args)));
#ifdef STRICT
  store_redex(Rslt_18Args, glblVal596);
#else
  swapStore(term_loc(Rslt_18), pair_make(LAZ, 0, Rslt_18Args, glblVal596));
#endif

  // call o at sidequest.toc: 111
  Term Rslt_19Args = pair_make(APP, 0, Rslt_18, SUB);
  Term Rslt_19 = term_new(VAR, 0, port(2, term_loc(Rslt_19Args)));
#ifdef STRICT
  store_redex(Rslt_19Args, o_6);
#else
  swapStore(term_loc(Rslt_19), pair_make(LAZ, 0, Rslt_19Args, o_6));
#endif

  // link args to body
  swapStore(r_seq_16, Rslt_19);

  Term Rslt_19V = get(term_loc(Rslt_19));
  apps = take(port(1, term_loc(Rslt_19V)));
  lams = take(port(2, term_loc(Rslt_19V)));

  vLoc = port(2, term_loc(apps));
  swapStore(term_loc(Rslt_19), pair_make(LAZ, 0, apps, lams));
  // call x at sidequest.toc: 109
  Term Rslt_20Args = pair_make(APP, 0, seq_16, SUB);
  Term Rslt_20 = term_new(VAR, 0, port(2, term_loc(Rslt_20Args)));
#ifdef STRICT
  store_redex(Rslt_20Args, x_1);
#else
  swapStore(term_loc(Rslt_20), pair_make(LAZ, 0, Rslt_20Args, x_1));
#endif

  // call clr at sidequest.toc: 109
  Term Rslt_26Args = pair_make(APP, 0, p_23, SUB);
  Term Rslt_26 = term_new(VAR, 0, port(2, term_loc(Rslt_26Args)));
#ifdef STRICT
  store_redex(Rslt_26Args, glblVal596);
#else
  swapStore(term_loc(Rslt_26), pair_make(LAZ, 0, Rslt_26Args, glblVal596));
#endif

  // call o at sidequest.toc: 111
  Term Rslt_27Args = pair_make(APP, 0, Rslt_26, SUB);
  Term Rslt_27 = term_new(VAR, 0, port(2, term_loc(Rslt_27Args)));
#ifdef STRICT
  store_redex(Rslt_27Args, o_6_1);
#else
  swapStore(term_loc(Rslt_27), pair_make(LAZ, 0, Rslt_27Args, o_6_1));
#endif

  // link args to body
  swapStore(r_seq_24, Rslt_27);

  Term Rslt_27V = get(term_loc(Rslt_27));
  apps = take(port(1, term_loc(Rslt_27V)));
  lams = take(port(2, term_loc(Rslt_27V)));

  vLoc = port(2, term_loc(apps));
  swapStore(term_loc(Rslt_27), pair_make(LAZ, 0, apps, lams));
  // call  at sidequest.toc: 114
  Term Rslt_28Args = pair_make(APP, 0, e_11, SUB);
  Term Rslt_28 = term_new(VAR, 0, port(2, term_loc(Rslt_28Args)));
  Rslt_28Args = pair_make(APP, 0, seq_24, Rslt_28Args);
#ifdef STRICT
  store_redex(Rslt_28Args, Rslt_20);
#else
  swapStore(term_loc(Rslt_28), pair_make(LAZ, 0, Rslt_28Args, Rslt_20));
#endif

  // link args to body
  swapStore(r_seq_12, Rslt_28);

  Term Rslt_28V = get(term_loc(Rslt_28));
  apps = take(port(1, term_loc(Rslt_28V)));
  lams = take(port(2, term_loc(Rslt_28V)));

  vLoc = port(2, term_loc(apps));
  swapStore(term_loc(Rslt_28), pair_make(LAZ, 0, apps, lams));
  // link args to body
  swapStore(r_seq_7, seq_12);

  V = get(r_seq_7);
  apps = pair_make(APP, 0, V, SUB);
  lams = pair_make(LAM, 0, SUB, NUL);
  swapStore(port(2, term_loc(lams)), term_new(VAR, 0, port(1, term_loc(lams))));
  vLoc = port(2, term_loc(apps));
  swapStore(vLoc, pair_make(LAZ, 0, apps, lams));
  swapStore(r_seq_7, term_new(VAR, 0, vLoc));
  // link args to body
  swapStore(r_seq_2, seq_7);

  V = get(r_seq_2);
  apps = pair_make(APP, 0, V, SUB);
  lams = pair_make(LAM, 0, SUB, NUL);
  swapStore(port(2, term_loc(lams)), term_new(VAR, 0, port(1, term_loc(lams))));
  vLoc = port(2, term_loc(apps));
  apps = pair_make(APP, 0, Rslt_4, apps);
  lams = pair_make(LAM, 0, sideEffects, lams);

  swapStore(vLoc, pair_make(LAZ, 0, apps, lams));
  swapStore(r_seq_2, term_new(VAR, 0, vLoc));
  store_redex(args, seq_2);
  return;
}
Term glblVal596 = new_ref(glblCFn603);
Term glblFn611;
void glblCFn610(Term ref, Term args) {
  Term apps, lams, V;
  Location vLoc;
  Term seq_2 = pair_make(LAM, 0, SUB, NUL);
  Location r_seq_2 = port(2, term_loc(seq_2));
  Term arg_1 = term_new(VAR, 0, port(1, term_loc(seq_2)));
  // allocate args at sidequest.toc: 122
  swapStore(term_loc(arg_1), ERA);
  // call bin at sidequest.toc: 127
  Term Rslt_4Args = pair_make(APP, 0, new_i60(2), SUB);
  Term Rslt_4 = term_new(VAR, 0, port(2, term_loc(Rslt_4Args)));
  Rslt_4Args = pair_make(APP, 0, new_i60(3), Rslt_4Args);
#ifdef STRICT
  store_redex(Rslt_4Args, glblFn584);
#else
  swapStore(term_loc(Rslt_4), pair_make(LAZ, 0, Rslt_4Args, glblFn584));
#endif

  // call clr at sidequest.toc: 127
  Term Rslt_5Args = pair_make(APP, 0, Rslt_4, SUB);
  Term Rslt_5 = term_new(VAR, 0, port(2, term_loc(Rslt_5Args)));
#ifdef STRICT
  store_redex(Rslt_5Args, glblVal596);
#else
  swapStore(term_loc(Rslt_5), pair_make(LAZ, 0, Rslt_5Args, glblVal596));
#endif

  // link args to body
  swapStore(r_seq_2, new_i60(0));

  V = get(r_seq_2);
  apps = pair_make(APP, 0, V, SUB);
  lams = pair_make(LAM, 0, SUB, NUL);
  swapStore(port(2, term_loc(lams)), term_new(VAR, 0, port(1, term_loc(lams))));
  vLoc = port(2, term_loc(apps));
  apps = pair_make(APP, 0, Rslt_5, apps);
  lams = pair_make(LAM, 0, sideEffects, lams);

  swapStore(vLoc, pair_make(LAZ, 0, apps, lams));
  swapStore(r_seq_2, term_new(VAR, 0, vLoc));
  graphDown("main", seq_2);
  store_redex(args, seq_2);
  return;
}
Term glblFn611 = new_ref(glblCFn610);
Term glblProto50;
void glblCFn51(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 3, &arityArgs);
  if (arityArgs.count == 3) {
    String *fileStr = (String *)arityArgs.args[0];
    Value *dispVal = (Value *)arityArgs.args[2];
    swapStore(term_loc(args), (Term)dispVal);
    if (term_tag((Term)dispVal) == I60) {
      fprintf(stderr,
              "No implementation of 'wrap' found for integers called from "
              "%.*s: %ld\n",
              (int)fileStr->len, fileStr->buffer, get_i60(arityArgs.args[1]));
      BOOM("Dispatch failed");
    } else {
      switch (dispVal->type) {
      case 8:
        store_redex(args, glblwrap438);
        break;
      default: {
        fprintf(stderr,
                "No implementation of 'wrap' found for type %s (%ld) called "
                "from %.*s: %ld\n",
                typeName(dispVal->type), dispVal->type, (int)fileStr->len,
                fileStr->buffer, get_i60(arityArgs.args[1]));
        BOOM("Dispatch failed");
      }
      }
    }
  }
  return;
}
Term glblProto50 = new_ref(glblCFn51);
Term glblProto68;
void glblCFn69(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 3, &arityArgs);
  if (arityArgs.count == 3) {
    String *fileStr = (String *)arityArgs.args[0];
    Value *dispVal = (Value *)arityArgs.args[2];
    swapStore(term_loc(args), (Term)dispVal);
    if (term_tag((Term)dispVal) == I60) {
      fprintf(stderr,
              "No implementation of 'reduce' found for integers called from "
              "%.*s: %ld\n",
              (int)fileStr->len, fileStr->buffer, get_i60(arityArgs.args[1]));
      BOOM("Dispatch failed");
    } else {
      switch (dispVal->type) {
      case 8:
        store_redex(args, glblreduce444);
        break;
      default: {
        fprintf(stderr,
                "No implementation of 'reduce' found for type %s (%ld) called "
                "from %.*s: %ld\n",
                typeName(dispVal->type), dispVal->type, (int)fileStr->len,
                fileStr->buffer, get_i60(arityArgs.args[1]));
        BOOM("Dispatch failed");
      }
      }
    }
  }
  return;
}
Term glblProto68 = new_ref(glblCFn69);
Term glblProto104;
void glblCFn105(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 3, &arityArgs);
  if (arityArgs.count == 3) {
    String *fileStr = (String *)arityArgs.args[0];
    Value *dispVal = (Value *)arityArgs.args[2];
    swapStore(term_loc(args), (Term)dispVal);
    if (term_tag((Term)dispVal) == I60) {
      fprintf(stderr,
              "No implementation of 'vals' found for integers called from "
              "%.*s: %ld\n",
              (int)fileStr->len, fileStr->buffer, get_i60(arityArgs.args[1]));
      BOOM("Dispatch failed");
    } else {
      switch (dispVal->type) {
      default: {
        fprintf(stderr,
                "No implementation of 'vals' found for type %s (%ld) called "
                "from %.*s: %ld\n",
                typeName(dispVal->type), dispVal->type, (int)fileStr->len,
                fileStr->buffer, get_i60(arityArgs.args[1]));
        BOOM("Dispatch failed");
      }
      }
    }
  }
  return;
}
Term glblProto104 = new_ref(glblCFn105);
Term glblProto62;
void glblCFn63(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 3, &arityArgs);
  if (arityArgs.count == 3) {
    String *fileStr = (String *)arityArgs.args[0];
    Value *dispVal = (Value *)arityArgs.args[2];
    swapStore(term_loc(args), (Term)dispVal);
    if (term_tag((Term)dispVal) == I60) {
      fprintf(stderr,
              "No implementation of 'empty' found for integers called from "
              "%.*s: %ld\n",
              (int)fileStr->len, fileStr->buffer, get_i60(arityArgs.args[1]));
      BOOM("Dispatch failed");
    } else {
      switch (dispVal->type) {
      case 8:
        store_redex(args, glblempty458);
        break;
      default: {
        fprintf(stderr,
                "No implementation of 'empty' found for type %s (%ld) called "
                "from %.*s: %ld\n",
                typeName(dispVal->type), dispVal->type, (int)fileStr->len,
                fileStr->buffer, get_i60(arityArgs.args[1]));
        BOOM("Dispatch failed");
      }
      }
    }
  }
  return;
}
Term glblProto62 = new_ref(glblCFn63);
Term glblProto80;
void glblCFn81(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 3, &arityArgs);
  if (arityArgs.count == 3) {
    String *fileStr = (String *)arityArgs.args[0];
    Value *dispVal = (Value *)arityArgs.args[2];
    swapStore(term_loc(args), (Term)dispVal);
    if (term_tag((Term)dispVal) == I60) {
      fprintf(stderr,
              "No implementation of 'split' found for integers called from "
              "%.*s: %ld\n",
              (int)fileStr->len, fileStr->buffer, get_i60(arityArgs.args[1]));
      BOOM("Dispatch failed");
    } else {
      switch (dispVal->type) {
      default: {
        fprintf(stderr,
                "No implementation of 'split' found for type %s (%ld) called "
                "from %.*s: %ld\n",
                typeName(dispVal->type), dispVal->type, (int)fileStr->len,
                fileStr->buffer, get_i60(arityArgs.args[1]));
        BOOM("Dispatch failed");
      }
      }
    }
  }
  return;
}
Term glblProto80 = new_ref(glblCFn81);
Term glblProto88;
void glblCFn89(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 3, &arityArgs);
  if (arityArgs.count == 3) {
    String *fileStr = (String *)arityArgs.args[0];
    Value *dispVal = (Value *)arityArgs.args[2];
    swapStore(term_loc(args), (Term)dispVal);
    if (term_tag((Term)dispVal) == I60) {
      fprintf(stderr,
              "No implementation of 'drop-while' found for integers called "
              "from %.*s: %ld\n",
              (int)fileStr->len, fileStr->buffer, get_i60(arityArgs.args[1]));
      BOOM("Dispatch failed");
    } else {
      switch (dispVal->type) {
      default: {
        fprintf(stderr,
                "No implementation of 'drop-while' found for type %s (%ld) "
                "called from %.*s: %ld\n",
                typeName(dispVal->type), dispVal->type, (int)fileStr->len,
                fileStr->buffer, get_i60(arityArgs.args[1]));
        BOOM("Dispatch failed");
      }
      }
    }
  }
  return;
}
Term glblProto88 = new_ref(glblCFn89);
Term glblProto36;
void glblCFn37(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 3, &arityArgs);
  if (arityArgs.count == 3) {
    String *fileStr = (String *)arityArgs.args[0];
    Value *dispVal = (Value *)arityArgs.args[2];
    swapStore(term_loc(args), (Term)dispVal);
    if (term_tag((Term)dispVal) == I60) {
      fprintf(stderr,
              "No implementation of 'recurse' found for integers called from "
              "%.*s: %ld\n",
              (int)fileStr->len, fileStr->buffer, get_i60(arityArgs.args[1]));
      BOOM("Dispatch failed");
    } else {
      switch (dispVal->type) {
      case 2:
        store_redex(args, glblrecurse329);
        break;
      case 5:
        store_redex(args, glblrecurse327);
        break;
      case 46:
        store_redex(args, glblrecurse483);
        break;
      case 43:
        store_redex(args, glblrecurse188);
        break;
      case 45:
        store_redex(args, glblrecurse508);
        break;
      case 42:
        store_redex(args, glblrecurse146);
        break;
      default: {
        fprintf(stderr,
                "No implementation of 'recurse' found for type %s (%ld) called "
                "from %.*s: %ld\n",
                typeName(dispVal->type), dispVal->type, (int)fileStr->len,
                fileStr->buffer, get_i60(arityArgs.args[1]));
        BOOM("Dispatch failed");
      }
      }
    }
  }
  return;
}
Term glblProto36 = new_ref(glblCFn37);
Term glblProto282;
void glblFldFn283(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 3, &arityArgs);
  if (arityArgs.count == 3) {
    String *fileStr = (String *)arityArgs.args[0];
    Value *dispVal = (Value *)arityArgs.args[2];
    swapStore(term_loc(args), (Term)dispVal);
    if (term_tag((Term)dispVal) == I60) {
      fprintf(stderr,
              "No implementation of '.s' found for integers called from %.*s: "
              "%ld\n",
              (int)fileStr->len, fileStr->buffer, get_i60(arityArgs.args[1]));
      BOOM("Dispatch failed");
    } else {
      switch (dispVal->type) {
      case 5:
        store_redex(args, glblFld297);
        break;
      default: {
        fprintf(stderr,
                "No implementation of '.s' found for type %s (%ld) called from "
                "%.*s: %ld\n",
                typeName(dispVal->type), dispVal->type, (int)fileStr->len,
                fileStr->buffer, get_i60(arityArgs.args[1]));
        BOOM("Dispatch failed");
      }
      }
    }
  }
  return;
}
Term glblProto282 = new_ref(glblFldFn283);
Term glblProto499;
void glblFldFn501(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 3, &arityArgs);
  if (arityArgs.count == 3) {
    String *fileStr = (String *)arityArgs.args[0];
    Value *dispVal = (Value *)arityArgs.args[2];
    swapStore(term_loc(args), (Term)dispVal);
    if (term_tag((Term)dispVal) == I60) {
      fprintf(stderr,
              "No implementation of '.head' found for integers called from "
              "%.*s: %ld\n",
              (int)fileStr->len, fileStr->buffer, get_i60(arityArgs.args[1]));
      BOOM("Dispatch failed");
    } else {
      switch (dispVal->type) {
      case 45:
        store_redex(args, glblFld514);
        break;
      default: {
        fprintf(stderr,
                "No implementation of '.head' found for type %s (%ld) called "
                "from %.*s: %ld\n",
                typeName(dispVal->type), dispVal->type, (int)fileStr->len,
                fileStr->buffer, get_i60(arityArgs.args[1]));
        BOOM("Dispatch failed");
      }
      }
    }
  }
  return;
}
Term glblProto499 = new_ref(glblFldFn501);
Term glblProto28;
void glblCFn29(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 3, &arityArgs);
  if (arityArgs.count == 3) {
    String *fileStr = (String *)arityArgs.args[0];
    Value *dispVal = (Value *)arityArgs.args[2];
    swapStore(term_loc(args), (Term)dispVal);
    if (term_tag((Term)dispVal) == I60) {
      fprintf(stderr,
              "No implementation of 'either' found for integers called from "
              "%.*s: %ld\n",
              (int)fileStr->len, fileStr->buffer, get_i60(arityArgs.args[1]));
      BOOM("Dispatch failed");
    } else {
      switch (dispVal->type) {
      case 43:
        store_redex(args, glbleither186);
        break;
      case 42:
        store_redex(args, glbleither144);
        break;
      default: {
        fprintf(stderr,
                "No implementation of 'either' found for type %s (%ld) called "
                "from %.*s: %ld\n",
                typeName(dispVal->type), dispVal->type, (int)fileStr->len,
                fileStr->buffer, get_i60(arityArgs.args[1]));
        BOOM("Dispatch failed");
      }
      }
    }
  }
  return;
}
Term glblProto28 = new_ref(glblCFn29);
Term glblProto92;
void glblCFn93(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 3, &arityArgs);
  if (arityArgs.count == 3) {
    String *fileStr = (String *)arityArgs.args[0];
    Value *dispVal = (Value *)arityArgs.args[2];
    swapStore(term_loc(args), (Term)dispVal);
    if (term_tag((Term)dispVal) == I60) {
      fprintf(stderr,
              "No implementation of 'reverse' found for integers called from "
              "%.*s: %ld\n",
              (int)fileStr->len, fileStr->buffer, get_i60(arityArgs.args[1]));
      BOOM("Dispatch failed");
    } else {
      switch (dispVal->type) {
      case 8:
        store_redex(args, glblreverse442);
        break;
      default: {
        fprintf(stderr,
                "No implementation of 'reverse' found for type %s (%ld) called "
                "from %.*s: %ld\n",
                typeName(dispVal->type), dispVal->type, (int)fileStr->len,
                fileStr->buffer, get_i60(arityArgs.args[1]));
        BOOM("Dispatch failed");
      }
      }
    }
  }
  return;
}
Term glblProto92 = new_ref(glblCFn93);
Term glblProto19;
void glblCFn20(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 3, &arityArgs);
  if (arityArgs.count == 3) {
    String *fileStr = (String *)arityArgs.args[0];
    Value *dispVal = (Value *)arityArgs.args[2];
    swapStore(term_loc(args), (Term)dispVal);
    if (term_tag((Term)dispVal) == I60) {
      store_redex(args, glbl_EQ_234);
    } else {
      switch (dispVal->type) {
      case 2:
        store_redex(args, glbl_EQ_314);
        break;
      case 5:
        store_redex(args, glbl_EQ_293);
        break;
      case 43:
        store_redex(args, glbl_EQ_192);
        break;
      case 8:
        store_redex(args, glbl_EQ_472);
        break;
      case 42:
        store_redex(args, glbl_EQ_148);
        break;
      default: {
        fprintf(stderr,
                "No implementation of '=' found for type %s (%ld) called from "
                "%.*s: %ld\n",
                typeName(dispVal->type), dispVal->type, (int)fileStr->len,
                fileStr->buffer, get_i60(arityArgs.args[1]));
        BOOM("Dispatch failed");
      }
      }
    }
  }
  return;
}
Term glblProto19 = new_ref(glblCFn20);
Term glblProto94;
void glblCFn95(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 3, &arityArgs);
  if (arityArgs.count == 3) {
    String *fileStr = (String *)arityArgs.args[0];
    Value *dispVal = (Value *)arityArgs.args[2];
    swapStore(term_loc(args), (Term)dispVal);
    if (term_tag((Term)dispVal) == I60) {
      fprintf(stderr,
              "No implementation of 'nth' found for integers called from %.*s: "
              "%ld\n",
              (int)fileStr->len, fileStr->buffer, get_i60(arityArgs.args[1]));
      BOOM("Dispatch failed");
    } else {
      switch (dispVal->type) {
      case 8:
        store_redex(args, glblnth462);
        break;
      default: {
        fprintf(stderr,
                "No implementation of 'nth' found for type %s (%ld) called "
                "from %.*s: %ld\n",
                typeName(dispVal->type), dispVal->type, (int)fileStr->len,
                fileStr->buffer, get_i60(arityArgs.args[1]));
        BOOM("Dispatch failed");
      }
      }
    }
  }
  return;
}
Term glblProto94 = new_ref(glblCFn95);
Term glblProto3;
void glblCFn4(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 3, &arityArgs);
  if (arityArgs.count == 3) {
    String *fileStr = (String *)arityArgs.args[0];
    Value *dispVal = (Value *)arityArgs.args[2];
    swapStore(term_loc(args), (Term)dispVal);
    if (term_tag((Term)dispVal) == I60) {
      store_redex(args, glbltype_name240);
    } else {
      switch (dispVal->type) {
      case 2:
        store_redex(args, glbltype_name337);
        break;
      case 5:
        store_redex(args, glbltype_name335);
        break;
      case 8:
        store_redex(args, glbltype_name470);
        break;
      default: {
        store_redex(args, glbltype_name6);
      }
      }
    }
  }
  return;
}
Term glblProto3 = new_ref(glblCFn4);
Term glblProto24;
void glblCFn25(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 3, &arityArgs);
  if (arityArgs.count == 3) {
    String *fileStr = (String *)arityArgs.args[0];
    Value *dispVal = (Value *)arityArgs.args[2];
    swapStore(term_loc(args), (Term)dispVal);
    if (term_tag((Term)dispVal) == I60) {
      fprintf(stderr,
              "No implementation of 'and' found for integers called from %.*s: "
              "%ld\n",
              (int)fileStr->len, fileStr->buffer, get_i60(arityArgs.args[1]));
      BOOM("Dispatch failed");
    } else {
      switch (dispVal->type) {
      case 43:
        store_redex(args, glbland182);
        break;
      case 42:
        store_redex(args, glbland140);
        break;
      default: {
        fprintf(stderr,
                "No implementation of 'and' found for type %s (%ld) called "
                "from %.*s: %ld\n",
                typeName(dispVal->type), dispVal->type, (int)fileStr->len,
                fileStr->buffer, get_i60(arityArgs.args[1]));
        BOOM("Dispatch failed");
      }
      }
    }
  }
  return;
}
Term glblProto24 = new_ref(glblCFn25);
Term glblProto276;
void glblCFn277(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 3, &arityArgs);
  if (arityArgs.count == 3) {
    String *fileStr = (String *)arityArgs.args[0];
    Value *dispVal = (Value *)arityArgs.args[2];
    swapStore(term_loc(args), (Term)dispVal);
    if (term_tag((Term)dispVal) == I60) {
      fprintf(stderr,
              "No implementation of 'subs' found for integers called from "
              "%.*s: %ld\n",
              (int)fileStr->len, fileStr->buffer, get_i60(arityArgs.args[1]));
      BOOM("Dispatch failed");
    } else {
      switch (dispVal->type) {
      case 2:
        store_redex(args, glblsubs312);
        break;
      case 5:
        store_redex(args, glblsubs295);
        break;
      default: {
        fprintf(stderr,
                "No implementation of 'subs' found for type %s (%ld) called "
                "from %.*s: %ld\n",
                typeName(dispVal->type), dispVal->type, (int)fileStr->len,
                fileStr->buffer, get_i60(arityArgs.args[1]));
        BOOM("Dispatch failed");
      }
      }
    }
  }
  return;
}
Term glblProto276 = new_ref(glblCFn277);
Term glblProto502;
void glblFldFn504(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 3, &arityArgs);
  if (arityArgs.count == 3) {
    String *fileStr = (String *)arityArgs.args[0];
    Value *dispVal = (Value *)arityArgs.args[2];
    swapStore(term_loc(args), (Term)dispVal);
    if (term_tag((Term)dispVal) == I60) {
      fprintf(stderr,
              "No implementation of '.tail' found for integers called from "
              "%.*s: %ld\n",
              (int)fileStr->len, fileStr->buffer, get_i60(arityArgs.args[1]));
      BOOM("Dispatch failed");
    } else {
      switch (dispVal->type) {
      case 45:
        store_redex(args, glblFld515);
        break;
      default: {
        fprintf(stderr,
                "No implementation of '.tail' found for type %s (%ld) called "
                "from %.*s: %ld\n",
                typeName(dispVal->type), dispVal->type, (int)fileStr->len,
                fileStr->buffer, get_i60(arityArgs.args[1]));
        BOOM("Dispatch failed");
      }
      }
    }
  }
  return;
}
Term glblProto502 = new_ref(glblFldFn504);
Term glblProto90;
void glblCFn91(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 3, &arityArgs);
  if (arityArgs.count == 3) {
    String *fileStr = (String *)arityArgs.args[0];
    Value *dispVal = (Value *)arityArgs.args[2];
    swapStore(term_loc(args), (Term)dispVal);
    if (term_tag((Term)dispVal) == I60) {
      fprintf(stderr,
              "No implementation of 'take-while' found for integers called "
              "from %.*s: %ld\n",
              (int)fileStr->len, fileStr->buffer, get_i60(arityArgs.args[1]));
      BOOM("Dispatch failed");
    } else {
      switch (dispVal->type) {
      default: {
        fprintf(stderr,
                "No implementation of 'take-while' found for type %s (%ld) "
                "called from %.*s: %ld\n",
                typeName(dispVal->type), dispVal->type, (int)fileStr->len,
                fileStr->buffer, get_i60(arityArgs.args[1]));
        BOOM("Dispatch failed");
      }
      }
    }
  }
  return;
}
Term glblProto90 = new_ref(glblCFn91);
Term glblProto365;
void glblCFn366(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 3, &arityArgs);
  if (arityArgs.count == 3) {
    String *fileStr = (String *)arityArgs.args[0];
    Value *dispVal = (Value *)arityArgs.args[2];
    swapStore(term_loc(args), (Term)dispVal);
    if (term_tag((Term)dispVal) == I60) {
      fprintf(stderr,
              "No implementation of 'vect-reverse' found for integers called "
              "from %.*s: %ld\n",
              (int)fileStr->len, fileStr->buffer, get_i60(arityArgs.args[1]));
      BOOM("Dispatch failed");
    } else {
      switch (dispVal->type) {
      case 43:
        store_redex(args, glblvect_reverse375);
        break;
      case 42:
        store_redex(args, glblvect_reverse372);
        break;
      default: {
        fprintf(stderr,
                "No implementation of 'vect-reverse' found for type %s (%ld) "
                "called from %.*s: %ld\n",
                typeName(dispVal->type), dispVal->type, (int)fileStr->len,
                fileStr->buffer, get_i60(arityArgs.args[1]));
        BOOM("Dispatch failed");
      }
      }
    }
  }
  return;
}
Term glblProto365 = new_ref(glblCFn366);
Term glblProto44;
void glblCFn45(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 3, &arityArgs);
  if (arityArgs.count == 3) {
    String *fileStr = (String *)arityArgs.args[0];
    Value *dispVal = (Value *)arityArgs.args[2];
    swapStore(term_loc(args), (Term)dispVal);
    if (term_tag((Term)dispVal) == I60) {
      fprintf(stderr,
              "No implementation of 'flatten' found for integers called from "
              "%.*s: %ld\n",
              (int)fileStr->len, fileStr->buffer, get_i60(arityArgs.args[1]));
      BOOM("Dispatch failed");
    } else {
      switch (dispVal->type) {
      case 8:
        store_redex(args, glblflatten434);
        break;
      default: {
        fprintf(stderr,
                "No implementation of 'flatten' found for type %s (%ld) called "
                "from %.*s: %ld\n",
                typeName(dispVal->type), dispVal->type, (int)fileStr->len,
                fileStr->buffer, get_i60(arityArgs.args[1]));
        BOOM("Dispatch failed");
      }
      }
    }
  }
  return;
}
Term glblProto44 = new_ref(glblCFn45);
Term glblProto22;
void glblCFn23(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 3, &arityArgs);
  if (arityArgs.count == 3) {
    String *fileStr = (String *)arityArgs.args[0];
    Value *dispVal = (Value *)arityArgs.args[2];
    swapStore(term_loc(args), (Term)dispVal);
    if (term_tag((Term)dispVal) == I60) {
      store_redex(args, glbl_LT_232);
    } else {
      switch (dispVal->type) {
      default: {
        fprintf(stderr,
                "No implementation of '<' found for type %s (%ld) called from "
                "%.*s: %ld\n",
                typeName(dispVal->type), dispVal->type, (int)fileStr->len,
                fileStr->buffer, get_i60(arityArgs.args[1]));
        BOOM("Dispatch failed");
      }
      }
    }
  }
  return;
}
Term glblProto22 = new_ref(glblCFn23);
Term glblProto86;
void glblCFn87(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 3, &arityArgs);
  if (arityArgs.count == 3) {
    String *fileStr = (String *)arityArgs.args[0];
    Value *dispVal = (Value *)arityArgs.args[2];
    swapStore(term_loc(args), (Term)dispVal);
    if (term_tag((Term)dispVal) == I60) {
      fprintf(stderr,
              "No implementation of 'drop' found for integers called from "
              "%.*s: %ld\n",
              (int)fileStr->len, fileStr->buffer, get_i60(arityArgs.args[1]));
      BOOM("Dispatch failed");
    } else {
      switch (dispVal->type) {
      default: {
        fprintf(stderr,
                "No implementation of 'drop' found for type %s (%ld) called "
                "from %.*s: %ld\n",
                typeName(dispVal->type), dispVal->type, (int)fileStr->len,
                fileStr->buffer, get_i60(arityArgs.args[1]));
        BOOM("Dispatch failed");
      }
      }
    }
  }
  return;
}
Term glblProto86 = new_ref(glblCFn87);
Term glblProto38;
void glblCFn39(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 3, &arityArgs);
  if (arityArgs.count == 3) {
    String *fileStr = (String *)arityArgs.args[0];
    Value *dispVal = (Value *)arityArgs.args[2];
    swapStore(term_loc(args), (Term)dispVal);
    if (term_tag((Term)dispVal) == I60) {
      fprintf(stderr,
              "No implementation of 'map' found for integers called from %.*s: "
              "%ld\n",
              (int)fileStr->len, fileStr->buffer, get_i60(arityArgs.args[1]));
      BOOM("Dispatch failed");
    } else {
      switch (dispVal->type) {
      case 43:
        store_redex(args, glblmap178);
        break;
      case 8:
        store_redex(args, glblmap440);
        break;
      case 42:
        store_redex(args, glblmap136);
        break;
      default: {
        fprintf(stderr,
                "No implementation of 'map' found for type %s (%ld) called "
                "from %.*s: %ld\n",
                typeName(dispVal->type), dispVal->type, (int)fileStr->len,
                fileStr->buffer, get_i60(arityArgs.args[1]));
        BOOM("Dispatch failed");
      }
      }
    }
  }
  return;
}
Term glblProto38 = new_ref(glblCFn39);
Term glblProto40;
void glblCFn41(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 3, &arityArgs);
  if (arityArgs.count == 3) {
    String *fileStr = (String *)arityArgs.args[0];
    Value *dispVal = (Value *)arityArgs.args[2];
    swapStore(term_loc(args), (Term)dispVal);
    if (term_tag((Term)dispVal) == I60) {
      fprintf(stderr,
              "No implementation of 'map!' found for integers called from "
              "%.*s: %ld\n",
              (int)fileStr->len, fileStr->buffer, get_i60(arityArgs.args[1]));
      BOOM("Dispatch failed");
    } else {
      switch (dispVal->type) {
      default: {
        fprintf(stderr,
                "No implementation of 'map!' found for type %s (%ld) called "
                "from %.*s: %ld\n",
                typeName(dispVal->type), dispVal->type, (int)fileStr->len,
                fileStr->buffer, get_i60(arityArgs.args[1]));
        BOOM("Dispatch failed");
      }
      }
    }
  }
  return;
}
Term glblProto40 = new_ref(glblCFn41);
Term glblProto64;
void glblCFn65(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 3, &arityArgs);
  if (arityArgs.count == 3) {
    String *fileStr = (String *)arityArgs.args[0];
    Value *dispVal = (Value *)arityArgs.args[2];
    swapStore(term_loc(args), (Term)dispVal);
    if (term_tag((Term)dispVal) == I60) {
      fprintf(stderr,
              "No implementation of 'conj' found for integers called from "
              "%.*s: %ld\n",
              (int)fileStr->len, fileStr->buffer, get_i60(arityArgs.args[1]));
      BOOM("Dispatch failed");
    } else {
      switch (dispVal->type) {
      case 8:
        store_redex(args, glblconj448);
        break;
      default: {
        fprintf(stderr,
                "No implementation of 'conj' found for type %s (%ld) called "
                "from %.*s: %ld\n",
                typeName(dispVal->type), dispVal->type, (int)fileStr->len,
                fileStr->buffer, get_i60(arityArgs.args[1]));
        BOOM("Dispatch failed");
      }
      }
    }
  }
  return;
}
Term glblProto64 = new_ref(glblCFn65);
Term glblProto72;
void glblCFn73(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 3, &arityArgs);
  if (arityArgs.count == 3) {
    String *fileStr = (String *)arityArgs.args[0];
    Value *dispVal = (Value *)arityArgs.args[2];
    swapStore(term_loc(args), (Term)dispVal);
    if (term_tag((Term)dispVal) == I60) {
      fprintf(stderr,
              "No implementation of 'first' found for integers called from "
              "%.*s: %ld\n",
              (int)fileStr->len, fileStr->buffer, get_i60(arityArgs.args[1]));
      BOOM("Dispatch failed");
    } else {
      switch (dispVal->type) {
      case 8:
        store_redex(args, glblfirst454);
        break;
      default: {
        fprintf(stderr,
                "No implementation of 'first' found for type %s (%ld) called "
                "from %.*s: %ld\n",
                typeName(dispVal->type), dispVal->type, (int)fileStr->len,
                fileStr->buffer, get_i60(arityArgs.args[1]));
        BOOM("Dispatch failed");
      }
      }
    }
  }
  return;
}
Term glblProto72 = new_ref(glblCFn73);
Term glblProto78;
void glblCFn79(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 3, &arityArgs);
  if (arityArgs.count == 3) {
    String *fileStr = (String *)arityArgs.args[0];
    Value *dispVal = (Value *)arityArgs.args[2];
    swapStore(term_loc(args), (Term)dispVal);
    if (term_tag((Term)dispVal) == I60) {
      fprintf(stderr,
              "No implementation of 'butlast' found for integers called from "
              "%.*s: %ld\n",
              (int)fileStr->len, fileStr->buffer, get_i60(arityArgs.args[1]));
      BOOM("Dispatch failed");
    } else {
      switch (dispVal->type) {
      default: {
        fprintf(stderr,
                "No implementation of 'butlast' found for type %s (%ld) called "
                "from %.*s: %ld\n",
                typeName(dispVal->type), dispVal->type, (int)fileStr->len,
                fileStr->buffer, get_i60(arityArgs.args[1]));
        BOOM("Dispatch failed");
      }
      }
    }
  }
  return;
}
Term glblProto78 = new_ref(glblCFn79);
Term glblProto358;
void glblCFn359(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 3, &arityArgs);
  if (arityArgs.count == 3) {
    String *fileStr = (String *)arityArgs.args[0];
    Value *dispVal = (Value *)arityArgs.args[2];
    swapStore(term_loc(args), (Term)dispVal);
    if (term_tag((Term)dispVal) == I60) {
      fprintf(stderr,
              "No implementation of 'vect-reduce' found for integers called "
              "from %.*s: %ld\n",
              (int)fileStr->len, fileStr->buffer, get_i60(arityArgs.args[1]));
      BOOM("Dispatch failed");
    } else {
      switch (dispVal->type) {
      case 43:
        store_redex(args, glblvect_reduce364);
        break;
      case 42:
        store_redex(args, glblvect_reduce361);
        break;
      default: {
        fprintf(stderr,
                "No implementation of 'vect-reduce' found for type %s (%ld) "
                "called from %.*s: %ld\n",
                typeName(dispVal->type), dispVal->type, (int)fileStr->len,
                fileStr->buffer, get_i60(arityArgs.args[1]));
        BOOM("Dispatch failed");
      }
      }
    }
  }
  return;
}
Term glblProto358 = new_ref(glblCFn359);
Term glblProto32;
void glblCFn33(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 3, &arityArgs);
  if (arityArgs.count == 3) {
    String *fileStr = (String *)arityArgs.args[0];
    Value *dispVal = (Value *)arityArgs.args[2];
    swapStore(term_loc(args), (Term)dispVal);
    if (term_tag((Term)dispVal) == I60) {
      fprintf(stderr,
              "No implementation of 'zero' found for integers called from "
              "%.*s: %ld\n",
              (int)fileStr->len, fileStr->buffer, get_i60(arityArgs.args[1]));
      BOOM("Dispatch failed");
    } else {
      switch (dispVal->type) {
      default: {
        fprintf(stderr,
                "No implementation of 'zero' found for type %s (%ld) called "
                "from %.*s: %ld\n",
                typeName(dispVal->type), dispVal->type, (int)fileStr->len,
                fileStr->buffer, get_i60(arityArgs.args[1]));
        BOOM("Dispatch failed");
      }
      }
    }
  }
  return;
}
Term glblProto32 = new_ref(glblCFn33);
Term glblProto42;
void glblCFn43(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 3, &arityArgs);
  if (arityArgs.count == 3) {
    String *fileStr = (String *)arityArgs.args[0];
    Value *dispVal = (Value *)arityArgs.args[2];
    swapStore(term_loc(args), (Term)dispVal);
    if (term_tag((Term)dispVal) == I60) {
      fprintf(stderr,
              "No implementation of 'flat-map' found for integers called from "
              "%.*s: %ld\n",
              (int)fileStr->len, fileStr->buffer, get_i60(arityArgs.args[1]));
      BOOM("Dispatch failed");
    } else {
      switch (dispVal->type) {
      case 43:
        store_redex(args, glblflat_map176);
        break;
      case 8:
        store_redex(args, glblflat_map436);
        break;
      default: {
        fprintf(stderr,
                "No implementation of 'flat-map' found for type %s (%ld) "
                "called from %.*s: %ld\n",
                typeName(dispVal->type), dispVal->type, (int)fileStr->len,
                fileStr->buffer, get_i60(arityArgs.args[1]));
        BOOM("Dispatch failed");
      }
      }
    }
  }
  return;
}
Term glblProto42 = new_ref(glblCFn43);
Term glblProto70;
void glblCFn71(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 3, &arityArgs);
  if (arityArgs.count == 3) {
    String *fileStr = (String *)arityArgs.args[0];
    Value *dispVal = (Value *)arityArgs.args[2];
    swapStore(term_loc(args), (Term)dispVal);
    if (term_tag((Term)dispVal) == I60) {
      fprintf(stderr,
              "No implementation of 'vec' found for integers called from %.*s: "
              "%ld\n",
              (int)fileStr->len, fileStr->buffer, get_i60(arityArgs.args[1]));
      BOOM("Dispatch failed");
    } else {
      switch (dispVal->type) {
      default: {
        fprintf(stderr,
                "No implementation of 'vec' found for type %s (%ld) called "
                "from %.*s: %ld\n",
                typeName(dispVal->type), dispVal->type, (int)fileStr->len,
                fileStr->buffer, get_i60(arityArgs.args[1]));
        BOOM("Dispatch failed");
      }
      }
    }
  }
  return;
}
Term glblProto70 = new_ref(glblCFn71);
Term glblProto284;
void glblFldFn285(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 3, &arityArgs);
  if (arityArgs.count == 3) {
    String *fileStr = (String *)arityArgs.args[0];
    Value *dispVal = (Value *)arityArgs.args[2];
    swapStore(term_loc(args), (Term)dispVal);
    if (term_tag((Term)dispVal) == I60) {
      fprintf(stderr,
              "No implementation of '.start' found for integers called from "
              "%.*s: %ld\n",
              (int)fileStr->len, fileStr->buffer, get_i60(arityArgs.args[1]));
      BOOM("Dispatch failed");
    } else {
      switch (dispVal->type) {
      case 5:
        store_redex(args, glblFld298);
        break;
      default: {
        fprintf(stderr,
                "No implementation of '.start' found for type %s (%ld) called "
                "from %.*s: %ld\n",
                typeName(dispVal->type), dispVal->type, (int)fileStr->len,
                fileStr->buffer, get_i60(arityArgs.args[1]));
        BOOM("Dispatch failed");
      }
      }
    }
  }
  return;
}
Term glblProto284 = new_ref(glblFldFn285);
Term glblProto376;
void glblCFn377(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 3, &arityArgs);
  if (arityArgs.count == 3) {
    String *fileStr = (String *)arityArgs.args[0];
    Value *dispVal = (Value *)arityArgs.args[2];
    swapStore(term_loc(args), (Term)dispVal);
    if (term_tag((Term)dispVal) == I60) {
      fprintf(stderr,
              "No implementation of 'vect-sub' found for integers called from "
              "%.*s: %ld\n",
              (int)fileStr->len, fileStr->buffer, get_i60(arityArgs.args[1]));
      BOOM("Dispatch failed");
    } else {
      switch (dispVal->type) {
      case 43:
        store_redex(args, glblvect_sub388);
        break;
      case 42:
        store_redex(args, glblvect_sub385);
        break;
      default: {
        fprintf(stderr,
                "No implementation of 'vect-sub' found for type %s (%ld) "
                "called from %.*s: %ld\n",
                typeName(dispVal->type), dispVal->type, (int)fileStr->len,
                fileStr->buffer, get_i60(arityArgs.args[1]));
        BOOM("Dispatch failed");
      }
      }
    }
  }
  return;
}
Term glblProto376 = new_ref(glblCFn377);
Term glblProto74;
void glblCFn75(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 3, &arityArgs);
  if (arityArgs.count == 3) {
    String *fileStr = (String *)arityArgs.args[0];
    Value *dispVal = (Value *)arityArgs.args[2];
    swapStore(term_loc(args), (Term)dispVal);
    if (term_tag((Term)dispVal) == I60) {
      fprintf(stderr,
              "No implementation of 'rest' found for integers called from "
              "%.*s: %ld\n",
              (int)fileStr->len, fileStr->buffer, get_i60(arityArgs.args[1]));
      BOOM("Dispatch failed");
    } else {
      switch (dispVal->type) {
      case 8:
        store_redex(args, glblrest450);
        break;
      default: {
        fprintf(stderr,
                "No implementation of 'rest' found for type %s (%ld) called "
                "from %.*s: %ld\n",
                typeName(dispVal->type), dispVal->type, (int)fileStr->len,
                fileStr->buffer, get_i60(arityArgs.args[1]));
        BOOM("Dispatch failed");
      }
      }
    }
  }
  return;
}
Term glblProto74 = new_ref(glblCFn75);
Term glblProto46;
void glblCFn47(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 3, &arityArgs);
  if (arityArgs.count == 3) {
    String *fileStr = (String *)arityArgs.args[0];
    Value *dispVal = (Value *)arityArgs.args[2];
    swapStore(term_loc(args), (Term)dispVal);
    if (term_tag((Term)dispVal) == I60) {
      fprintf(stderr,
              "No implementation of 'extend' found for integers called from "
              "%.*s: %ld\n",
              (int)fileStr->len, fileStr->buffer, get_i60(arityArgs.args[1]));
      BOOM("Dispatch failed");
    } else {
      switch (dispVal->type) {
      default: {
        fprintf(stderr,
                "No implementation of 'extend' found for type %s (%ld) called "
                "from %.*s: %ld\n",
                typeName(dispVal->type), dispVal->type, (int)fileStr->len,
                fileStr->buffer, get_i60(arityArgs.args[1]));
        BOOM("Dispatch failed");
      }
      }
    }
  }
  return;
}
Term glblProto46 = new_ref(glblCFn47);
Term glblProto82;
void glblCFn83(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 3, &arityArgs);
  if (arityArgs.count == 3) {
    String *fileStr = (String *)arityArgs.args[0];
    Value *dispVal = (Value *)arityArgs.args[2];
    swapStore(term_loc(args), (Term)dispVal);
    if (term_tag((Term)dispVal) == I60) {
      fprintf(stderr,
              "No implementation of 'split-with' found for integers called "
              "from %.*s: %ld\n",
              (int)fileStr->len, fileStr->buffer, get_i60(arityArgs.args[1]));
      BOOM("Dispatch failed");
    } else {
      switch (dispVal->type) {
      default: {
        fprintf(stderr,
                "No implementation of 'split-with' found for type %s (%ld) "
                "called from %.*s: %ld\n",
                typeName(dispVal->type), dispVal->type, (int)fileStr->len,
                fileStr->buffer, get_i60(arityArgs.args[1]));
        BOOM("Dispatch failed");
      }
      }
    }
  }
  return;
}
Term glblProto82 = new_ref(glblCFn83);
Term glblProto58;
void glblCFn59(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 3, &arityArgs);
  if (arityArgs.count == 3) {
    String *fileStr = (String *)arityArgs.args[0];
    Value *dispVal = (Value *)arityArgs.args[2];
    swapStore(term_loc(args), (Term)dispVal);
    if (term_tag((Term)dispVal) == I60) {
      fprintf(stderr,
              "No implementation of 'empty?' found for integers called from "
              "%.*s: %ld\n",
              (int)fileStr->len, fileStr->buffer, get_i60(arityArgs.args[1]));
      BOOM("Dispatch failed");
    } else {
      switch (dispVal->type) {
      case 8:
        store_redex(args, glblempty_QM_460);
        break;
      default: {
        fprintf(stderr,
                "No implementation of 'empty?' found for type %s (%ld) called "
                "from %.*s: %ld\n",
                typeName(dispVal->type), dispVal->type, (int)fileStr->len,
                fileStr->buffer, get_i60(arityArgs.args[1]));
        BOOM("Dispatch failed");
      }
      }
    }
  }
  return;
}
Term glblProto58 = new_ref(glblCFn59);
Term glblProto66;
void glblCFn67(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 3, &arityArgs);
  if (arityArgs.count == 3) {
    String *fileStr = (String *)arityArgs.args[0];
    Value *dispVal = (Value *)arityArgs.args[2];
    swapStore(term_loc(args), (Term)dispVal);
    if (term_tag((Term)dispVal) == I60) {
      fprintf(stderr,
              "No implementation of 'filter' found for integers called from "
              "%.*s: %ld\n",
              (int)fileStr->len, fileStr->buffer, get_i60(arityArgs.args[1]));
      BOOM("Dispatch failed");
    } else {
      switch (dispVal->type) {
      case 8:
        store_redex(args, glblfilter432);
        break;
      default: {
        fprintf(stderr,
                "No implementation of 'filter' found for type %s (%ld) called "
                "from %.*s: %ld\n",
                typeName(dispVal->type), dispVal->type, (int)fileStr->len,
                fileStr->buffer, get_i60(arityArgs.args[1]));
        BOOM("Dispatch failed");
      }
      }
    }
  }
  return;
}
Term glblProto66 = new_ref(glblCFn67);
Term glblProto96;
void glblCFn97(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 3, &arityArgs);
  if (arityArgs.count == 3) {
    String *fileStr = (String *)arityArgs.args[0];
    Value *dispVal = (Value *)arityArgs.args[2];
    swapStore(term_loc(args), (Term)dispVal);
    if (term_tag((Term)dispVal) == I60) {
      fprintf(stderr,
              "No implementation of 'store' found for integers called from "
              "%.*s: %ld\n",
              (int)fileStr->len, fileStr->buffer, get_i60(arityArgs.args[1]));
      BOOM("Dispatch failed");
    } else {
      switch (dispVal->type) {
      case 8:
        store_redex(args, glblstore446);
        break;
      default: {
        fprintf(stderr,
                "No implementation of 'store' found for type %s (%ld) called "
                "from %.*s: %ld\n",
                typeName(dispVal->type), dispVal->type, (int)fileStr->len,
                fileStr->buffer, get_i60(arityArgs.args[1]));
        BOOM("Dispatch failed");
      }
      }
    }
  }
  return;
}
Term glblProto96 = new_ref(glblCFn97);
Term glblProto84;
void glblCFn85(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 3, &arityArgs);
  if (arityArgs.count == 3) {
    String *fileStr = (String *)arityArgs.args[0];
    Value *dispVal = (Value *)arityArgs.args[2];
    swapStore(term_loc(args), (Term)dispVal);
    if (term_tag((Term)dispVal) == I60) {
      fprintf(stderr,
              "No implementation of 'take' found for integers called from "
              "%.*s: %ld\n",
              (int)fileStr->len, fileStr->buffer, get_i60(arityArgs.args[1]));
      BOOM("Dispatch failed");
    } else {
      switch (dispVal->type) {
      default: {
        fprintf(stderr,
                "No implementation of 'take' found for type %s (%ld) called "
                "from %.*s: %ld\n",
                typeName(dispVal->type), dispVal->type, (int)fileStr->len,
                fileStr->buffer, get_i60(arityArgs.args[1]));
        BOOM("Dispatch failed");
      }
      }
    }
  }
  return;
}
Term glblProto84 = new_ref(glblCFn85);
Term glblProto60;
void glblCFn61(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 3, &arityArgs);
  if (arityArgs.count == 3) {
    String *fileStr = (String *)arityArgs.args[0];
    Value *dispVal = (Value *)arityArgs.args[2];
    swapStore(term_loc(args), (Term)dispVal);
    if (term_tag((Term)dispVal) == I60) {
      fprintf(stderr,
              "No implementation of 'count' found for integers called from "
              "%.*s: %ld\n",
              (int)fileStr->len, fileStr->buffer, get_i60(arityArgs.args[1]));
      BOOM("Dispatch failed");
    } else {
      switch (dispVal->type) {
      case 2:
        store_redex(args, glblcount316);
        break;
      case 5:
        store_redex(args, glblcount291);
        break;
      case 46:
        store_redex(args, glblcount485);
        break;
      case 8:
        store_redex(args, glblcount466);
        break;
      case 45:
        store_redex(args, glblcount511);
        break;
      default: {
        fprintf(stderr,
                "No implementation of 'count' found for type %s (%ld) called "
                "from %.*s: %ld\n",
                typeName(dispVal->type), dispVal->type, (int)fileStr->len,
                fileStr->buffer, get_i60(arityArgs.args[1]));
        BOOM("Dispatch failed");
      }
      }
    }
  }
  return;
}
Term glblProto60 = new_ref(glblCFn61);
Term glblProto14;
void glblFldFn15(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 3, &arityArgs);
  if (arityArgs.count == 3) {
    String *fileStr = (String *)arityArgs.args[0];
    Value *dispVal = (Value *)arityArgs.args[2];
    swapStore(term_loc(args), (Term)dispVal);
    if (term_tag((Term)dispVal) == I60) {
      fprintf(stderr,
              "No implementation of '.x' found for integers called from %.*s: "
              "%ld\n",
              (int)fileStr->len, fileStr->buffer, get_i60(arityArgs.args[1]));
      BOOM("Dispatch failed");
    } else {
      switch (dispVal->type) {
      case 43:
        store_redex(args, glblFld17);
        break;
      default: {
        fprintf(stderr,
                "No implementation of '.x' found for type %s (%ld) called from "
                "%.*s: %ld\n",
                typeName(dispVal->type), dispVal->type, (int)fileStr->len,
                fileStr->buffer, get_i60(arityArgs.args[1]));
        BOOM("Dispatch failed");
      }
      }
    }
  }
  return;
}
Term glblProto14 = new_ref(glblFldFn15);
Term glblProto106;
void glblCFn107(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 3, &arityArgs);
  if (arityArgs.count == 3) {
    String *fileStr = (String *)arityArgs.args[0];
    Value *dispVal = (Value *)arityArgs.args[2];
    swapStore(term_loc(args), (Term)dispVal);
    if (term_tag((Term)dispVal) == I60) {
      store_redex(args, glblstr_vect236);
    } else {
      switch (dispVal->type) {
      case 2:
        store_redex(args, glblstr_vect333);
        break;
      case 5:
        store_redex(args, glblstr_vect331);
        break;
      case 43:
        store_redex(args, glblstr_vect194);
        break;
      case 8:
        store_redex(args, glblstr_vect468);
        break;
      default: {
        fprintf(stderr,
                "No implementation of 'str-vect' found for type %s (%ld) "
                "called from %.*s: %ld\n",
                typeName(dispVal->type), dispVal->type, (int)fileStr->len,
                fileStr->buffer, get_i60(arityArgs.args[1]));
        BOOM("Dispatch failed");
      }
      }
    }
  }
  return;
}
Term glblProto106 = new_ref(glblCFn107);
Term glblProto56;
void glblCFn57(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 3, &arityArgs);
  if (arityArgs.count == 3) {
    String *fileStr = (String *)arityArgs.args[0];
    Value *dispVal = (Value *)arityArgs.args[2];
    swapStore(term_loc(args), (Term)dispVal);
    if (term_tag((Term)dispVal) == I60) {
      fprintf(stderr,
              "No implementation of 'interpose' found for integers called from "
              "%.*s: %ld\n",
              (int)fileStr->len, fileStr->buffer, get_i60(arityArgs.args[1]));
      BOOM("Dispatch failed");
    } else {
      switch (dispVal->type) {
      case 8:
        store_redex(args, glblinterpose430);
        break;
      default: {
        fprintf(stderr,
                "No implementation of 'interpose' found for type %s (%ld) "
                "called from %.*s: %ld\n",
                typeName(dispVal->type), dispVal->type, (int)fileStr->len,
                fileStr->buffer, get_i60(arityArgs.args[1]));
        BOOM("Dispatch failed");
      }
      }
    }
  }
  return;
}
Term glblProto56 = new_ref(glblCFn57);
Term glblProto34;
void glblCFn35(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 3, &arityArgs);
  if (arityArgs.count == 3) {
    String *fileStr = (String *)arityArgs.args[0];
    Value *dispVal = (Value *)arityArgs.args[2];
    swapStore(term_loc(args), (Term)dispVal);
    if (term_tag((Term)dispVal) == I60) {
      fprintf(stderr,
              "No implementation of 'comp' found for integers called from "
              "%.*s: %ld\n",
              (int)fileStr->len, fileStr->buffer, get_i60(arityArgs.args[1]));
      BOOM("Dispatch failed");
    } else {
      switch (dispVal->type) {
      case 8:
        store_redex(args, glblcomp456);
        break;
      default: {
        fprintf(stderr,
                "No implementation of 'comp' found for type %s (%ld) called "
                "from %.*s: %ld\n",
                typeName(dispVal->type), dispVal->type, (int)fileStr->len,
                fileStr->buffer, get_i60(arityArgs.args[1]));
        BOOM("Dispatch failed");
      }
      }
    }
  }
  return;
}
Term glblProto34 = new_ref(glblCFn35);
Term glblProto102;
void glblCFn103(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 3, &arityArgs);
  if (arityArgs.count == 3) {
    String *fileStr = (String *)arityArgs.args[0];
    Value *dispVal = (Value *)arityArgs.args[2];
    swapStore(term_loc(args), (Term)dispVal);
    if (term_tag((Term)dispVal) == I60) {
      fprintf(stderr,
              "No implementation of 'keys' found for integers called from "
              "%.*s: %ld\n",
              (int)fileStr->len, fileStr->buffer, get_i60(arityArgs.args[1]));
      BOOM("Dispatch failed");
    } else {
      switch (dispVal->type) {
      default: {
        fprintf(stderr,
                "No implementation of 'keys' found for type %s (%ld) called "
                "from %.*s: %ld\n",
                typeName(dispVal->type), dispVal->type, (int)fileStr->len,
                fileStr->buffer, get_i60(arityArgs.args[1]));
        BOOM("Dispatch failed");
      }
      }
    }
  }
  return;
}
Term glblProto102 = new_ref(glblCFn103);
Term glblProto54;
void glblCFn55(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 3, &arityArgs);
  if (arityArgs.count == 3) {
    String *fileStr = (String *)arityArgs.args[0];
    Value *dispVal = (Value *)arityArgs.args[2];
    swapStore(term_loc(args), (Term)dispVal);
    if (term_tag((Term)dispVal) == I60) {
      fprintf(stderr,
              "No implementation of 'apply' found for integers called from "
              "%.*s: %ld\n",
              (int)fileStr->len, fileStr->buffer, get_i60(arityArgs.args[1]));
      BOOM("Dispatch failed");
    } else {
      switch (dispVal->type) {
      default: {
        fprintf(stderr,
                "No implementation of 'apply' found for type %s (%ld) called "
                "from %.*s: %ld\n",
                typeName(dispVal->type), dispVal->type, (int)fileStr->len,
                fileStr->buffer, get_i60(arityArgs.args[1]));
        BOOM("Dispatch failed");
      }
      }
    }
  }
  return;
}
Term glblProto54 = new_ref(glblCFn55);
Term glblProto26;
void glblCFn27(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 3, &arityArgs);
  if (arityArgs.count == 3) {
    String *fileStr = (String *)arityArgs.args[0];
    Value *dispVal = (Value *)arityArgs.args[2];
    swapStore(term_loc(args), (Term)dispVal);
    if (term_tag((Term)dispVal) == I60) {
      fprintf(stderr,
              "No implementation of 'or' found for integers called from %.*s: "
              "%ld\n",
              (int)fileStr->len, fileStr->buffer, get_i60(arityArgs.args[1]));
      BOOM("Dispatch failed");
    } else {
      switch (dispVal->type) {
      case 43:
        store_redex(args, glblor180);
        break;
      case 42:
        store_redex(args, glblor138);
        break;
      default: {
        fprintf(stderr,
                "No implementation of 'or' found for type %s (%ld) called from "
                "%.*s: %ld\n",
                typeName(dispVal->type), dispVal->type, (int)fileStr->len,
                fileStr->buffer, get_i60(arityArgs.args[1]));
        BOOM("Dispatch failed");
      }
      }
    }
  }
  return;
}
Term glblProto26 = new_ref(glblCFn27);
Term glblProto30;
void glblCFn31(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 3, &arityArgs);
  if (arityArgs.count == 3) {
    String *fileStr = (String *)arityArgs.args[0];
    Value *dispVal = (Value *)arityArgs.args[2];
    swapStore(term_loc(args), (Term)dispVal);
    if (term_tag((Term)dispVal) == I60) {
      fprintf(stderr,
              "No implementation of 'cond' found for integers called from "
              "%.*s: %ld\n",
              (int)fileStr->len, fileStr->buffer, get_i60(arityArgs.args[1]));
      BOOM("Dispatch failed");
    } else {
      switch (dispVal->type) {
      case 43:
        store_redex(args, glblcond184);
        break;
      case 42:
        store_redex(args, glblcond142);
        break;
      default: {
        fprintf(stderr,
                "No implementation of 'cond' found for type %s (%ld) called "
                "from %.*s: %ld\n",
                typeName(dispVal->type), dispVal->type, (int)fileStr->len,
                fileStr->buffer, get_i60(arityArgs.args[1]));
        BOOM("Dispatch failed");
      }
      }
    }
  }
  return;
}
Term glblProto30 = new_ref(glblCFn31);
Term glblProto76;
void glblCFn77(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 3, &arityArgs);
  if (arityArgs.count == 3) {
    String *fileStr = (String *)arityArgs.args[0];
    Value *dispVal = (Value *)arityArgs.args[2];
    swapStore(term_loc(args), (Term)dispVal);
    if (term_tag((Term)dispVal) == I60) {
      fprintf(stderr,
              "No implementation of 'last' found for integers called from "
              "%.*s: %ld\n",
              (int)fileStr->len, fileStr->buffer, get_i60(arityArgs.args[1]));
      BOOM("Dispatch failed");
    } else {
      switch (dispVal->type) {
      case 8:
        store_redex(args, glbllast452);
        break;
      default: {
        fprintf(stderr,
                "No implementation of 'last' found for type %s (%ld) called "
                "from %.*s: %ld\n",
                typeName(dispVal->type), dispVal->type, (int)fileStr->len,
                fileStr->buffer, get_i60(arityArgs.args[1]));
        BOOM("Dispatch failed");
      }
      }
    }
  }
  return;
}
Term glblProto76 = new_ref(glblCFn77);
Term glblProto286;
void glblFldFn287(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 3, &arityArgs);
  if (arityArgs.count == 3) {
    String *fileStr = (String *)arityArgs.args[0];
    Value *dispVal = (Value *)arityArgs.args[2];
    swapStore(term_loc(args), (Term)dispVal);
    if (term_tag((Term)dispVal) == I60) {
      fprintf(stderr,
              "No implementation of '.len' found for integers called from "
              "%.*s: %ld\n",
              (int)fileStr->len, fileStr->buffer, get_i60(arityArgs.args[1]));
      BOOM("Dispatch failed");
    } else {
      switch (dispVal->type) {
      case 5:
        store_redex(args, glblFld299);
        break;
      case 45:
        store_redex(args, glblFld516);
        break;
      default: {
        fprintf(stderr,
                "No implementation of '.len' found for type %s (%ld) called "
                "from %.*s: %ld\n",
                typeName(dispVal->type), dispVal->type, (int)fileStr->len,
                fileStr->buffer, get_i60(arityArgs.args[1]));
        BOOM("Dispatch failed");
      }
      }
    }
  }
  return;
}
Term glblProto286 = new_ref(glblFldFn287);
Term glblProto98;
void glblCFn99(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 3, &arityArgs);
  if (arityArgs.count == 3) {
    String *fileStr = (String *)arityArgs.args[0];
    Value *dispVal = (Value *)arityArgs.args[2];
    swapStore(term_loc(args), (Term)dispVal);
    if (term_tag((Term)dispVal) == I60) {
      fprintf(stderr,
              "No implementation of 'assoc' found for integers called from "
              "%.*s: %ld\n",
              (int)fileStr->len, fileStr->buffer, get_i60(arityArgs.args[1]));
      BOOM("Dispatch failed");
    } else {
      switch (dispVal->type) {
      default: {
        fprintf(stderr,
                "No implementation of 'assoc' found for type %s (%ld) called "
                "from %.*s: %ld\n",
                typeName(dispVal->type), dispVal->type, (int)fileStr->len,
                fileStr->buffer, get_i60(arityArgs.args[1]));
        BOOM("Dispatch failed");
      }
      }
    }
  }
  return;
}
Term glblProto98 = new_ref(glblCFn99);
Term glblProto52;
void glblCFn53(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 3, &arityArgs);
  if (arityArgs.count == 3) {
    String *fileStr = (String *)arityArgs.args[0];
    Value *dispVal = (Value *)arityArgs.args[2];
    swapStore(term_loc(args), (Term)dispVal);
    if (term_tag((Term)dispVal) == I60) {
      fprintf(stderr,
              "No implementation of 'extract' found for integers called from "
              "%.*s: %ld\n",
              (int)fileStr->len, fileStr->buffer, get_i60(arityArgs.args[1]));
      BOOM("Dispatch failed");
    } else {
      switch (dispVal->type) {
      case 43:
        store_redex(args, glblextract190);
        break;
      default: {
        fprintf(stderr,
                "No implementation of 'extract' found for type %s (%ld) called "
                "from %.*s: %ld\n",
                typeName(dispVal->type), dispVal->type, (int)fileStr->len,
                fileStr->buffer, get_i60(arityArgs.args[1]));
        BOOM("Dispatch failed");
      }
      }
    }
  }
  return;
}
Term glblProto52 = new_ref(glblCFn53);
Term glblProto48;
void glblCFn49(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 3, &arityArgs);
  if (arityArgs.count == 3) {
    String *fileStr = (String *)arityArgs.args[0];
    Value *dispVal = (Value *)arityArgs.args[2];
    swapStore(term_loc(args), (Term)dispVal);
    if (term_tag((Term)dispVal) == I60) {
      fprintf(stderr,
              "No implementation of 'duplicate' found for integers called from "
              "%.*s: %ld\n",
              (int)fileStr->len, fileStr->buffer, get_i60(arityArgs.args[1]));
      BOOM("Dispatch failed");
    } else {
      switch (dispVal->type) {
      default: {
        fprintf(stderr,
                "No implementation of 'duplicate' found for type %s (%ld) "
                "called from %.*s: %ld\n",
                typeName(dispVal->type), dispVal->type, (int)fileStr->len,
                fileStr->buffer, get_i60(arityArgs.args[1]));
        BOOM("Dispatch failed");
      }
      }
    }
  }
  return;
}
Term glblProto48 = new_ref(glblCFn49);
Term glblProto108;
void glblCFn109(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 3, &arityArgs);
  if (arityArgs.count == 3) {
    String *fileStr = (String *)arityArgs.args[0];
    Value *dispVal = (Value *)arityArgs.args[2];
    swapStore(term_loc(args), (Term)dispVal);
    if (term_tag((Term)dispVal) == I60) {
      fprintf(stderr,
              "No implementation of 'free-resource' found for integers called "
              "from %.*s: %ld\n",
              (int)fileStr->len, fileStr->buffer, get_i60(arityArgs.args[1]));
      BOOM("Dispatch failed");
    } else {
      switch (dispVal->type) {
      default: {
        fprintf(stderr,
                "No implementation of 'free-resource' found for type %s (%ld) "
                "called from %.*s: %ld\n",
                typeName(dispVal->type), dispVal->type, (int)fileStr->len,
                fileStr->buffer, get_i60(arityArgs.args[1]));
        BOOM("Dispatch failed");
      }
      }
    }
  }
  return;
}
Term glblProto108 = new_ref(glblCFn109);
Term glblProto100;
void glblCFn101(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 3, &arityArgs);
  if (arityArgs.count == 3) {
    String *fileStr = (String *)arityArgs.args[0];
    Value *dispVal = (Value *)arityArgs.args[2];
    swapStore(term_loc(args), (Term)dispVal);
    if (term_tag((Term)dispVal) == I60) {
      fprintf(stderr,
              "No implementation of 'get' found for integers called from %.*s: "
              "%ld\n",
              (int)fileStr->len, fileStr->buffer, get_i60(arityArgs.args[1]));
      BOOM("Dispatch failed");
    } else {
      switch (dispVal->type) {
      case 8:
        store_redex(args, glblget464);
        break;
      default: {
        fprintf(stderr,
                "No implementation of 'get' found for type %s (%ld) called "
                "from %.*s: %ld\n",
                typeName(dispVal->type), dispVal->type, (int)fileStr->len,
                fileStr->buffer, get_i60(arityArgs.args[1]));
        BOOM("Dispatch failed");
      }
      }
    }
  }
  return;
}
Term glblProto100 = new_ref(glblCFn101);
unsigned refsCount = 101;
unsigned refNameCount = 101;
refMap refNames[107] = {{vectMap, "vectMap"},
                        {vectConjFn, "vectConj"},
                        {accessFieldFn, "accessField"},
                        {hvmVectFn, "hvmVect"},
                        {constructFn, "construct"},
                        {glblCFn113, "pr*"},
                        {glblCFn51, "wrap"},
                        {glblCFn195, "int-="},
                        {glblCFn69, "reduce"},
                        {glblCFn105, "vals"},
                        {glblCFn63, "empty"},
                        {glblCFn81, "split"},
                        {glblCFn89, "drop-while"},
                        {glblCFn401, "to-str"},
                        {glblCFn37, "recurse"},
                        {glblFldFn283, ".s"},
                        {glblCFn128, "*"},
                        {glblFldFn501, ".head"},
                        {glblCFn29, "either"},
                        {glblCFn203, "<="},
                        {glblCFn93, "reverse"},
                        {glblCFn20, "="},
                        {glblCFn244, "vect-count"},
                        {glblCons512, "Cons"},
                        {glblCFn95, "nth"},
                        {glblCFn116, "+"},
                        {glblCFn242, "min"},
                        {glblCFn4, "type-name"},
                        {glblCFn340, "vect-get"},
                        {glblCFn479, "str*"},
                        {glblCFn25, "and"},
                        {glblCFn477, "cons"},
                        {glblCFn277, "subs"},
                        {glblFldFn504, ".tail"},
                        {glblCFn349, "range*"},
                        {glblCFn91, "take-while"},
                        {glblCFn355, "range"},
                        {glblCFn366, "vect-reverse"},
                        {glblCFn7, "type-num"},
                        {glblCFn110, "abort"},
                        {glblCFn45, "flatten"},
                        {glblCFn23, "<"},
                        {glblCFn87, "drop"},
                        {glblCFn279, "str="},
                        {glblCFn39, "map"},
                        {glblCFn257, "fold"},
                        {glblCFn41, "map!"},
                        {glblCFn65, "conj"},
                        {glblCFn73, "first"},
                        {glblCFn79, "butlast"},
                        {glblCFn270, "str-malloc"},
                        {glblCFn359, "vect-reduce"},
                        {glblCFn274, "str-append"},
                        {glblCFn33, "zero"},
                        {glblCFn43, "flat-map"},
                        {glblCFn71, "vec"},
                        {glblFldFn285, ".start"},
                        {glblCFn377, "vect-sub"},
                        {glblCFn201, "number-str"},
                        {glblCFn261, "unfold"},
                        {glblCFn75, "rest"},
                        {glblCFn47, "extend"},
                        {glblCFn157, "println*"},
                        {glblCFn415, "vect-="},
                        {glblCFn83, "split-with"},
                        {glblCFn59, "empty?"},
                        {glblCFn67, "filter"},
                        {glblCFn417, "vect-store"},
                        {glblCFn97, "store"},
                        {glblCFn85, "take"},
                        {glblCFn61, "count"},
                        {glblFldFn15, ".x"},
                        {glblSome16, "Some"},
                        {glblCFn107, "str-vect"},
                        {glblCFn119, "inc"},
                        {glblCFn57, "interpose"},
                        {glblCFn122, "-"},
                        {glblCFn396, "subvec"},
                        {glblCFn125, "dec"},
                        {glblCFn132, "vect-conj"},
                        {glblCFn35, "comp"},
                        {glblCFn0, "default-type-name"},
                        {glblCFn103, "keys"},
                        {glblCFn55, "apply"},
                        {glblCFn27, "or"},
                        {glblCFn31, "cond"},
                        {glblCFn77, "last"},
                        {glblFldFn287, ".len"},
                        {glblCFn99, "assoc"},
                        {glblCFn263, "str-count"},
                        {glblCFn53, "extract"},
                        {glblCFn49, "duplicate"},
                        {glblCFn249, "identity"},
                        {glblSubString296, "SubString"},
                        {glblCFn109, "free-resource"},
                        {glblCFn101, "get"},
                        {glblCFn199, "int-<"},
                        {glblCFn579, "output"},
                        {glblCFn565, ">>"},
                        {glblCFn563, "bit-and"},
                        {glblCFn603, "clr"},
                        {glblCFn575, "I"},
                        {glblCFn610, "main"},
                        {glblCFn594, "rep"},
                        {glblCFn571, "O"},
                        {glblCFn583, "bin"}};
typeNameMap typeNames[21] = {
    {1, "Integer"},    {2, "String"},     {8, "Vector"},
    {9, "VectorNode"}, {11, "HashMap"},   {12, "HashMap"},
    {13, "HashMap"},   {14, "HashMap"},   {11, "BitmapIndexedNode"},
    {45, "Cons"},      {18, "Opaque"},    {3, "FnArity"},
    {12, "ArrayNode"}, {43, "Some"},      {13, "HashCollisionNode"},
    {42, "None"},      {46, "EndOfList"}, {44, "Leaf"},
    {5, "SubString"}};
void normGlobals() {
  Term apps, lams, V;
  Location vLoc;
  {
    Term glblVal13_1;
    glblVal13 = dupeArg(glblVal13, &glblVal13_1, 0);

    // call None at core: 63
    Term glblRslt12Args = pair_make(APP, 0, NUL, SUB);
    glblRslt12 = term_new(VAR, 0, port(2, term_loc(glblRslt12Args)));
    store_redex(glblRslt12Args, glblVal13_1);
  }
  {
    Term glblVal267_1;
    glblVal267 = dupeArg(glblVal267, &glblVal267_1, 0);

    // call Leaf at core: 423
    Term glblRslt266Args = pair_make(APP, 0, NUL, SUB);
    glblRslt266 = term_new(VAR, 0, port(2, term_loc(glblRslt266Args)));
    store_redex(glblRslt266Args, glblVal267_1);
  }
  {
    Term glblVal489_1;
    glblVal489 = dupeArg(glblVal489, &glblVal489_1, 0);

    // call EndOfList at core: 790
    Term glblRslt487Args = pair_make(APP, 0, NUL, SUB);
    glblRslt487 = term_new(VAR, 0, port(2, term_loc(glblRslt487Args)));
    store_redex(glblRslt487Args, glblVal489_1);
  }
  normalize(NULL);
  if (term_tag(glblVal519) == VAR)
    glblVal519 = take(term_loc(glblVal519));
  if (term_tag(glblRslt487) == VAR)
    glblRslt487 = take(term_loc(glblRslt487));
  if (term_tag(glblVal489) == VAR)
    glblVal489 = take(term_loc(glblVal489));
  if (term_tag(glblVal398) == VAR)
    glblVal398 = take(term_loc(glblVal398));
  if (term_tag(glblVal300) == VAR)
    glblVal300 = take(term_loc(glblVal300));
  if (term_tag(glblRslt266) == VAR)
    glblRslt266 = take(term_loc(glblRslt266));
  if (term_tag(glblVal267) == VAR)
    glblVal267 = take(term_loc(glblVal267));
  if (term_tag(glblVal18) == VAR)
    glblVal18 = take(term_loc(glblVal18));
  if (term_tag(glblRslt12) == VAR)
    glblRslt12 = take(term_loc(glblRslt12));
  if (term_tag(glblVal13) == VAR)
    glblVal13 = take(term_loc(glblVal13));
}
void freeGlobals() {
  freeGlobal(glblVal519);
  freeGlobal(glblRslt487);
  freeGlobal(glblVal489);
  freeGlobal(glblVal398);
  freeGlobal(glblVal300);
  freeGlobal(glblRslt266);
  freeGlobal(glblVal267);
  freeGlobal(glblVal18);
  freeGlobal(glblRslt12);
  freeGlobal(glblVal13);
  normalize(NULL);
  check_buff();
}
Term mainFn = new_ref(glblCFn610);
int typeCount = 47;
