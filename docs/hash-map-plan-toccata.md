# Hash-Map Plan — Toccata-Level Implementation and Testing

## Overview

The Toccata layer wraps the C-level bitmap-trie functions (see `hash-map-plan-c.md`) with inline-C wrappers in `hvm-core.toc` and implements the hash-map protocol methods in Toccata. Testing is exercised through `regression-tests/test-bmi.toc` (BMI node level) and, eventually, `regression-tests/hash-map-regressions.toc` (full map level).

**State (2026-08-23):** `bmiCopyAssoc` and `bmiMutateAssoc` are implemented at the Toccata level and fully branch-tested; both suites pass — `test-bmi` (27 cases: Group A copy-assoc + Group B mutate-assoc + `get`, `diff: 0`, `remaining nodes: 0`, `SAFETY=1`) and `test-hash-map` (55 tests, all green). Phase 1 (`get`) complete: `bmiGet` is wrapped (task 1.1), the `get*` protocol + `get` method for `BitmapIndexedNode` are in place (task 1.2), and `get` is exercised by tests 1.3–1.7 (flat hit, flat miss, sub-node hit, sub-node miss, collision-node hit). `HashCollisionNode` gained a `get*` method (task 1.7) so `bmiGet`'s sub-node recursion reaches collision children: Toccata `collisionGet` — a linear scan over `collisionCount`/`collisionKey`/`collisionVal` wrappers, `=` protocol with the entry key first (the `bmiCopyAssoc` convention; matches the commented integration tests' expectation that an `eq=Some` lookup key finds its own entry). Phase 2 (`dissoc`) complete: `bmiDissoc` is wrapped (task 2.1) and the `dissoc*` protocol + `dissoc` method for `BitmapIndexedNode` are in place (task 2.2) — a miss returns the map unchanged, and `bmiDissoc`'s sub-node recursion now goes through `dissoc*` (type dispatch, the Toccata counterpart of C's `baseDissoc`); the `dissoc` method is exercised by tests 2.3–2.7 (remove from a flat node → `bmiShrink`; remove the last entry → `emptyBMI`; missing key — bit-not-set and key-mismatch — returns the map unchanged; remove a key inside a sub-node → child shrinks, parent cloned via `bmiUpdate`; remove the last key inside a sub-node that is the parent's only entry → the whole map collapses to `emptyBMI`). Phase 3 (`ArrayNode`) in progress: the Toccata-level `arrayNodeCopyAssoc` (task 3.5) and `arrayNodeMutateAssoc` (task 3.6) are implemented — both route the sub-node recurse through a protocol (`copyAssoc`/`assoc*` respectively, type dispatch) and install a fresh BMI child into an empty slot; `arrayNodeMutateAssoc` borrows the child (`arrayNodeMutateChild`, no `incRef`) and swaps the slot in place (`arrayNodeSetSlot`), so an exclusively-owned child is mutated in place and a shared one is copied. The `ArrayNode` protocol methods are wired (task 3.7): `copyAssoc`/`assoc*` (→ 3.5/3.6, the `refs-count == 3` dispatch), `count` (wraps the C `arrayNodeCount`), `get*`/`dissoc*` (Toccata-level — occupied slots recurse through the `get*`/`dissoc*` protocols), and `vec` (Toccata-level slot walk — the C `arrayNodeVec` aborts via the dead `hashVec` dispatcher). Phase 3 complete: the ArrayNode test suite (`test-array-node`) is seeded (task 3.1) with its cases in 3.9–3.15 — count, get (hit/miss), vec, assoc (empty-slot and sub-node-slot, copy and mutate paths), and dissoc (empty-slot miss, child key-mismatch miss, child shrink, child → `emptyBMI` slot). Phase 4 (`collisionAssoc` + collision children) complete: the CollisionNode test suite (`test-collision-node`) is seeded (task 4.1), the `emptyCollisionNode` constructor is wrapped (task 4.2), and the Toccata-level `collisionAssoc` is implemented (task 4.3) — mirroring the C `collisionAssoc` (hash match → update the first `=`-equal entry or append; hash mismatch → promote to a BMI), with the mechanical node building in two new wrappers (`collisionUpdate`/`collisionAppend`) and the semantic parts in the `=`/`sha1` protocols; the `HashCollisionNode` `copyAssoc`/`assoc*` methods are wired (task 4.4) — both route to the Toccata `collisionAssoc` (C's `copyAssoc` dispatcher sends `HashCollisionNodeType` → `collisionAssoc`, and C's `mutateAssoc` has no collision case, so `assoc*` always copies — no in-place variant exists), which also fixes the BOOM when `assoc*`/`copyAssoc` recurses into a collision-node child; the `HashCollisionNode` `vec` method is wired (task 4.9) via the new `collisionVec` wrapper over the C `collisionVec` (runtime3.c:2633 — alive and purely mechanical, so called directly, the `arrayNodeCount` pattern; its NULL guard lets a fresh `emptyCollisionNode` vec to `[]`); the `HashCollisionNode` `dissoc*` method is wired (task 4.11) via the Toccata `collisionDissoc` (the `=` protocol with the lookup key first, per the C `collisionDissoc`/`bmiDissoc`; the mechanical shrink in the new `collisionShrink` wrapper) — a miss returns the node unchanged, removing the last entry returns `emptyBMI`, and a multi-entry hit shrinks via `collisionShrink`; the BMI-side integration is exercised (task 4.12) — `assoc*` into a map whose child is a collision node (the 4.4 BOOM fix): the borrowed child is copied via `collisionAssoc` (append/update) and reinstalled by `bmiSetChild`. Phase 5 (`hash-seq`/`vals` + stress) complete: the `hash-seq` protocol and the Toccata-level `bmiHashSeq`/`arrayNodeHashSeq` are implemented (task 5.1) — the seed vector is threaded through the recursion and each slot's pair(s) conjs'd in slot order, the sub-node recurse going through the `hash-seq` protocol (type dispatch, the Toccata counterpart of the dead C `hashSeq`/`hashVec` dispatchers); the `HashCollisionNode` `hash-seq` reuses the C `collisionVec` (alive and mechanical); the `vals` method for all three node types maps over `hash-seq m []` extracting the pair value via the new `pair-val` helper (a destructuring lambda does not codegen). The 3-entry BMI `hash-seq`/`vals` test is in `test-bmi.toc` (task 5.2), the ArrayNode `hash-seq`/`vals` test is in `test-array-node.toc` (task 5.3), and the collision-node test is in `test-collision-node.toc` (task 5.4). The 27-entry stress test is in `test-array-node.toc` (task 5.5) — a 27-entry map built by `reduce` over a range (I60 keys, the commented tests' shape) promotes to an ArrayNode, asserting count 27; the 2001-entry stress test is in `test-array-node.toc` (task 5.6) — a 2001-entry map built by `reduce` over a range (CHash keys 0..2000, the commented tests' key type) promotes to a 3-level ArrayNode → ArrayNode → BMI structure, asserting count 2001 and type 12. Note: building the 27-entry map with I60 keys exposed a pre-existing `runtime3.c` bug — `integerSha1` (runtime3.c:1829) reads 8 bytes from a 4-byte `unsigned` local, capturing 4 bytes of uninitialized stack padding, so the I60 hash is non-deterministic across call contexts; the `addCopiedBMI` promotion path recomputes each flat key's hash via `getHashVal`→`integerSha1` in a different C context than the Toccata insertion, so promoted sub-nodes get bitmap bits inconsistent with the lookup hash and I60 `get*` misses in promoted ArrayNodes (count/vec are unaffected; out of scope to fix here — a `runtime3.c` change). Phase 6 (`map-level assoc`) complete: the `HashMap` `assoc` method is wired (task 6.1) — `(assoc m k v)` → `(assoc* m k v (sha1 k) 0)` in the existing `extend-type HashMap` block (one method registered for all three node types, the `empty?` pattern); no tests added (the owner's, per the task). **Roadmap complete (task 6.2):** `make -j tests` fully green — every suite prints its success line, `diff: 0`, `remaining nodes: 0` (except the known deferred `test-fusing` leak), and all `.rslt` byte-identical to baseline; `hash-map-regressions.toc` is left untouched for the owner. Phase 7 (task 7.1) then closed a gap the owner's integration-suite work exposed: `HashCollisionNode` had no `count` method (BMI and ArrayNode do), so `(count m)` — and `empty?`/the `=` method, which dispatch `count` — aborted with `No implementation of 'count' found for type HashMap (13)` on a collision node; fixed with a one-line `(count [m] (collisionCount m))` in the `extend-type HashCollisionNode` block (the `collisionCount` defn already reads `node->count / 2` and consumes its arg, the `bmiCount`/`arrayNodeCount` pattern).

## Big Picture: Node Types and the Toccata-Level Assoc Requirement

There are 3 hash-map node types, each with a copy-assoc and a mutate-assoc function in `runtime3.c`:

| Node type | C copy-assoc | C mutate-assoc |
|---|---|---|
| `BitmapIndexedNode` | `bmiCopyAssoc` (runtime3.c:2270) | `bmiMutateAssoc` (2297) |
| `ArrayNode` | `arrayNodeCopyAssoc` (2456) | `arrayNodeMutateAssoc` (2494) |
| `HashCollisionNode` | `collisionAssoc` (2514) | none — the `HashCollisionNodeType` case in `mutateAssoc` (2775) is commented out, so collision nodes fall back to `copyAssoc` |

The C-level dispatchers are `copyAssoc` (runtime3.c:2761) and `mutateAssoc` (2775), switching on `node->type`.

**Why the C versions can't be called from Toccata.** They perform equality checks internally via the C-level `equal` (runtime3.c:1875), which only handles I60 and String (`strCmp`, BOOM for anything else) and cannot dispatch to the `=` protocol function. Correct behavior for arbitrary key types (e.g. deftypes with custom `=`) requires the `=` protocol, which lives at the Toccata level — C cannot call up into it.

**Consequence.** The copy-assoc and mutate-assoc logic for all 3 node types must be implemented in Toccata, using the wrapped C functions for the mechanical parts (bitmap ops, node cloning, array manipulation) and the protocol `=`/`sha1` for the semantic parts. The Toccata-level `bmiCopyAssoc` (hvm-core.toc:1186) is the working example of this pattern: it does `(= currKey k)` in Toccata and delegates to `bitpos`, `bmiBitMap`, `bmiCopyChild`, `bmiKey`, `bmiVal`, `bmiClone`, `bmiUpdate`, `addCopiedBMI`, `bmiReplaceCopied`.

**Status of the 5 functions at the Toccata level:** `bmiCopyAssoc`, `bmiMutateAssoc`, `arrayNodeCopyAssoc`, and `arrayNodeMutateAssoc` are implemented (the BMI pair fully tested; the ArrayNode pair scratch-verified, with the `ArrayNode` protocol methods wired in task 3.7). `collisionAssoc` (copy) is not yet implemented in Toccata. Collision nodes need no mutate variant (copy path only, matching the C dispatcher).

## Protocol Functions NOT wired up yet

- `count` — returns count of sequence
- `sha1` — computes SHA1 hash
- `get` — polymorphic get dispatch
- `baseDissoc` — polymorphic dissoc dispatch
- `vals` — get all values

## BMI Surface (what's testable from Toccata today)

**Exposed in `hvm-core.toc`:** `emptyBMI`, `emptyArrayNode` (constructor — fresh empty ArrayNode per call, caller owns the ref), `emptyCollisionNode` (constructor — fresh empty HashCollisionNode per call, caller owns the ref; `count` field is dual-use so `collisionCount` reports 2, not 0, for a fresh node), `arrayNodeCopyChild` (owned — `incRef`s the sub-node, `None` for an empty slot), `arrayNodeUpdate` (clone with slot `idx` replaced — the C `bmiUpdate` same-child no-op path), `arrayNodeCopyAssoc` (Toccata-level — sub-node slots recurse through the `copyAssoc` protocol; an empty slot gets a fresh BMI child), `arrayNodeMutateChild` (borrowed — no `incRef`, `None` for an empty slot — the `bmiMutateChild` pattern), `arrayNodeSetSlot` (in-place slot set — the C `arrayNodeMutateAssoc` `node->array[idx] = ...`), `arrayNodeMutateAssoc` (Toccata-level — sub-node slots recurse through the `assoc*` protocol; an empty slot gets a fresh BMI child), `arrayNodeCount` (read-only — the C `arrayNodeCount` consumes its arg; sums `mapCount` over the non-empty slots), `arrayNodeGet` (Toccata-level — occupied slots recurse through the `get*` protocol), `arrayNodeDissoc` (Toccata-level — occupied slots recurse through the `dissoc*` protocol; an emptied child leaves its slot pointing at `emptyBMI`), `arrayNodeVec` (Toccata-level recursion — the C `arrayNodeVec` aborts via the dead `hashVec` dispatcher), `bitpos`, `bmiBitMap`, `bmiKey`, `bmiVal`, `bmiCopyChild` (owned — `incRef`s the child), `bmiMutateChild` (borrowed — no `incRef`), `bmiClone`, `bmiUpdate`, `addCopiedBMI`, `bmiReplaceCopied`, `bmiCopyAssoc`, the mutate family (`bmiMutateAssoc`, `bmiReplaceMutate`, `bmiSetKV`, `bmiSetChild`), `bmiGet` (Toccata-level — sub-node slots recurse through the `get*` protocol), `bmiDissoc` (Toccata-level — sub-node slots recurse through the `dissoc*` protocol, the Toccata counterpart of C's `baseDissoc` dispatcher; `=` protocol with the lookup key first, per the C `bmiDissoc` and the commented integration tests' eq-gate expectation), `bmiSlotCount` (read-only — popcount of the bitmap), `bmiShrink` (copy with one slot removed — the C `bmiDissoc` flat-removal path), `collisionCount` (read-only — the C `collisionCount` consumes its arg), `collisionKey`/`collisionVal` (owned refs — entries are always non-NULL), `collisionGet` (Toccata-level — linear scan, `=` protocol, entry key first), `collisionUpdate` (clone with entry `i` replaced by (k v) — the C `collisionAssoc` update outcome, count unchanged), `collisionAppend` (clone with (k v) appended — the C `collisionAssoc` append outcome, count +1), `collisionShrink` (clone with entry `i` removed — the C `collisionDissoc` multi-entry hit path, surviving entries compacted), `collisionVec` (wraps the C `collisionVec` directly — alive and purely mechanical, no key equality; conjs (k v) pairs in entry order, NULL-guarded; consumes the node and the initial vec), `collisionAssoc` (Toccata-level — hash match → `collisionUpdate`/`collisionAppend`; hash mismatch → promote to a BMI via `copyAssoc` on `emptyBMI` + an `assoc*` fold of the existing entries), `collisionDissoc` (Toccata-level — `=` protocol with the lookup key first, per the C `collisionDissoc`/`bmiDissoc`; last entry → `emptyBMI`, miss → node unchanged, multi-entry hit → `collisionShrink`), `refs-count`, `set-hash-val`, `bmiCount`/`count`, `bmiHashVec`, `bmiVec`/`vec` (Toccata-level recursion — the C `bmiHashVec` aborts on sub-node slots via the dead `hashVec` dispatcher), `bmiHashSeq`/`arrayNodeHashSeq` (Toccata-level `hash-seq` — the seed vector is threaded through the recursion, each slot's pair(s) conjs'd in slot order; the sub-node recurse goes through the `hash-seq` protocol, type dispatch — the C `bmiHashSeq`/`arrayNodeSeq` route through the dead `hashSeq` dispatcher), `pair-val` (the value of a [k v] pair — `extract (get pair 1)`; a destructuring lambda does not codegen), `empty?`, the `copyAssoc`/`assoc*`/`get*`/`dissoc*`/`hash-seq` protocols, the `get` method for `BitmapIndexedNode` (sentinel default → `Some`/`None`), the `dissoc`/`dissoc*` methods for `BitmapIndexedNode` (miss returns the map unchanged), the `hash-seq`/`vals` methods for all three node types (`hash-seq` → the Toccata `bmiHashSeq`/`arrayNodeHashSeq`, or `collisionVec` for a collision node; `vals` → `map` over `hash-seq m []` extracting the pair value via `pair-val`), the `copyAssoc`/`assoc*`/`get*`/`vec`/`dissoc*` methods for `HashCollisionNode` (`copyAssoc`/`assoc*` both route to the Toccata `collisionAssoc` — C's `mutateAssoc` has no collision case, so `assoc*` always copies; `vec` → `collisionVec` over the C `collisionVec`; `dissoc*` → the Toccata `collisionDissoc`), the `copyAssoc`/`assoc*`/`get*`/`count`/`vec`/`dissoc*` methods for `ArrayNode`, the `assoc` method for `HashMap` (the map-level `assoc` — `(assoc m k v)` → `(assoc* m k v (sha1 k) 0)`; `extend-type HashMap` registers it for all three node types — types 11/12/13 all map to the name "HashMap" in the codegen typeNameMap, the `empty?` pattern; the `refs-count` dispatch inside `assoc*` handles mutate-vs-copy — verified the mutate path fires when the map is used exactly once)

**Not exposed** (in `runtime3.c` only — can't test until wrapped): `mapGet` (the type dispatcher — protocol dispatch replaces it), `addMutateBMI` (the Toccata `bmiMutateAssoc` reuses `addCopiedBMI` for the empty-slot case), raw `bmiSetKey`/`bmiSetVal`

**Currently covered in test-bmi.toc:** 39 cases — Group A (`bmiCopyAssoc` integration, 10), Group B (`bmiMutateAssoc` integration, 9), `assoc*` collision-child (2: append, update — the 4.4 BOOM fix, the `bmiMutateAssoc` child-recurse into a HashCollisionNode), `get` (7: flat hit, flat miss bit-not-set, flat miss key-mismatch, sub-node hit, sub-node miss bit-not-set, sub-node miss key-mismatch, collision-node hit), `dissoc` (6: flat-node remove, last-entry → `emptyBMI`, missing-key no-op bit-not-set, missing-key no-op key-mismatch, sub-node remove → child shrink + parent clone, sub-node emptied as the parent's only entry → collapse to `emptyBMI`), promotion (2: 16-entry flat-BMI precondition, 17th-entry BMI→ArrayNode promotion → count 17), `hash-seq`/`vals` (2: flat 3-entry — `hash-seq` with empty and seeded vectors + `vals`, 3-entry with sub-node — protocol-dispatched sub-node recurse, pairs flattened in), plus bitmap-of-empty. See the checklists below.

## BMI Tests (`regression-tests/test-bmi.toc`)

The C-level tests in `hash-map-plan-c.md` verify the raw functions. The cases below cover the Toccata protocol layer — the inline-C wrappers and the Toccata `bmiCopyAssoc`/`bmiMutateAssoc` implementations.

**Running the tests:** `make test-bmi` is the correct way to run this suite. `make` can be used for any Makefile target without thinking further — no need to run the underlying `./new-toc` + `clang` steps by hand. The full suite can be run in parallel with `make -j tests` — much faster than serial `make tests`.

### Prerequisite: controlled-hash key type

Add a `ControlledHash`-style deftype (same pattern as `hash-map-regressions.toc`) so tests can pin exact bit positions:

```
(deftype CHash [h eq]
  (= [x y] (and (= (type-num x) (type-num y)) eq (= h (.h y))))
  (sha1 [_] h))
```

Useful hashes: `3` (bit 3), `35` (bit 3 at shift 0, bit 1 at shift 5), `4` (bit 4), `67` (bit 3, then bit 2), `195` (bit 3, then bit 6 at shift 5), and `7` with `eq=None` vs `eq=Some` for full collisions. Note `=` is asymmetric by design (the `eq` gate is on the first arg): to make two same-hash keys unequal, the `eq=None` key must be the *new* key in the assoc.

### Test cases

Low-level functions (`bitpos`, `bmiBitMap`, `bmiKey`, `bmiVal`, `bmiCopyChild`, `bmiMutateChild`, `bmiClone`, `bmiUpdate`, `addCopiedBMI`, `bmiReplaceCopied`) are not tested standalone right now — they are covered via the higher-level tests below.

**A. `bmiCopyAssoc` integration**
- [x] *(existing)* add to empty
- [x] *(existing)* same key + same value → no-op, count 1
- [x] same key, **different** value → count 1, `vec == [[k v2]]` (bmiClone branch)
- [x] different key, different bit → count 2 (add branch)
- [x] different key, same bit, different hash at shift 5 (3 vs 35) → count 2, sub-node (replace→createNode branch)
- [x] different key, identical hash (7/7 collision) → count 2, collision node (replace→collision branch)
- [x] **recurse-into-child branch:** node with keys 3+35, then assoc key 67 (bit 3 again) → count 3
- [x] **child-recurse + add at depth 1:** keys 3, 35, 67, then 195 (bit 3 again → bit 6 at shift 5, empty slot in the child) → count 4
- [x] same key, different value, **key located inside a sub-node** (depth 2) → count unchanged, `vec` shows updated value (child-recurse + clone at depth)
- [x] same key, same value, **key located inside a sub-node** (depth 2) → count and `vec` unchanged (child-recurse no-op → `bmiUpdate` same-child path)

Note: `createNode`'s same-bit-at-next-level recursion is covered only via the mutate path (Group B, 3 vs 1027) — it is shared C code, but the copy path (`bmiReplaceCopied`) never drives it.

**B. `bmiMutateAssoc` integration** (reached via `assoc*`, which routes here when `refs-count m == 3`)
- [x] same key, different value → `bmiSetKV` in place, count 1, `vec` updated
- [x] same key, **same** value → `bmiSetKV` still runs (the mutate path has no no-op branch), count 1, `vec` unchanged
- [x] different key, different bit → `addCopiedBMI`, count 2
- [x] different key, same bit, different hash at shift 5 (3 vs 35) → `bmiReplaceMutate` (createNode), count 2, `vec` flattened
- [x] different key, same bit at shifts 0 and 5, different at 10 (3 vs 1027) → `bmiReplaceMutate` (createNode recursion — nested sub-node), count 2, `vec` flattened
- [x] different key, identical hash (7/7 collision) → `bmiReplaceMutate` (collision node), count 2
- [x] **recurse-into-child, exclusive child** → child rebuilt with the new key, count 3, `vec` flattened
- [x] **recurse-into-child, same key inside sub-node** → `bmiSetKV` at depth, count unchanged, `vec` updated
- [x] **shared child** (sub-node also held by a second map) → child's refs-count ≠ 3, so `assoc*` copies instead of mutating; the mutated map gains the key, the other map is untouched (copy fallback — see Lessons)

**C. `count` / `vec` recursion (untested beyond count-1)**
- [ ] flat 3-entry node → count 3
- [ ] mixed node (2 flat + sub-node of 2) → count 4
- [ ] `vec` of flat node → `[[k1 v1] [k2 v2] ...]` in bit order
- [ ] `vec` of node with sub-node → sub-node's pairs **flattened in** (not nested)

**D. `empty?` (untested)**
- [ ] `(empty? emptyBMI) == (Some emptyBMI)`
- [ ] `(empty? one-entry-node) == None`

**E. Memory hygiene (implicit, per status.md failure conditions)**
Every case above must end with `diff: 0` and `remaining nodes: 0` (see Lessons for what the counters actually measure). Highest-risk paths, all now exercised: owned refs from `bmiKey`/`bmiVal`/`bmiCopyChild` (the `test-bmi` double-free area), the no-op branch's `dec_and_free` dance, `bmiUpdate` same-child path, collision-node `incRef`s, `bmiSetKV`'s double-release of the old slot's key/val, and `bmiSetChild`'s slot swap in both the in-place and copy-fallback child cases.

**When a memory leak or double-free is found** (non-zero `diff:`, `refs too small` abort, or end-of-test leak), follow the `skills/memory-leak-hunting.md` skill — it covers the shrink-to-minimal-expression workflow, refcount trajectory logging, the lldb workflow, and the known leak/double-free patterns.

### Out of scope until more is exposed

- `get` / `dissoc` tests — belong in `hash-map-regressions.toc` once `bmiGet`/`bmiDissoc` are wrapped
- 16+ entry ArrayNode conversion — the `BOOM("wtf")` guard in copied `addCopiedBMI` has been removed; the C-level branch is exercised and fixed (task 3.2, `testBmiCopyAssocPromote`); the Toccata-level exercise is Roadmap 3.8
- `bmiKey`/`bmiVal` on a sub-node slot — aborts: the slot's key is `0` and the wrapper unconditionally `incRef`s it (`incRef` aborts on NULL, `runtime3.c:808`). Needs a `None` return for sub-node slots before it can be tested
- shift-exhaustion (`shift > 60` abort) — deliberate crash, don't test

## Roadmap: Remaining Toccata-Level Work (Ralph loop tasks)

The BMI assoc side is done (Groups A/B, fully branch-tested). What remains is the rest of the Toccata-level hash-map functionality. This section is the task list for the Ralph loop: **each iteration implements exactly one unchecked task, in list order, and nothing else.** Tasks are dependency-ordered — do not skip ahead. The commented-out tests in `regression-tests/hash-map-regressions.toc` are a reference for what the full-map functionality should do — but do not run or modify that suite: it is the final integration test, run by the owner after this roadmap is done. This roadmap supersedes the "Out of scope until more is exposed" list as those items get unblocked.

### Conventions (apply to every task)

1. First check `git status`: if a previously interrupted iteration left uncommitted changes, either finish that task properly or revert them before starting.
2. Before wrapping anything, read the C reference function in `runtime3.c` and the existing wrappers in `hvm-core.toc`. The pattern to mirror is the `bmiCopyAssoc`/`bmiMutateAssoc` pair: C for the mechanical parts, protocol `=`/`sha1` for the semantic parts, owned refs from wrappers (see Lessons).
3. Verify after each change — every Toccata suite you touched or added (`make test-bmi`, `make test-array-node`, `make test-collision-node`, as applicable):
   - the suite's success line is printed (every `rt/test` passed)
   - `diff: 0` and `remaining nodes: 0`
   - the suite's `.rslt` diff shows only execution-stat changes
   - touched `hvm-core.toc` → `make -j tests` (the full suite, in parallel — much faster; no other suite may regress)
   - touched `runtime3.c` → additionally `make test-hash-map` (the 55 C tests)
     - **Shuffle-stability (run the binary 1000×, don't recompile):** `test-hash-map` runs its tests in a random order every run (Fisher-Yates seeded by `time ^ pid`), and its `check_counts` expectations are pool-sensitive (e.g. `testBmiCopyAssocPromote` `(0, 0)`, `testBmiMutateAssocPromote` `(120, 0)`), so a single run only validates one order. After any change that affects this suite (`runtime3.c` or `regression-tests/test-hash-map.c`), re-run the already-built binary 1000 times to confirm the counts stay order-independent:
       `for i in $(seq 1 1000); do ./regression-tests/test-hash-map >/dev/null 2>&1 || { echo "FAIL at $i"; break; }; done`
       Completes with no `FAIL` line = 1000/1000 green (verified 2026-08-22); a `FAIL at N` means iteration N aborted — re-run `./regression-tests/test-hash-map` once to see the message.

   **Known pre-existing failure (deferred, do not chase):** `test-fusing` leaks — `remaining nodes: 239 (656)` on stdout and `Leaked pairs!! 239` on stderr. This is present in the committed baseline, is unrelated to the hash-map work, and is deferred until later. When running `make -j tests`, ignore the `test-fusing` leak; every other suite must still be clean (`diff: 0`, `remaining nodes: 0`).
4. Memory error → follow `skills/memory-leak-hunting.md`. "Memory leak" includes double frees: a leak shows as non-zero `diff:` or `remaining nodes`; a double free shows as an abort from `dec_and_free` ("failure in decRefs, refs too small").
5. Compiler changes (`new.c`, codegen, rebuilding `toccata`/`new-toc`) are out of scope — respond STUCK. `runtime3.c` changes are allowed only when a task explicitly says so (or for a leak fix).
6. Mark the task `[x]`, update the BMI Surface / State lines if the exposed surface changed, and commit staging only the files you changed. Message: `task N: <short name> — <what/why>`.

### Phase 1 — `get`

- [x] **1.1** Wrap `bmiGet` (C ref `runtime3.c:2368` — `(node, key, def, hash, shift)`, returns `def` on miss) in `hvm-core.toc`. C's `mapGet` (2401) is the type dispatcher; at the Toccata level protocol dispatch replaces it, so wrap only the BMI variant for now.
- [x] **1.2** Add a `get*` protocol and a `get` method for `BitmapIndexedNode`, mirroring the `assoc*`/`copyAssoc` pattern: `get* [m k def hash shift]` → the wrapper; `get [m k]` → computes `(sha1 k)`, calls `get*` with a sentinel default, and maps the result to `Some`/`None` per the commented tests' usage (`(= (Some "a") (get ...))`).
- [x] **1.3** `test-bmi.toc`: get test — hit (flat).
- [x] **1.4** `test-bmi.toc`: get test — miss (returns `def`).
- [x] **1.5** `test-bmi.toc`: get test — hit inside a sub-node (depth 2).
- [x] **1.6** `test-bmi.toc`: get test — miss inside a sub-node.
- [x] **1.7** `test-bmi.toc`: get test — hit inside a collision node.

### Phase 2 — `dissoc`

- [x] **2.1** Wrap `bmiDissoc` (C ref `runtime3.c:2431` — `(node, key, hash, shift)`; returns `emptyBMI` when the last entry is removed; watch the sub-node shrink/clone paths).
- [x] **2.2** Add a `dissoc*` protocol and a `dissoc` method for `BitmapIndexedNode` (same pattern as 1.2; no default value — a miss returns the map unchanged).
- [x] **2.3** `test-bmi.toc`: dissoc test — remove from a flat node.
- [x] **2.4** `test-bmi.toc`: dissoc test — remove the last entry (→ `emptyBMI`).
- [x] **2.5** `test-bmi.toc`: dissoc test — remove a missing key (no-op).
- [x] **2.6** `test-bmi.toc`: dissoc test — remove a key inside a sub-node (shrink).
- [x] **2.7** `test-bmi.toc`: dissoc test — remove so a sub-node becomes empty and is the parent's only entry.

### Phase 3 — ArrayNode (16+ entries)

ArrayNode tests live in their own suite — `test-bmi.toc` is for BitmapIndexedNodes only. The suite covers both directly-built (3.4) and promoted (3.8) ArrayNodes.

- [x] **3.1** Create the ArrayNode test suite: `regression-tests/test-array-node.toc` (copy the scaffolding from `test-bmi.toc` — `rt` import, `CHash`/`Key1`/`Val` deftypes, main + success-println shape) and add `test-array-node` to `REG_TESTS` in the Makefile (the existing pattern rules build and run it exactly like `test-bmi`). Seed it with one trivially-green test and commit the baseline `.rslt`.
- [x] **3.2** C: 16+ promotion branch of `addCopiedBMI` — the `BOOM("wtf")` calls have been removed by the owner; the conversion code is now live and `make test-hash-map` passes, but the branch has never been exercised (the removed TODO flagged doubt about the `cloneBitmapIndexedNode(&emptyBMI, ...)` calls, and the C suite only tests mutate-side promotion via `addMutateBMI`). The first real exercise is 3.8 — if it exposes a bug in this branch, fixing it is in scope for that task. Done: `testBmiCopyAssocPromote` (C suite, 55 tests) drives `bmiCopyAssoc`'s empty-slot path on a full 16-slot node; it exposed the doubt for real — `cloneBitmapIndexedNode(&emptyBMI, ...)` sees `emptyBMI.bitmap == 0`, builds an empty sub-node, and silently drops the key/val (data loss + ref leaks). Fixed by building the sub-nodes like `addMutateBMI` does (`malloc_bmiNode(1)` + `bitpos(hash, shift+5)`), with copy-path refcounts (the old node is freed without clearing its slots, so each moved pair takes fresh refs). The size-1/size-16 BMI pools are prewarmed in the C suite's `main` so both promotion tests' `check_counts` stay order-independent under the shuffle (`testBmiMutateAssocPromote` 140→120).
- [x] **3.3** Make the Toccata mutate path promotable — decided: **reuse the fixed `addCopiedBMI`** in `bmiMutateAssoc`'s empty-slot case (no `addMutateBMI` wrapper). Reading the C: `addCopiedBMI` and `addMutateBMI` are identical except in the 16+ promotion branch, where `addCopiedBMI` incRefs existing entries (copy) and `addMutateBMI` moves them (no incRef, old slots cleared); both yield the same final ArrayNode (one ref per entry, old node freed), so `addCopiedBMI` is correct for an exclusively-owned node — its extra incRefs balance against the old node's release. The empty-slot case already called `addCopiedBMI` (now fixed by 3.2), so no code change was needed beyond documenting the decision in `bmiMutateAssoc`'s comment. Verified with a scratch build: a 16-entry BMI built via `assoc*` (the mutate path) promotes to an ArrayNode (type 12) on the 17th entry, `diff: 0`, `remaining nodes: 0`.
- [x] **3.4** Wrap a constructor for an empty `ArrayNode` (see how C's `testArrayNode` in `test-hash-map.c` allocates one) so tests can build ArrayNodes directly, not only via promotion. Done: `emptyArrayNode` in `hvm-core.toc` — a zero-arg defn whose inline C is `malloc_arrayNode()` (runtime3.c:650, declared in runtime3.h:376), the exact allocation C's `testArrayNode` uses. Returns a fresh node per call (all 32 slots 0, `refs = refsInit` — caller owns the ref; a `def`-inline would be a single shared global, so it must be a `defn`). Verified with a scratch build: type 12 (`ArrayNodeType`), two calls yield distinct pointers, `diff: 0`, `remaining nodes: 0`.
- [x] **3.5** Toccata `arrayNodeCopyAssoc` mirroring C `runtime3.c:2488` (protocol `=`/`sha1` for the semantic parts). Done: `arrayNodeCopyAssoc` in `hvm-core.toc` — the slot index is `mask(hash, shift) = (hash >> shift) & 31` (computed in Toccata via `bit-shift-right`/`bit-and`); the mechanical slot ops are two new wrappers, `arrayNodeCopyChild` (read slot `idx`, owned ref via `incRef`, `None` for an empty slot — the `bmiCopyChild` pattern) and `arrayNodeUpdate` (clone with slot `idx` replaced, the C `bmiUpdate` same-child no-op path). The semantic part is the sub-node recurse through the `copyAssoc` protocol (type dispatch — C's internal `equal` can't reach the `=` protocol); an empty slot gets a fresh BMI child (`copyAssoc` on `emptyBMI` at `shift+5`), mirroring C. No `=`/`sha1` at this level — the C `arrayNodeCopyAssoc` does no key equality (it routes by index; equality happens in the child's own `copyAssoc`); `set-hash-val` caches the hash on the key, per the `bmiCopyAssoc` convention. Verified with a scratch build (17-entry promoted ArrayNode): empty-slot assoc, occupied-slot assoc, no-op, and update all give the right counts with `diff: 0` and `remaining nodes: 0`; `make -j tests` clean (all `.rslt` byte-identical to baseline).
- [x] **3.6** Toccata `arrayNodeMutateAssoc` mirroring C `runtime3.c:2526`. Done: `arrayNodeMutateAssoc` in `hvm-core.toc` — the slot index is `mask(hash, shift) = (hash >> shift) & 31` (computed in Toccata via `bit-shift-right`/`bit-and`, the `arrayNodeCopyAssoc` convention). The mechanical slot ops are two new wrappers: `arrayNodeMutateChild` (read slot `idx`, **borrowed** — no `incRef`, `None` for an empty slot — the `bmiMutateChild` pattern; the ref the caller receives IS the slot's ref) and `arrayNodeSetSlot` (point slot `idx` at `child` in place — the C `node->array[idx] = ...`, no release of the old slot ref; transfers `m`'s ref and takes `child`'s). The semantic part is the sub-node recurse through the `assoc*` protocol (type dispatch — the Toccata counterpart of C's `mutateAssoc` dispatcher): the borrowed child is mutated in place when exclusively owned (`refs-count == 3`) and copied otherwise; either way `arrayNodeSetSlot` points the slot at the (re)built child — in the copy case the swap balances, since the copy path consumed the old child's slot ref. An empty slot gets a fresh BMI child (`copyAssoc` on `emptyBMI` at `shift+5`), mirroring C. The C-level `refs != 1` fallback is handled by the `assoc*` dispatch instead (task 3.7), per the `bmiMutateAssoc` convention. `set-hash-val` caches the hash on the key. Verified with a scratch build: empty-slot install, occupied-slot in-place child mutation, same-key update inside the child, and the shared-child copy fallback (a second map sharing the child is left untouched) all give the right counts/vecs with `diff: 0` and `remaining nodes: 0`; `make test-bmi`/`make test-array-node`/`make -j tests` clean (all `.rslt` byte-identical to baseline). Note: the shared-child scratch case needed a dataflow thread (a `seq2` helper whose result depends on `refs-count` of the copy) to force the copy before the mutation — independent let bindings are forced in no guaranteed order, and the unused-binding form of the thread does not reliably force.
- [x] **3.7** Wire the `ArrayNode` protocol methods: `copyAssoc`/`assoc*` (→ 3.5/3.6), plus wrappers + methods for `arrayNodeGet` (2589), `arrayNodeCount` (2607), `arrayNodeVec` (2709), `arrayNodeDissoc` (2727). Done: `extend-type ArrayNode` in `hvm-core.toc` — `copyAssoc`/`assoc*` route to 3.5/3.6 (`assoc*` with the same `refs-count == 3` magic-number dispatch as BMI: mutate in place when exclusively owned, copy otherwise). `count` wraps the C `arrayNodeCount` directly (runtime3.c:2613 — it sums `mapCount` over the non-empty slots, all mechanical I60 arithmetic, no key equality, so no Toccata reimplementation is needed). `get*` (Toccata `arrayNodeGet`, mirroring runtime3.c:2595) and `dissoc*` (Toccata `arrayNodeDissoc`, mirroring runtime3.c:2733) recurse an occupied slot through the `get*`/`dissoc*` protocol at shift+5 — type dispatch, the Toccata counterpart of C's `mapGet`/`baseDissoc` dispatchers (C's `arrayNodeGet` calls `bmiGet` directly, which can't reach the `=` protocol and can't handle a collision-node child); `arrayNodeDissoc`'s no-change case rides `arrayNodeUpdate`'s same-child path, and an emptied child leaves its slot pointing at `emptyBMI` — an ArrayNode never collapses to `emptyBMI` itself. `vec` (Toccata `arrayNodeVec`) walks slots 0..31 in index order, flattening each occupied slot's child `vec` in (the C `arrayNodeVec` at runtime3.c:2715 aborts — its body is commented out, routing through the dead `hashVec` dispatcher). Verified with a scratch build (17-entry promoted ArrayNode): type 12, count 17, vec in slot order, get* hit/miss, dissoc* remove/miss, copyAssoc empty-slot/occupied-slot, assoc* in-place empty-slot/occupied-slot — all with `diff: 0` and `remaining nodes: 0`; `make test-bmi`/`make test-array-node`/`make -j tests` clean (all `.rslt` byte-identical to baseline).
- [x] **3.8** `test-bmi.toc`: the promotion test — assoc a 17th entry into a BMI map (BMI→ArrayNode), count 17. Done: `test-promotion` in `test-bmi.toc` — builds a 16-entry flat BMI via `reduce (range 16)` (16 `CHash` keys with 16 distinct bits at shift 0), asserts count 16 (the full-flat-BMI precondition), then `copyAssoc`es a 17th key (hash 20 → empty bit) whose `bmiCopyAssoc` empty-bit branch calls `addCopiedBMI`'s `n >= 16` promotion branch (BMI→ArrayNode: each flat entry wrapped in its own 1-entry BMI sub-node, the new key in a fresh sub-node at its slot); asserts count 17 (dispatches to ArrayNode's `count` = C `arrayNodeCount` summing `mapCount` over the 17 non-empty slots). `make test-bmi` clean (`BMI tests are good`, `diff: 0`, `remaining nodes: 0`, `.rslt` diff only execution-stats).
- [x] **3.9** `test-array-node.toc`: count test. Done: `test-count` in `test-array-node.toc` — two cases over the C `arrayNodeCount` (runtime3.c:2613, sums `mapCount` over the non-empty slots): (1) an empty ArrayNode built directly via `emptyArrayNode` (all 32 slots 0) → count 0; (2) a promoted ArrayNode — a 16-entry flat BMI built via `reduce (range 16)` (16 `CHash` keys with 16 distinct bits at shift 0), then `copyAssoc` of a 17th key (hash 20 → empty bit) whose `bmiCopyAssoc` empty-bit branch calls `addCopiedBMI`'s `n >= 16` promotion branch (BMI→ArrayNode, 17 one-entry BMI sub-nodes) → count dispatches to ArrayNode's `count` (C `arrayNodeCount` summing `mapCount` over the 17 non-empty slots) → 17. `make test-array-node` clean (`ArrayNode tests are good`, `diff: 0`, `remaining nodes: 0`, `.rslt` diff only execution-stats).
- [x] **3.10** `test-array-node.toc`: get test — hit. Done: `test-get` in `test-array-node.toc` — a promoted ArrayNode (a 16-entry flat BMI via `reduce (range 16)`, then `copyAssoc` of a 17th key, hash 20 → empty bit, whose `bmiCopyAssoc` empty-bit branch calls `addCopiedBMI`'s `n >= 16` promotion branch → an ArrayNode of 17 one-entry BMI sub-nodes). `get*` is asserted with a `def` value (ArrayNode has no `get` method, only the `get*` protocol): `arrayNodeGet` routes by slot index `mask(hash, shift)` and recurses the occupied slot's child at shift+5 through the `get*` protocol; two hits are asserted — key 5 (slot 5, from the flat part) → `Val 6`, and key 20 (slot 20, the 17th) → `Val 21`. `make test-array-node` clean (`ArrayNode tests are good`, `diff: 0`, `remaining nodes: 0`, `.rslt` diff only execution-stats).
- [x] **3.11** `test-array-node.toc`: get test — miss. Done: `test-get-miss` in `test-array-node.toc` — the same promoted ArrayNode as `test-get` (a 16-entry flat BMI via `reduce (range 16)`, then `copyAssoc` of a 17th key, hash 20 → empty bit, `addCopiedBMI`'s `n >= 16` promotion → 17 one-entry BMI sub-nodes, slots 0..15 and 20 occupied); `get*` with a `def` value asserts both of `arrayNodeGet`'s miss branches — key 21 (slot 21 = (21 >> 0) & 31, empty → `arrayNodeCopyChild` returns `None` → `def`) and key 69 (slot 5, occupied by key 5's one-entry BMI; at shift 5 both 69 and 5 have bit 0, but the hashes differ → the child's key-compare branch returns `def`). `make test-array-node` clean (`ArrayNode tests are good`, `diff: 0`, `remaining nodes: 0`, `.rslt` diff only execution-stats).
- [x] **3.12** `test-array-node.toc`: vec test. Done: `test-vec` in `test-array-node.toc` — two cases over the Toccata `arrayNodeVec` (the slot walk 0..31 in `arrayNodeVec-i`, flattening each occupied slot's child `vec` in via the `vec` protocol — type dispatch, the Toccata counterpart of the dead `hashVec` dispatcher): (1) an empty ArrayNode built directly via `emptyArrayNode` (all 32 slots 0) → vec `[]` (every slot's `arrayNodeCopyChild` returns `None`, contributing nothing); (2) a promoted ArrayNode — a 16-entry flat BMI via `reduce (range 16)` (16 `CHash` keys with 16 distinct bits at shift 0), then `copyAssoc` of a 17th key (hash 20 → empty bit) whose `bmiCopyAssoc` empty-bit branch calls `addCopiedBMI`'s `n >= 16` promotion branch (BMI→ArrayNode, 17 one-entry BMI sub-nodes, slots 0..15 and 20 occupied) → vec flattens the children in slot order → 17 pairs: keys 0..15 → values 1..16 (slots 0..15), then key 20 → `Val 21` (slot 20); the expected vec is built by `reduce (range 16)` over the same keys/values plus the 17th pair `conj`ed on (slot 20 comes after slots 0..15). Each child is a one-entry BMI, so its own `vec` is a single pair and the overall order is the deterministic slot order. `make test-array-node` clean (`ArrayNode tests are good`, `diff: 0`, `remaining nodes: 0`, `.rslt` diff only execution-stats).
- [x] **3.13** `test-array-node.toc`: assoc test — into an empty slot. Done: `test-assoc` in `test-array-node.toc` — four cases over the empty-slot branch of `arrayNodeCopyAssoc`/`arrayNodeMutateAssoc` (the slot index `mask(hash, shift)` points at a 0 slot → a fresh 1-entry BMI child is installed via `copyAssoc` on `emptyBMI` at shift+5): (1) copy path on a directly-built empty ArrayNode (all 32 slots 0) → count 1, vec the single pair; (2) mutate path (`assoc*`, `refs-count == 3`) on the empty ArrayNode → count 1, vec the single pair; (3) copy path on a promoted 17-entry ArrayNode (slots 0..15 + 20 occupied) with a key at empty slot 21 (hash 21) → count 18, `get*` hits the new key (Val 22) and an old key (Val 6); (4) mutate path on the promoted ArrayNode → count 18, `get*` hits both. `make test-array-node` clean (`diff: 0`, `remaining nodes: 0`, `.rslt` only execution-stats).
- [x] **3.14** `test-array-node.toc`: assoc test — into a sub-node slot. Done: `test-assoc-subnode` in `test-array-node.toc` — four cases over the occupied-slot branch of `arrayNodeCopyAssoc`/`arrayNodeMutateAssoc` (the slot index `mask(hash, shift)` points at an occupied slot → the sub-node recurse at shift+5 through the `copyAssoc`/`assoc*` protocol): on the promoted 17-entry ArrayNode (slots 0..15 + 20 occupied, each a 1-entry BMI child), targeting slot 5 (key 5's child, bit 0 at shift 5): (1) copy path, new key 37 (slot 5; bit 1 at shift 5, empty in the child) → the child's `addCopiedBMI` branch → 2-entry child, `arrayNodeUpdate` clones the parent with slot 5 replaced → count 18, `get*` hits the new key (Val 38), the old key in the same child (Val 6), and a key in another slot (Val 21); (2) mutate path (`assoc*`, `refs-count == 3`), new key 37 → the borrowed child (exclusively owned) is mutated in place, `arrayNodeSetSlot` points slot 5 at the rebuilt child → count 18, same `get*` hits; (3) copy path, same key 5 different value → the child's `bmiClone` branch → count 17, `get*` shows Val 99 and another slot intact; (4) mutate path, same key 5 different value → the child's `bmiSetKV` in place (the mutate path has no no-op branch) → count 17, same. Note: the mutate cases use the 3.13 shape (m let-bound, used exactly once, m2 in a nested let) — re-reading m in the body adds a ref and flips `assoc*` to the copy path, so in-place-ness is not asserted. `make test-array-node` clean (`diff: 0`, `remaining nodes: 0`, `.rslt` only execution-stats); `make -j tests` clean (all other `.rslt` byte-identical).
- [x] **3.15** `test-array-node.toc`: dissoc test. Done: `test-dissoc` in `test-array-node.toc` — four cases over `arrayNodeDissoc` (ArrayNode has no `dissoc` method, only the `dissoc*` protocol — called with an explicit hash/shift, the `get*` convention): (1) miss — empty slot: the promoted 17-entry ArrayNode (slots 0..15 + 20), key 21 (slot 21 empty) → `arrayNodeCopyChild` returns `None` → the node is unchanged (count 17, `get*` still hits key 5 Val 6 + key 20 Val 21); (2) miss — key mismatch in child: key 1029 (slot 5; window bit 0 at shift 5, the same bit the child holds for key 5, but hash 1029 ≠ 5) → the child's `bmiDissoc` key-compare branch returns the child unchanged → `arrayNodeUpdate`'s same-child path → the node is unchanged (count 17, `get*` still hits key 5); (3) hit — child shrinks: the promoted ArrayNode + `copyAssoc` key 37 (slot 5, bit 1 at shift 5) → a 2-entry child → 18 entries; `dissoc*` key 5 → the child's `bmiDissoc` (bit set, flat slot, key match, popcount 2) → `bmiShrink` → a 1-entry child (key 37 survives), the parent cloned via `arrayNodeUpdate` with slot 5 replaced → count 17, `get*` hits key 37 (Val 38) + key 20 (Val 21), misses key 5, vec the 17 surviving pairs in slot order (slot 5 holds key 37's pair); (4) hit — child becomes empty: `dissoc*` key 5 (slot 5, a 1-entry child) → the child → `emptyBMI`; the parent is cloned with slot 5 pointed at `emptyBMI` — an ArrayNode never collapses to `emptyBMI` itself → count 16, `get*` misses key 5 + hits key 20, vec the 16 pairs (slot 5's `emptyBMI` contributes nothing). Every case asserts the source map is untouched (dissoc copies, it never mutates). `make test-array-node` clean (`diff: 0`, `remaining nodes: 0`, `.rslt` only execution-stats).

### Phase 4 — `collisionAssoc` + collision children

CollisionNode tests live in their own suite — `test-bmi.toc` is for BitmapIndexedNodes only.

- [x] **4.1** Create the CollisionNode test suite: `regression-tests/test-collision-node.toc` (same scaffolding as 3.1) and add `test-collision-node` to `REG_TESTS` in the Makefile. Seed with one trivially-green test and commit the baseline `.rslt`. Done: `test-collision-node.toc` — the scaffolding copied from `test-array-node.toc` (rt import, CHash/Key1/Val deftypes, main + success-println shape) and added to `REG_TESTS` so the existing pattern rules build and run it exactly like `test-bmi`/`test-array-node`; seeded with one trivially-green test (emptyBMI's bitmap is 0, the 3.1 seed). `make test-collision-node` green (`CollisionNode tests are good`, `diff: 0`, `remaining nodes: 0`); baseline `.rslt` committed; `make -j tests` clean (all other `.rslt` byte-identical).
- [x] **4.2** Wrap a constructor for an empty `HashCollisionNode` (see how C's `testCollisionNode` in `test-hash-map.c` allocates one) so tests can build collision nodes directly. Done: `emptyCollisionNode` in `hvm-core.toc` — a zero-arg defn whose inline C is `malloc_hashCollisionNode(2)` (runtime3.c:617, declared in runtime3.h:377), the exact allocation C's `testCollisionNode` uses (direct `my_malloc`, no pool). Returns a fresh node per call (type 13 `HashCollisionNodeType`, all entries NULL, `refs = refsInit` — caller owns the ref; a `def`-inline would be a single shared global, so it must be a `defn`). Note: C's `count` field is dual-use — `malloc` sets `count = 2 * capacity`, so `collisionCount` reports 2 for a fresh node, not 0 (C's `collisionVec`/`collisionGet` guard each entry with a NULL check for this reason; `collisionAssoc`/`collisionDissoc` do not). Verified with a scratch build: type 13, two calls yield distinct pointers, `diff: 0`, `remaining nodes: 0`; `make test-bmi`/`make test-array-node`/`make test-collision-node`/`make -j tests` clean (all `.rslt` byte-identical to baseline).
- [x] **4.3** Toccata `collisionAssoc` mirroring C `runtime3.c:2546` (add, update, promote to BMI on hash mismatch). Done: `collisionAssoc` in `hvm-core.toc` — the hash check is `(sha1 (collisionKey m 0)) == hash` (C reads `array[0]`'s hash; a collision node holds one hash). Hash match: `collisionAssoc-find` scans for the first entry whose key `=`-equals `k` (entry key first, the `bmiCopyAssoc` convention; the C `collisionDissoc` first-match convention) — a match calls the new `collisionUpdate` wrapper (clone with entry `i` replaced by (k v), count unchanged — C's `newNode->count -= 2`), a miss calls `collisionAppend` (clone with (k v) appended, count +1). Hash mismatch: promote to a BMI — `copyAssoc` on `emptyBMI` at shift 0 for the new (k v), then `collisionAssoc-promote-i` folds each existing entry in via `assoc*` at shift 0 with its own `sha1` (the Toccata counterpart of C's `mutateAssoc` loop; the fresh BMI is exclusively owned so `assoc*` mutates in place). The mechanical node building is two new inline-C wrappers over `malloc_hashCollisionNode` (`collisionUpdate`/`collisionAppend`); the semantic parts use the `=`/`sha1` protocols. The `HashCollisionNode` `copyAssoc`/`assoc*` methods are not wired yet (task 4.4). Verified with a scratch build (a 2-entry collision node built via BMI promotion, extracted with `bmiCopyChild`): add (count 3, values [v1 v2 v3]), update (count 2, values [v1 v3] — the `eq=Some` entry replaced, the other untouched), promote (BMI count 3) — all with `diff: 0` and `remaining nodes: 0`; `make test-bmi`/`make test-array-node`/`make test-collision-node`/`make -j tests` clean (all `.rslt` byte-identical to baseline).
- [x] **4.4** Wire `copyAssoc`/`assoc*` for `HashCollisionNode` — this also fixes the known BOOM when `assoc*` recurses into a collision-node child. Done: `extend-type HashCollisionNode` in `hvm-core.toc` gains `copyAssoc` and `assoc*`, both routing to the Toccata `collisionAssoc` (task 4.3). This mirrors the C dispatchers: C's `copyAssoc` (runtime3.c:2761) sends `HashCollisionNodeType` → `collisionAssoc`, and C's `mutateAssoc` (runtime3.c:2775) has its `HashCollisionNodeType` case commented out, falling through to `copyAssoc` — so there is no in-place variant for collision nodes and `assoc*` always copies (no `refs-count == 3` dispatch, unlike BMI/ArrayNode). The refcount accounting matches the shared-child copy pattern: `collisionAssoc` consumes the caller's ref to the (borrowed) child and returns a fresh node (or a promoted BMI on hash mismatch), and the `bmiSetChild`/`arrayNodeSetSlot` swap balances since the copy path consumed the old child's slot ref. This unblocks the `copyAssoc`/`assoc*` recurse into a collision-node child from `bmiCopyAssoc`/`bmiMutateAssoc`/`arrayNodeCopyAssoc`/`arrayNodeMutateAssoc` (previously a BOOM — no method for the type). `make test-bmi`/`make test-array-node`/`make test-collision-node`/`make -j tests` all clean (`diff: 0`, `remaining nodes: 0` except the known deferred `test-fusing` leak; all `.rslt` byte-identical to baseline). The direct exercise of the fixed BOOM path is task 4.12.
- [x] **4.5** `test-collision-node.toc`: add test — add a key to a collision node. Done: `test-assoc-add` in `test-collision-node.toc` — the hash-match/append branch of `collisionAssoc` (task 4.3): a 2-entry collision node is built the way a map builds one (two 7/7 keys assoc'd into `emptyBMI`, the first with `eq=None` so `bmiCopyAssoc`'s `(= currKey k)` is false → the collision branch → a BMI with a single slot (bit 7) holding `[k1 v1, k2 v2]`), extracted with `bmiCopyChild` (owned ref, bit mask 128 = 1 << 7), then `copyAssoc` of a third key (hash 7, `eq=Some`) that is not `=`-equal to either entry (both carry `eq=None`, so `collisionAssoc-find`'s entry-key-first `=` is false for both) → `collisionAppend` → a 3-entry collision node: count 3, the old entries preserved in order (their values pin the order — the `eq=None` keys are mutually indistinguishable under the `=` protocol), `get*` hits the new key. A fresh `emptyCollisionNode` is not a valid starting point (`collisionAssoc`/`collisionAppend` do not NULL-guard, per 4.2), so the BMI-promoted node is the construction. `make test-collision-node` clean (`CollisionNode tests are good`, `diff: 0`, `remaining nodes: 0`, `.rslt` only execution-stats).
- [x] **4.6** `test-collision-node.toc`: update test — update an existing key. Done: `test-assoc-update` in `test-collision-node.toc` — the hash-match/update branch of `collisionAssoc` (task 4.3): a 2-entry collision node is built the way a map builds one (two 7/7 keys assoc'd into `emptyBMI`, the first with `eq=None` so `bmiCopyAssoc`'s `(= currKey k)` is false → the collision branch → a BMI with a single slot (bit 7) holding `[k1 v1, k2 v2]`), the second key carrying `eq=Some` so a later same-hash lookup key can `=`-equal it (the eq gate is on the first arg, and `collisionAssoc-find` scans entry-key-first), extracted with `bmiCopyChild` (owned ref, mask 128 = 1<<7), then `copyAssoc` of a third key (hash 7, `eq=Some`): `collisionAssoc-find`'s entry-key-first `=` is false for entry 0 (k1's `eq=None` gate) and true for entry 1 → `collisionUpdate` clones the node with entry 1 replaced by (k3 v3), count unchanged → a 2-entry node `[k1 v1, k3 v3]`: count 2 (not 3 — the update, not the append, branch), entry 0 untouched, `get*` hits the updated value. `make test-collision-node` clean (`CollisionNode tests are good`, `diff: 0`, `remaining nodes: 0`, `.rslt` only execution-stats).
- [x] **4.7** `test-collision-node.toc`: promote test — promote to BMI on hash mismatch. Done: `test-assoc-promote` in `test-collision-node.toc` — the hash-mismatch/promote branch of `collisionAssoc` (task 4.3): a 2-entry collision node is built the way a map builds one (two 7/7 keys assoc'd into `emptyBMI`, the first with `eq=None` so `bmiCopyAssoc`'s `(= currKey k)` is false → the collision branch → a BMI with a single slot (bit 7) holding `[k1 v1, k2 v2]`), extracted with `bmiCopyChild` (owned ref, mask 128 = 1<<7), then `copyAssoc` of a third key with a **different** hash (3) → the hash-mismatch branch: `k3` goes into a fresh BMI at shift 0 (bit 3, flat), then the existing entries are `assoc*`'d in with their own `sha1` — `k1` (hash 7 → bit 7, a new empty bit → flat) and `k2` (hash 7 → bit 7 again; `(= currKey k)` is false — `k1`'s `eq=None` gate — so `bmiReplaceMutate`'s collision case) → the slot at bit 7 becomes a collision node `[k1 v1, k2 v2]`. The result is a `BitmapIndexedNode` (type 11), **not** a collision node: bitmap 136 (8 | 128), count 3 (1 flat + 2 in the collision child), `get*` hits `k3` (flat, Val 97) and `k2` (inside the collision child, Val 98 — `k1`'s entry is unreachable under `get*` because its `eq=None` gate sits on the first arg and `collisionGet` scans entry-key-first), the flat slot at bit 3 holds `k3`/Val 97, and the child at bit 7 is a 2-entry collision node (type 13) with the original entries preserved in order (Val 99, Val 98). `make test-collision-node` clean (`CollisionNode tests are good`, `diff: 0`, `remaining nodes: 0`, `.rslt` only execution-stats).
- [x] **4.8** `test-collision-node.toc`: count test. Done: `test-count` in `test-collision-node.toc` — four cases over the read-only `collisionCount` wrapper (HashCollisionNode has no `count` method, only the `collisionCount` defn, since the C `collisionCount` consumes its arg — the `get*`-convention analog): (1) a fresh `emptyCollisionNode` → 2, not 0 — pins the documented dual-use count-field quirk (C's `malloc_hashCollisionNode` sets count = 2 * capacity, `collisionCount` returns count / 2, the 4.2 note); (2) a 2-entry collision node built the way a map builds it (two 7/7 keys into `emptyBMI`, first `eq=None` → `bmiCopyAssoc`'s collision branch, extracted with `bmiCopyChild`, mask 128 = 1<<7) → 2; (3) append a third same-hash key (`eq=None`, not `=`-equal to either entry — the eq gate is on the entry key, the first arg) → `collisionAppend` → 3; (4) append a fourth (`eq=Some`, still not `=`-equal to any of the three `eq=None` entries) → `collisionAppend` again → 4. `make test-collision-node` clean (`CollisionNode tests are good`, `diff: 0`, `remaining nodes: 0`, `.rslt` only execution-stats).
- [x] **4.9** `test-collision-node.toc`: vec test. Done: `test-vec` in `test-collision-node.toc` — three cases over the `vec` protocol for `HashCollisionNode` (new `vec` method → the new `collisionVec` wrapper over the C `collisionVec`, runtime3.c:2633 — alive and purely mechanical (no key equality), so called directly, the `arrayNodeCount` pattern, unlike `arrayNodeVec`/`bmiHashVec`, which abort via the dead `hashVec` dispatcher): (1) a fresh `emptyCollisionNode` → `[]` — the C NULL guard skips the trailing slots (count = 2 * capacity, entries NULL, the 4.2 note); (2) a 2-entry collision node built the way a map builds it (two 7/7 `eq=None` keys into `emptyBMI` → `bmiCopyAssoc`'s collision branch, extracted with `bmiCopyChild`, mask 128 = 1<<7) → the two pairs in entry order; (3) a third same-hash key appended (`collisionAppend`) → the three pairs in entry order, the appended pair last. The expected keys carry `eq=Some` (same hash): the eq gate is on the first arg, and `vect-=` compares the expected element first. `make test-collision-node` clean (`CollisionNode tests are good`, `diff: 0`, `remaining nodes: 0`, `.rslt` only execution-stats); `make -j tests` clean (all other `.rslt` byte-identical to baseline).
- [x] **4.10** `test-collision-node.toc`: get test. Done: `test-get` in `test-collision-node.toc` — three cases over the `get*` protocol for `HashCollisionNode` (no `get` method — only `get*`, called with an explicit def/hash/shift, the `get*` convention; `get*` routes to the Toccata `collisionGet`, a linear scan with the `=` protocol entry-key-first, mirroring the C `collisionGet` at runtime3.c:2690, which never checks the hash): (1) hit — a 2-entry node built the way a map builds it (two 7/7 keys into `emptyBMI`, first `eq=None` → `bmiCopyAssoc`'s collision branch, extracted with `bmiCopyChild`, mask 128 = 1<<7), the second key carrying `eq=Some` so a same-hash lookup key can `=`-equal it; lookup k2 (hash 7, `eq=Some`) → the scan passes entry 0 (`= k1 k2` false via k1's `eq=None` gate) and hits entry 1 → Val 98; (2) miss, same hash, key mismatch — a 2-entry node with BOTH entries `eq=None`: the eq gate is on the first arg (the entry key), so no lookup key can `=`-equal an `eq=None` entry; lookup k3 (hash 7, `eq=Some`) scans both entries false → the scan's base case → def; (3) miss, different hash — the hit-case node (entries hash 7), lookup k3 (hash 3, `eq=Some`): CHash's `=` compares the first arg's h against the second arg's h (7 ≠ 3) → false for both → def (a real map would never route a hash-3 lookup to a hash-7 collision node — the parent's bitmap check fails first — but the scan is hash-agnostic, mirroring C; this pins that the `=` protocol, not the hash, drives the comparison). `make test-collision-node` clean (`CollisionNode tests are good`, `diff: 0`, `remaining nodes: 0`, `.rslt` only execution-stats).
- [x] **4.11** `test-collision-node.toc`: dissoc test. Done: `test-dissoc` in `test-collision-node.toc` — four cases over the `dissoc*` protocol for `HashCollisionNode` (no `dissoc` method — only `dissoc*`, called with an explicit hash/shift, the `get*`/ArrayNode-dissoc convention; `dissoc*` routes to the new Toccata `collisionDissoc`, mirroring the C `collisionDissoc` at runtime3.c:2649 — the `=` protocol with the **lookup key first**, the C `collisionDissoc`/`bmiDissoc` convention, so an eq-gated lookup key that gates off removes nothing; the mechanical shrink is the new `collisionShrink` wrapper over `malloc_hashCollisionNode`, the C multi-entry hit path with surviving entries compacted). Each case builds a collision node the way a map builds one (two 7/7 `eq=None` keys into `emptyBMI` → `bmiCopyAssoc`'s collision branch, extracted with `bmiCopyChild`, mask 128 = 1<<7): (1) multi-entry hit (n=2) — `dissoc*` a lookup key (hash 7, `eq=Some`) → the scan's lookup-key-first `=` is true for entry 0 → `collisionShrink` removes entry 0, compaction moves entry 1 to index 0 → a 1-entry node (count 1, survivor Val 98); (2) last-entry hit (n=1) — `dissoc*` the 1-entry node from (1) with a same-hash `eq=Some` key → the C `itemCount == 1` path frees the node and returns the global `emptyBMI` (count 0, bitmap 0, vec []); (3) multi-entry miss (n=2, key mismatch — `eq=None` gate) — `dissoc*` a lookup key (hash 7, `eq=None`) → the lookup-key-first `=` is gated off by the lookup key's own `eq=None` (first arg) → false for both entries → the scan's base case → the node returned unchanged (count 2, vec the two original pairs); (4) 1-entry miss (n=1, different hash) — `dissoc*` the 1-entry node with a different-hash key (3, `eq=Some`) → `(= k ek)` is false (CHash's `=` compares the first arg's h against the second arg's h, 3 ≠ 7) → the node returned unchanged (count 1, entry Val 98 intact). Every case asserts the source nodes are untouched (dissoc copies, it never mutates). `make test-collision-node` clean (`CollisionNode tests are good`, `diff: 0`, `remaining nodes: 0`, `.rslt` only execution-stats); `make -j tests` clean (all other `.rslt` byte-identical to baseline).
- [x] **4.12** `test-bmi.toc`: the BMI-side integration test — `assoc*` into a map whose child is a collision node. Done: `test-assoc-collision-child` in `test-bmi.toc` — two cases over the `bmiMutateAssoc` child-recurse into a HashCollisionNode (the dispatch that BOOMed before 4.4 — no method for the type — now routes via the `assoc*` protocol to HashCollisionNode's `assoc*`, which always copies: `collisionAssoc` consumes the borrowed slot ref from `bmiMutateChild` and returns a fresh node, and `bmiSetChild` points the slot at it — the swap balances, since the copy consumed the old slot ref). Each case builds the map the way a map builds one (two 7/7 keys into `emptyBMI`, the first with `eq=None` so `bmiCopyAssoc` takes the collision branch → a BMI with a single slot (bit 7, mask 128) holding a collision child), with `m` let-bound and used exactly once so `refs-count m == 3` → the mutate path: (1) append — the child holds two `eq=None` entries, so `collisionAssoc-find`'s entry-key-first `=` is false for both; a third same-hash key (`eq=Some`) is `collisionAppend`ed → a 3-entry collision child `[k1 v1, k2 v2, k3 v3]`; a flat sibling at bit 3 (`k0`) survives the in-place slot swap; count 4 (1 flat + 3 in the child), bitmap 136, the child is type 13 with the three values in entry order, `get` hits the new key and the sibling; (2) update — the child holds `k1` (`eq=None`) and `k2` (`eq=Some`); a third same-hash key (`eq=Some`) `=`-equals entry 1 (entry-key-first `=`: false for entry 0 via `k1`'s `eq=None` gate, true for entry 1) → `collisionUpdate` replaces entry 1 with (k3 v3), count unchanged → a 2-entry collision child `[k1 v1, k3 v3]`; count 2, bitmap 128, `get` hits the updated value. (The hash-mismatch/promote branch is not exercised here: it rebuilds at shift 0 — the C reference's quirk, pinned by 4.7 — so a parent-routed `get` of the new key would miss; that is a C-level issue, out of scope for this test.) `make test-bmi` clean (`BMI tests are good`, `diff: 0`, `remaining nodes: 0`, `.rslt` only execution-stats).

### Phase 5 — `hash-seq`/`vals` + stress

- [x] **5.1** Wire `vals`/`hash-seq` per the commented tests' usage in `hash-map-regressions.toc` (`(hash-seq full-bmi [""])` — read them to pin the exact protocol shape; the Toccata `bmiVec-bm` recursion is the working pattern for descending into sub-nodes; the C `hashVec` dispatcher is dead). No tests in this step — those are 5.2–5.4. Done: the `hash-seq` protocol (`(defp hash-seq [m s])` — conjs the [k v] pairs of `m` onto the seed vector `s`) and the Toccata-level `bmiHashSeq`/`arrayNodeHashSeq` in `hvm-core.toc`. The shape is pinned by the commented tests: `(hash-seq full-bmi [""])` → a Vector of the map's [k v] pairs conjs'd onto the seed (`[""]` + 16 pairs = count 17), so `hash-seq` takes a Vector seed and returns a Vector (the C `bmiHashSeq`/`arrayNodeSeq`/`collisionSeq` in `core.c` used a List seed, but `runtime3.c` has no List type — Vector is the seed). `bmiHashSeq` mirrors the `bmiVec-bm` recursion (bit order, sub-node pairs flattened in) but threads the seed through and, critically, descends into sub-nodes through the `hash-seq` **protocol** (type dispatch) rather than a direct call — the C `hashSeq`/`hashVec` dispatchers are dead, and a BMI child may be a BMI, ArrayNode, or HashCollisionNode (a direct `bmiVec-bm`-style call would misread a collision child — `bmiVec` is unsafe for collision children, but `hash-seq` is not). `arrayNodeHashSeq` walks slots 0..31 in index order, dispatching occupied slots through the protocol. The `HashCollisionNode` `hash-seq` reuses the C `collisionVec` directly (alive and purely mechanical — the `arrayNodeCount` pattern). The `vals` method for all three node types is `(map (hash-seq m []) pair-val)` — the old `core.toc` built `vals` on `seq` (= `hash-seq`); `pair-val` is a new helper (`extract (get pair 1)`) because a destructuring lambda `(fn [[k v]] v)` does not codegen (the compiler emits undeclared destructured vars). Verified with a scratch build (flat 3-entry BMI, BMI with a BMI sub-node, BMI with a collision child — the case `bmiVec` can't handle — a collision node directly, and a promoted 17-entry ArrayNode, each with `hash-seq` + `vals`): all correct, `diff: 0`, `remaining nodes: 0`; `make -j tests` clean (all other `.rslt` byte-identical — the new functions are not called by any existing test).
- [x] **5.2** `test-bmi.toc`: `hash-seq`/`vals` test — a 3-entry BMI. Done: `test-hash-seq` in `test-bmi.toc` — two cases over the `hash-seq` protocol + `vals` method for `BitmapIndexedNode` (wired in 5.1): (1) a flat 3-entry BMI (keys at bits 3, 4, 8 — hashes 3, 4, 8, all distinct flat slots, no sub-node): `(hash-seq m [])` → the 3 [k v] pairs in bit order; `(hash-seq m [""])` → the seed element first, then the 3 pairs (count 4 — the commented integration tests' `(hash-seq full-bmi [""])` shape); `(vals m)` → the 3 values in the same bit order (`map` over `hash-seq m []` via `pair-val`); the source map is untouched (hash-seq only reads). (2) a 3-entry BMI with a sub-node (keys 3, 35, 67 all share bit 3 at shift 0 → the parent holds a single slot (bitmap 8) whose child, a BMI at shift 5, holds k1 (bit 0), k2 (bit 1), k3 (bit 2)): `hash-seq` recurses into the sub-node slot through the `hash-seq` protocol (type dispatch — the child may be a BMI, ArrayNode, or HashCollisionNode) and flattens the child's pairs in bit order → the 3 pairs; `vals` → the 3 values; the source map is untouched. `make test-bmi` clean (`BMI tests are good`, `diff: 0`, `remaining nodes: 0`, `.rslt` diff only ITRS 46442→54881). Note: `new-toc` segfaulted intermittently during codegen (pre-existing flaky-compiler issue — reproduced on the unmodified baseline, ~1/10 runs; see implementation-notes.md §10 and the prior Ralph iteration's log) — the `.c` generation was retried until it succeeded, per the established workaround.
- [x] **5.3** `test-array-node.toc`: `hash-seq`/`vals` test — an ArrayNode. Done: `test-hash-seq` in `test-array-node.toc` — three cases over the `hash-seq` protocol + `vals` method for `ArrayNode` (wired in 5.1): (1) an empty ArrayNode built directly via `emptyArrayNode` (all 32 slots 0) → `arrayNodeHashSeq-i` walks every slot, each `arrayNodeCopyChild` returns None, and the seed passes through untouched: `hash-seq m []` → `[]`, `hash-seq m [""]` → the seed alone (count 1), `vals` → `[]`; (2) the promoted 17-entry ArrayNode (a 16-entry flat BMI via `reduce (range 16)`, then `copyAssoc` of a 17th key, hash 20 → empty bit, `addCopiedBMI`'s `n >= 16` promotion → 17 one-entry BMI sub-nodes, slots 0..15 and 20 occupied) → `arrayNodeHashSeq` walks slots 0..31 in index order, dispatching each occupied slot's child through the `hash-seq` protocol (each child a one-entry BMI): `hash-seq m []` → the 17 pairs in slot order (keys 0..15 → values 1..16, then key 20 → Val 21), `hash-seq m [""]` → the seed element first, then the 17 pairs (count 18 — the commented integration tests' `(hash-seq full-bmi [""])` shape), `vals` → the 17 values in the same slot order, the source map untouched; (3) the promoted ArrayNode + `copyAssoc` key 37 (slot 5, bit 1 at shift 5, empty in the child) → a 2-entry child (key 5 at bit 0, key 37 at bit 1) → 18 entries: the child's pairs flatten into the slot's position in the child's bit order (key 5 first, then key 37), the other slots each contribute their single pair, slot 20 last — `hash-seq` → 18 pairs, `vals` → 18 values, the 17-entry source untouched. `make test-array-node` clean (`ArrayNode tests are good`, `diff: 0`, `remaining nodes: 0`, `.rslt` diff only ITRS 116859→193992).
- [x] **5.4** `test-collision-node.toc`: `hash-seq`/`vals` test — a collision node. Done: `test-hash-seq` in `test-collision-node.toc` — three cases over the `hash-seq` protocol + `vals` method for `HashCollisionNode` (wired in 5.1 — the `hash-seq` method routes to the `collisionVec` wrapper over the C `collisionVec`, runtime3.c:2633, alive and purely mechanical, NULL-guarded, so it is the hash-seq for a collision node directly, the `arrayNodeCount` pattern; `vals` maps `pair-val` over `hash-seq m []`): (1) a fresh `emptyCollisionNode` (all entries NULL, count = 2 * capacity — the 4.2 note): `collisionVec`'s NULL guard skips every slot, so the seed passes through untouched: `hash-seq m []` → `[]`, `hash-seq m [""]` → the seed alone (count 1), `vals` → `[]`; (2) a 2-entry collision node built the way a map builds one (two 7/7 `eq=None` keys into `emptyBMI` → `bmiCopyAssoc`'s collision branch, extracted with `bmiCopyChild`, mask 128 = 1<<7): `hash-seq cn []` → the two pairs in entry order, `hash-seq cn [""]` → the seed element first, then the two pairs (count 3 — the commented integration tests' `(hash-seq collided [""])` shape), `vals` → the two values in entry order, the source node untouched; (3) a 3-entry node (a third same-hash `eq=None` key `collisionAppend`ed — the eq gate is on the entry key, the first arg): `hash-seq` → the three pairs in entry order, the appended pair last, `vals` → the three values, the 2-entry source untouched. The expected keys carry `eq=Some` (same hash 7): the eq gate is on the first arg, and `vect-=` compares the expected element first. `make test-collision-node` clean (`CollisionNode tests are good`, `diff: 0`, `remaining nodes: 0`, `.rslt` only execution-stats ITRS 16313→22206); `make -j tests` clean (all other `.rslt` byte-identical).
- [x] **5.5** `test-array-node.toc`: stress test — 27-entry map built by `reduce` over a range (per the commented tests' shape), assert count (promotes to ArrayNode). Done: `test-stress-27` in `test-array-node.toc` — the commented integration test's shape `(reduce (range 27) {} (fn [m n] (assoc m (- n 1) (inc n))))` ported to the Toccata level: the map-level `assoc` is not wired yet (roadmap 6.1), so each step is a `copyAssoc` with an explicit hash/shift (this suite's convention); I60 keys hash via `integerSha1`. The 27 keys fill 16+ distinct slots at shift 0, so the root BMI promotes to an ArrayNode (type 12) during the build and `count` dispatches to ArrayNode's count (C `arrayNodeCount`, summing `mapCount` over the non-empty slots) → 27; the test asserts count 27 **and** type 12 (pinning the promotion). `make test-array-node` clean (`ArrayNode tests are good`, `diff: 0`, `remaining nodes: 0`, `.rslt` diff only ITRS 193992→199188). Note: building this map with I60 keys exposed a pre-existing `runtime3.c` bug — `integerSha1` (runtime3.c:1829) reads 8 bytes from a 4-byte `unsigned` local (`Sha1Update(&context, (void *)&type, 8)`), capturing 4 bytes of uninitialized stack padding, so the I60 hash is non-deterministic across call contexts; the `addCopiedBMI` promotion path (runtime3.c:2095) recomputes each flat key's hash via the C `sha1` function-pointer → `getHashVal` → `integerSha1` in a different C context than the Toccata insertion, so promoted sub-nodes get bitmap bits inconsistent with the lookup hash and I60 `get*` misses in promoted ArrayNodes (count/vec are unaffected — they don't use hash routing — and memory is clean). This is a `runtime3.c` correctness bug, out of scope to fix here (not a leak fix); the test therefore asserts count/type, not `get*`.
- [x] **5.6** `test-array-node.toc`: stress test — 2001-entry map built by `reduce` over a range (per the commented tests' shape), assert count (promotes to ArrayNode). Done: `test-stress-2001` in `test-array-node.toc` — the commented integration test's shape `(reduce (range 2000) {} (fn [m n] (assoc m (ControlledHash n (Some n)) (inc n))))` ported to the Toccata level: the map-level `assoc` is not wired yet (roadmap 6.1), so each step is a `copyAssoc` with an explicit hash/shift (this suite's convention); 2001 `CHash` keys (hashes 0..2000, all distinct — no collision nodes; the controlled hashes also sidestep the 5.5 I60 `integerSha1` non-determinism). The 2001 keys fill 16+ distinct slots at shift 0, so the root BMI promotes to an ArrayNode (type 12) during the build, and the per-slot children (~62 keys each) promote again at shift 5 — a 3-level ArrayNode → ArrayNode → BMI structure; `count` dispatches to ArrayNode's count (C `arrayNodeCount`, summing `mapCount` over the non-empty slots, `mapCount` recursing through the type dispatcher) → 2001. The test asserts count 2001 **and** type 12 (pinning the promotion). `make test-array-node` clean (`ArrayNode tests are good`, `diff: 0`, `remaining nodes: 0`, `.rslt` diff only execution-stats ITRS 199188→822198).

### Phase 6 — map-level `assoc` (last)

- [x] **6.1** Add an `assoc` protocol for `HashMap`: `(assoc m k v)` → `(assoc* m k v (sha1 k) 0)` (the `refs-count` dispatch inside `assoc*` handles mutate-vs-copy). Do not add tests for this — the owner will handle them later. Done: the `assoc` protocol already existed (`(defp assoc [m k v])`, hvm-core.toc:206) — what was missing was the method. Added the `assoc` method to the existing `(extend-type HashMap ...)` block (hvm-core.toc:1955, beside `empty?`): `(let [h (sha1 k)] (assoc* m k v h 0))`. `extend-type HashMap` registers the method for all three node types (types 11/12/13 all map to the name "HashMap" in the codegen typeNameMap, codegen.toc:1044-1047 — the `empty?` dispatcher confirms cases 11/12/13), so one method covers BMI/ArrayNode/HashCollisionNode. No tests added (the owner's, per the task). Verified with a scratch build (not committed): BMI flat add (mutate shape — `m` let-bound, used exactly once), BMI same-key update, BMI copy shape (`m` used 3× — the source left untouched), I60 keys (the commented tests' key type — count only, per the 5.5 `integerSha1` caveat), a promoted 17-entry ArrayNode (assoc into empty slot 21 → count 18, `get*` hits the new and an old key), and a 2-entry collision node (a third same-hash `eq=Some` key → `collisionAppend` → count 3, `vals` in entry order) — all with `diff: 0` and `remaining nodes: 0`. The mutate-vs-copy dispatch through the wrapper was verified with temporary C-side `fprintf` instrumentation (`bmiSetKV` — mutate-only — vs `bmiClone` — copy-only — since `bmiMutateAssoc`/`bmiClone`'s callers are Toccata-level with no single C chokepoint): a same-key update routed through `assoc` with `m` used exactly once took the **mutate** path (`bmiSetKV` fired, `bmiClone` did not) — the `refs-count == 3` dispatch still fires through the wrapper. (A Toccata-level probe inside the `assoc*` body — a let-bound `refs-count` — confounded the measurement: it changed the observed count from 3 to 2 even for a direct call, because the refs-count redex's code position changes the compiler's ref schedule; the C-side observation leaves the Toccata body untouched.) `make -j tests` clean (all `.rslt` byte-identical to baseline — the new method is not called by any existing test).
- [x] **6.2** Final: `make tests` fully green (including `test-bmi`, `test-array-node`, `test-collision-node`); update the State line; mark this roadmap complete. `hash-map-regressions.toc` (the integration suite) is intentionally left untouched for the owner. Done: `make -j tests` fully green (exit 0) — every suite prints its success line (`BMI tests are good`, `ArrayNode tests are good`, `CollisionNode tests are good`, and the rest of the regression suites), every `malloc count: N  free count: N  diff: 0`, and every `remaining nodes: 0` except the known deferred `test-fusing` leak (`remaining nodes: 239 (656)` + `Leaked pairs!! 239`, present in the committed baseline); all `.rslt` files are byte-identical to the committed baseline (zero `.rslt` diff — the execution stats are deterministic across runs). The State line is updated and this roadmap is marked complete; `hash-map-regressions.toc` is untouched, as the task directs.

### Phase 7 — CollisionNode `count` (integration-suite gap)

The integration suite (`hash-map-regressions.toc`, worked through by the owner after the roadmap) exposed a gap: `HashCollisionNode` has no `count` method. BMI and ArrayNode each have one, but the `HashCollisionNode` block does not, so `(count m)` — and anything that dispatches `count` on a collision node (`empty?`, the `=` method's `(= (count m1) (count m2))`, and a `dissoc` result that is a bare collision node) — aborts with `No implementation of 'count' found for type HashMap (13)`. The failing integration test is `(= 1 (count (dissoc collided (ControlledHash (inc -1) (Some (inc -1))))))`.

- [x] **7.1** Add a `count` method for `HashCollisionNode` in `hvm-core.toc`: `(count [m] (collisionCount m))`. The `collisionCount` defn (hvm-core.toc:1237) already reads `node->count / 2` and consumes its arg (the `bmiCount`/`arrayNodeCount` pattern), so the method is a one-line route mirroring the BMI/ArrayNode `count` methods. This also unblocks `empty?` and the `=` method on collision nodes (both dispatch `count`). Done: added `(count [m] (collisionCount m))` as the first method in the `(extend-type HashCollisionNode ...)` block (hvm-core.toc), mirroring the BMI/ArrayNode `count` methods. The previously-failing integration test `(= 1 (count (dissoc collided (ControlledHash (inc -1) (Some (inc -1))))))` now passes — `dissoc` of the collided map yields a node whose `count` dispatches to the new method. Verified: `make hash-map-regressions` clean (`hash-maps are good`, `diff: 0`, `remaining nodes: 0`); `make -j tests` clean — all three tracked hash-map suite `.rslt` (`test-bmi`, `test-array-node`, `test-collision-node`) byte-identical to baseline (none of them call `count` on a collision node — they use the `collisionCount` defn directly), only the known deferred `test-fusing` leak.

## Lessons (from the mutate-assoc work, Aug 2026)

- **Owned vs borrowed child wrappers.** `bmiChild` was split into `bmiCopyChild` (incRefs — the caller owns a ref) and `bmiMutateChild` (borrowed — no incRef). The original mutate-path leak (`diff: 7`) came from the child-recurse always copying the child while `bmiSetChild` swapped the slot without releasing the old child's slot ref. The fix routes the recurse through `assoc*` on the borrowed child: an exclusively-owned child is mutated in place (no slot swap), a shared child is copied (the swap balances — verified by the shared-child test).
- **Don't hand-trace refcounts; let the counters decide.** The compiler gives every source-level appearance of a value its own ref, scheduled by code position (implementation-notes §12). A static trace predicted a leak in the shared-child copy fallback that the test disproved (`remaining nodes: 0`). Write the test; `CHECK_MEM_LEAK` + `SAFETY=1` are the oracle.
- **What the counters measure.** `malloc_count` counts `my_malloc` pool refills (50/300-struct batches with free lists) and saturates at the high-water mark — it stays ~constant across workloads and is not a workload signal. The leak signals are `diff` (checked-out values never returned) and `remaining nodes: 0 (N)`.
- **`assoc*`'s magic number 3.** `refs-count == 3` means the caller holds exactly one ref to the node (1 external + 2 dupe-refs for the node's other two appearances in `assoc*`'s body) — i.e. safe to mutate in place.
- **The mutate path has no no-op branch.** Unlike `bmiCopyAssoc`, `bmiMutateAssoc` runs `bmiSetKV` even for same-key-same-value (slot reset, old contents freed).
- **`vec` element order is implementation-dependent** — noted at the top of `test-bmi.toc`; an order change is not an error, just update the tests.

## Files to Read

| File | Purpose |
|---|---|
| `hvm-core.toc` | Inline-C BMI wrappers + Toccata protocol implementations |
| `regression-tests/test-bmi.toc` | BMI-level regression tests |
| `regression-tests/hash-map-regressions.toc` | Full hash-map regression tests (ControlledHash pattern reference) |
| `regression-tests/regression-tester.toc` | `rt/test` assertion macro |
| `new.c` | Compiler — may need modifications for `{}` literal |
| `docs/implementation-notes.md` | Working notes on Toccata/HVM internals — term layout, arg passing, protocol dispatch codegen, BMI C API hazards, build/test pipeline |
| `skills/memory-leak-hunting.md` | Skill to follow when a memory leak or double-free is found |
