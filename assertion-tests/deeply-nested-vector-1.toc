
(defn g []
  ["str"])

(defn f [x]
  (assert (instance? Integer x))
  (assert-result r (instance? (vector-of String) r))

  ;; TODO: 'comp' is not a special proto call yet
  (comp []
        [(g)]

        []))

(main [_]
  (print-err 'strs (f 3)))
