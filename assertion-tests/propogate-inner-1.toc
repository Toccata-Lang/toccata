
(defn f [l v]
  (assert (instance? Vector l))
  (assert-result m (instance? (vector-of Integer) m))
  (conj l v))

(main [_]
  ;; TODO: when the vector literal is empty, no error is thrown
  (f [9] "99"))
