
(deftype AType [x y])

(defn f [boop]
  (assert (instance? AType boop))

  (-> boop
      (.x 99)
      (.y "string")))

;; TODO: error message sucks, but it works
(main [_]
  (inc (.y (f (AType 'a 'b)))))
