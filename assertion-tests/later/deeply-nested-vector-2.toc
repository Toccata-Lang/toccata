
;; TODO: old toccata does not catch an error here either

(defn f [x]
  (assert (instance? Integer x))
  (assert-result r (instance? (vector-of String) r))

  (cond (< 0 x)
        [(f (dec x))]

        []))

(main [_]
  (print-err 'strs (f 3)))
