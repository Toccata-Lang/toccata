
;; TODO: error message needs help

(defn f [[a b]]
  (println 'a (inc a) 'b b)
  nothing)

;; TODO: the 'either' expression is what's causing problems
(main [_]
      (f (either (maybe [['a 3] 4 6])
                 nothing))
      (println 'done))
