
(deftype Popper [x y]
  ;; TODO: this doesn't work
  ;; ! x Symbol
  ;; ! y Integer
  (assert (instance? Symbol x))
  (assert (instance? Integer y)))

(defn g [& vs]
  (apply Popper vs))

(main [_]
  (print-err (g 's "8")))
