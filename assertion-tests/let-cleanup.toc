
(def r (reify
         Stringable
         (string-list [_]
           (let [whoop "whoop"
                 pop 'pop]
             (list whoop " and pop")))))

(def _ (show-type whoop))
(def _ (show-type pop))

(main [_]
  'done)

