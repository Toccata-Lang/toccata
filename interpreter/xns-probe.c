
#define _XOPEN_SOURCE 600
#include <stdlib.h>
#include <limits.h>
#include <stdio.h>
#include <stdatomic.h>
#include "runtime3.h"
#include "graph.h"

// TODO: these are only needed until core.toc codegen is complete
Term termNoImpl1(FnArity *arity, Term str_0) {
BOOM("No Term impl for 1 arg");
abort();
}

// TODO: these are only needed until core.toc codegen is complete
Value *noImpl1(FnArity *arity, Value *str_0) {
abort();
}

Value *noImpl2(FnArity *arity, Value *str_0, Value *arg1) {
abort();
}

Value *noImpl3(FnArity *arity, Value *str_0, Value *arg1, Value *arg2) {
abort();
}
Term glblFn1;
void glblCFn0(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 2, &arityArgs);
  Term result = NUL;
    if (arityArgs.count == 2) {
  Term tag_1 = arityArgs.args[0];
  Term x_2 = arityArgs.args[1];

    {
      char tagBuf[64];
      int64_t n;
      char *buf;
      if (termTag(tag_1) == VAL && ((Value *)tag_1)->type == SubStringType) {
        ReifiedVal *ss = (ReifiedVal *)tag_1;
        String *parent = (String *)ss->impls[0];
        buf = &parent->buffer[getI60(ss->impls[1])];
        n = getI60(ss->impls[2]);
      } else {
        String *s = (String *)tag_1;
        buf = s->buffer;
        n = s->len;
      }
      if (n > 63) n = 63;
      memcpy(tagBuf, buf, n);
      tagBuf[n] = 0;
      prefs(tagBuf, x_2);
    }
    dec_and_free(tag_1, 1);
    result = x_2;

move(portLoc(2, args), result);
}
return;
}
Term glblFn1 = newRef(glblCFn0);
Term glblFn3;
void glblCFn2(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  Term result = NUL;
    if (arityArgs.count == 1) {
  Term x_1 = arityArgs.args[0];
if (termTag(x_1) == I60) {
    result = termVal((Term)stringValue("Integer"));
  } else {
    char *name = typeName(((Value *)x_1)->type);
    result = termVal((Term)stringValue(name));
  };
move(portLoc(2, args), result);
}
return;
}
Term glblFn3 = newRef(glblCFn2);
Term glbltype_name9;
void glblglbl8(Term ref, Term args) {
Term apps, lams, V;
Location vLoc;
Term seq_5 = makePair(LAM, 0, SUB, NUL);
Location r_seq_5 = portLoc(2, seq_5);
Term x_4 = newTerm(VAR, 0, portLoc(1, seq_5));
// allocate args at core: 70
// call default-type-name at core: 72
Term Rslt_7Args = makePair(APP, 0, x_4, SUB);
Term Rslt_7 = newTerm(VAR, 0, portLoc(2, Rslt_7Args));
#ifdef STRICT
pushRedex(Rslt_7Args, glblFn3);
#else
swap(termLoc(Rslt_7), makePair(LAZ, 0, Rslt_7Args, glblFn3));
#endif

// link args to body
swap(r_seq_5, Rslt_7);

Term Rslt_7V = get(termLoc(Rslt_7));
apps = take(portLoc(1, Rslt_7V));
lams = take(portLoc(2, Rslt_7V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_7), makePair(LAZ, 0, apps, lams));
pushRedex(args, seq_5);
return;
}
Term glbltype_name9 = newRef(glblglbl8);
Term glblProto6;
Term glblFn11;
void glblCFn10(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  Term result = NUL;
    if (arityArgs.count == 1) {
  Term x_1 = arityArgs.args[0];

    switch(termTag(x_1)) {
    case I60:
      result = newI60(IntegerType);
      break;

    case F60:
      result = newI60(FloatType);
      break;

    case REF:
      BOOM("too tire");
      break;

    default:
      result = newI60(((Value *)x_1)->type);
      dec_and_free(x_1, 1);
    }
move(portLoc(2, args), result);
}
return;
}
Term glblFn11 = newRef(glblCFn10);
Term glblVal13 = newRef(graphFn);;
Term glblVal13;
Term glblProto18;
void glblSome22(Term ref, Term args) {
Term callArgs = makePair(APP, 0, newI60(1), args);
callArgs = makePair(APP, 0, newI60(43), callArgs);
interact(callArgs, construct);
return;
}
void glblNone15(Term ref, Term args) {
Term callArgs = makePair(APP, 0, newI60(0), args);
callArgs = makePair(APP, 0, newI60(42), callArgs);
interact(callArgs, construct);
return;
}
Term glblVal17 = newRef(glblNone15);
Term glblVal17;
Term glblRslt16;
Term glblVal25 = newRef(glblSome22);
Term glblVal25;
Term glblFld24;
void glblFldFn23(Term ref, Term args) {
args = makePair(APP, 0, newI60(0), args);
pushRedex(args, accessField);
return;
}
Term glblFld24 = newRef(glblFldFn23);
Term glblProto20;
Term glblProto26;
Term glblProto28;
Term glblProto30;
Term glblProto32;
Term glblProto34;
Term glblProto36;
Term glblProto38;
Term glblProto40;
Term glblProto42;
Term glblProto44;
Term glblProto46;
Term glblProto48;
Term glblProto50;
Term glblProto52;
Term glblProto54;
Term glblProto56;
Term glblProto58;
Term glblProto60;
Term glblProto62;
Term glblProto64;
Term glblProto66;
Term glblProto68;
Term glblProto70;
Term glblProto72;
Term glblProto74;
Term glblProto76;
Term glblProto78;
Term glblProto80;
Term glblProto82;
Term glblProto84;
Term glblProto86;
Term glblProto88;
Term glblProto90;
Term glblProto92;
Term glblProto94;
Term glblProto96;
Term glblProto98;
Term glblProto100;
Term glblProto102;
Term glblProto104;
Term glblVal106 = (Term)&emptyBMI;
Term glblVal106;
Term glblProto107;
Term glblProto109;
Term glblProto111;
Term glblProto113;
Term glblProto115;
Term glblProto117;
Term glblProto119;
Term glblProto121;
Term glblProto123;
Term glblProto125;
Term glblProto127;
Term glblProto129;
Term glblFn132;
void glblCFn131(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  Term result = NUL;
    if (arityArgs.count == 1) {
  Term str_1 = arityArgs.args[0];

    if (termTag(str_1) != VAL) {
      fprintf(stderr, "\ninvalid type for 'abort': Integer or Float\n");
      abort();
    }
    Value *s = (Value *)((u64)str_1 & ~7);
    if (s->type == StringBufferType) {
      fprintf(stderr, "%-.*s\n", (int)((String *)s)->len, ((String *)s)->buffer);
    } else {
      fprintf(stderr, "\ninvalid type for 'abort': %ld\n", s->type);
      abort();
    }
    abort();
move(portLoc(2, args), result);
}
return;
}
Term glblFn132 = newRef(glblCFn131);
Term glblFn135;
void glblCFn134(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  Term result = NUL;
    if (arityArgs.count == 1) {
  Term str_1 = arityArgs.args[0];

    if (termTag(str_1) != VAL) {
      fprintf(stderr, "\ninvalid type for 'pr*': Integer or Float\n");
      abort();
    }
    Value *s = (Value *)((u64)str_1 & ~7);
    if (s->type == StringBufferType) {
      fprintf(stdout, "%-.*s", (int)((String *)s)->len, ((String *)s)->buffer);
    } else if (s->type == SubStringType) {
      ReifiedVal *ss = (ReifiedVal *)str_1;
      String *parent = (String *)ss->impls[0];
      long start = getI60(ss->impls[1]);
      int len = (int)getI60(ss->impls[2]);
      fprintf(stdout, "%-.*s", len, &parent->buffer[start]);
    } else {
      fprintf(stdout, "\ninvalid type for 'pr*': %ld\n", s->type);
      abort();
    }
    dec_and_free(str_1, 1);
    result = newI60(0);
move(portLoc(2, args), result);
}
return;
}
Term glblFn135 = newRef(glblCFn134);
Term glblFn138;
void glblCFn137(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 2, &arityArgs);
  Term result = NUL;
    if (arityArgs.count == 2) {
  Term x_1 = arityArgs.args[0];
  Term y_2 = arityArgs.args[1];

    if (termTag(x_1) != I60 && termTag(y_2) != I60) {
      BOOM("Invalid inputs to '+'");
    }
    result = newI60(getI60(x_1) + getI60(y_2));

move(portLoc(2, args), result);
}
return;
}
Term glblFn138 = newRef(glblCFn137);
Term glblFn141;
void glblCFn140(Term ref, Term args) {
Term apps, lams, V;
Location vLoc;
Term seq_2 = makePair(LAM, 0, SUB, NUL);
Location r_seq_2 = portLoc(2, seq_2);
Term x_1 = newTerm(VAR, 0, portLoc(1, seq_2));
// allocate args at core: 313
Term Rslt_4 = makeOp(OP_ADD, newI60(1), x_1);
// link args to body
swap(r_seq_2, Rslt_4);

Term Rslt_4V = get(termLoc(Rslt_4));
apps = take(portLoc(1, Rslt_4V));
lams = take(portLoc(2, Rslt_4V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_4), makePair(LAZ, 0, apps, lams));
pushRedex(args, seq_2);
return;
}
Term glblFn141 = newRef(glblCFn140);
Term glblFn144;
void glblCFn143(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 2, &arityArgs);
  Term result = NUL;
    if (arityArgs.count == 2) {
  Term x_1 = arityArgs.args[0];
  Term y_2 = arityArgs.args[1];

    if (termTag(x_1) != I60 && termTag(y_2) != I60) {
      BOOM("Invalid inputs to '-'");
    }
    result = newI60(getI60(x_1) - getI60(y_2));

move(portLoc(2, args), result);
}
return;
}
Term glblFn144 = newRef(glblCFn143);
Term glblFn147;
void glblCFn146(Term ref, Term args) {
Term apps, lams, V;
Location vLoc;
Term seq_2 = makePair(LAM, 0, SUB, NUL);
Location r_seq_2 = portLoc(2, seq_2);
Term x_1 = newTerm(VAR, 0, portLoc(1, seq_2));
// allocate args at core: 327
Term Rslt_4 = makeOp(OP_SUB, x_1, newI60(1));
// link args to body
swap(r_seq_2, Rslt_4);

Term Rslt_4V = get(termLoc(Rslt_4));
apps = take(portLoc(1, Rslt_4V));
lams = take(portLoc(2, Rslt_4V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_4), makePair(LAZ, 0, apps, lams));
pushRedex(args, seq_2);
return;
}
Term glblFn147 = newRef(glblCFn146);
Term glblFn150;
void glblCFn149(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 2, &arityArgs);
  Term result = NUL;
    if (arityArgs.count == 2) {
  Term x_1 = arityArgs.args[0];
  Term y_2 = arityArgs.args[1];

    if (termTag(x_1) != I60 && termTag(y_2) != I60) {
      BOOM("Invalid inputs to '*'");
    }
    result = newI60(getI60(x_1) * getI60(y_2));

move(portLoc(2, args), result);
}
return;
}
Term glblFn150 = newRef(glblCFn149);
Term glblFn153;
void glblCFn152(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 2, &arityArgs);
  Term result = NUL;
    if (arityArgs.count == 2) {
  Term x_1 = arityArgs.args[0];
  Term y_2 = arityArgs.args[1];

    if (termTag(x_1) != I60 && termTag(y_2) != I60) {
      BOOM("Invalid inputs to '/'");
    }
    int64_t denom = getI60(y_2);
    if (denom == 0) {
      fprintf(stderr, "*** Can not divide by 0\n");
      abort();
    }
    result = newI60(getI60(x_1) / denom);

move(portLoc(2, args), result);
}
return;
}
Term glblFn153 = newRef(glblCFn152);
Term glblFn156;
void glblCFn155(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 2, &arityArgs);
  Term result = NUL;
    if (arityArgs.count == 2) {
  Term x_1 = arityArgs.args[0];
  Term y_2 = arityArgs.args[1];

    if (termTag(x_1) != I60 && termTag(y_2) != I60) {
      BOOM("Invalid inputs to '%'");
    }
    int64_t denom = getI60(y_2);
    if (denom == 0) {
      fprintf(stderr, "*** Can not divide by 0\n");
      abort();
    }
    result = newI60(getI60(x_1) % denom);

move(portLoc(2, args), result);
}
return;
}
Term glblFn156 = newRef(glblCFn155);
Term glblFn159;
void glblCFn158(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 2, &arityArgs);
  Term result = NUL;
    if (arityArgs.count == 2) {
  Term x_1 = arityArgs.args[0];
  Term y_2 = arityArgs.args[1];

    if (termTag(x_1) != I60 && termTag(y_2) != I60) {
      BOOM("Invalid inputs to '&'");
    }
    result = newI60(getI60(x_1) & getI60(y_2));

move(portLoc(2, args), result);
}
return;
}
Term glblFn159 = newRef(glblCFn158);
Term glblFn162;
void glblCFn161(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 2, &arityArgs);
  Term result = NUL;
    if (arityArgs.count == 2) {
  Term x_1 = arityArgs.args[0];
  Term y_2 = arityArgs.args[1];

    if (termTag(x_1) != I60 && termTag(y_2) != I60) {
      BOOM("Invalid inputs to '|'");
    }
    result = newI60(getI60(x_1) | getI60(y_2));

move(portLoc(2, args), result);
}
return;
}
Term glblFn162 = newRef(glblCFn161);
Term glblFn165;
void glblCFn164(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 2, &arityArgs);
  Term result = NUL;
    if (arityArgs.count == 2) {
  Term x_1 = arityArgs.args[0];
  Term y_2 = arityArgs.args[1];

    if (termTag(x_1) != I60 && termTag(y_2) != I60) {
      BOOM("Invalid inputs to '^'");
    }
    result = newI60(getI60(x_1) ^ getI60(y_2));

move(portLoc(2, args), result);
}
return;
}
Term glblFn165 = newRef(glblCFn164);
Term glblFn168;
void glblCFn167(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  Term result = NUL;
    if (arityArgs.count == 1) {
  Term x_1 = arityArgs.args[0];

    if (termTag(x_1) != I60) {
      BOOM("Invalid inputs to '~'");
    }
    result = newI60(~getI60(x_1));

move(portLoc(2, args), result);
}
return;
}
Term glblFn168 = newRef(glblCFn167);
Term glblFn171;
void glblCFn170(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 2, &arityArgs);
  Term result = NUL;
    if (arityArgs.count == 2) {
  Term x_1 = arityArgs.args[0];
  Term y_2 = arityArgs.args[1];

    if (termTag(x_1) != I60 && termTag(y_2) != I60) {
      BOOM("Invalid inputs to '<<'");
    }
    result = newI60(getI60(x_1) << getI60(y_2));

move(portLoc(2, args), result);
}
return;
}
Term glblFn171 = newRef(glblCFn170);
Term glblFn174;
void glblCFn173(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 2, &arityArgs);
  Term result = NUL;
    if (arityArgs.count == 2) {
  Term x_1 = arityArgs.args[0];
  Term y_2 = arityArgs.args[1];

    if (termTag(x_1) != I60 && termTag(y_2) != I60) {
      BOOM("Invalid inputs to '>>'");
    }
    result = newI60(getI60(x_1) >> getI60(y_2));

move(portLoc(2, args), result);
}
return;
}
Term glblFn174 = newRef(glblCFn173);
Term glblVal175 = (Term)&empty_vect_struct;
Term glblVal175;
Term glblFn178;
void glblCFn177(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 2, &arityArgs);
  Term result = NUL;
    if (arityArgs.count == 2) {
  Term vect_1 = arityArgs.args[0];
  Term v_2 = arityArgs.args[1];

  result = termVal((Term)vectConj((Vector *)vect_1, v_2));
move(portLoc(2, args), result);
}
return;
}
Term glblFn178 = newRef(glblCFn177);
struct {TYPE_SIZE type;
        REFS_SIZE refs;
        int64_t *hash;
        Value *parent;
        int64_t len;
        char buffer[5];
} str0 = {StringBufferType, REFS_STATIC, 0, 0, 4, "None"};
Term glblStr196 = newTerm_(VAL, (Term)&str0);
Term glblstr_vect195;
void glblstr_vect194(Term ref, Term args) {
Term apps, lams, V;
Location vLoc;
Term seq_7 = makePair(LAM, 0, SUB, NUL);
Location r_seq_7 = portLoc(2, seq_7);
Term arg_6 = newTerm(VAR, 0, portLoc(1, seq_7));
// allocate args at core: 445
swap(termLoc(arg_6), ERA);
Term glblVal175_1;
glblVal175 = dupeArg(glblVal175, &glblVal175_1, 0);

// call vect-conj at core: 446
Term Rslt_9Args = makePair(APP, 0, glblStr196, SUB);
Term Rslt_9 = newTerm(VAR, 0, portLoc(2, Rslt_9Args));
Rslt_9Args = makePair(APP, 0, glblVal175_1, Rslt_9Args);
#ifdef STRICT
pushRedex(Rslt_9Args, glblFn178);
#else
swap(termLoc(Rslt_9), makePair(LAZ, 0, Rslt_9Args, glblFn178));
#endif

// link args to body
swap(r_seq_7, Rslt_9);

Term Rslt_9V = get(termLoc(Rslt_9));
apps = take(portLoc(1, Rslt_9V));
lams = take(portLoc(2, Rslt_9V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_9), makePair(LAZ, 0, apps, lams));
pushRedex(args, seq_7);
return;
}
Term glblstr_vect195 = newRef(glblstr_vect194);
struct {TYPE_SIZE type;
        REFS_SIZE refs;
        int64_t *hash;
        Value *parent;
        int64_t len;
        char buffer[5];
} str1 = {StringBufferType, REFS_STATIC, 0, 0, 4, "core"};
Term glblStr199 = newTerm_(VAL, (Term)&str1);
void glblProto20_198(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(448), args);
  args = makePair(APP, 0, glblStr199, args);
  interact(args, glblProto20);
}
}
Term glbl_EQ_193;
void glbl_EQ_192(Term ref, Term args) {
Term apps, lams, V;
Location vLoc;
Term seq_13 = makePair(LAM, 0, SUB, NUL);
Location r_seq_13 = portLoc(2, seq_13);
Term y_12 = newTerm(VAR, 0, portLoc(1, seq_13));
seq_13 = makePair(LAM, 0, SUB, seq_13);
Term x_11 = newTerm(VAR, 0, portLoc(1, seq_13));
// allocate args at core: 447
// call type-num at core: 448
Term Rslt_15Args = makePair(APP, 0, x_11, SUB);
Term Rslt_15 = newTerm(VAR, 0, portLoc(2, Rslt_15Args));
#ifdef STRICT
pushRedex(Rslt_15Args, glblFn11);
#else
swap(termLoc(Rslt_15), makePair(LAZ, 0, Rslt_15Args, glblFn11));
#endif

// call type-num at core: 448
Term Rslt_16Args = makePair(APP, 0, y_12, SUB);
Term Rslt_16 = newTerm(VAR, 0, portLoc(2, Rslt_16Args));
#ifdef STRICT
pushRedex(Rslt_16Args, glblFn11);
#else
swap(termLoc(Rslt_16), makePair(LAZ, 0, Rslt_16Args, glblFn11));
#endif

// call = at core: 448
Term Rslt_17Args = makePair(APP, 0, Rslt_16, SUB);
Term Rslt_17 = newTerm(VAR, 0, portLoc(2, Rslt_17Args));
Rslt_17Args = makePair(APP, 0, Rslt_15, Rslt_17Args);
#ifdef STRICT
pushRedex(Rslt_17Args, newRef(glblProto20_198));
#else
swap(termLoc(Rslt_17), makePair(LAZ, 0, Rslt_17Args, newRef(glblProto20_198)));
#endif
// link args to body
swap(r_seq_13, Rslt_17);

Term Rslt_17V = get(termLoc(Rslt_17));
apps = take(portLoc(1, Rslt_17V));
lams = take(portLoc(2, Rslt_17V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_17), makePair(LAZ, 0, apps, lams));
pushRedex(args, seq_13);
return;
}
Term glbl_EQ_193 = newRef(glbl_EQ_192);
Term glblrecurse191;
void glblrecurse190(Term ref, Term args) {
Term apps, lams, V;
Location vLoc;
Term seq_21 = makePair(LAM, 0, SUB, NUL);
Location r_seq_21 = portLoc(2, seq_21);
Term f_20 = newTerm(VAR, 0, portLoc(1, seq_21));
seq_21 = makePair(LAM, 0, SUB, seq_21);
Term v_19 = newTerm(VAR, 0, portLoc(1, seq_21));
// allocate args at core: 449
swap(termLoc(f_20), ERA);
// link args to body
swap(r_seq_21, v_19);

V = get(r_seq_21);
apps = makePair(APP, 0, V, SUB);
lams = makePair(LAM, 0, SUB, NUL);
swap(portLoc(2, lams),newTerm(VAR, 0, portLoc(1, lams)));
vLoc = portLoc(2, apps);
swap(vLoc, makePair(LAZ, 0, apps, lams));
swap(r_seq_21, newTerm(VAR, 0, vLoc));
pushRedex(args, seq_21);
return;
}
Term glblrecurse191 = newRef(glblrecurse190);
Term glbleither189;
void glbleither188(Term ref, Term args) {
Term apps, lams, V;
Location vLoc;
Term seq_26 = makePair(LAM, 0, SUB, NUL);
Location r_seq_26 = portLoc(2, seq_26);
Term cont_25 = newTerm(VAR, 0, portLoc(1, seq_26));
seq_26 = makePair(LAM, 0, SUB, seq_26);
Term v_24 = newTerm(VAR, 0, portLoc(1, seq_26));
// allocate args at core: 451
swap(termLoc(v_24), ERA);
// link args to body
swap(r_seq_26, cont_25);

V = get(r_seq_26);
apps = makePair(APP, 0, V, SUB);
lams = makePair(LAM, 0, SUB, NUL);
swap(portLoc(2, lams),newTerm(VAR, 0, portLoc(1, lams)));
vLoc = portLoc(2, apps);
swap(vLoc, makePair(LAZ, 0, apps, lams));
swap(r_seq_26, newTerm(VAR, 0, vLoc));
pushRedex(args, seq_26);
return;
}
Term glbleither189 = newRef(glbleither188);
Term glblcond187;
void glblcond186(Term ref, Term args) {
Term apps, lams, V;
Location vLoc;
Term seq_32 = makePair(LAM, 0, SUB, NUL);
Location r_seq_32 = portLoc(2, seq_32);
Term cont_31 = newTerm(VAR, 0, portLoc(1, seq_32));
seq_32 = makePair(LAM, 0, SUB, seq_32);
Term x_30 = newTerm(VAR, 0, portLoc(1, seq_32));
seq_32 = makePair(LAM, 0, SUB, seq_32);
Term v_29 = newTerm(VAR, 0, portLoc(1, seq_32));
// allocate args at core: 453
swap(termLoc(v_29), ERA);
swap(termLoc(x_30), ERA);
// link args to body
swap(r_seq_32, cont_31);

V = get(r_seq_32);
apps = makePair(APP, 0, V, SUB);
lams = makePair(LAM, 0, SUB, NUL);
swap(portLoc(2, lams),newTerm(VAR, 0, portLoc(1, lams)));
vLoc = portLoc(2, apps);
swap(vLoc, makePair(LAZ, 0, apps, lams));
swap(r_seq_32, newTerm(VAR, 0, vLoc));
pushRedex(args, seq_32);
return;
}
Term glblcond187 = newRef(glblcond186);
Term glbland185;
void glbland184(Term ref, Term args) {
Term apps, lams, V;
Location vLoc;
Term seq_37 = makePair(LAM, 0, SUB, NUL);
Location r_seq_37 = portLoc(2, seq_37);
Term arg_36 = newTerm(VAR, 0, portLoc(1, seq_37));
seq_37 = makePair(LAM, 0, SUB, seq_37);
Term x_35 = newTerm(VAR, 0, portLoc(1, seq_37));
// allocate args at core: 455
swap(termLoc(arg_36), ERA);
// link args to body
swap(r_seq_37, x_35);

V = get(r_seq_37);
apps = makePair(APP, 0, V, SUB);
lams = makePair(LAM, 0, SUB, NUL);
swap(portLoc(2, lams),newTerm(VAR, 0, portLoc(1, lams)));
vLoc = portLoc(2, apps);
swap(vLoc, makePair(LAZ, 0, apps, lams));
swap(r_seq_37, newTerm(VAR, 0, vLoc));
pushRedex(args, seq_37);
return;
}
Term glbland185 = newRef(glbland184);
Term glblor183;
void glblor182(Term ref, Term args) {
Term apps, lams, V;
Location vLoc;
Term seq_42 = makePair(LAM, 0, SUB, NUL);
Location r_seq_42 = portLoc(2, seq_42);
Term y_41 = newTerm(VAR, 0, portLoc(1, seq_42));
seq_42 = makePair(LAM, 0, SUB, seq_42);
Term arg_40 = newTerm(VAR, 0, portLoc(1, seq_42));
// allocate args at core: 457
swap(termLoc(arg_40), ERA);
// link args to body
swap(r_seq_42, y_41);

V = get(r_seq_42);
apps = makePair(APP, 0, V, SUB);
lams = makePair(LAM, 0, SUB, NUL);
swap(portLoc(2, lams),newTerm(VAR, 0, portLoc(1, lams)));
vLoc = portLoc(2, apps);
swap(vLoc, makePair(LAZ, 0, apps, lams));
swap(r_seq_42, newTerm(VAR, 0, vLoc));
pushRedex(args, seq_42);
return;
}
Term glblor183 = newRef(glblor182);
Term glblmap181;
void glblmap180(Term ref, Term args) {
Term apps, lams, V;
Location vLoc;
Term seq_47 = makePair(LAM, 0, SUB, NUL);
Location r_seq_47 = portLoc(2, seq_47);
Term f_46 = newTerm(VAR, 0, portLoc(1, seq_47));
seq_47 = makePair(LAM, 0, SUB, seq_47);
Term x_45 = newTerm(VAR, 0, portLoc(1, seq_47));
// allocate args at core: 459
swap(termLoc(f_46), ERA);
// link args to body
swap(r_seq_47, x_45);

V = get(r_seq_47);
apps = makePair(APP, 0, V, SUB);
lams = makePair(LAM, 0, SUB, NUL);
swap(portLoc(2, lams),newTerm(VAR, 0, portLoc(1, lams)));
vLoc = portLoc(2, apps);
swap(vLoc, makePair(LAZ, 0, apps, lams));
swap(r_seq_47, newTerm(VAR, 0, vLoc));
pushRedex(args, seq_47);
return;
}
Term glblmap181 = newRef(glblmap180);
struct {TYPE_SIZE type;
        REFS_SIZE refs;
        int64_t *hash;
        Value *parent;
        int64_t len;
        char buffer[2];
} str2 = {StringBufferType, REFS_STATIC, 0, 0, 1, "\n"};
Term glblStr209 = newTerm_(VAL, (Term)&str2);
void glblProto68_215(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(464), args);
  args = makePair(APP, 0, glblStr199, args);
  interact(args, glblProto68);
}
}
void glblProto127_217(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(466), args);
  args = makePair(APP, 0, glblStr199, args);
  interact(args, glblProto127);
}
}
struct {TYPE_SIZE type;
        REFS_SIZE refs;
        int64_t *hash;
        Value *parent;
        int64_t len;
        char buffer[2];
} str3 = {StringBufferType, REFS_STATIC, 0, 0, 1, " "};
Term glblStr210 = newTerm_(VAL, (Term)&str3);
void glblProto68_219(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(466), args);
  args = makePair(APP, 0, glblStr199, args);
  interact(args, glblProto68);
}
}
void glblProto68_221(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(468), args);
  args = makePair(APP, 0, glblStr199, args);
  interact(args, glblProto68);
}
}
Term glblc212;
void glblc211(Term ref, Term args) {
Term apps, lams, V;
Location vLoc;
Term seq_15 = makePair(LAM, 0, SUB, NUL);
Location r_seq_15 = portLoc(2, seq_15);
Term s_14 = newTerm(VAR, 0, portLoc(1, seq_15));
seq_15 = makePair(LAM, 0, SUB, seq_15);
Term v_13 = newTerm(VAR, 0, portLoc(1, seq_15));
// allocate args at core: 467
// call pr* at core: 468
Term Rslt_17Args = makePair(APP, 0, s_14, SUB);
Term Rslt_17 = newTerm(VAR, 0, portLoc(2, Rslt_17Args));
#ifdef STRICT
pushRedex(Rslt_17Args, glblFn135);
#else
swap(termLoc(Rslt_17), makePair(LAZ, 0, Rslt_17Args, glblFn135));
#endif

// call conj at core: 468
Term Rslt_18Args = makePair(APP, 0, Rslt_17, SUB);
Term Rslt_18 = newTerm(VAR, 0, portLoc(2, Rslt_18Args));
Rslt_18Args = makePair(APP, 0, v_13, Rslt_18Args);
#ifdef STRICT
pushRedex(Rslt_18Args, newRef(glblProto68_221));
#else
swap(termLoc(Rslt_18), makePair(LAZ, 0, Rslt_18Args, newRef(glblProto68_221)));
#endif
// link args to body
swap(r_seq_15, Rslt_18);

Term Rslt_18V = get(termLoc(Rslt_18));
apps = take(portLoc(1, Rslt_18V));
lams = take(portLoc(2, Rslt_18V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_18), makePair(LAZ, 0, apps, lams));
pushRedex(args, seq_15);
return;
}
Term glblc212 = newRef(glblc211);
void glblProto72_224(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(466), args);
  args = makePair(APP, 0, glblStr199, args);
  interact(args, glblProto72);
}
}
void glblProto68_226(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(466), args);
  args = makePair(APP, 0, glblStr199, args);
  interact(args, glblProto68);
}
}
Term glblc214;
void glblc213(Term ref, Term args) {
Term apps, lams, V;
Location vLoc;
Term seq_8 = makePair(LAM, 0, SUB, NUL);
Location r_seq_8 = portLoc(2, seq_8);
Term val_7 = newTerm(VAR, 0, portLoc(1, seq_8));
seq_8 = makePair(LAM, 0, SUB, seq_8);
Term v_6 = newTerm(VAR, 0, portLoc(1, seq_8));
// allocate args at core: 465
Term glblVal175_3;
glblVal175 = dupeArg(glblVal175, &glblVal175_3, 0);

// call str-vect at core: 466
Term Rslt_10Args = makePair(APP, 0, val_7, SUB);
Term Rslt_10 = newTerm(VAR, 0, portLoc(2, Rslt_10Args));
#ifdef STRICT
pushRedex(Rslt_10Args, newRef(glblProto127_217));
#else
swap(termLoc(Rslt_10), makePair(LAZ, 0, Rslt_10Args, newRef(glblProto127_217)));
#endif
// call conj at core: 466
Term Rslt_11Args = makePair(APP, 0, glblStr210, SUB);
Term Rslt_11 = newTerm(VAR, 0, portLoc(2, Rslt_11Args));
Rslt_11Args = makePair(APP, 0, Rslt_10, Rslt_11Args);
#ifdef STRICT
pushRedex(Rslt_11Args, newRef(glblProto68_219));
#else
swap(termLoc(Rslt_11), makePair(LAZ, 0, Rslt_11Args, newRef(glblProto68_219)));
#endif
// call reduce at core: 466
Term Rslt_19Args = makePair(APP, 0, glblc212, SUB);
Term Rslt_19 = newTerm(VAR, 0, portLoc(2, Rslt_19Args));
Rslt_19Args = makePair(APP, 0, glblVal175_3, Rslt_19Args);
Rslt_19Args = makePair(APP, 0, Rslt_11, Rslt_19Args);
#ifdef STRICT
pushRedex(Rslt_19Args, newRef(glblProto72_224));
#else
swap(termLoc(Rslt_19), makePair(LAZ, 0, Rslt_19Args, newRef(glblProto72_224)));
#endif
// call conj at core: 466
Term Rslt_20Args = makePair(APP, 0, Rslt_19, SUB);
Term Rslt_20 = newTerm(VAR, 0, portLoc(2, Rslt_20Args));
Rslt_20Args = makePair(APP, 0, v_6, Rslt_20Args);
#ifdef STRICT
pushRedex(Rslt_20Args, newRef(glblProto68_226));
#else
swap(termLoc(Rslt_20), makePair(LAZ, 0, Rslt_20Args, newRef(glblProto68_226)));
#endif
// link args to body
swap(r_seq_8, Rslt_20);

Term Rslt_20V = get(termLoc(Rslt_20));
apps = take(portLoc(1, Rslt_20V));
lams = take(portLoc(2, Rslt_20V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_20), makePair(LAZ, 0, apps, lams));
pushRedex(args, seq_8);
return;
}
Term glblc214 = newRef(glblc213);
void glblProto72_229(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(464), args);
  args = makePair(APP, 0, glblStr199, args);
  interact(args, glblProto72);
}
}
Term glblFn208;
void glblCFn207(Term ref, Term args) {
Term apps, lams, V;
Location vLoc;
Term seq_2 = makePair(LAM, 0, SUB, NUL);
Location r_seq_2 = portLoc(2, seq_2);
Term vs_1 = newTerm(VAR, 0, portLoc(1, seq_2));
// allocate args at core: 462
Term glblVal175_2;
glblVal175 = dupeArg(glblVal175, &glblVal175_2, 0);

// call conj at core: 464
Term Rslt_4Args = makePair(APP, 0, glblStr209, SUB);
Term Rslt_4 = newTerm(VAR, 0, portLoc(2, Rslt_4Args));
Rslt_4Args = makePair(APP, 0, vs_1, Rslt_4Args);
#ifdef STRICT
pushRedex(Rslt_4Args, newRef(glblProto68_215));
#else
swap(termLoc(Rslt_4), makePair(LAZ, 0, Rslt_4Args, newRef(glblProto68_215)));
#endif
// call reduce at core: 464
Term Rslt_21Args = makePair(APP, 0, glblc214, SUB);
Term Rslt_21 = newTerm(VAR, 0, portLoc(2, Rslt_21Args));
Rslt_21Args = makePair(APP, 0, glblVal175_2, Rslt_21Args);
Rslt_21Args = makePair(APP, 0, Rslt_4, Rslt_21Args);
#ifdef STRICT
pushRedex(Rslt_21Args, newRef(glblProto72_229));
#else
swap(termLoc(Rslt_21), makePair(LAZ, 0, Rslt_21Args, newRef(glblProto72_229)));
#endif
// link args to body
swap(r_seq_2, Rslt_21);

Term Rslt_21V = get(termLoc(Rslt_21));
apps = take(portLoc(1, Rslt_21V));
lams = take(portLoc(2, Rslt_21V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_21), makePair(LAZ, 0, apps, lams));
pushRedex(args, seq_2);
return;
}
Term glblFn208 = newRef(glblCFn207);
struct {TYPE_SIZE type;
        REFS_SIZE refs;
        int64_t *hash;
        Value *parent;
        int64_t len;
        char buffer[7];
} str4 = {StringBufferType, REFS_STATIC, 0, 0, 6, "(Some "};
Term glblStr255 = newTerm_(VAL, (Term)&str4);
void glblProto18_265(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(473), args);
  args = makePair(APP, 0, glblStr199, args);
  interact(args, glblProto18);
}
}
void glblProto127_267(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(473), args);
  args = makePair(APP, 0, glblStr199, args);
  interact(args, glblProto127);
}
}
void glblProto38_269(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(473), args);
  args = makePair(APP, 0, glblStr199, args);
  interact(args, glblProto38);
}
}
struct {TYPE_SIZE type;
        REFS_SIZE refs;
        int64_t *hash;
        Value *parent;
        int64_t len;
        char buffer[2];
} str5 = {StringBufferType, REFS_STATIC, 0, 0, 1, ")"};
Term glblStr254 = newTerm_(VAL, (Term)&str5);
Term glblstr_vect251;
void glblstr_vect250(Term ref, Term args) {
Term apps, lams, V;
Location vLoc;
Term seq_24 = makePair(LAM, 0, SUB, NUL);
Location r_seq_24 = portLoc(2, seq_24);
Term s_23 = newTerm(VAR, 0, portLoc(1, seq_24));
// allocate args at core: 471
Term glblVal175_4;
glblVal175 = dupeArg(glblVal175, &glblVal175_4, 0);

// call vect-conj at core: 472
Term Rslt_26Args = makePair(APP, 0, glblStr255, SUB);
Term Rslt_26 = newTerm(VAR, 0, portLoc(2, Rslt_26Args));
Rslt_26Args = makePair(APP, 0, glblVal175_4, Rslt_26Args);
#ifdef STRICT
pushRedex(Rslt_26Args, glblFn178);
#else
swap(termLoc(Rslt_26), makePair(LAZ, 0, Rslt_26Args, glblFn178));
#endif

// call .x at core: 473
Term Rslt_27Args = makePair(APP, 0, s_23, SUB);
Term Rslt_27 = newTerm(VAR, 0, portLoc(2, Rslt_27Args));
#ifdef STRICT
pushRedex(Rslt_27Args, newRef(glblProto18_265));
#else
swap(termLoc(Rslt_27), makePair(LAZ, 0, Rslt_27Args, newRef(glblProto18_265)));
#endif
// call str-vect at core: 473
Term Rslt_28Args = makePair(APP, 0, Rslt_27, SUB);
Term Rslt_28 = newTerm(VAR, 0, portLoc(2, Rslt_28Args));
#ifdef STRICT
pushRedex(Rslt_28Args, newRef(glblProto127_267));
#else
swap(termLoc(Rslt_28), makePair(LAZ, 0, Rslt_28Args, newRef(glblProto127_267)));
#endif
// call comp at core: 473
Term Rslt_29Args = makePair(APP, 0, Rslt_28, SUB);
Term Rslt_29 = newTerm(VAR, 0, portLoc(2, Rslt_29Args));
Rslt_29Args = makePair(APP, 0, Rslt_26, Rslt_29Args);
#ifdef STRICT
pushRedex(Rslt_29Args, newRef(glblProto38_269));
#else
swap(termLoc(Rslt_29), makePair(LAZ, 0, Rslt_29Args, newRef(glblProto38_269)));
#endif
// call vect-conj at core: 474
Term Rslt_30Args = makePair(APP, 0, glblStr254, SUB);
Term Rslt_30 = newTerm(VAR, 0, portLoc(2, Rslt_30Args));
Rslt_30Args = makePair(APP, 0, Rslt_29, Rslt_30Args);
#ifdef STRICT
pushRedex(Rslt_30Args, glblFn178);
#else
swap(termLoc(Rslt_30), makePair(LAZ, 0, Rslt_30Args, glblFn178));
#endif

// link args to body
swap(r_seq_24, Rslt_30);

Term Rslt_30V = get(termLoc(Rslt_30));
apps = take(portLoc(1, Rslt_30V));
lams = take(portLoc(2, Rslt_30V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_30), makePair(LAZ, 0, apps, lams));
pushRedex(args, seq_24);
return;
}
Term glblstr_vect251 = newRef(glblstr_vect250);
void glblProto20_272(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(476), args);
  args = makePair(APP, 0, glblStr199, args);
  interact(args, glblProto20);
}
}
void glblProto18_274(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(477), args);
  args = makePair(APP, 0, glblStr199, args);
  interact(args, glblProto18);
}
}
void glblProto18_276(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(477), args);
  args = makePair(APP, 0, glblStr199, args);
  interact(args, glblProto18);
}
}
void glblProto20_278(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(477), args);
  args = makePair(APP, 0, glblStr199, args);
  interact(args, glblProto20);
}
}
void glblProto28_280(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(476), args);
  args = makePair(APP, 0, glblStr199, args);
  interact(args, glblProto28);
}
}
Term glbl_EQ_249;
void glbl_EQ_248(Term ref, Term args) {
Term apps, lams, V;
Location vLoc;
Term seq_34 = makePair(LAM, 0, SUB, NUL);
Location r_seq_34 = portLoc(2, seq_34);
Term y_33 = newTerm(VAR, 0, portLoc(1, seq_34));
seq_34 = makePair(LAM, 0, SUB, seq_34);
Term x_32 = newTerm(VAR, 0, portLoc(1, seq_34));
// allocate args at core: 475

Term x_32_1;
x_32 = dupeArg(x_32, &x_32_1, 4);

Term y_33_1;
y_33 = dupeArg(y_33, &y_33_1, 5);
// call type-num at core: 476
Term Rslt_36Args = makePair(APP, 0, x_32, SUB);
Term Rslt_36 = newTerm(VAR, 0, portLoc(2, Rslt_36Args));
#ifdef STRICT
pushRedex(Rslt_36Args, glblFn11);
#else
swap(termLoc(Rslt_36), makePair(LAZ, 0, Rslt_36Args, glblFn11));
#endif

// call type-num at core: 476
Term Rslt_37Args = makePair(APP, 0, y_33, SUB);
Term Rslt_37 = newTerm(VAR, 0, portLoc(2, Rslt_37Args));
#ifdef STRICT
pushRedex(Rslt_37Args, glblFn11);
#else
swap(termLoc(Rslt_37), makePair(LAZ, 0, Rslt_37Args, glblFn11));
#endif

// call = at core: 476
Term Rslt_38Args = makePair(APP, 0, Rslt_37, SUB);
Term Rslt_38 = newTerm(VAR, 0, portLoc(2, Rslt_38Args));
Rslt_38Args = makePair(APP, 0, Rslt_36, Rslt_38Args);
#ifdef STRICT
pushRedex(Rslt_38Args, newRef(glblProto20_272));
#else
swap(termLoc(Rslt_38), makePair(LAZ, 0, Rslt_38Args, newRef(glblProto20_272)));
#endif
// call .x at core: 477
Term Rslt_39Args = makePair(APP, 0, x_32_1, SUB);
Term Rslt_39 = newTerm(VAR, 0, portLoc(2, Rslt_39Args));
#ifdef STRICT
pushRedex(Rslt_39Args, newRef(glblProto18_274));
#else
swap(termLoc(Rslt_39), makePair(LAZ, 0, Rslt_39Args, newRef(glblProto18_274)));
#endif
// call .x at core: 477
Term Rslt_40Args = makePair(APP, 0, y_33_1, SUB);
Term Rslt_40 = newTerm(VAR, 0, portLoc(2, Rslt_40Args));
#ifdef STRICT
pushRedex(Rslt_40Args, newRef(glblProto18_276));
#else
swap(termLoc(Rslt_40), makePair(LAZ, 0, Rslt_40Args, newRef(glblProto18_276)));
#endif
// call = at core: 477
Term Rslt_41Args = makePair(APP, 0, Rslt_40, SUB);
Term Rslt_41 = newTerm(VAR, 0, portLoc(2, Rslt_41Args));
Rslt_41Args = makePair(APP, 0, Rslt_39, Rslt_41Args);
#ifdef STRICT
pushRedex(Rslt_41Args, newRef(glblProto20_278));
#else
swap(termLoc(Rslt_41), makePair(LAZ, 0, Rslt_41Args, newRef(glblProto20_278)));
#endif
// call and at core: 476
Term Rslt_42Args = makePair(APP, 0, Rslt_41, SUB);
Term Rslt_42 = newTerm(VAR, 0, portLoc(2, Rslt_42Args));
Rslt_42Args = makePair(APP, 0, Rslt_38, Rslt_42Args);
#ifdef STRICT
pushRedex(Rslt_42Args, newRef(glblProto28_280));
#else
swap(termLoc(Rslt_42), makePair(LAZ, 0, Rslt_42Args, newRef(glblProto28_280)));
#endif
// link args to body
swap(r_seq_34, Rslt_42);

Term Rslt_42V = get(termLoc(Rslt_42));
apps = take(portLoc(1, Rslt_42V));
lams = take(portLoc(2, Rslt_42V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_42), makePair(LAZ, 0, apps, lams));
pushRedex(args, seq_34);
return;
}
Term glbl_EQ_249 = newRef(glbl_EQ_248);
void glblProto18_283(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(479), args);
  args = makePair(APP, 0, glblStr199, args);
  interact(args, glblProto18);
}
}
Term glblextract247;
void glblextract246(Term ref, Term args) {
Term apps, lams, V;
Location vLoc;
Term seq_45 = makePair(LAM, 0, SUB, NUL);
Location r_seq_45 = portLoc(2, seq_45);
Term v_44 = newTerm(VAR, 0, portLoc(1, seq_45));
// allocate args at core: 478
// call .x at core: 479
Term Rslt_47Args = makePair(APP, 0, v_44, SUB);
Term Rslt_47 = newTerm(VAR, 0, portLoc(2, Rslt_47Args));
#ifdef STRICT
pushRedex(Rslt_47Args, newRef(glblProto18_283));
#else
swap(termLoc(Rslt_47), makePair(LAZ, 0, Rslt_47Args, newRef(glblProto18_283)));
#endif
// link args to body
swap(r_seq_45, Rslt_47);

Term Rslt_47V = get(termLoc(Rslt_47));
apps = take(portLoc(1, Rslt_47V));
lams = take(portLoc(2, Rslt_47V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_47), makePair(LAZ, 0, apps, lams));
pushRedex(args, seq_45);
return;
}
Term glblextract247 = newRef(glblextract246);
void glblProto18_286(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(481), args);
  args = makePair(APP, 0, glblStr199, args);
  interact(args, glblProto18);
}
}
Term glblrecurse245;
void glblrecurse244(Term ref, Term args) {
Term apps, lams, V;
Location vLoc;
Term seq_51 = makePair(LAM, 0, SUB, NUL);
Location r_seq_51 = portLoc(2, seq_51);
Term f_50 = newTerm(VAR, 0, portLoc(1, seq_51));
seq_51 = makePair(LAM, 0, SUB, seq_51);
Term v_49 = newTerm(VAR, 0, portLoc(1, seq_51));
// allocate args at core: 480
Term glblVal25_1;
glblVal25 = dupeArg(glblVal25, &glblVal25_1, 0);

// call .x at core: 481
Term Rslt_53Args = makePair(APP, 0, v_49, SUB);
Term Rslt_53 = newTerm(VAR, 0, portLoc(2, Rslt_53Args));
#ifdef STRICT
pushRedex(Rslt_53Args, newRef(glblProto18_286));
#else
swap(termLoc(Rslt_53), makePair(LAZ, 0, Rslt_53Args, newRef(glblProto18_286)));
#endif
// call f at core: 480
Term Rslt_54Args = makePair(APP, 0, Rslt_53, SUB);
Term Rslt_54 = newTerm(VAR, 0, portLoc(2, Rslt_54Args));
#ifdef STRICT
pushRedex(Rslt_54Args, f_50);
#else
swap(termLoc(Rslt_54), makePair(LAZ, 0, Rslt_54Args, f_50));
#endif

// call Some at core: 481
Term Rslt_55Args = makePair(APP, 0, Rslt_54, SUB);
Term Rslt_55 = newTerm(VAR, 0, portLoc(2, Rslt_55Args));
#ifdef STRICT
pushRedex(Rslt_55Args, glblVal25_1);
#else
swap(termLoc(Rslt_55), makePair(LAZ, 0, Rslt_55Args, glblVal25_1));
#endif

// link args to body
swap(r_seq_51, Rslt_55);

Term Rslt_55V = get(termLoc(Rslt_55));
apps = take(portLoc(1, Rslt_55V));
lams = take(portLoc(2, Rslt_55V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_55), makePair(LAZ, 0, apps, lams));
pushRedex(args, seq_51);
return;
}
Term glblrecurse245 = newRef(glblrecurse244);
void glblProto18_289(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(483), args);
  args = makePair(APP, 0, glblStr199, args);
  interact(args, glblProto18);
}
}
Term glbleither243;
void glbleither242(Term ref, Term args) {
Term apps, lams, V;
Location vLoc;
Term seq_59 = makePair(LAM, 0, SUB, NUL);
Location r_seq_59 = portLoc(2, seq_59);
Term arg_58 = newTerm(VAR, 0, portLoc(1, seq_59));
seq_59 = makePair(LAM, 0, SUB, seq_59);
Term v_57 = newTerm(VAR, 0, portLoc(1, seq_59));
// allocate args at core: 482
swap(termLoc(arg_58), ERA);
// call .x at core: 483
Term Rslt_61Args = makePair(APP, 0, v_57, SUB);
Term Rslt_61 = newTerm(VAR, 0, portLoc(2, Rslt_61Args));
#ifdef STRICT
pushRedex(Rslt_61Args, newRef(glblProto18_289));
#else
swap(termLoc(Rslt_61), makePair(LAZ, 0, Rslt_61Args, newRef(glblProto18_289)));
#endif
// link args to body
swap(r_seq_59, Rslt_61);

Term Rslt_61V = get(termLoc(Rslt_61));
apps = take(portLoc(1, Rslt_61V));
lams = take(portLoc(2, Rslt_61V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_61), makePair(LAZ, 0, apps, lams));
pushRedex(args, seq_59);
return;
}
Term glbleither243 = newRef(glbleither242);
Term glblcond241;
void glblcond240(Term ref, Term args) {
Term apps, lams, V;
Location vLoc;
Term seq_66 = makePair(LAM, 0, SUB, NUL);
Location r_seq_66 = portLoc(2, seq_66);
Term arg_65 = newTerm(VAR, 0, portLoc(1, seq_66));
seq_66 = makePair(LAM, 0, SUB, seq_66);
Term clause_64 = newTerm(VAR, 0, portLoc(1, seq_66));
seq_66 = makePair(LAM, 0, SUB, seq_66);
Term v_63 = newTerm(VAR, 0, portLoc(1, seq_66));
// allocate args at core: 484
swap(termLoc(v_63), ERA);
swap(termLoc(arg_65), ERA);
// link args to body
swap(r_seq_66, clause_64);

V = get(r_seq_66);
apps = makePair(APP, 0, V, SUB);
lams = makePair(LAM, 0, SUB, NUL);
swap(portLoc(2, lams),newTerm(VAR, 0, portLoc(1, lams)));
vLoc = portLoc(2, apps);
swap(vLoc, makePair(LAZ, 0, apps, lams));
swap(r_seq_66, newTerm(VAR, 0, vLoc));
pushRedex(args, seq_66);
return;
}
Term glblcond241 = newRef(glblcond240);
Term glbland239;
void glbland238(Term ref, Term args) {
Term apps, lams, V;
Location vLoc;
Term seq_71 = makePair(LAM, 0, SUB, NUL);
Location r_seq_71 = portLoc(2, seq_71);
Term y_70 = newTerm(VAR, 0, portLoc(1, seq_71));
seq_71 = makePair(LAM, 0, SUB, seq_71);
Term arg_69 = newTerm(VAR, 0, portLoc(1, seq_71));
// allocate args at core: 486
swap(termLoc(arg_69), ERA);
// link args to body
swap(r_seq_71, y_70);

V = get(r_seq_71);
apps = makePair(APP, 0, V, SUB);
lams = makePair(LAM, 0, SUB, NUL);
swap(portLoc(2, lams),newTerm(VAR, 0, portLoc(1, lams)));
vLoc = portLoc(2, apps);
swap(vLoc, makePair(LAZ, 0, apps, lams));
swap(r_seq_71, newTerm(VAR, 0, vLoc));
pushRedex(args, seq_71);
return;
}
Term glbland239 = newRef(glbland238);
Term glblor237;
void glblor236(Term ref, Term args) {
Term apps, lams, V;
Location vLoc;
Term seq_76 = makePair(LAM, 0, SUB, NUL);
Location r_seq_76 = portLoc(2, seq_76);
Term arg_75 = newTerm(VAR, 0, portLoc(1, seq_76));
seq_76 = makePair(LAM, 0, SUB, seq_76);
Term q_74 = newTerm(VAR, 0, portLoc(1, seq_76));
// allocate args at core: 488
swap(termLoc(arg_75), ERA);
// link args to body
swap(r_seq_76, q_74);

V = get(r_seq_76);
apps = makePair(APP, 0, V, SUB);
lams = makePair(LAM, 0, SUB, NUL);
swap(portLoc(2, lams),newTerm(VAR, 0, portLoc(1, lams)));
vLoc = portLoc(2, apps);
swap(vLoc, makePair(LAZ, 0, apps, lams));
swap(r_seq_76, newTerm(VAR, 0, vLoc));
pushRedex(args, seq_76);
return;
}
Term glblor237 = newRef(glblor236);
void glblProto18_295(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(491), args);
  args = makePair(APP, 0, glblStr199, args);
  interact(args, glblProto18);
}
}
Term glblmap235;
void glblmap234(Term ref, Term args) {
Term apps, lams, V;
Location vLoc;
Term seq_81 = makePair(LAM, 0, SUB, NUL);
Location r_seq_81 = portLoc(2, seq_81);
Term f_80 = newTerm(VAR, 0, portLoc(1, seq_81));
seq_81 = makePair(LAM, 0, SUB, seq_81);
Term v_79 = newTerm(VAR, 0, portLoc(1, seq_81));
// allocate args at core: 490
Term glblVal25_2;
glblVal25 = dupeArg(glblVal25, &glblVal25_2, 0);

// call .x at core: 491
Term Rslt_83Args = makePair(APP, 0, v_79, SUB);
Term Rslt_83 = newTerm(VAR, 0, portLoc(2, Rslt_83Args));
#ifdef STRICT
pushRedex(Rslt_83Args, newRef(glblProto18_295));
#else
swap(termLoc(Rslt_83), makePair(LAZ, 0, Rslt_83Args, newRef(glblProto18_295)));
#endif
// call f at core: 490
Term Rslt_84Args = makePair(APP, 0, Rslt_83, SUB);
Term Rslt_84 = newTerm(VAR, 0, portLoc(2, Rslt_84Args));
#ifdef STRICT
pushRedex(Rslt_84Args, f_80);
#else
swap(termLoc(Rslt_84), makePair(LAZ, 0, Rslt_84Args, f_80));
#endif

// call Some at core: 491
Term Rslt_85Args = makePair(APP, 0, Rslt_84, SUB);
Term Rslt_85 = newTerm(VAR, 0, portLoc(2, Rslt_85Args));
#ifdef STRICT
pushRedex(Rslt_85Args, glblVal25_2);
#else
swap(termLoc(Rslt_85), makePair(LAZ, 0, Rslt_85Args, glblVal25_2));
#endif

// link args to body
swap(r_seq_81, Rslt_85);

Term Rslt_85V = get(termLoc(Rslt_85));
apps = take(portLoc(1, Rslt_85V));
lams = take(portLoc(2, Rslt_85V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_85), makePair(LAZ, 0, apps, lams));
pushRedex(args, seq_81);
return;
}
Term glblmap235 = newRef(glblmap234);
void glblProto18_298(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(493), args);
  args = makePair(APP, 0, glblStr199, args);
  interact(args, glblProto18);
}
}
Term glblflat_map233;
void glblflat_map232(Term ref, Term args) {
Term apps, lams, V;
Location vLoc;
Term seq_89 = makePair(LAM, 0, SUB, NUL);
Location r_seq_89 = portLoc(2, seq_89);
Term f_88 = newTerm(VAR, 0, portLoc(1, seq_89));
seq_89 = makePair(LAM, 0, SUB, seq_89);
Term v_87 = newTerm(VAR, 0, portLoc(1, seq_89));
// allocate args at core: 492
// call .x at core: 493
Term Rslt_91Args = makePair(APP, 0, v_87, SUB);
Term Rslt_91 = newTerm(VAR, 0, portLoc(2, Rslt_91Args));
#ifdef STRICT
pushRedex(Rslt_91Args, newRef(glblProto18_298));
#else
swap(termLoc(Rslt_91), makePair(LAZ, 0, Rslt_91Args, newRef(glblProto18_298)));
#endif
// call f at core: 492
Term Rslt_92Args = makePair(APP, 0, Rslt_91, SUB);
Term Rslt_92 = newTerm(VAR, 0, portLoc(2, Rslt_92Args));
#ifdef STRICT
pushRedex(Rslt_92Args, f_88);
#else
swap(termLoc(Rslt_92), makePair(LAZ, 0, Rslt_92Args, f_88));
#endif

// link args to body
swap(r_seq_89, Rslt_92);

Term Rslt_92V = get(termLoc(Rslt_92));
apps = take(portLoc(1, Rslt_92V));
lams = take(portLoc(2, Rslt_92V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_92), makePair(LAZ, 0, apps, lams));
pushRedex(args, seq_89);
return;
}
Term glblflat_map233 = newRef(glblflat_map232);
Term glblFn253;
void glblCFn252(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 2, &arityArgs);
  Term result = NUL;
    if (arityArgs.count == 2) {
  Term x_1 = arityArgs.args[0];
  Term y_2 = arityArgs.args[1];

    result = termVal(integer_EQ(x_1, y_2));
move(portLoc(2, args), result);
}
return;
}
Term glblFn253 = newRef(glblCFn252);
Term glblFn257;
void glblCFn256(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 2, &arityArgs);
  Term result = NUL;
    if (arityArgs.count == 2) {
  Term x_1 = arityArgs.args[0];
  Term y_2 = arityArgs.args[1];

    result = termVal(integer_LT(x_1, y_2));
move(portLoc(2, args), result);
}
return;
}
Term glblFn257 = newRef(glblCFn256);
Term glblFn259;
void glblCFn258(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  Term result = NUL;
    if (arityArgs.count == 1) {
  Term n_1 = arityArgs.args[0];

    if (termTag(n_1) == VAL) {
      fprintf(stderr, "\ninvalid type for 'number-str': Value\n");
      abort();
    }
    result = number_str(n_1);

move(portLoc(2, args), result);
}
return;
}
Term glblFn259 = newRef(glblCFn258);
void glblProto30_304(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(518), args);
  args = makePair(APP, 0, glblStr199, args);
  interact(args, glblProto30);
}
}
Term glblFn261;
void glblCFn260(Term ref, Term args) {
Term apps, lams, V;
Location vLoc;
Term seq_3 = makePair(LAM, 0, SUB, NUL);
Location r_seq_3 = portLoc(2, seq_3);
Term y_2 = newTerm(VAR, 0, portLoc(1, seq_3));
seq_3 = makePair(LAM, 0, SUB, seq_3);
Term x_1 = newTerm(VAR, 0, portLoc(1, seq_3));
// allocate args at core: 517

Term x_1_1;
x_1 = dupeArg(x_1, &x_1_1, 6);

Term y_2_1;
y_2 = dupeArg(y_2, &y_2_1, 7);
// call int-< at core: 518
Term Rslt_5Args = makePair(APP, 0, y_2, SUB);
Term Rslt_5 = newTerm(VAR, 0, portLoc(2, Rslt_5Args));
Rslt_5Args = makePair(APP, 0, x_1, Rslt_5Args);
#ifdef STRICT
pushRedex(Rslt_5Args, glblFn257);
#else
swap(termLoc(Rslt_5), makePair(LAZ, 0, Rslt_5Args, glblFn257));
#endif

// call int-= at core: 519
Term Rslt_6Args = makePair(APP, 0, y_2_1, SUB);
Term Rslt_6 = newTerm(VAR, 0, portLoc(2, Rslt_6Args));
Rslt_6Args = makePair(APP, 0, x_1_1, Rslt_6Args);
#ifdef STRICT
pushRedex(Rslt_6Args, glblFn253);
#else
swap(termLoc(Rslt_6), makePair(LAZ, 0, Rslt_6Args, glblFn253));
#endif

// call or at core: 518
Term Rslt_7Args = makePair(APP, 0, Rslt_6, SUB);
Term Rslt_7 = newTerm(VAR, 0, portLoc(2, Rslt_7Args));
Rslt_7Args = makePair(APP, 0, Rslt_5, Rslt_7Args);
#ifdef STRICT
pushRedex(Rslt_7Args, newRef(glblProto30_304));
#else
swap(termLoc(Rslt_7), makePair(LAZ, 0, Rslt_7Args, newRef(glblProto30_304)));
#endif
// link args to body
swap(r_seq_3, Rslt_7);

Term Rslt_7V = get(termLoc(Rslt_7));
apps = take(portLoc(1, Rslt_7V));
lams = take(portLoc(2, Rslt_7V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_7), makePair(LAZ, 0, apps, lams));
pushRedex(args, seq_3);
return;
}
Term glblFn261 = newRef(glblCFn260);
Term glblVal262 = newRef(intCond);;
Term glblVal262;
Term glblFn264;
void glblCFn263(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  Term result = NUL;
    if (arityArgs.count == 1) {
  Term x_1 = arityArgs.args[0];
result = newI60(integerSha1(x_1));
move(portLoc(2, args), result);
}
return;
}
Term glblFn264 = newRef(glblCFn263);
struct {TYPE_SIZE type;
        REFS_SIZE refs;
        int64_t *hash;
        Value *parent;
        int64_t len;
        char buffer[8];
} str6 = {StringBufferType, REFS_STATIC, 0, 0, 7, "Integer"};
Term glblStr320 = newTerm_(VAL, (Term)&str6);
Term glbltype_name319;
void glbltype_name318(Term ref, Term args) {
Term apps, lams, V;
Location vLoc;
Term seq_6 = makePair(LAM, 0, SUB, NUL);
Location r_seq_6 = portLoc(2, seq_6);
Term arg_5 = newTerm(VAR, 0, portLoc(1, seq_6));
// allocate args at core: 529
swap(termLoc(arg_5), ERA);
// link args to body
swap(r_seq_6, glblStr320);

V = get(r_seq_6);
apps = makePair(APP, 0, V, SUB);
lams = makePair(LAM, 0, SUB, NUL);
swap(portLoc(2, lams),newTerm(VAR, 0, portLoc(1, lams)));
vLoc = portLoc(2, apps);
swap(vLoc, makePair(LAZ, 0, apps, lams));
swap(r_seq_6, newTerm(VAR, 0, vLoc));
pushRedex(args, seq_6);
return;
}
Term glbltype_name319 = newRef(glbltype_name318);
Term glblstr_vect317;
void glblstr_vect316(Term ref, Term args) {
Term apps, lams, V;
Location vLoc;
Term seq_10 = makePair(LAM, 0, SUB, NUL);
Location r_seq_10 = portLoc(2, seq_10);
Term n_9 = newTerm(VAR, 0, portLoc(1, seq_10));
// allocate args at core: 532
Term glblVal175_5;
glblVal175 = dupeArg(glblVal175, &glblVal175_5, 0);

// call number-str at core: 533
Term Rslt_12Args = makePair(APP, 0, n_9, SUB);
Term Rslt_12 = newTerm(VAR, 0, portLoc(2, Rslt_12Args));
#ifdef STRICT
pushRedex(Rslt_12Args, glblFn259);
#else
swap(termLoc(Rslt_12), makePair(LAZ, 0, Rslt_12Args, glblFn259));
#endif

// call vect-conj at core: 533
Term Rslt_13Args = makePair(APP, 0, Rslt_12, SUB);
Term Rslt_13 = newTerm(VAR, 0, portLoc(2, Rslt_13Args));
Rslt_13Args = makePair(APP, 0, glblVal175_5, Rslt_13Args);
#ifdef STRICT
pushRedex(Rslt_13Args, glblFn178);
#else
swap(termLoc(Rslt_13), makePair(LAZ, 0, Rslt_13Args, glblFn178));
#endif

// link args to body
swap(r_seq_10, Rslt_13);

Term Rslt_13V = get(termLoc(Rslt_13));
apps = take(portLoc(1, Rslt_13V));
lams = take(portLoc(2, Rslt_13V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_13), makePair(LAZ, 0, apps, lams));
pushRedex(args, seq_10);
return;
}
Term glblstr_vect317 = newRef(glblstr_vect316);
Term glbl_EQ_315;
void glbl_EQ_314(Term ref, Term args) {
Term apps, lams, V;
Location vLoc;
Term seq_17 = makePair(LAM, 0, SUB, NUL);
Location r_seq_17 = portLoc(2, seq_17);
Term y_16 = newTerm(VAR, 0, portLoc(1, seq_17));
seq_17 = makePair(LAM, 0, SUB, seq_17);
Term x_15 = newTerm(VAR, 0, portLoc(1, seq_17));
// allocate args at core: 535
// call int-= at core: 536
Term Rslt_19Args = makePair(APP, 0, y_16, SUB);
Term Rslt_19 = newTerm(VAR, 0, portLoc(2, Rslt_19Args));
Rslt_19Args = makePair(APP, 0, x_15, Rslt_19Args);
#ifdef STRICT
pushRedex(Rslt_19Args, glblFn253);
#else
swap(termLoc(Rslt_19), makePair(LAZ, 0, Rslt_19Args, glblFn253));
#endif

// link args to body
swap(r_seq_17, Rslt_19);

Term Rslt_19V = get(termLoc(Rslt_19));
apps = take(portLoc(1, Rslt_19V));
lams = take(portLoc(2, Rslt_19V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_19), makePair(LAZ, 0, apps, lams));
pushRedex(args, seq_17);
return;
}
Term glbl_EQ_315 = newRef(glbl_EQ_314);
Term glbl_LT_313;
void glbl_LT_312(Term ref, Term args) {
Term apps, lams, V;
Location vLoc;
Term seq_23 = makePair(LAM, 0, SUB, NUL);
Location r_seq_23 = portLoc(2, seq_23);
Term y_22 = newTerm(VAR, 0, portLoc(1, seq_23));
seq_23 = makePair(LAM, 0, SUB, seq_23);
Term x_21 = newTerm(VAR, 0, portLoc(1, seq_23));
// allocate args at core: 538
// call int-< at core: 539
Term Rslt_25Args = makePair(APP, 0, y_22, SUB);
Term Rslt_25 = newTerm(VAR, 0, portLoc(2, Rslt_25Args));
Rslt_25Args = makePair(APP, 0, x_21, Rslt_25Args);
#ifdef STRICT
pushRedex(Rslt_25Args, glblFn257);
#else
swap(termLoc(Rslt_25), makePair(LAZ, 0, Rslt_25Args, glblFn257));
#endif

// link args to body
swap(r_seq_23, Rslt_25);

Term Rslt_25V = get(termLoc(Rslt_25));
apps = take(portLoc(1, Rslt_25V));
lams = take(portLoc(2, Rslt_25V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_25), makePair(LAZ, 0, apps, lams));
pushRedex(args, seq_23);
return;
}
Term glbl_LT_313 = newRef(glbl_LT_312);
Term glblcond311;
void glblcond310(Term ref, Term args) {
Term apps, lams, V;
Location vLoc;
Term seq_30 = makePair(LAM, 0, SUB, NUL);
Location r_seq_30 = portLoc(2, seq_30);
Term z_29 = newTerm(VAR, 0, portLoc(1, seq_30));
seq_30 = makePair(LAM, 0, SUB, seq_30);
Term non_z_28 = newTerm(VAR, 0, portLoc(1, seq_30));
seq_30 = makePair(LAM, 0, SUB, seq_30);
Term n_27 = newTerm(VAR, 0, portLoc(1, seq_30));
// allocate args at core: 541
Term glblVal262_1;
glblVal262 = dupeArg(glblVal262, &glblVal262_1, 0);

// call int-cond at core: 542
Term Rslt_32Args = makePair(APP, 0, z_29, SUB);
Term Rslt_32 = newTerm(VAR, 0, portLoc(2, Rslt_32Args));
Rslt_32Args = makePair(APP, 0, non_z_28, Rslt_32Args);
Rslt_32Args = makePair(APP, 0, n_27, Rslt_32Args);
#ifdef STRICT
pushRedex(Rslt_32Args, glblVal262_1);
#else
swap(termLoc(Rslt_32), makePair(LAZ, 0, Rslt_32Args, glblVal262_1));
#endif

// link args to body
swap(r_seq_30, Rslt_32);

Term Rslt_32V = get(termLoc(Rslt_32));
apps = take(portLoc(1, Rslt_32V));
lams = take(portLoc(2, Rslt_32V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_32), makePair(LAZ, 0, apps, lams));
pushRedex(args, seq_30);
return;
}
Term glblcond311 = newRef(glblcond310);
Term glblsha1309;
void glblsha1308(Term ref, Term args) {
Term apps, lams, V;
Location vLoc;
Term seq_35 = makePair(LAM, 0, SUB, NUL);
Location r_seq_35 = portLoc(2, seq_35);
Term x_34 = newTerm(VAR, 0, portLoc(1, seq_35));
// allocate args at core: 544
// call int-sha1 at core: 545
Term Rslt_37Args = makePair(APP, 0, x_34, SUB);
Term Rslt_37 = newTerm(VAR, 0, portLoc(2, Rslt_37Args));
#ifdef STRICT
pushRedex(Rslt_37Args, glblFn264);
#else
swap(termLoc(Rslt_37), makePair(LAZ, 0, Rslt_37Args, glblFn264));
#endif

// link args to body
swap(r_seq_35, Rslt_37);

Term Rslt_37V = get(termLoc(Rslt_37));
apps = take(portLoc(1, Rslt_37V));
lams = take(portLoc(2, Rslt_37V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_37), makePair(LAZ, 0, apps, lams));
pushRedex(args, seq_35);
return;
}
Term glblsha1309 = newRef(glblsha1308);
void glblProto26_331(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(551), args);
  args = makePair(APP, 0, glblStr199, args);
  interact(args, glblProto26);
}
}
void glblProto32_333(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(551), args);
  args = makePair(APP, 0, glblStr199, args);
  interact(args, glblProto32);
}
}
Term glblFn322;
void glblCFn321(Term ref, Term args) {
Term apps, lams, V;
Location vLoc;
Term seq_3 = makePair(LAM, 0, SUB, NUL);
Location r_seq_3 = portLoc(2, seq_3);
Term y_2 = newTerm(VAR, 0, portLoc(1, seq_3));
seq_3 = makePair(LAM, 0, SUB, seq_3);
Term x_1 = newTerm(VAR, 0, portLoc(1, seq_3));
// allocate args at core: 547

Term y_2_1;
y_2 = dupeArg(y_2, &y_2_1, 8);
// call < at core: 551
Term Rslt_5Args = makePair(APP, 0, y_2, SUB);
Term Rslt_5 = newTerm(VAR, 0, portLoc(2, Rslt_5Args));
Rslt_5Args = makePair(APP, 0, x_1, Rslt_5Args);
#ifdef STRICT
pushRedex(Rslt_5Args, newRef(glblProto26_331));
#else
swap(termLoc(Rslt_5), makePair(LAZ, 0, Rslt_5Args, newRef(glblProto26_331)));
#endif
// call either at core: 551
Term Rslt_6Args = makePair(APP, 0, y_2_1, SUB);
Term Rslt_6 = newTerm(VAR, 0, portLoc(2, Rslt_6Args));
Rslt_6Args = makePair(APP, 0, Rslt_5, Rslt_6Args);
#ifdef STRICT
pushRedex(Rslt_6Args, newRef(glblProto32_333));
#else
swap(termLoc(Rslt_6), makePair(LAZ, 0, Rslt_6Args, newRef(glblProto32_333)));
#endif
// link args to body
swap(r_seq_3, Rslt_6);

Term Rslt_6V = get(termLoc(Rslt_6));
apps = take(portLoc(1, Rslt_6V));
lams = take(portLoc(2, Rslt_6V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_6), makePair(LAZ, 0, apps, lams));
pushRedex(args, seq_3);
return;
}
Term glblFn322 = newRef(glblCFn321);
Term glblFn325;
void glblCFn324(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  Term result = NUL;
    if (arityArgs.count == 1) {
  Term v_1 = arityArgs.args[0];

   Vector *v = (Vector *)((u64)v_1 & ~7);
   result = newI60((int64_t)((uint32_t)v->count));
   dec_and_free((Term)v, 1);
move(portLoc(2, args), result);
}
return;
}
Term glblFn325 = newRef(glblCFn324);
Term glblFn336;
void glblCFn334(Term ref, Term args) {
Term apps, lams, V;
Location vLoc;
Term seq_2 = makePair(LAM, 0, SUB, NUL);
Location r_seq_2 = portLoc(2, seq_2);
Term x_1 = newTerm(VAR, 0, portLoc(1, seq_2));
// allocate args at core: 563
// link args to body
swap(r_seq_2, x_1);

V = get(r_seq_2);
apps = makePair(APP, 0, V, SUB);
lams = makePair(LAM, 0, SUB, NUL);
swap(portLoc(2, lams),newTerm(VAR, 0, portLoc(1, lams)));
vLoc = portLoc(2, apps);
swap(vLoc, makePair(LAZ, 0, apps, lams));
swap(r_seq_2, newTerm(VAR, 0, vLoc));
pushRedex(args, seq_2);
return;
}
Term glblFn336 = newRef(glblCFn334);
void glblProto40_342(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(568), args);
  args = makePair(APP, 0, glblStr199, args);
  interact(args, glblProto40);
}
}
Term glblFn341;
void glblCFn340(Term ref, Term args) {
Term apps, lams, V;
Location vLoc;
Term seq_3 = makePair(LAM, 0, SUB, NUL);
Location r_seq_3 = portLoc(2, seq_3);
Term f_2 = newTerm(VAR, 0, portLoc(1, seq_3));
seq_3 = makePair(LAM, 0, SUB, seq_3);
Term x_1 = newTerm(VAR, 0, portLoc(1, seq_3));
// allocate args at core: 567

Term f_2_1;
f_2 = dupeArg(f_2, &f_2_1, 9);
Term seq_7 = makePair(LAM, 0, SUB, NUL);
Location r_seq_7 = portLoc(2, seq_7);
Term v_6 = newTerm(VAR, 0, portLoc(1, seq_7));
// allocate args at core: 568
// call fold at core: 567
Term Rslt_9Args = makePair(APP, 0, f_2_1, SUB);
Term Rslt_9 = newTerm(VAR, 0, portLoc(2, Rslt_9Args));
Rslt_9Args = makePair(APP, 0, v_6, Rslt_9Args);
#ifdef STRICT
pushRedex(Rslt_9Args, glblFn341);
#else
swap(termLoc(Rslt_9), makePair(LAZ, 0, Rslt_9Args, glblFn341));
#endif

// link args to body
swap(r_seq_7, Rslt_9);

Term Rslt_9V = get(termLoc(Rslt_9));
apps = take(portLoc(1, Rslt_9V));
lams = take(portLoc(2, Rslt_9V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_9), makePair(LAZ, 0, apps, lams));
// call recurse at core: 568
Term Rslt_10Args = makePair(APP, 0, seq_7, SUB);
Term Rslt_10 = newTerm(VAR, 0, portLoc(2, Rslt_10Args));
Rslt_10Args = makePair(APP, 0, x_1, Rslt_10Args);
#ifdef STRICT
pushRedex(Rslt_10Args, newRef(glblProto40_342));
#else
swap(termLoc(Rslt_10), makePair(LAZ, 0, Rslt_10Args, newRef(glblProto40_342)));
#endif
// call f at core: 567
Term Rslt_12Args = makePair(APP, 0, Rslt_10, SUB);
Term Rslt_12 = newTerm(VAR, 0, portLoc(2, Rslt_12Args));
#ifdef STRICT
pushRedex(Rslt_12Args, f_2);
#else
swap(termLoc(Rslt_12), makePair(LAZ, 0, Rslt_12Args, f_2));
#endif

// link args to body
swap(r_seq_3, Rslt_12);

Term Rslt_12V = get(termLoc(Rslt_12));
apps = take(portLoc(1, Rslt_12V));
lams = take(portLoc(2, Rslt_12V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_12), makePair(LAZ, 0, apps, lams));
pushRedex(args, seq_3);
return;
}
Term glblFn341 = newRef(glblCFn340);
void glblLeaf349(Term ref, Term args) {
Term callArgs = makePair(APP, 0, newI60(0), args);
callArgs = makePair(APP, 0, newI60(44), callArgs);
interact(callArgs, construct);
return;
}
void glblProto40_352(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(578), args);
  args = makePair(APP, 0, glblStr199, args);
  interact(args, glblProto40);
}
}
Term glblFn346;
void glblCFn345(Term ref, Term args) {
Term apps, lams, V;
Location vLoc;
Term seq_3 = makePair(LAM, 0, SUB, NUL);
Location r_seq_3 = portLoc(2, seq_3);
Term f_2 = newTerm(VAR, 0, portLoc(1, seq_3));
seq_3 = makePair(LAM, 0, SUB, seq_3);
Term x_1 = newTerm(VAR, 0, portLoc(1, seq_3));
// allocate args at core: 577

Term f_2_1;
f_2 = dupeArg(f_2, &f_2_1, 10);
Term seq_8 = makePair(LAM, 0, SUB, NUL);
Location r_seq_8 = portLoc(2, seq_8);
Term v_7 = newTerm(VAR, 0, portLoc(1, seq_8));
// allocate args at core: 578
// call f at core: 577
Term Rslt_5Args = makePair(APP, 0, x_1, SUB);
Term Rslt_5 = newTerm(VAR, 0, portLoc(2, Rslt_5Args));
#ifdef STRICT
pushRedex(Rslt_5Args, f_2);
#else
swap(termLoc(Rslt_5), makePair(LAZ, 0, Rslt_5Args, f_2));
#endif

// call unfold at core: 577
Term Rslt_10Args = makePair(APP, 0, f_2_1, SUB);
Term Rslt_10 = newTerm(VAR, 0, portLoc(2, Rslt_10Args));
Rslt_10Args = makePair(APP, 0, v_7, Rslt_10Args);
#ifdef STRICT
pushRedex(Rslt_10Args, glblFn346);
#else
swap(termLoc(Rslt_10), makePair(LAZ, 0, Rslt_10Args, glblFn346));
#endif

// link args to body
swap(r_seq_8, Rslt_10);

Term Rslt_10V = get(termLoc(Rslt_10));
apps = take(portLoc(1, Rslt_10V));
lams = take(portLoc(2, Rslt_10V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_10), makePair(LAZ, 0, apps, lams));
// call recurse at core: 578
Term Rslt_11Args = makePair(APP, 0, seq_8, SUB);
Term Rslt_11 = newTerm(VAR, 0, portLoc(2, Rslt_11Args));
Rslt_11Args = makePair(APP, 0, Rslt_5, Rslt_11Args);
#ifdef STRICT
pushRedex(Rslt_11Args, newRef(glblProto40_352));
#else
swap(termLoc(Rslt_11), makePair(LAZ, 0, Rslt_11Args, newRef(glblProto40_352)));
#endif
// link args to body
swap(r_seq_3, Rslt_11);

Term Rslt_11V = get(termLoc(Rslt_11));
apps = take(portLoc(1, Rslt_11V));
lams = take(portLoc(2, Rslt_11V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_11), makePair(LAZ, 0, apps, lams));
pushRedex(args, seq_3);
return;
}
Term glblFn346 = newRef(glblCFn345);
Term glblVal351 = newRef(glblLeaf349);
Term glblVal351;
Term glblRslt350;
Term glblFn348;
void glblCFn347(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  Term result = NUL;
    if (arityArgs.count == 1) {
  Term s_1 = arityArgs.args[0];

    result = strCount(s_1);
    dec_and_free(s_1, 1);
move(portLoc(2, args), result);
}
return;
}
Term glblFn348 = newRef(glblCFn347);
Term glblFn357;
void glblCFn356(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  Term result = NUL;
    if (arityArgs.count == 1) {
  Term len_1 = arityArgs.args[0];

  String *strVal = malloc_string(getI60(len_1));
  strVal->len = 0;
  strVal->buffer[0] = 0;
  dec_and_free(len_1, 1);
  result = termVal((Term)strVal);

move(portLoc(2, args), result);
}
return;
}
Term glblFn357 = newRef(glblCFn356);
Term glblFn360;
void glblCFn359(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 2, &arityArgs);
  Term result = NUL;
    if (arityArgs.count == 2) {
  Term dest_1 = arityArgs.args[0];
  Term src_2 = arityArgs.args[1];

  String *s_1 = (String *)dest_1;
  String *s_2 = (String *)src_2;
  strncat(s_1->buffer, s_2->buffer, s_2->len);
  s_1->len += s_2->len;
  dec_and_free(src_2, 1);
  result = termVal(dest_1);
move(portLoc(2, args), result);
}
return;
}
Term glblFn360 = newRef(glblCFn359);
Term glblFn363;
void glblCFn362(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 2, &arityArgs);
  Term result = NUL;
    if (arityArgs.count == 2) {
  Term s_1 = arityArgs.args[0];
  Term tgt_2 = arityArgs.args[1];
result = strCmp(s_1, tgt_2, STR_EQ);
move(portLoc(2, args), result);
}
return;
}
Term glblFn363 = newRef(glblCFn362);
Term glblFn366;
void glblCFn365(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 2, &arityArgs);
  Term result = NUL;
    if (arityArgs.count == 2) {
  Term s_1 = arityArgs.args[0];
  Term tgt_2 = arityArgs.args[1];
result = strCmp(s_1, tgt_2, STR_LT);
move(portLoc(2, args), result);
}
return;
}
Term glblFn366 = newRef(glblCFn365);
Term glblFn369;
void glblCFn368(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 2, &arityArgs);
  Term result = NUL;
    if (arityArgs.count == 2) {
  Term s_1 = arityArgs.args[0];
  Term tgt_2 = arityArgs.args[1];
result = strCmp(s_1, tgt_2, STR_LT | STR_EQ);
move(portLoc(2, args), result);
}
return;
}
Term glblFn369 = newRef(glblCFn368);
Term glblFn372;
void glblCFn371(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  Term result = NUL;
    if (arityArgs.count == 1) {
  Term s_1 = arityArgs.args[0];
result = termVal((Term)escapeChars(s_1));
move(portLoc(2, args), result);
}
return;
}
Term glblFn372 = newRef(glblCFn371);
Term glblFn375;
void glblCFn374(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  Term result = NUL;
    if (arityArgs.count == 1) {
  Term n_1 = arityArgs.args[0];

  String *strVal = malloc_string(2);
  strVal->len = 1;
  strVal->buffer[0] = getI60(n_1);
  strVal->buffer[1] = 0;
  result = termVal((Term)strVal);

move(portLoc(2, args), result);
}
return;
}
Term glblFn375 = newRef(glblCFn374);
Term glblFn378;
void glblCFn377(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  Term result = NUL;
    if (arityArgs.count == 1) {
  Term c_1 = arityArgs.args[0];

   if (((Value *)c_1)->type == StringBufferType) {
     String *s = (String *)c_1;
     result = newI60((unsigned char)s->buffer[0]);
     dec_and_free(c_1, 1);
   } else if (((Value *)c_1)->type == SubStringType) {
     ReifiedVal *ss = (ReifiedVal *)c_1;
     String *parent = (String *)ss->impls[0];
     long start = getI60(ss->impls[1]);
     result = newI60((unsigned char)parent->buffer[start]);
     dec_and_free(c_1, 1);
   }

move(portLoc(2, args), result);
}
return;
}
Term glblFn378 = newRef(glblCFn377);
Term glblProto380;
Term glblProto386;
Term glblProto384;
Term glblProto382;
void glblSubString394(Term ref, Term args) {
Term callArgs = makePair(APP, 0, newI60(3), args);
callArgs = makePair(APP, 0, newI60(44), callArgs);
interact(callArgs, construct);
return;
}
Term glblVal401 = newRef(glblSubString394);
Term glblVal401;
Term glblstr_vect389;
void glblstr_vect388(Term ref, Term args) {
Term apps, lams, V;
Location vLoc;
Term seq_2 = makePair(LAM, 0, SUB, NUL);
Location r_seq_2 = portLoc(2, seq_2);
Term s_1 = newTerm(VAR, 0, portLoc(1, seq_2));
// allocate args at core: 669
Term s_1Args = makePair(APP, 0, s_1, SUB);
Term Rslt_4 = newTerm(VAR, 0, portLoc(2, s_1Args));
s_1Args = makePair(APP, 0, newI60(0), s_1Args);
pushRedex(s_1Args, accessField);
Term glblVal175_6;
glblVal175 = dupeArg(glblVal175, &glblVal175_6, 0);

// call vect-conj at core: 669
Term Rslt_5Args = makePair(APP, 0, Rslt_4, SUB);
Term Rslt_5 = newTerm(VAR, 0, portLoc(2, Rslt_5Args));
Rslt_5Args = makePair(APP, 0, glblVal175_6, Rslt_5Args);
#ifdef STRICT
pushRedex(Rslt_5Args, glblFn178);
#else
swap(termLoc(Rslt_5), makePair(LAZ, 0, Rslt_5Args, glblFn178));
#endif

// link args to body
swap(r_seq_2, Rslt_5);

Term Rslt_5V = get(termLoc(Rslt_5));
apps = take(portLoc(1, Rslt_5V));
lams = take(portLoc(2, Rslt_5V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_5), makePair(LAZ, 0, apps, lams));
pushRedex(args, seq_2);
return;
}
Term glblstr_vect389 = newRef(glblstr_vect388);
Term glblcount391;
void glblcount390(Term ref, Term args) {
Term apps, lams, V;
Location vLoc;
Term seq_8 = makePair(LAM, 0, SUB, NUL);
Location r_seq_8 = portLoc(2, seq_8);
Term arg_7 = newTerm(VAR, 0, portLoc(1, seq_8));
// allocate args at core: 670
Term arg_7Args = makePair(APP, 0, arg_7, SUB);
Term Rslt_10 = newTerm(VAR, 0, portLoc(2, arg_7Args));
arg_7Args = makePair(APP, 0, newI60(2), arg_7Args);
pushRedex(arg_7Args, accessField);
// link args to body
swap(r_seq_8, Rslt_10);

V = get(r_seq_8);
apps = makePair(APP, 0, V, SUB);
lams = makePair(LAM, 0, SUB, NUL);
swap(portLoc(2, lams),newTerm(VAR, 0, portLoc(1, lams)));
vLoc = portLoc(2, apps);
swap(vLoc, makePair(LAZ, 0, apps, lams));
swap(r_seq_8, newTerm(VAR, 0, vLoc));
pushRedex(args, seq_8);
return;
}
Term glblcount391 = newRef(glblcount390);
struct {TYPE_SIZE type;
        REFS_SIZE refs;
        int64_t *hash;
        Value *parent;
        int64_t len;
        char buffer[1];
} str7 = {StringBufferType, REFS_STATIC, 0, 0, 0, ""};
Term glblStr402 = newTerm_(VAL, (Term)&str7);
void glblProto34_405(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(674), args);
  args = makePair(APP, 0, glblStr199, args);
  interact(args, glblProto34);
}
}
Term glblsubs393;
void glblsubs392(Term ref, Term args) {
Term apps, lams, V;
Location vLoc;
Term seq_15 = makePair(LAM, 0, SUB, NUL);
Location r_seq_15 = portLoc(2, seq_15);
Term new_len_14 = newTerm(VAR, 0, portLoc(1, seq_15));
seq_15 = makePair(LAM, 0, SUB, seq_15);
Term new_start_13 = newTerm(VAR, 0, portLoc(1, seq_15));
seq_15 = makePair(LAM, 0, SUB, seq_15);
Term s_12 = newTerm(VAR, 0, portLoc(1, seq_15));
// allocate args at core: 672

Term s_12_2;
s_12 = dupeArg(s_12, &s_12_2, 11);

Term s_12_1;
s_12 = dupeArg(s_12, &s_12_1, 12);

Term new_start_13_1;
new_start_13 = dupeArg(new_start_13, &new_start_13_1, 13);
Term s_12_2Args = makePair(APP, 0, s_12_2, SUB);
Term Rslt_27 = newTerm(VAR, 0, portLoc(2, s_12_2Args));
s_12_2Args = makePair(APP, 0, newI60(2), s_12_2Args);
pushRedex(s_12_2Args, accessField);
Term seq_19 = makePair(LAM, 0, SUB, NUL);
Location r_seq_19 = portLoc(2, seq_19);
Term final_len_18 = newTerm(VAR, 0, portLoc(1, seq_19));
// allocate args at core: 673

Term final_len_18_1;
final_len_18 = dupeArg(final_len_18, &final_len_18_1, 0);
Term s_12_1Args = makePair(APP, 0, s_12_1, SUB);
Term Rslt_23 = newTerm(VAR, 0, portLoc(2, s_12_1Args));
s_12_1Args = makePair(APP, 0, newI60(1), s_12_1Args);
pushRedex(s_12_1Args, accessField);
Term s_12Args = makePair(APP, 0, s_12, SUB);
Term Rslt_22 = newTerm(VAR, 0, portLoc(2, s_12Args));
s_12Args = makePair(APP, 0, newI60(0), s_12Args);
pushRedex(s_12Args, accessField);
Term glblVal401_1;
glblVal401 = dupeArg(glblVal401, &glblVal401_1, 0);

// call <= at core: 674
Term Rslt_21Args = makePair(APP, 0, newI60(0), SUB);
Term Rslt_21 = newTerm(VAR, 0, portLoc(2, Rslt_21Args));
Rslt_21Args = makePair(APP, 0, final_len_18, Rslt_21Args);
#ifdef STRICT
pushRedex(Rslt_21Args, glblFn261);
#else
swap(termLoc(Rslt_21), makePair(LAZ, 0, Rslt_21Args, glblFn261));
#endif

Term Rslt_24 = makeOp(OP_ADD, Rslt_23, new_start_13);
// call SubString at core: 676
Term Rslt_25Args = makePair(APP, 0, final_len_18_1, SUB);
Term Rslt_25 = newTerm(VAR, 0, portLoc(2, Rslt_25Args));
Rslt_25Args = makePair(APP, 0, Rslt_24, Rslt_25Args);
Rslt_25Args = makePair(APP, 0, Rslt_22, Rslt_25Args);
#ifdef STRICT
pushRedex(Rslt_25Args, glblVal401_1);
#else
swap(termLoc(Rslt_25), makePair(LAZ, 0, Rslt_25Args, glblVal401_1));
#endif

// call cond at core: 674
Term Rslt_26Args = makePair(APP, 0, Rslt_25, SUB);
Term Rslt_26 = newTerm(VAR, 0, portLoc(2, Rslt_26Args));
Rslt_26Args = makePair(APP, 0, glblStr402, Rslt_26Args);
Rslt_26Args = makePair(APP, 0, Rslt_21, Rslt_26Args);
#ifdef STRICT
pushRedex(Rslt_26Args, newRef(glblProto34_405));
#else
swap(termLoc(Rslt_26), makePair(LAZ, 0, Rslt_26Args, newRef(glblProto34_405)));
#endif
// link args to body
swap(r_seq_19, Rslt_26);

Term Rslt_26V = get(termLoc(Rslt_26));
apps = take(portLoc(1, Rslt_26V));
lams = take(portLoc(2, Rslt_26V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_26), makePair(LAZ, 0, apps, lams));
Term Rslt_28 = makeOp(OP_SUB, Rslt_27, new_start_13_1);
// call min at core: 673
Term Rslt_29Args = makePair(APP, 0, Rslt_28, SUB);
Term Rslt_29 = newTerm(VAR, 0, portLoc(2, Rslt_29Args));
Rslt_29Args = makePair(APP, 0, new_len_14, Rslt_29Args);
#ifdef STRICT
pushRedex(Rslt_29Args, glblFn322);
#else
swap(termLoc(Rslt_29), makePair(LAZ, 0, Rslt_29Args, glblFn322));
#endif

// call  at core: 673
Term Rslt_30Args = makePair(APP, 0, Rslt_29, SUB);
Term Rslt_30 = newTerm(VAR, 0, portLoc(2, Rslt_30Args));
#ifdef STRICT
pushRedex(Rslt_30Args, seq_19);
#else
swap(termLoc(Rslt_30), makePair(LAZ, 0, Rslt_30Args, seq_19));
#endif

// link args to body
swap(r_seq_15, Rslt_30);

Term Rslt_30V = get(termLoc(Rslt_30));
apps = take(portLoc(1, Rslt_30V));
lams = take(portLoc(2, Rslt_30V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_30), makePair(LAZ, 0, apps, lams));
pushRedex(args, seq_15);
return;
}
Term glblsubs393 = newRef(glblsubs392);
Term glblFld396;
void glblFldFn395(Term ref, Term args) {
args = makePair(APP, 0, newI60(0), args);
pushRedex(args, accessField);
return;
}
Term glblFld396 = newRef(glblFldFn395);
Term glblFld398;
void glblFldFn397(Term ref, Term args) {
args = makePair(APP, 0, newI60(1), args);
pushRedex(args, accessField);
return;
}
Term glblFld398 = newRef(glblFldFn397);
Term glblFld400;
void glblFldFn399(Term ref, Term args) {
args = makePair(APP, 0, newI60(2), args);
pushRedex(args, accessField);
return;
}
Term glblFld400 = newRef(glblFldFn399);
Term glblstr_vect413;
void glblstr_vect412(Term ref, Term args) {
Term apps, lams, V;
Location vLoc;
Term seq_33 = makePair(LAM, 0, SUB, NUL);
Location r_seq_33 = portLoc(2, seq_33);
Term s_32 = newTerm(VAR, 0, portLoc(1, seq_33));
// allocate args at core: 679
Term glblVal175_7;
glblVal175 = dupeArg(glblVal175, &glblVal175_7, 0);

// call vect-conj at core: 680
Term Rslt_35Args = makePair(APP, 0, s_32, SUB);
Term Rslt_35 = newTerm(VAR, 0, portLoc(2, Rslt_35Args));
Rslt_35Args = makePair(APP, 0, glblVal175_7, Rslt_35Args);
#ifdef STRICT
pushRedex(Rslt_35Args, glblFn178);
#else
swap(termLoc(Rslt_35), makePair(LAZ, 0, Rslt_35Args, glblFn178));
#endif

// link args to body
swap(r_seq_33, Rslt_35);

Term Rslt_35V = get(termLoc(Rslt_35));
apps = take(portLoc(1, Rslt_35V));
lams = take(portLoc(2, Rslt_35V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_35), makePair(LAZ, 0, apps, lams));
pushRedex(args, seq_33);
return;
}
Term glblstr_vect413 = newRef(glblstr_vect412);
Term glblcount411;
void glblcount410(Term ref, Term args) {
Term apps, lams, V;
Location vLoc;
Term seq_38 = makePair(LAM, 0, SUB, NUL);
Location r_seq_38 = portLoc(2, seq_38);
Term s_37 = newTerm(VAR, 0, portLoc(1, seq_38));
// allocate args at core: 682
// call str-count at core: 683
Term Rslt_40Args = makePair(APP, 0, s_37, SUB);
Term Rslt_40 = newTerm(VAR, 0, portLoc(2, Rslt_40Args));
#ifdef STRICT
pushRedex(Rslt_40Args, glblFn348);
#else
swap(termLoc(Rslt_40), makePair(LAZ, 0, Rslt_40Args, glblFn348));
#endif

// link args to body
swap(r_seq_38, Rslt_40);

Term Rslt_40V = get(termLoc(Rslt_40));
apps = take(portLoc(1, Rslt_40V));
lams = take(portLoc(2, Rslt_40V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_40), makePair(LAZ, 0, apps, lams));
pushRedex(args, seq_38);
return;
}
Term glblcount411 = newRef(glblcount410);
void glblProto34_421(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(687), args);
  args = makePair(APP, 0, glblStr199, args);
  interact(args, glblProto34);
}
}
void glblProto64_423(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(686), args);
  args = makePair(APP, 0, glblStr199, args);
  interact(args, glblProto64);
}
}
Term glblsubs409;
void glblsubs408(Term ref, Term args) {
Term apps, lams, V;
Location vLoc;
Term seq_45 = makePair(LAM, 0, SUB, NUL);
Location r_seq_45 = portLoc(2, seq_45);
Term len_44 = newTerm(VAR, 0, portLoc(1, seq_45));
seq_45 = makePair(LAM, 0, SUB, seq_45);
Term start_43 = newTerm(VAR, 0, portLoc(1, seq_45));
seq_45 = makePair(LAM, 0, SUB, seq_45);
Term s_42 = newTerm(VAR, 0, portLoc(1, seq_45));
// allocate args at core: 685

Term s_42_1;
s_42 = dupeArg(s_42, &s_42_1, 14);

Term start_43_1;
start_43 = dupeArg(start_43, &start_43_1, 15);
Term seq_49 = makePair(LAM, 0, SUB, NUL);
Location r_seq_49 = portLoc(2, seq_49);
Term final_len_48 = newTerm(VAR, 0, portLoc(1, seq_49));
// allocate args at core: 686

Term final_len_48_1;
final_len_48 = dupeArg(final_len_48, &final_len_48_1, 0);
Term glblVal401_2;
glblVal401 = dupeArg(glblVal401, &glblVal401_2, 0);

// call <= at core: 687
Term Rslt_51Args = makePair(APP, 0, newI60(0), SUB);
Term Rslt_51 = newTerm(VAR, 0, portLoc(2, Rslt_51Args));
Rslt_51Args = makePair(APP, 0, final_len_48, Rslt_51Args);
#ifdef STRICT
pushRedex(Rslt_51Args, glblFn261);
#else
swap(termLoc(Rslt_51), makePair(LAZ, 0, Rslt_51Args, glblFn261));
#endif

// call SubString at core: 689
Term Rslt_52Args = makePair(APP, 0, final_len_48_1, SUB);
Term Rslt_52 = newTerm(VAR, 0, portLoc(2, Rslt_52Args));
Rslt_52Args = makePair(APP, 0, start_43, Rslt_52Args);
Rslt_52Args = makePair(APP, 0, s_42, Rslt_52Args);
#ifdef STRICT
pushRedex(Rslt_52Args, glblVal401_2);
#else
swap(termLoc(Rslt_52), makePair(LAZ, 0, Rslt_52Args, glblVal401_2));
#endif

// call cond at core: 687
Term Rslt_53Args = makePair(APP, 0, Rslt_52, SUB);
Term Rslt_53 = newTerm(VAR, 0, portLoc(2, Rslt_53Args));
Rslt_53Args = makePair(APP, 0, glblStr402, Rslt_53Args);
Rslt_53Args = makePair(APP, 0, Rslt_51, Rslt_53Args);
#ifdef STRICT
pushRedex(Rslt_53Args, newRef(glblProto34_421));
#else
swap(termLoc(Rslt_53), makePair(LAZ, 0, Rslt_53Args, newRef(glblProto34_421)));
#endif
// link args to body
swap(r_seq_49, Rslt_53);

Term Rslt_53V = get(termLoc(Rslt_53));
apps = take(portLoc(1, Rslt_53V));
lams = take(portLoc(2, Rslt_53V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_53), makePair(LAZ, 0, apps, lams));
// call count at core: 686
Term Rslt_54Args = makePair(APP, 0, s_42_1, SUB);
Term Rslt_54 = newTerm(VAR, 0, portLoc(2, Rslt_54Args));
#ifdef STRICT
pushRedex(Rslt_54Args, newRef(glblProto64_423));
#else
swap(termLoc(Rslt_54), makePair(LAZ, 0, Rslt_54Args, newRef(glblProto64_423)));
#endif
Term Rslt_55 = makeOp(OP_SUB, Rslt_54, start_43_1);
// call min at core: 686
Term Rslt_56Args = makePair(APP, 0, len_44, SUB);
Term Rslt_56 = newTerm(VAR, 0, portLoc(2, Rslt_56Args));
Rslt_56Args = makePair(APP, 0, Rslt_55, Rslt_56Args);
#ifdef STRICT
pushRedex(Rslt_56Args, glblFn322);
#else
swap(termLoc(Rslt_56), makePair(LAZ, 0, Rslt_56Args, glblFn322));
#endif

// call  at core: 686
Term Rslt_57Args = makePair(APP, 0, Rslt_56, SUB);
Term Rslt_57 = newTerm(VAR, 0, portLoc(2, Rslt_57Args));
#ifdef STRICT
pushRedex(Rslt_57Args, seq_49);
#else
swap(termLoc(Rslt_57), makePair(LAZ, 0, Rslt_57Args, seq_49));
#endif

// link args to body
swap(r_seq_45, Rslt_57);

Term Rslt_57V = get(termLoc(Rslt_57));
apps = take(portLoc(1, Rslt_57V));
lams = take(portLoc(2, Rslt_57V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_57), makePair(LAZ, 0, apps, lams));
pushRedex(args, seq_45);
return;
}
Term glblsubs409 = newRef(glblsubs408);
Term glblFn415;
void glblCFn414(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  Term result = NUL;
    if (arityArgs.count == 1) {
  Term s_1 = arityArgs.args[0];
result = termVal((Term)strVec((Value *)s_1));
move(portLoc(2, args), result);
}
return;
}
Term glblFn415 = newRef(glblCFn414);
Term glblFn417;
void glblCFn416(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  Term result = NUL;
    if (arityArgs.count == 1) {
  Term s_1 = arityArgs.args[0];

  result = newI60(strSha1((Value *)s_1));
move(portLoc(2, args), result);
}
return;
}
Term glblFn417 = newRef(glblCFn416);
struct {TYPE_SIZE type;
        REFS_SIZE refs;
        int64_t *hash;
        Value *parent;
        int64_t len;
        char buffer[7];
} str8 = {StringBufferType, REFS_STATIC, 0, 0, 6, "String"};
Term glblStr486 = newTerm_(VAL, (Term)&str8);
Term glbltype_name483;
void glbltype_name482(Term ref, Term args) {
Term apps, lams, V;
Location vLoc;
Term seq_6 = makePair(LAM, 0, SUB, NUL);
Location r_seq_6 = portLoc(2, seq_6);
Term arg_5 = newTerm(VAR, 0, portLoc(1, seq_6));
// allocate args at core: 700
swap(termLoc(arg_5), ERA);
// link args to body
swap(r_seq_6, glblStr486);

V = get(r_seq_6);
apps = makePair(APP, 0, V, SUB);
lams = makePair(LAM, 0, SUB, NUL);
swap(portLoc(2, lams),newTerm(VAR, 0, portLoc(1, lams)));
vLoc = portLoc(2, apps);
swap(vLoc, makePair(LAZ, 0, apps, lams));
swap(r_seq_6, newTerm(VAR, 0, vLoc));
pushRedex(args, seq_6);
return;
}
Term glbltype_name483 = newRef(glbltype_name482);
Term glbltype_name485;
void glbltype_name484(Term ref, Term args) {
Term apps, lams, V;
Location vLoc;
Term seq_10 = makePair(LAM, 0, SUB, NUL);
Location r_seq_10 = portLoc(2, seq_10);
Term arg_9 = newTerm(VAR, 0, portLoc(1, seq_10));
// allocate args at core: 700
swap(termLoc(arg_9), ERA);
// link args to body
swap(r_seq_10, glblStr486);

V = get(r_seq_10);
apps = makePair(APP, 0, V, SUB);
lams = makePair(LAM, 0, SUB, NUL);
swap(portLoc(2, lams),newTerm(VAR, 0, portLoc(1, lams)));
vLoc = portLoc(2, apps);
swap(vLoc, makePair(LAZ, 0, apps, lams));
swap(r_seq_10, newTerm(VAR, 0, vLoc));
pushRedex(args, seq_10);
return;
}
Term glbltype_name485 = newRef(glbltype_name484);
Term glblempty478;
void glblempty476(Term ref, Term args) {
Term apps, lams, V;
Location vLoc;
Term seq_14 = makePair(LAM, 0, SUB, NUL);
Location r_seq_14 = portLoc(2, seq_14);
Term s_13 = newTerm(VAR, 0, portLoc(1, seq_14));
// allocate args at core: 703
swap(termLoc(s_13), ERA);
// link args to body
swap(r_seq_14, glblStr402);

V = get(r_seq_14);
apps = makePair(APP, 0, V, SUB);
lams = makePair(LAM, 0, SUB, NUL);
swap(portLoc(2, lams),newTerm(VAR, 0, portLoc(1, lams)));
vLoc = portLoc(2, apps);
swap(vLoc, makePair(LAZ, 0, apps, lams));
swap(r_seq_14, newTerm(VAR, 0, vLoc));
pushRedex(args, seq_14);
return;
}
Term glblempty478 = newRef(glblempty476);
Term glblempty481;
void glblempty480(Term ref, Term args) {
Term apps, lams, V;
Location vLoc;
Term seq_18 = makePair(LAM, 0, SUB, NUL);
Location r_seq_18 = portLoc(2, seq_18);
Term s_17 = newTerm(VAR, 0, portLoc(1, seq_18));
// allocate args at core: 703
swap(termLoc(s_17), ERA);
// link args to body
swap(r_seq_18, glblStr402);

V = get(r_seq_18);
apps = makePair(APP, 0, V, SUB);
lams = makePair(LAM, 0, SUB, NUL);
swap(portLoc(2, lams),newTerm(VAR, 0, portLoc(1, lams)));
vLoc = portLoc(2, apps);
swap(vLoc, makePair(LAZ, 0, apps, lams));
swap(r_seq_18, newTerm(VAR, 0, vLoc));
pushRedex(args, seq_18);
return;
}
Term glblempty481 = newRef(glblempty480);
Term glblstr_vect473;
void glblstr_vect472(Term ref, Term args) {
Term apps, lams, V;
Location vLoc;
Term seq_22 = makePair(LAM, 0, SUB, NUL);
Location r_seq_22 = portLoc(2, seq_22);
Term s_21 = newTerm(VAR, 0, portLoc(1, seq_22));
// allocate args at core: 706
Term glblVal175_8;
glblVal175 = dupeArg(glblVal175, &glblVal175_8, 0);

// call vect-conj at core: 707
Term Rslt_24Args = makePair(APP, 0, s_21, SUB);
Term Rslt_24 = newTerm(VAR, 0, portLoc(2, Rslt_24Args));
Rslt_24Args = makePair(APP, 0, glblVal175_8, Rslt_24Args);
#ifdef STRICT
pushRedex(Rslt_24Args, glblFn178);
#else
swap(termLoc(Rslt_24), makePair(LAZ, 0, Rslt_24Args, glblFn178));
#endif

// link args to body
swap(r_seq_22, Rslt_24);

Term Rslt_24V = get(termLoc(Rslt_24));
apps = take(portLoc(1, Rslt_24V));
lams = take(portLoc(2, Rslt_24V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_24), makePair(LAZ, 0, apps, lams));
pushRedex(args, seq_22);
return;
}
Term glblstr_vect473 = newRef(glblstr_vect472);
Term glblstr_vect475;
void glblstr_vect474(Term ref, Term args) {
Term apps, lams, V;
Location vLoc;
Term seq_27 = makePair(LAM, 0, SUB, NUL);
Location r_seq_27 = portLoc(2, seq_27);
Term s_26 = newTerm(VAR, 0, portLoc(1, seq_27));
// allocate args at core: 706
Term glblVal175_9;
glblVal175 = dupeArg(glblVal175, &glblVal175_9, 0);

// call vect-conj at core: 707
Term Rslt_29Args = makePair(APP, 0, s_26, SUB);
Term Rslt_29 = newTerm(VAR, 0, portLoc(2, Rslt_29Args));
Rslt_29Args = makePair(APP, 0, glblVal175_9, Rslt_29Args);
#ifdef STRICT
pushRedex(Rslt_29Args, glblFn178);
#else
swap(termLoc(Rslt_29), makePair(LAZ, 0, Rslt_29Args, glblFn178));
#endif

// link args to body
swap(r_seq_27, Rslt_29);

Term Rslt_29V = get(termLoc(Rslt_29));
apps = take(portLoc(1, Rslt_29V));
lams = take(portLoc(2, Rslt_29V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_29), makePair(LAZ, 0, apps, lams));
pushRedex(args, seq_27);
return;
}
Term glblstr_vect475 = newRef(glblstr_vect474);
Term glblrecurse469;
void glblrecurse468(Term ref, Term args) {
Term apps, lams, V;
Location vLoc;
Term seq_33 = makePair(LAM, 0, SUB, NUL);
Location r_seq_33 = portLoc(2, seq_33);
Term f_32 = newTerm(VAR, 0, portLoc(1, seq_33));
seq_33 = makePair(LAM, 0, SUB, seq_33);
Term s_31 = newTerm(VAR, 0, portLoc(1, seq_33));
// allocate args at core: 709
swap(termLoc(f_32), ERA);
// link args to body
swap(r_seq_33, s_31);

V = get(r_seq_33);
apps = makePair(APP, 0, V, SUB);
lams = makePair(LAM, 0, SUB, NUL);
swap(portLoc(2, lams),newTerm(VAR, 0, portLoc(1, lams)));
vLoc = portLoc(2, apps);
swap(vLoc, makePair(LAZ, 0, apps, lams));
swap(r_seq_33, newTerm(VAR, 0, vLoc));
pushRedex(args, seq_33);
return;
}
Term glblrecurse469 = newRef(glblrecurse468);
Term glblrecurse471;
void glblrecurse470(Term ref, Term args) {
Term apps, lams, V;
Location vLoc;
Term seq_38 = makePair(LAM, 0, SUB, NUL);
Location r_seq_38 = portLoc(2, seq_38);
Term f_37 = newTerm(VAR, 0, portLoc(1, seq_38));
seq_38 = makePair(LAM, 0, SUB, seq_38);
Term s_36 = newTerm(VAR, 0, portLoc(1, seq_38));
// allocate args at core: 709
swap(termLoc(f_37), ERA);
// link args to body
swap(r_seq_38, s_36);

V = get(r_seq_38);
apps = makePair(APP, 0, V, SUB);
lams = makePair(LAM, 0, SUB, NUL);
swap(portLoc(2, lams),newTerm(VAR, 0, portLoc(1, lams)));
vLoc = portLoc(2, apps);
swap(vLoc, makePair(LAZ, 0, apps, lams));
swap(r_seq_38, newTerm(VAR, 0, vLoc));
pushRedex(args, seq_38);
return;
}
Term glblrecurse471 = newRef(glblrecurse470);
void glblProto64_508(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(713), args);
  args = makePair(APP, 0, glblStr199, args);
  interact(args, glblProto64);
}
}
void glblProto20_510(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(713), args);
  args = makePair(APP, 0, glblStr199, args);
  interact(args, glblProto20);
}
}
void glblProto28_512(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(713), args);
  args = makePair(APP, 0, glblStr199, args);
  interact(args, glblProto28);
}
}
Term glblempty_QM_465;
void glblempty_QM_464(Term ref, Term args) {
Term apps, lams, V;
Location vLoc;
Term seq_42 = makePair(LAM, 0, SUB, NUL);
Location r_seq_42 = portLoc(2, seq_42);
Term s_41 = newTerm(VAR, 0, portLoc(1, seq_42));
// allocate args at core: 712
Term glblVal25_3;
glblVal25 = dupeArg(glblVal25, &glblVal25_3, 0);

// call count at core: 713
Term Rslt_44Args = makePair(APP, 0, s_41, SUB);
Term Rslt_44 = newTerm(VAR, 0, portLoc(2, Rslt_44Args));
#ifdef STRICT
pushRedex(Rslt_44Args, newRef(glblProto64_508));
#else
swap(termLoc(Rslt_44), makePair(LAZ, 0, Rslt_44Args, newRef(glblProto64_508)));
#endif
// call = at core: 713
Term Rslt_45Args = makePair(APP, 0, Rslt_44, SUB);
Term Rslt_45 = newTerm(VAR, 0, portLoc(2, Rslt_45Args));
Rslt_45Args = makePair(APP, 0, newI60(0), Rslt_45Args);
#ifdef STRICT
pushRedex(Rslt_45Args, newRef(glblProto20_510));
#else
swap(termLoc(Rslt_45), makePair(LAZ, 0, Rslt_45Args, newRef(glblProto20_510)));
#endif
// call Some at core: 714
Term Rslt_46Args = makePair(APP, 0, glblStr402, SUB);
Term Rslt_46 = newTerm(VAR, 0, portLoc(2, Rslt_46Args));
#ifdef STRICT
pushRedex(Rslt_46Args, glblVal25_3);
#else
swap(termLoc(Rslt_46), makePair(LAZ, 0, Rslt_46Args, glblVal25_3));
#endif

// call and at core: 713
Term Rslt_47Args = makePair(APP, 0, Rslt_46, SUB);
Term Rslt_47 = newTerm(VAR, 0, portLoc(2, Rslt_47Args));
Rslt_47Args = makePair(APP, 0, Rslt_45, Rslt_47Args);
#ifdef STRICT
pushRedex(Rslt_47Args, newRef(glblProto28_512));
#else
swap(termLoc(Rslt_47), makePair(LAZ, 0, Rslt_47Args, newRef(glblProto28_512)));
#endif
// link args to body
swap(r_seq_42, Rslt_47);

Term Rslt_47V = get(termLoc(Rslt_47));
apps = take(portLoc(1, Rslt_47V));
lams = take(portLoc(2, Rslt_47V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_47), makePair(LAZ, 0, apps, lams));
pushRedex(args, seq_42);
return;
}
Term glblempty_QM_465 = newRef(glblempty_QM_464);
void glblProto64_515(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(713), args);
  args = makePair(APP, 0, glblStr199, args);
  interact(args, glblProto64);
}
}
void glblProto20_517(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(713), args);
  args = makePair(APP, 0, glblStr199, args);
  interact(args, glblProto20);
}
}
void glblProto28_519(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(713), args);
  args = makePair(APP, 0, glblStr199, args);
  interact(args, glblProto28);
}
}
Term glblempty_QM_467;
void glblempty_QM_466(Term ref, Term args) {
Term apps, lams, V;
Location vLoc;
Term seq_50 = makePair(LAM, 0, SUB, NUL);
Location r_seq_50 = portLoc(2, seq_50);
Term s_49 = newTerm(VAR, 0, portLoc(1, seq_50));
// allocate args at core: 712
Term glblVal25_4;
glblVal25 = dupeArg(glblVal25, &glblVal25_4, 0);

// call count at core: 713
Term Rslt_52Args = makePair(APP, 0, s_49, SUB);
Term Rslt_52 = newTerm(VAR, 0, portLoc(2, Rslt_52Args));
#ifdef STRICT
pushRedex(Rslt_52Args, newRef(glblProto64_515));
#else
swap(termLoc(Rslt_52), makePair(LAZ, 0, Rslt_52Args, newRef(glblProto64_515)));
#endif
// call = at core: 713
Term Rslt_53Args = makePair(APP, 0, Rslt_52, SUB);
Term Rslt_53 = newTerm(VAR, 0, portLoc(2, Rslt_53Args));
Rslt_53Args = makePair(APP, 0, newI60(0), Rslt_53Args);
#ifdef STRICT
pushRedex(Rslt_53Args, newRef(glblProto20_517));
#else
swap(termLoc(Rslt_53), makePair(LAZ, 0, Rslt_53Args, newRef(glblProto20_517)));
#endif
// call Some at core: 714
Term Rslt_54Args = makePair(APP, 0, glblStr402, SUB);
Term Rslt_54 = newTerm(VAR, 0, portLoc(2, Rslt_54Args));
#ifdef STRICT
pushRedex(Rslt_54Args, glblVal25_4);
#else
swap(termLoc(Rslt_54), makePair(LAZ, 0, Rslt_54Args, glblVal25_4));
#endif

// call and at core: 713
Term Rslt_55Args = makePair(APP, 0, Rslt_54, SUB);
Term Rslt_55 = newTerm(VAR, 0, portLoc(2, Rslt_55Args));
Rslt_55Args = makePair(APP, 0, Rslt_53, Rslt_55Args);
#ifdef STRICT
pushRedex(Rslt_55Args, newRef(glblProto28_519));
#else
swap(termLoc(Rslt_55), makePair(LAZ, 0, Rslt_55Args, newRef(glblProto28_519)));
#endif
// link args to body
swap(r_seq_50, Rslt_55);

Term Rslt_55V = get(termLoc(Rslt_55));
apps = take(portLoc(1, Rslt_55V));
lams = take(portLoc(2, Rslt_55V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_55), makePair(LAZ, 0, apps, lams));
pushRedex(args, seq_50);
return;
}
Term glblempty_QM_467 = newRef(glblempty_QM_466);
Term glbl_EQ_461;
void glbl_EQ_460(Term ref, Term args) {
Term apps, lams, V;
Location vLoc;
Term seq_59 = makePair(LAM, 0, SUB, NUL);
Location r_seq_59 = portLoc(2, seq_59);
Term y_58 = newTerm(VAR, 0, portLoc(1, seq_59));
seq_59 = makePair(LAM, 0, SUB, seq_59);
Term x_57 = newTerm(VAR, 0, portLoc(1, seq_59));
// allocate args at core: 716
// call str= at core: 720
Term Rslt_61Args = makePair(APP, 0, y_58, SUB);
Term Rslt_61 = newTerm(VAR, 0, portLoc(2, Rslt_61Args));
Rslt_61Args = makePair(APP, 0, x_57, Rslt_61Args);
#ifdef STRICT
pushRedex(Rslt_61Args, glblFn363);
#else
swap(termLoc(Rslt_61), makePair(LAZ, 0, Rslt_61Args, glblFn363));
#endif

// link args to body
swap(r_seq_59, Rslt_61);

Term Rslt_61V = get(termLoc(Rslt_61));
apps = take(portLoc(1, Rslt_61V));
lams = take(portLoc(2, Rslt_61V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_61), makePair(LAZ, 0, apps, lams));
pushRedex(args, seq_59);
return;
}
Term glbl_EQ_461 = newRef(glbl_EQ_460);
Term glbl_EQ_463;
void glbl_EQ_462(Term ref, Term args) {
Term apps, lams, V;
Location vLoc;
Term seq_65 = makePair(LAM, 0, SUB, NUL);
Location r_seq_65 = portLoc(2, seq_65);
Term y_64 = newTerm(VAR, 0, portLoc(1, seq_65));
seq_65 = makePair(LAM, 0, SUB, seq_65);
Term x_63 = newTerm(VAR, 0, portLoc(1, seq_65));
// allocate args at core: 716
// call str= at core: 720
Term Rslt_67Args = makePair(APP, 0, y_64, SUB);
Term Rslt_67 = newTerm(VAR, 0, portLoc(2, Rslt_67Args));
Rslt_67Args = makePair(APP, 0, x_63, Rslt_67Args);
#ifdef STRICT
pushRedex(Rslt_67Args, glblFn363);
#else
swap(termLoc(Rslt_67), makePair(LAZ, 0, Rslt_67Args, glblFn363));
#endif

// link args to body
swap(r_seq_65, Rslt_67);

Term Rslt_67V = get(termLoc(Rslt_67));
apps = take(portLoc(1, Rslt_67V));
lams = take(portLoc(2, Rslt_67V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_67), makePair(LAZ, 0, apps, lams));
pushRedex(args, seq_65);
return;
}
Term glbl_EQ_463 = newRef(glbl_EQ_462);
Term glbl_LT_457;
void glbl_LT_456(Term ref, Term args) {
Term apps, lams, V;
Location vLoc;
Term seq_71 = makePair(LAM, 0, SUB, NUL);
Location r_seq_71 = portLoc(2, seq_71);
Term y_70 = newTerm(VAR, 0, portLoc(1, seq_71));
seq_71 = makePair(LAM, 0, SUB, seq_71);
Term x_69 = newTerm(VAR, 0, portLoc(1, seq_71));
// allocate args at core: 722
// call str< at core: 726
Term Rslt_73Args = makePair(APP, 0, y_70, SUB);
Term Rslt_73 = newTerm(VAR, 0, portLoc(2, Rslt_73Args));
Rslt_73Args = makePair(APP, 0, x_69, Rslt_73Args);
#ifdef STRICT
pushRedex(Rslt_73Args, glblFn366);
#else
swap(termLoc(Rslt_73), makePair(LAZ, 0, Rslt_73Args, glblFn366));
#endif

// link args to body
swap(r_seq_71, Rslt_73);

Term Rslt_73V = get(termLoc(Rslt_73));
apps = take(portLoc(1, Rslt_73V));
lams = take(portLoc(2, Rslt_73V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_73), makePair(LAZ, 0, apps, lams));
pushRedex(args, seq_71);
return;
}
Term glbl_LT_457 = newRef(glbl_LT_456);
Term glbl_LT_459;
void glbl_LT_458(Term ref, Term args) {
Term apps, lams, V;
Location vLoc;
Term seq_77 = makePair(LAM, 0, SUB, NUL);
Location r_seq_77 = portLoc(2, seq_77);
Term y_76 = newTerm(VAR, 0, portLoc(1, seq_77));
seq_77 = makePair(LAM, 0, SUB, seq_77);
Term x_75 = newTerm(VAR, 0, portLoc(1, seq_77));
// allocate args at core: 722
// call str< at core: 726
Term Rslt_79Args = makePair(APP, 0, y_76, SUB);
Term Rslt_79 = newTerm(VAR, 0, portLoc(2, Rslt_79Args));
Rslt_79Args = makePair(APP, 0, x_75, Rslt_79Args);
#ifdef STRICT
pushRedex(Rslt_79Args, glblFn366);
#else
swap(termLoc(Rslt_79), makePair(LAZ, 0, Rslt_79Args, glblFn366));
#endif

// link args to body
swap(r_seq_77, Rslt_79);

Term Rslt_79V = get(termLoc(Rslt_79));
apps = take(portLoc(1, Rslt_79V));
lams = take(portLoc(2, Rslt_79V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_79), makePair(LAZ, 0, apps, lams));
pushRedex(args, seq_77);
return;
}
Term glbl_LT_459 = newRef(glbl_LT_458);
void glblProto64_526(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(730), args);
  args = makePair(APP, 0, glblStr199, args);
  interact(args, glblProto64);
}
}
void glblProto380_528(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(731), args);
  args = makePair(APP, 0, glblStr199, args);
  interact(args, glblProto380);
}
}
Term glblfirst453;
void glblfirst452(Term ref, Term args) {
Term apps, lams, V;
Location vLoc;
Term seq_82 = makePair(LAM, 0, SUB, NUL);
Location r_seq_82 = portLoc(2, seq_82);
Term s_81 = newTerm(VAR, 0, portLoc(1, seq_82));
// allocate args at core: 729

Term s_81_1;
s_81 = dupeArg(s_81, &s_81_1, 16);
Term glblRslt16_1;
glblRslt16 = dupeArg(glblRslt16, &glblRslt16_1, 0);

Term glblVal25_5;
glblVal25 = dupeArg(glblVal25, &glblVal25_5, 0);

Term glblVal262_2;
glblVal262 = dupeArg(glblVal262, &glblVal262_2, 0);

// call count at core: 730
Term Rslt_84Args = makePair(APP, 0, s_81, SUB);
Term Rslt_84 = newTerm(VAR, 0, portLoc(2, Rslt_84Args));
#ifdef STRICT
pushRedex(Rslt_84Args, newRef(glblProto64_526));
#else
swap(termLoc(Rslt_84), makePair(LAZ, 0, Rslt_84Args, newRef(glblProto64_526)));
#endif
// call subs at core: 731
Term Rslt_85Args = makePair(APP, 0, newI60(1), SUB);
Term Rslt_85 = newTerm(VAR, 0, portLoc(2, Rslt_85Args));
Rslt_85Args = makePair(APP, 0, newI60(0), Rslt_85Args);
Rslt_85Args = makePair(APP, 0, s_81_1, Rslt_85Args);
#ifdef STRICT
pushRedex(Rslt_85Args, newRef(glblProto380_528));
#else
swap(termLoc(Rslt_85), makePair(LAZ, 0, Rslt_85Args, newRef(glblProto380_528)));
#endif
// call Some at core: 731
Term Rslt_86Args = makePair(APP, 0, Rslt_85, SUB);
Term Rslt_86 = newTerm(VAR, 0, portLoc(2, Rslt_86Args));
#ifdef STRICT
pushRedex(Rslt_86Args, glblVal25_5);
#else
swap(termLoc(Rslt_86), makePair(LAZ, 0, Rslt_86Args, glblVal25_5));
#endif

// call int-cond at core: 730
Term Rslt_87Args = makePair(APP, 0, glblRslt16_1, SUB);
Term Rslt_87 = newTerm(VAR, 0, portLoc(2, Rslt_87Args));
Rslt_87Args = makePair(APP, 0, Rslt_86, Rslt_87Args);
Rslt_87Args = makePair(APP, 0, Rslt_84, Rslt_87Args);
#ifdef STRICT
pushRedex(Rslt_87Args, glblVal262_2);
#else
swap(termLoc(Rslt_87), makePair(LAZ, 0, Rslt_87Args, glblVal262_2));
#endif

// link args to body
swap(r_seq_82, Rslt_87);

Term Rslt_87V = get(termLoc(Rslt_87));
apps = take(portLoc(1, Rslt_87V));
lams = take(portLoc(2, Rslt_87V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_87), makePair(LAZ, 0, apps, lams));
pushRedex(args, seq_82);
return;
}
Term glblfirst453 = newRef(glblfirst452);
void glblProto64_531(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(730), args);
  args = makePair(APP, 0, glblStr199, args);
  interact(args, glblProto64);
}
}
void glblProto380_533(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(731), args);
  args = makePair(APP, 0, glblStr199, args);
  interact(args, glblProto380);
}
}
Term glblfirst455;
void glblfirst454(Term ref, Term args) {
Term apps, lams, V;
Location vLoc;
Term seq_90 = makePair(LAM, 0, SUB, NUL);
Location r_seq_90 = portLoc(2, seq_90);
Term s_89 = newTerm(VAR, 0, portLoc(1, seq_90));
// allocate args at core: 729

Term s_89_1;
s_89 = dupeArg(s_89, &s_89_1, 17);
Term glblRslt16_2;
glblRslt16 = dupeArg(glblRslt16, &glblRslt16_2, 0);

Term glblVal25_6;
glblVal25 = dupeArg(glblVal25, &glblVal25_6, 0);

Term glblVal262_3;
glblVal262 = dupeArg(glblVal262, &glblVal262_3, 0);

// call count at core: 730
Term Rslt_92Args = makePair(APP, 0, s_89, SUB);
Term Rslt_92 = newTerm(VAR, 0, portLoc(2, Rslt_92Args));
#ifdef STRICT
pushRedex(Rslt_92Args, newRef(glblProto64_531));
#else
swap(termLoc(Rslt_92), makePair(LAZ, 0, Rslt_92Args, newRef(glblProto64_531)));
#endif
// call subs at core: 731
Term Rslt_93Args = makePair(APP, 0, newI60(1), SUB);
Term Rslt_93 = newTerm(VAR, 0, portLoc(2, Rslt_93Args));
Rslt_93Args = makePair(APP, 0, newI60(0), Rslt_93Args);
Rslt_93Args = makePair(APP, 0, s_89_1, Rslt_93Args);
#ifdef STRICT
pushRedex(Rslt_93Args, newRef(glblProto380_533));
#else
swap(termLoc(Rslt_93), makePair(LAZ, 0, Rslt_93Args, newRef(glblProto380_533)));
#endif
// call Some at core: 731
Term Rslt_94Args = makePair(APP, 0, Rslt_93, SUB);
Term Rslt_94 = newTerm(VAR, 0, portLoc(2, Rslt_94Args));
#ifdef STRICT
pushRedex(Rslt_94Args, glblVal25_6);
#else
swap(termLoc(Rslt_94), makePair(LAZ, 0, Rslt_94Args, glblVal25_6));
#endif

// call int-cond at core: 730
Term Rslt_95Args = makePair(APP, 0, glblRslt16_2, SUB);
Term Rslt_95 = newTerm(VAR, 0, portLoc(2, Rslt_95Args));
Rslt_95Args = makePair(APP, 0, Rslt_94, Rslt_95Args);
Rslt_95Args = makePair(APP, 0, Rslt_92, Rslt_95Args);
#ifdef STRICT
pushRedex(Rslt_95Args, glblVal262_3);
#else
swap(termLoc(Rslt_95), makePair(LAZ, 0, Rslt_95Args, glblVal262_3));
#endif

// link args to body
swap(r_seq_90, Rslt_95);

Term Rslt_95V = get(termLoc(Rslt_95));
apps = take(portLoc(1, Rslt_95V));
lams = take(portLoc(2, Rslt_95V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_95), makePair(LAZ, 0, apps, lams));
pushRedex(args, seq_90);
return;
}
Term glblfirst455 = newRef(glblfirst454);
void glblProto64_536(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(735), args);
  args = makePair(APP, 0, glblStr199, args);
  interact(args, glblProto64);
}
}
void glblProto380_538(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(735), args);
  args = makePair(APP, 0, glblStr199, args);
  interact(args, glblProto380);
}
}
Term glblrest449;
void glblrest448(Term ref, Term args) {
Term apps, lams, V;
Location vLoc;
Term seq_98 = makePair(LAM, 0, SUB, NUL);
Location r_seq_98 = portLoc(2, seq_98);
Term s_97 = newTerm(VAR, 0, portLoc(1, seq_98));
// allocate args at core: 734

Term s_97_1;
s_97 = dupeArg(s_97, &s_97_1, 18);
// call count at core: 735
Term Rslt_100Args = makePair(APP, 0, s_97_1, SUB);
Term Rslt_100 = newTerm(VAR, 0, portLoc(2, Rslt_100Args));
#ifdef STRICT
pushRedex(Rslt_100Args, newRef(glblProto64_536));
#else
swap(termLoc(Rslt_100), makePair(LAZ, 0, Rslt_100Args, newRef(glblProto64_536)));
#endif
// call subs at core: 735
Term Rslt_101Args = makePair(APP, 0, Rslt_100, SUB);
Term Rslt_101 = newTerm(VAR, 0, portLoc(2, Rslt_101Args));
Rslt_101Args = makePair(APP, 0, newI60(1), Rslt_101Args);
Rslt_101Args = makePair(APP, 0, s_97, Rslt_101Args);
#ifdef STRICT
pushRedex(Rslt_101Args, newRef(glblProto380_538));
#else
swap(termLoc(Rslt_101), makePair(LAZ, 0, Rslt_101Args, newRef(glblProto380_538)));
#endif
// link args to body
swap(r_seq_98, Rslt_101);

Term Rslt_101V = get(termLoc(Rslt_101));
apps = take(portLoc(1, Rslt_101V));
lams = take(portLoc(2, Rslt_101V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_101), makePair(LAZ, 0, apps, lams));
pushRedex(args, seq_98);
return;
}
Term glblrest449 = newRef(glblrest448);
void glblProto64_541(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(735), args);
  args = makePair(APP, 0, glblStr199, args);
  interact(args, glblProto64);
}
}
void glblProto380_543(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(735), args);
  args = makePair(APP, 0, glblStr199, args);
  interact(args, glblProto380);
}
}
Term glblrest451;
void glblrest450(Term ref, Term args) {
Term apps, lams, V;
Location vLoc;
Term seq_104 = makePair(LAM, 0, SUB, NUL);
Location r_seq_104 = portLoc(2, seq_104);
Term s_103 = newTerm(VAR, 0, portLoc(1, seq_104));
// allocate args at core: 734

Term s_103_1;
s_103 = dupeArg(s_103, &s_103_1, 19);
// call count at core: 735
Term Rslt_106Args = makePair(APP, 0, s_103_1, SUB);
Term Rslt_106 = newTerm(VAR, 0, portLoc(2, Rslt_106Args));
#ifdef STRICT
pushRedex(Rslt_106Args, newRef(glblProto64_541));
#else
swap(termLoc(Rslt_106), makePair(LAZ, 0, Rslt_106Args, newRef(glblProto64_541)));
#endif
// call subs at core: 735
Term Rslt_107Args = makePair(APP, 0, Rslt_106, SUB);
Term Rslt_107 = newTerm(VAR, 0, portLoc(2, Rslt_107Args));
Rslt_107Args = makePair(APP, 0, newI60(1), Rslt_107Args);
Rslt_107Args = makePair(APP, 0, s_103, Rslt_107Args);
#ifdef STRICT
pushRedex(Rslt_107Args, newRef(glblProto380_543));
#else
swap(termLoc(Rslt_107), makePair(LAZ, 0, Rslt_107Args, newRef(glblProto380_543)));
#endif
// link args to body
swap(r_seq_104, Rslt_107);

Term Rslt_107V = get(termLoc(Rslt_107));
apps = take(portLoc(1, Rslt_107V));
lams = take(portLoc(2, Rslt_107V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_107), makePair(LAZ, 0, apps, lams));
pushRedex(args, seq_104);
return;
}
Term glblrest451 = newRef(glblrest450);
void glblProto64_546(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(738), args);
  args = makePair(APP, 0, glblStr199, args);
  interact(args, glblProto64);
}
}
void glblProto380_548(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(738), args);
  args = makePair(APP, 0, glblStr199, args);
  interact(args, glblProto380);
}
}
Term glblbutlast445;
void glblbutlast444(Term ref, Term args) {
Term apps, lams, V;
Location vLoc;
Term seq_110 = makePair(LAM, 0, SUB, NUL);
Location r_seq_110 = portLoc(2, seq_110);
Term s_109 = newTerm(VAR, 0, portLoc(1, seq_110));
// allocate args at core: 737

Term s_109_1;
s_109 = dupeArg(s_109, &s_109_1, 20);
// call count at core: 738
Term Rslt_112Args = makePair(APP, 0, s_109_1, SUB);
Term Rslt_112 = newTerm(VAR, 0, portLoc(2, Rslt_112Args));
#ifdef STRICT
pushRedex(Rslt_112Args, newRef(glblProto64_546));
#else
swap(termLoc(Rslt_112), makePair(LAZ, 0, Rslt_112Args, newRef(glblProto64_546)));
#endif
// call dec at core: 738
Term Rslt_113Args = makePair(APP, 0, Rslt_112, SUB);
Term Rslt_113 = newTerm(VAR, 0, portLoc(2, Rslt_113Args));
#ifdef STRICT
pushRedex(Rslt_113Args, glblFn147);
#else
swap(termLoc(Rslt_113), makePair(LAZ, 0, Rslt_113Args, glblFn147));
#endif

// call subs at core: 738
Term Rslt_114Args = makePair(APP, 0, Rslt_113, SUB);
Term Rslt_114 = newTerm(VAR, 0, portLoc(2, Rslt_114Args));
Rslt_114Args = makePair(APP, 0, newI60(0), Rslt_114Args);
Rslt_114Args = makePair(APP, 0, s_109, Rslt_114Args);
#ifdef STRICT
pushRedex(Rslt_114Args, newRef(glblProto380_548));
#else
swap(termLoc(Rslt_114), makePair(LAZ, 0, Rslt_114Args, newRef(glblProto380_548)));
#endif
// link args to body
swap(r_seq_110, Rslt_114);

Term Rslt_114V = get(termLoc(Rslt_114));
apps = take(portLoc(1, Rslt_114V));
lams = take(portLoc(2, Rslt_114V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_114), makePair(LAZ, 0, apps, lams));
pushRedex(args, seq_110);
return;
}
Term glblbutlast445 = newRef(glblbutlast444);
void glblProto64_551(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(738), args);
  args = makePair(APP, 0, glblStr199, args);
  interact(args, glblProto64);
}
}
void glblProto380_555(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(738), args);
  args = makePair(APP, 0, glblStr199, args);
  interact(args, glblProto380);
}
}
Term glblbutlast447;
void glblbutlast446(Term ref, Term args) {
Term apps, lams, V;
Location vLoc;
Term seq_117 = makePair(LAM, 0, SUB, NUL);
Location r_seq_117 = portLoc(2, seq_117);
Term s_116 = newTerm(VAR, 0, portLoc(1, seq_117));
// allocate args at core: 737

Term s_116_1;
s_116 = dupeArg(s_116, &s_116_1, 21);
// call count at core: 738
Term Rslt_119Args = makePair(APP, 0, s_116_1, SUB);
Term Rslt_119 = newTerm(VAR, 0, portLoc(2, Rslt_119Args));
#ifdef STRICT
pushRedex(Rslt_119Args, newRef(glblProto64_551));
#else
swap(termLoc(Rslt_119), makePair(LAZ, 0, Rslt_119Args, newRef(glblProto64_551)));
#endif
// call dec at core: 738
Term Rslt_120Args = makePair(APP, 0, Rslt_119, SUB);
Term Rslt_120 = newTerm(VAR, 0, portLoc(2, Rslt_120Args));
#ifdef STRICT
pushRedex(Rslt_120Args, glblFn147);
#else
swap(termLoc(Rslt_120), makePair(LAZ, 0, Rslt_120Args, glblFn147));
#endif

// call subs at core: 738
Term Rslt_121Args = makePair(APP, 0, Rslt_120, SUB);
Term Rslt_121 = newTerm(VAR, 0, portLoc(2, Rslt_121Args));
Rslt_121Args = makePair(APP, 0, newI60(0), Rslt_121Args);
Rslt_121Args = makePair(APP, 0, s_116, Rslt_121Args);
#ifdef STRICT
pushRedex(Rslt_121Args, newRef(glblProto380_555));
#else
swap(termLoc(Rslt_121), makePair(LAZ, 0, Rslt_121Args, newRef(glblProto380_555)));
#endif
// link args to body
swap(r_seq_117, Rslt_121);

Term Rslt_121V = get(termLoc(Rslt_121));
apps = take(portLoc(1, Rslt_121V));
lams = take(portLoc(2, Rslt_121V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_121), makePair(LAZ, 0, apps, lams));
pushRedex(args, seq_117);
return;
}
Term glblbutlast447 = newRef(glblbutlast446);
void glblProto64_560(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(741), args);
  args = makePair(APP, 0, glblStr199, args);
  interact(args, glblProto64);
}
}
void glblProto64_562(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(742), args);
  args = makePair(APP, 0, glblStr199, args);
  interact(args, glblProto64);
}
}
void glblProto380_564(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(742), args);
  args = makePair(APP, 0, glblStr199, args);
  interact(args, glblProto380);
}
}
Term glbllast441;
void glbllast440(Term ref, Term args) {
Term apps, lams, V;
Location vLoc;
Term seq_124 = makePair(LAM, 0, SUB, NUL);
Location r_seq_124 = portLoc(2, seq_124);
Term s_123 = newTerm(VAR, 0, portLoc(1, seq_124));
// allocate args at core: 740

Term s_123_2;
s_123 = dupeArg(s_123, &s_123_2, 22);

Term s_123_1;
s_123 = dupeArg(s_123, &s_123_1, 23);
Term glblRslt16_3;
glblRslt16 = dupeArg(glblRslt16, &glblRslt16_3, 0);

Term glblVal25_7;
glblVal25 = dupeArg(glblVal25, &glblVal25_7, 0);

Term glblVal262_4;
glblVal262 = dupeArg(glblVal262, &glblVal262_4, 0);

// call count at core: 741
Term Rslt_126Args = makePair(APP, 0, s_123, SUB);
Term Rslt_126 = newTerm(VAR, 0, portLoc(2, Rslt_126Args));
#ifdef STRICT
pushRedex(Rslt_126Args, newRef(glblProto64_560));
#else
swap(termLoc(Rslt_126), makePair(LAZ, 0, Rslt_126Args, newRef(glblProto64_560)));
#endif
// call count at core: 742
Term Rslt_127Args = makePair(APP, 0, s_123_2, SUB);
Term Rslt_127 = newTerm(VAR, 0, portLoc(2, Rslt_127Args));
#ifdef STRICT
pushRedex(Rslt_127Args, newRef(glblProto64_562));
#else
swap(termLoc(Rslt_127), makePair(LAZ, 0, Rslt_127Args, newRef(glblProto64_562)));
#endif
// call dec at core: 742
Term Rslt_128Args = makePair(APP, 0, Rslt_127, SUB);
Term Rslt_128 = newTerm(VAR, 0, portLoc(2, Rslt_128Args));
#ifdef STRICT
pushRedex(Rslt_128Args, glblFn147);
#else
swap(termLoc(Rslt_128), makePair(LAZ, 0, Rslt_128Args, glblFn147));
#endif

// call subs at core: 742
Term Rslt_129Args = makePair(APP, 0, newI60(1), SUB);
Term Rslt_129 = newTerm(VAR, 0, portLoc(2, Rslt_129Args));
Rslt_129Args = makePair(APP, 0, Rslt_128, Rslt_129Args);
Rslt_129Args = makePair(APP, 0, s_123_1, Rslt_129Args);
#ifdef STRICT
pushRedex(Rslt_129Args, newRef(glblProto380_564));
#else
swap(termLoc(Rslt_129), makePair(LAZ, 0, Rslt_129Args, newRef(glblProto380_564)));
#endif
// call Some at core: 742
Term Rslt_130Args = makePair(APP, 0, Rslt_129, SUB);
Term Rslt_130 = newTerm(VAR, 0, portLoc(2, Rslt_130Args));
#ifdef STRICT
pushRedex(Rslt_130Args, glblVal25_7);
#else
swap(termLoc(Rslt_130), makePair(LAZ, 0, Rslt_130Args, glblVal25_7));
#endif

// call int-cond at core: 741
Term Rslt_131Args = makePair(APP, 0, glblRslt16_3, SUB);
Term Rslt_131 = newTerm(VAR, 0, portLoc(2, Rslt_131Args));
Rslt_131Args = makePair(APP, 0, Rslt_130, Rslt_131Args);
Rslt_131Args = makePair(APP, 0, Rslt_126, Rslt_131Args);
#ifdef STRICT
pushRedex(Rslt_131Args, glblVal262_4);
#else
swap(termLoc(Rslt_131), makePair(LAZ, 0, Rslt_131Args, glblVal262_4));
#endif

// link args to body
swap(r_seq_124, Rslt_131);

Term Rslt_131V = get(termLoc(Rslt_131));
apps = take(portLoc(1, Rslt_131V));
lams = take(portLoc(2, Rslt_131V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_131), makePair(LAZ, 0, apps, lams));
pushRedex(args, seq_124);
return;
}
Term glbllast441 = newRef(glbllast440);
void glblProto64_567(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(741), args);
  args = makePair(APP, 0, glblStr199, args);
  interact(args, glblProto64);
}
}
void glblProto64_569(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(742), args);
  args = makePair(APP, 0, glblStr199, args);
  interact(args, glblProto64);
}
}
void glblProto380_571(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(742), args);
  args = makePair(APP, 0, glblStr199, args);
  interact(args, glblProto380);
}
}
Term glbllast443;
void glbllast442(Term ref, Term args) {
Term apps, lams, V;
Location vLoc;
Term seq_134 = makePair(LAM, 0, SUB, NUL);
Location r_seq_134 = portLoc(2, seq_134);
Term s_133 = newTerm(VAR, 0, portLoc(1, seq_134));
// allocate args at core: 740

Term s_133_2;
s_133 = dupeArg(s_133, &s_133_2, 24);

Term s_133_1;
s_133 = dupeArg(s_133, &s_133_1, 25);
Term glblRslt16_4;
glblRslt16 = dupeArg(glblRslt16, &glblRslt16_4, 0);

Term glblVal25_8;
glblVal25 = dupeArg(glblVal25, &glblVal25_8, 0);

Term glblVal262_5;
glblVal262 = dupeArg(glblVal262, &glblVal262_5, 0);

// call count at core: 741
Term Rslt_136Args = makePair(APP, 0, s_133, SUB);
Term Rslt_136 = newTerm(VAR, 0, portLoc(2, Rslt_136Args));
#ifdef STRICT
pushRedex(Rslt_136Args, newRef(glblProto64_567));
#else
swap(termLoc(Rslt_136), makePair(LAZ, 0, Rslt_136Args, newRef(glblProto64_567)));
#endif
// call count at core: 742
Term Rslt_137Args = makePair(APP, 0, s_133_2, SUB);
Term Rslt_137 = newTerm(VAR, 0, portLoc(2, Rslt_137Args));
#ifdef STRICT
pushRedex(Rslt_137Args, newRef(glblProto64_569));
#else
swap(termLoc(Rslt_137), makePair(LAZ, 0, Rslt_137Args, newRef(glblProto64_569)));
#endif
// call dec at core: 742
Term Rslt_138Args = makePair(APP, 0, Rslt_137, SUB);
Term Rslt_138 = newTerm(VAR, 0, portLoc(2, Rslt_138Args));
#ifdef STRICT
pushRedex(Rslt_138Args, glblFn147);
#else
swap(termLoc(Rslt_138), makePair(LAZ, 0, Rslt_138Args, glblFn147));
#endif

// call subs at core: 742
Term Rslt_139Args = makePair(APP, 0, newI60(1), SUB);
Term Rslt_139 = newTerm(VAR, 0, portLoc(2, Rslt_139Args));
Rslt_139Args = makePair(APP, 0, Rslt_138, Rslt_139Args);
Rslt_139Args = makePair(APP, 0, s_133_1, Rslt_139Args);
#ifdef STRICT
pushRedex(Rslt_139Args, newRef(glblProto380_571));
#else
swap(termLoc(Rslt_139), makePair(LAZ, 0, Rslt_139Args, newRef(glblProto380_571)));
#endif
// call Some at core: 742
Term Rslt_140Args = makePair(APP, 0, Rslt_139, SUB);
Term Rslt_140 = newTerm(VAR, 0, portLoc(2, Rslt_140Args));
#ifdef STRICT
pushRedex(Rslt_140Args, glblVal25_8);
#else
swap(termLoc(Rslt_140), makePair(LAZ, 0, Rslt_140Args, glblVal25_8));
#endif

// call int-cond at core: 741
Term Rslt_141Args = makePair(APP, 0, glblRslt16_4, SUB);
Term Rslt_141 = newTerm(VAR, 0, portLoc(2, Rslt_141Args));
Rslt_141Args = makePair(APP, 0, Rslt_140, Rslt_141Args);
Rslt_141Args = makePair(APP, 0, Rslt_136, Rslt_141Args);
#ifdef STRICT
pushRedex(Rslt_141Args, glblVal262_5);
#else
swap(termLoc(Rslt_141), makePair(LAZ, 0, Rslt_141Args, glblVal262_5));
#endif

// link args to body
swap(r_seq_134, Rslt_141);

Term Rslt_141V = get(termLoc(Rslt_141));
apps = take(portLoc(1, Rslt_141V));
lams = take(portLoc(2, Rslt_141V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_141), makePair(LAZ, 0, apps, lams));
pushRedex(args, seq_134);
return;
}
Term glbllast443 = newRef(glbllast442);
Term glblvec437;
void glblvec436(Term ref, Term args) {
Term apps, lams, V;
Location vLoc;
Term seq_144 = makePair(LAM, 0, SUB, NUL);
Location r_seq_144 = portLoc(2, seq_144);
Term coll_143 = newTerm(VAR, 0, portLoc(1, seq_144));
// allocate args at core: 745
swap(termLoc(coll_143), ERA);
// call string-vec at core: 746
Term Rslt_146Args = makePair(APP, 0, NUL, SUB);
Term Rslt_146 = newTerm(VAR, 0, portLoc(2, Rslt_146Args));
#ifdef STRICT
pushRedex(Rslt_146Args, glblFn415);
#else
swap(termLoc(Rslt_146), makePair(LAZ, 0, Rslt_146Args, glblFn415));
#endif

// link args to body
swap(r_seq_144, Rslt_146);

Term Rslt_146V = get(termLoc(Rslt_146));
apps = take(portLoc(1, Rslt_146V));
lams = take(portLoc(2, Rslt_146V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_146), makePair(LAZ, 0, apps, lams));
pushRedex(args, seq_144);
return;
}
Term glblvec437 = newRef(glblvec436);
Term glblvec439;
void glblvec438(Term ref, Term args) {
Term apps, lams, V;
Location vLoc;
Term seq_149 = makePair(LAM, 0, SUB, NUL);
Location r_seq_149 = portLoc(2, seq_149);
Term coll_148 = newTerm(VAR, 0, portLoc(1, seq_149));
// allocate args at core: 745
swap(termLoc(coll_148), ERA);
// call string-vec at core: 746
Term Rslt_151Args = makePair(APP, 0, NUL, SUB);
Term Rslt_151 = newTerm(VAR, 0, portLoc(2, Rslt_151Args));
#ifdef STRICT
pushRedex(Rslt_151Args, glblFn415);
#else
swap(termLoc(Rslt_151), makePair(LAZ, 0, Rslt_151Args, glblFn415));
#endif

// link args to body
swap(r_seq_149, Rslt_151);

Term Rslt_151V = get(termLoc(Rslt_151));
apps = take(portLoc(1, Rslt_151V));
lams = take(portLoc(2, Rslt_151V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_151), makePair(LAZ, 0, apps, lams));
pushRedex(args, seq_149);
return;
}
Term glblvec439 = newRef(glblvec438);
void glblProto64_577(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(749), args);
  args = makePair(APP, 0, glblStr199, args);
  interact(args, glblProto64);
}
}
void glblProto26_580(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(749), args);
  args = makePair(APP, 0, glblStr199, args);
  interact(args, glblProto26);
}
}
void glblProto380_582(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(750), args);
  args = makePair(APP, 0, glblStr199, args);
  interact(args, glblProto380);
}
}
void glblProto34_584(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(749), args);
  args = makePair(APP, 0, glblStr199, args);
  interact(args, glblProto34);
}
}
Term glblnth433;
void glblnth432(Term ref, Term args) {
Term apps, lams, V;
Location vLoc;
Term seq_155 = makePair(LAM, 0, SUB, NUL);
Location r_seq_155 = portLoc(2, seq_155);
Term n_154 = newTerm(VAR, 0, portLoc(1, seq_155));
seq_155 = makePair(LAM, 0, SUB, seq_155);
Term s_153 = newTerm(VAR, 0, portLoc(1, seq_155));
// allocate args at core: 748

Term s_153_1;
s_153 = dupeArg(s_153, &s_153_1, 26);

Term n_154_1;
n_154 = dupeArg(n_154, &n_154_1, 27);
Term glblRslt16_5;
glblRslt16 = dupeArg(glblRslt16, &glblRslt16_5, 0);

Term glblVal25_9;
glblVal25 = dupeArg(glblVal25, &glblVal25_9, 0);

// call count at core: 749
Term Rslt_157Args = makePair(APP, 0, s_153, SUB);
Term Rslt_157 = newTerm(VAR, 0, portLoc(2, Rslt_157Args));
#ifdef STRICT
pushRedex(Rslt_157Args, newRef(glblProto64_577));
#else
swap(termLoc(Rslt_157), makePair(LAZ, 0, Rslt_157Args, newRef(glblProto64_577)));
#endif
// call < at core: 749
Term Rslt_158Args = makePair(APP, 0, Rslt_157, SUB);
Term Rslt_158 = newTerm(VAR, 0, portLoc(2, Rslt_158Args));
Rslt_158Args = makePair(APP, 0, n_154, Rslt_158Args);
#ifdef STRICT
pushRedex(Rslt_158Args, newRef(glblProto26_580));
#else
swap(termLoc(Rslt_158), makePair(LAZ, 0, Rslt_158Args, newRef(glblProto26_580)));
#endif
// call subs at core: 750
Term Rslt_159Args = makePair(APP, 0, newI60(1), SUB);
Term Rslt_159 = newTerm(VAR, 0, portLoc(2, Rslt_159Args));
Rslt_159Args = makePair(APP, 0, n_154_1, Rslt_159Args);
Rslt_159Args = makePair(APP, 0, s_153_1, Rslt_159Args);
#ifdef STRICT
pushRedex(Rslt_159Args, newRef(glblProto380_582));
#else
swap(termLoc(Rslt_159), makePair(LAZ, 0, Rslt_159Args, newRef(glblProto380_582)));
#endif
// call Some at core: 750
Term Rslt_160Args = makePair(APP, 0, Rslt_159, SUB);
Term Rslt_160 = newTerm(VAR, 0, portLoc(2, Rslt_160Args));
#ifdef STRICT
pushRedex(Rslt_160Args, glblVal25_9);
#else
swap(termLoc(Rslt_160), makePair(LAZ, 0, Rslt_160Args, glblVal25_9));
#endif

// call cond at core: 749
Term Rslt_161Args = makePair(APP, 0, glblRslt16_5, SUB);
Term Rslt_161 = newTerm(VAR, 0, portLoc(2, Rslt_161Args));
Rslt_161Args = makePair(APP, 0, Rslt_160, Rslt_161Args);
Rslt_161Args = makePair(APP, 0, Rslt_158, Rslt_161Args);
#ifdef STRICT
pushRedex(Rslt_161Args, newRef(glblProto34_584));
#else
swap(termLoc(Rslt_161), makePair(LAZ, 0, Rslt_161Args, newRef(glblProto34_584)));
#endif
// link args to body
swap(r_seq_155, Rslt_161);

Term Rslt_161V = get(termLoc(Rslt_161));
apps = take(portLoc(1, Rslt_161V));
lams = take(portLoc(2, Rslt_161V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_161), makePair(LAZ, 0, apps, lams));
pushRedex(args, seq_155);
return;
}
Term glblnth433 = newRef(glblnth432);
void glblProto64_587(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(749), args);
  args = makePair(APP, 0, glblStr199, args);
  interact(args, glblProto64);
}
}
void glblProto26_589(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(749), args);
  args = makePair(APP, 0, glblStr199, args);
  interact(args, glblProto26);
}
}
void glblProto380_591(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(750), args);
  args = makePair(APP, 0, glblStr199, args);
  interact(args, glblProto380);
}
}
void glblProto34_593(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(749), args);
  args = makePair(APP, 0, glblStr199, args);
  interact(args, glblProto34);
}
}
Term glblnth435;
void glblnth434(Term ref, Term args) {
Term apps, lams, V;
Location vLoc;
Term seq_165 = makePair(LAM, 0, SUB, NUL);
Location r_seq_165 = portLoc(2, seq_165);
Term n_164 = newTerm(VAR, 0, portLoc(1, seq_165));
seq_165 = makePair(LAM, 0, SUB, seq_165);
Term s_163 = newTerm(VAR, 0, portLoc(1, seq_165));
// allocate args at core: 748

Term s_163_1;
s_163 = dupeArg(s_163, &s_163_1, 28);

Term n_164_1;
n_164 = dupeArg(n_164, &n_164_1, 29);
Term glblRslt16_6;
glblRslt16 = dupeArg(glblRslt16, &glblRslt16_6, 0);

Term glblVal25_10;
glblVal25 = dupeArg(glblVal25, &glblVal25_10, 0);

// call count at core: 749
Term Rslt_167Args = makePair(APP, 0, s_163, SUB);
Term Rslt_167 = newTerm(VAR, 0, portLoc(2, Rslt_167Args));
#ifdef STRICT
pushRedex(Rslt_167Args, newRef(glblProto64_587));
#else
swap(termLoc(Rslt_167), makePair(LAZ, 0, Rslt_167Args, newRef(glblProto64_587)));
#endif
// call < at core: 749
Term Rslt_168Args = makePair(APP, 0, Rslt_167, SUB);
Term Rslt_168 = newTerm(VAR, 0, portLoc(2, Rslt_168Args));
Rslt_168Args = makePair(APP, 0, n_164, Rslt_168Args);
#ifdef STRICT
pushRedex(Rslt_168Args, newRef(glblProto26_589));
#else
swap(termLoc(Rslt_168), makePair(LAZ, 0, Rslt_168Args, newRef(glblProto26_589)));
#endif
// call subs at core: 750
Term Rslt_169Args = makePair(APP, 0, newI60(1), SUB);
Term Rslt_169 = newTerm(VAR, 0, portLoc(2, Rslt_169Args));
Rslt_169Args = makePair(APP, 0, n_164_1, Rslt_169Args);
Rslt_169Args = makePair(APP, 0, s_163_1, Rslt_169Args);
#ifdef STRICT
pushRedex(Rslt_169Args, newRef(glblProto380_591));
#else
swap(termLoc(Rslt_169), makePair(LAZ, 0, Rslt_169Args, newRef(glblProto380_591)));
#endif
// call Some at core: 750
Term Rslt_170Args = makePair(APP, 0, Rslt_169, SUB);
Term Rslt_170 = newTerm(VAR, 0, portLoc(2, Rslt_170Args));
#ifdef STRICT
pushRedex(Rslt_170Args, glblVal25_10);
#else
swap(termLoc(Rslt_170), makePair(LAZ, 0, Rslt_170Args, glblVal25_10));
#endif

// call cond at core: 749
Term Rslt_171Args = makePair(APP, 0, glblRslt16_6, SUB);
Term Rslt_171 = newTerm(VAR, 0, portLoc(2, Rslt_171Args));
Rslt_171Args = makePair(APP, 0, Rslt_170, Rslt_171Args);
Rslt_171Args = makePair(APP, 0, Rslt_168, Rslt_171Args);
#ifdef STRICT
pushRedex(Rslt_171Args, newRef(glblProto34_593));
#else
swap(termLoc(Rslt_171), makePair(LAZ, 0, Rslt_171Args, newRef(glblProto34_593)));
#endif
// link args to body
swap(r_seq_165, Rslt_171);

Term Rslt_171V = get(termLoc(Rslt_171));
apps = take(portLoc(1, Rslt_171V));
lams = take(portLoc(2, Rslt_171V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_171), makePair(LAZ, 0, apps, lams));
pushRedex(args, seq_165);
return;
}
Term glblnth435 = newRef(glblnth434);
Term glblsha1429;
void glblsha1428(Term ref, Term args) {
Term apps, lams, V;
Location vLoc;
Term seq_174 = makePair(LAM, 0, SUB, NUL);
Location r_seq_174 = portLoc(2, seq_174);
Term s_173 = newTerm(VAR, 0, portLoc(1, seq_174));
// allocate args at core: 760
// call str-sha1 at core: 761
Term Rslt_176Args = makePair(APP, 0, s_173, SUB);
Term Rslt_176 = newTerm(VAR, 0, portLoc(2, Rslt_176Args));
#ifdef STRICT
pushRedex(Rslt_176Args, glblFn417);
#else
swap(termLoc(Rslt_176), makePair(LAZ, 0, Rslt_176Args, glblFn417));
#endif

// link args to body
swap(r_seq_174, Rslt_176);

Term Rslt_176V = get(termLoc(Rslt_176));
apps = take(portLoc(1, Rslt_176V));
lams = take(portLoc(2, Rslt_176V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_176), makePair(LAZ, 0, apps, lams));
pushRedex(args, seq_174);
return;
}
Term glblsha1429 = newRef(glblsha1428);
Term glblsha1431;
void glblsha1430(Term ref, Term args) {
Term apps, lams, V;
Location vLoc;
Term seq_179 = makePair(LAM, 0, SUB, NUL);
Location r_seq_179 = portLoc(2, seq_179);
Term s_178 = newTerm(VAR, 0, portLoc(1, seq_179));
// allocate args at core: 760
// call str-sha1 at core: 761
Term Rslt_181Args = makePair(APP, 0, s_178, SUB);
Term Rslt_181 = newTerm(VAR, 0, portLoc(2, Rslt_181Args));
#ifdef STRICT
pushRedex(Rslt_181Args, glblFn417);
#else
swap(termLoc(Rslt_181), makePair(LAZ, 0, Rslt_181Args, glblFn417));
#endif

// link args to body
swap(r_seq_179, Rslt_181);

Term Rslt_181V = get(termLoc(Rslt_181));
apps = take(portLoc(1, Rslt_181V));
lams = take(portLoc(2, Rslt_181V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_181), makePair(LAZ, 0, apps, lams));
pushRedex(args, seq_179);
return;
}
Term glblsha1431 = newRef(glblsha1430);
Term glblFn479;
void glblCFn477(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 2, &arityArgs);
  Term result = NUL;
    if (arityArgs.count == 2) {
  Term v_1 = arityArgs.args[0];
  Term n_2 = arityArgs.args[1];

   result = termVal((Term)vectorGet(v_1, n_2));
move(portLoc(2, args), result);
}
return;
}
Term glblFn479 = newRef(glblCFn477);
void glblProto20_599(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(818), args);
  args = makePair(APP, 0, glblStr199, args);
  interact(args, glblProto20);
}
}
void glblProto68_601(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(820), args);
  args = makePair(APP, 0, glblStr199, args);
  interact(args, glblProto68);
}
}
void glblProto34_603(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(818), args);
  args = makePair(APP, 0, glblStr199, args);
  interact(args, glblProto34);
}
}
Term glblFn493;
void glblCFn492(Term ref, Term args) {
Term apps, lams, V;
Location vLoc;
Term seq_4 = makePair(LAM, 0, SUB, NUL);
Location r_seq_4 = portLoc(2, seq_4);
Term x_3 = newTerm(VAR, 0, portLoc(1, seq_4));
seq_4 = makePair(LAM, 0, SUB, seq_4);
Term n_2 = newTerm(VAR, 0, portLoc(1, seq_4));
seq_4 = makePair(LAM, 0, SUB, seq_4);
Term v_1 = newTerm(VAR, 0, portLoc(1, seq_4));
// allocate args at core: 813

Term v_1_1;
v_1 = dupeArg(v_1, &v_1_1, 30);

Term n_2_1;
n_2 = dupeArg(n_2, &n_2_1, 31);

Term x_3_2;
x_3 = dupeArg(x_3, &x_3_2, 32);

Term x_3_1;
x_3 = dupeArg(x_3, &x_3_1, 33);
// call = at core: 818
Term Rslt_6Args = makePair(APP, 0, x_3, SUB);
Term Rslt_6 = newTerm(VAR, 0, portLoc(2, Rslt_6Args));
Rslt_6Args = makePair(APP, 0, n_2, Rslt_6Args);
#ifdef STRICT
pushRedex(Rslt_6Args, newRef(glblProto20_599));
#else
swap(termLoc(Rslt_6), makePair(LAZ, 0, Rslt_6Args, newRef(glblProto20_599)));
#endif
// call conj at core: 820
Term Rslt_7Args = makePair(APP, 0, x_3_1, SUB);
Term Rslt_7 = newTerm(VAR, 0, portLoc(2, Rslt_7Args));
Rslt_7Args = makePair(APP, 0, v_1_1, Rslt_7Args);
#ifdef STRICT
pushRedex(Rslt_7Args, newRef(glblProto68_601));
#else
swap(termLoc(Rslt_7), makePair(LAZ, 0, Rslt_7Args, newRef(glblProto68_601)));
#endif
// call inc at core: 820
Term Rslt_8Args = makePair(APP, 0, x_3_2, SUB);
Term Rslt_8 = newTerm(VAR, 0, portLoc(2, Rslt_8Args));
#ifdef STRICT
pushRedex(Rslt_8Args, glblFn141);
#else
swap(termLoc(Rslt_8), makePair(LAZ, 0, Rslt_8Args, glblFn141));
#endif

// call range* at core: 813
Term Rslt_9Args = makePair(APP, 0, Rslt_8, SUB);
Term Rslt_9 = newTerm(VAR, 0, portLoc(2, Rslt_9Args));
Rslt_9Args = makePair(APP, 0, n_2_1, Rslt_9Args);
Rslt_9Args = makePair(APP, 0, Rslt_7, Rslt_9Args);
#ifdef STRICT
pushRedex(Rslt_9Args, glblFn493);
#else
swap(termLoc(Rslt_9), makePair(LAZ, 0, Rslt_9Args, glblFn493));
#endif

// call cond at core: 818
Term Rslt_10Args = makePair(APP, 0, Rslt_9, SUB);
Term Rslt_10 = newTerm(VAR, 0, portLoc(2, Rslt_10Args));
Rslt_10Args = makePair(APP, 0, v_1, Rslt_10Args);
Rslt_10Args = makePair(APP, 0, Rslt_6, Rslt_10Args);
#ifdef STRICT
pushRedex(Rslt_10Args, newRef(glblProto34_603));
#else
swap(termLoc(Rslt_10), makePair(LAZ, 0, Rslt_10Args, newRef(glblProto34_603)));
#endif
// link args to body
swap(r_seq_4, Rslt_10);

Term Rslt_10V = get(termLoc(Rslt_10));
apps = take(portLoc(1, Rslt_10V));
lams = take(portLoc(2, Rslt_10V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_10), makePair(LAZ, 0, apps, lams));
pushRedex(args, seq_4);
return;
}
Term glblFn493 = newRef(glblCFn492);
Term glblFn495;
void glblCFn494(Term ref, Term args) {
Term apps, lams, V;
Location vLoc;
Term seq_2 = makePair(LAM, 0, SUB, NUL);
Location r_seq_2 = portLoc(2, seq_2);
Term n_1 = newTerm(VAR, 0, portLoc(1, seq_2));
// allocate args at core: 822
Term glblVal175_10;
glblVal175 = dupeArg(glblVal175, &glblVal175_10, 0);

// call range* at core: 825
Term Rslt_4Args = makePair(APP, 0, newI60(0), SUB);
Term Rslt_4 = newTerm(VAR, 0, portLoc(2, Rslt_4Args));
Rslt_4Args = makePair(APP, 0, n_1, Rslt_4Args);
Rslt_4Args = makePair(APP, 0, glblVal175_10, Rslt_4Args);
#ifdef STRICT
pushRedex(Rslt_4Args, glblFn493);
#else
swap(termLoc(Rslt_4), makePair(LAZ, 0, Rslt_4Args, glblFn493));
#endif

// link args to body
swap(r_seq_2, Rslt_4);

Term Rslt_4V = get(termLoc(Rslt_4));
apps = take(portLoc(1, Rslt_4V));
lams = take(portLoc(2, Rslt_4V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_4), makePair(LAZ, 0, apps, lams));
pushRedex(args, seq_2);
return;
}
Term glblFn495 = newRef(glblCFn494);
Term glblProto496;
Term glblvect_reduce559;
void glblvect_reduce557(Term ref, Term args) {
Term apps, lams, V;
Location vLoc;
Term seq_13 = makePair(LAM, 0, SUB, NUL);
Location r_seq_13 = portLoc(2, seq_13);
Term f_12 = newTerm(VAR, 0, portLoc(1, seq_13));
seq_13 = makePair(LAM, 0, SUB, seq_13);
Term result_11 = newTerm(VAR, 0, portLoc(1, seq_13));
seq_13 = makePair(LAM, 0, SUB, seq_13);
Term n_10 = newTerm(VAR, 0, portLoc(1, seq_13));
seq_13 = makePair(LAM, 0, SUB, seq_13);
Term vect_9 = newTerm(VAR, 0, portLoc(1, seq_13));
seq_13 = makePair(LAM, 0, SUB, seq_13);
Term el_8 = newTerm(VAR, 0, portLoc(1, seq_13));
// allocate args at core: 836
swap(termLoc(el_8), ERA);
swap(termLoc(vect_9), ERA);
swap(termLoc(n_10), ERA);
swap(termLoc(f_12), ERA);
// link args to body
swap(r_seq_13, result_11);

V = get(r_seq_13);
apps = makePair(APP, 0, V, SUB);
lams = makePair(LAM, 0, SUB, NUL);
swap(portLoc(2, lams),newTerm(VAR, 0, portLoc(1, lams)));
vLoc = portLoc(2, apps);
swap(vLoc, makePair(LAZ, 0, apps, lams));
swap(r_seq_13, newTerm(VAR, 0, vLoc));
pushRedex(args, seq_13);
return;
}
Term glblvect_reduce559 = newRef(glblvect_reduce557);
void glblProto119_608(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(840), args);
  args = makePair(APP, 0, glblStr199, args);
  interact(args, glblProto119);
}
}
void glblProto18_610(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(840), args);
  args = makePair(APP, 0, glblStr199, args);
  interact(args, glblProto18);
}
}
void glblProto496_612(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(840), args);
  args = makePair(APP, 0, glblStr199, args);
  interact(args, glblProto496);
}
}
Term glblvect_reduce579;
void glblvect_reduce576(Term ref, Term args) {
Term apps, lams, V;
Location vLoc;
Term seq_21 = makePair(LAM, 0, SUB, NUL);
Location r_seq_21 = portLoc(2, seq_21);
Term f_20 = newTerm(VAR, 0, portLoc(1, seq_21));
seq_21 = makePair(LAM, 0, SUB, seq_21);
Term result_19 = newTerm(VAR, 0, portLoc(1, seq_21));
seq_21 = makePair(LAM, 0, SUB, seq_21);
Term n_18 = newTerm(VAR, 0, portLoc(1, seq_21));
seq_21 = makePair(LAM, 0, SUB, seq_21);
Term vect_17 = newTerm(VAR, 0, portLoc(1, seq_21));
seq_21 = makePair(LAM, 0, SUB, seq_21);
Term el_16 = newTerm(VAR, 0, portLoc(1, seq_21));
// allocate args at core: 839

Term vect_17_1;
vect_17 = dupeArg(vect_17, &vect_17_1, 34);

Term n_18_1;
n_18 = dupeArg(n_18, &n_18_1, 35);

Term f_20_1;
f_20 = dupeArg(f_20, &f_20_1, 36);
// call get at core: 840
Term Rslt_23Args = makePair(APP, 0, n_18, SUB);
Term Rslt_23 = newTerm(VAR, 0, portLoc(2, Rslt_23Args));
Rslt_23Args = makePair(APP, 0, vect_17, Rslt_23Args);
#ifdef STRICT
pushRedex(Rslt_23Args, newRef(glblProto119_608));
#else
swap(termLoc(Rslt_23), makePair(LAZ, 0, Rslt_23Args, newRef(glblProto119_608)));
#endif
// call inc at core: 840
Term Rslt_24Args = makePair(APP, 0, n_18_1, SUB);
Term Rslt_24 = newTerm(VAR, 0, portLoc(2, Rslt_24Args));
#ifdef STRICT
pushRedex(Rslt_24Args, glblFn141);
#else
swap(termLoc(Rslt_24), makePair(LAZ, 0, Rslt_24Args, glblFn141));
#endif

// call .x at core: 840
Term Rslt_25Args = makePair(APP, 0, el_16, SUB);
Term Rslt_25 = newTerm(VAR, 0, portLoc(2, Rslt_25Args));
#ifdef STRICT
pushRedex(Rslt_25Args, newRef(glblProto18_610));
#else
swap(termLoc(Rslt_25), makePair(LAZ, 0, Rslt_25Args, newRef(glblProto18_610)));
#endif
// call f at core: 839
Term Rslt_26Args = makePair(APP, 0, Rslt_25, SUB);
Term Rslt_26 = newTerm(VAR, 0, portLoc(2, Rslt_26Args));
Rslt_26Args = makePair(APP, 0, result_19, Rslt_26Args);
#ifdef STRICT
pushRedex(Rslt_26Args, f_20);
#else
swap(termLoc(Rslt_26), makePair(LAZ, 0, Rslt_26Args, f_20));
#endif

// call vect-reduce at core: 840
Term Rslt_27Args = makePair(APP, 0, f_20_1, SUB);
Term Rslt_27 = newTerm(VAR, 0, portLoc(2, Rslt_27Args));
Rslt_27Args = makePair(APP, 0, Rslt_26, Rslt_27Args);
Rslt_27Args = makePair(APP, 0, Rslt_24, Rslt_27Args);
Rslt_27Args = makePair(APP, 0, vect_17_1, Rslt_27Args);
Rslt_27Args = makePair(APP, 0, Rslt_23, Rslt_27Args);
#ifdef STRICT
pushRedex(Rslt_27Args, newRef(glblProto496_612));
#else
swap(termLoc(Rslt_27), makePair(LAZ, 0, Rslt_27Args, newRef(glblProto496_612)));
#endif
// link args to body
swap(r_seq_21, Rslt_27);

Term Rslt_27V = get(termLoc(Rslt_27));
apps = take(portLoc(1, Rslt_27V));
lams = take(portLoc(2, Rslt_27V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_27), makePair(LAZ, 0, apps, lams));
pushRedex(args, seq_21);
return;
}
Term glblvect_reduce579 = newRef(glblvect_reduce576);
Term glblProto498;
Term glblvect_reverse616;
void glblvect_reverse614(Term ref, Term args) {
Term apps, lams, V;
Location vLoc;
Term seq_11 = makePair(LAM, 0, SUB, NUL);
Location r_seq_11 = portLoc(2, seq_11);
Term result_10 = newTerm(VAR, 0, portLoc(1, seq_11));
seq_11 = makePair(LAM, 0, SUB, seq_11);
Term n_9 = newTerm(VAR, 0, portLoc(1, seq_11));
seq_11 = makePair(LAM, 0, SUB, seq_11);
Term vect_8 = newTerm(VAR, 0, portLoc(1, seq_11));
seq_11 = makePair(LAM, 0, SUB, seq_11);
Term el_7 = newTerm(VAR, 0, portLoc(1, seq_11));
// allocate args at core: 850
swap(termLoc(el_7), ERA);
swap(termLoc(vect_8), ERA);
swap(termLoc(n_9), ERA);
// link args to body
swap(r_seq_11, result_10);

V = get(r_seq_11);
apps = makePair(APP, 0, V, SUB);
lams = makePair(LAM, 0, SUB, NUL);
swap(portLoc(2, lams),newTerm(VAR, 0, portLoc(1, lams)));
vLoc = portLoc(2, apps);
swap(vLoc, makePair(LAZ, 0, apps, lams));
swap(r_seq_11, newTerm(VAR, 0, vLoc));
pushRedex(args, seq_11);
return;
}
Term glblvect_reverse616 = newRef(glblvect_reverse614);
void glblProto119_620(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(854), args);
  args = makePair(APP, 0, glblStr199, args);
  interact(args, glblProto119);
}
}
void glblProto28_622(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(854), args);
  args = makePair(APP, 0, glblStr199, args);
  interact(args, glblProto28);
}
}
void glblProto18_624(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(855), args);
  args = makePair(APP, 0, glblStr199, args);
  interact(args, glblProto18);
}
}
void glblProto68_626(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(855), args);
  args = makePair(APP, 0, glblStr199, args);
  interact(args, glblProto68);
}
}
void glblProto498_628(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(854), args);
  args = makePair(APP, 0, glblStr199, args);
  interact(args, glblProto498);
}
}
Term glblvect_reverse619;
void glblvect_reverse617(Term ref, Term args) {
Term apps, lams, V;
Location vLoc;
Term seq_18 = makePair(LAM, 0, SUB, NUL);
Location r_seq_18 = portLoc(2, seq_18);
Term result_17 = newTerm(VAR, 0, portLoc(1, seq_18));
seq_18 = makePair(LAM, 0, SUB, seq_18);
Term n_16 = newTerm(VAR, 0, portLoc(1, seq_18));
seq_18 = makePair(LAM, 0, SUB, seq_18);
Term vect_15 = newTerm(VAR, 0, portLoc(1, seq_18));
seq_18 = makePair(LAM, 0, SUB, seq_18);
Term el_14 = newTerm(VAR, 0, portLoc(1, seq_18));
// allocate args at core: 853

Term vect_15_1;
vect_15 = dupeArg(vect_15, &vect_15_1, 37);

Term n_16_2;
n_16 = dupeArg(n_16, &n_16_2, 38);

Term n_16_1;
n_16 = dupeArg(n_16, &n_16_1, 39);
// call <= at core: 854
Term Rslt_20Args = makePair(APP, 0, n_16, SUB);
Term Rslt_20 = newTerm(VAR, 0, portLoc(2, Rslt_20Args));
Rslt_20Args = makePair(APP, 0, newI60(0), Rslt_20Args);
#ifdef STRICT
pushRedex(Rslt_20Args, glblFn261);
#else
swap(termLoc(Rslt_20), makePair(LAZ, 0, Rslt_20Args, glblFn261));
#endif

// call get at core: 854
Term Rslt_21Args = makePair(APP, 0, n_16_1, SUB);
Term Rslt_21 = newTerm(VAR, 0, portLoc(2, Rslt_21Args));
Rslt_21Args = makePair(APP, 0, vect_15, Rslt_21Args);
#ifdef STRICT
pushRedex(Rslt_21Args, newRef(glblProto119_620));
#else
swap(termLoc(Rslt_21), makePair(LAZ, 0, Rslt_21Args, newRef(glblProto119_620)));
#endif
// call and at core: 854
Term Rslt_22Args = makePair(APP, 0, Rslt_21, SUB);
Term Rslt_22 = newTerm(VAR, 0, portLoc(2, Rslt_22Args));
Rslt_22Args = makePair(APP, 0, Rslt_20, Rslt_22Args);
#ifdef STRICT
pushRedex(Rslt_22Args, newRef(glblProto28_622));
#else
swap(termLoc(Rslt_22), makePair(LAZ, 0, Rslt_22Args, newRef(glblProto28_622)));
#endif
// call dec at core: 855
Term Rslt_23Args = makePair(APP, 0, n_16_2, SUB);
Term Rslt_23 = newTerm(VAR, 0, portLoc(2, Rslt_23Args));
#ifdef STRICT
pushRedex(Rslt_23Args, glblFn147);
#else
swap(termLoc(Rslt_23), makePair(LAZ, 0, Rslt_23Args, glblFn147));
#endif

// call .x at core: 855
Term Rslt_24Args = makePair(APP, 0, el_14, SUB);
Term Rslt_24 = newTerm(VAR, 0, portLoc(2, Rslt_24Args));
#ifdef STRICT
pushRedex(Rslt_24Args, newRef(glblProto18_624));
#else
swap(termLoc(Rslt_24), makePair(LAZ, 0, Rslt_24Args, newRef(glblProto18_624)));
#endif
// call conj at core: 855
Term Rslt_25Args = makePair(APP, 0, Rslt_24, SUB);
Term Rslt_25 = newTerm(VAR, 0, portLoc(2, Rslt_25Args));
Rslt_25Args = makePair(APP, 0, result_17, Rslt_25Args);
#ifdef STRICT
pushRedex(Rslt_25Args, newRef(glblProto68_626));
#else
swap(termLoc(Rslt_25), makePair(LAZ, 0, Rslt_25Args, newRef(glblProto68_626)));
#endif
// call vect-reverse at core: 854
Term Rslt_26Args = makePair(APP, 0, Rslt_25, SUB);
Term Rslt_26 = newTerm(VAR, 0, portLoc(2, Rslt_26Args));
Rslt_26Args = makePair(APP, 0, Rslt_23, Rslt_26Args);
Rslt_26Args = makePair(APP, 0, vect_15_1, Rslt_26Args);
Rslt_26Args = makePair(APP, 0, Rslt_22, Rslt_26Args);
#ifdef STRICT
pushRedex(Rslt_26Args, newRef(glblProto498_628));
#else
swap(termLoc(Rslt_26), makePair(LAZ, 0, Rslt_26Args, newRef(glblProto498_628)));
#endif
// link args to body
swap(r_seq_18, Rslt_26);

Term Rslt_26V = get(termLoc(Rslt_26));
apps = take(portLoc(1, Rslt_26V));
lams = take(portLoc(2, Rslt_26V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_26), makePair(LAZ, 0, apps, lams));
pushRedex(args, seq_18);
return;
}
Term glblvect_reverse619 = newRef(glblvect_reverse617);
Term glblProto552;
Term glblvect_sub631;
void glblvect_sub630(Term ref, Term args) {
Term apps, lams, V;
Location vLoc;
Term seq_13 = makePair(LAM, 0, SUB, NUL);
Location r_seq_13 = portLoc(2, seq_13);
Term result_12 = newTerm(VAR, 0, portLoc(1, seq_13));
seq_13 = makePair(LAM, 0, SUB, seq_13);
Term max_n_11 = newTerm(VAR, 0, portLoc(1, seq_13));
seq_13 = makePair(LAM, 0, SUB, seq_13);
Term n_10 = newTerm(VAR, 0, portLoc(1, seq_13));
seq_13 = makePair(LAM, 0, SUB, seq_13);
Term vect_9 = newTerm(VAR, 0, portLoc(1, seq_13));
seq_13 = makePair(LAM, 0, SUB, seq_13);
Term el_8 = newTerm(VAR, 0, portLoc(1, seq_13));
// allocate args at core: 864
swap(termLoc(el_8), ERA);
swap(termLoc(vect_9), ERA);
swap(termLoc(n_10), ERA);
swap(termLoc(max_n_11), ERA);
// link args to body
swap(r_seq_13, result_12);

V = get(r_seq_13);
apps = makePair(APP, 0, V, SUB);
lams = makePair(LAM, 0, SUB, NUL);
swap(portLoc(2, lams),newTerm(VAR, 0, portLoc(1, lams)));
vLoc = portLoc(2, apps);
swap(vLoc, makePair(LAZ, 0, apps, lams));
swap(r_seq_13, newTerm(VAR, 0, vLoc));
pushRedex(args, seq_13);
return;
}
Term glblvect_sub631 = newRef(glblvect_sub630);
void glblProto26_636(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(868), args);
  args = makePair(APP, 0, glblStr199, args);
  interact(args, glblProto26);
}
}
void glblProto119_638(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(868), args);
  args = makePair(APP, 0, glblStr199, args);
  interact(args, glblProto119);
}
}
void glblProto28_640(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(868), args);
  args = makePair(APP, 0, glblStr199, args);
  interact(args, glblProto28);
}
}
void glblProto18_642(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(869), args);
  args = makePair(APP, 0, glblStr199, args);
  interact(args, glblProto18);
}
}
void glblProto68_644(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(869), args);
  args = makePair(APP, 0, glblStr199, args);
  interact(args, glblProto68);
}
}
void glblProto552_646(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(868), args);
  args = makePair(APP, 0, glblStr199, args);
  interact(args, glblProto552);
}
}
Term glblvect_sub635;
void glblvect_sub633(Term ref, Term args) {
Term apps, lams, V;
Location vLoc;
Term seq_21 = makePair(LAM, 0, SUB, NUL);
Location r_seq_21 = portLoc(2, seq_21);
Term result_20 = newTerm(VAR, 0, portLoc(1, seq_21));
seq_21 = makePair(LAM, 0, SUB, seq_21);
Term max_n_19 = newTerm(VAR, 0, portLoc(1, seq_21));
seq_21 = makePair(LAM, 0, SUB, seq_21);
Term n_18 = newTerm(VAR, 0, portLoc(1, seq_21));
seq_21 = makePair(LAM, 0, SUB, seq_21);
Term vect_17 = newTerm(VAR, 0, portLoc(1, seq_21));
seq_21 = makePair(LAM, 0, SUB, seq_21);
Term el_16 = newTerm(VAR, 0, portLoc(1, seq_21));
// allocate args at core: 867

Term vect_17_1;
vect_17 = dupeArg(vect_17, &vect_17_1, 40);

Term n_18_2;
n_18 = dupeArg(n_18, &n_18_2, 41);

Term n_18_1;
n_18 = dupeArg(n_18, &n_18_1, 42);

Term max_n_19_1;
max_n_19 = dupeArg(max_n_19, &max_n_19_1, 43);
// call < at core: 868
Term Rslt_23Args = makePair(APP, 0, max_n_19, SUB);
Term Rslt_23 = newTerm(VAR, 0, portLoc(2, Rslt_23Args));
Rslt_23Args = makePair(APP, 0, n_18, Rslt_23Args);
#ifdef STRICT
pushRedex(Rslt_23Args, newRef(glblProto26_636));
#else
swap(termLoc(Rslt_23), makePair(LAZ, 0, Rslt_23Args, newRef(glblProto26_636)));
#endif
// call get at core: 868
Term Rslt_24Args = makePair(APP, 0, n_18_1, SUB);
Term Rslt_24 = newTerm(VAR, 0, portLoc(2, Rslt_24Args));
Rslt_24Args = makePair(APP, 0, vect_17, Rslt_24Args);
#ifdef STRICT
pushRedex(Rslt_24Args, newRef(glblProto119_638));
#else
swap(termLoc(Rslt_24), makePair(LAZ, 0, Rslt_24Args, newRef(glblProto119_638)));
#endif
// call and at core: 868
Term Rslt_25Args = makePair(APP, 0, Rslt_24, SUB);
Term Rslt_25 = newTerm(VAR, 0, portLoc(2, Rslt_25Args));
Rslt_25Args = makePair(APP, 0, Rslt_23, Rslt_25Args);
#ifdef STRICT
pushRedex(Rslt_25Args, newRef(glblProto28_640));
#else
swap(termLoc(Rslt_25), makePair(LAZ, 0, Rslt_25Args, newRef(glblProto28_640)));
#endif
// call inc at core: 869
Term Rslt_26Args = makePair(APP, 0, n_18_2, SUB);
Term Rslt_26 = newTerm(VAR, 0, portLoc(2, Rslt_26Args));
#ifdef STRICT
pushRedex(Rslt_26Args, glblFn141);
#else
swap(termLoc(Rslt_26), makePair(LAZ, 0, Rslt_26Args, glblFn141));
#endif

// call .x at core: 869
Term Rslt_27Args = makePair(APP, 0, el_16, SUB);
Term Rslt_27 = newTerm(VAR, 0, portLoc(2, Rslt_27Args));
#ifdef STRICT
pushRedex(Rslt_27Args, newRef(glblProto18_642));
#else
swap(termLoc(Rslt_27), makePair(LAZ, 0, Rslt_27Args, newRef(glblProto18_642)));
#endif
// call conj at core: 869
Term Rslt_28Args = makePair(APP, 0, Rslt_27, SUB);
Term Rslt_28 = newTerm(VAR, 0, portLoc(2, Rslt_28Args));
Rslt_28Args = makePair(APP, 0, result_20, Rslt_28Args);
#ifdef STRICT
pushRedex(Rslt_28Args, newRef(glblProto68_644));
#else
swap(termLoc(Rslt_28), makePair(LAZ, 0, Rslt_28Args, newRef(glblProto68_644)));
#endif
// call vect-sub at core: 868
Term Rslt_29Args = makePair(APP, 0, Rslt_28, SUB);
Term Rslt_29 = newTerm(VAR, 0, portLoc(2, Rslt_29Args));
Rslt_29Args = makePair(APP, 0, max_n_19_1, Rslt_29Args);
Rslt_29Args = makePair(APP, 0, Rslt_26, Rslt_29Args);
Rslt_29Args = makePair(APP, 0, vect_17_1, Rslt_29Args);
Rslt_29Args = makePair(APP, 0, Rslt_25, Rslt_29Args);
#ifdef STRICT
pushRedex(Rslt_29Args, newRef(glblProto552_646));
#else
swap(termLoc(Rslt_29), makePair(LAZ, 0, Rslt_29Args, newRef(glblProto552_646)));
#endif
// link args to body
swap(r_seq_21, Rslt_29);

Term Rslt_29V = get(termLoc(Rslt_29));
apps = take(portLoc(1, Rslt_29V));
lams = take(portLoc(2, Rslt_29V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_29), makePair(LAZ, 0, apps, lams));
pushRedex(args, seq_21);
return;
}
Term glblvect_sub635 = newRef(glblvect_sub633);
void glblProto552_652(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(874), args);
  args = makePair(APP, 0, glblStr199, args);
  interact(args, glblProto552);
}
}
Term glblFn650;
void glblCFn649(Term ref, Term args) {
Term apps, lams, V;
Location vLoc;
Term seq_4 = makePair(LAM, 0, SUB, NUL);
Location r_seq_4 = portLoc(2, seq_4);
Term len_3 = newTerm(VAR, 0, portLoc(1, seq_4));
seq_4 = makePair(LAM, 0, SUB, seq_4);
Term start_2 = newTerm(VAR, 0, portLoc(1, seq_4));
seq_4 = makePair(LAM, 0, SUB, seq_4);
Term v_1 = newTerm(VAR, 0, portLoc(1, seq_4));
// allocate args at core: 871

Term v_1_1;
v_1 = dupeArg(v_1, &v_1_1, 44);

Term start_2_2;
start_2 = dupeArg(start_2, &start_2_2, 45);

Term start_2_1;
start_2 = dupeArg(start_2, &start_2_1, 46);
Term glblVal175_11;
glblVal175 = dupeArg(glblVal175, &glblVal175_11, 0);

// call vect-get at core: 874
Term Rslt_6Args = makePair(APP, 0, start_2, SUB);
Term Rslt_6 = newTerm(VAR, 0, portLoc(2, Rslt_6Args));
Rslt_6Args = makePair(APP, 0, v_1, Rslt_6Args);
#ifdef STRICT
pushRedex(Rslt_6Args, glblFn479);
#else
swap(termLoc(Rslt_6), makePair(LAZ, 0, Rslt_6Args, glblFn479));
#endif

// call inc at core: 874
Term Rslt_7Args = makePair(APP, 0, start_2_1, SUB);
Term Rslt_7 = newTerm(VAR, 0, portLoc(2, Rslt_7Args));
#ifdef STRICT
pushRedex(Rslt_7Args, glblFn141);
#else
swap(termLoc(Rslt_7), makePair(LAZ, 0, Rslt_7Args, glblFn141));
#endif

Term Rslt_8 = makeOp(OP_ADD, start_2_2, len_3);
// call vect-sub at core: 874
Term Rslt_9Args = makePair(APP, 0, glblVal175_11, SUB);
Term Rslt_9 = newTerm(VAR, 0, portLoc(2, Rslt_9Args));
Rslt_9Args = makePair(APP, 0, Rslt_8, Rslt_9Args);
Rslt_9Args = makePair(APP, 0, Rslt_7, Rslt_9Args);
Rslt_9Args = makePair(APP, 0, v_1_1, Rslt_9Args);
Rslt_9Args = makePair(APP, 0, Rslt_6, Rslt_9Args);
#ifdef STRICT
pushRedex(Rslt_9Args, newRef(glblProto552_652));
#else
swap(termLoc(Rslt_9), makePair(LAZ, 0, Rslt_9Args, newRef(glblProto552_652)));
#endif
// link args to body
swap(r_seq_4, Rslt_9);

Term Rslt_9V = get(termLoc(Rslt_9));
apps = take(portLoc(1, Rslt_9V));
lams = take(portLoc(2, Rslt_9V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_9), makePair(LAZ, 0, apps, lams));
pushRedex(args, seq_4);
return;
}
Term glblFn650 = newRef(glblCFn649);
Term glblVal651 = newRef(vectMap);
Term glblVal651;
void glblProto76_659(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(884), args);
  args = makePair(APP, 0, glblStr199, args);
  interact(args, glblProto76);
}
}
void glblProto496_661(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(884), args);
  args = makePair(APP, 0, glblStr199, args);
  interact(args, glblProto496);
}
}
void glblProto76_663(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(881), args);
  args = makePair(APP, 0, glblStr199, args);
  interact(args, glblProto76);
}
}
void glblProto64_665(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(883), args);
  args = makePair(APP, 0, glblStr199, args);
  interact(args, glblProto64);
}
}
Term glblc658;
void glblc657(Term ref, Term args) {
Term apps, lams, V;
Location vLoc;
Term seq_19 = makePair(LAM, 0, SUB, NUL);
Location r_seq_19 = portLoc(2, seq_19);
Term s_18 = newTerm(VAR, 0, portLoc(1, seq_19));
seq_19 = makePair(LAM, 0, SUB, seq_19);
Term len_17 = newTerm(VAR, 0, portLoc(1, seq_19));
// allocate args at core: 882
// call count at core: 883
Term Rslt_21Args = makePair(APP, 0, s_18, SUB);
Term Rslt_21 = newTerm(VAR, 0, portLoc(2, Rslt_21Args));
#ifdef STRICT
pushRedex(Rslt_21Args, newRef(glblProto64_665));
#else
swap(termLoc(Rslt_21), makePair(LAZ, 0, Rslt_21Args, newRef(glblProto64_665)));
#endif
Term Rslt_22 = makeOp(OP_ADD, len_17, Rslt_21);
// link args to body
swap(r_seq_19, Rslt_22);

Term Rslt_22V = get(termLoc(Rslt_22));
apps = take(portLoc(1, Rslt_22V));
lams = take(portLoc(2, Rslt_22V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_22), makePair(LAZ, 0, apps, lams));
pushRedex(args, seq_19);
return;
}
Term glblc658 = newRef(glblc657);
void glblProto496_668(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(881), args);
  args = makePair(APP, 0, glblStr199, args);
  interact(args, glblProto496);
}
}
void glblProto46_670(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(880), args);
  args = makePair(APP, 0, glblStr199, args);
  interact(args, glblProto46);
}
}
void glblProto127_672(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(880), args);
  args = makePair(APP, 0, glblStr199, args);
  interact(args, glblProto127);
}
}
Term glblFn656;
void glblCFn655(Term ref, Term args) {
Term apps, lams, V;
Location vLoc;
Term seq_2 = makePair(LAM, 0, SUB, NUL);
Location r_seq_2 = portLoc(2, seq_2);
Term vect_1 = newTerm(VAR, 0, portLoc(1, seq_2));
// allocate args at core: 878
Term seq_6 = makePair(LAM, 0, SUB, NUL);
Location r_seq_6 = portLoc(2, seq_6);
Term ss_vect_5 = newTerm(VAR, 0, portLoc(1, seq_6));
// allocate args at core: 880

Term ss_vect_5_3;
ss_vect_5 = dupeArg(ss_vect_5, &ss_vect_5_3, 0);

Term ss_vect_5_2;
ss_vect_5 = dupeArg(ss_vect_5, &ss_vect_5_2, 0);

Term ss_vect_5_1;
ss_vect_5 = dupeArg(ss_vect_5, &ss_vect_5_1, 0);
Term seq_10 = makePair(LAM, 0, SUB, NUL);
Location r_seq_10 = portLoc(2, seq_10);
Term new_len_9 = newTerm(VAR, 0, portLoc(1, seq_10));
// allocate args at core: 881
// call first at core: 884
Term Rslt_12Args = makePair(APP, 0, ss_vect_5, SUB);
Term Rslt_12 = newTerm(VAR, 0, portLoc(2, Rslt_12Args));
#ifdef STRICT
pushRedex(Rslt_12Args, newRef(glblProto76_659));
#else
swap(termLoc(Rslt_12), makePair(LAZ, 0, Rslt_12Args, newRef(glblProto76_659)));
#endif
// call str-malloc at core: 884
Term Rslt_13Args = makePair(APP, 0, new_len_9, SUB);
Term Rslt_13 = newTerm(VAR, 0, portLoc(2, Rslt_13Args));
#ifdef STRICT
pushRedex(Rslt_13Args, glblFn357);
#else
swap(termLoc(Rslt_13), makePair(LAZ, 0, Rslt_13Args, glblFn357));
#endif

// call vect-reduce at core: 884
Term Rslt_14Args = makePair(APP, 0, glblFn360, SUB);
Term Rslt_14 = newTerm(VAR, 0, portLoc(2, Rslt_14Args));
Rslt_14Args = makePair(APP, 0, Rslt_13, Rslt_14Args);
Rslt_14Args = makePair(APP, 0, newI60(1), Rslt_14Args);
Rslt_14Args = makePair(APP, 0, ss_vect_5_1, Rslt_14Args);
Rslt_14Args = makePair(APP, 0, Rslt_12, Rslt_14Args);
#ifdef STRICT
pushRedex(Rslt_14Args, newRef(glblProto496_661));
#else
swap(termLoc(Rslt_14), makePair(LAZ, 0, Rslt_14Args, newRef(glblProto496_661)));
#endif
// link args to body
swap(r_seq_10, Rslt_14);

Term Rslt_14V = get(termLoc(Rslt_14));
apps = take(portLoc(1, Rslt_14V));
lams = take(portLoc(2, Rslt_14V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_14), makePair(LAZ, 0, apps, lams));
// call first at core: 881
Term Rslt_15Args = makePair(APP, 0, ss_vect_5_2, SUB);
Term Rslt_15 = newTerm(VAR, 0, portLoc(2, Rslt_15Args));
#ifdef STRICT
pushRedex(Rslt_15Args, newRef(glblProto76_663));
#else
swap(termLoc(Rslt_15), makePair(LAZ, 0, Rslt_15Args, newRef(glblProto76_663)));
#endif
// call vect-reduce at core: 881
Term Rslt_23Args = makePair(APP, 0, glblc658, SUB);
Term Rslt_23 = newTerm(VAR, 0, portLoc(2, Rslt_23Args));
Rslt_23Args = makePair(APP, 0, newI60(0), Rslt_23Args);
Rslt_23Args = makePair(APP, 0, newI60(1), Rslt_23Args);
Rslt_23Args = makePair(APP, 0, ss_vect_5_3, Rslt_23Args);
Rslt_23Args = makePair(APP, 0, Rslt_15, Rslt_23Args);
#ifdef STRICT
pushRedex(Rslt_23Args, newRef(glblProto496_668));
#else
swap(termLoc(Rslt_23), makePair(LAZ, 0, Rslt_23Args, newRef(glblProto496_668)));
#endif
// call  at core: 881
Term Rslt_24Args = makePair(APP, 0, Rslt_23, SUB);
Term Rslt_24 = newTerm(VAR, 0, portLoc(2, Rslt_24Args));
#ifdef STRICT
pushRedex(Rslt_24Args, seq_10);
#else
swap(termLoc(Rslt_24), makePair(LAZ, 0, Rslt_24Args, seq_10));
#endif

// link args to body
swap(r_seq_6, Rslt_24);

Term Rslt_24V = get(termLoc(Rslt_24));
apps = take(portLoc(1, Rslt_24V));
lams = take(portLoc(2, Rslt_24V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_24), makePair(LAZ, 0, apps, lams));
// call flat-map at core: 880
Term Rslt_25Args = makePair(APP, 0, newRef(glblProto127_672), SUB);
Term Rslt_25 = newTerm(VAR, 0, portLoc(2, Rslt_25Args));
Rslt_25Args = makePair(APP, 0, vect_1, Rslt_25Args);
#ifdef STRICT
pushRedex(Rslt_25Args, newRef(glblProto46_670));
#else
swap(termLoc(Rslt_25), makePair(LAZ, 0, Rslt_25Args, newRef(glblProto46_670)));
#endif
// call  at core: 880
Term Rslt_26Args = makePair(APP, 0, Rslt_25, SUB);
Term Rslt_26 = newTerm(VAR, 0, portLoc(2, Rslt_26Args));
#ifdef STRICT
pushRedex(Rslt_26Args, seq_6);
#else
swap(termLoc(Rslt_26), makePair(LAZ, 0, Rslt_26Args, seq_6));
#endif

// link args to body
swap(r_seq_2, Rslt_26);

Term Rslt_26V = get(termLoc(Rslt_26));
apps = take(portLoc(1, Rslt_26V));
lams = take(portLoc(2, Rslt_26V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_26), makePair(LAZ, 0, apps, lams));
pushRedex(args, seq_2);
return;
}
Term glblFn656 = newRef(glblCFn655);
void glblProto26_679(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(899), args);
  args = makePair(APP, 0, glblStr199, args);
  interact(args, glblProto26);
}
}
void glblProto98_681(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(901), args);
  args = makePair(APP, 0, glblStr199, args);
  interact(args, glblProto98);
}
}
void glblProto18_683(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(901), args);
  args = makePair(APP, 0, glblStr199, args);
  interact(args, glblProto18);
}
}
void glblProto98_685(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(901), args);
  args = makePair(APP, 0, glblStr199, args);
  interact(args, glblProto98);
}
}
void glblProto18_687(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(901), args);
  args = makePair(APP, 0, glblStr199, args);
  interact(args, glblProto18);
}
}
void glblProto20_689(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(901), args);
  args = makePair(APP, 0, glblStr199, args);
  interact(args, glblProto20);
}
}
void glblProto28_691(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(901), args);
  args = makePair(APP, 0, glblStr199, args);
  interact(args, glblProto28);
}
}
void glblProto34_693(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(899), args);
  args = makePair(APP, 0, glblStr199, args);
  interact(args, glblProto34);
}
}
Term glblFn676;
void glblCFn675(Term ref, Term args) {
Term apps, lams, V;
Location vLoc;
Term seq_4 = makePair(LAM, 0, SUB, NUL);
Location r_seq_4 = portLoc(2, seq_4);
Term start_3 = newTerm(VAR, 0, portLoc(1, seq_4));
seq_4 = makePair(LAM, 0, SUB, seq_4);
Term y_2 = newTerm(VAR, 0, portLoc(1, seq_4));
seq_4 = makePair(LAM, 0, SUB, seq_4);
Term x_1 = newTerm(VAR, 0, portLoc(1, seq_4));
// allocate args at core: 886

Term x_1_2;
x_1 = dupeArg(x_1, &x_1_2, 47);

Term x_1_1;
x_1 = dupeArg(x_1, &x_1_1, 48);

Term y_2_1;
y_2 = dupeArg(y_2, &y_2_1, 49);

Term start_3_3;
start_3 = dupeArg(start_3, &start_3_3, 50);

Term start_3_2;
start_3 = dupeArg(start_3, &start_3_2, 51);

Term start_3_1;
start_3 = dupeArg(start_3, &start_3_1, 52);
Term glblVal25_11;
glblVal25 = dupeArg(glblVal25, &glblVal25_11, 0);

// call < at core: 899
Term Rslt_6Args = makePair(APP, 0, newI60(0), SUB);
Term Rslt_6 = newTerm(VAR, 0, portLoc(2, Rslt_6Args));
Rslt_6Args = makePair(APP, 0, start_3, Rslt_6Args);
#ifdef STRICT
pushRedex(Rslt_6Args, newRef(glblProto26_679));
#else
swap(termLoc(Rslt_6), makePair(LAZ, 0, Rslt_6Args, newRef(glblProto26_679)));
#endif
// call Some at core: 900
Term Rslt_7Args = makePair(APP, 0, x_1, SUB);
Term Rslt_7 = newTerm(VAR, 0, portLoc(2, Rslt_7Args));
#ifdef STRICT
pushRedex(Rslt_7Args, glblVal25_11);
#else
swap(termLoc(Rslt_7), makePair(LAZ, 0, Rslt_7Args, glblVal25_11));
#endif

// call nth at core: 901
Term Rslt_8Args = makePair(APP, 0, start_3_1, SUB);
Term Rslt_8 = newTerm(VAR, 0, portLoc(2, Rslt_8Args));
Rslt_8Args = makePair(APP, 0, x_1_1, Rslt_8Args);
#ifdef STRICT
pushRedex(Rslt_8Args, newRef(glblProto98_681));
#else
swap(termLoc(Rslt_8), makePair(LAZ, 0, Rslt_8Args, newRef(glblProto98_681)));
#endif
// call .x at core: 901
Term Rslt_9Args = makePair(APP, 0, Rslt_8, SUB);
Term Rslt_9 = newTerm(VAR, 0, portLoc(2, Rslt_9Args));
#ifdef STRICT
pushRedex(Rslt_9Args, newRef(glblProto18_683));
#else
swap(termLoc(Rslt_9), makePair(LAZ, 0, Rslt_9Args, newRef(glblProto18_683)));
#endif
// call nth at core: 901
Term Rslt_10Args = makePair(APP, 0, start_3_2, SUB);
Term Rslt_10 = newTerm(VAR, 0, portLoc(2, Rslt_10Args));
Rslt_10Args = makePair(APP, 0, y_2, Rslt_10Args);
#ifdef STRICT
pushRedex(Rslt_10Args, newRef(glblProto98_685));
#else
swap(termLoc(Rslt_10), makePair(LAZ, 0, Rslt_10Args, newRef(glblProto98_685)));
#endif
// call .x at core: 901
Term Rslt_11Args = makePair(APP, 0, Rslt_10, SUB);
Term Rslt_11 = newTerm(VAR, 0, portLoc(2, Rslt_11Args));
#ifdef STRICT
pushRedex(Rslt_11Args, newRef(glblProto18_687));
#else
swap(termLoc(Rslt_11), makePair(LAZ, 0, Rslt_11Args, newRef(glblProto18_687)));
#endif
// call = at core: 901
Term Rslt_12Args = makePair(APP, 0, Rslt_11, SUB);
Term Rslt_12 = newTerm(VAR, 0, portLoc(2, Rslt_12Args));
Rslt_12Args = makePair(APP, 0, Rslt_9, Rslt_12Args);
#ifdef STRICT
pushRedex(Rslt_12Args, newRef(glblProto20_689));
#else
swap(termLoc(Rslt_12), makePair(LAZ, 0, Rslt_12Args, newRef(glblProto20_689)));
#endif
// call dec at core: 902
Term Rslt_13Args = makePair(APP, 0, start_3_3, SUB);
Term Rslt_13 = newTerm(VAR, 0, portLoc(2, Rslt_13Args));
#ifdef STRICT
pushRedex(Rslt_13Args, glblFn147);
#else
swap(termLoc(Rslt_13), makePair(LAZ, 0, Rslt_13Args, glblFn147));
#endif

// call vect-= at core: 886
Term Rslt_14Args = makePair(APP, 0, Rslt_13, SUB);
Term Rslt_14 = newTerm(VAR, 0, portLoc(2, Rslt_14Args));
Rslt_14Args = makePair(APP, 0, y_2_1, Rslt_14Args);
Rslt_14Args = makePair(APP, 0, x_1_2, Rslt_14Args);
#ifdef STRICT
pushRedex(Rslt_14Args, glblFn676);
#else
swap(termLoc(Rslt_14), makePair(LAZ, 0, Rslt_14Args, glblFn676));
#endif

// call and at core: 901
Term Rslt_15Args = makePair(APP, 0, Rslt_14, SUB);
Term Rslt_15 = newTerm(VAR, 0, portLoc(2, Rslt_15Args));
Rslt_15Args = makePair(APP, 0, Rslt_12, Rslt_15Args);
#ifdef STRICT
pushRedex(Rslt_15Args, newRef(glblProto28_691));
#else
swap(termLoc(Rslt_15), makePair(LAZ, 0, Rslt_15Args, newRef(glblProto28_691)));
#endif
// call cond at core: 899
Term Rslt_16Args = makePair(APP, 0, Rslt_15, SUB);
Term Rslt_16 = newTerm(VAR, 0, portLoc(2, Rslt_16Args));
Rslt_16Args = makePair(APP, 0, Rslt_7, Rslt_16Args);
Rslt_16Args = makePair(APP, 0, Rslt_6, Rslt_16Args);
#ifdef STRICT
pushRedex(Rslt_16Args, newRef(glblProto34_693));
#else
swap(termLoc(Rslt_16), makePair(LAZ, 0, Rslt_16Args, newRef(glblProto34_693)));
#endif
// link args to body
swap(r_seq_4, Rslt_16);

Term Rslt_16V = get(termLoc(Rslt_16));
apps = take(portLoc(1, Rslt_16V));
lams = take(portLoc(2, Rslt_16V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_16), makePair(LAZ, 0, apps, lams));
pushRedex(args, seq_4);
return;
}
Term glblFn676 = newRef(glblCFn675);
Term glblFn678;
void glblCFn677(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 3, &arityArgs);
  Term result = NUL;
    if (arityArgs.count == 3) {
  Term v_1 = arityArgs.args[0];
  Term n_2 = arityArgs.args[1];
  Term x_3 = arityArgs.args[2];

  result = vectStore((Vector *)v_1, getI60(n_2), (Term)x_3);
  dec_and_free(n_2, 1);
move(portLoc(2, args), result);
}
return;
}
Term glblFn678 = newRef(glblCFn677);
void glblProto20_771(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(916), args);
  args = makePair(APP, 0, glblStr199, args);
  interact(args, glblProto20);
}
}
void glblProto20_775(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(917), args);
  args = makePair(APP, 0, glblStr199, args);
  interact(args, glblProto20);
}
}
void glblProto64_777(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(918), args);
  args = makePair(APP, 0, glblStr199, args);
  interact(args, glblProto64);
}
}
void glblProto28_779(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(917), args);
  args = makePair(APP, 0, glblStr199, args);
  interact(args, glblProto28);
}
}
void glblProto28_781(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(916), args);
  args = makePair(APP, 0, glblStr199, args);
  interact(args, glblProto28);
}
}
Term glbl_EQ_742;
void glbl_EQ_741(Term ref, Term args) {
Term apps, lams, V;
Location vLoc;
Term seq_9 = makePair(LAM, 0, SUB, NUL);
Location r_seq_9 = portLoc(2, seq_9);
Term y_8 = newTerm(VAR, 0, portLoc(1, seq_9));
seq_9 = makePair(LAM, 0, SUB, seq_9);
Term x_7 = newTerm(VAR, 0, portLoc(1, seq_9));
// allocate args at core: 914

Term x_7_3;
x_7 = dupeArg(x_7, &x_7_3, 53);

Term x_7_2;
x_7 = dupeArg(x_7, &x_7_2, 54);

Term x_7_1;
x_7 = dupeArg(x_7, &x_7_1, 55);

Term y_8_2;
y_8 = dupeArg(y_8, &y_8_2, 56);

Term y_8_1;
y_8 = dupeArg(y_8, &y_8_1, 57);
// call type-num at core: 916
Term Rslt_11Args = makePair(APP, 0, x_7, SUB);
Term Rslt_11 = newTerm(VAR, 0, portLoc(2, Rslt_11Args));
#ifdef STRICT
pushRedex(Rslt_11Args, glblFn11);
#else
swap(termLoc(Rslt_11), makePair(LAZ, 0, Rslt_11Args, glblFn11));
#endif

// call type-num at core: 916
Term Rslt_12Args = makePair(APP, 0, y_8, SUB);
Term Rslt_12 = newTerm(VAR, 0, portLoc(2, Rslt_12Args));
#ifdef STRICT
pushRedex(Rslt_12Args, glblFn11);
#else
swap(termLoc(Rslt_12), makePair(LAZ, 0, Rslt_12Args, glblFn11));
#endif

// call = at core: 916
Term Rslt_13Args = makePair(APP, 0, Rslt_12, SUB);
Term Rslt_13 = newTerm(VAR, 0, portLoc(2, Rslt_13Args));
Rslt_13Args = makePair(APP, 0, Rslt_11, Rslt_13Args);
#ifdef STRICT
pushRedex(Rslt_13Args, newRef(glblProto20_771));
#else
swap(termLoc(Rslt_13), makePair(LAZ, 0, Rslt_13Args, newRef(glblProto20_771)));
#endif
// call vect-count at core: 917
Term Rslt_14Args = makePair(APP, 0, x_7_1, SUB);
Term Rslt_14 = newTerm(VAR, 0, portLoc(2, Rslt_14Args));
#ifdef STRICT
pushRedex(Rslt_14Args, glblFn325);
#else
swap(termLoc(Rslt_14), makePair(LAZ, 0, Rslt_14Args, glblFn325));
#endif

// call vect-count at core: 917
Term Rslt_15Args = makePair(APP, 0, y_8_1, SUB);
Term Rslt_15 = newTerm(VAR, 0, portLoc(2, Rslt_15Args));
#ifdef STRICT
pushRedex(Rslt_15Args, glblFn325);
#else
swap(termLoc(Rslt_15), makePair(LAZ, 0, Rslt_15Args, glblFn325));
#endif

// call = at core: 917
Term Rslt_16Args = makePair(APP, 0, Rslt_15, SUB);
Term Rslt_16 = newTerm(VAR, 0, portLoc(2, Rslt_16Args));
Rslt_16Args = makePair(APP, 0, Rslt_14, Rslt_16Args);
#ifdef STRICT
pushRedex(Rslt_16Args, newRef(glblProto20_775));
#else
swap(termLoc(Rslt_16), makePair(LAZ, 0, Rslt_16Args, newRef(glblProto20_775)));
#endif
// call count at core: 918
Term Rslt_17Args = makePair(APP, 0, x_7_3, SUB);
Term Rslt_17 = newTerm(VAR, 0, portLoc(2, Rslt_17Args));
#ifdef STRICT
pushRedex(Rslt_17Args, newRef(glblProto64_777));
#else
swap(termLoc(Rslt_17), makePair(LAZ, 0, Rslt_17Args, newRef(glblProto64_777)));
#endif
// call dec at core: 918
Term Rslt_18Args = makePair(APP, 0, Rslt_17, SUB);
Term Rslt_18 = newTerm(VAR, 0, portLoc(2, Rslt_18Args));
#ifdef STRICT
pushRedex(Rslt_18Args, glblFn147);
#else
swap(termLoc(Rslt_18), makePair(LAZ, 0, Rslt_18Args, glblFn147));
#endif

// call vect-= at core: 918
Term Rslt_19Args = makePair(APP, 0, Rslt_18, SUB);
Term Rslt_19 = newTerm(VAR, 0, portLoc(2, Rslt_19Args));
Rslt_19Args = makePair(APP, 0, y_8_2, Rslt_19Args);
Rslt_19Args = makePair(APP, 0, x_7_2, Rslt_19Args);
#ifdef STRICT
pushRedex(Rslt_19Args, glblFn676);
#else
swap(termLoc(Rslt_19), makePair(LAZ, 0, Rslt_19Args, glblFn676));
#endif

// call and at core: 917
Term Rslt_20Args = makePair(APP, 0, Rslt_19, SUB);
Term Rslt_20 = newTerm(VAR, 0, portLoc(2, Rslt_20Args));
Rslt_20Args = makePair(APP, 0, Rslt_16, Rslt_20Args);
#ifdef STRICT
pushRedex(Rslt_20Args, newRef(glblProto28_779));
#else
swap(termLoc(Rslt_20), makePair(LAZ, 0, Rslt_20Args, newRef(glblProto28_779)));
#endif
// call and at core: 916
Term Rslt_21Args = makePair(APP, 0, Rslt_20, SUB);
Term Rslt_21 = newTerm(VAR, 0, portLoc(2, Rslt_21Args));
Rslt_21Args = makePair(APP, 0, Rslt_13, Rslt_21Args);
#ifdef STRICT
pushRedex(Rslt_21Args, newRef(glblProto28_781));
#else
swap(termLoc(Rslt_21), makePair(LAZ, 0, Rslt_21Args, newRef(glblProto28_781)));
#endif
// link args to body
swap(r_seq_9, Rslt_21);

Term Rslt_21V = get(termLoc(Rslt_21));
apps = take(portLoc(1, Rslt_21V));
lams = take(portLoc(2, Rslt_21V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_21), makePair(LAZ, 0, apps, lams));
pushRedex(args, seq_9);
return;
}
Term glbl_EQ_742 = newRef(glbl_EQ_741);
struct {TYPE_SIZE type;
        REFS_SIZE refs;
        int64_t *hash;
        Value *parent;
        int64_t len;
        char buffer[7];
} str9 = {StringBufferType, REFS_STATIC, 0, 0, 6, "Vector"};
Term glblStr745 = newTerm_(VAL, (Term)&str9);
Term glbltype_name740;
void glbltype_name739(Term ref, Term args) {
Term apps, lams, V;
Location vLoc;
Term seq_24 = makePair(LAM, 0, SUB, NUL);
Location r_seq_24 = portLoc(2, seq_24);
Term arg_23 = newTerm(VAR, 0, portLoc(1, seq_24));
// allocate args at core: 920
swap(termLoc(arg_23), ERA);
// link args to body
swap(r_seq_24, glblStr745);

V = get(r_seq_24);
apps = makePair(APP, 0, V, SUB);
lams = makePair(LAM, 0, SUB, NUL);
swap(portLoc(2, lams),newTerm(VAR, 0, portLoc(1, lams)));
vLoc = portLoc(2, apps);
swap(vLoc, makePair(LAZ, 0, apps, lams));
swap(r_seq_24, newTerm(VAR, 0, vLoc));
pushRedex(args, seq_24);
return;
}
Term glbltype_name740 = newRef(glbltype_name739);
struct {TYPE_SIZE type;
        REFS_SIZE refs;
        int64_t *hash;
        Value *parent;
        int64_t len;
        char buffer[2];
} str10 = {StringBufferType, REFS_STATIC, 0, 0, 1, "["};
Term glblStr747 = newTerm_(VAL, (Term)&str10);
void glblProto60_785(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(924), args);
  args = makePair(APP, 0, glblStr199, args);
  interact(args, glblProto60);
}
}
struct {TYPE_SIZE type;
        REFS_SIZE refs;
        int64_t *hash;
        Value *parent;
        int64_t len;
        char buffer[2];
} str11 = {StringBufferType, REFS_STATIC, 0, 0, 1, "]"};
Term glblStr746 = newTerm_(VAL, (Term)&str11);
Term glblstr_vect738;
void glblstr_vect737(Term ref, Term args) {
Term apps, lams, V;
Location vLoc;
Term seq_28 = makePair(LAM, 0, SUB, NUL);
Location r_seq_28 = portLoc(2, seq_28);
Term v_27 = newTerm(VAR, 0, portLoc(1, seq_28));
// allocate args at core: 923
Term glblVal175_12;
glblVal175 = dupeArg(glblVal175, &glblVal175_12, 0);

// call vect-conj at core: 924
Term Rslt_30Args = makePair(APP, 0, glblStr747, SUB);
Term Rslt_30 = newTerm(VAR, 0, portLoc(2, Rslt_30Args));
Rslt_30Args = makePair(APP, 0, glblVal175_12, Rslt_30Args);
#ifdef STRICT
pushRedex(Rslt_30Args, glblFn178);
#else
swap(termLoc(Rslt_30), makePair(LAZ, 0, Rslt_30Args, glblFn178));
#endif

// call interpose at core: 924
Term Rslt_31Args = makePair(APP, 0, glblStr210, SUB);
Term Rslt_31 = newTerm(VAR, 0, portLoc(2, Rslt_31Args));
Rslt_31Args = makePair(APP, 0, v_27, Rslt_31Args);
#ifdef STRICT
pushRedex(Rslt_31Args, newRef(glblProto60_785));
#else
swap(termLoc(Rslt_31), makePair(LAZ, 0, Rslt_31Args, newRef(glblProto60_785)));
#endif
// call to-str at core: 924
Term Rslt_32Args = makePair(APP, 0, Rslt_31, SUB);
Term Rslt_32 = newTerm(VAR, 0, portLoc(2, Rslt_32Args));
#ifdef STRICT
pushRedex(Rslt_32Args, glblFn656);
#else
swap(termLoc(Rslt_32), makePair(LAZ, 0, Rslt_32Args, glblFn656));
#endif

// call vect-conj at core: 924
Term Rslt_33Args = makePair(APP, 0, Rslt_32, SUB);
Term Rslt_33 = newTerm(VAR, 0, portLoc(2, Rslt_33Args));
Rslt_33Args = makePair(APP, 0, Rslt_30, Rslt_33Args);
#ifdef STRICT
pushRedex(Rslt_33Args, glblFn178);
#else
swap(termLoc(Rslt_33), makePair(LAZ, 0, Rslt_33Args, glblFn178));
#endif

// call vect-conj at core: 924
Term Rslt_34Args = makePair(APP, 0, glblStr746, SUB);
Term Rslt_34 = newTerm(VAR, 0, portLoc(2, Rslt_34Args));
Rslt_34Args = makePair(APP, 0, Rslt_33, Rslt_34Args);
#ifdef STRICT
pushRedex(Rslt_34Args, glblFn178);
#else
swap(termLoc(Rslt_34), makePair(LAZ, 0, Rslt_34Args, glblFn178));
#endif

// link args to body
swap(r_seq_28, Rslt_34);

Term Rslt_34V = get(termLoc(Rslt_34));
apps = take(portLoc(1, Rslt_34V));
lams = take(portLoc(2, Rslt_34V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_34), makePair(LAZ, 0, apps, lams));
pushRedex(args, seq_28);
return;
}
Term glblstr_vect738 = newRef(glblstr_vect737);
Term glblcount736;
void glblcount735(Term ref, Term args) {
Term apps, lams, V;
Location vLoc;
Term seq_37 = makePair(LAM, 0, SUB, NUL);
Location r_seq_37 = portLoc(2, seq_37);
Term l_36 = newTerm(VAR, 0, portLoc(1, seq_37));
// allocate args at core: 926
// call vect-count at core: 927
Term Rslt_39Args = makePair(APP, 0, l_36, SUB);
Term Rslt_39 = newTerm(VAR, 0, portLoc(2, Rslt_39Args));
#ifdef STRICT
pushRedex(Rslt_39Args, glblFn325);
#else
swap(termLoc(Rslt_39), makePair(LAZ, 0, Rslt_39Args, glblFn325));
#endif

// link args to body
swap(r_seq_37, Rslt_39);

Term Rslt_39V = get(termLoc(Rslt_39));
apps = take(portLoc(1, Rslt_39V));
lams = take(portLoc(2, Rslt_39V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_39), makePair(LAZ, 0, apps, lams));
pushRedex(args, seq_37);
return;
}
Term glblcount736 = newRef(glblcount735);
Term glblget734;
void glblget733(Term ref, Term args) {
Term apps, lams, V;
Location vLoc;
Term seq_43 = makePair(LAM, 0, SUB, NUL);
Location r_seq_43 = portLoc(2, seq_43);
Term n_42 = newTerm(VAR, 0, portLoc(1, seq_43));
seq_43 = makePair(LAM, 0, SUB, seq_43);
Term v_41 = newTerm(VAR, 0, portLoc(1, seq_43));
// allocate args at core: 929
// call vect-get at core: 930
Term Rslt_45Args = makePair(APP, 0, n_42, SUB);
Term Rslt_45 = newTerm(VAR, 0, portLoc(2, Rslt_45Args));
Rslt_45Args = makePair(APP, 0, v_41, Rslt_45Args);
#ifdef STRICT
pushRedex(Rslt_45Args, glblFn479);
#else
swap(termLoc(Rslt_45), makePair(LAZ, 0, Rslt_45Args, glblFn479));
#endif

// link args to body
swap(r_seq_43, Rslt_45);

Term Rslt_45V = get(termLoc(Rslt_45));
apps = take(portLoc(1, Rslt_45V));
lams = take(portLoc(2, Rslt_45V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_45), makePair(LAZ, 0, apps, lams));
pushRedex(args, seq_43);
return;
}
Term glblget734 = newRef(glblget733);
Term glblnth732;
void glblnth731(Term ref, Term args) {
Term apps, lams, V;
Location vLoc;
Term seq_49 = makePair(LAM, 0, SUB, NUL);
Location r_seq_49 = portLoc(2, seq_49);
Term n_48 = newTerm(VAR, 0, portLoc(1, seq_49));
seq_49 = makePair(LAM, 0, SUB, seq_49);
Term v_47 = newTerm(VAR, 0, portLoc(1, seq_49));
// allocate args at core: 932
// call vect-get at core: 933
Term Rslt_51Args = makePair(APP, 0, n_48, SUB);
Term Rslt_51 = newTerm(VAR, 0, portLoc(2, Rslt_51Args));
Rslt_51Args = makePair(APP, 0, v_47, Rslt_51Args);
#ifdef STRICT
pushRedex(Rslt_51Args, glblFn479);
#else
swap(termLoc(Rslt_51), makePair(LAZ, 0, Rslt_51Args, glblFn479));
#endif

// link args to body
swap(r_seq_49, Rslt_51);

Term Rslt_51V = get(termLoc(Rslt_51));
apps = take(portLoc(1, Rslt_51V));
lams = take(portLoc(2, Rslt_51V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_51), makePair(LAZ, 0, apps, lams));
pushRedex(args, seq_49);
return;
}
Term glblnth732 = newRef(glblnth731);
void glblProto64_791(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(936), args);
  args = makePair(APP, 0, glblStr199, args);
  interact(args, glblProto64);
}
}
void glblProto28_793(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(936), args);
  args = makePair(APP, 0, glblStr199, args);
  interact(args, glblProto28);
}
}
Term glblempty_QM_730;
void glblempty_QM_729(Term ref, Term args) {
Term apps, lams, V;
Location vLoc;
Term seq_54 = makePair(LAM, 0, SUB, NUL);
Location r_seq_54 = portLoc(2, seq_54);
Term v_53 = newTerm(VAR, 0, portLoc(1, seq_54));
// allocate args at core: 935
Term glblVal175_13;
glblVal175 = dupeArg(glblVal175, &glblVal175_13, 0);

Term glblVal25_12;
glblVal25 = dupeArg(glblVal25, &glblVal25_12, 0);

// call count at core: 936
Term Rslt_56Args = makePair(APP, 0, v_53, SUB);
Term Rslt_56 = newTerm(VAR, 0, portLoc(2, Rslt_56Args));
#ifdef STRICT
pushRedex(Rslt_56Args, newRef(glblProto64_791));
#else
swap(termLoc(Rslt_56), makePair(LAZ, 0, Rslt_56Args, newRef(glblProto64_791)));
#endif
// call int-= at core: 936
Term Rslt_57Args = makePair(APP, 0, Rslt_56, SUB);
Term Rslt_57 = newTerm(VAR, 0, portLoc(2, Rslt_57Args));
Rslt_57Args = makePair(APP, 0, newI60(0), Rslt_57Args);
#ifdef STRICT
pushRedex(Rslt_57Args, glblFn253);
#else
swap(termLoc(Rslt_57), makePair(LAZ, 0, Rslt_57Args, glblFn253));
#endif

// call Some at core: 937
Term Rslt_58Args = makePair(APP, 0, glblVal175_13, SUB);
Term Rslt_58 = newTerm(VAR, 0, portLoc(2, Rslt_58Args));
#ifdef STRICT
pushRedex(Rslt_58Args, glblVal25_12);
#else
swap(termLoc(Rslt_58), makePair(LAZ, 0, Rslt_58Args, glblVal25_12));
#endif

// call and at core: 936
Term Rslt_59Args = makePair(APP, 0, Rslt_58, SUB);
Term Rslt_59 = newTerm(VAR, 0, portLoc(2, Rslt_59Args));
Rslt_59Args = makePair(APP, 0, Rslt_57, Rslt_59Args);
#ifdef STRICT
pushRedex(Rslt_59Args, newRef(glblProto28_793));
#else
swap(termLoc(Rslt_59), makePair(LAZ, 0, Rslt_59Args, newRef(glblProto28_793)));
#endif
// link args to body
swap(r_seq_54, Rslt_59);

Term Rslt_59V = get(termLoc(Rslt_59));
apps = take(portLoc(1, Rslt_59V));
lams = take(portLoc(2, Rslt_59V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_59), makePair(LAZ, 0, apps, lams));
pushRedex(args, seq_54);
return;
}
Term glblempty_QM_730 = newRef(glblempty_QM_729);
Term glblempty728;
void glblempty727(Term ref, Term args) {
Term apps, lams, V;
Location vLoc;
Term seq_62 = makePair(LAM, 0, SUB, NUL);
Location r_seq_62 = portLoc(2, seq_62);
Term coll_61 = newTerm(VAR, 0, portLoc(1, seq_62));
// allocate args at core: 939
swap(termLoc(coll_61), ERA);
Term glblVal175_14;
glblVal175 = dupeArg(glblVal175, &glblVal175_14, 0);

// link args to body
swap(r_seq_62, glblVal175_14);

V = get(r_seq_62);
apps = makePair(APP, 0, V, SUB);
lams = makePair(LAM, 0, SUB, NUL);
swap(portLoc(2, lams),newTerm(VAR, 0, portLoc(1, lams)));
vLoc = portLoc(2, apps);
swap(vLoc, makePair(LAZ, 0, apps, lams));
swap(r_seq_62, newTerm(VAR, 0, vLoc));
pushRedex(args, seq_62);
return;
}
Term glblempty728 = newRef(glblempty727);
void glblProto72_799(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(943), args);
  args = makePair(APP, 0, glblStr199, args);
  interact(args, glblProto72);
}
}
Term glblcomp726;
void glblcomp725(Term ref, Term args) {
Term apps, lams, V;
Location vLoc;
Term seq_67 = makePair(LAM, 0, SUB, NUL);
Location r_seq_67 = portLoc(2, seq_67);
Term v2_66 = newTerm(VAR, 0, portLoc(1, seq_67));
seq_67 = makePair(LAM, 0, SUB, seq_67);
Term v1_65 = newTerm(VAR, 0, portLoc(1, seq_67));
// allocate args at core: 942
// call reduce at core: 943
Term Rslt_69Args = makePair(APP, 0, glblFn178, SUB);
Term Rslt_69 = newTerm(VAR, 0, portLoc(2, Rslt_69Args));
Rslt_69Args = makePair(APP, 0, v1_65, Rslt_69Args);
Rslt_69Args = makePair(APP, 0, v2_66, Rslt_69Args);
#ifdef STRICT
pushRedex(Rslt_69Args, newRef(glblProto72_799));
#else
swap(termLoc(Rslt_69), makePair(LAZ, 0, Rslt_69Args, newRef(glblProto72_799)));
#endif
// link args to body
swap(r_seq_67, Rslt_69);

Term Rslt_69V = get(termLoc(Rslt_69));
apps = take(portLoc(1, Rslt_69V));
lams = take(portLoc(2, Rslt_69V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_69), makePair(LAZ, 0, apps, lams));
pushRedex(args, seq_67);
return;
}
Term glblcomp726 = newRef(glblcomp725);
void glblProto119_802(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(946), args);
  args = makePair(APP, 0, glblStr199, args);
  interact(args, glblProto119);
}
}
Term glblfirst724;
void glblfirst723(Term ref, Term args) {
Term apps, lams, V;
Location vLoc;
Term seq_72 = makePair(LAM, 0, SUB, NUL);
Location r_seq_72 = portLoc(2, seq_72);
Term v_71 = newTerm(VAR, 0, portLoc(1, seq_72));
// allocate args at core: 945
// call get at core: 946
Term Rslt_74Args = makePair(APP, 0, newI60(0), SUB);
Term Rslt_74 = newTerm(VAR, 0, portLoc(2, Rslt_74Args));
Rslt_74Args = makePair(APP, 0, v_71, Rslt_74Args);
#ifdef STRICT
pushRedex(Rslt_74Args, newRef(glblProto119_802));
#else
swap(termLoc(Rslt_74), makePair(LAZ, 0, Rslt_74Args, newRef(glblProto119_802)));
#endif
// link args to body
swap(r_seq_72, Rslt_74);

Term Rslt_74V = get(termLoc(Rslt_74));
apps = take(portLoc(1, Rslt_74V));
lams = take(portLoc(2, Rslt_74V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_74), makePair(LAZ, 0, apps, lams));
pushRedex(args, seq_72);
return;
}
Term glblfirst724 = newRef(glblfirst723);
void glblProto64_805(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(949), args);
  args = makePair(APP, 0, glblStr199, args);
  interact(args, glblProto64);
}
}
void glblProto119_807(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(949), args);
  args = makePair(APP, 0, glblStr199, args);
  interact(args, glblProto119);
}
}
Term glbllast722;
void glbllast721(Term ref, Term args) {
Term apps, lams, V;
Location vLoc;
Term seq_77 = makePair(LAM, 0, SUB, NUL);
Location r_seq_77 = portLoc(2, seq_77);
Term v_76 = newTerm(VAR, 0, portLoc(1, seq_77));
// allocate args at core: 948

Term v_76_1;
v_76 = dupeArg(v_76, &v_76_1, 58);
// call count at core: 949
Term Rslt_79Args = makePair(APP, 0, v_76_1, SUB);
Term Rslt_79 = newTerm(VAR, 0, portLoc(2, Rslt_79Args));
#ifdef STRICT
pushRedex(Rslt_79Args, newRef(glblProto64_805));
#else
swap(termLoc(Rslt_79), makePair(LAZ, 0, Rslt_79Args, newRef(glblProto64_805)));
#endif
// call dec at core: 949
Term Rslt_80Args = makePair(APP, 0, Rslt_79, SUB);
Term Rslt_80 = newTerm(VAR, 0, portLoc(2, Rslt_80Args));
#ifdef STRICT
pushRedex(Rslt_80Args, glblFn147);
#else
swap(termLoc(Rslt_80), makePair(LAZ, 0, Rslt_80Args, glblFn147));
#endif

// call get at core: 949
Term Rslt_81Args = makePair(APP, 0, Rslt_80, SUB);
Term Rslt_81 = newTerm(VAR, 0, portLoc(2, Rslt_81Args));
Rslt_81Args = makePair(APP, 0, v_76, Rslt_81Args);
#ifdef STRICT
pushRedex(Rslt_81Args, newRef(glblProto119_807));
#else
swap(termLoc(Rslt_81), makePair(LAZ, 0, Rslt_81Args, newRef(glblProto119_807)));
#endif
// link args to body
swap(r_seq_77, Rslt_81);

Term Rslt_81V = get(termLoc(Rslt_81));
apps = take(portLoc(1, Rslt_81V));
lams = take(portLoc(2, Rslt_81V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_81), makePair(LAZ, 0, apps, lams));
pushRedex(args, seq_77);
return;
}
Term glbllast722 = newRef(glbllast721);
void glblProto64_810(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(952), args);
  args = makePair(APP, 0, glblStr199, args);
  interact(args, glblProto64);
}
}
Term glblrest719;
void glblrest717(Term ref, Term args) {
Term apps, lams, V;
Location vLoc;
Term seq_84 = makePair(LAM, 0, SUB, NUL);
Location r_seq_84 = portLoc(2, seq_84);
Term v_83 = newTerm(VAR, 0, portLoc(1, seq_84));
// allocate args at core: 951

Term v_83_1;
v_83 = dupeArg(v_83, &v_83_1, 59);
// call count at core: 952
Term Rslt_86Args = makePair(APP, 0, v_83_1, SUB);
Term Rslt_86 = newTerm(VAR, 0, portLoc(2, Rslt_86Args));
#ifdef STRICT
pushRedex(Rslt_86Args, newRef(glblProto64_810));
#else
swap(termLoc(Rslt_86), makePair(LAZ, 0, Rslt_86Args, newRef(glblProto64_810)));
#endif
// call dec at core: 952
Term Rslt_87Args = makePair(APP, 0, Rslt_86, SUB);
Term Rslt_87 = newTerm(VAR, 0, portLoc(2, Rslt_87Args));
#ifdef STRICT
pushRedex(Rslt_87Args, glblFn147);
#else
swap(termLoc(Rslt_87), makePair(LAZ, 0, Rslt_87Args, glblFn147));
#endif

// call subvec at core: 952
Term Rslt_88Args = makePair(APP, 0, Rslt_87, SUB);
Term Rslt_88 = newTerm(VAR, 0, portLoc(2, Rslt_88Args));
Rslt_88Args = makePair(APP, 0, newI60(1), Rslt_88Args);
Rslt_88Args = makePair(APP, 0, v_83, Rslt_88Args);
#ifdef STRICT
pushRedex(Rslt_88Args, glblFn650);
#else
swap(termLoc(Rslt_88), makePair(LAZ, 0, Rslt_88Args, glblFn650));
#endif

// link args to body
swap(r_seq_84, Rslt_88);

Term Rslt_88V = get(termLoc(Rslt_88));
apps = take(portLoc(1, Rslt_88V));
lams = take(portLoc(2, Rslt_88V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_88), makePair(LAZ, 0, apps, lams));
pushRedex(args, seq_84);
return;
}
Term glblrest719 = newRef(glblrest717);
Term glblconj716;
void glblconj715(Term ref, Term args) {
Term apps, lams, V;
Location vLoc;
Term seq_92 = makePair(LAM, 0, SUB, NUL);
Location r_seq_92 = portLoc(2, seq_92);
Term v_91 = newTerm(VAR, 0, portLoc(1, seq_92));
seq_92 = makePair(LAM, 0, SUB, seq_92);
Term vect_90 = newTerm(VAR, 0, portLoc(1, seq_92));
// allocate args at core: 954
// call vect-conj at core: 955
Term Rslt_94Args = makePair(APP, 0, v_91, SUB);
Term Rslt_94 = newTerm(VAR, 0, portLoc(2, Rslt_94Args));
Rslt_94Args = makePair(APP, 0, vect_90, Rslt_94Args);
#ifdef STRICT
pushRedex(Rslt_94Args, glblFn178);
#else
swap(termLoc(Rslt_94), makePair(LAZ, 0, Rslt_94Args, glblFn178));
#endif

// link args to body
swap(r_seq_92, Rslt_94);

Term Rslt_94V = get(termLoc(Rslt_94));
apps = take(portLoc(1, Rslt_94V));
lams = take(portLoc(2, Rslt_94V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_94), makePair(LAZ, 0, apps, lams));
pushRedex(args, seq_92);
return;
}
Term glblconj716 = newRef(glblconj715);
Term glblstore714;
void glblstore713(Term ref, Term args) {
Term apps, lams, V;
Location vLoc;
Term seq_99 = makePair(LAM, 0, SUB, NUL);
Location r_seq_99 = portLoc(2, seq_99);
Term v_98 = newTerm(VAR, 0, portLoc(1, seq_99));
seq_99 = makePair(LAM, 0, SUB, seq_99);
Term n_97 = newTerm(VAR, 0, portLoc(1, seq_99));
seq_99 = makePair(LAM, 0, SUB, seq_99);
Term vect_96 = newTerm(VAR, 0, portLoc(1, seq_99));
// allocate args at core: 957
// call vect-store at core: 958
Term Rslt_101Args = makePair(APP, 0, v_98, SUB);
Term Rslt_101 = newTerm(VAR, 0, portLoc(2, Rslt_101Args));
Rslt_101Args = makePair(APP, 0, n_97, Rslt_101Args);
Rslt_101Args = makePair(APP, 0, vect_96, Rslt_101Args);
#ifdef STRICT
pushRedex(Rslt_101Args, glblFn678);
#else
swap(termLoc(Rslt_101), makePair(LAZ, 0, Rslt_101Args, glblFn678));
#endif

// link args to body
swap(r_seq_99, Rslt_101);

Term Rslt_101V = get(termLoc(Rslt_101));
apps = take(portLoc(1, Rslt_101V));
lams = take(portLoc(2, Rslt_101V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_101), makePair(LAZ, 0, apps, lams));
pushRedex(args, seq_99);
return;
}
Term glblstore714 = newRef(glblstore713);
void glblProto76_815(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(961), args);
  args = makePair(APP, 0, glblStr199, args);
  interact(args, glblProto76);
}
}
void glblProto496_817(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(961), args);
  args = makePair(APP, 0, glblStr199, args);
  interact(args, glblProto496);
}
}
Term glblreduce712;
void glblreduce711(Term ref, Term args) {
Term apps, lams, V;
Location vLoc;
Term seq_106 = makePair(LAM, 0, SUB, NUL);
Location r_seq_106 = portLoc(2, seq_106);
Term f_105 = newTerm(VAR, 0, portLoc(1, seq_106));
seq_106 = makePair(LAM, 0, SUB, seq_106);
Term result_104 = newTerm(VAR, 0, portLoc(1, seq_106));
seq_106 = makePair(LAM, 0, SUB, seq_106);
Term v_103 = newTerm(VAR, 0, portLoc(1, seq_106));
// allocate args at core: 960

Term v_103_1;
v_103 = dupeArg(v_103, &v_103_1, 60);
// call first at core: 961
Term Rslt_108Args = makePair(APP, 0, v_103, SUB);
Term Rslt_108 = newTerm(VAR, 0, portLoc(2, Rslt_108Args));
#ifdef STRICT
pushRedex(Rslt_108Args, newRef(glblProto76_815));
#else
swap(termLoc(Rslt_108), makePair(LAZ, 0, Rslt_108Args, newRef(glblProto76_815)));
#endif
// call vect-reduce at core: 961
Term Rslt_109Args = makePair(APP, 0, f_105, SUB);
Term Rslt_109 = newTerm(VAR, 0, portLoc(2, Rslt_109Args));
Rslt_109Args = makePair(APP, 0, result_104, Rslt_109Args);
Rslt_109Args = makePair(APP, 0, newI60(1), Rslt_109Args);
Rslt_109Args = makePair(APP, 0, v_103_1, Rslt_109Args);
Rslt_109Args = makePair(APP, 0, Rslt_108, Rslt_109Args);
#ifdef STRICT
pushRedex(Rslt_109Args, newRef(glblProto496_817));
#else
swap(termLoc(Rslt_109), makePair(LAZ, 0, Rslt_109Args, newRef(glblProto496_817)));
#endif
// link args to body
swap(r_seq_106, Rslt_109);

Term Rslt_109V = get(termLoc(Rslt_109));
apps = take(portLoc(1, Rslt_109V));
lams = take(portLoc(2, Rslt_109V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_109), makePair(LAZ, 0, apps, lams));
pushRedex(args, seq_106);
return;
}
Term glblreduce712 = newRef(glblreduce711);
void glblProto80_822(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(964), args);
  args = makePair(APP, 0, glblStr199, args);
  interact(args, glblProto80);
}
}
void glblProto64_824(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(964), args);
  args = makePair(APP, 0, glblStr199, args);
  interact(args, glblProto64);
}
}
void glblProto498_826(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(964), args);
  args = makePair(APP, 0, glblStr199, args);
  interact(args, glblProto498);
}
}
Term glblreverse710;
void glblreverse709(Term ref, Term args) {
Term apps, lams, V;
Location vLoc;
Term seq_112 = makePair(LAM, 0, SUB, NUL);
Location r_seq_112 = portLoc(2, seq_112);
Term v_111 = newTerm(VAR, 0, portLoc(1, seq_112));
// allocate args at core: 963

Term v_111_2;
v_111 = dupeArg(v_111, &v_111_2, 61);

Term v_111_1;
v_111 = dupeArg(v_111, &v_111_1, 62);
Term glblVal175_15;
glblVal175 = dupeArg(glblVal175, &glblVal175_15, 0);

// call last at core: 964
Term Rslt_114Args = makePair(APP, 0, v_111, SUB);
Term Rslt_114 = newTerm(VAR, 0, portLoc(2, Rslt_114Args));
#ifdef STRICT
pushRedex(Rslt_114Args, newRef(glblProto80_822));
#else
swap(termLoc(Rslt_114), makePair(LAZ, 0, Rslt_114Args, newRef(glblProto80_822)));
#endif
// call count at core: 964
Term Rslt_115Args = makePair(APP, 0, v_111_2, SUB);
Term Rslt_115 = newTerm(VAR, 0, portLoc(2, Rslt_115Args));
#ifdef STRICT
pushRedex(Rslt_115Args, newRef(glblProto64_824));
#else
swap(termLoc(Rslt_115), makePair(LAZ, 0, Rslt_115Args, newRef(glblProto64_824)));
#endif
Term Rslt_116 = makeOp(OP_SUB, Rslt_115, newI60(2));
// call vect-reverse at core: 964
Term Rslt_117Args = makePair(APP, 0, glblVal175_15, SUB);
Term Rslt_117 = newTerm(VAR, 0, portLoc(2, Rslt_117Args));
Rslt_117Args = makePair(APP, 0, Rslt_116, Rslt_117Args);
Rslt_117Args = makePair(APP, 0, v_111_1, Rslt_117Args);
Rslt_117Args = makePair(APP, 0, Rslt_114, Rslt_117Args);
#ifdef STRICT
pushRedex(Rslt_117Args, newRef(glblProto498_826));
#else
swap(termLoc(Rslt_117), makePair(LAZ, 0, Rslt_117Args, newRef(glblProto498_826)));
#endif
// link args to body
swap(r_seq_112, Rslt_117);

Term Rslt_117V = get(termLoc(Rslt_117));
apps = take(portLoc(1, Rslt_117V));
lams = take(portLoc(2, Rslt_117V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_117), makePair(LAZ, 0, apps, lams));
pushRedex(args, seq_112);
return;
}
Term glblreverse710 = newRef(glblreverse709);
Term glblmap708;
void glblmap707(Term ref, Term args) {
Term apps, lams, V;
Location vLoc;
Term seq_121 = makePair(LAM, 0, SUB, NUL);
Location r_seq_121 = portLoc(2, seq_121);
Term f_120 = newTerm(VAR, 0, portLoc(1, seq_121));
seq_121 = makePair(LAM, 0, SUB, seq_121);
Term v_119 = newTerm(VAR, 0, portLoc(1, seq_121));
// allocate args at core: 966
Term glblVal651_1;
glblVal651 = dupeArg(glblVal651, &glblVal651_1, 0);

// call vect-map at core: 968
Term Rslt_123Args = makePair(APP, 0, f_120, SUB);
Term Rslt_123 = newTerm(VAR, 0, portLoc(2, Rslt_123Args));
Rslt_123Args = makePair(APP, 0, v_119, Rslt_123Args);
#ifdef STRICT
pushRedex(Rslt_123Args, glblVal651_1);
#else
swap(termLoc(Rslt_123), makePair(LAZ, 0, Rslt_123Args, glblVal651_1));
#endif

// link args to body
swap(r_seq_121, Rslt_123);

Term Rslt_123V = get(termLoc(Rslt_123));
apps = take(portLoc(1, Rslt_123V));
lams = take(portLoc(2, Rslt_123V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_123), makePair(LAZ, 0, apps, lams));
pushRedex(args, seq_121);
return;
}
Term glblmap708 = newRef(glblmap707);
Term glblwrap706;
void glblwrap705(Term ref, Term args) {
Term apps, lams, V;
Location vLoc;
Term seq_127 = makePair(LAM, 0, SUB, NUL);
Location r_seq_127 = portLoc(2, seq_127);
Term v_126 = newTerm(VAR, 0, portLoc(1, seq_127));
seq_127 = makePair(LAM, 0, SUB, seq_127);
Term arg_125 = newTerm(VAR, 0, portLoc(1, seq_127));
// allocate args at core: 970
swap(termLoc(arg_125), ERA);
Term glblVal175_16;
glblVal175 = dupeArg(glblVal175, &glblVal175_16, 0);

// call vect-conj at core: 971
Term Rslt_129Args = makePair(APP, 0, v_126, SUB);
Term Rslt_129 = newTerm(VAR, 0, portLoc(2, Rslt_129Args));
Rslt_129Args = makePair(APP, 0, glblVal175_16, Rslt_129Args);
#ifdef STRICT
pushRedex(Rslt_129Args, glblFn178);
#else
swap(termLoc(Rslt_129), makePair(LAZ, 0, Rslt_129Args, glblFn178));
#endif

// link args to body
swap(r_seq_127, Rslt_129);

Term Rslt_129V = get(termLoc(Rslt_129));
apps = take(portLoc(1, Rslt_129V));
lams = take(portLoc(2, Rslt_129V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_129), makePair(LAZ, 0, apps, lams));
pushRedex(args, seq_127);
return;
}
Term glblwrap706 = newRef(glblwrap705);
void glblProto48_831(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(974), args);
  args = makePair(APP, 0, glblStr199, args);
  interact(args, glblProto48);
}
}
Term glblflat_map704;
void glblflat_map703(Term ref, Term args) {
Term apps, lams, V;
Location vLoc;
Term seq_133 = makePair(LAM, 0, SUB, NUL);
Location r_seq_133 = portLoc(2, seq_133);
Term f_132 = newTerm(VAR, 0, portLoc(1, seq_133));
seq_133 = makePair(LAM, 0, SUB, seq_133);
Term v_131 = newTerm(VAR, 0, portLoc(1, seq_133));
// allocate args at core: 973
Term glblVal651_2;
glblVal651 = dupeArg(glblVal651, &glblVal651_2, 0);

// call vect-map at core: 974
Term Rslt_135Args = makePair(APP, 0, f_132, SUB);
Term Rslt_135 = newTerm(VAR, 0, portLoc(2, Rslt_135Args));
Rslt_135Args = makePair(APP, 0, v_131, Rslt_135Args);
#ifdef STRICT
pushRedex(Rslt_135Args, glblVal651_2);
#else
swap(termLoc(Rslt_135), makePair(LAZ, 0, Rslt_135Args, glblVal651_2));
#endif

// call flatten at core: 974
Term Rslt_136Args = makePair(APP, 0, Rslt_135, SUB);
Term Rslt_136 = newTerm(VAR, 0, portLoc(2, Rslt_136Args));
#ifdef STRICT
pushRedex(Rslt_136Args, newRef(glblProto48_831));
#else
swap(termLoc(Rslt_136), makePair(LAZ, 0, Rslt_136Args, newRef(glblProto48_831)));
#endif
// link args to body
swap(r_seq_133, Rslt_136);

Term Rslt_136V = get(termLoc(Rslt_136));
apps = take(portLoc(1, Rslt_136V));
lams = take(portLoc(2, Rslt_136V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_136), makePair(LAZ, 0, apps, lams));
pushRedex(args, seq_133);
return;
}
Term glblflat_map704 = newRef(glblflat_map703);
void glblProto76_834(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(977), args);
  args = makePair(APP, 0, glblStr199, args);
  interact(args, glblProto76);
}
}
void glblProto496_836(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(977), args);
  args = makePair(APP, 0, glblStr199, args);
  interact(args, glblProto496);
}
}
void glblProto38_838(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(977), args);
  args = makePair(APP, 0, glblStr199, args);
  interact(args, glblProto38);
}
}
Term glblflatten702;
void glblflatten701(Term ref, Term args) {
Term apps, lams, V;
Location vLoc;
Term seq_139 = makePair(LAM, 0, SUB, NUL);
Location r_seq_139 = portLoc(2, seq_139);
Term v_138 = newTerm(VAR, 0, portLoc(1, seq_139));
// allocate args at core: 976

Term v_138_1;
v_138 = dupeArg(v_138, &v_138_1, 63);
Term glblVal175_17;
glblVal175 = dupeArg(glblVal175, &glblVal175_17, 0);

// call first at core: 977
Term Rslt_141Args = makePair(APP, 0, v_138, SUB);
Term Rslt_141 = newTerm(VAR, 0, portLoc(2, Rslt_141Args));
#ifdef STRICT
pushRedex(Rslt_141Args, newRef(glblProto76_834));
#else
swap(termLoc(Rslt_141), makePair(LAZ, 0, Rslt_141Args, newRef(glblProto76_834)));
#endif
// call vect-reduce at core: 977
Term Rslt_142Args = makePair(APP, 0, newRef(glblProto38_838), SUB);
Term Rslt_142 = newTerm(VAR, 0, portLoc(2, Rslt_142Args));
Rslt_142Args = makePair(APP, 0, glblVal175_17, Rslt_142Args);
Rslt_142Args = makePair(APP, 0, newI60(1), Rslt_142Args);
Rslt_142Args = makePair(APP, 0, v_138_1, Rslt_142Args);
Rslt_142Args = makePair(APP, 0, Rslt_141, Rslt_142Args);
#ifdef STRICT
pushRedex(Rslt_142Args, newRef(glblProto496_836));
#else
swap(termLoc(Rslt_142), makePair(LAZ, 0, Rslt_142Args, newRef(glblProto496_836)));
#endif
// link args to body
swap(r_seq_139, Rslt_142);

Term Rslt_142V = get(termLoc(Rslt_142));
apps = take(portLoc(1, Rslt_142V));
lams = take(portLoc(2, Rslt_142V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_142), makePair(LAZ, 0, apps, lams));
pushRedex(args, seq_139);
return;
}
Term glblflatten702 = newRef(glblflatten701);
void glblProto76_841(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(980), args);
  args = makePair(APP, 0, glblStr199, args);
  interact(args, glblProto76);
}
}
void glblProto68_843(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(982), args);
  args = makePair(APP, 0, glblStr199, args);
  interact(args, glblProto68);
}
}
void glblProto34_845(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(981), args);
  args = makePair(APP, 0, glblStr199, args);
  interact(args, glblProto34);
}
}
void glblProto496_847(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(980), args);
  args = makePair(APP, 0, glblStr199, args);
  interact(args, glblProto496);
}
}
Term glblfilter700;
void glblfilter699(Term ref, Term args) {
Term apps, lams, V;
Location vLoc;
Term seq_146 = makePair(LAM, 0, SUB, NUL);
Location r_seq_146 = portLoc(2, seq_146);
Term pred_145 = newTerm(VAR, 0, portLoc(1, seq_146));
seq_146 = makePair(LAM, 0, SUB, seq_146);
Term v_144 = newTerm(VAR, 0, portLoc(1, seq_146));
// allocate args at core: 979

Term v_144_1;
v_144 = dupeArg(v_144, &v_144_1, 64);
Term seq_152 = makePair(LAM, 0, SUB, NUL);
Location r_seq_152 = portLoc(2, seq_152);
Term x_151 = newTerm(VAR, 0, portLoc(1, seq_152));
seq_152 = makePair(LAM, 0, SUB, seq_152);
Term result_150 = newTerm(VAR, 0, portLoc(1, seq_152));
// allocate args at core: 980

Term result_150_1;
result_150 = dupeArg(result_150, &result_150_1, 0);

Term x_151_1;
x_151 = dupeArg(x_151, &x_151_1, 0);
Term glblVal175_18;
glblVal175 = dupeArg(glblVal175, &glblVal175_18, 0);

// call first at core: 980
Term Rslt_148Args = makePair(APP, 0, v_144, SUB);
Term Rslt_148 = newTerm(VAR, 0, portLoc(2, Rslt_148Args));
#ifdef STRICT
pushRedex(Rslt_148Args, newRef(glblProto76_841));
#else
swap(termLoc(Rslt_148), makePair(LAZ, 0, Rslt_148Args, newRef(glblProto76_841)));
#endif
// call pred at core: 979
Term Rslt_154Args = makePair(APP, 0, x_151, SUB);
Term Rslt_154 = newTerm(VAR, 0, portLoc(2, Rslt_154Args));
#ifdef STRICT
pushRedex(Rslt_154Args, pred_145);
#else
swap(termLoc(Rslt_154), makePair(LAZ, 0, Rslt_154Args, pred_145));
#endif

// call conj at core: 982
Term Rslt_155Args = makePair(APP, 0, x_151_1, SUB);
Term Rslt_155 = newTerm(VAR, 0, portLoc(2, Rslt_155Args));
Rslt_155Args = makePair(APP, 0, result_150, Rslt_155Args);
#ifdef STRICT
pushRedex(Rslt_155Args, newRef(glblProto68_843));
#else
swap(termLoc(Rslt_155), makePair(LAZ, 0, Rslt_155Args, newRef(glblProto68_843)));
#endif
// call cond at core: 981
Term Rslt_156Args = makePair(APP, 0, result_150_1, SUB);
Term Rslt_156 = newTerm(VAR, 0, portLoc(2, Rslt_156Args));
Rslt_156Args = makePair(APP, 0, Rslt_155, Rslt_156Args);
Rslt_156Args = makePair(APP, 0, Rslt_154, Rslt_156Args);
#ifdef STRICT
pushRedex(Rslt_156Args, newRef(glblProto34_845));
#else
swap(termLoc(Rslt_156), makePair(LAZ, 0, Rslt_156Args, newRef(glblProto34_845)));
#endif
// link args to body
swap(r_seq_152, Rslt_156);

Term Rslt_156V = get(termLoc(Rslt_156));
apps = take(portLoc(1, Rslt_156V));
lams = take(portLoc(2, Rslt_156V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_156), makePair(LAZ, 0, apps, lams));
// call vect-reduce at core: 980
Term Rslt_157Args = makePair(APP, 0, seq_152, SUB);
Term Rslt_157 = newTerm(VAR, 0, portLoc(2, Rslt_157Args));
Rslt_157Args = makePair(APP, 0, glblVal175_18, Rslt_157Args);
Rslt_157Args = makePair(APP, 0, newI60(1), Rslt_157Args);
Rslt_157Args = makePair(APP, 0, v_144_1, Rslt_157Args);
Rslt_157Args = makePair(APP, 0, Rslt_148, Rslt_157Args);
#ifdef STRICT
pushRedex(Rslt_157Args, newRef(glblProto496_847));
#else
swap(termLoc(Rslt_157), makePair(LAZ, 0, Rslt_157Args, newRef(glblProto496_847)));
#endif
// link args to body
swap(r_seq_146, Rslt_157);

Term Rslt_157V = get(termLoc(Rslt_157));
apps = take(portLoc(1, Rslt_157V));
lams = take(portLoc(2, Rslt_157V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_157), makePair(LAZ, 0, apps, lams));
pushRedex(args, seq_146);
return;
}
Term glblfilter700 = newRef(glblfilter699);
void glblProto46_852(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(988), args);
  args = makePair(APP, 0, glblStr199, args);
  interact(args, glblProto46);
}
}
void glblProto78_854(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(989), args);
  args = makePair(APP, 0, glblStr199, args);
  interact(args, glblProto78);
}
}
Term glblinterpose698;
void glblinterpose697(Term ref, Term args) {
Term apps, lams, V;
Location vLoc;
Term seq_162 = makePair(LAM, 0, SUB, NUL);
Location r_seq_162 = portLoc(2, seq_162);
Term sep_161 = newTerm(VAR, 0, portLoc(1, seq_162));
seq_162 = makePair(LAM, 0, SUB, seq_162);
Term coll_160 = newTerm(VAR, 0, portLoc(1, seq_162));
// allocate args at core: 985
Term seq_166 = makePair(LAM, 0, SUB, NUL);
Location r_seq_166 = portLoc(2, seq_166);
Term x_165 = newTerm(VAR, 0, portLoc(1, seq_166));
// allocate args at core: 988
Term glblVal175_19;
glblVal175 = dupeArg(glblVal175, &glblVal175_19, 0);

// call vect-conj at core: 988
Term Rslt_168Args = makePair(APP, 0, sep_161, SUB);
Term Rslt_168 = newTerm(VAR, 0, portLoc(2, Rslt_168Args));
Rslt_168Args = makePair(APP, 0, glblVal175_19, Rslt_168Args);
#ifdef STRICT
pushRedex(Rslt_168Args, glblFn178);
#else
swap(termLoc(Rslt_168), makePair(LAZ, 0, Rslt_168Args, glblFn178));
#endif

// call vect-conj at core: 988
Term Rslt_169Args = makePair(APP, 0, x_165, SUB);
Term Rslt_169 = newTerm(VAR, 0, portLoc(2, Rslt_169Args));
Rslt_169Args = makePair(APP, 0, Rslt_168, Rslt_169Args);
#ifdef STRICT
pushRedex(Rslt_169Args, glblFn178);
#else
swap(termLoc(Rslt_169), makePair(LAZ, 0, Rslt_169Args, glblFn178));
#endif

// link args to body
swap(r_seq_166, Rslt_169);

Term Rslt_169V = get(termLoc(Rslt_169));
apps = take(portLoc(1, Rslt_169V));
lams = take(portLoc(2, Rslt_169V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_169), makePair(LAZ, 0, apps, lams));
// call flat-map at core: 988
Term Rslt_170Args = makePair(APP, 0, seq_166, SUB);
Term Rslt_170 = newTerm(VAR, 0, portLoc(2, Rslt_170Args));
Rslt_170Args = makePair(APP, 0, coll_160, Rslt_170Args);
#ifdef STRICT
pushRedex(Rslt_170Args, newRef(glblProto46_852));
#else
swap(termLoc(Rslt_170), makePair(LAZ, 0, Rslt_170Args, newRef(glblProto46_852)));
#endif
// call rest at core: 989
Term Rslt_172Args = makePair(APP, 0, Rslt_170, SUB);
Term Rslt_172 = newTerm(VAR, 0, portLoc(2, Rslt_172Args));
#ifdef STRICT
pushRedex(Rslt_172Args, newRef(glblProto78_854));
#else
swap(termLoc(Rslt_172), makePair(LAZ, 0, Rslt_172Args, newRef(glblProto78_854)));
#endif
// link args to body
swap(r_seq_162, Rslt_172);

Term Rslt_172V = get(termLoc(Rslt_172));
apps = take(portLoc(1, Rslt_172V));
lams = take(portLoc(2, Rslt_172V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_172), makePair(LAZ, 0, apps, lams));
pushRedex(args, seq_162);
return;
}
Term glblinterpose698 = newRef(glblinterpose697);
Term glblFn720;
void glblCFn718(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 0, &arityArgs);
  Term result = NUL;
    if (arityArgs.count == 0) {

   result = (Term)malloc_arrayNode();
move(portLoc(2, args), result);
}
return;
}
Term glblFn720 = newRef(glblCFn718);
Term glblFn744;
void glblCFn743(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 2, &arityArgs);
  Term result = NUL;
    if (arityArgs.count == 2) {
  Term m_1 = arityArgs.args[0];
  Term idx_2 = arityArgs.args[1];

  ArrayNode *node = (ArrayNode *)m_1;
  Term child = node->array[getI60(idx_2)];
  if (child != 0) {
    incRef(child, 1);
    result = some(child);
  } else
    result = nothing();
  dec_and_free((Term)m_1, 1);
move(portLoc(2, args), result);
}
return;
}
Term glblFn744 = newRef(glblCFn743);
Term glblFn750;
void glblCFn749(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 3, &arityArgs);
  Term result = NUL;
    if (arityArgs.count == 3) {
  Term m_1 = arityArgs.args[0];
  Term idx_2 = arityArgs.args[1];
  Term child_3 = arityArgs.args[2];

   ArrayNode *node = (ArrayNode *)m_1;
   int idx = getI60(idx_2);
   if (node->array[idx] == child_3) {
     dec_and_free(child_3, 1);
     result = (Term)node;
   } else {
     ArrayNode *newNode = (ArrayNode *)malloc_arrayNode();
     for (int i = 0; i < ARRAY_NODE_LEN; i++) {
       if (i != idx && node->array[i] != 0) {
         newNode->array[i] = node->array[i];
         incRef(node->array[i], 1);
       }
     }
     newNode->array[idx] = child_3;
     dec_and_free((Term)node, 1);
     result = (Term)newNode;
   }
move(portLoc(2, args), result);
}
return;
}
Term glblFn750 = newRef(glblCFn749);
Term glblFn752;
void glblCFn751(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 2, &arityArgs);
  Term result = NUL;
    if (arityArgs.count == 2) {
  Term m_1 = arityArgs.args[0];
  Term idx_2 = arityArgs.args[1];

  ArrayNode *node = (ArrayNode *)m_1;
  Term child = node->array[getI60(idx_2)];
  if (child != 0)
    result = some(child);
  else
    result = nothing();
  dec_and_free((Term)m_1, 1);
move(portLoc(2, args), result);
}
return;
}
Term glblFn752 = newRef(glblCFn751);
Term glblFn754;
void glblCFn753(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 3, &arityArgs);
  Term result = NUL;
    if (arityArgs.count == 3) {
  Term m_1 = arityArgs.args[0];
  Term idx_2 = arityArgs.args[1];
  Term child_3 = arityArgs.args[2];

   ((ArrayNode *)m_1)->array[getI60(idx_2)] = child_3;
   result = (Term)m_1;
move(portLoc(2, args), result);
}
return;
}
Term glblFn754 = newRef(glblCFn753);
Term glblFn756;
void glblCFn755(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  Term result = NUL;
    if (arityArgs.count == 1) {
  Term m_1 = arityArgs.args[0];
result = (Term)bmiCount((Value *)m_1);
move(portLoc(2, args), result);
}
return;
}
Term glblFn756 = newRef(glblCFn755);
Term glblFn758;
void glblCFn757(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  Term result = NUL;
    if (arityArgs.count == 1) {
  Term m_1 = arityArgs.args[0];
result = (Term)arrayNodeCount((Value *)m_1);
move(portLoc(2, args), result);
}
return;
}
Term glblFn758 = newRef(glblCFn757);
Term glblFn760;
void glblCFn759(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  Term result = NUL;
    if (arityArgs.count == 1) {
  Term m_1 = arityArgs.args[0];
result = (Term)bmiHashVec((Value *)m_1, (Value *)empty_vect);
move(portLoc(2, args), result);
}
return;
}
Term glblFn760 = newRef(glblCFn759);
Term glblFn762;
void glblCFn761(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 5, &arityArgs);
  Term result = NUL;
    if (arityArgs.count == 5) {
  Term m_1 = arityArgs.args[0];
  Term k_2 = arityArgs.args[1];
  Term v_3 = arityArgs.args[2];
  Term hash_4 = arityArgs.args[3];
  Term shift_5 = arityArgs.args[4];

    result = (Term)addCopiedBMI((BitmapIndexedNode *)m_1, k_2, v_3, getI60(hash_4), getI60(shift_5));

move(portLoc(2, args), result);
}
return;
}
Term glblFn762 = newRef(glblCFn761);
Term glblFn764;
void glblCFn763(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  Term result = NUL;
    if (arityArgs.count == 1) {
  Term bmi_1 = arityArgs.args[0];

   result = newI60((int64_t)((uint32_t)((BitmapIndexedNode *)bmi_1)->bitmap));
   dec_and_free((Term)bmi_1, 1);

move(portLoc(2, args), result);
}
return;
}
Term glblFn764 = newRef(glblCFn763);
Term glblFn766;
void glblCFn765(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 2, &arityArgs);
  Term result = NUL;
    if (arityArgs.count == 2) {
  Term hash_1 = arityArgs.args[0];
  Term shift_2 = arityArgs.args[1];

   result = newI60((int64_t)((uint32_t)bitpos(getI60(hash_1), getI60(shift_2))));

move(portLoc(2, args), result);
}
return;
}
Term glblFn766 = newRef(glblCFn765);
Term glblFn768;
void glblCFn767(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 4, &arityArgs);
  Term result = NUL;
    if (arityArgs.count == 4) {
  Term bmi_1 = arityArgs.args[0];
  Term bit_2 = arityArgs.args[1];
  Term key_3 = arityArgs.args[2];
  Term val_4 = arityArgs.args[3];

   result = (Term)bmiClone((BitmapIndexedNode *)bmi_1, getI60(bit_2), key_3, val_4);
move(portLoc(2, args), result);
}
return;
}
Term glblFn768 = newRef(glblCFn767);
Term glblFn770;
void glblCFn769(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 3, &arityArgs);
  Term result = NUL;
    if (arityArgs.count == 3) {
  Term bmi_1 = arityArgs.args[0];
  Term bit_2 = arityArgs.args[1];
  Term child_3 = arityArgs.args[2];

   result = (Term)bmiUpdate((BitmapIndexedNode *)bmi_1, getI60(bit_2), child_3);
move(portLoc(2, args), result);
}
return;
}
Term glblFn770 = newRef(glblCFn769);
Term glblFn774;
void glblCFn773(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 2, &arityArgs);
  Term result = NUL;
    if (arityArgs.count == 2) {
  Term bmi_1 = arityArgs.args[0];
  Term bit_2 = arityArgs.args[1];

  BitmapIndexedNode *node = (BitmapIndexedNode *)bmi_1; 
  int idx = __builtin_popcount(node->bitmap & (getI60(bit_2) - 1));
  Term child;
  if (node->array[2 * idx] == 0) {
    child = node->array[2 * idx + 1];
    incRef(child, 1);
    result = some(child);
  } else
    result = nothing();
  dec_and_free(bmi_1, 1);
move(portLoc(2, args), result);
}
return;
}
Term glblFn774 = newRef(glblCFn773);
Term glblFn796;
void glblCFn795(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 2, &arityArgs);
  Term result = NUL;
    if (arityArgs.count == 2) {
  Term bmi_1 = arityArgs.args[0];
  Term bit_2 = arityArgs.args[1];

  BitmapIndexedNode *node = (BitmapIndexedNode *)bmi_1; 
  int idx = __builtin_popcount(node->bitmap & (getI60(bit_2) - 1));
  Term child;
  if (node->array[2 * idx] == 0) {
    child = node->array[2 * idx + 1];
    result = some(child);
  } else
    result = nothing();
  dec_and_free(bmi_1, 1);
move(portLoc(2, args), result);
}
return;
}
Term glblFn796 = newRef(glblCFn795);
Term glblFn821;
void glblCFn820(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 2, &arityArgs);
  Term result = NUL;
    if (arityArgs.count == 2) {
  Term bmi_1 = arityArgs.args[0];
  Term bit_2 = arityArgs.args[1];

   result = incRef((Term)bmiKey((BitmapIndexedNode *)bmi_1, getI60(bit_2)), 1);
   dec_and_free(bmi_1, 1);
move(portLoc(2, args), result);
}
return;
}
Term glblFn821 = newRef(glblCFn820);
Term glblFn850;
void glblCFn849(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 2, &arityArgs);
  Term result = NUL;
    if (arityArgs.count == 2) {
  Term bmi_1 = arityArgs.args[0];
  Term bit_2 = arityArgs.args[1];

   result = incRef((Term)bmiVal((BitmapIndexedNode *)bmi_1, getI60(bit_2)), 1);
   dec_and_free(bmi_1, 1);
move(portLoc(2, args), result);
}
return;
}
Term glblFn850 = newRef(glblCFn849);
Term glblFn867;
void glblCFn866(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  Term result = NUL;
    if (arityArgs.count == 1) {
  Term bmi_1 = arityArgs.args[0];

   result = newI60(__builtin_popcount(((BitmapIndexedNode *)bmi_1)->bitmap));
   dec_and_free((Term)bmi_1, 1);
move(portLoc(2, args), result);
}
return;
}
Term glblFn867 = newRef(glblCFn866);
Term glblFn872;
void glblCFn871(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 2, &arityArgs);
  Term result = NUL;
    if (arityArgs.count == 2) {
  Term bmi_1 = arityArgs.args[0];
  Term bit_2 = arityArgs.args[1];

   extern BitmapIndexedNode *malloc_bmiNode(int itemCount);
   BitmapIndexedNode *node = (BitmapIndexedNode *)bmi_1;
   int bit = getI60(bit_2);
   int idx = __builtin_popcount(node->bitmap & (bit - 1));
   int itemCount = __builtin_popcount(node->bitmap);
   BitmapIndexedNode *newNode = malloc_bmiNode(itemCount - 1);
   newNode->bitmap = node->bitmap;
   for (int i = 0, j = 0; i < itemCount; i++) {
     if (i != idx) {
       if (node->array[i * 2] != 0) {
         incRef((Term)node->array[i * 2], 1);
       }
       if (node->array[i * 2 + 1] != 0) {
         incRef((Term)node->array[i * 2 + 1], 1);
       }
       newNode->array[j * 2] = node->array[i * 2];
       newNode->array[j * 2 + 1] = node->array[i * 2 + 1];
       j++;
     }
   }
   newNode->bitmap &= ~bit;
   dec_and_free((Term)bmi_1, 1);
   result = (Term)newNode;
move(portLoc(2, args), result);
}
return;
}
Term glblFn872 = newRef(glblCFn871);
Term glblFn876;
void glblCFn875(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 0, &arityArgs);
  Term result = NUL;
    if (arityArgs.count == 0) {

   result = (Term)malloc_hashCollisionNode(2);
move(portLoc(2, args), result);
}
return;
}
Term glblFn876 = newRef(glblCFn875);
Term glblFn880;
void glblCFn879(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  Term result = NUL;
    if (arityArgs.count == 1) {
  Term m_1 = arityArgs.args[0];

   result = newI60((int64_t)((uint32_t)(((HashCollisionNode *)m_1)->count / 2)));
   dec_and_free((Term)m_1, 1);
move(portLoc(2, args), result);
}
return;
}
Term glblFn880 = newRef(glblCFn879);
Term glblFn885;
void glblCFn884(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 2, &arityArgs);
  Term result = NUL;
    if (arityArgs.count == 2) {
  Term m_1 = arityArgs.args[0];
  Term i_2 = arityArgs.args[1];

   result = incRef((Term)((HashCollisionNode *)m_1)->array[2 * getI60(i_2)], 1);
   dec_and_free((Term)m_1, 1);
move(portLoc(2, args), result);
}
return;
}
Term glblFn885 = newRef(glblCFn884);
Term glblFn890;
void glblCFn889(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 2, &arityArgs);
  Term result = NUL;
    if (arityArgs.count == 2) {
  Term m_1 = arityArgs.args[0];
  Term i_2 = arityArgs.args[1];

   result = incRef((Term)((HashCollisionNode *)m_1)->array[2 * getI60(i_2) + 1], 1);
   dec_and_free((Term)m_1, 1);
move(portLoc(2, args), result);
}
return;
}
Term glblFn890 = newRef(glblCFn889);
Term glblFn893;
void glblCFn892(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 4, &arityArgs);
  Term result = NUL;
    if (arityArgs.count == 4) {
  Term m_1 = arityArgs.args[0];
  Term i_2 = arityArgs.args[1];
  Term k_3 = arityArgs.args[2];
  Term v_4 = arityArgs.args[3];

   HashCollisionNode *node = (HashCollisionNode *)m_1;
   int64_t i = getI60(i_2);
   int itemCount = node->count / 2;
   HashCollisionNode *newNode = malloc_hashCollisionNode(itemCount + 1);
   for (int j = 0; j < itemCount; j++) {
     if (j == i) {
       newNode->array[2 * j] = (Value *)k_3;
       newNode->array[2 * j + 1] = (Value *)v_4;
     } else {
       newNode->array[2 * j] = node->array[2 * j];
       newNode->array[2 * j + 1] = node->array[2 * j + 1];
       incRef((Term)node->array[2 * j], 1);
       incRef((Term)node->array[2 * j + 1], 1);
     }
   }
   newNode->count = 2 * itemCount;
   dec_and_free((Term)m_1, 1);
   result = (Term)newNode;
move(portLoc(2, args), result);
}
return;
}
Term glblFn893 = newRef(glblCFn892);
Term glblFn896;
void glblCFn895(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 3, &arityArgs);
  Term result = NUL;
    if (arityArgs.count == 3) {
  Term m_1 = arityArgs.args[0];
  Term k_2 = arityArgs.args[1];
  Term v_3 = arityArgs.args[2];

   HashCollisionNode *node = (HashCollisionNode *)m_1;
   int itemCount = node->count / 2;
   HashCollisionNode *newNode = malloc_hashCollisionNode(itemCount + 1);
   for (int j = 0; j < itemCount; j++) {
     newNode->array[2 * j] = node->array[2 * j];
     newNode->array[2 * j + 1] = node->array[2 * j + 1];
     incRef((Term)node->array[2 * j], 1);
     incRef((Term)node->array[2 * j + 1], 1);
   }
   newNode->array[2 * itemCount] = (Value *)k_2;
   newNode->array[2 * itemCount + 1] = (Value *)v_3;
   dec_and_free((Term)m_1, 1);
   result = (Term)newNode;
move(portLoc(2, args), result);
}
return;
}
Term glblFn896 = newRef(glblCFn895);
Term glblFn899;
void glblCFn898(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 2, &arityArgs);
  Term result = NUL;
    if (arityArgs.count == 2) {
  Term m_1 = arityArgs.args[0];
  Term i_2 = arityArgs.args[1];

   HashCollisionNode *node = (HashCollisionNode *)m_1;
   int64_t i = getI60(i_2);
   int itemCount = node->count / 2;
   HashCollisionNode *newNode = malloc_hashCollisionNode(itemCount - 1);
   for (int j = 0, k = 0; j < itemCount; j++) {
     if (j != i) {
       newNode->array[2 * k] = node->array[2 * j];
       newNode->array[2 * k + 1] = node->array[2 * j + 1];
       incRef((Term)newNode->array[2 * k], 1);
       incRef((Term)newNode->array[2 * k + 1], 1);
       k++;
     }
   }
   dec_and_free((Term)m_1, 1);
   result = (Term)newNode;
move(portLoc(2, args), result);
}
return;
}
Term glblFn899 = newRef(glblCFn898);
Term glblFn902;
void glblCFn901(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 2, &arityArgs);
  Term result = NUL;
    if (arityArgs.count == 2) {
  Term m_1 = arityArgs.args[0];
  Term v_2 = arityArgs.args[1];

   result = (Term)collisionVec((Value *)m_1, (Value *)v_2);
move(portLoc(2, args), result);
}
return;
}
Term glblFn902 = newRef(glblCFn901);
Term glblFn905;
void glblCFn904(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 7, &arityArgs);
  Term result = NUL;
    if (arityArgs.count == 7) {
  Term bmi_1 = arityArgs.args[0];
  Term key_2 = arityArgs.args[1];
  Term val_3 = arityArgs.args[2];
  Term hash_4 = arityArgs.args[3];
  Term shift_5 = arityArgs.args[4];
  Term currKey_6 = arityArgs.args[5];
  Term currVal_7 = arityArgs.args[6];

   result = (Term)bmiReplaceCopied((BitmapIndexedNode *)bmi_1, key_2, val_3, getI60(hash_4), getI60(shift_5), currKey_6, currVal_7);
   dec_and_free(currKey_6, 1);
   dec_and_free(currVal_7, 1);
move(portLoc(2, args), result);
}
return;
}
Term glblFn905 = newRef(glblCFn904);
Term glblFn908;
void glblCFn907(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 7, &arityArgs);
  Term result = NUL;
    if (arityArgs.count == 7) {
  Term bmi_1 = arityArgs.args[0];
  Term key_2 = arityArgs.args[1];
  Term val_3 = arityArgs.args[2];
  Term hash_4 = arityArgs.args[3];
  Term shift_5 = arityArgs.args[4];
  Term currKey_6 = arityArgs.args[5];
  Term currVal_7 = arityArgs.args[6];

   result = (Term)bmiReplaceMutate((BitmapIndexedNode *)bmi_1, key_2, val_3, getI60(hash_4), getI60(shift_5), currKey_6, currVal_7);
   dec_and_free(currKey_6, 1);
   dec_and_free(currVal_7, 1);
move(portLoc(2, args), result);
}
return;
}
Term glblFn908 = newRef(glblCFn907);
Term glblFn911;
void glblCFn910(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 6, &arityArgs);
  Term result = NUL;
    if (arityArgs.count == 6) {
  Term bmi_1 = arityArgs.args[0];
  Term bit_2 = arityArgs.args[1];
  Term k_3 = arityArgs.args[2];
  Term v_4 = arityArgs.args[3];
  Term currKey_5 = arityArgs.args[4];
  Term currVal_6 = arityArgs.args[5];

    bmiSetKey((BitmapIndexedNode *)bmi_1, getI60(bit_2), k_3);
    bmiSetVal((BitmapIndexedNode *)bmi_1, getI60(bit_2), v_4);
    dec_and_free(currKey_5, 2);
    dec_and_free(currVal_6, 2);
    result = (Term)bmi_1;
move(portLoc(2, args), result);
}
return;
}
Term glblFn911 = newRef(glblCFn910);
Term glblFn914;
void glblCFn913(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 3, &arityArgs);
  Term result = NUL;
    if (arityArgs.count == 3) {
  Term bmi_1 = arityArgs.args[0];
  Term bit_2 = arityArgs.args[1];
  Term n_3 = arityArgs.args[2];

    bmiSetKey((BitmapIndexedNode *)bmi_1, getI60(bit_2), 0);
    bmiSetVal((BitmapIndexedNode *)bmi_1, getI60(bit_2), n_3);
    result = (Term)bmi_1;
move(portLoc(2, args), result);
}
return;
}
Term glblFn914 = newRef(glblCFn913);
Term glblFn917;
void glblCFn916(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 2, &arityArgs);
  Term result = NUL;
    if (arityArgs.count == 2) {
  Term k_1 = arityArgs.args[0];
  Term hash_2 = arityArgs.args[1];

    if (termTag(k_1) == VAL) {
      Value *v = (Value *)k_1;
      switch (v->type) {
      case StringBufferType:
        ((String *)v)->hashVal = getI60(hash_2);
        break;
      case VectorType:
        ((Vector *)v)->hashVal = getI60(hash_2);
        break;
      case BitmapIndexedType:
        ((BitmapIndexedNode *)v)->hashVal = getI60(hash_2);
        break;
      case ArrayNodeType:
        ((ArrayNode *)v)->hashVal = getI60(hash_2);
        break;
      case HashCollisionNodeType:
        ((HashCollisionNode *)v)->hashVal = getI60(hash_2);
        break;
      default:
        // user deftypes (type >= CoreTypeCount) are ReifiedVals
        if (v->type >= CoreTypeCount)
          ((ReifiedVal *)v)->hashVal = getI60(hash_2);
        break;
      }
    }
    dec_and_free(hash_2, 1);
    result = k_1;
move(portLoc(2, args), result);
}
return;
}
Term glblFn917 = newRef(glblCFn916);
void glblProto20_923(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(1474), args);
  args = makePair(APP, 0, glblStr199, args);
  interact(args, glblProto20);
}
}
void glblProto38_925(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(1480), args);
  args = makePair(APP, 0, glblStr199, args);
  interact(args, glblProto38);
}
}
void glblProto42_927(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(1478), args);
  args = makePair(APP, 0, glblStr199, args);
  interact(args, glblProto42);
}
}
void glblProto38_929(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(1484), args);
  args = makePair(APP, 0, glblStr199, args);
  interact(args, glblProto38);
}
}
void glblProto32_931(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(1478), args);
  args = makePair(APP, 0, glblStr199, args);
  interact(args, glblProto32);
}
}
void glblProto34_933(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(1474), args);
  args = makePair(APP, 0, glblStr199, args);
  interact(args, glblProto34);
}
}
Term glblFn920;
void glblCFn919(Term ref, Term args) {
Term apps, lams, V;
Location vLoc;
Term seq_3 = makePair(LAM, 0, SUB, NUL);
Location r_seq_3 = portLoc(2, seq_3);
Term bm_2 = newTerm(VAR, 0, portLoc(1, seq_3));
seq_3 = makePair(LAM, 0, SUB, seq_3);
Term m_1 = newTerm(VAR, 0, portLoc(1, seq_3));
// allocate args at core: 1471

Term m_1_4;
m_1 = dupeArg(m_1, &m_1_4, 65);

Term m_1_3;
m_1 = dupeArg(m_1, &m_1_3, 66);

Term m_1_2;
m_1 = dupeArg(m_1, &m_1_2, 67);

Term m_1_1;
m_1 = dupeArg(m_1, &m_1_1, 68);

Term bm_2_3;
bm_2 = dupeArg(bm_2, &bm_2_3, 69);

Term bm_2_2;
bm_2 = dupeArg(bm_2, &bm_2_2, 70);

Term bm_2_1;
bm_2 = dupeArg(bm_2, &bm_2_1, 71);
Term seq_8 = makePair(LAM, 0, SUB, NUL);
Location r_seq_8 = portLoc(2, seq_8);
Term bit_7 = newTerm(VAR, 0, portLoc(1, seq_8));
// allocate args at core: 1476

Term bit_7_3;
bit_7 = dupeArg(bit_7, &bit_7_3, 0);

Term bit_7_2;
bit_7 = dupeArg(bit_7, &bit_7_2, 0);

Term bit_7_1;
bit_7 = dupeArg(bit_7, &bit_7_1, 0);
Term seq_12 = makePair(LAM, 0, SUB, NUL);
Location r_seq_12 = portLoc(2, seq_12);
Term rest_bm_11 = newTerm(VAR, 0, portLoc(1, seq_12));
// allocate args at core: 1477

Term rest_bm_11_1;
rest_bm_11 = dupeArg(rest_bm_11, &rest_bm_11_1, 0);
Term seq_27 = makePair(LAM, 0, SUB, NUL);
Location r_seq_27 = portLoc(2, seq_27);
Term k_26 = newTerm(VAR, 0, portLoc(1, seq_27));
// allocate args at core: 1482
Term seq_31 = makePair(LAM, 0, SUB, NUL);
Location r_seq_31 = portLoc(2, seq_31);
Term v_30 = newTerm(VAR, 0, portLoc(1, seq_31));
// allocate args at core: 1483
Term glblVal175_22;
glblVal175 = dupeArg(glblVal175, &glblVal175_22, 0);

Term glblVal175_21;
glblVal175 = dupeArg(glblVal175, &glblVal175_21, 0);

Term seq_17 = makePair(LAM, 0, SUB, NUL);
Location r_seq_17 = portLoc(2, seq_17);
Term child_16 = newTerm(VAR, 0, portLoc(1, seq_17));
// allocate args at core: 1479

Term child_16_1;
child_16 = dupeArg(child_16, &child_16_1, 0);
Term glblVal175_20;
glblVal175 = dupeArg(glblVal175, &glblVal175_20, 0);

// call = at core: 1474
Term Rslt_5Args = makePair(APP, 0, bm_2, SUB);
Term Rslt_5 = newTerm(VAR, 0, portLoc(2, Rslt_5Args));
Rslt_5Args = makePair(APP, 0, newI60(0), Rslt_5Args);
#ifdef STRICT
pushRedex(Rslt_5Args, newRef(glblProto20_923));
#else
swap(termLoc(Rslt_5), makePair(LAZ, 0, Rslt_5Args, newRef(glblProto20_923)));
#endif
// call bmiCopyChild at core: 1478
Term Rslt_14Args = makePair(APP, 0, bit_7, SUB);
Term Rslt_14 = newTerm(VAR, 0, portLoc(2, Rslt_14Args));
Rslt_14Args = makePair(APP, 0, m_1, Rslt_14Args);
#ifdef STRICT
pushRedex(Rslt_14Args, glblFn774);
#else
swap(termLoc(Rslt_14), makePair(LAZ, 0, Rslt_14Args, glblFn774));
#endif

// call bmiBitMap at core: 1480
Term Rslt_19Args = makePair(APP, 0, child_16_1, SUB);
Term Rslt_19 = newTerm(VAR, 0, portLoc(2, Rslt_19Args));
#ifdef STRICT
pushRedex(Rslt_19Args, glblFn764);
#else
swap(termLoc(Rslt_19), makePair(LAZ, 0, Rslt_19Args, glblFn764));
#endif

// call bmiVec-bm at core: 1471
Term Rslt_20Args = makePair(APP, 0, Rslt_19, SUB);
Term Rslt_20 = newTerm(VAR, 0, portLoc(2, Rslt_20Args));
Rslt_20Args = makePair(APP, 0, child_16, Rslt_20Args);
#ifdef STRICT
pushRedex(Rslt_20Args, glblFn920);
#else
swap(termLoc(Rslt_20), makePair(LAZ, 0, Rslt_20Args, glblFn920));
#endif

// call bmiVec-bm at core: 1471
Term Rslt_21Args = makePair(APP, 0, rest_bm_11, SUB);
Term Rslt_21 = newTerm(VAR, 0, portLoc(2, Rslt_21Args));
Rslt_21Args = makePair(APP, 0, m_1_1, Rslt_21Args);
#ifdef STRICT
pushRedex(Rslt_21Args, glblFn920);
#else
swap(termLoc(Rslt_21), makePair(LAZ, 0, Rslt_21Args, glblFn920));
#endif

// call comp at core: 1480
Term Rslt_22Args = makePair(APP, 0, Rslt_21, SUB);
Term Rslt_22 = newTerm(VAR, 0, portLoc(2, Rslt_22Args));
Rslt_22Args = makePair(APP, 0, Rslt_20, Rslt_22Args);
#ifdef STRICT
pushRedex(Rslt_22Args, newRef(glblProto38_925));
#else
swap(termLoc(Rslt_22), makePair(LAZ, 0, Rslt_22Args, newRef(glblProto38_925)));
#endif
// link args to body
swap(r_seq_17, Rslt_22);

Term Rslt_22V = get(termLoc(Rslt_22));
apps = take(portLoc(1, Rslt_22V));
lams = take(portLoc(2, Rslt_22V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_22), makePair(LAZ, 0, apps, lams));
// call map at core: 1478
Term Rslt_23Args = makePair(APP, 0, seq_17, SUB);
Term Rslt_23 = newTerm(VAR, 0, portLoc(2, Rslt_23Args));
Rslt_23Args = makePair(APP, 0, Rslt_14, Rslt_23Args);
#ifdef STRICT
pushRedex(Rslt_23Args, newRef(glblProto42_927));
#else
swap(termLoc(Rslt_23), makePair(LAZ, 0, Rslt_23Args, newRef(glblProto42_927)));
#endif
// call vect-conj at core: 1484
Term Rslt_33Args = makePair(APP, 0, k_26, SUB);
Term Rslt_33 = newTerm(VAR, 0, portLoc(2, Rslt_33Args));
Rslt_33Args = makePair(APP, 0, glblVal175_22, Rslt_33Args);
#ifdef STRICT
pushRedex(Rslt_33Args, glblFn178);
#else
swap(termLoc(Rslt_33), makePair(LAZ, 0, Rslt_33Args, glblFn178));
#endif

// call vect-conj at core: 1484
Term Rslt_34Args = makePair(APP, 0, v_30, SUB);
Term Rslt_34 = newTerm(VAR, 0, portLoc(2, Rslt_34Args));
Rslt_34Args = makePair(APP, 0, Rslt_33, Rslt_34Args);
#ifdef STRICT
pushRedex(Rslt_34Args, glblFn178);
#else
swap(termLoc(Rslt_34), makePair(LAZ, 0, Rslt_34Args, glblFn178));
#endif

// call vect-conj at core: 1484
Term Rslt_35Args = makePair(APP, 0, Rslt_34, SUB);
Term Rslt_35 = newTerm(VAR, 0, portLoc(2, Rslt_35Args));
Rslt_35Args = makePair(APP, 0, glblVal175_21, Rslt_35Args);
#ifdef STRICT
pushRedex(Rslt_35Args, glblFn178);
#else
swap(termLoc(Rslt_35), makePair(LAZ, 0, Rslt_35Args, glblFn178));
#endif

// call bmiVec-bm at core: 1471
Term Rslt_36Args = makePair(APP, 0, rest_bm_11_1, SUB);
Term Rslt_36 = newTerm(VAR, 0, portLoc(2, Rslt_36Args));
Rslt_36Args = makePair(APP, 0, m_1_2, Rslt_36Args);
#ifdef STRICT
pushRedex(Rslt_36Args, glblFn920);
#else
swap(termLoc(Rslt_36), makePair(LAZ, 0, Rslt_36Args, glblFn920));
#endif

// call comp at core: 1484
Term Rslt_37Args = makePair(APP, 0, Rslt_36, SUB);
Term Rslt_37 = newTerm(VAR, 0, portLoc(2, Rslt_37Args));
Rslt_37Args = makePair(APP, 0, Rslt_35, Rslt_37Args);
#ifdef STRICT
pushRedex(Rslt_37Args, newRef(glblProto38_929));
#else
swap(termLoc(Rslt_37), makePair(LAZ, 0, Rslt_37Args, newRef(glblProto38_929)));
#endif
// link args to body
swap(r_seq_31, Rslt_37);

Term Rslt_37V = get(termLoc(Rslt_37));
apps = take(portLoc(1, Rslt_37V));
lams = take(portLoc(2, Rslt_37V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_37), makePair(LAZ, 0, apps, lams));
// call bmiVal at core: 1483
Term Rslt_38Args = makePair(APP, 0, bit_7_1, SUB);
Term Rslt_38 = newTerm(VAR, 0, portLoc(2, Rslt_38Args));
Rslt_38Args = makePair(APP, 0, m_1_3, Rslt_38Args);
#ifdef STRICT
pushRedex(Rslt_38Args, glblFn850);
#else
swap(termLoc(Rslt_38), makePair(LAZ, 0, Rslt_38Args, glblFn850));
#endif

// call  at core: 1483
Term Rslt_39Args = makePair(APP, 0, Rslt_38, SUB);
Term Rslt_39 = newTerm(VAR, 0, portLoc(2, Rslt_39Args));
#ifdef STRICT
pushRedex(Rslt_39Args, seq_31);
#else
swap(termLoc(Rslt_39), makePair(LAZ, 0, Rslt_39Args, seq_31));
#endif

// link args to body
swap(r_seq_27, Rslt_39);

Term Rslt_39V = get(termLoc(Rslt_39));
apps = take(portLoc(1, Rslt_39V));
lams = take(portLoc(2, Rslt_39V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_39), makePair(LAZ, 0, apps, lams));
// call bmiKey at core: 1482
Term Rslt_40Args = makePair(APP, 0, bit_7_2, SUB);
Term Rslt_40 = newTerm(VAR, 0, portLoc(2, Rslt_40Args));
Rslt_40Args = makePair(APP, 0, m_1_4, Rslt_40Args);
#ifdef STRICT
pushRedex(Rslt_40Args, glblFn821);
#else
swap(termLoc(Rslt_40), makePair(LAZ, 0, Rslt_40Args, glblFn821));
#endif

// call  at core: 1482
Term Rslt_41Args = makePair(APP, 0, Rslt_40, SUB);
Term Rslt_41 = newTerm(VAR, 0, portLoc(2, Rslt_41Args));
#ifdef STRICT
pushRedex(Rslt_41Args, seq_27);
#else
swap(termLoc(Rslt_41), makePair(LAZ, 0, Rslt_41Args, seq_27));
#endif

// call either at core: 1478
Term Rslt_42Args = makePair(APP, 0, Rslt_41, SUB);
Term Rslt_42 = newTerm(VAR, 0, portLoc(2, Rslt_42Args));
Rslt_42Args = makePair(APP, 0, Rslt_23, Rslt_42Args);
#ifdef STRICT
pushRedex(Rslt_42Args, newRef(glblProto32_931));
#else
swap(termLoc(Rslt_42), makePair(LAZ, 0, Rslt_42Args, newRef(glblProto32_931)));
#endif
// link args to body
swap(r_seq_12, Rslt_42);

Term Rslt_42V = get(termLoc(Rslt_42));
apps = take(portLoc(1, Rslt_42V));
lams = take(portLoc(2, Rslt_42V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_42), makePair(LAZ, 0, apps, lams));
// call bit-not at core: 1477
Term Rslt_43Args = makePair(APP, 0, bit_7_3, SUB);
Term Rslt_43 = newTerm(VAR, 0, portLoc(2, Rslt_43Args));
#ifdef STRICT
pushRedex(Rslt_43Args, glblFn168);
#else
swap(termLoc(Rslt_43), makePair(LAZ, 0, Rslt_43Args, glblFn168));
#endif

Term Rslt_44 = makeOp(OP_AND, bm_2_1, Rslt_43);
// call  at core: 1477
Term Rslt_45Args = makePair(APP, 0, Rslt_44, SUB);
Term Rslt_45 = newTerm(VAR, 0, portLoc(2, Rslt_45Args));
#ifdef STRICT
pushRedex(Rslt_45Args, seq_12);
#else
swap(termLoc(Rslt_45), makePair(LAZ, 0, Rslt_45Args, seq_12));
#endif

// link args to body
swap(r_seq_8, Rslt_45);

Term Rslt_45V = get(termLoc(Rslt_45));
apps = take(portLoc(1, Rslt_45V));
lams = take(portLoc(2, Rslt_45V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_45), makePair(LAZ, 0, apps, lams));
// call dec at core: 1476
Term Rslt_46Args = makePair(APP, 0, bm_2_3, SUB);
Term Rslt_46 = newTerm(VAR, 0, portLoc(2, Rslt_46Args));
#ifdef STRICT
pushRedex(Rslt_46Args, glblFn147);
#else
swap(termLoc(Rslt_46), makePair(LAZ, 0, Rslt_46Args, glblFn147));
#endif

// call bit-not at core: 1476
Term Rslt_47Args = makePair(APP, 0, Rslt_46, SUB);
Term Rslt_47 = newTerm(VAR, 0, portLoc(2, Rslt_47Args));
#ifdef STRICT
pushRedex(Rslt_47Args, glblFn168);
#else
swap(termLoc(Rslt_47), makePair(LAZ, 0, Rslt_47Args, glblFn168));
#endif

Term Rslt_48 = makeOp(OP_AND, bm_2_2, Rslt_47);
// call  at core: 1476
Term Rslt_49Args = makePair(APP, 0, Rslt_48, SUB);
Term Rslt_49 = newTerm(VAR, 0, portLoc(2, Rslt_49Args));
#ifdef STRICT
pushRedex(Rslt_49Args, seq_8);
#else
swap(termLoc(Rslt_49), makePair(LAZ, 0, Rslt_49Args, seq_8));
#endif

// call cond at core: 1474
Term Rslt_50Args = makePair(APP, 0, Rslt_49, SUB);
Term Rslt_50 = newTerm(VAR, 0, portLoc(2, Rslt_50Args));
Rslt_50Args = makePair(APP, 0, glblVal175_20, Rslt_50Args);
Rslt_50Args = makePair(APP, 0, Rslt_5, Rslt_50Args);
#ifdef STRICT
pushRedex(Rslt_50Args, newRef(glblProto34_933));
#else
swap(termLoc(Rslt_50), makePair(LAZ, 0, Rslt_50Args, newRef(glblProto34_933)));
#endif
// link args to body
swap(r_seq_3, Rslt_50);

Term Rslt_50V = get(termLoc(Rslt_50));
apps = take(portLoc(1, Rslt_50V));
lams = take(portLoc(2, Rslt_50V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_50), makePair(LAZ, 0, apps, lams));
pushRedex(args, seq_3);
return;
}
Term glblFn920 = newRef(glblCFn919);
Term glblFn922;
void glblCFn921(Term ref, Term args) {
Term apps, lams, V;
Location vLoc;
Term seq_2 = makePair(LAM, 0, SUB, NUL);
Location r_seq_2 = portLoc(2, seq_2);
Term m_1 = newTerm(VAR, 0, portLoc(1, seq_2));
// allocate args at core: 1486

Term m_1_1;
m_1 = dupeArg(m_1, &m_1_1, 72);
// call bmiBitMap at core: 1488
Term Rslt_4Args = makePair(APP, 0, m_1_1, SUB);
Term Rslt_4 = newTerm(VAR, 0, portLoc(2, Rslt_4Args));
#ifdef STRICT
pushRedex(Rslt_4Args, glblFn764);
#else
swap(termLoc(Rslt_4), makePair(LAZ, 0, Rslt_4Args, glblFn764));
#endif

// call bmiVec-bm at core: 1488
Term Rslt_5Args = makePair(APP, 0, Rslt_4, SUB);
Term Rslt_5 = newTerm(VAR, 0, portLoc(2, Rslt_5Args));
Rslt_5Args = makePair(APP, 0, m_1, Rslt_5Args);
#ifdef STRICT
pushRedex(Rslt_5Args, glblFn920);
#else
swap(termLoc(Rslt_5), makePair(LAZ, 0, Rslt_5Args, glblFn920));
#endif

// link args to body
swap(r_seq_2, Rslt_5);

Term Rslt_5V = get(termLoc(Rslt_5));
apps = take(portLoc(1, Rslt_5V));
lams = take(portLoc(2, Rslt_5V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_5), makePair(LAZ, 0, apps, lams));
pushRedex(args, seq_2);
return;
}
Term glblFn922 = newRef(glblCFn921);
void glblProto20_941(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(1500), args);
  args = makePair(APP, 0, glblStr199, args);
  interact(args, glblProto20);
}
}
void glblProto74_943(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(1504), args);
  args = makePair(APP, 0, glblStr199, args);
  interact(args, glblProto74);
}
}
void glblProto38_945(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(1504), args);
  args = makePair(APP, 0, glblStr199, args);
  interact(args, glblProto38);
}
}
void glblProto42_947(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(1502), args);
  args = makePair(APP, 0, glblStr199, args);
  interact(args, glblProto42);
}
}
void glblProto32_949(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(1502), args);
  args = makePair(APP, 0, glblStr199, args);
  interact(args, glblProto32);
}
}
void glblProto34_951(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(1500), args);
  args = makePair(APP, 0, glblStr199, args);
  interact(args, glblProto34);
}
}
Term glblFn938;
void glblCFn937(Term ref, Term args) {
Term apps, lams, V;
Location vLoc;
Term seq_3 = makePair(LAM, 0, SUB, NUL);
Location r_seq_3 = portLoc(2, seq_3);
Term i_2 = newTerm(VAR, 0, portLoc(1, seq_3));
seq_3 = makePair(LAM, 0, SUB, seq_3);
Term m_1 = newTerm(VAR, 0, portLoc(1, seq_3));
// allocate args at core: 1497

Term m_1_2;
m_1 = dupeArg(m_1, &m_1_2, 73);

Term m_1_1;
m_1 = dupeArg(m_1, &m_1_1, 74);

Term i_2_3;
i_2 = dupeArg(i_2, &i_2_3, 75);

Term i_2_2;
i_2 = dupeArg(i_2, &i_2_2, 76);

Term i_2_1;
i_2 = dupeArg(i_2, &i_2_1, 77);
Term seq_9 = makePair(LAM, 0, SUB, NUL);
Location r_seq_9 = portLoc(2, seq_9);
Term child_8 = newTerm(VAR, 0, portLoc(1, seq_9));
// allocate args at core: 1503
Term glblVal175_23;
glblVal175 = dupeArg(glblVal175, &glblVal175_23, 0);

// call = at core: 1500
Term Rslt_5Args = makePair(APP, 0, newI60(32), SUB);
Term Rslt_5 = newTerm(VAR, 0, portLoc(2, Rslt_5Args));
Rslt_5Args = makePair(APP, 0, i_2, Rslt_5Args);
#ifdef STRICT
pushRedex(Rslt_5Args, newRef(glblProto20_941));
#else
swap(termLoc(Rslt_5), makePair(LAZ, 0, Rslt_5Args, newRef(glblProto20_941)));
#endif
// call arrayNodeCopyChild at core: 1502
Term Rslt_6Args = makePair(APP, 0, i_2_1, SUB);
Term Rslt_6 = newTerm(VAR, 0, portLoc(2, Rslt_6Args));
Rslt_6Args = makePair(APP, 0, m_1, Rslt_6Args);
#ifdef STRICT
pushRedex(Rslt_6Args, glblFn744);
#else
swap(termLoc(Rslt_6), makePair(LAZ, 0, Rslt_6Args, glblFn744));
#endif

// call vec at core: 1504
Term Rslt_11Args = makePair(APP, 0, child_8, SUB);
Term Rslt_11 = newTerm(VAR, 0, portLoc(2, Rslt_11Args));
#ifdef STRICT
pushRedex(Rslt_11Args, newRef(glblProto74_943));
#else
swap(termLoc(Rslt_11), makePair(LAZ, 0, Rslt_11Args, newRef(glblProto74_943)));
#endif
// call inc at core: 1504
Term Rslt_12Args = makePair(APP, 0, i_2_2, SUB);
Term Rslt_12 = newTerm(VAR, 0, portLoc(2, Rslt_12Args));
#ifdef STRICT
pushRedex(Rslt_12Args, glblFn141);
#else
swap(termLoc(Rslt_12), makePair(LAZ, 0, Rslt_12Args, glblFn141));
#endif

// call arrayNodeVec-i at core: 1497
Term Rslt_13Args = makePair(APP, 0, Rslt_12, SUB);
Term Rslt_13 = newTerm(VAR, 0, portLoc(2, Rslt_13Args));
Rslt_13Args = makePair(APP, 0, m_1_1, Rslt_13Args);
#ifdef STRICT
pushRedex(Rslt_13Args, glblFn938);
#else
swap(termLoc(Rslt_13), makePair(LAZ, 0, Rslt_13Args, glblFn938));
#endif

// call comp at core: 1504
Term Rslt_14Args = makePair(APP, 0, Rslt_13, SUB);
Term Rslt_14 = newTerm(VAR, 0, portLoc(2, Rslt_14Args));
Rslt_14Args = makePair(APP, 0, Rslt_11, Rslt_14Args);
#ifdef STRICT
pushRedex(Rslt_14Args, newRef(glblProto38_945));
#else
swap(termLoc(Rslt_14), makePair(LAZ, 0, Rslt_14Args, newRef(glblProto38_945)));
#endif
// link args to body
swap(r_seq_9, Rslt_14);

Term Rslt_14V = get(termLoc(Rslt_14));
apps = take(portLoc(1, Rslt_14V));
lams = take(portLoc(2, Rslt_14V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_14), makePair(LAZ, 0, apps, lams));
// call map at core: 1502
Term Rslt_15Args = makePair(APP, 0, seq_9, SUB);
Term Rslt_15 = newTerm(VAR, 0, portLoc(2, Rslt_15Args));
Rslt_15Args = makePair(APP, 0, Rslt_6, Rslt_15Args);
#ifdef STRICT
pushRedex(Rslt_15Args, newRef(glblProto42_947));
#else
swap(termLoc(Rslt_15), makePair(LAZ, 0, Rslt_15Args, newRef(glblProto42_947)));
#endif
// call inc at core: 1505
Term Rslt_17Args = makePair(APP, 0, i_2_3, SUB);
Term Rslt_17 = newTerm(VAR, 0, portLoc(2, Rslt_17Args));
#ifdef STRICT
pushRedex(Rslt_17Args, glblFn141);
#else
swap(termLoc(Rslt_17), makePair(LAZ, 0, Rslt_17Args, glblFn141));
#endif

// call arrayNodeVec-i at core: 1497
Term Rslt_18Args = makePair(APP, 0, Rslt_17, SUB);
Term Rslt_18 = newTerm(VAR, 0, portLoc(2, Rslt_18Args));
Rslt_18Args = makePair(APP, 0, m_1_2, Rslt_18Args);
#ifdef STRICT
pushRedex(Rslt_18Args, glblFn938);
#else
swap(termLoc(Rslt_18), makePair(LAZ, 0, Rslt_18Args, glblFn938));
#endif

// call either at core: 1502
Term Rslt_19Args = makePair(APP, 0, Rslt_18, SUB);
Term Rslt_19 = newTerm(VAR, 0, portLoc(2, Rslt_19Args));
Rslt_19Args = makePair(APP, 0, Rslt_15, Rslt_19Args);
#ifdef STRICT
pushRedex(Rslt_19Args, newRef(glblProto32_949));
#else
swap(termLoc(Rslt_19), makePair(LAZ, 0, Rslt_19Args, newRef(glblProto32_949)));
#endif
// call cond at core: 1500
Term Rslt_20Args = makePair(APP, 0, Rslt_19, SUB);
Term Rslt_20 = newTerm(VAR, 0, portLoc(2, Rslt_20Args));
Rslt_20Args = makePair(APP, 0, glblVal175_23, Rslt_20Args);
Rslt_20Args = makePair(APP, 0, Rslt_5, Rslt_20Args);
#ifdef STRICT
pushRedex(Rslt_20Args, newRef(glblProto34_951));
#else
swap(termLoc(Rslt_20), makePair(LAZ, 0, Rslt_20Args, newRef(glblProto34_951)));
#endif
// link args to body
swap(r_seq_3, Rslt_20);

Term Rslt_20V = get(termLoc(Rslt_20));
apps = take(portLoc(1, Rslt_20V));
lams = take(portLoc(2, Rslt_20V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_20), makePair(LAZ, 0, apps, lams));
pushRedex(args, seq_3);
return;
}
Term glblFn938 = newRef(glblCFn937);
Term glblFn940;
void glblCFn939(Term ref, Term args) {
Term apps, lams, V;
Location vLoc;
Term seq_2 = makePair(LAM, 0, SUB, NUL);
Location r_seq_2 = portLoc(2, seq_2);
Term m_1 = newTerm(VAR, 0, portLoc(1, seq_2));
// allocate args at core: 1507
// call arrayNodeVec-i at core: 1509
Term Rslt_4Args = makePair(APP, 0, newI60(0), SUB);
Term Rslt_4 = newTerm(VAR, 0, portLoc(2, Rslt_4Args));
Rslt_4Args = makePair(APP, 0, m_1, Rslt_4Args);
#ifdef STRICT
pushRedex(Rslt_4Args, glblFn938);
#else
swap(termLoc(Rslt_4), makePair(LAZ, 0, Rslt_4Args, glblFn938));
#endif

// link args to body
swap(r_seq_2, Rslt_4);

Term Rslt_4V = get(termLoc(Rslt_4));
apps = take(portLoc(1, Rslt_4V));
lams = take(portLoc(2, Rslt_4V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_4), makePair(LAZ, 0, apps, lams));
pushRedex(args, seq_2);
return;
}
Term glblFn940 = newRef(glblCFn939);
void glblProto20_959(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(1522), args);
  args = makePair(APP, 0, glblStr199, args);
  interact(args, glblProto20);
}
}
void glblProto121_961(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(1528), args);
  args = makePair(APP, 0, glblStr199, args);
  interact(args, glblProto121);
}
}
void glblProto42_963(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(1526), args);
  args = makePair(APP, 0, glblStr199, args);
  interact(args, glblProto42);
}
}
void glblProto68_965(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(1531), args);
  args = makePair(APP, 0, glblStr199, args);
  interact(args, glblProto68);
}
}
void glblProto32_967(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(1526), args);
  args = makePair(APP, 0, glblStr199, args);
  interact(args, glblProto32);
}
}
void glblProto34_969(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(1522), args);
  args = makePair(APP, 0, glblStr199, args);
  interact(args, glblProto34);
}
}
Term glblFn956;
void glblCFn955(Term ref, Term args) {
Term apps, lams, V;
Location vLoc;
Term seq_4 = makePair(LAM, 0, SUB, NUL);
Location r_seq_4 = portLoc(2, seq_4);
Term bm_3 = newTerm(VAR, 0, portLoc(1, seq_4));
seq_4 = makePair(LAM, 0, SUB, seq_4);
Term s_2 = newTerm(VAR, 0, portLoc(1, seq_4));
seq_4 = makePair(LAM, 0, SUB, seq_4);
Term m_1 = newTerm(VAR, 0, portLoc(1, seq_4));
// allocate args at core: 1518

Term m_1_4;
m_1 = dupeArg(m_1, &m_1_4, 78);

Term m_1_3;
m_1 = dupeArg(m_1, &m_1_3, 79);

Term m_1_2;
m_1 = dupeArg(m_1, &m_1_2, 80);

Term m_1_1;
m_1 = dupeArg(m_1, &m_1_1, 81);

Term s_2_2;
s_2 = dupeArg(s_2, &s_2_2, 82);

Term s_2_1;
s_2 = dupeArg(s_2, &s_2_1, 83);

Term bm_3_3;
bm_3 = dupeArg(bm_3, &bm_3_3, 84);

Term bm_3_2;
bm_3 = dupeArg(bm_3, &bm_3_2, 85);

Term bm_3_1;
bm_3 = dupeArg(bm_3, &bm_3_1, 86);
Term seq_9 = makePair(LAM, 0, SUB, NUL);
Location r_seq_9 = portLoc(2, seq_9);
Term bit_8 = newTerm(VAR, 0, portLoc(1, seq_9));
// allocate args at core: 1524

Term bit_8_3;
bit_8 = dupeArg(bit_8, &bit_8_3, 0);

Term bit_8_2;
bit_8 = dupeArg(bit_8, &bit_8_2, 0);

Term bit_8_1;
bit_8 = dupeArg(bit_8, &bit_8_1, 0);
Term seq_13 = makePair(LAM, 0, SUB, NUL);
Location r_seq_13 = portLoc(2, seq_13);
Term rest_bm_12 = newTerm(VAR, 0, portLoc(1, seq_13));
// allocate args at core: 1525

Term rest_bm_12_1;
rest_bm_12 = dupeArg(rest_bm_12, &rest_bm_12_1, 0);
Term seq_26 = makePair(LAM, 0, SUB, NUL);
Location r_seq_26 = portLoc(2, seq_26);
Term k_25 = newTerm(VAR, 0, portLoc(1, seq_26));
// allocate args at core: 1529
Term seq_30 = makePair(LAM, 0, SUB, NUL);
Location r_seq_30 = portLoc(2, seq_30);
Term v_29 = newTerm(VAR, 0, portLoc(1, seq_30));
// allocate args at core: 1530
Term glblVal175_24;
glblVal175 = dupeArg(glblVal175, &glblVal175_24, 0);

Term seq_18 = makePair(LAM, 0, SUB, NUL);
Location r_seq_18 = portLoc(2, seq_18);
Term child_17 = newTerm(VAR, 0, portLoc(1, seq_18));
// allocate args at core: 1527
// call = at core: 1522
Term Rslt_6Args = makePair(APP, 0, bm_3, SUB);
Term Rslt_6 = newTerm(VAR, 0, portLoc(2, Rslt_6Args));
Rslt_6Args = makePair(APP, 0, newI60(0), Rslt_6Args);
#ifdef STRICT
pushRedex(Rslt_6Args, newRef(glblProto20_959));
#else
swap(termLoc(Rslt_6), makePair(LAZ, 0, Rslt_6Args, newRef(glblProto20_959)));
#endif
// call bmiCopyChild at core: 1526
Term Rslt_15Args = makePair(APP, 0, bit_8, SUB);
Term Rslt_15 = newTerm(VAR, 0, portLoc(2, Rslt_15Args));
Rslt_15Args = makePair(APP, 0, m_1, Rslt_15Args);
#ifdef STRICT
pushRedex(Rslt_15Args, glblFn774);
#else
swap(termLoc(Rslt_15), makePair(LAZ, 0, Rslt_15Args, glblFn774));
#endif

// call hash-vec at core: 1528
Term Rslt_20Args = makePair(APP, 0, s_2_1, SUB);
Term Rslt_20 = newTerm(VAR, 0, portLoc(2, Rslt_20Args));
Rslt_20Args = makePair(APP, 0, child_17, Rslt_20Args);
#ifdef STRICT
pushRedex(Rslt_20Args, newRef(glblProto121_961));
#else
swap(termLoc(Rslt_20), makePair(LAZ, 0, Rslt_20Args, newRef(glblProto121_961)));
#endif
// call bmiHashSeq-bm at core: 1518
Term Rslt_21Args = makePair(APP, 0, rest_bm_12, SUB);
Term Rslt_21 = newTerm(VAR, 0, portLoc(2, Rslt_21Args));
Rslt_21Args = makePair(APP, 0, Rslt_20, Rslt_21Args);
Rslt_21Args = makePair(APP, 0, m_1_1, Rslt_21Args);
#ifdef STRICT
pushRedex(Rslt_21Args, glblFn956);
#else
swap(termLoc(Rslt_21), makePair(LAZ, 0, Rslt_21Args, glblFn956));
#endif

// link args to body
swap(r_seq_18, Rslt_21);

Term Rslt_21V = get(termLoc(Rslt_21));
apps = take(portLoc(1, Rslt_21V));
lams = take(portLoc(2, Rslt_21V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_21), makePair(LAZ, 0, apps, lams));
// call map at core: 1526
Term Rslt_22Args = makePair(APP, 0, seq_18, SUB);
Term Rslt_22 = newTerm(VAR, 0, portLoc(2, Rslt_22Args));
Rslt_22Args = makePair(APP, 0, Rslt_15, Rslt_22Args);
#ifdef STRICT
pushRedex(Rslt_22Args, newRef(glblProto42_963));
#else
swap(termLoc(Rslt_22), makePair(LAZ, 0, Rslt_22Args, newRef(glblProto42_963)));
#endif
// call vect-conj at core: 1531
Term Rslt_32Args = makePair(APP, 0, k_25, SUB);
Term Rslt_32 = newTerm(VAR, 0, portLoc(2, Rslt_32Args));
Rslt_32Args = makePair(APP, 0, glblVal175_24, Rslt_32Args);
#ifdef STRICT
pushRedex(Rslt_32Args, glblFn178);
#else
swap(termLoc(Rslt_32), makePair(LAZ, 0, Rslt_32Args, glblFn178));
#endif

// call vect-conj at core: 1531
Term Rslt_33Args = makePair(APP, 0, v_29, SUB);
Term Rslt_33 = newTerm(VAR, 0, portLoc(2, Rslt_33Args));
Rslt_33Args = makePair(APP, 0, Rslt_32, Rslt_33Args);
#ifdef STRICT
pushRedex(Rslt_33Args, glblFn178);
#else
swap(termLoc(Rslt_33), makePair(LAZ, 0, Rslt_33Args, glblFn178));
#endif

// call conj at core: 1531
Term Rslt_34Args = makePair(APP, 0, Rslt_33, SUB);
Term Rslt_34 = newTerm(VAR, 0, portLoc(2, Rslt_34Args));
Rslt_34Args = makePair(APP, 0, s_2_2, Rslt_34Args);
#ifdef STRICT
pushRedex(Rslt_34Args, newRef(glblProto68_965));
#else
swap(termLoc(Rslt_34), makePair(LAZ, 0, Rslt_34Args, newRef(glblProto68_965)));
#endif
// call bmiHashSeq-bm at core: 1518
Term Rslt_35Args = makePair(APP, 0, rest_bm_12_1, SUB);
Term Rslt_35 = newTerm(VAR, 0, portLoc(2, Rslt_35Args));
Rslt_35Args = makePair(APP, 0, Rslt_34, Rslt_35Args);
Rslt_35Args = makePair(APP, 0, m_1_2, Rslt_35Args);
#ifdef STRICT
pushRedex(Rslt_35Args, glblFn956);
#else
swap(termLoc(Rslt_35), makePair(LAZ, 0, Rslt_35Args, glblFn956));
#endif

// link args to body
swap(r_seq_30, Rslt_35);

Term Rslt_35V = get(termLoc(Rslt_35));
apps = take(portLoc(1, Rslt_35V));
lams = take(portLoc(2, Rslt_35V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_35), makePair(LAZ, 0, apps, lams));
// call bmiVal at core: 1530
Term Rslt_36Args = makePair(APP, 0, bit_8_1, SUB);
Term Rslt_36 = newTerm(VAR, 0, portLoc(2, Rslt_36Args));
Rslt_36Args = makePair(APP, 0, m_1_3, Rslt_36Args);
#ifdef STRICT
pushRedex(Rslt_36Args, glblFn850);
#else
swap(termLoc(Rslt_36), makePair(LAZ, 0, Rslt_36Args, glblFn850));
#endif

// call  at core: 1530
Term Rslt_37Args = makePair(APP, 0, Rslt_36, SUB);
Term Rslt_37 = newTerm(VAR, 0, portLoc(2, Rslt_37Args));
#ifdef STRICT
pushRedex(Rslt_37Args, seq_30);
#else
swap(termLoc(Rslt_37), makePair(LAZ, 0, Rslt_37Args, seq_30));
#endif

// link args to body
swap(r_seq_26, Rslt_37);

Term Rslt_37V = get(termLoc(Rslt_37));
apps = take(portLoc(1, Rslt_37V));
lams = take(portLoc(2, Rslt_37V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_37), makePair(LAZ, 0, apps, lams));
// call bmiKey at core: 1529
Term Rslt_38Args = makePair(APP, 0, bit_8_2, SUB);
Term Rslt_38 = newTerm(VAR, 0, portLoc(2, Rslt_38Args));
Rslt_38Args = makePair(APP, 0, m_1_4, Rslt_38Args);
#ifdef STRICT
pushRedex(Rslt_38Args, glblFn821);
#else
swap(termLoc(Rslt_38), makePair(LAZ, 0, Rslt_38Args, glblFn821));
#endif

// call  at core: 1529
Term Rslt_39Args = makePair(APP, 0, Rslt_38, SUB);
Term Rslt_39 = newTerm(VAR, 0, portLoc(2, Rslt_39Args));
#ifdef STRICT
pushRedex(Rslt_39Args, seq_26);
#else
swap(termLoc(Rslt_39), makePair(LAZ, 0, Rslt_39Args, seq_26));
#endif

// call either at core: 1526
Term Rslt_40Args = makePair(APP, 0, Rslt_39, SUB);
Term Rslt_40 = newTerm(VAR, 0, portLoc(2, Rslt_40Args));
Rslt_40Args = makePair(APP, 0, Rslt_22, Rslt_40Args);
#ifdef STRICT
pushRedex(Rslt_40Args, newRef(glblProto32_967));
#else
swap(termLoc(Rslt_40), makePair(LAZ, 0, Rslt_40Args, newRef(glblProto32_967)));
#endif
// link args to body
swap(r_seq_13, Rslt_40);

Term Rslt_40V = get(termLoc(Rslt_40));
apps = take(portLoc(1, Rslt_40V));
lams = take(portLoc(2, Rslt_40V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_40), makePair(LAZ, 0, apps, lams));
// call bit-not at core: 1525
Term Rslt_41Args = makePair(APP, 0, bit_8_3, SUB);
Term Rslt_41 = newTerm(VAR, 0, portLoc(2, Rslt_41Args));
#ifdef STRICT
pushRedex(Rslt_41Args, glblFn168);
#else
swap(termLoc(Rslt_41), makePair(LAZ, 0, Rslt_41Args, glblFn168));
#endif

Term Rslt_42 = makeOp(OP_AND, bm_3_1, Rslt_41);
// call  at core: 1525
Term Rslt_43Args = makePair(APP, 0, Rslt_42, SUB);
Term Rslt_43 = newTerm(VAR, 0, portLoc(2, Rslt_43Args));
#ifdef STRICT
pushRedex(Rslt_43Args, seq_13);
#else
swap(termLoc(Rslt_43), makePair(LAZ, 0, Rslt_43Args, seq_13));
#endif

// link args to body
swap(r_seq_9, Rslt_43);

Term Rslt_43V = get(termLoc(Rslt_43));
apps = take(portLoc(1, Rslt_43V));
lams = take(portLoc(2, Rslt_43V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_43), makePair(LAZ, 0, apps, lams));
// call dec at core: 1524
Term Rslt_44Args = makePair(APP, 0, bm_3_3, SUB);
Term Rslt_44 = newTerm(VAR, 0, portLoc(2, Rslt_44Args));
#ifdef STRICT
pushRedex(Rslt_44Args, glblFn147);
#else
swap(termLoc(Rslt_44), makePair(LAZ, 0, Rslt_44Args, glblFn147));
#endif

// call bit-not at core: 1524
Term Rslt_45Args = makePair(APP, 0, Rslt_44, SUB);
Term Rslt_45 = newTerm(VAR, 0, portLoc(2, Rslt_45Args));
#ifdef STRICT
pushRedex(Rslt_45Args, glblFn168);
#else
swap(termLoc(Rslt_45), makePair(LAZ, 0, Rslt_45Args, glblFn168));
#endif

Term Rslt_46 = makeOp(OP_AND, bm_3_2, Rslt_45);
// call  at core: 1524
Term Rslt_47Args = makePair(APP, 0, Rslt_46, SUB);
Term Rslt_47 = newTerm(VAR, 0, portLoc(2, Rslt_47Args));
#ifdef STRICT
pushRedex(Rslt_47Args, seq_9);
#else
swap(termLoc(Rslt_47), makePair(LAZ, 0, Rslt_47Args, seq_9));
#endif

// call cond at core: 1522
Term Rslt_48Args = makePair(APP, 0, Rslt_47, SUB);
Term Rslt_48 = newTerm(VAR, 0, portLoc(2, Rslt_48Args));
Rslt_48Args = makePair(APP, 0, s_2, Rslt_48Args);
Rslt_48Args = makePair(APP, 0, Rslt_6, Rslt_48Args);
#ifdef STRICT
pushRedex(Rslt_48Args, newRef(glblProto34_969));
#else
swap(termLoc(Rslt_48), makePair(LAZ, 0, Rslt_48Args, newRef(glblProto34_969)));
#endif
// link args to body
swap(r_seq_4, Rslt_48);

Term Rslt_48V = get(termLoc(Rslt_48));
apps = take(portLoc(1, Rslt_48V));
lams = take(portLoc(2, Rslt_48V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_48), makePair(LAZ, 0, apps, lams));
pushRedex(args, seq_4);
return;
}
Term glblFn956 = newRef(glblCFn955);
Term glblFn958;
void glblCFn957(Term ref, Term args) {
Term apps, lams, V;
Location vLoc;
Term seq_3 = makePair(LAM, 0, SUB, NUL);
Location r_seq_3 = portLoc(2, seq_3);
Term s_2 = newTerm(VAR, 0, portLoc(1, seq_3));
seq_3 = makePair(LAM, 0, SUB, seq_3);
Term m_1 = newTerm(VAR, 0, portLoc(1, seq_3));
// allocate args at core: 1533

Term m_1_1;
m_1 = dupeArg(m_1, &m_1_1, 87);
// call bmiBitMap at core: 1536
Term Rslt_5Args = makePair(APP, 0, m_1_1, SUB);
Term Rslt_5 = newTerm(VAR, 0, portLoc(2, Rslt_5Args));
#ifdef STRICT
pushRedex(Rslt_5Args, glblFn764);
#else
swap(termLoc(Rslt_5), makePair(LAZ, 0, Rslt_5Args, glblFn764));
#endif

// call bmiHashSeq-bm at core: 1536
Term Rslt_6Args = makePair(APP, 0, Rslt_5, SUB);
Term Rslt_6 = newTerm(VAR, 0, portLoc(2, Rslt_6Args));
Rslt_6Args = makePair(APP, 0, s_2, Rslt_6Args);
Rslt_6Args = makePair(APP, 0, m_1, Rslt_6Args);
#ifdef STRICT
pushRedex(Rslt_6Args, glblFn956);
#else
swap(termLoc(Rslt_6), makePair(LAZ, 0, Rslt_6Args, glblFn956));
#endif

// link args to body
swap(r_seq_3, Rslt_6);

Term Rslt_6V = get(termLoc(Rslt_6));
apps = take(portLoc(1, Rslt_6V));
lams = take(portLoc(2, Rslt_6V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_6), makePair(LAZ, 0, apps, lams));
pushRedex(args, seq_3);
return;
}
Term glblFn958 = newRef(glblCFn957);
void glblProto20_975(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(1548), args);
  args = makePair(APP, 0, glblStr199, args);
  interact(args, glblProto20);
}
}
void glblProto121_979(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(1552), args);
  args = makePair(APP, 0, glblStr199, args);
  interact(args, glblProto121);
}
}
void glblProto42_981(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(1550), args);
  args = makePair(APP, 0, glblStr199, args);
  interact(args, glblProto42);
}
}
void glblProto32_983(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(1550), args);
  args = makePair(APP, 0, glblStr199, args);
  interact(args, glblProto32);
}
}
void glblProto34_985(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(1548), args);
  args = makePair(APP, 0, glblStr199, args);
  interact(args, glblProto34);
}
}
Term glblFn974;
void glblCFn973(Term ref, Term args) {
Term apps, lams, V;
Location vLoc;
Term seq_4 = makePair(LAM, 0, SUB, NUL);
Location r_seq_4 = portLoc(2, seq_4);
Term i_3 = newTerm(VAR, 0, portLoc(1, seq_4));
seq_4 = makePair(LAM, 0, SUB, seq_4);
Term s_2 = newTerm(VAR, 0, portLoc(1, seq_4));
seq_4 = makePair(LAM, 0, SUB, seq_4);
Term m_1 = newTerm(VAR, 0, portLoc(1, seq_4));
// allocate args at core: 1544

Term m_1_2;
m_1 = dupeArg(m_1, &m_1_2, 88);

Term m_1_1;
m_1 = dupeArg(m_1, &m_1_1, 89);

Term s_2_2;
s_2 = dupeArg(s_2, &s_2_2, 90);

Term s_2_1;
s_2 = dupeArg(s_2, &s_2_1, 91);

Term i_3_3;
i_3 = dupeArg(i_3, &i_3_3, 92);

Term i_3_2;
i_3 = dupeArg(i_3, &i_3_2, 93);

Term i_3_1;
i_3 = dupeArg(i_3, &i_3_1, 94);
Term seq_10 = makePair(LAM, 0, SUB, NUL);
Location r_seq_10 = portLoc(2, seq_10);
Term child_9 = newTerm(VAR, 0, portLoc(1, seq_10));
// allocate args at core: 1551
// call = at core: 1548
Term Rslt_6Args = makePair(APP, 0, newI60(32), SUB);
Term Rslt_6 = newTerm(VAR, 0, portLoc(2, Rslt_6Args));
Rslt_6Args = makePair(APP, 0, i_3, Rslt_6Args);
#ifdef STRICT
pushRedex(Rslt_6Args, newRef(glblProto20_975));
#else
swap(termLoc(Rslt_6), makePair(LAZ, 0, Rslt_6Args, newRef(glblProto20_975)));
#endif
// call arrayNodeCopyChild at core: 1550
Term Rslt_7Args = makePair(APP, 0, i_3_1, SUB);
Term Rslt_7 = newTerm(VAR, 0, portLoc(2, Rslt_7Args));
Rslt_7Args = makePair(APP, 0, m_1, Rslt_7Args);
#ifdef STRICT
pushRedex(Rslt_7Args, glblFn744);
#else
swap(termLoc(Rslt_7), makePair(LAZ, 0, Rslt_7Args, glblFn744));
#endif

// call hash-vec at core: 1552
Term Rslt_12Args = makePair(APP, 0, s_2_1, SUB);
Term Rslt_12 = newTerm(VAR, 0, portLoc(2, Rslt_12Args));
Rslt_12Args = makePair(APP, 0, child_9, Rslt_12Args);
#ifdef STRICT
pushRedex(Rslt_12Args, newRef(glblProto121_979));
#else
swap(termLoc(Rslt_12), makePair(LAZ, 0, Rslt_12Args, newRef(glblProto121_979)));
#endif
// call inc at core: 1552
Term Rslt_13Args = makePair(APP, 0, i_3_2, SUB);
Term Rslt_13 = newTerm(VAR, 0, portLoc(2, Rslt_13Args));
#ifdef STRICT
pushRedex(Rslt_13Args, glblFn141);
#else
swap(termLoc(Rslt_13), makePair(LAZ, 0, Rslt_13Args, glblFn141));
#endif

// call arrayNodeHashSeq-i at core: 1544
Term Rslt_14Args = makePair(APP, 0, Rslt_13, SUB);
Term Rslt_14 = newTerm(VAR, 0, portLoc(2, Rslt_14Args));
Rslt_14Args = makePair(APP, 0, Rslt_12, Rslt_14Args);
Rslt_14Args = makePair(APP, 0, m_1_1, Rslt_14Args);
#ifdef STRICT
pushRedex(Rslt_14Args, glblFn974);
#else
swap(termLoc(Rslt_14), makePair(LAZ, 0, Rslt_14Args, glblFn974));
#endif

// link args to body
swap(r_seq_10, Rslt_14);

Term Rslt_14V = get(termLoc(Rslt_14));
apps = take(portLoc(1, Rslt_14V));
lams = take(portLoc(2, Rslt_14V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_14), makePair(LAZ, 0, apps, lams));
// call map at core: 1550
Term Rslt_15Args = makePair(APP, 0, seq_10, SUB);
Term Rslt_15 = newTerm(VAR, 0, portLoc(2, Rslt_15Args));
Rslt_15Args = makePair(APP, 0, Rslt_7, Rslt_15Args);
#ifdef STRICT
pushRedex(Rslt_15Args, newRef(glblProto42_981));
#else
swap(termLoc(Rslt_15), makePair(LAZ, 0, Rslt_15Args, newRef(glblProto42_981)));
#endif
// call inc at core: 1553
Term Rslt_17Args = makePair(APP, 0, i_3_3, SUB);
Term Rslt_17 = newTerm(VAR, 0, portLoc(2, Rslt_17Args));
#ifdef STRICT
pushRedex(Rslt_17Args, glblFn141);
#else
swap(termLoc(Rslt_17), makePair(LAZ, 0, Rslt_17Args, glblFn141));
#endif

// call arrayNodeHashSeq-i at core: 1544
Term Rslt_18Args = makePair(APP, 0, Rslt_17, SUB);
Term Rslt_18 = newTerm(VAR, 0, portLoc(2, Rslt_18Args));
Rslt_18Args = makePair(APP, 0, s_2_2, Rslt_18Args);
Rslt_18Args = makePair(APP, 0, m_1_2, Rslt_18Args);
#ifdef STRICT
pushRedex(Rslt_18Args, glblFn974);
#else
swap(termLoc(Rslt_18), makePair(LAZ, 0, Rslt_18Args, glblFn974));
#endif

// call either at core: 1550
Term Rslt_19Args = makePair(APP, 0, Rslt_18, SUB);
Term Rslt_19 = newTerm(VAR, 0, portLoc(2, Rslt_19Args));
Rslt_19Args = makePair(APP, 0, Rslt_15, Rslt_19Args);
#ifdef STRICT
pushRedex(Rslt_19Args, newRef(glblProto32_983));
#else
swap(termLoc(Rslt_19), makePair(LAZ, 0, Rslt_19Args, newRef(glblProto32_983)));
#endif
// call cond at core: 1548
Term Rslt_20Args = makePair(APP, 0, Rslt_19, SUB);
Term Rslt_20 = newTerm(VAR, 0, portLoc(2, Rslt_20Args));
Rslt_20Args = makePair(APP, 0, s_2, Rslt_20Args);
Rslt_20Args = makePair(APP, 0, Rslt_6, Rslt_20Args);
#ifdef STRICT
pushRedex(Rslt_20Args, newRef(glblProto34_985));
#else
swap(termLoc(Rslt_20), makePair(LAZ, 0, Rslt_20Args, newRef(glblProto34_985)));
#endif
// link args to body
swap(r_seq_4, Rslt_20);

Term Rslt_20V = get(termLoc(Rslt_20));
apps = take(portLoc(1, Rslt_20V));
lams = take(portLoc(2, Rslt_20V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_20), makePair(LAZ, 0, apps, lams));
pushRedex(args, seq_4);
return;
}
Term glblFn974 = newRef(glblCFn973);
Term glblFn978;
void glblCFn977(Term ref, Term args) {
Term apps, lams, V;
Location vLoc;
Term seq_3 = makePair(LAM, 0, SUB, NUL);
Location r_seq_3 = portLoc(2, seq_3);
Term s_2 = newTerm(VAR, 0, portLoc(1, seq_3));
seq_3 = makePair(LAM, 0, SUB, seq_3);
Term m_1 = newTerm(VAR, 0, portLoc(1, seq_3));
// allocate args at core: 1555
// call arrayNodeHashSeq-i at core: 1558
Term Rslt_5Args = makePair(APP, 0, newI60(0), SUB);
Term Rslt_5 = newTerm(VAR, 0, portLoc(2, Rslt_5Args));
Rslt_5Args = makePair(APP, 0, s_2, Rslt_5Args);
Rslt_5Args = makePair(APP, 0, m_1, Rslt_5Args);
#ifdef STRICT
pushRedex(Rslt_5Args, glblFn974);
#else
swap(termLoc(Rslt_5), makePair(LAZ, 0, Rslt_5Args, glblFn974));
#endif

// link args to body
swap(r_seq_3, Rslt_5);

Term Rslt_5V = get(termLoc(Rslt_5));
apps = take(portLoc(1, Rslt_5V));
lams = take(portLoc(2, Rslt_5V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_5), makePair(LAZ, 0, apps, lams));
pushRedex(args, seq_3);
return;
}
Term glblFn978 = newRef(glblCFn977);
void glblProto119_991(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(1565), args);
  args = makePair(APP, 0, glblStr199, args);
  interact(args, glblProto119);
}
}
void glblProto56_993(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(1565), args);
  args = makePair(APP, 0, glblStr199, args);
  interact(args, glblProto56);
}
}
Term glblFn990;
void glblCFn989(Term ref, Term args) {
Term apps, lams, V;
Location vLoc;
Term seq_2 = makePair(LAM, 0, SUB, NUL);
Location r_seq_2 = portLoc(2, seq_2);
Term pair_1 = newTerm(VAR, 0, portLoc(1, seq_2));
// allocate args at core: 1560
// call get at core: 1565
Term Rslt_4Args = makePair(APP, 0, newI60(1), SUB);
Term Rslt_4 = newTerm(VAR, 0, portLoc(2, Rslt_4Args));
Rslt_4Args = makePair(APP, 0, pair_1, Rslt_4Args);
#ifdef STRICT
pushRedex(Rslt_4Args, newRef(glblProto119_991));
#else
swap(termLoc(Rslt_4), makePair(LAZ, 0, Rslt_4Args, newRef(glblProto119_991)));
#endif
// call extract at core: 1565
Term Rslt_5Args = makePair(APP, 0, Rslt_4, SUB);
Term Rslt_5 = newTerm(VAR, 0, portLoc(2, Rslt_5Args));
#ifdef STRICT
pushRedex(Rslt_5Args, newRef(glblProto56_993));
#else
swap(termLoc(Rslt_5), makePair(LAZ, 0, Rslt_5Args, newRef(glblProto56_993)));
#endif
// link args to body
swap(r_seq_2, Rslt_5);

Term Rslt_5V = get(termLoc(Rslt_5));
apps = take(portLoc(1, Rslt_5V));
lams = take(portLoc(2, Rslt_5V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_5), makePair(LAZ, 0, apps, lams));
pushRedex(args, seq_2);
return;
}
Term glblFn990 = newRef(glblCFn989);
void glblProto109_998(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(1574), args);
  args = makePair(APP, 0, glblStr199, args);
  interact(args, glblProto109);
}
}
void glblProto42_1000(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(1572), args);
  args = makePair(APP, 0, glblStr199, args);
  interact(args, glblProto42);
}
}
void glblProto20_1002(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(1577), args);
  args = makePair(APP, 0, glblStr199, args);
  interact(args, glblProto20);
}
}
void glblProto20_1004(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(1578), args);
  args = makePair(APP, 0, glblStr199, args);
  interact(args, glblProto20);
}
}
void glblProto34_1006(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(1578), args);
  args = makePair(APP, 0, glblStr199, args);
  interact(args, glblProto34);
}
}
void glblProto34_1008(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(1577), args);
  args = makePair(APP, 0, glblStr199, args);
  interact(args, glblProto34);
}
}
void glblProto32_1010(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(1572), args);
  args = makePair(APP, 0, glblStr199, args);
  interact(args, glblProto32);
}
}
Term glblFn997;
void glblCFn996(Term ref, Term args) {
Term apps, lams, V;
Location vLoc;
Term seq_6 = makePair(LAM, 0, SUB, NUL);
Location r_seq_6 = portLoc(2, seq_6);
Term shift_5 = newTerm(VAR, 0, portLoc(1, seq_6));
seq_6 = makePair(LAM, 0, SUB, seq_6);
Term hash_4 = newTerm(VAR, 0, portLoc(1, seq_6));
seq_6 = makePair(LAM, 0, SUB, seq_6);
Term v_3 = newTerm(VAR, 0, portLoc(1, seq_6));
seq_6 = makePair(LAM, 0, SUB, seq_6);
Term k_2 = newTerm(VAR, 0, portLoc(1, seq_6));
seq_6 = makePair(LAM, 0, SUB, seq_6);
Term m_1 = newTerm(VAR, 0, portLoc(1, seq_6));
// allocate args at core: 1567

Term m_1_8;
m_1 = dupeArg(m_1, &m_1_8, 95);

Term m_1_7;
m_1 = dupeArg(m_1, &m_1_7, 96);

Term m_1_6;
m_1 = dupeArg(m_1, &m_1_6, 97);

Term m_1_5;
m_1 = dupeArg(m_1, &m_1_5, 98);

Term m_1_4;
m_1 = dupeArg(m_1, &m_1_4, 99);

Term m_1_3;
m_1 = dupeArg(m_1, &m_1_3, 100);

Term m_1_2;
m_1 = dupeArg(m_1, &m_1_2, 101);

Term m_1_1;
m_1 = dupeArg(m_1, &m_1_1, 102);

Term v_3_4;
v_3 = dupeArg(v_3, &v_3_4, 103);

Term v_3_3;
v_3 = dupeArg(v_3, &v_3_3, 104);

Term v_3_2;
v_3 = dupeArg(v_3, &v_3_2, 105);

Term v_3_1;
v_3 = dupeArg(v_3, &v_3_1, 106);

Term hash_4_4;
hash_4 = dupeArg(hash_4, &hash_4_4, 107);

Term hash_4_3;
hash_4 = dupeArg(hash_4, &hash_4_3, 108);

Term hash_4_2;
hash_4 = dupeArg(hash_4, &hash_4_2, 109);

Term hash_4_1;
hash_4 = dupeArg(hash_4, &hash_4_1, 110);

Term shift_5_3;
shift_5 = dupeArg(shift_5, &shift_5_3, 111);

Term shift_5_2;
shift_5 = dupeArg(shift_5, &shift_5_2, 112);

Term shift_5_1;
shift_5 = dupeArg(shift_5, &shift_5_1, 113);
Term seq_10 = makePair(LAM, 0, SUB, NUL);
Location r_seq_10 = portLoc(2, seq_10);
Term k_9 = newTerm(VAR, 0, portLoc(1, seq_10));
// allocate args at core: 1569

Term k_9_4;
k_9 = dupeArg(k_9, &k_9_4, 0);

Term k_9_3;
k_9 = dupeArg(k_9, &k_9_3, 0);

Term k_9_2;
k_9 = dupeArg(k_9, &k_9_2, 0);

Term k_9_1;
k_9 = dupeArg(k_9, &k_9_1, 0);
Term seq_14 = makePair(LAM, 0, SUB, NUL);
Location r_seq_14 = portLoc(2, seq_14);
Term bit_13 = newTerm(VAR, 0, portLoc(1, seq_14));
// allocate args at core: 1570

Term bit_13_5;
bit_13 = dupeArg(bit_13, &bit_13_5, 0);

Term bit_13_4;
bit_13 = dupeArg(bit_13, &bit_13_4, 0);

Term bit_13_3;
bit_13 = dupeArg(bit_13, &bit_13_3, 0);

Term bit_13_2;
bit_13 = dupeArg(bit_13, &bit_13_2, 0);

Term bit_13_1;
bit_13 = dupeArg(bit_13, &bit_13_1, 0);
Term seq_30 = makePair(LAM, 0, SUB, NUL);
Location r_seq_30 = portLoc(2, seq_30);
Term currKey_29 = newTerm(VAR, 0, portLoc(1, seq_30));
// allocate args at core: 1575

Term currKey_29_1;
currKey_29 = dupeArg(currKey_29, &currKey_29_1, 0);
Term seq_34 = makePair(LAM, 0, SUB, NUL);
Location r_seq_34 = portLoc(2, seq_34);
Term currVal_33 = newTerm(VAR, 0, portLoc(1, seq_34));
// allocate args at core: 1576

Term currVal_33_1;
currVal_33 = dupeArg(currVal_33, &currVal_33_1, 0);
Term seq_21 = makePair(LAM, 0, SUB, NUL);
Location r_seq_21 = portLoc(2, seq_21);
Term childNode_20 = newTerm(VAR, 0, portLoc(1, seq_21));
// allocate args at core: 1573
Term glblVal262_6;
glblVal262 = dupeArg(glblVal262, &glblVal262_6, 0);

// call bmiBitMap at core: 1571
Term Rslt_16Args = makePair(APP, 0, m_1, SUB);
Term Rslt_16 = newTerm(VAR, 0, portLoc(2, Rslt_16Args));
#ifdef STRICT
pushRedex(Rslt_16Args, glblFn764);
#else
swap(termLoc(Rslt_16), makePair(LAZ, 0, Rslt_16Args, glblFn764));
#endif

Term Rslt_17 = makeOp(OP_AND, bit_13, Rslt_16);
// call bmiCopyChild at core: 1572
Term Rslt_18Args = makePair(APP, 0, bit_13_1, SUB);
Term Rslt_18 = newTerm(VAR, 0, portLoc(2, Rslt_18Args));
Rslt_18Args = makePair(APP, 0, m_1_1, Rslt_18Args);
#ifdef STRICT
pushRedex(Rslt_18Args, glblFn774);
#else
swap(termLoc(Rslt_18), makePair(LAZ, 0, Rslt_18Args, glblFn774));
#endif

Term Rslt_23 = makeOp(OP_ADD, newI60(5), shift_5);
// call copyAssoc at core: 1574
Term Rslt_24Args = makePair(APP, 0, Rslt_23, SUB);
Term Rslt_24 = newTerm(VAR, 0, portLoc(2, Rslt_24Args));
Rslt_24Args = makePair(APP, 0, hash_4, Rslt_24Args);
Rslt_24Args = makePair(APP, 0, v_3, Rslt_24Args);
Rslt_24Args = makePair(APP, 0, k_9, Rslt_24Args);
Rslt_24Args = makePair(APP, 0, childNode_20, Rslt_24Args);
#ifdef STRICT
pushRedex(Rslt_24Args, newRef(glblProto109_998));
#else
swap(termLoc(Rslt_24), makePair(LAZ, 0, Rslt_24Args, newRef(glblProto109_998)));
#endif
// call bmiUpdate at core: 1574
Term Rslt_25Args = makePair(APP, 0, Rslt_24, SUB);
Term Rslt_25 = newTerm(VAR, 0, portLoc(2, Rslt_25Args));
Rslt_25Args = makePair(APP, 0, bit_13_2, Rslt_25Args);
Rslt_25Args = makePair(APP, 0, m_1_2, Rslt_25Args);
#ifdef STRICT
pushRedex(Rslt_25Args, glblFn770);
#else
swap(termLoc(Rslt_25), makePair(LAZ, 0, Rslt_25Args, glblFn770));
#endif

// link args to body
swap(r_seq_21, Rslt_25);

Term Rslt_25V = get(termLoc(Rslt_25));
apps = take(portLoc(1, Rslt_25V));
lams = take(portLoc(2, Rslt_25V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_25), makePair(LAZ, 0, apps, lams));
// call map at core: 1572
Term Rslt_26Args = makePair(APP, 0, seq_21, SUB);
Term Rslt_26 = newTerm(VAR, 0, portLoc(2, Rslt_26Args));
Rslt_26Args = makePair(APP, 0, Rslt_18, Rslt_26Args);
#ifdef STRICT
pushRedex(Rslt_26Args, newRef(glblProto42_1000));
#else
swap(termLoc(Rslt_26), makePair(LAZ, 0, Rslt_26Args, newRef(glblProto42_1000)));
#endif
// call = at core: 1577
Term Rslt_36Args = makePair(APP, 0, k_9_1, SUB);
Term Rslt_36 = newTerm(VAR, 0, portLoc(2, Rslt_36Args));
Rslt_36Args = makePair(APP, 0, currKey_29, Rslt_36Args);
#ifdef STRICT
pushRedex(Rslt_36Args, newRef(glblProto20_1002));
#else
swap(termLoc(Rslt_36), makePair(LAZ, 0, Rslt_36Args, newRef(glblProto20_1002)));
#endif
// call = at core: 1578
Term Rslt_37Args = makePair(APP, 0, v_3_1, SUB);
Term Rslt_37 = newTerm(VAR, 0, portLoc(2, Rslt_37Args));
Rslt_37Args = makePair(APP, 0, currVal_33, Rslt_37Args);
#ifdef STRICT
pushRedex(Rslt_37Args, newRef(glblProto20_1004));
#else
swap(termLoc(Rslt_37), makePair(LAZ, 0, Rslt_37Args, newRef(glblProto20_1004)));
#endif
// call bmiClone at core: 1580
Term Rslt_38Args = makePair(APP, 0, v_3_2, SUB);
Term Rslt_38 = newTerm(VAR, 0, portLoc(2, Rslt_38Args));
Rslt_38Args = makePair(APP, 0, k_9_2, Rslt_38Args);
Rslt_38Args = makePair(APP, 0, bit_13_3, Rslt_38Args);
Rslt_38Args = makePair(APP, 0, m_1_4, Rslt_38Args);
#ifdef STRICT
pushRedex(Rslt_38Args, glblFn768);
#else
swap(termLoc(Rslt_38), makePair(LAZ, 0, Rslt_38Args, glblFn768));
#endif

// call cond at core: 1578
Term Rslt_39Args = makePair(APP, 0, Rslt_38, SUB);
Term Rslt_39 = newTerm(VAR, 0, portLoc(2, Rslt_39Args));
Rslt_39Args = makePair(APP, 0, m_1_3, Rslt_39Args);
Rslt_39Args = makePair(APP, 0, Rslt_37, Rslt_39Args);
#ifdef STRICT
pushRedex(Rslt_39Args, newRef(glblProto34_1006));
#else
swap(termLoc(Rslt_39), makePair(LAZ, 0, Rslt_39Args, newRef(glblProto34_1006)));
#endif
// call bmiReplaceCopied at core: 1581
Term Rslt_40Args = makePair(APP, 0, currVal_33_1, SUB);
Term Rslt_40 = newTerm(VAR, 0, portLoc(2, Rslt_40Args));
Rslt_40Args = makePair(APP, 0, currKey_29_1, Rslt_40Args);
Rslt_40Args = makePair(APP, 0, shift_5_1, Rslt_40Args);
Rslt_40Args = makePair(APP, 0, hash_4_1, Rslt_40Args);
Rslt_40Args = makePair(APP, 0, v_3_3, Rslt_40Args);
Rslt_40Args = makePair(APP, 0, k_9_3, Rslt_40Args);
Rslt_40Args = makePair(APP, 0, m_1_5, Rslt_40Args);
#ifdef STRICT
pushRedex(Rslt_40Args, glblFn905);
#else
swap(termLoc(Rslt_40), makePair(LAZ, 0, Rslt_40Args, glblFn905));
#endif

// call cond at core: 1577
Term Rslt_41Args = makePair(APP, 0, Rslt_40, SUB);
Term Rslt_41 = newTerm(VAR, 0, portLoc(2, Rslt_41Args));
Rslt_41Args = makePair(APP, 0, Rslt_39, Rslt_41Args);
Rslt_41Args = makePair(APP, 0, Rslt_36, Rslt_41Args);
#ifdef STRICT
pushRedex(Rslt_41Args, newRef(glblProto34_1008));
#else
swap(termLoc(Rslt_41), makePair(LAZ, 0, Rslt_41Args, newRef(glblProto34_1008)));
#endif
// link args to body
swap(r_seq_34, Rslt_41);

Term Rslt_41V = get(termLoc(Rslt_41));
apps = take(portLoc(1, Rslt_41V));
lams = take(portLoc(2, Rslt_41V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_41), makePair(LAZ, 0, apps, lams));
// call bmiVal at core: 1576
Term Rslt_42Args = makePair(APP, 0, bit_13_4, SUB);
Term Rslt_42 = newTerm(VAR, 0, portLoc(2, Rslt_42Args));
Rslt_42Args = makePair(APP, 0, m_1_6, Rslt_42Args);
#ifdef STRICT
pushRedex(Rslt_42Args, glblFn850);
#else
swap(termLoc(Rslt_42), makePair(LAZ, 0, Rslt_42Args, glblFn850));
#endif

// call  at core: 1576
Term Rslt_43Args = makePair(APP, 0, Rslt_42, SUB);
Term Rslt_43 = newTerm(VAR, 0, portLoc(2, Rslt_43Args));
#ifdef STRICT
pushRedex(Rslt_43Args, seq_34);
#else
swap(termLoc(Rslt_43), makePair(LAZ, 0, Rslt_43Args, seq_34));
#endif

// link args to body
swap(r_seq_30, Rslt_43);

Term Rslt_43V = get(termLoc(Rslt_43));
apps = take(portLoc(1, Rslt_43V));
lams = take(portLoc(2, Rslt_43V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_43), makePair(LAZ, 0, apps, lams));
// call bmiKey at core: 1575
Term Rslt_44Args = makePair(APP, 0, bit_13_5, SUB);
Term Rslt_44 = newTerm(VAR, 0, portLoc(2, Rslt_44Args));
Rslt_44Args = makePair(APP, 0, m_1_7, Rslt_44Args);
#ifdef STRICT
pushRedex(Rslt_44Args, glblFn821);
#else
swap(termLoc(Rslt_44), makePair(LAZ, 0, Rslt_44Args, glblFn821));
#endif

// call  at core: 1575
Term Rslt_45Args = makePair(APP, 0, Rslt_44, SUB);
Term Rslt_45 = newTerm(VAR, 0, portLoc(2, Rslt_45Args));
#ifdef STRICT
pushRedex(Rslt_45Args, seq_30);
#else
swap(termLoc(Rslt_45), makePair(LAZ, 0, Rslt_45Args, seq_30));
#endif

// call either at core: 1572
Term Rslt_46Args = makePair(APP, 0, Rslt_45, SUB);
Term Rslt_46 = newTerm(VAR, 0, portLoc(2, Rslt_46Args));
Rslt_46Args = makePair(APP, 0, Rslt_26, Rslt_46Args);
#ifdef STRICT
pushRedex(Rslt_46Args, newRef(glblProto32_1010));
#else
swap(termLoc(Rslt_46), makePair(LAZ, 0, Rslt_46Args, newRef(glblProto32_1010)));
#endif
// call addCopiedBMI at core: 1582
Term Rslt_47Args = makePair(APP, 0, shift_5_2, SUB);
Term Rslt_47 = newTerm(VAR, 0, portLoc(2, Rslt_47Args));
Rslt_47Args = makePair(APP, 0, hash_4_2, Rslt_47Args);
Rslt_47Args = makePair(APP, 0, v_3_4, Rslt_47Args);
Rslt_47Args = makePair(APP, 0, k_9_4, Rslt_47Args);
Rslt_47Args = makePair(APP, 0, m_1_8, Rslt_47Args);
#ifdef STRICT
pushRedex(Rslt_47Args, glblFn762);
#else
swap(termLoc(Rslt_47), makePair(LAZ, 0, Rslt_47Args, glblFn762));
#endif

// call int-cond at core: 1571
Term Rslt_48Args = makePair(APP, 0, Rslt_47, SUB);
Term Rslt_48 = newTerm(VAR, 0, portLoc(2, Rslt_48Args));
Rslt_48Args = makePair(APP, 0, Rslt_46, Rslt_48Args);
Rslt_48Args = makePair(APP, 0, Rslt_17, Rslt_48Args);
#ifdef STRICT
pushRedex(Rslt_48Args, glblVal262_6);
#else
swap(termLoc(Rslt_48), makePair(LAZ, 0, Rslt_48Args, glblVal262_6));
#endif

// link args to body
swap(r_seq_14, Rslt_48);

Term Rslt_48V = get(termLoc(Rslt_48));
apps = take(portLoc(1, Rslt_48V));
lams = take(portLoc(2, Rslt_48V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_48), makePair(LAZ, 0, apps, lams));
// call bitpos at core: 1570
Term Rslt_49Args = makePair(APP, 0, shift_5_3, SUB);
Term Rslt_49 = newTerm(VAR, 0, portLoc(2, Rslt_49Args));
Rslt_49Args = makePair(APP, 0, hash_4_3, Rslt_49Args);
#ifdef STRICT
pushRedex(Rslt_49Args, glblFn766);
#else
swap(termLoc(Rslt_49), makePair(LAZ, 0, Rslt_49Args, glblFn766));
#endif

// call  at core: 1570
Term Rslt_50Args = makePair(APP, 0, Rslt_49, SUB);
Term Rslt_50 = newTerm(VAR, 0, portLoc(2, Rslt_50Args));
#ifdef STRICT
pushRedex(Rslt_50Args, seq_14);
#else
swap(termLoc(Rslt_50), makePair(LAZ, 0, Rslt_50Args, seq_14));
#endif

// link args to body
swap(r_seq_10, Rslt_50);

Term Rslt_50V = get(termLoc(Rslt_50));
apps = take(portLoc(1, Rslt_50V));
lams = take(portLoc(2, Rslt_50V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_50), makePair(LAZ, 0, apps, lams));
// call set-hash-val at core: 1569
Term Rslt_51Args = makePair(APP, 0, hash_4_4, SUB);
Term Rslt_51 = newTerm(VAR, 0, portLoc(2, Rslt_51Args));
Rslt_51Args = makePair(APP, 0, k_2, Rslt_51Args);
#ifdef STRICT
pushRedex(Rslt_51Args, glblFn917);
#else
swap(termLoc(Rslt_51), makePair(LAZ, 0, Rslt_51Args, glblFn917));
#endif

// call  at core: 1569
Term Rslt_52Args = makePair(APP, 0, Rslt_51, SUB);
Term Rslt_52 = newTerm(VAR, 0, portLoc(2, Rslt_52Args));
#ifdef STRICT
pushRedex(Rslt_52Args, seq_10);
#else
swap(termLoc(Rslt_52), makePair(LAZ, 0, Rslt_52Args, seq_10));
#endif

// link args to body
swap(r_seq_6, Rslt_52);

Term Rslt_52V = get(termLoc(Rslt_52));
apps = take(portLoc(1, Rslt_52V));
lams = take(portLoc(2, Rslt_52V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_52), makePair(LAZ, 0, apps, lams));
pushRedex(args, seq_6);
return;
}
Term glblFn997 = newRef(glblCFn996);
void glblProto111_1015(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(1604), args);
  args = makePair(APP, 0, glblStr199, args);
  interact(args, glblProto111);
}
}
void glblProto42_1017(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(1602), args);
  args = makePair(APP, 0, glblStr199, args);
  interact(args, glblProto42);
}
}
void glblProto20_1019(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(1607), args);
  args = makePair(APP, 0, glblStr199, args);
  interact(args, glblProto20);
}
}
void glblProto34_1021(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(1607), args);
  args = makePair(APP, 0, glblStr199, args);
  interact(args, glblProto34);
}
}
void glblProto32_1023(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(1602), args);
  args = makePair(APP, 0, glblStr199, args);
  interact(args, glblProto32);
}
}
Term glblFn1014;
void glblCFn1013(Term ref, Term args) {
Term apps, lams, V;
Location vLoc;
Term seq_6 = makePair(LAM, 0, SUB, NUL);
Location r_seq_6 = portLoc(2, seq_6);
Term shift_5 = newTerm(VAR, 0, portLoc(1, seq_6));
seq_6 = makePair(LAM, 0, SUB, seq_6);
Term hash_4 = newTerm(VAR, 0, portLoc(1, seq_6));
seq_6 = makePair(LAM, 0, SUB, seq_6);
Term v_3 = newTerm(VAR, 0, portLoc(1, seq_6));
seq_6 = makePair(LAM, 0, SUB, seq_6);
Term k_2 = newTerm(VAR, 0, portLoc(1, seq_6));
seq_6 = makePair(LAM, 0, SUB, seq_6);
Term m_1 = newTerm(VAR, 0, portLoc(1, seq_6));
// allocate args at core: 1584

Term m_1_7;
m_1 = dupeArg(m_1, &m_1_7, 114);

Term m_1_6;
m_1 = dupeArg(m_1, &m_1_6, 115);

Term m_1_5;
m_1 = dupeArg(m_1, &m_1_5, 116);

Term m_1_4;
m_1 = dupeArg(m_1, &m_1_4, 117);

Term m_1_3;
m_1 = dupeArg(m_1, &m_1_3, 118);

Term m_1_2;
m_1 = dupeArg(m_1, &m_1_2, 119);

Term m_1_1;
m_1 = dupeArg(m_1, &m_1_1, 120);

Term v_3_3;
v_3 = dupeArg(v_3, &v_3_3, 121);

Term v_3_2;
v_3 = dupeArg(v_3, &v_3_2, 122);

Term v_3_1;
v_3 = dupeArg(v_3, &v_3_1, 123);

Term hash_4_4;
hash_4 = dupeArg(hash_4, &hash_4_4, 124);

Term hash_4_3;
hash_4 = dupeArg(hash_4, &hash_4_3, 125);

Term hash_4_2;
hash_4 = dupeArg(hash_4, &hash_4_2, 126);

Term hash_4_1;
hash_4 = dupeArg(hash_4, &hash_4_1, 127);

Term shift_5_3;
shift_5 = dupeArg(shift_5, &shift_5_3, 128);

Term shift_5_2;
shift_5 = dupeArg(shift_5, &shift_5_2, 129);

Term shift_5_1;
shift_5 = dupeArg(shift_5, &shift_5_1, 130);
Term seq_10 = makePair(LAM, 0, SUB, NUL);
Location r_seq_10 = portLoc(2, seq_10);
Term k_9 = newTerm(VAR, 0, portLoc(1, seq_10));
// allocate args at core: 1599

Term k_9_4;
k_9 = dupeArg(k_9, &k_9_4, 0);

Term k_9_3;
k_9 = dupeArg(k_9, &k_9_3, 0);

Term k_9_2;
k_9 = dupeArg(k_9, &k_9_2, 0);

Term k_9_1;
k_9 = dupeArg(k_9, &k_9_1, 0);
Term seq_14 = makePair(LAM, 0, SUB, NUL);
Location r_seq_14 = portLoc(2, seq_14);
Term bit_13 = newTerm(VAR, 0, portLoc(1, seq_14));
// allocate args at core: 1600

Term bit_13_5;
bit_13 = dupeArg(bit_13, &bit_13_5, 0);

Term bit_13_4;
bit_13 = dupeArg(bit_13, &bit_13_4, 0);

Term bit_13_3;
bit_13 = dupeArg(bit_13, &bit_13_3, 0);

Term bit_13_2;
bit_13 = dupeArg(bit_13, &bit_13_2, 0);

Term bit_13_1;
bit_13 = dupeArg(bit_13, &bit_13_1, 0);
Term seq_30 = makePair(LAM, 0, SUB, NUL);
Location r_seq_30 = portLoc(2, seq_30);
Term currKey_29 = newTerm(VAR, 0, portLoc(1, seq_30));
// allocate args at core: 1605

Term currKey_29_2;
currKey_29 = dupeArg(currKey_29, &currKey_29_2, 0);

Term currKey_29_1;
currKey_29 = dupeArg(currKey_29, &currKey_29_1, 0);
Term seq_34 = makePair(LAM, 0, SUB, NUL);
Location r_seq_34 = portLoc(2, seq_34);
Term currVal_33 = newTerm(VAR, 0, portLoc(1, seq_34));
// allocate args at core: 1606

Term currVal_33_1;
currVal_33 = dupeArg(currVal_33, &currVal_33_1, 0);
Term seq_21 = makePair(LAM, 0, SUB, NUL);
Location r_seq_21 = portLoc(2, seq_21);
Term childNode_20 = newTerm(VAR, 0, portLoc(1, seq_21));
// allocate args at core: 1603
Term glblVal262_7;
glblVal262 = dupeArg(glblVal262, &glblVal262_7, 0);

// call bmiBitMap at core: 1601
Term Rslt_16Args = makePair(APP, 0, m_1, SUB);
Term Rslt_16 = newTerm(VAR, 0, portLoc(2, Rslt_16Args));
#ifdef STRICT
pushRedex(Rslt_16Args, glblFn764);
#else
swap(termLoc(Rslt_16), makePair(LAZ, 0, Rslt_16Args, glblFn764));
#endif

Term Rslt_17 = makeOp(OP_AND, bit_13, Rslt_16);
// call bmiMutateChild at core: 1602
Term Rslt_18Args = makePair(APP, 0, bit_13_1, SUB);
Term Rslt_18 = newTerm(VAR, 0, portLoc(2, Rslt_18Args));
Rslt_18Args = makePair(APP, 0, m_1_1, Rslt_18Args);
#ifdef STRICT
pushRedex(Rslt_18Args, glblFn796);
#else
swap(termLoc(Rslt_18), makePair(LAZ, 0, Rslt_18Args, glblFn796));
#endif

Term Rslt_23 = makeOp(OP_ADD, newI60(5), shift_5);
// call assoc* at core: 1604
Term Rslt_24Args = makePair(APP, 0, Rslt_23, SUB);
Term Rslt_24 = newTerm(VAR, 0, portLoc(2, Rslt_24Args));
Rslt_24Args = makePair(APP, 0, hash_4, Rslt_24Args);
Rslt_24Args = makePair(APP, 0, v_3, Rslt_24Args);
Rslt_24Args = makePair(APP, 0, k_9, Rslt_24Args);
Rslt_24Args = makePair(APP, 0, childNode_20, Rslt_24Args);
#ifdef STRICT
pushRedex(Rslt_24Args, newRef(glblProto111_1015));
#else
swap(termLoc(Rslt_24), makePair(LAZ, 0, Rslt_24Args, newRef(glblProto111_1015)));
#endif
// call bmiSetChild at core: 1604
Term Rslt_25Args = makePair(APP, 0, Rslt_24, SUB);
Term Rslt_25 = newTerm(VAR, 0, portLoc(2, Rslt_25Args));
Rslt_25Args = makePair(APP, 0, bit_13_2, Rslt_25Args);
Rslt_25Args = makePair(APP, 0, m_1_2, Rslt_25Args);
#ifdef STRICT
pushRedex(Rslt_25Args, glblFn914);
#else
swap(termLoc(Rslt_25), makePair(LAZ, 0, Rslt_25Args, glblFn914));
#endif

// link args to body
swap(r_seq_21, Rslt_25);

Term Rslt_25V = get(termLoc(Rslt_25));
apps = take(portLoc(1, Rslt_25V));
lams = take(portLoc(2, Rslt_25V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_25), makePair(LAZ, 0, apps, lams));
// call map at core: 1602
Term Rslt_26Args = makePair(APP, 0, seq_21, SUB);
Term Rslt_26 = newTerm(VAR, 0, portLoc(2, Rslt_26Args));
Rslt_26Args = makePair(APP, 0, Rslt_18, Rslt_26Args);
#ifdef STRICT
pushRedex(Rslt_26Args, newRef(glblProto42_1017));
#else
swap(termLoc(Rslt_26), makePair(LAZ, 0, Rslt_26Args, newRef(glblProto42_1017)));
#endif
// call = at core: 1607
Term Rslt_36Args = makePair(APP, 0, k_9_1, SUB);
Term Rslt_36 = newTerm(VAR, 0, portLoc(2, Rslt_36Args));
Rslt_36Args = makePair(APP, 0, currKey_29, Rslt_36Args);
#ifdef STRICT
pushRedex(Rslt_36Args, newRef(glblProto20_1019));
#else
swap(termLoc(Rslt_36), makePair(LAZ, 0, Rslt_36Args, newRef(glblProto20_1019)));
#endif
// call bmiSetKV at core: 1608
Term Rslt_37Args = makePair(APP, 0, currVal_33, SUB);
Term Rslt_37 = newTerm(VAR, 0, portLoc(2, Rslt_37Args));
Rslt_37Args = makePair(APP, 0, currKey_29_1, Rslt_37Args);
Rslt_37Args = makePair(APP, 0, v_3_1, Rslt_37Args);
Rslt_37Args = makePair(APP, 0, k_9_2, Rslt_37Args);
Rslt_37Args = makePair(APP, 0, bit_13_3, Rslt_37Args);
Rslt_37Args = makePair(APP, 0, m_1_3, Rslt_37Args);
#ifdef STRICT
pushRedex(Rslt_37Args, glblFn911);
#else
swap(termLoc(Rslt_37), makePair(LAZ, 0, Rslt_37Args, glblFn911));
#endif

// call bmiReplaceMutate at core: 1609
Term Rslt_38Args = makePair(APP, 0, currVal_33_1, SUB);
Term Rslt_38 = newTerm(VAR, 0, portLoc(2, Rslt_38Args));
Rslt_38Args = makePair(APP, 0, currKey_29_2, Rslt_38Args);
Rslt_38Args = makePair(APP, 0, shift_5_1, Rslt_38Args);
Rslt_38Args = makePair(APP, 0, hash_4_1, Rslt_38Args);
Rslt_38Args = makePair(APP, 0, v_3_2, Rslt_38Args);
Rslt_38Args = makePair(APP, 0, k_9_3, Rslt_38Args);
Rslt_38Args = makePair(APP, 0, m_1_4, Rslt_38Args);
#ifdef STRICT
pushRedex(Rslt_38Args, glblFn908);
#else
swap(termLoc(Rslt_38), makePair(LAZ, 0, Rslt_38Args, glblFn908));
#endif

// call cond at core: 1607
Term Rslt_39Args = makePair(APP, 0, Rslt_38, SUB);
Term Rslt_39 = newTerm(VAR, 0, portLoc(2, Rslt_39Args));
Rslt_39Args = makePair(APP, 0, Rslt_37, Rslt_39Args);
Rslt_39Args = makePair(APP, 0, Rslt_36, Rslt_39Args);
#ifdef STRICT
pushRedex(Rslt_39Args, newRef(glblProto34_1021));
#else
swap(termLoc(Rslt_39), makePair(LAZ, 0, Rslt_39Args, newRef(glblProto34_1021)));
#endif
// link args to body
swap(r_seq_34, Rslt_39);

Term Rslt_39V = get(termLoc(Rslt_39));
apps = take(portLoc(1, Rslt_39V));
lams = take(portLoc(2, Rslt_39V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_39), makePair(LAZ, 0, apps, lams));
// call bmiVal at core: 1606
Term Rslt_40Args = makePair(APP, 0, bit_13_4, SUB);
Term Rslt_40 = newTerm(VAR, 0, portLoc(2, Rslt_40Args));
Rslt_40Args = makePair(APP, 0, m_1_5, Rslt_40Args);
#ifdef STRICT
pushRedex(Rslt_40Args, glblFn850);
#else
swap(termLoc(Rslt_40), makePair(LAZ, 0, Rslt_40Args, glblFn850));
#endif

// call  at core: 1606
Term Rslt_41Args = makePair(APP, 0, Rslt_40, SUB);
Term Rslt_41 = newTerm(VAR, 0, portLoc(2, Rslt_41Args));
#ifdef STRICT
pushRedex(Rslt_41Args, seq_34);
#else
swap(termLoc(Rslt_41), makePair(LAZ, 0, Rslt_41Args, seq_34));
#endif

// link args to body
swap(r_seq_30, Rslt_41);

Term Rslt_41V = get(termLoc(Rslt_41));
apps = take(portLoc(1, Rslt_41V));
lams = take(portLoc(2, Rslt_41V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_41), makePair(LAZ, 0, apps, lams));
// call bmiKey at core: 1605
Term Rslt_42Args = makePair(APP, 0, bit_13_5, SUB);
Term Rslt_42 = newTerm(VAR, 0, portLoc(2, Rslt_42Args));
Rslt_42Args = makePair(APP, 0, m_1_6, Rslt_42Args);
#ifdef STRICT
pushRedex(Rslt_42Args, glblFn821);
#else
swap(termLoc(Rslt_42), makePair(LAZ, 0, Rslt_42Args, glblFn821));
#endif

// call  at core: 1605
Term Rslt_43Args = makePair(APP, 0, Rslt_42, SUB);
Term Rslt_43 = newTerm(VAR, 0, portLoc(2, Rslt_43Args));
#ifdef STRICT
pushRedex(Rslt_43Args, seq_30);
#else
swap(termLoc(Rslt_43), makePair(LAZ, 0, Rslt_43Args, seq_30));
#endif

// call either at core: 1602
Term Rslt_44Args = makePair(APP, 0, Rslt_43, SUB);
Term Rslt_44 = newTerm(VAR, 0, portLoc(2, Rslt_44Args));
Rslt_44Args = makePair(APP, 0, Rslt_26, Rslt_44Args);
#ifdef STRICT
pushRedex(Rslt_44Args, newRef(glblProto32_1023));
#else
swap(termLoc(Rslt_44), makePair(LAZ, 0, Rslt_44Args, newRef(glblProto32_1023)));
#endif
// call addCopiedBMI at core: 1610
Term Rslt_45Args = makePair(APP, 0, shift_5_2, SUB);
Term Rslt_45 = newTerm(VAR, 0, portLoc(2, Rslt_45Args));
Rslt_45Args = makePair(APP, 0, hash_4_2, Rslt_45Args);
Rslt_45Args = makePair(APP, 0, v_3_3, Rslt_45Args);
Rslt_45Args = makePair(APP, 0, k_9_4, Rslt_45Args);
Rslt_45Args = makePair(APP, 0, m_1_7, Rslt_45Args);
#ifdef STRICT
pushRedex(Rslt_45Args, glblFn762);
#else
swap(termLoc(Rslt_45), makePair(LAZ, 0, Rslt_45Args, glblFn762));
#endif

// call int-cond at core: 1601
Term Rslt_46Args = makePair(APP, 0, Rslt_45, SUB);
Term Rslt_46 = newTerm(VAR, 0, portLoc(2, Rslt_46Args));
Rslt_46Args = makePair(APP, 0, Rslt_44, Rslt_46Args);
Rslt_46Args = makePair(APP, 0, Rslt_17, Rslt_46Args);
#ifdef STRICT
pushRedex(Rslt_46Args, glblVal262_7);
#else
swap(termLoc(Rslt_46), makePair(LAZ, 0, Rslt_46Args, glblVal262_7));
#endif

// link args to body
swap(r_seq_14, Rslt_46);

Term Rslt_46V = get(termLoc(Rslt_46));
apps = take(portLoc(1, Rslt_46V));
lams = take(portLoc(2, Rslt_46V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_46), makePair(LAZ, 0, apps, lams));
// call bitpos at core: 1600
Term Rslt_47Args = makePair(APP, 0, shift_5_3, SUB);
Term Rslt_47 = newTerm(VAR, 0, portLoc(2, Rslt_47Args));
Rslt_47Args = makePair(APP, 0, hash_4_3, Rslt_47Args);
#ifdef STRICT
pushRedex(Rslt_47Args, glblFn766);
#else
swap(termLoc(Rslt_47), makePair(LAZ, 0, Rslt_47Args, glblFn766));
#endif

// call  at core: 1600
Term Rslt_48Args = makePair(APP, 0, Rslt_47, SUB);
Term Rslt_48 = newTerm(VAR, 0, portLoc(2, Rslt_48Args));
#ifdef STRICT
pushRedex(Rslt_48Args, seq_14);
#else
swap(termLoc(Rslt_48), makePair(LAZ, 0, Rslt_48Args, seq_14));
#endif

// link args to body
swap(r_seq_10, Rslt_48);

Term Rslt_48V = get(termLoc(Rslt_48));
apps = take(portLoc(1, Rslt_48V));
lams = take(portLoc(2, Rslt_48V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_48), makePair(LAZ, 0, apps, lams));
// call set-hash-val at core: 1599
Term Rslt_49Args = makePair(APP, 0, hash_4_4, SUB);
Term Rslt_49 = newTerm(VAR, 0, portLoc(2, Rslt_49Args));
Rslt_49Args = makePair(APP, 0, k_2, Rslt_49Args);
#ifdef STRICT
pushRedex(Rslt_49Args, glblFn917);
#else
swap(termLoc(Rslt_49), makePair(LAZ, 0, Rslt_49Args, glblFn917));
#endif

// call  at core: 1599
Term Rslt_50Args = makePair(APP, 0, Rslt_49, SUB);
Term Rslt_50 = newTerm(VAR, 0, portLoc(2, Rslt_50Args));
#ifdef STRICT
pushRedex(Rslt_50Args, seq_10);
#else
swap(termLoc(Rslt_50), makePair(LAZ, 0, Rslt_50Args, seq_10));
#endif

// link args to body
swap(r_seq_6, Rslt_50);

Term Rslt_50V = get(termLoc(Rslt_50));
apps = take(portLoc(1, Rslt_50V));
lams = take(portLoc(2, Rslt_50V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_50), makePair(LAZ, 0, apps, lams));
pushRedex(args, seq_6);
return;
}
Term glblFn1014 = newRef(glblCFn1013);
void glblProto109_1028(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(1629), args);
  args = makePair(APP, 0, glblStr199, args);
  interact(args, glblProto109);
}
}
void glblProto42_1030(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(1627), args);
  args = makePair(APP, 0, glblStr199, args);
  interact(args, glblProto42);
}
}
void glblProto109_1032(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(1630), args);
  args = makePair(APP, 0, glblStr199, args);
  interact(args, glblProto109);
}
}
void glblProto32_1034(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(1627), args);
  args = makePair(APP, 0, glblStr199, args);
  interact(args, glblProto32);
}
}
Term glblFn1027;
void glblCFn1026(Term ref, Term args) {
Term apps, lams, V;
Location vLoc;
Term seq_6 = makePair(LAM, 0, SUB, NUL);
Location r_seq_6 = portLoc(2, seq_6);
Term shift_5 = newTerm(VAR, 0, portLoc(1, seq_6));
seq_6 = makePair(LAM, 0, SUB, seq_6);
Term hash_4 = newTerm(VAR, 0, portLoc(1, seq_6));
seq_6 = makePair(LAM, 0, SUB, seq_6);
Term v_3 = newTerm(VAR, 0, portLoc(1, seq_6));
seq_6 = makePair(LAM, 0, SUB, seq_6);
Term k_2 = newTerm(VAR, 0, portLoc(1, seq_6));
seq_6 = makePair(LAM, 0, SUB, seq_6);
Term m_1 = newTerm(VAR, 0, portLoc(1, seq_6));
// allocate args at core: 1612

Term m_1_2;
m_1 = dupeArg(m_1, &m_1_2, 131);

Term m_1_1;
m_1 = dupeArg(m_1, &m_1_1, 132);

Term v_3_1;
v_3 = dupeArg(v_3, &v_3_1, 133);

Term hash_4_3;
hash_4 = dupeArg(hash_4, &hash_4_3, 134);

Term hash_4_2;
hash_4 = dupeArg(hash_4, &hash_4_2, 135);

Term hash_4_1;
hash_4 = dupeArg(hash_4, &hash_4_1, 136);

Term shift_5_2;
shift_5 = dupeArg(shift_5, &shift_5_2, 137);

Term shift_5_1;
shift_5 = dupeArg(shift_5, &shift_5_1, 138);
Term seq_10 = makePair(LAM, 0, SUB, NUL);
Location r_seq_10 = portLoc(2, seq_10);
Term k_9 = newTerm(VAR, 0, portLoc(1, seq_10));
// allocate args at core: 1625

Term k_9_1;
k_9 = dupeArg(k_9, &k_9_1, 0);
Term seq_14 = makePair(LAM, 0, SUB, NUL);
Location r_seq_14 = portLoc(2, seq_14);
Term idx_13 = newTerm(VAR, 0, portLoc(1, seq_14));
// allocate args at core: 1626

Term idx_13_2;
idx_13 = dupeArg(idx_13, &idx_13_2, 0);

Term idx_13_1;
idx_13 = dupeArg(idx_13, &idx_13_1, 0);
Term glblVal106_1;
glblVal106 = dupeArg(glblVal106, &glblVal106_1, 0);

Term seq_19 = makePair(LAM, 0, SUB, NUL);
Location r_seq_19 = portLoc(2, seq_19);
Term child_18 = newTerm(VAR, 0, portLoc(1, seq_19));
// allocate args at core: 1628
// call arrayNodeCopyChild at core: 1627
Term Rslt_16Args = makePair(APP, 0, idx_13, SUB);
Term Rslt_16 = newTerm(VAR, 0, portLoc(2, Rslt_16Args));
Rslt_16Args = makePair(APP, 0, m_1, Rslt_16Args);
#ifdef STRICT
pushRedex(Rslt_16Args, glblFn744);
#else
swap(termLoc(Rslt_16), makePair(LAZ, 0, Rslt_16Args, glblFn744));
#endif

Term Rslt_21 = makeOp(OP_ADD, newI60(5), shift_5);
// call copyAssoc at core: 1629
Term Rslt_22Args = makePair(APP, 0, Rslt_21, SUB);
Term Rslt_22 = newTerm(VAR, 0, portLoc(2, Rslt_22Args));
Rslt_22Args = makePair(APP, 0, hash_4, Rslt_22Args);
Rslt_22Args = makePair(APP, 0, v_3, Rslt_22Args);
Rslt_22Args = makePair(APP, 0, k_9, Rslt_22Args);
Rslt_22Args = makePair(APP, 0, child_18, Rslt_22Args);
#ifdef STRICT
pushRedex(Rslt_22Args, newRef(glblProto109_1028));
#else
swap(termLoc(Rslt_22), makePair(LAZ, 0, Rslt_22Args, newRef(glblProto109_1028)));
#endif
// call arrayNodeUpdate at core: 1629
Term Rslt_23Args = makePair(APP, 0, Rslt_22, SUB);
Term Rslt_23 = newTerm(VAR, 0, portLoc(2, Rslt_23Args));
Rslt_23Args = makePair(APP, 0, idx_13_1, Rslt_23Args);
Rslt_23Args = makePair(APP, 0, m_1_1, Rslt_23Args);
#ifdef STRICT
pushRedex(Rslt_23Args, glblFn750);
#else
swap(termLoc(Rslt_23), makePair(LAZ, 0, Rslt_23Args, glblFn750));
#endif

// link args to body
swap(r_seq_19, Rslt_23);

Term Rslt_23V = get(termLoc(Rslt_23));
apps = take(portLoc(1, Rslt_23V));
lams = take(portLoc(2, Rslt_23V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_23), makePair(LAZ, 0, apps, lams));
// call map at core: 1627
Term Rslt_24Args = makePair(APP, 0, seq_19, SUB);
Term Rslt_24 = newTerm(VAR, 0, portLoc(2, Rslt_24Args));
Rslt_24Args = makePair(APP, 0, Rslt_16, Rslt_24Args);
#ifdef STRICT
pushRedex(Rslt_24Args, newRef(glblProto42_1030));
#else
swap(termLoc(Rslt_24), makePair(LAZ, 0, Rslt_24Args, newRef(glblProto42_1030)));
#endif
Term Rslt_26 = makeOp(OP_ADD, newI60(5), shift_5_1);
// call copyAssoc at core: 1630
Term Rslt_27Args = makePair(APP, 0, Rslt_26, SUB);
Term Rslt_27 = newTerm(VAR, 0, portLoc(2, Rslt_27Args));
Rslt_27Args = makePair(APP, 0, hash_4_1, Rslt_27Args);
Rslt_27Args = makePair(APP, 0, v_3_1, Rslt_27Args);
Rslt_27Args = makePair(APP, 0, k_9_1, Rslt_27Args);
Rslt_27Args = makePair(APP, 0, glblVal106_1, Rslt_27Args);
#ifdef STRICT
pushRedex(Rslt_27Args, newRef(glblProto109_1032));
#else
swap(termLoc(Rslt_27), makePair(LAZ, 0, Rslt_27Args, newRef(glblProto109_1032)));
#endif
// call arrayNodeUpdate at core: 1630
Term Rslt_28Args = makePair(APP, 0, Rslt_27, SUB);
Term Rslt_28 = newTerm(VAR, 0, portLoc(2, Rslt_28Args));
Rslt_28Args = makePair(APP, 0, idx_13_2, Rslt_28Args);
Rslt_28Args = makePair(APP, 0, m_1_2, Rslt_28Args);
#ifdef STRICT
pushRedex(Rslt_28Args, glblFn750);
#else
swap(termLoc(Rslt_28), makePair(LAZ, 0, Rslt_28Args, glblFn750));
#endif

// call either at core: 1627
Term Rslt_29Args = makePair(APP, 0, Rslt_28, SUB);
Term Rslt_29 = newTerm(VAR, 0, portLoc(2, Rslt_29Args));
Rslt_29Args = makePair(APP, 0, Rslt_24, Rslt_29Args);
#ifdef STRICT
pushRedex(Rslt_29Args, newRef(glblProto32_1034));
#else
swap(termLoc(Rslt_29), makePair(LAZ, 0, Rslt_29Args, newRef(glblProto32_1034)));
#endif
// link args to body
swap(r_seq_14, Rslt_29);

Term Rslt_29V = get(termLoc(Rslt_29));
apps = take(portLoc(1, Rslt_29V));
lams = take(portLoc(2, Rslt_29V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_29), makePair(LAZ, 0, apps, lams));
Term Rslt_30 = makeOp(OP_RSH, hash_4_2, shift_5_2);
Term Rslt_31 = makeOp(OP_AND, Rslt_30, newI60(31));
// call  at core: 1626
Term Rslt_32Args = makePair(APP, 0, Rslt_31, SUB);
Term Rslt_32 = newTerm(VAR, 0, portLoc(2, Rslt_32Args));
#ifdef STRICT
pushRedex(Rslt_32Args, seq_14);
#else
swap(termLoc(Rslt_32), makePair(LAZ, 0, Rslt_32Args, seq_14));
#endif

// link args to body
swap(r_seq_10, Rslt_32);

Term Rslt_32V = get(termLoc(Rslt_32));
apps = take(portLoc(1, Rslt_32V));
lams = take(portLoc(2, Rslt_32V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_32), makePair(LAZ, 0, apps, lams));
// call set-hash-val at core: 1625
Term Rslt_33Args = makePair(APP, 0, hash_4_3, SUB);
Term Rslt_33 = newTerm(VAR, 0, portLoc(2, Rslt_33Args));
Rslt_33Args = makePair(APP, 0, k_2, Rslt_33Args);
#ifdef STRICT
pushRedex(Rslt_33Args, glblFn917);
#else
swap(termLoc(Rslt_33), makePair(LAZ, 0, Rslt_33Args, glblFn917));
#endif

// call  at core: 1625
Term Rslt_34Args = makePair(APP, 0, Rslt_33, SUB);
Term Rslt_34 = newTerm(VAR, 0, portLoc(2, Rslt_34Args));
#ifdef STRICT
pushRedex(Rslt_34Args, seq_10);
#else
swap(termLoc(Rslt_34), makePair(LAZ, 0, Rslt_34Args, seq_10));
#endif

// link args to body
swap(r_seq_6, Rslt_34);

Term Rslt_34V = get(termLoc(Rslt_34));
apps = take(portLoc(1, Rslt_34V));
lams = take(portLoc(2, Rslt_34V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_34), makePair(LAZ, 0, apps, lams));
pushRedex(args, seq_6);
return;
}
Term glblFn1027 = newRef(glblCFn1026);
void glblProto111_1039(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(1654), args);
  args = makePair(APP, 0, glblStr199, args);
  interact(args, glblProto111);
}
}
void glblProto42_1041(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(1652), args);
  args = makePair(APP, 0, glblStr199, args);
  interact(args, glblProto42);
}
}
void glblProto109_1043(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(1655), args);
  args = makePair(APP, 0, glblStr199, args);
  interact(args, glblProto109);
}
}
void glblProto32_1045(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(1652), args);
  args = makePair(APP, 0, glblStr199, args);
  interact(args, glblProto32);
}
}
Term glblFn1038;
void glblCFn1037(Term ref, Term args) {
Term apps, lams, V;
Location vLoc;
Term seq_6 = makePair(LAM, 0, SUB, NUL);
Location r_seq_6 = portLoc(2, seq_6);
Term shift_5 = newTerm(VAR, 0, portLoc(1, seq_6));
seq_6 = makePair(LAM, 0, SUB, seq_6);
Term hash_4 = newTerm(VAR, 0, portLoc(1, seq_6));
seq_6 = makePair(LAM, 0, SUB, seq_6);
Term v_3 = newTerm(VAR, 0, portLoc(1, seq_6));
seq_6 = makePair(LAM, 0, SUB, seq_6);
Term k_2 = newTerm(VAR, 0, portLoc(1, seq_6));
seq_6 = makePair(LAM, 0, SUB, seq_6);
Term m_1 = newTerm(VAR, 0, portLoc(1, seq_6));
// allocate args at core: 1632

Term m_1_2;
m_1 = dupeArg(m_1, &m_1_2, 139);

Term m_1_1;
m_1 = dupeArg(m_1, &m_1_1, 140);

Term v_3_1;
v_3 = dupeArg(v_3, &v_3_1, 141);

Term hash_4_3;
hash_4 = dupeArg(hash_4, &hash_4_3, 142);

Term hash_4_2;
hash_4 = dupeArg(hash_4, &hash_4_2, 143);

Term hash_4_1;
hash_4 = dupeArg(hash_4, &hash_4_1, 144);

Term shift_5_2;
shift_5 = dupeArg(shift_5, &shift_5_2, 145);

Term shift_5_1;
shift_5 = dupeArg(shift_5, &shift_5_1, 146);
Term seq_10 = makePair(LAM, 0, SUB, NUL);
Location r_seq_10 = portLoc(2, seq_10);
Term k_9 = newTerm(VAR, 0, portLoc(1, seq_10));
// allocate args at core: 1650

Term k_9_1;
k_9 = dupeArg(k_9, &k_9_1, 0);
Term seq_14 = makePair(LAM, 0, SUB, NUL);
Location r_seq_14 = portLoc(2, seq_14);
Term idx_13 = newTerm(VAR, 0, portLoc(1, seq_14));
// allocate args at core: 1651

Term idx_13_2;
idx_13 = dupeArg(idx_13, &idx_13_2, 0);

Term idx_13_1;
idx_13 = dupeArg(idx_13, &idx_13_1, 0);
Term glblVal106_2;
glblVal106 = dupeArg(glblVal106, &glblVal106_2, 0);

Term seq_19 = makePair(LAM, 0, SUB, NUL);
Location r_seq_19 = portLoc(2, seq_19);
Term child_18 = newTerm(VAR, 0, portLoc(1, seq_19));
// allocate args at core: 1653
// call arrayNodeMutateChild at core: 1652
Term Rslt_16Args = makePair(APP, 0, idx_13, SUB);
Term Rslt_16 = newTerm(VAR, 0, portLoc(2, Rslt_16Args));
Rslt_16Args = makePair(APP, 0, m_1, Rslt_16Args);
#ifdef STRICT
pushRedex(Rslt_16Args, glblFn752);
#else
swap(termLoc(Rslt_16), makePair(LAZ, 0, Rslt_16Args, glblFn752));
#endif

Term Rslt_21 = makeOp(OP_ADD, newI60(5), shift_5);
// call assoc* at core: 1654
Term Rslt_22Args = makePair(APP, 0, Rslt_21, SUB);
Term Rslt_22 = newTerm(VAR, 0, portLoc(2, Rslt_22Args));
Rslt_22Args = makePair(APP, 0, hash_4, Rslt_22Args);
Rslt_22Args = makePair(APP, 0, v_3, Rslt_22Args);
Rslt_22Args = makePair(APP, 0, k_9, Rslt_22Args);
Rslt_22Args = makePair(APP, 0, child_18, Rslt_22Args);
#ifdef STRICT
pushRedex(Rslt_22Args, newRef(glblProto111_1039));
#else
swap(termLoc(Rslt_22), makePair(LAZ, 0, Rslt_22Args, newRef(glblProto111_1039)));
#endif
// call arrayNodeSetSlot at core: 1654
Term Rslt_23Args = makePair(APP, 0, Rslt_22, SUB);
Term Rslt_23 = newTerm(VAR, 0, portLoc(2, Rslt_23Args));
Rslt_23Args = makePair(APP, 0, idx_13_1, Rslt_23Args);
Rslt_23Args = makePair(APP, 0, m_1_1, Rslt_23Args);
#ifdef STRICT
pushRedex(Rslt_23Args, glblFn754);
#else
swap(termLoc(Rslt_23), makePair(LAZ, 0, Rslt_23Args, glblFn754));
#endif

// link args to body
swap(r_seq_19, Rslt_23);

Term Rslt_23V = get(termLoc(Rslt_23));
apps = take(portLoc(1, Rslt_23V));
lams = take(portLoc(2, Rslt_23V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_23), makePair(LAZ, 0, apps, lams));
// call map at core: 1652
Term Rslt_24Args = makePair(APP, 0, seq_19, SUB);
Term Rslt_24 = newTerm(VAR, 0, portLoc(2, Rslt_24Args));
Rslt_24Args = makePair(APP, 0, Rslt_16, Rslt_24Args);
#ifdef STRICT
pushRedex(Rslt_24Args, newRef(glblProto42_1041));
#else
swap(termLoc(Rslt_24), makePair(LAZ, 0, Rslt_24Args, newRef(glblProto42_1041)));
#endif
Term Rslt_26 = makeOp(OP_ADD, newI60(5), shift_5_1);
// call copyAssoc at core: 1655
Term Rslt_27Args = makePair(APP, 0, Rslt_26, SUB);
Term Rslt_27 = newTerm(VAR, 0, portLoc(2, Rslt_27Args));
Rslt_27Args = makePair(APP, 0, hash_4_1, Rslt_27Args);
Rslt_27Args = makePair(APP, 0, v_3_1, Rslt_27Args);
Rslt_27Args = makePair(APP, 0, k_9_1, Rslt_27Args);
Rslt_27Args = makePair(APP, 0, glblVal106_2, Rslt_27Args);
#ifdef STRICT
pushRedex(Rslt_27Args, newRef(glblProto109_1043));
#else
swap(termLoc(Rslt_27), makePair(LAZ, 0, Rslt_27Args, newRef(glblProto109_1043)));
#endif
// call arrayNodeSetSlot at core: 1655
Term Rslt_28Args = makePair(APP, 0, Rslt_27, SUB);
Term Rslt_28 = newTerm(VAR, 0, portLoc(2, Rslt_28Args));
Rslt_28Args = makePair(APP, 0, idx_13_2, Rslt_28Args);
Rslt_28Args = makePair(APP, 0, m_1_2, Rslt_28Args);
#ifdef STRICT
pushRedex(Rslt_28Args, glblFn754);
#else
swap(termLoc(Rslt_28), makePair(LAZ, 0, Rslt_28Args, glblFn754));
#endif

// call either at core: 1652
Term Rslt_29Args = makePair(APP, 0, Rslt_28, SUB);
Term Rslt_29 = newTerm(VAR, 0, portLoc(2, Rslt_29Args));
Rslt_29Args = makePair(APP, 0, Rslt_24, Rslt_29Args);
#ifdef STRICT
pushRedex(Rslt_29Args, newRef(glblProto32_1045));
#else
swap(termLoc(Rslt_29), makePair(LAZ, 0, Rslt_29Args, newRef(glblProto32_1045)));
#endif
// link args to body
swap(r_seq_14, Rslt_29);

Term Rslt_29V = get(termLoc(Rslt_29));
apps = take(portLoc(1, Rslt_29V));
lams = take(portLoc(2, Rslt_29V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_29), makePair(LAZ, 0, apps, lams));
Term Rslt_30 = makeOp(OP_RSH, hash_4_2, shift_5_2);
Term Rslt_31 = makeOp(OP_AND, Rslt_30, newI60(31));
// call  at core: 1651
Term Rslt_32Args = makePair(APP, 0, Rslt_31, SUB);
Term Rslt_32 = newTerm(VAR, 0, portLoc(2, Rslt_32Args));
#ifdef STRICT
pushRedex(Rslt_32Args, seq_14);
#else
swap(termLoc(Rslt_32), makePair(LAZ, 0, Rslt_32Args, seq_14));
#endif

// link args to body
swap(r_seq_10, Rslt_32);

Term Rslt_32V = get(termLoc(Rslt_32));
apps = take(portLoc(1, Rslt_32V));
lams = take(portLoc(2, Rslt_32V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_32), makePair(LAZ, 0, apps, lams));
// call set-hash-val at core: 1650
Term Rslt_33Args = makePair(APP, 0, hash_4_3, SUB);
Term Rslt_33 = newTerm(VAR, 0, portLoc(2, Rslt_33Args));
Rslt_33Args = makePair(APP, 0, k_2, Rslt_33Args);
#ifdef STRICT
pushRedex(Rslt_33Args, glblFn917);
#else
swap(termLoc(Rslt_33), makePair(LAZ, 0, Rslt_33Args, glblFn917));
#endif

// call  at core: 1650
Term Rslt_34Args = makePair(APP, 0, Rslt_33, SUB);
Term Rslt_34 = newTerm(VAR, 0, portLoc(2, Rslt_34Args));
#ifdef STRICT
pushRedex(Rslt_34Args, seq_10);
#else
swap(termLoc(Rslt_34), makePair(LAZ, 0, Rslt_34Args, seq_10));
#endif

// link args to body
swap(r_seq_6, Rslt_34);

Term Rslt_34V = get(termLoc(Rslt_34));
apps = take(portLoc(1, Rslt_34V));
lams = take(portLoc(2, Rslt_34V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_34), makePair(LAZ, 0, apps, lams));
pushRedex(args, seq_6);
return;
}
Term glblFn1038 = newRef(glblCFn1037);
void glblProto113_1050(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(1669), args);
  args = makePair(APP, 0, glblStr199, args);
  interact(args, glblProto113);
}
}
void glblProto42_1052(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(1667), args);
  args = makePair(APP, 0, glblStr199, args);
  interact(args, glblProto42);
}
}
void glblProto20_1054(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(1671), args);
  args = makePair(APP, 0, glblStr199, args);
  interact(args, glblProto20);
}
}
void glblProto34_1056(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(1671), args);
  args = makePair(APP, 0, glblStr199, args);
  interact(args, glblProto34);
}
}
void glblProto32_1058(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(1667), args);
  args = makePair(APP, 0, glblStr199, args);
  interact(args, glblProto32);
}
}
Term glblFn1049;
void glblCFn1048(Term ref, Term args) {
Term apps, lams, V;
Location vLoc;
Term seq_6 = makePair(LAM, 0, SUB, NUL);
Location r_seq_6 = portLoc(2, seq_6);
Term shift_5 = newTerm(VAR, 0, portLoc(1, seq_6));
seq_6 = makePair(LAM, 0, SUB, seq_6);
Term hash_4 = newTerm(VAR, 0, portLoc(1, seq_6));
seq_6 = makePair(LAM, 0, SUB, seq_6);
Term def_3 = newTerm(VAR, 0, portLoc(1, seq_6));
seq_6 = makePair(LAM, 0, SUB, seq_6);
Term k_2 = newTerm(VAR, 0, portLoc(1, seq_6));
seq_6 = makePair(LAM, 0, SUB, seq_6);
Term m_1 = newTerm(VAR, 0, portLoc(1, seq_6));
// allocate args at core: 1657

Term m_1_3;
m_1 = dupeArg(m_1, &m_1_3, 147);

Term m_1_2;
m_1 = dupeArg(m_1, &m_1_2, 148);

Term m_1_1;
m_1 = dupeArg(m_1, &m_1_1, 149);

Term k_2_1;
k_2 = dupeArg(k_2, &k_2_1, 150);

Term def_3_2;
def_3 = dupeArg(def_3, &def_3_2, 151);

Term def_3_1;
def_3 = dupeArg(def_3, &def_3_1, 152);

Term hash_4_1;
hash_4 = dupeArg(hash_4, &hash_4_1, 153);

Term shift_5_1;
shift_5 = dupeArg(shift_5, &shift_5_1, 154);
Term seq_10 = makePair(LAM, 0, SUB, NUL);
Location r_seq_10 = portLoc(2, seq_10);
Term bit_9 = newTerm(VAR, 0, portLoc(1, seq_10));
// allocate args at core: 1665

Term bit_9_3;
bit_9 = dupeArg(bit_9, &bit_9_3, 0);

Term bit_9_2;
bit_9 = dupeArg(bit_9, &bit_9_2, 0);

Term bit_9_1;
bit_9 = dupeArg(bit_9, &bit_9_1, 0);
Term seq_25 = makePair(LAM, 0, SUB, NUL);
Location r_seq_25 = portLoc(2, seq_25);
Term currKey_24 = newTerm(VAR, 0, portLoc(1, seq_25));
// allocate args at core: 1670
Term seq_17 = makePair(LAM, 0, SUB, NUL);
Location r_seq_17 = portLoc(2, seq_17);
Term childNode_16 = newTerm(VAR, 0, portLoc(1, seq_17));
// allocate args at core: 1668
Term glblVal262_8;
glblVal262 = dupeArg(glblVal262, &glblVal262_8, 0);

// call bmiBitMap at core: 1666
Term Rslt_12Args = makePair(APP, 0, m_1, SUB);
Term Rslt_12 = newTerm(VAR, 0, portLoc(2, Rslt_12Args));
#ifdef STRICT
pushRedex(Rslt_12Args, glblFn764);
#else
swap(termLoc(Rslt_12), makePair(LAZ, 0, Rslt_12Args, glblFn764));
#endif

Term Rslt_13 = makeOp(OP_AND, bit_9, Rslt_12);
// call bmiCopyChild at core: 1667
Term Rslt_14Args = makePair(APP, 0, bit_9_1, SUB);
Term Rslt_14 = newTerm(VAR, 0, portLoc(2, Rslt_14Args));
Rslt_14Args = makePair(APP, 0, m_1_1, Rslt_14Args);
#ifdef STRICT
pushRedex(Rslt_14Args, glblFn774);
#else
swap(termLoc(Rslt_14), makePair(LAZ, 0, Rslt_14Args, glblFn774));
#endif

Term Rslt_19 = makeOp(OP_ADD, newI60(5), shift_5);
// call get* at core: 1669
Term Rslt_20Args = makePair(APP, 0, Rslt_19, SUB);
Term Rslt_20 = newTerm(VAR, 0, portLoc(2, Rslt_20Args));
Rslt_20Args = makePair(APP, 0, hash_4, Rslt_20Args);
Rslt_20Args = makePair(APP, 0, def_3, Rslt_20Args);
Rslt_20Args = makePair(APP, 0, k_2, Rslt_20Args);
Rslt_20Args = makePair(APP, 0, childNode_16, Rslt_20Args);
#ifdef STRICT
pushRedex(Rslt_20Args, newRef(glblProto113_1050));
#else
swap(termLoc(Rslt_20), makePair(LAZ, 0, Rslt_20Args, newRef(glblProto113_1050)));
#endif
// link args to body
swap(r_seq_17, Rslt_20);

Term Rslt_20V = get(termLoc(Rslt_20));
apps = take(portLoc(1, Rslt_20V));
lams = take(portLoc(2, Rslt_20V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_20), makePair(LAZ, 0, apps, lams));
// call map at core: 1667
Term Rslt_21Args = makePair(APP, 0, seq_17, SUB);
Term Rslt_21 = newTerm(VAR, 0, portLoc(2, Rslt_21Args));
Rslt_21Args = makePair(APP, 0, Rslt_14, Rslt_21Args);
#ifdef STRICT
pushRedex(Rslt_21Args, newRef(glblProto42_1052));
#else
swap(termLoc(Rslt_21), makePair(LAZ, 0, Rslt_21Args, newRef(glblProto42_1052)));
#endif
// call = at core: 1671
Term Rslt_27Args = makePair(APP, 0, currKey_24, SUB);
Term Rslt_27 = newTerm(VAR, 0, portLoc(2, Rslt_27Args));
Rslt_27Args = makePair(APP, 0, k_2_1, Rslt_27Args);
#ifdef STRICT
pushRedex(Rslt_27Args, newRef(glblProto20_1054));
#else
swap(termLoc(Rslt_27), makePair(LAZ, 0, Rslt_27Args, newRef(glblProto20_1054)));
#endif
// call bmiVal at core: 1672
Term Rslt_28Args = makePair(APP, 0, bit_9_2, SUB);
Term Rslt_28 = newTerm(VAR, 0, portLoc(2, Rslt_28Args));
Rslt_28Args = makePair(APP, 0, m_1_2, Rslt_28Args);
#ifdef STRICT
pushRedex(Rslt_28Args, glblFn850);
#else
swap(termLoc(Rslt_28), makePair(LAZ, 0, Rslt_28Args, glblFn850));
#endif

// call cond at core: 1671
Term Rslt_29Args = makePair(APP, 0, def_3_1, SUB);
Term Rslt_29 = newTerm(VAR, 0, portLoc(2, Rslt_29Args));
Rslt_29Args = makePair(APP, 0, Rslt_28, Rslt_29Args);
Rslt_29Args = makePair(APP, 0, Rslt_27, Rslt_29Args);
#ifdef STRICT
pushRedex(Rslt_29Args, newRef(glblProto34_1056));
#else
swap(termLoc(Rslt_29), makePair(LAZ, 0, Rslt_29Args, newRef(glblProto34_1056)));
#endif
// link args to body
swap(r_seq_25, Rslt_29);

Term Rslt_29V = get(termLoc(Rslt_29));
apps = take(portLoc(1, Rslt_29V));
lams = take(portLoc(2, Rslt_29V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_29), makePair(LAZ, 0, apps, lams));
// call bmiKey at core: 1670
Term Rslt_30Args = makePair(APP, 0, bit_9_3, SUB);
Term Rslt_30 = newTerm(VAR, 0, portLoc(2, Rslt_30Args));
Rslt_30Args = makePair(APP, 0, m_1_3, Rslt_30Args);
#ifdef STRICT
pushRedex(Rslt_30Args, glblFn821);
#else
swap(termLoc(Rslt_30), makePair(LAZ, 0, Rslt_30Args, glblFn821));
#endif

// call  at core: 1670
Term Rslt_31Args = makePair(APP, 0, Rslt_30, SUB);
Term Rslt_31 = newTerm(VAR, 0, portLoc(2, Rslt_31Args));
#ifdef STRICT
pushRedex(Rslt_31Args, seq_25);
#else
swap(termLoc(Rslt_31), makePair(LAZ, 0, Rslt_31Args, seq_25));
#endif

// call either at core: 1667
Term Rslt_32Args = makePair(APP, 0, Rslt_31, SUB);
Term Rslt_32 = newTerm(VAR, 0, portLoc(2, Rslt_32Args));
Rslt_32Args = makePair(APP, 0, Rslt_21, Rslt_32Args);
#ifdef STRICT
pushRedex(Rslt_32Args, newRef(glblProto32_1058));
#else
swap(termLoc(Rslt_32), makePair(LAZ, 0, Rslt_32Args, newRef(glblProto32_1058)));
#endif
// call int-cond at core: 1666
Term Rslt_33Args = makePair(APP, 0, def_3_2, SUB);
Term Rslt_33 = newTerm(VAR, 0, portLoc(2, Rslt_33Args));
Rslt_33Args = makePair(APP, 0, Rslt_32, Rslt_33Args);
Rslt_33Args = makePair(APP, 0, Rslt_13, Rslt_33Args);
#ifdef STRICT
pushRedex(Rslt_33Args, glblVal262_8);
#else
swap(termLoc(Rslt_33), makePair(LAZ, 0, Rslt_33Args, glblVal262_8));
#endif

// link args to body
swap(r_seq_10, Rslt_33);

Term Rslt_33V = get(termLoc(Rslt_33));
apps = take(portLoc(1, Rslt_33V));
lams = take(portLoc(2, Rslt_33V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_33), makePair(LAZ, 0, apps, lams));
// call bitpos at core: 1665
Term Rslt_34Args = makePair(APP, 0, shift_5_1, SUB);
Term Rslt_34 = newTerm(VAR, 0, portLoc(2, Rslt_34Args));
Rslt_34Args = makePair(APP, 0, hash_4_1, Rslt_34Args);
#ifdef STRICT
pushRedex(Rslt_34Args, glblFn766);
#else
swap(termLoc(Rslt_34), makePair(LAZ, 0, Rslt_34Args, glblFn766));
#endif

// call  at core: 1665
Term Rslt_35Args = makePair(APP, 0, Rslt_34, SUB);
Term Rslt_35 = newTerm(VAR, 0, portLoc(2, Rslt_35Args));
#ifdef STRICT
pushRedex(Rslt_35Args, seq_10);
#else
swap(termLoc(Rslt_35), makePair(LAZ, 0, Rslt_35Args, seq_10));
#endif

// link args to body
swap(r_seq_6, Rslt_35);

Term Rslt_35V = get(termLoc(Rslt_35));
apps = take(portLoc(1, Rslt_35V));
lams = take(portLoc(2, Rslt_35V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_35), makePair(LAZ, 0, apps, lams));
pushRedex(args, seq_6);
return;
}
Term glblFn1049 = newRef(glblCFn1048);
void glblProto113_1063(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(1689), args);
  args = makePair(APP, 0, glblStr199, args);
  interact(args, glblProto113);
}
}
void glblProto42_1065(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(1687), args);
  args = makePair(APP, 0, glblStr199, args);
  interact(args, glblProto42);
}
}
void glblProto32_1067(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(1687), args);
  args = makePair(APP, 0, glblStr199, args);
  interact(args, glblProto32);
}
}
Term glblFn1062;
void glblCFn1061(Term ref, Term args) {
Term apps, lams, V;
Location vLoc;
Term seq_6 = makePair(LAM, 0, SUB, NUL);
Location r_seq_6 = portLoc(2, seq_6);
Term shift_5 = newTerm(VAR, 0, portLoc(1, seq_6));
seq_6 = makePair(LAM, 0, SUB, seq_6);
Term hash_4 = newTerm(VAR, 0, portLoc(1, seq_6));
seq_6 = makePair(LAM, 0, SUB, seq_6);
Term def_3 = newTerm(VAR, 0, portLoc(1, seq_6));
seq_6 = makePair(LAM, 0, SUB, seq_6);
Term k_2 = newTerm(VAR, 0, portLoc(1, seq_6));
seq_6 = makePair(LAM, 0, SUB, seq_6);
Term m_1 = newTerm(VAR, 0, portLoc(1, seq_6));
// allocate args at core: 1676

Term def_3_1;
def_3 = dupeArg(def_3, &def_3_1, 155);

Term hash_4_1;
hash_4 = dupeArg(hash_4, &hash_4_1, 156);

Term shift_5_1;
shift_5 = dupeArg(shift_5, &shift_5_1, 157);
Term seq_10 = makePair(LAM, 0, SUB, NUL);
Location r_seq_10 = portLoc(2, seq_10);
Term idx_9 = newTerm(VAR, 0, portLoc(1, seq_10));
// allocate args at core: 1686
Term seq_15 = makePair(LAM, 0, SUB, NUL);
Location r_seq_15 = portLoc(2, seq_15);
Term child_14 = newTerm(VAR, 0, portLoc(1, seq_15));
// allocate args at core: 1688
// call arrayNodeCopyChild at core: 1687
Term Rslt_12Args = makePair(APP, 0, idx_9, SUB);
Term Rslt_12 = newTerm(VAR, 0, portLoc(2, Rslt_12Args));
Rslt_12Args = makePair(APP, 0, m_1, Rslt_12Args);
#ifdef STRICT
pushRedex(Rslt_12Args, glblFn744);
#else
swap(termLoc(Rslt_12), makePair(LAZ, 0, Rslt_12Args, glblFn744));
#endif

Term Rslt_17 = makeOp(OP_ADD, newI60(5), shift_5);
// call get* at core: 1689
Term Rslt_18Args = makePair(APP, 0, Rslt_17, SUB);
Term Rslt_18 = newTerm(VAR, 0, portLoc(2, Rslt_18Args));
Rslt_18Args = makePair(APP, 0, hash_4, Rslt_18Args);
Rslt_18Args = makePair(APP, 0, def_3, Rslt_18Args);
Rslt_18Args = makePair(APP, 0, k_2, Rslt_18Args);
Rslt_18Args = makePair(APP, 0, child_14, Rslt_18Args);
#ifdef STRICT
pushRedex(Rslt_18Args, newRef(glblProto113_1063));
#else
swap(termLoc(Rslt_18), makePair(LAZ, 0, Rslt_18Args, newRef(glblProto113_1063)));
#endif
// link args to body
swap(r_seq_15, Rslt_18);

Term Rslt_18V = get(termLoc(Rslt_18));
apps = take(portLoc(1, Rslt_18V));
lams = take(portLoc(2, Rslt_18V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_18), makePair(LAZ, 0, apps, lams));
// call map at core: 1687
Term Rslt_19Args = makePair(APP, 0, seq_15, SUB);
Term Rslt_19 = newTerm(VAR, 0, portLoc(2, Rslt_19Args));
Rslt_19Args = makePair(APP, 0, Rslt_12, Rslt_19Args);
#ifdef STRICT
pushRedex(Rslt_19Args, newRef(glblProto42_1065));
#else
swap(termLoc(Rslt_19), makePair(LAZ, 0, Rslt_19Args, newRef(glblProto42_1065)));
#endif
// call either at core: 1687
Term Rslt_21Args = makePair(APP, 0, def_3_1, SUB);
Term Rslt_21 = newTerm(VAR, 0, portLoc(2, Rslt_21Args));
Rslt_21Args = makePair(APP, 0, Rslt_19, Rslt_21Args);
#ifdef STRICT
pushRedex(Rslt_21Args, newRef(glblProto32_1067));
#else
swap(termLoc(Rslt_21), makePair(LAZ, 0, Rslt_21Args, newRef(glblProto32_1067)));
#endif
// link args to body
swap(r_seq_10, Rslt_21);

Term Rslt_21V = get(termLoc(Rslt_21));
apps = take(portLoc(1, Rslt_21V));
lams = take(portLoc(2, Rslt_21V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_21), makePair(LAZ, 0, apps, lams));
Term Rslt_22 = makeOp(OP_RSH, hash_4_1, shift_5_1);
Term Rslt_23 = makeOp(OP_AND, Rslt_22, newI60(31));
// call  at core: 1686
Term Rslt_24Args = makePair(APP, 0, Rslt_23, SUB);
Term Rslt_24 = newTerm(VAR, 0, portLoc(2, Rslt_24Args));
#ifdef STRICT
pushRedex(Rslt_24Args, seq_10);
#else
swap(termLoc(Rslt_24), makePair(LAZ, 0, Rslt_24Args, seq_10));
#endif

// link args to body
swap(r_seq_6, Rslt_24);

Term Rslt_24V = get(termLoc(Rslt_24));
apps = take(portLoc(1, Rslt_24V));
lams = take(portLoc(2, Rslt_24V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_24), makePair(LAZ, 0, apps, lams));
pushRedex(args, seq_6);
return;
}
Term glblFn1062 = newRef(glblCFn1061);
void glblProto62_1072(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(1709), args);
  args = makePair(APP, 0, glblStr199, args);
  interact(args, glblProto62);
}
}
void glblProto20_1074(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(1709), args);
  args = makePair(APP, 0, glblStr199, args);
  interact(args, glblProto20);
}
}
void glblProto28_1076(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(1709), args);
  args = makePair(APP, 0, glblStr199, args);
  interact(args, glblProto28);
}
}
void glblProto34_1078(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(1709), args);
  args = makePair(APP, 0, glblStr199, args);
  interact(args, glblProto34);
}
}
void glblProto115_1080(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(1708), args);
  args = makePair(APP, 0, glblStr199, args);
  interact(args, glblProto115);
}
}
void glblProto42_1082(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(1706), args);
  args = makePair(APP, 0, glblStr199, args);
  interact(args, glblProto42);
}
}
void glblProto20_1084(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(1713), args);
  args = makePair(APP, 0, glblStr199, args);
  interact(args, glblProto20);
}
}
void glblProto20_1086(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(1714), args);
  args = makePair(APP, 0, glblStr199, args);
  interact(args, glblProto20);
}
}
void glblProto34_1088(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(1714), args);
  args = makePair(APP, 0, glblStr199, args);
  interact(args, glblProto34);
}
}
void glblProto34_1090(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(1713), args);
  args = makePair(APP, 0, glblStr199, args);
  interact(args, glblProto34);
}
}
void glblProto32_1092(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(1706), args);
  args = makePair(APP, 0, glblStr199, args);
  interact(args, glblProto32);
}
}
Term glblFn1071;
void glblCFn1070(Term ref, Term args) {
Term apps, lams, V;
Location vLoc;
Term seq_5 = makePair(LAM, 0, SUB, NUL);
Location r_seq_5 = portLoc(2, seq_5);
Term shift_4 = newTerm(VAR, 0, portLoc(1, seq_5));
seq_5 = makePair(LAM, 0, SUB, seq_5);
Term hash_3 = newTerm(VAR, 0, portLoc(1, seq_5));
seq_5 = makePair(LAM, 0, SUB, seq_5);
Term k_2 = newTerm(VAR, 0, portLoc(1, seq_5));
seq_5 = makePair(LAM, 0, SUB, seq_5);
Term m_1 = newTerm(VAR, 0, portLoc(1, seq_5));
// allocate args at core: 1692

Term m_1_8;
m_1 = dupeArg(m_1, &m_1_8, 158);

Term m_1_7;
m_1 = dupeArg(m_1, &m_1_7, 159);

Term m_1_6;
m_1 = dupeArg(m_1, &m_1_6, 160);

Term m_1_5;
m_1 = dupeArg(m_1, &m_1_5, 161);

Term m_1_4;
m_1 = dupeArg(m_1, &m_1_4, 162);

Term m_1_3;
m_1 = dupeArg(m_1, &m_1_3, 163);

Term m_1_2;
m_1 = dupeArg(m_1, &m_1_2, 164);

Term m_1_1;
m_1 = dupeArg(m_1, &m_1_1, 165);

Term k_2_1;
k_2 = dupeArg(k_2, &k_2_1, 166);

Term hash_3_1;
hash_3 = dupeArg(hash_3, &hash_3_1, 167);

Term shift_4_1;
shift_4 = dupeArg(shift_4, &shift_4_1, 168);
Term seq_9 = makePair(LAM, 0, SUB, NUL);
Location r_seq_9 = portLoc(2, seq_9);
Term bit_8 = newTerm(VAR, 0, portLoc(1, seq_9));
// allocate args at core: 1704

Term bit_8_4;
bit_8 = dupeArg(bit_8, &bit_8_4, 0);

Term bit_8_3;
bit_8 = dupeArg(bit_8, &bit_8_3, 0);

Term bit_8_2;
bit_8 = dupeArg(bit_8, &bit_8_2, 0);

Term bit_8_1;
bit_8 = dupeArg(bit_8, &bit_8_1, 0);
Term seq_35 = makePair(LAM, 0, SUB, NUL);
Location r_seq_35 = portLoc(2, seq_35);
Term currKey_34 = newTerm(VAR, 0, portLoc(1, seq_35));
// allocate args at core: 1712
Term glblVal106_4;
glblVal106 = dupeArg(glblVal106, &glblVal106_4, 0);

Term seq_16 = makePair(LAM, 0, SUB, NUL);
Location r_seq_16 = portLoc(2, seq_16);
Term childNode_15 = newTerm(VAR, 0, portLoc(1, seq_16));
// allocate args at core: 1707
Term seq_20 = makePair(LAM, 0, SUB, NUL);
Location r_seq_20 = portLoc(2, seq_20);
Term n_19 = newTerm(VAR, 0, portLoc(1, seq_20));
// allocate args at core: 1708

Term n_19_1;
n_19 = dupeArg(n_19, &n_19_1, 0);
Term glblVal106_3;
glblVal106 = dupeArg(glblVal106, &glblVal106_3, 0);

Term glblVal262_9;
glblVal262 = dupeArg(glblVal262, &glblVal262_9, 0);

// call bmiBitMap at core: 1705
Term Rslt_11Args = makePair(APP, 0, m_1, SUB);
Term Rslt_11 = newTerm(VAR, 0, portLoc(2, Rslt_11Args));
#ifdef STRICT
pushRedex(Rslt_11Args, glblFn764);
#else
swap(termLoc(Rslt_11), makePair(LAZ, 0, Rslt_11Args, glblFn764));
#endif

Term Rslt_12 = makeOp(OP_AND, bit_8, Rslt_11);
// call bmiCopyChild at core: 1706
Term Rslt_13Args = makePair(APP, 0, bit_8_1, SUB);
Term Rslt_13 = newTerm(VAR, 0, portLoc(2, Rslt_13Args));
Rslt_13Args = makePair(APP, 0, m_1_1, Rslt_13Args);
#ifdef STRICT
pushRedex(Rslt_13Args, glblFn774);
#else
swap(termLoc(Rslt_13), makePair(LAZ, 0, Rslt_13Args, glblFn774));
#endif

// call empty? at core: 1709
Term Rslt_22Args = makePair(APP, 0, n_19, SUB);
Term Rslt_22 = newTerm(VAR, 0, portLoc(2, Rslt_22Args));
#ifdef STRICT
pushRedex(Rslt_22Args, newRef(glblProto62_1072));
#else
swap(termLoc(Rslt_22), makePair(LAZ, 0, Rslt_22Args, newRef(glblProto62_1072)));
#endif
// call bmiSlotCount at core: 1709
Term Rslt_23Args = makePair(APP, 0, m_1_2, SUB);
Term Rslt_23 = newTerm(VAR, 0, portLoc(2, Rslt_23Args));
#ifdef STRICT
pushRedex(Rslt_23Args, glblFn867);
#else
swap(termLoc(Rslt_23), makePair(LAZ, 0, Rslt_23Args, glblFn867));
#endif

// call = at core: 1709
Term Rslt_24Args = makePair(APP, 0, Rslt_23, SUB);
Term Rslt_24 = newTerm(VAR, 0, portLoc(2, Rslt_24Args));
Rslt_24Args = makePair(APP, 0, newI60(1), Rslt_24Args);
#ifdef STRICT
pushRedex(Rslt_24Args, newRef(glblProto20_1074));
#else
swap(termLoc(Rslt_24), makePair(LAZ, 0, Rslt_24Args, newRef(glblProto20_1074)));
#endif
// call and at core: 1709
Term Rslt_25Args = makePair(APP, 0, Rslt_24, SUB);
Term Rslt_25 = newTerm(VAR, 0, portLoc(2, Rslt_25Args));
Rslt_25Args = makePair(APP, 0, Rslt_22, Rslt_25Args);
#ifdef STRICT
pushRedex(Rslt_25Args, newRef(glblProto28_1076));
#else
swap(termLoc(Rslt_25), makePair(LAZ, 0, Rslt_25Args, newRef(glblProto28_1076)));
#endif
// call bmiUpdate at core: 1711
Term Rslt_26Args = makePair(APP, 0, n_19_1, SUB);
Term Rslt_26 = newTerm(VAR, 0, portLoc(2, Rslt_26Args));
Rslt_26Args = makePair(APP, 0, bit_8_2, Rslt_26Args);
Rslt_26Args = makePair(APP, 0, m_1_3, Rslt_26Args);
#ifdef STRICT
pushRedex(Rslt_26Args, glblFn770);
#else
swap(termLoc(Rslt_26), makePair(LAZ, 0, Rslt_26Args, glblFn770));
#endif

// call cond at core: 1709
Term Rslt_27Args = makePair(APP, 0, Rslt_26, SUB);
Term Rslt_27 = newTerm(VAR, 0, portLoc(2, Rslt_27Args));
Rslt_27Args = makePair(APP, 0, glblVal106_3, Rslt_27Args);
Rslt_27Args = makePair(APP, 0, Rslt_25, Rslt_27Args);
#ifdef STRICT
pushRedex(Rslt_27Args, newRef(glblProto34_1078));
#else
swap(termLoc(Rslt_27), makePair(LAZ, 0, Rslt_27Args, newRef(glblProto34_1078)));
#endif
// link args to body
swap(r_seq_20, Rslt_27);

Term Rslt_27V = get(termLoc(Rslt_27));
apps = take(portLoc(1, Rslt_27V));
lams = take(portLoc(2, Rslt_27V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_27), makePair(LAZ, 0, apps, lams));
Term Rslt_28 = makeOp(OP_ADD, newI60(5), shift_4);
// call dissoc* at core: 1708
Term Rslt_29Args = makePair(APP, 0, Rslt_28, SUB);
Term Rslt_29 = newTerm(VAR, 0, portLoc(2, Rslt_29Args));
Rslt_29Args = makePair(APP, 0, hash_3, Rslt_29Args);
Rslt_29Args = makePair(APP, 0, k_2, Rslt_29Args);
Rslt_29Args = makePair(APP, 0, childNode_15, Rslt_29Args);
#ifdef STRICT
pushRedex(Rslt_29Args, newRef(glblProto115_1080));
#else
swap(termLoc(Rslt_29), makePair(LAZ, 0, Rslt_29Args, newRef(glblProto115_1080)));
#endif
// call  at core: 1708
Term Rslt_30Args = makePair(APP, 0, Rslt_29, SUB);
Term Rslt_30 = newTerm(VAR, 0, portLoc(2, Rslt_30Args));
#ifdef STRICT
pushRedex(Rslt_30Args, seq_20);
#else
swap(termLoc(Rslt_30), makePair(LAZ, 0, Rslt_30Args, seq_20));
#endif

// link args to body
swap(r_seq_16, Rslt_30);

Term Rslt_30V = get(termLoc(Rslt_30));
apps = take(portLoc(1, Rslt_30V));
lams = take(portLoc(2, Rslt_30V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_30), makePair(LAZ, 0, apps, lams));
// call map at core: 1706
Term Rslt_31Args = makePair(APP, 0, seq_16, SUB);
Term Rslt_31 = newTerm(VAR, 0, portLoc(2, Rslt_31Args));
Rslt_31Args = makePair(APP, 0, Rslt_13, Rslt_31Args);
#ifdef STRICT
pushRedex(Rslt_31Args, newRef(glblProto42_1082));
#else
swap(termLoc(Rslt_31), makePair(LAZ, 0, Rslt_31Args, newRef(glblProto42_1082)));
#endif
// call = at core: 1713
Term Rslt_37Args = makePair(APP, 0, currKey_34, SUB);
Term Rslt_37 = newTerm(VAR, 0, portLoc(2, Rslt_37Args));
Rslt_37Args = makePair(APP, 0, k_2_1, Rslt_37Args);
#ifdef STRICT
pushRedex(Rslt_37Args, newRef(glblProto20_1084));
#else
swap(termLoc(Rslt_37), makePair(LAZ, 0, Rslt_37Args, newRef(glblProto20_1084)));
#endif
// call bmiSlotCount at core: 1714
Term Rslt_38Args = makePair(APP, 0, m_1_4, SUB);
Term Rslt_38 = newTerm(VAR, 0, portLoc(2, Rslt_38Args));
#ifdef STRICT
pushRedex(Rslt_38Args, glblFn867);
#else
swap(termLoc(Rslt_38), makePair(LAZ, 0, Rslt_38Args, glblFn867));
#endif

// call = at core: 1714
Term Rslt_39Args = makePair(APP, 0, Rslt_38, SUB);
Term Rslt_39 = newTerm(VAR, 0, portLoc(2, Rslt_39Args));
Rslt_39Args = makePair(APP, 0, newI60(1), Rslt_39Args);
#ifdef STRICT
pushRedex(Rslt_39Args, newRef(glblProto20_1086));
#else
swap(termLoc(Rslt_39), makePair(LAZ, 0, Rslt_39Args, newRef(glblProto20_1086)));
#endif
// call bmiShrink at core: 1716
Term Rslt_40Args = makePair(APP, 0, bit_8_3, SUB);
Term Rslt_40 = newTerm(VAR, 0, portLoc(2, Rslt_40Args));
Rslt_40Args = makePair(APP, 0, m_1_5, Rslt_40Args);
#ifdef STRICT
pushRedex(Rslt_40Args, glblFn872);
#else
swap(termLoc(Rslt_40), makePair(LAZ, 0, Rslt_40Args, glblFn872));
#endif

// call cond at core: 1714
Term Rslt_41Args = makePair(APP, 0, Rslt_40, SUB);
Term Rslt_41 = newTerm(VAR, 0, portLoc(2, Rslt_41Args));
Rslt_41Args = makePair(APP, 0, glblVal106_4, Rslt_41Args);
Rslt_41Args = makePair(APP, 0, Rslt_39, Rslt_41Args);
#ifdef STRICT
pushRedex(Rslt_41Args, newRef(glblProto34_1088));
#else
swap(termLoc(Rslt_41), makePair(LAZ, 0, Rslt_41Args, newRef(glblProto34_1088)));
#endif
// call cond at core: 1713
Term Rslt_42Args = makePair(APP, 0, m_1_6, SUB);
Term Rslt_42 = newTerm(VAR, 0, portLoc(2, Rslt_42Args));
Rslt_42Args = makePair(APP, 0, Rslt_41, Rslt_42Args);
Rslt_42Args = makePair(APP, 0, Rslt_37, Rslt_42Args);
#ifdef STRICT
pushRedex(Rslt_42Args, newRef(glblProto34_1090));
#else
swap(termLoc(Rslt_42), makePair(LAZ, 0, Rslt_42Args, newRef(glblProto34_1090)));
#endif
// link args to body
swap(r_seq_35, Rslt_42);

Term Rslt_42V = get(termLoc(Rslt_42));
apps = take(portLoc(1, Rslt_42V));
lams = take(portLoc(2, Rslt_42V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_42), makePair(LAZ, 0, apps, lams));
// call bmiKey at core: 1712
Term Rslt_43Args = makePair(APP, 0, bit_8_4, SUB);
Term Rslt_43 = newTerm(VAR, 0, portLoc(2, Rslt_43Args));
Rslt_43Args = makePair(APP, 0, m_1_7, Rslt_43Args);
#ifdef STRICT
pushRedex(Rslt_43Args, glblFn821);
#else
swap(termLoc(Rslt_43), makePair(LAZ, 0, Rslt_43Args, glblFn821));
#endif

// call  at core: 1712
Term Rslt_44Args = makePair(APP, 0, Rslt_43, SUB);
Term Rslt_44 = newTerm(VAR, 0, portLoc(2, Rslt_44Args));
#ifdef STRICT
pushRedex(Rslt_44Args, seq_35);
#else
swap(termLoc(Rslt_44), makePair(LAZ, 0, Rslt_44Args, seq_35));
#endif

// call either at core: 1706
Term Rslt_45Args = makePair(APP, 0, Rslt_44, SUB);
Term Rslt_45 = newTerm(VAR, 0, portLoc(2, Rslt_45Args));
Rslt_45Args = makePair(APP, 0, Rslt_31, Rslt_45Args);
#ifdef STRICT
pushRedex(Rslt_45Args, newRef(glblProto32_1092));
#else
swap(termLoc(Rslt_45), makePair(LAZ, 0, Rslt_45Args, newRef(glblProto32_1092)));
#endif
// call int-cond at core: 1705
Term Rslt_46Args = makePair(APP, 0, m_1_8, SUB);
Term Rslt_46 = newTerm(VAR, 0, portLoc(2, Rslt_46Args));
Rslt_46Args = makePair(APP, 0, Rslt_45, Rslt_46Args);
Rslt_46Args = makePair(APP, 0, Rslt_12, Rslt_46Args);
#ifdef STRICT
pushRedex(Rslt_46Args, glblVal262_9);
#else
swap(termLoc(Rslt_46), makePair(LAZ, 0, Rslt_46Args, glblVal262_9));
#endif

// link args to body
swap(r_seq_9, Rslt_46);

Term Rslt_46V = get(termLoc(Rslt_46));
apps = take(portLoc(1, Rslt_46V));
lams = take(portLoc(2, Rslt_46V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_46), makePair(LAZ, 0, apps, lams));
// call bitpos at core: 1704
Term Rslt_47Args = makePair(APP, 0, shift_4_1, SUB);
Term Rslt_47 = newTerm(VAR, 0, portLoc(2, Rslt_47Args));
Rslt_47Args = makePair(APP, 0, hash_3_1, Rslt_47Args);
#ifdef STRICT
pushRedex(Rslt_47Args, glblFn766);
#else
swap(termLoc(Rslt_47), makePair(LAZ, 0, Rslt_47Args, glblFn766));
#endif

// call  at core: 1704
Term Rslt_48Args = makePair(APP, 0, Rslt_47, SUB);
Term Rslt_48 = newTerm(VAR, 0, portLoc(2, Rslt_48Args));
#ifdef STRICT
pushRedex(Rslt_48Args, seq_9);
#else
swap(termLoc(Rslt_48), makePair(LAZ, 0, Rslt_48Args, seq_9));
#endif

// link args to body
swap(r_seq_5, Rslt_48);

Term Rslt_48V = get(termLoc(Rslt_48));
apps = take(portLoc(1, Rslt_48V));
lams = take(portLoc(2, Rslt_48V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_48), makePair(LAZ, 0, apps, lams));
pushRedex(args, seq_5);
return;
}
Term glblFn1071 = newRef(glblCFn1070);
void glblProto115_1097(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(1735), args);
  args = makePair(APP, 0, glblStr199, args);
  interact(args, glblProto115);
}
}
void glblProto42_1099(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(1733), args);
  args = makePair(APP, 0, glblStr199, args);
  interact(args, glblProto42);
}
}
void glblProto32_1101(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(1733), args);
  args = makePair(APP, 0, glblStr199, args);
  interact(args, glblProto32);
}
}
Term glblFn1096;
void glblCFn1095(Term ref, Term args) {
Term apps, lams, V;
Location vLoc;
Term seq_5 = makePair(LAM, 0, SUB, NUL);
Location r_seq_5 = portLoc(2, seq_5);
Term shift_4 = newTerm(VAR, 0, portLoc(1, seq_5));
seq_5 = makePair(LAM, 0, SUB, seq_5);
Term hash_3 = newTerm(VAR, 0, portLoc(1, seq_5));
seq_5 = makePair(LAM, 0, SUB, seq_5);
Term k_2 = newTerm(VAR, 0, portLoc(1, seq_5));
seq_5 = makePair(LAM, 0, SUB, seq_5);
Term m_1 = newTerm(VAR, 0, portLoc(1, seq_5));
// allocate args at core: 1720

Term m_1_2;
m_1 = dupeArg(m_1, &m_1_2, 169);

Term m_1_1;
m_1 = dupeArg(m_1, &m_1_1, 170);

Term hash_3_1;
hash_3 = dupeArg(hash_3, &hash_3_1, 171);

Term shift_4_1;
shift_4 = dupeArg(shift_4, &shift_4_1, 172);
Term seq_9 = makePair(LAM, 0, SUB, NUL);
Location r_seq_9 = portLoc(2, seq_9);
Term idx_8 = newTerm(VAR, 0, portLoc(1, seq_9));
// allocate args at core: 1732

Term idx_8_1;
idx_8 = dupeArg(idx_8, &idx_8_1, 0);
Term seq_14 = makePair(LAM, 0, SUB, NUL);
Location r_seq_14 = portLoc(2, seq_14);
Term child_13 = newTerm(VAR, 0, portLoc(1, seq_14));
// allocate args at core: 1734
Term seq_18 = makePair(LAM, 0, SUB, NUL);
Location r_seq_18 = portLoc(2, seq_18);
Term n_17 = newTerm(VAR, 0, portLoc(1, seq_18));
// allocate args at core: 1735
// call arrayNodeCopyChild at core: 1733
Term Rslt_11Args = makePair(APP, 0, idx_8, SUB);
Term Rslt_11 = newTerm(VAR, 0, portLoc(2, Rslt_11Args));
Rslt_11Args = makePair(APP, 0, m_1, Rslt_11Args);
#ifdef STRICT
pushRedex(Rslt_11Args, glblFn744);
#else
swap(termLoc(Rslt_11), makePair(LAZ, 0, Rslt_11Args, glblFn744));
#endif

// call arrayNodeUpdate at core: 1736
Term Rslt_20Args = makePair(APP, 0, n_17, SUB);
Term Rslt_20 = newTerm(VAR, 0, portLoc(2, Rslt_20Args));
Rslt_20Args = makePair(APP, 0, idx_8_1, Rslt_20Args);
Rslt_20Args = makePair(APP, 0, m_1_1, Rslt_20Args);
#ifdef STRICT
pushRedex(Rslt_20Args, glblFn750);
#else
swap(termLoc(Rslt_20), makePair(LAZ, 0, Rslt_20Args, glblFn750));
#endif

// link args to body
swap(r_seq_18, Rslt_20);

Term Rslt_20V = get(termLoc(Rslt_20));
apps = take(portLoc(1, Rslt_20V));
lams = take(portLoc(2, Rslt_20V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_20), makePair(LAZ, 0, apps, lams));
Term Rslt_21 = makeOp(OP_ADD, newI60(5), shift_4);
// call dissoc* at core: 1735
Term Rslt_22Args = makePair(APP, 0, Rslt_21, SUB);
Term Rslt_22 = newTerm(VAR, 0, portLoc(2, Rslt_22Args));
Rslt_22Args = makePair(APP, 0, hash_3, Rslt_22Args);
Rslt_22Args = makePair(APP, 0, k_2, Rslt_22Args);
Rslt_22Args = makePair(APP, 0, child_13, Rslt_22Args);
#ifdef STRICT
pushRedex(Rslt_22Args, newRef(glblProto115_1097));
#else
swap(termLoc(Rslt_22), makePair(LAZ, 0, Rslt_22Args, newRef(glblProto115_1097)));
#endif
// call  at core: 1735
Term Rslt_23Args = makePair(APP, 0, Rslt_22, SUB);
Term Rslt_23 = newTerm(VAR, 0, portLoc(2, Rslt_23Args));
#ifdef STRICT
pushRedex(Rslt_23Args, seq_18);
#else
swap(termLoc(Rslt_23), makePair(LAZ, 0, Rslt_23Args, seq_18));
#endif

// link args to body
swap(r_seq_14, Rslt_23);

Term Rslt_23V = get(termLoc(Rslt_23));
apps = take(portLoc(1, Rslt_23V));
lams = take(portLoc(2, Rslt_23V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_23), makePair(LAZ, 0, apps, lams));
// call map at core: 1733
Term Rslt_24Args = makePair(APP, 0, seq_14, SUB);
Term Rslt_24 = newTerm(VAR, 0, portLoc(2, Rslt_24Args));
Rslt_24Args = makePair(APP, 0, Rslt_11, Rslt_24Args);
#ifdef STRICT
pushRedex(Rslt_24Args, newRef(glblProto42_1099));
#else
swap(termLoc(Rslt_24), makePair(LAZ, 0, Rslt_24Args, newRef(glblProto42_1099)));
#endif
// call either at core: 1733
Term Rslt_26Args = makePair(APP, 0, m_1_2, SUB);
Term Rslt_26 = newTerm(VAR, 0, portLoc(2, Rslt_26Args));
Rslt_26Args = makePair(APP, 0, Rslt_24, Rslt_26Args);
#ifdef STRICT
pushRedex(Rslt_26Args, newRef(glblProto32_1101));
#else
swap(termLoc(Rslt_26), makePair(LAZ, 0, Rslt_26Args, newRef(glblProto32_1101)));
#endif
// link args to body
swap(r_seq_9, Rslt_26);

Term Rslt_26V = get(termLoc(Rslt_26));
apps = take(portLoc(1, Rslt_26V));
lams = take(portLoc(2, Rslt_26V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_26), makePair(LAZ, 0, apps, lams));
Term Rslt_27 = makeOp(OP_RSH, hash_3_1, shift_4_1);
Term Rslt_28 = makeOp(OP_AND, Rslt_27, newI60(31));
// call  at core: 1732
Term Rslt_29Args = makePair(APP, 0, Rslt_28, SUB);
Term Rslt_29 = newTerm(VAR, 0, portLoc(2, Rslt_29Args));
#ifdef STRICT
pushRedex(Rslt_29Args, seq_9);
#else
swap(termLoc(Rslt_29), makePair(LAZ, 0, Rslt_29Args, seq_9));
#endif

// link args to body
swap(r_seq_5, Rslt_29);

Term Rslt_29V = get(termLoc(Rslt_29));
apps = take(portLoc(1, Rslt_29V));
lams = take(portLoc(2, Rslt_29V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_29), makePair(LAZ, 0, apps, lams));
pushRedex(args, seq_5);
return;
}
Term glblFn1096 = newRef(glblCFn1095);
void glblProto20_1106(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(1740), args);
  args = makePair(APP, 0, glblStr199, args);
  interact(args, glblProto20);
}
}
void glblProto20_1108(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(1743), args);
  args = makePair(APP, 0, glblStr199, args);
  interact(args, glblProto20);
}
}
void glblProto34_1110(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(1743), args);
  args = makePair(APP, 0, glblStr199, args);
  interact(args, glblProto34);
}
}
void glblProto34_1112(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(1740), args);
  args = makePair(APP, 0, glblStr199, args);
  interact(args, glblProto34);
}
}
Term glblFn1105;
void glblCFn1104(Term ref, Term args) {
Term apps, lams, V;
Location vLoc;
Term seq_6 = makePair(LAM, 0, SUB, NUL);
Location r_seq_6 = portLoc(2, seq_6);
Term i_5 = newTerm(VAR, 0, portLoc(1, seq_6));
seq_6 = makePair(LAM, 0, SUB, seq_6);
Term n_4 = newTerm(VAR, 0, portLoc(1, seq_6));
seq_6 = makePair(LAM, 0, SUB, seq_6);
Term def_3 = newTerm(VAR, 0, portLoc(1, seq_6));
seq_6 = makePair(LAM, 0, SUB, seq_6);
Term k_2 = newTerm(VAR, 0, portLoc(1, seq_6));
seq_6 = makePair(LAM, 0, SUB, seq_6);
Term m_1 = newTerm(VAR, 0, portLoc(1, seq_6));
// allocate args at core: 1739

Term m_1_2;
m_1 = dupeArg(m_1, &m_1_2, 173);

Term m_1_1;
m_1 = dupeArg(m_1, &m_1_1, 174);

Term k_2_1;
k_2 = dupeArg(k_2, &k_2_1, 175);

Term def_3_1;
def_3 = dupeArg(def_3, &def_3_1, 176);

Term n_4_1;
n_4 = dupeArg(n_4, &n_4_1, 177);

Term i_5_3;
i_5 = dupeArg(i_5, &i_5_3, 178);

Term i_5_2;
i_5 = dupeArg(i_5, &i_5_2, 179);

Term i_5_1;
i_5 = dupeArg(i_5, &i_5_1, 180);
Term seq_11 = makePair(LAM, 0, SUB, NUL);
Location r_seq_11 = portLoc(2, seq_11);
Term ek_10 = newTerm(VAR, 0, portLoc(1, seq_11));
// allocate args at core: 1742
// call = at core: 1740
Term Rslt_8Args = makePair(APP, 0, n_4, SUB);
Term Rslt_8 = newTerm(VAR, 0, portLoc(2, Rslt_8Args));
Rslt_8Args = makePair(APP, 0, i_5, Rslt_8Args);
#ifdef STRICT
pushRedex(Rslt_8Args, newRef(glblProto20_1106));
#else
swap(termLoc(Rslt_8), makePair(LAZ, 0, Rslt_8Args, newRef(glblProto20_1106)));
#endif
// call = at core: 1743
Term Rslt_13Args = makePair(APP, 0, k_2, SUB);
Term Rslt_13 = newTerm(VAR, 0, portLoc(2, Rslt_13Args));
Rslt_13Args = makePair(APP, 0, ek_10, Rslt_13Args);
#ifdef STRICT
pushRedex(Rslt_13Args, newRef(glblProto20_1108));
#else
swap(termLoc(Rslt_13), makePair(LAZ, 0, Rslt_13Args, newRef(glblProto20_1108)));
#endif
// call collisionVal at core: 1744
Term Rslt_14Args = makePair(APP, 0, i_5_1, SUB);
Term Rslt_14 = newTerm(VAR, 0, portLoc(2, Rslt_14Args));
Rslt_14Args = makePair(APP, 0, m_1, Rslt_14Args);
#ifdef STRICT
pushRedex(Rslt_14Args, glblFn890);
#else
swap(termLoc(Rslt_14), makePair(LAZ, 0, Rslt_14Args, glblFn890));
#endif

// call inc at core: 1745
Term Rslt_15Args = makePair(APP, 0, i_5_2, SUB);
Term Rslt_15 = newTerm(VAR, 0, portLoc(2, Rslt_15Args));
#ifdef STRICT
pushRedex(Rslt_15Args, glblFn141);
#else
swap(termLoc(Rslt_15), makePair(LAZ, 0, Rslt_15Args, glblFn141));
#endif

// call collisionGet-i at core: 1739
Term Rslt_16Args = makePair(APP, 0, Rslt_15, SUB);
Term Rslt_16 = newTerm(VAR, 0, portLoc(2, Rslt_16Args));
Rslt_16Args = makePair(APP, 0, n_4_1, Rslt_16Args);
Rslt_16Args = makePair(APP, 0, def_3_1, Rslt_16Args);
Rslt_16Args = makePair(APP, 0, k_2_1, Rslt_16Args);
Rslt_16Args = makePair(APP, 0, m_1_1, Rslt_16Args);
#ifdef STRICT
pushRedex(Rslt_16Args, glblFn1105);
#else
swap(termLoc(Rslt_16), makePair(LAZ, 0, Rslt_16Args, glblFn1105));
#endif

// call cond at core: 1743
Term Rslt_17Args = makePair(APP, 0, Rslt_16, SUB);
Term Rslt_17 = newTerm(VAR, 0, portLoc(2, Rslt_17Args));
Rslt_17Args = makePair(APP, 0, Rslt_14, Rslt_17Args);
Rslt_17Args = makePair(APP, 0, Rslt_13, Rslt_17Args);
#ifdef STRICT
pushRedex(Rslt_17Args, newRef(glblProto34_1110));
#else
swap(termLoc(Rslt_17), makePair(LAZ, 0, Rslt_17Args, newRef(glblProto34_1110)));
#endif
// link args to body
swap(r_seq_11, Rslt_17);

Term Rslt_17V = get(termLoc(Rslt_17));
apps = take(portLoc(1, Rslt_17V));
lams = take(portLoc(2, Rslt_17V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_17), makePair(LAZ, 0, apps, lams));
// call collisionKey at core: 1742
Term Rslt_18Args = makePair(APP, 0, i_5_3, SUB);
Term Rslt_18 = newTerm(VAR, 0, portLoc(2, Rslt_18Args));
Rslt_18Args = makePair(APP, 0, m_1_2, Rslt_18Args);
#ifdef STRICT
pushRedex(Rslt_18Args, glblFn885);
#else
swap(termLoc(Rslt_18), makePair(LAZ, 0, Rslt_18Args, glblFn885));
#endif

// call  at core: 1742
Term Rslt_19Args = makePair(APP, 0, Rslt_18, SUB);
Term Rslt_19 = newTerm(VAR, 0, portLoc(2, Rslt_19Args));
#ifdef STRICT
pushRedex(Rslt_19Args, seq_11);
#else
swap(termLoc(Rslt_19), makePair(LAZ, 0, Rslt_19Args, seq_11));
#endif

// call cond at core: 1740
Term Rslt_20Args = makePair(APP, 0, Rslt_19, SUB);
Term Rslt_20 = newTerm(VAR, 0, portLoc(2, Rslt_20Args));
Rslt_20Args = makePair(APP, 0, def_3, Rslt_20Args);
Rslt_20Args = makePair(APP, 0, Rslt_8, Rslt_20Args);
#ifdef STRICT
pushRedex(Rslt_20Args, newRef(glblProto34_1112));
#else
swap(termLoc(Rslt_20), makePair(LAZ, 0, Rslt_20Args, newRef(glblProto34_1112)));
#endif
// link args to body
swap(r_seq_6, Rslt_20);

Term Rslt_20V = get(termLoc(Rslt_20));
apps = take(portLoc(1, Rslt_20V));
lams = take(portLoc(2, Rslt_20V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_20), makePair(LAZ, 0, apps, lams));
pushRedex(args, seq_6);
return;
}
Term glblFn1105 = newRef(glblCFn1104);
Term glblFn1116;
void glblCFn1115(Term ref, Term args) {
Term apps, lams, V;
Location vLoc;
Term seq_6 = makePair(LAM, 0, SUB, NUL);
Location r_seq_6 = portLoc(2, seq_6);
Term shift_5 = newTerm(VAR, 0, portLoc(1, seq_6));
seq_6 = makePair(LAM, 0, SUB, seq_6);
Term hash_4 = newTerm(VAR, 0, portLoc(1, seq_6));
seq_6 = makePair(LAM, 0, SUB, seq_6);
Term def_3 = newTerm(VAR, 0, portLoc(1, seq_6));
seq_6 = makePair(LAM, 0, SUB, seq_6);
Term k_2 = newTerm(VAR, 0, portLoc(1, seq_6));
seq_6 = makePair(LAM, 0, SUB, seq_6);
Term m_1 = newTerm(VAR, 0, portLoc(1, seq_6));
// allocate args at core: 1747

Term m_1_1;
m_1 = dupeArg(m_1, &m_1_1, 181);
swap(termLoc(hash_4), ERA);
swap(termLoc(shift_5), ERA);
// call collisionCount at core: 1755
Term Rslt_8Args = makePair(APP, 0, m_1_1, SUB);
Term Rslt_8 = newTerm(VAR, 0, portLoc(2, Rslt_8Args));
#ifdef STRICT
pushRedex(Rslt_8Args, glblFn880);
#else
swap(termLoc(Rslt_8), makePair(LAZ, 0, Rslt_8Args, glblFn880));
#endif

// call collisionGet-i at core: 1755
Term Rslt_9Args = makePair(APP, 0, newI60(0), SUB);
Term Rslt_9 = newTerm(VAR, 0, portLoc(2, Rslt_9Args));
Rslt_9Args = makePair(APP, 0, Rslt_8, Rslt_9Args);
Rslt_9Args = makePair(APP, 0, def_3, Rslt_9Args);
Rslt_9Args = makePair(APP, 0, k_2, Rslt_9Args);
Rslt_9Args = makePair(APP, 0, m_1, Rslt_9Args);
#ifdef STRICT
pushRedex(Rslt_9Args, glblFn1105);
#else
swap(termLoc(Rslt_9), makePair(LAZ, 0, Rslt_9Args, glblFn1105));
#endif

// link args to body
swap(r_seq_6, Rslt_9);

Term Rslt_9V = get(termLoc(Rslt_9));
apps = take(portLoc(1, Rslt_9V));
lams = take(portLoc(2, Rslt_9V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_9), makePair(LAZ, 0, apps, lams));
pushRedex(args, seq_6);
return;
}
Term glblFn1116 = newRef(glblCFn1115);
void glblProto20_1120(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(1761), args);
  args = makePair(APP, 0, glblStr199, args);
  interact(args, glblProto20);
}
}
void glblProto20_1122(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(1764), args);
  args = makePair(APP, 0, glblStr199, args);
  interact(args, glblProto20);
}
}
void glblProto34_1124(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(1764), args);
  args = makePair(APP, 0, glblStr199, args);
  interact(args, glblProto34);
}
}
void glblProto34_1126(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(1761), args);
  args = makePair(APP, 0, glblStr199, args);
  interact(args, glblProto34);
}
}
Term glblFn1119;
void glblCFn1118(Term ref, Term args) {
Term apps, lams, V;
Location vLoc;
Term seq_5 = makePair(LAM, 0, SUB, NUL);
Location r_seq_5 = portLoc(2, seq_5);
Term i_4 = newTerm(VAR, 0, portLoc(1, seq_5));
seq_5 = makePair(LAM, 0, SUB, seq_5);
Term n_3 = newTerm(VAR, 0, portLoc(1, seq_5));
seq_5 = makePair(LAM, 0, SUB, seq_5);
Term k_2 = newTerm(VAR, 0, portLoc(1, seq_5));
seq_5 = makePair(LAM, 0, SUB, seq_5);
Term m_1 = newTerm(VAR, 0, portLoc(1, seq_5));
// allocate args at core: 1757

Term m_1_1;
m_1 = dupeArg(m_1, &m_1_1, 182);

Term k_2_1;
k_2 = dupeArg(k_2, &k_2_1, 183);

Term n_3_1;
n_3 = dupeArg(n_3, &n_3_1, 184);

Term i_4_3;
i_4 = dupeArg(i_4, &i_4_3, 185);

Term i_4_2;
i_4 = dupeArg(i_4, &i_4_2, 186);

Term i_4_1;
i_4 = dupeArg(i_4, &i_4_1, 187);
Term seq_10 = makePair(LAM, 0, SUB, NUL);
Location r_seq_10 = portLoc(2, seq_10);
Term ek_9 = newTerm(VAR, 0, portLoc(1, seq_10));
// allocate args at core: 1763
Term glblVal25_13;
glblVal25 = dupeArg(glblVal25, &glblVal25_13, 0);

Term glblRslt16_7;
glblRslt16 = dupeArg(glblRslt16, &glblRslt16_7, 0);

// call = at core: 1761
Term Rslt_7Args = makePair(APP, 0, n_3, SUB);
Term Rslt_7 = newTerm(VAR, 0, portLoc(2, Rslt_7Args));
Rslt_7Args = makePair(APP, 0, i_4, Rslt_7Args);
#ifdef STRICT
pushRedex(Rslt_7Args, newRef(glblProto20_1120));
#else
swap(termLoc(Rslt_7), makePair(LAZ, 0, Rslt_7Args, newRef(glblProto20_1120)));
#endif
// call = at core: 1764
Term Rslt_12Args = makePair(APP, 0, k_2, SUB);
Term Rslt_12 = newTerm(VAR, 0, portLoc(2, Rslt_12Args));
Rslt_12Args = makePair(APP, 0, ek_9, Rslt_12Args);
#ifdef STRICT
pushRedex(Rslt_12Args, newRef(glblProto20_1122));
#else
swap(termLoc(Rslt_12), makePair(LAZ, 0, Rslt_12Args, newRef(glblProto20_1122)));
#endif
// call Some at core: 1765
Term Rslt_13Args = makePair(APP, 0, i_4_1, SUB);
Term Rslt_13 = newTerm(VAR, 0, portLoc(2, Rslt_13Args));
#ifdef STRICT
pushRedex(Rslt_13Args, glblVal25_13);
#else
swap(termLoc(Rslt_13), makePair(LAZ, 0, Rslt_13Args, glblVal25_13));
#endif

// call inc at core: 1766
Term Rslt_14Args = makePair(APP, 0, i_4_2, SUB);
Term Rslt_14 = newTerm(VAR, 0, portLoc(2, Rslt_14Args));
#ifdef STRICT
pushRedex(Rslt_14Args, glblFn141);
#else
swap(termLoc(Rslt_14), makePair(LAZ, 0, Rslt_14Args, glblFn141));
#endif

// call collisionAssoc-find at core: 1757
Term Rslt_15Args = makePair(APP, 0, Rslt_14, SUB);
Term Rslt_15 = newTerm(VAR, 0, portLoc(2, Rslt_15Args));
Rslt_15Args = makePair(APP, 0, n_3_1, Rslt_15Args);
Rslt_15Args = makePair(APP, 0, k_2_1, Rslt_15Args);
Rslt_15Args = makePair(APP, 0, m_1, Rslt_15Args);
#ifdef STRICT
pushRedex(Rslt_15Args, glblFn1119);
#else
swap(termLoc(Rslt_15), makePair(LAZ, 0, Rslt_15Args, glblFn1119));
#endif

// call cond at core: 1764
Term Rslt_16Args = makePair(APP, 0, Rslt_15, SUB);
Term Rslt_16 = newTerm(VAR, 0, portLoc(2, Rslt_16Args));
Rslt_16Args = makePair(APP, 0, Rslt_13, Rslt_16Args);
Rslt_16Args = makePair(APP, 0, Rslt_12, Rslt_16Args);
#ifdef STRICT
pushRedex(Rslt_16Args, newRef(glblProto34_1124));
#else
swap(termLoc(Rslt_16), makePair(LAZ, 0, Rslt_16Args, newRef(glblProto34_1124)));
#endif
// link args to body
swap(r_seq_10, Rslt_16);

Term Rslt_16V = get(termLoc(Rslt_16));
apps = take(portLoc(1, Rslt_16V));
lams = take(portLoc(2, Rslt_16V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_16), makePair(LAZ, 0, apps, lams));
// call collisionKey at core: 1763
Term Rslt_17Args = makePair(APP, 0, i_4_3, SUB);
Term Rslt_17 = newTerm(VAR, 0, portLoc(2, Rslt_17Args));
Rslt_17Args = makePair(APP, 0, m_1_1, Rslt_17Args);
#ifdef STRICT
pushRedex(Rslt_17Args, glblFn885);
#else
swap(termLoc(Rslt_17), makePair(LAZ, 0, Rslt_17Args, glblFn885));
#endif

// call  at core: 1763
Term Rslt_18Args = makePair(APP, 0, Rslt_17, SUB);
Term Rslt_18 = newTerm(VAR, 0, portLoc(2, Rslt_18Args));
#ifdef STRICT
pushRedex(Rslt_18Args, seq_10);
#else
swap(termLoc(Rslt_18), makePair(LAZ, 0, Rslt_18Args, seq_10));
#endif

// call cond at core: 1761
Term Rslt_19Args = makePair(APP, 0, Rslt_18, SUB);
Term Rslt_19 = newTerm(VAR, 0, portLoc(2, Rslt_19Args));
Rslt_19Args = makePair(APP, 0, glblRslt16_7, Rslt_19Args);
Rslt_19Args = makePair(APP, 0, Rslt_7, Rslt_19Args);
#ifdef STRICT
pushRedex(Rslt_19Args, newRef(glblProto34_1126));
#else
swap(termLoc(Rslt_19), makePair(LAZ, 0, Rslt_19Args, newRef(glblProto34_1126)));
#endif
// link args to body
swap(r_seq_5, Rslt_19);

Term Rslt_19V = get(termLoc(Rslt_19));
apps = take(portLoc(1, Rslt_19V));
lams = take(portLoc(2, Rslt_19V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_19), makePair(LAZ, 0, apps, lams));
pushRedex(args, seq_5);
return;
}
Term glblFn1119 = newRef(glblCFn1118);
void glblProto20_1131(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(1775), args);
  args = makePair(APP, 0, glblStr199, args);
  interact(args, glblProto20);
}
}
void glblProto111_1133(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(1780), args);
  args = makePair(APP, 0, glblStr199, args);
  interact(args, glblProto111);
}
}
void glblProto104_1135(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(1779), args);
  args = makePair(APP, 0, glblStr199, args);
  interact(args, glblProto104);
}
}
void glblProto34_1137(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(1775), args);
  args = makePair(APP, 0, glblStr199, args);
  interact(args, glblProto34);
}
}
Term glblFn1130;
void glblCFn1129(Term ref, Term args) {
Term apps, lams, V;
Location vLoc;
Term seq_5 = makePair(LAM, 0, SUB, NUL);
Location r_seq_5 = portLoc(2, seq_5);
Term i_4 = newTerm(VAR, 0, portLoc(1, seq_5));
seq_5 = makePair(LAM, 0, SUB, seq_5);
Term n_3 = newTerm(VAR, 0, portLoc(1, seq_5));
seq_5 = makePair(LAM, 0, SUB, seq_5);
Term m_2 = newTerm(VAR, 0, portLoc(1, seq_5));
seq_5 = makePair(LAM, 0, SUB, seq_5);
Term bmi_1 = newTerm(VAR, 0, portLoc(1, seq_5));
// allocate args at core: 1768

Term bmi_1_1;
bmi_1 = dupeArg(bmi_1, &bmi_1_1, 188);

Term m_2_2;
m_2 = dupeArg(m_2, &m_2_2, 189);

Term m_2_1;
m_2 = dupeArg(m_2, &m_2_1, 190);

Term n_3_1;
n_3 = dupeArg(n_3, &n_3_1, 191);

Term i_4_3;
i_4 = dupeArg(i_4, &i_4_3, 192);

Term i_4_2;
i_4 = dupeArg(i_4, &i_4_2, 193);

Term i_4_1;
i_4 = dupeArg(i_4, &i_4_1, 194);
Term seq_10 = makePair(LAM, 0, SUB, NUL);
Location r_seq_10 = portLoc(2, seq_10);
Term ek_9 = newTerm(VAR, 0, portLoc(1, seq_10));
// allocate args at core: 1777

Term ek_9_1;
ek_9 = dupeArg(ek_9, &ek_9_1, 0);
Term seq_14 = makePair(LAM, 0, SUB, NUL);
Location r_seq_14 = portLoc(2, seq_14);
Term ev_13 = newTerm(VAR, 0, portLoc(1, seq_14));
// allocate args at core: 1778
Term seq_18 = makePair(LAM, 0, SUB, NUL);
Location r_seq_18 = portLoc(2, seq_18);
Term eh_17 = newTerm(VAR, 0, portLoc(1, seq_18));
// allocate args at core: 1779
// call = at core: 1775
Term Rslt_7Args = makePair(APP, 0, n_3, SUB);
Term Rslt_7 = newTerm(VAR, 0, portLoc(2, Rslt_7Args));
Rslt_7Args = makePair(APP, 0, i_4, Rslt_7Args);
#ifdef STRICT
pushRedex(Rslt_7Args, newRef(glblProto20_1131));
#else
swap(termLoc(Rslt_7), makePair(LAZ, 0, Rslt_7Args, newRef(glblProto20_1131)));
#endif
// call assoc* at core: 1780
Term Rslt_20Args = makePair(APP, 0, newI60(0), SUB);
Term Rslt_20 = newTerm(VAR, 0, portLoc(2, Rslt_20Args));
Rslt_20Args = makePair(APP, 0, eh_17, Rslt_20Args);
Rslt_20Args = makePair(APP, 0, ev_13, Rslt_20Args);
Rslt_20Args = makePair(APP, 0, ek_9, Rslt_20Args);
Rslt_20Args = makePair(APP, 0, bmi_1_1, Rslt_20Args);
#ifdef STRICT
pushRedex(Rslt_20Args, newRef(glblProto111_1133));
#else
swap(termLoc(Rslt_20), makePair(LAZ, 0, Rslt_20Args, newRef(glblProto111_1133)));
#endif
// call inc at core: 1780
Term Rslt_21Args = makePair(APP, 0, i_4_1, SUB);
Term Rslt_21 = newTerm(VAR, 0, portLoc(2, Rslt_21Args));
#ifdef STRICT
pushRedex(Rslt_21Args, glblFn141);
#else
swap(termLoc(Rslt_21), makePair(LAZ, 0, Rslt_21Args, glblFn141));
#endif

// call collisionAssoc-promote-i at core: 1768
Term Rslt_22Args = makePair(APP, 0, Rslt_21, SUB);
Term Rslt_22 = newTerm(VAR, 0, portLoc(2, Rslt_22Args));
Rslt_22Args = makePair(APP, 0, n_3_1, Rslt_22Args);
Rslt_22Args = makePair(APP, 0, m_2, Rslt_22Args);
Rslt_22Args = makePair(APP, 0, Rslt_20, Rslt_22Args);
#ifdef STRICT
pushRedex(Rslt_22Args, glblFn1130);
#else
swap(termLoc(Rslt_22), makePair(LAZ, 0, Rslt_22Args, glblFn1130));
#endif

// link args to body
swap(r_seq_18, Rslt_22);

Term Rslt_22V = get(termLoc(Rslt_22));
apps = take(portLoc(1, Rslt_22V));
lams = take(portLoc(2, Rslt_22V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_22), makePair(LAZ, 0, apps, lams));
// call sha1 at core: 1779
Term Rslt_23Args = makePair(APP, 0, ek_9_1, SUB);
Term Rslt_23 = newTerm(VAR, 0, portLoc(2, Rslt_23Args));
#ifdef STRICT
pushRedex(Rslt_23Args, newRef(glblProto104_1135));
#else
swap(termLoc(Rslt_23), makePair(LAZ, 0, Rslt_23Args, newRef(glblProto104_1135)));
#endif
// call  at core: 1779
Term Rslt_24Args = makePair(APP, 0, Rslt_23, SUB);
Term Rslt_24 = newTerm(VAR, 0, portLoc(2, Rslt_24Args));
#ifdef STRICT
pushRedex(Rslt_24Args, seq_18);
#else
swap(termLoc(Rslt_24), makePair(LAZ, 0, Rslt_24Args, seq_18));
#endif

// link args to body
swap(r_seq_14, Rslt_24);

Term Rslt_24V = get(termLoc(Rslt_24));
apps = take(portLoc(1, Rslt_24V));
lams = take(portLoc(2, Rslt_24V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_24), makePair(LAZ, 0, apps, lams));
// call collisionVal at core: 1778
Term Rslt_25Args = makePair(APP, 0, i_4_2, SUB);
Term Rslt_25 = newTerm(VAR, 0, portLoc(2, Rslt_25Args));
Rslt_25Args = makePair(APP, 0, m_2_1, Rslt_25Args);
#ifdef STRICT
pushRedex(Rslt_25Args, glblFn890);
#else
swap(termLoc(Rslt_25), makePair(LAZ, 0, Rslt_25Args, glblFn890));
#endif

// call  at core: 1778
Term Rslt_26Args = makePair(APP, 0, Rslt_25, SUB);
Term Rslt_26 = newTerm(VAR, 0, portLoc(2, Rslt_26Args));
#ifdef STRICT
pushRedex(Rslt_26Args, seq_14);
#else
swap(termLoc(Rslt_26), makePair(LAZ, 0, Rslt_26Args, seq_14));
#endif

// link args to body
swap(r_seq_10, Rslt_26);

Term Rslt_26V = get(termLoc(Rslt_26));
apps = take(portLoc(1, Rslt_26V));
lams = take(portLoc(2, Rslt_26V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_26), makePair(LAZ, 0, apps, lams));
// call collisionKey at core: 1777
Term Rslt_27Args = makePair(APP, 0, i_4_3, SUB);
Term Rslt_27 = newTerm(VAR, 0, portLoc(2, Rslt_27Args));
Rslt_27Args = makePair(APP, 0, m_2_2, Rslt_27Args);
#ifdef STRICT
pushRedex(Rslt_27Args, glblFn885);
#else
swap(termLoc(Rslt_27), makePair(LAZ, 0, Rslt_27Args, glblFn885));
#endif

// call  at core: 1777
Term Rslt_28Args = makePair(APP, 0, Rslt_27, SUB);
Term Rslt_28 = newTerm(VAR, 0, portLoc(2, Rslt_28Args));
#ifdef STRICT
pushRedex(Rslt_28Args, seq_10);
#else
swap(termLoc(Rslt_28), makePair(LAZ, 0, Rslt_28Args, seq_10));
#endif

// call cond at core: 1775
Term Rslt_29Args = makePair(APP, 0, Rslt_28, SUB);
Term Rslt_29 = newTerm(VAR, 0, portLoc(2, Rslt_29Args));
Rslt_29Args = makePair(APP, 0, bmi_1, Rslt_29Args);
Rslt_29Args = makePair(APP, 0, Rslt_7, Rslt_29Args);
#ifdef STRICT
pushRedex(Rslt_29Args, newRef(glblProto34_1137));
#else
swap(termLoc(Rslt_29), makePair(LAZ, 0, Rslt_29Args, newRef(glblProto34_1137)));
#endif
// link args to body
swap(r_seq_5, Rslt_29);

Term Rslt_29V = get(termLoc(Rslt_29));
apps = take(portLoc(1, Rslt_29V));
lams = take(portLoc(2, Rslt_29V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_29), makePair(LAZ, 0, apps, lams));
pushRedex(args, seq_5);
return;
}
Term glblFn1130 = newRef(glblCFn1129);
void glblProto104_1142(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(1809), args);
  args = makePair(APP, 0, glblStr199, args);
  interact(args, glblProto104);
}
}
void glblProto20_1144(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(1809), args);
  args = makePair(APP, 0, glblStr199, args);
  interact(args, glblProto20);
}
}
void glblProto42_1146(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(1810), args);
  args = makePair(APP, 0, glblStr199, args);
  interact(args, glblProto42);
}
}
void glblProto32_1148(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(1810), args);
  args = makePair(APP, 0, glblStr199, args);
  interact(args, glblProto32);
}
}
void glblProto109_1150(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(1813), args);
  args = makePair(APP, 0, glblStr199, args);
  interact(args, glblProto109);
}
}
void glblProto34_1152(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(1809), args);
  args = makePair(APP, 0, glblStr199, args);
  interact(args, glblProto34);
}
}
Term glblFn1141;
void glblCFn1140(Term ref, Term args) {
Term apps, lams, V;
Location vLoc;
Term seq_6 = makePair(LAM, 0, SUB, NUL);
Location r_seq_6 = portLoc(2, seq_6);
Term shift_5 = newTerm(VAR, 0, portLoc(1, seq_6));
seq_6 = makePair(LAM, 0, SUB, seq_6);
Term hash_4 = newTerm(VAR, 0, portLoc(1, seq_6));
seq_6 = makePair(LAM, 0, SUB, seq_6);
Term v_3 = newTerm(VAR, 0, portLoc(1, seq_6));
seq_6 = makePair(LAM, 0, SUB, seq_6);
Term k_2 = newTerm(VAR, 0, portLoc(1, seq_6));
seq_6 = makePair(LAM, 0, SUB, seq_6);
Term m_1 = newTerm(VAR, 0, portLoc(1, seq_6));
// allocate args at core: 1782

Term m_1_5;
m_1 = dupeArg(m_1, &m_1_5, 195);

Term m_1_4;
m_1 = dupeArg(m_1, &m_1_4, 196);

Term m_1_3;
m_1 = dupeArg(m_1, &m_1_3, 197);

Term m_1_2;
m_1 = dupeArg(m_1, &m_1_2, 198);

Term m_1_1;
m_1 = dupeArg(m_1, &m_1_1, 199);

Term v_3_2;
v_3 = dupeArg(v_3, &v_3_2, 200);

Term v_3_1;
v_3 = dupeArg(v_3, &v_3_1, 201);

Term hash_4_2;
hash_4 = dupeArg(hash_4, &hash_4_2, 202);

Term hash_4_1;
hash_4 = dupeArg(hash_4, &hash_4_1, 203);
swap(termLoc(shift_5), ERA);
Term seq_10 = makePair(LAM, 0, SUB, NUL);
Location r_seq_10 = portLoc(2, seq_10);
Term k_9 = newTerm(VAR, 0, portLoc(1, seq_10));
// allocate args at core: 1807

Term k_9_3;
k_9 = dupeArg(k_9, &k_9_3, 0);

Term k_9_2;
k_9 = dupeArg(k_9, &k_9_2, 0);

Term k_9_1;
k_9 = dupeArg(k_9, &k_9_1, 0);
Term seq_14 = makePair(LAM, 0, SUB, NUL);
Location r_seq_14 = portLoc(2, seq_14);
Term n_13 = newTerm(VAR, 0, portLoc(1, seq_14));
// allocate args at core: 1808

Term n_13_1;
n_13 = dupeArg(n_13, &n_13_1, 0);
Term glblVal106_5;
glblVal106 = dupeArg(glblVal106, &glblVal106_5, 0);

Term seq_31 = makePair(LAM, 0, SUB, NUL);
Location r_seq_31 = portLoc(2, seq_31);
Term bmi_30 = newTerm(VAR, 0, portLoc(1, seq_31));
// allocate args at core: 1813
Term seq_22 = makePair(LAM, 0, SUB, NUL);
Location r_seq_22 = portLoc(2, seq_22);
Term i_21 = newTerm(VAR, 0, portLoc(1, seq_22));
// allocate args at core: 1811
// call collisionKey at core: 1809
Term Rslt_16Args = makePair(APP, 0, newI60(0), SUB);
Term Rslt_16 = newTerm(VAR, 0, portLoc(2, Rslt_16Args));
Rslt_16Args = makePair(APP, 0, m_1, Rslt_16Args);
#ifdef STRICT
pushRedex(Rslt_16Args, glblFn885);
#else
swap(termLoc(Rslt_16), makePair(LAZ, 0, Rslt_16Args, glblFn885));
#endif

// call sha1 at core: 1809
Term Rslt_17Args = makePair(APP, 0, Rslt_16, SUB);
Term Rslt_17 = newTerm(VAR, 0, portLoc(2, Rslt_17Args));
#ifdef STRICT
pushRedex(Rslt_17Args, newRef(glblProto104_1142));
#else
swap(termLoc(Rslt_17), makePair(LAZ, 0, Rslt_17Args, newRef(glblProto104_1142)));
#endif
// call = at core: 1809
Term Rslt_18Args = makePair(APP, 0, hash_4, SUB);
Term Rslt_18 = newTerm(VAR, 0, portLoc(2, Rslt_18Args));
Rslt_18Args = makePair(APP, 0, Rslt_17, Rslt_18Args);
#ifdef STRICT
pushRedex(Rslt_18Args, newRef(glblProto20_1144));
#else
swap(termLoc(Rslt_18), makePair(LAZ, 0, Rslt_18Args, newRef(glblProto20_1144)));
#endif
// call collisionAssoc-find at core: 1810
Term Rslt_19Args = makePair(APP, 0, newI60(0), SUB);
Term Rslt_19 = newTerm(VAR, 0, portLoc(2, Rslt_19Args));
Rslt_19Args = makePair(APP, 0, n_13, Rslt_19Args);
Rslt_19Args = makePair(APP, 0, k_9, Rslt_19Args);
Rslt_19Args = makePair(APP, 0, m_1_1, Rslt_19Args);
#ifdef STRICT
pushRedex(Rslt_19Args, glblFn1119);
#else
swap(termLoc(Rslt_19), makePair(LAZ, 0, Rslt_19Args, glblFn1119));
#endif

// call collisionUpdate at core: 1811
Term Rslt_24Args = makePair(APP, 0, v_3, SUB);
Term Rslt_24 = newTerm(VAR, 0, portLoc(2, Rslt_24Args));
Rslt_24Args = makePair(APP, 0, k_9_1, Rslt_24Args);
Rslt_24Args = makePair(APP, 0, i_21, Rslt_24Args);
Rslt_24Args = makePair(APP, 0, m_1_2, Rslt_24Args);
#ifdef STRICT
pushRedex(Rslt_24Args, glblFn893);
#else
swap(termLoc(Rslt_24), makePair(LAZ, 0, Rslt_24Args, glblFn893));
#endif

// link args to body
swap(r_seq_22, Rslt_24);

Term Rslt_24V = get(termLoc(Rslt_24));
apps = take(portLoc(1, Rslt_24V));
lams = take(portLoc(2, Rslt_24V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_24), makePair(LAZ, 0, apps, lams));
// call map at core: 1810
Term Rslt_25Args = makePair(APP, 0, seq_22, SUB);
Term Rslt_25 = newTerm(VAR, 0, portLoc(2, Rslt_25Args));
Rslt_25Args = makePair(APP, 0, Rslt_19, Rslt_25Args);
#ifdef STRICT
pushRedex(Rslt_25Args, newRef(glblProto42_1146));
#else
swap(termLoc(Rslt_25), makePair(LAZ, 0, Rslt_25Args, newRef(glblProto42_1146)));
#endif
// call collisionAppend at core: 1812
Term Rslt_27Args = makePair(APP, 0, v_3_1, SUB);
Term Rslt_27 = newTerm(VAR, 0, portLoc(2, Rslt_27Args));
Rslt_27Args = makePair(APP, 0, k_9_2, Rslt_27Args);
Rslt_27Args = makePair(APP, 0, m_1_3, Rslt_27Args);
#ifdef STRICT
pushRedex(Rslt_27Args, glblFn896);
#else
swap(termLoc(Rslt_27), makePair(LAZ, 0, Rslt_27Args, glblFn896));
#endif

// call either at core: 1810
Term Rslt_28Args = makePair(APP, 0, Rslt_27, SUB);
Term Rslt_28 = newTerm(VAR, 0, portLoc(2, Rslt_28Args));
Rslt_28Args = makePair(APP, 0, Rslt_25, Rslt_28Args);
#ifdef STRICT
pushRedex(Rslt_28Args, newRef(glblProto32_1148));
#else
swap(termLoc(Rslt_28), makePair(LAZ, 0, Rslt_28Args, newRef(glblProto32_1148)));
#endif
// call collisionAssoc-promote-i at core: 1814
Term Rslt_33Args = makePair(APP, 0, newI60(0), SUB);
Term Rslt_33 = newTerm(VAR, 0, portLoc(2, Rslt_33Args));
Rslt_33Args = makePair(APP, 0, n_13_1, Rslt_33Args);
Rslt_33Args = makePair(APP, 0, m_1_4, Rslt_33Args);
Rslt_33Args = makePair(APP, 0, bmi_30, Rslt_33Args);
#ifdef STRICT
pushRedex(Rslt_33Args, glblFn1130);
#else
swap(termLoc(Rslt_33), makePair(LAZ, 0, Rslt_33Args, glblFn1130));
#endif

// link args to body
swap(r_seq_31, Rslt_33);

Term Rslt_33V = get(termLoc(Rslt_33));
apps = take(portLoc(1, Rslt_33V));
lams = take(portLoc(2, Rslt_33V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_33), makePair(LAZ, 0, apps, lams));
// call copyAssoc at core: 1813
Term Rslt_34Args = makePair(APP, 0, newI60(0), SUB);
Term Rslt_34 = newTerm(VAR, 0, portLoc(2, Rslt_34Args));
Rslt_34Args = makePair(APP, 0, hash_4_1, Rslt_34Args);
Rslt_34Args = makePair(APP, 0, v_3_2, Rslt_34Args);
Rslt_34Args = makePair(APP, 0, k_9_3, Rslt_34Args);
Rslt_34Args = makePair(APP, 0, glblVal106_5, Rslt_34Args);
#ifdef STRICT
pushRedex(Rslt_34Args, newRef(glblProto109_1150));
#else
swap(termLoc(Rslt_34), makePair(LAZ, 0, Rslt_34Args, newRef(glblProto109_1150)));
#endif
// call  at core: 1813
Term Rslt_35Args = makePair(APP, 0, Rslt_34, SUB);
Term Rslt_35 = newTerm(VAR, 0, portLoc(2, Rslt_35Args));
#ifdef STRICT
pushRedex(Rslt_35Args, seq_31);
#else
swap(termLoc(Rslt_35), makePair(LAZ, 0, Rslt_35Args, seq_31));
#endif

// call cond at core: 1809
Term Rslt_36Args = makePair(APP, 0, Rslt_35, SUB);
Term Rslt_36 = newTerm(VAR, 0, portLoc(2, Rslt_36Args));
Rslt_36Args = makePair(APP, 0, Rslt_28, Rslt_36Args);
Rslt_36Args = makePair(APP, 0, Rslt_18, Rslt_36Args);
#ifdef STRICT
pushRedex(Rslt_36Args, newRef(glblProto34_1152));
#else
swap(termLoc(Rslt_36), makePair(LAZ, 0, Rslt_36Args, newRef(glblProto34_1152)));
#endif
// link args to body
swap(r_seq_14, Rslt_36);

Term Rslt_36V = get(termLoc(Rslt_36));
apps = take(portLoc(1, Rslt_36V));
lams = take(portLoc(2, Rslt_36V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_36), makePair(LAZ, 0, apps, lams));
// call collisionCount at core: 1808
Term Rslt_37Args = makePair(APP, 0, m_1_5, SUB);
Term Rslt_37 = newTerm(VAR, 0, portLoc(2, Rslt_37Args));
#ifdef STRICT
pushRedex(Rslt_37Args, glblFn880);
#else
swap(termLoc(Rslt_37), makePair(LAZ, 0, Rslt_37Args, glblFn880));
#endif

// call  at core: 1808
Term Rslt_38Args = makePair(APP, 0, Rslt_37, SUB);
Term Rslt_38 = newTerm(VAR, 0, portLoc(2, Rslt_38Args));
#ifdef STRICT
pushRedex(Rslt_38Args, seq_14);
#else
swap(termLoc(Rslt_38), makePair(LAZ, 0, Rslt_38Args, seq_14));
#endif

// link args to body
swap(r_seq_10, Rslt_38);

Term Rslt_38V = get(termLoc(Rslt_38));
apps = take(portLoc(1, Rslt_38V));
lams = take(portLoc(2, Rslt_38V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_38), makePair(LAZ, 0, apps, lams));
// call set-hash-val at core: 1807
Term Rslt_39Args = makePair(APP, 0, hash_4_2, SUB);
Term Rslt_39 = newTerm(VAR, 0, portLoc(2, Rslt_39Args));
Rslt_39Args = makePair(APP, 0, k_2, Rslt_39Args);
#ifdef STRICT
pushRedex(Rslt_39Args, glblFn917);
#else
swap(termLoc(Rslt_39), makePair(LAZ, 0, Rslt_39Args, glblFn917));
#endif

// call  at core: 1807
Term Rslt_40Args = makePair(APP, 0, Rslt_39, SUB);
Term Rslt_40 = newTerm(VAR, 0, portLoc(2, Rslt_40Args));
#ifdef STRICT
pushRedex(Rslt_40Args, seq_10);
#else
swap(termLoc(Rslt_40), makePair(LAZ, 0, Rslt_40Args, seq_10));
#endif

// link args to body
swap(r_seq_6, Rslt_40);

Term Rslt_40V = get(termLoc(Rslt_40));
apps = take(portLoc(1, Rslt_40V));
lams = take(portLoc(2, Rslt_40V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_40), makePair(LAZ, 0, apps, lams));
pushRedex(args, seq_6);
return;
}
Term glblFn1141 = newRef(glblCFn1140);
void glblProto20_1157(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(1822), args);
  args = makePair(APP, 0, glblStr199, args);
  interact(args, glblProto20);
}
}
void glblProto20_1159(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(1825), args);
  args = makePair(APP, 0, glblStr199, args);
  interact(args, glblProto20);
}
}
void glblProto34_1161(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(1825), args);
  args = makePair(APP, 0, glblStr199, args);
  interact(args, glblProto34);
}
}
void glblProto34_1163(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(1822), args);
  args = makePair(APP, 0, glblStr199, args);
  interact(args, glblProto34);
}
}
Term glblFn1156;
void glblCFn1155(Term ref, Term args) {
Term apps, lams, V;
Location vLoc;
Term seq_5 = makePair(LAM, 0, SUB, NUL);
Location r_seq_5 = portLoc(2, seq_5);
Term i_4 = newTerm(VAR, 0, portLoc(1, seq_5));
seq_5 = makePair(LAM, 0, SUB, seq_5);
Term n_3 = newTerm(VAR, 0, portLoc(1, seq_5));
seq_5 = makePair(LAM, 0, SUB, seq_5);
Term k_2 = newTerm(VAR, 0, portLoc(1, seq_5));
seq_5 = makePair(LAM, 0, SUB, seq_5);
Term m_1 = newTerm(VAR, 0, portLoc(1, seq_5));
// allocate args at core: 1816

Term m_1_1;
m_1 = dupeArg(m_1, &m_1_1, 204);

Term k_2_1;
k_2 = dupeArg(k_2, &k_2_1, 205);

Term n_3_1;
n_3 = dupeArg(n_3, &n_3_1, 206);

Term i_4_3;
i_4 = dupeArg(i_4, &i_4_3, 207);

Term i_4_2;
i_4 = dupeArg(i_4, &i_4_2, 208);

Term i_4_1;
i_4 = dupeArg(i_4, &i_4_1, 209);
Term seq_10 = makePair(LAM, 0, SUB, NUL);
Location r_seq_10 = portLoc(2, seq_10);
Term ek_9 = newTerm(VAR, 0, portLoc(1, seq_10));
// allocate args at core: 1824
Term glblVal25_14;
glblVal25 = dupeArg(glblVal25, &glblVal25_14, 0);

Term glblRslt16_8;
glblRslt16 = dupeArg(glblRslt16, &glblRslt16_8, 0);

// call = at core: 1822
Term Rslt_7Args = makePair(APP, 0, n_3, SUB);
Term Rslt_7 = newTerm(VAR, 0, portLoc(2, Rslt_7Args));
Rslt_7Args = makePair(APP, 0, i_4, Rslt_7Args);
#ifdef STRICT
pushRedex(Rslt_7Args, newRef(glblProto20_1157));
#else
swap(termLoc(Rslt_7), makePair(LAZ, 0, Rslt_7Args, newRef(glblProto20_1157)));
#endif
// call = at core: 1825
Term Rslt_12Args = makePair(APP, 0, ek_9, SUB);
Term Rslt_12 = newTerm(VAR, 0, portLoc(2, Rslt_12Args));
Rslt_12Args = makePair(APP, 0, k_2, Rslt_12Args);
#ifdef STRICT
pushRedex(Rslt_12Args, newRef(glblProto20_1159));
#else
swap(termLoc(Rslt_12), makePair(LAZ, 0, Rslt_12Args, newRef(glblProto20_1159)));
#endif
// call Some at core: 1826
Term Rslt_13Args = makePair(APP, 0, i_4_1, SUB);
Term Rslt_13 = newTerm(VAR, 0, portLoc(2, Rslt_13Args));
#ifdef STRICT
pushRedex(Rslt_13Args, glblVal25_14);
#else
swap(termLoc(Rslt_13), makePair(LAZ, 0, Rslt_13Args, glblVal25_14));
#endif

// call inc at core: 1827
Term Rslt_14Args = makePair(APP, 0, i_4_2, SUB);
Term Rslt_14 = newTerm(VAR, 0, portLoc(2, Rslt_14Args));
#ifdef STRICT
pushRedex(Rslt_14Args, glblFn141);
#else
swap(termLoc(Rslt_14), makePair(LAZ, 0, Rslt_14Args, glblFn141));
#endif

// call collisionDissoc-find at core: 1816
Term Rslt_15Args = makePair(APP, 0, Rslt_14, SUB);
Term Rslt_15 = newTerm(VAR, 0, portLoc(2, Rslt_15Args));
Rslt_15Args = makePair(APP, 0, n_3_1, Rslt_15Args);
Rslt_15Args = makePair(APP, 0, k_2_1, Rslt_15Args);
Rslt_15Args = makePair(APP, 0, m_1, Rslt_15Args);
#ifdef STRICT
pushRedex(Rslt_15Args, glblFn1156);
#else
swap(termLoc(Rslt_15), makePair(LAZ, 0, Rslt_15Args, glblFn1156));
#endif

// call cond at core: 1825
Term Rslt_16Args = makePair(APP, 0, Rslt_15, SUB);
Term Rslt_16 = newTerm(VAR, 0, portLoc(2, Rslt_16Args));
Rslt_16Args = makePair(APP, 0, Rslt_13, Rslt_16Args);
Rslt_16Args = makePair(APP, 0, Rslt_12, Rslt_16Args);
#ifdef STRICT
pushRedex(Rslt_16Args, newRef(glblProto34_1161));
#else
swap(termLoc(Rslt_16), makePair(LAZ, 0, Rslt_16Args, newRef(glblProto34_1161)));
#endif
// link args to body
swap(r_seq_10, Rslt_16);

Term Rslt_16V = get(termLoc(Rslt_16));
apps = take(portLoc(1, Rslt_16V));
lams = take(portLoc(2, Rslt_16V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_16), makePair(LAZ, 0, apps, lams));
// call collisionKey at core: 1824
Term Rslt_17Args = makePair(APP, 0, i_4_3, SUB);
Term Rslt_17 = newTerm(VAR, 0, portLoc(2, Rslt_17Args));
Rslt_17Args = makePair(APP, 0, m_1_1, Rslt_17Args);
#ifdef STRICT
pushRedex(Rslt_17Args, glblFn885);
#else
swap(termLoc(Rslt_17), makePair(LAZ, 0, Rslt_17Args, glblFn885));
#endif

// call  at core: 1824
Term Rslt_18Args = makePair(APP, 0, Rslt_17, SUB);
Term Rslt_18 = newTerm(VAR, 0, portLoc(2, Rslt_18Args));
#ifdef STRICT
pushRedex(Rslt_18Args, seq_10);
#else
swap(termLoc(Rslt_18), makePair(LAZ, 0, Rslt_18Args, seq_10));
#endif

// call cond at core: 1822
Term Rslt_19Args = makePair(APP, 0, Rslt_18, SUB);
Term Rslt_19 = newTerm(VAR, 0, portLoc(2, Rslt_19Args));
Rslt_19Args = makePair(APP, 0, glblRslt16_8, Rslt_19Args);
Rslt_19Args = makePair(APP, 0, Rslt_7, Rslt_19Args);
#ifdef STRICT
pushRedex(Rslt_19Args, newRef(glblProto34_1163));
#else
swap(termLoc(Rslt_19), makePair(LAZ, 0, Rslt_19Args, newRef(glblProto34_1163)));
#endif
// link args to body
swap(r_seq_5, Rslt_19);

Term Rslt_19V = get(termLoc(Rslt_19));
apps = take(portLoc(1, Rslt_19V));
lams = take(portLoc(2, Rslt_19V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_19), makePair(LAZ, 0, apps, lams));
pushRedex(args, seq_5);
return;
}
Term glblFn1156 = newRef(glblCFn1155);
void glblProto20_1170(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(1842), args);
  args = makePair(APP, 0, glblStr199, args);
  interact(args, glblProto20);
}
}
void glblProto20_1172(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(1844), args);
  args = makePair(APP, 0, glblStr199, args);
  interact(args, glblProto20);
}
}
void glblProto34_1174(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(1844), args);
  args = makePair(APP, 0, glblStr199, args);
  interact(args, glblProto34);
}
}
void glblProto42_1179(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(1848), args);
  args = makePair(APP, 0, glblStr199, args);
  interact(args, glblProto42);
}
}
void glblProto32_1183(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(1848), args);
  args = makePair(APP, 0, glblStr199, args);
  interact(args, glblProto32);
}
}
void glblProto34_1185(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(1842), args);
  args = makePair(APP, 0, glblStr199, args);
  interact(args, glblProto34);
}
}
Term glblFn1167;
void glblCFn1166(Term ref, Term args) {
Term apps, lams, V;
Location vLoc;
Term seq_5 = makePair(LAM, 0, SUB, NUL);
Location r_seq_5 = portLoc(2, seq_5);
Term shift_4 = newTerm(VAR, 0, portLoc(1, seq_5));
seq_5 = makePair(LAM, 0, SUB, seq_5);
Term hash_3 = newTerm(VAR, 0, portLoc(1, seq_5));
seq_5 = makePair(LAM, 0, SUB, seq_5);
Term k_2 = newTerm(VAR, 0, portLoc(1, seq_5));
seq_5 = makePair(LAM, 0, SUB, seq_5);
Term m_1 = newTerm(VAR, 0, portLoc(1, seq_5));
// allocate args at core: 1829

Term m_1_5;
m_1 = dupeArg(m_1, &m_1_5, 210);

Term m_1_4;
m_1 = dupeArg(m_1, &m_1_4, 211);

Term m_1_3;
m_1 = dupeArg(m_1, &m_1_3, 212);

Term m_1_2;
m_1 = dupeArg(m_1, &m_1_2, 213);

Term m_1_1;
m_1 = dupeArg(m_1, &m_1_1, 214);

Term k_2_1;
k_2 = dupeArg(k_2, &k_2_1, 215);
swap(termLoc(hash_3), ERA);
swap(termLoc(shift_4), ERA);
Term seq_9 = makePair(LAM, 0, SUB, NUL);
Location r_seq_9 = portLoc(2, seq_9);
Term n_8 = newTerm(VAR, 0, portLoc(1, seq_9));
// allocate args at core: 1841

Term n_8_1;
n_8 = dupeArg(n_8, &n_8_1, 0);
Term seq_22 = makePair(LAM, 0, SUB, NUL);
Location r_seq_22 = portLoc(2, seq_22);
Term idx_21 = newTerm(VAR, 0, portLoc(1, seq_22));
// allocate args at core: 1847
Term seq_26 = makePair(LAM, 0, SUB, NUL);
Location r_seq_26 = portLoc(2, seq_26);
Term i_25 = newTerm(VAR, 0, portLoc(1, seq_26));
// allocate args at core: 1848
Term seq_14 = makePair(LAM, 0, SUB, NUL);
Location r_seq_14 = portLoc(2, seq_14);
Term ek_13 = newTerm(VAR, 0, portLoc(1, seq_14));
// allocate args at core: 1843
Term glblVal106_6;
glblVal106 = dupeArg(glblVal106, &glblVal106_6, 0);

// call = at core: 1842
Term Rslt_11Args = makePair(APP, 0, newI60(1), SUB);
Term Rslt_11 = newTerm(VAR, 0, portLoc(2, Rslt_11Args));
Rslt_11Args = makePair(APP, 0, n_8, Rslt_11Args);
#ifdef STRICT
pushRedex(Rslt_11Args, newRef(glblProto20_1170));
#else
swap(termLoc(Rslt_11), makePair(LAZ, 0, Rslt_11Args, newRef(glblProto20_1170)));
#endif
// call = at core: 1844
Term Rslt_16Args = makePair(APP, 0, ek_13, SUB);
Term Rslt_16 = newTerm(VAR, 0, portLoc(2, Rslt_16Args));
Rslt_16Args = makePair(APP, 0, k_2, Rslt_16Args);
#ifdef STRICT
pushRedex(Rslt_16Args, newRef(glblProto20_1172));
#else
swap(termLoc(Rslt_16), makePair(LAZ, 0, Rslt_16Args, newRef(glblProto20_1172)));
#endif
// call cond at core: 1844
Term Rslt_17Args = makePair(APP, 0, m_1, SUB);
Term Rslt_17 = newTerm(VAR, 0, portLoc(2, Rslt_17Args));
Rslt_17Args = makePair(APP, 0, glblVal106_6, Rslt_17Args);
Rslt_17Args = makePair(APP, 0, Rslt_16, Rslt_17Args);
#ifdef STRICT
pushRedex(Rslt_17Args, newRef(glblProto34_1174));
#else
swap(termLoc(Rslt_17), makePair(LAZ, 0, Rslt_17Args, newRef(glblProto34_1174)));
#endif
// link args to body
swap(r_seq_14, Rslt_17);

Term Rslt_17V = get(termLoc(Rslt_17));
apps = take(portLoc(1, Rslt_17V));
lams = take(portLoc(2, Rslt_17V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_17), makePair(LAZ, 0, apps, lams));
// call collisionKey at core: 1843
Term Rslt_18Args = makePair(APP, 0, newI60(0), SUB);
Term Rslt_18 = newTerm(VAR, 0, portLoc(2, Rslt_18Args));
Rslt_18Args = makePair(APP, 0, m_1_1, Rslt_18Args);
#ifdef STRICT
pushRedex(Rslt_18Args, glblFn885);
#else
swap(termLoc(Rslt_18), makePair(LAZ, 0, Rslt_18Args, glblFn885));
#endif

// call  at core: 1843
Term Rslt_19Args = makePair(APP, 0, Rslt_18, SUB);
Term Rslt_19 = newTerm(VAR, 0, portLoc(2, Rslt_19Args));
#ifdef STRICT
pushRedex(Rslt_19Args, seq_14);
#else
swap(termLoc(Rslt_19), makePair(LAZ, 0, Rslt_19Args, seq_14));
#endif

// call collisionShrink at core: 1848
Term Rslt_28Args = makePair(APP, 0, i_25, SUB);
Term Rslt_28 = newTerm(VAR, 0, portLoc(2, Rslt_28Args));
Rslt_28Args = makePair(APP, 0, m_1_2, Rslt_28Args);
#ifdef STRICT
pushRedex(Rslt_28Args, glblFn899);
#else
swap(termLoc(Rslt_28), makePair(LAZ, 0, Rslt_28Args, glblFn899));
#endif

// link args to body
swap(r_seq_26, Rslt_28);

Term Rslt_28V = get(termLoc(Rslt_28));
apps = take(portLoc(1, Rslt_28V));
lams = take(portLoc(2, Rslt_28V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_28), makePair(LAZ, 0, apps, lams));
// call map at core: 1848
Term Rslt_29Args = makePair(APP, 0, seq_26, SUB);
Term Rslt_29 = newTerm(VAR, 0, portLoc(2, Rslt_29Args));
Rslt_29Args = makePair(APP, 0, idx_21, Rslt_29Args);
#ifdef STRICT
pushRedex(Rslt_29Args, newRef(glblProto42_1179));
#else
swap(termLoc(Rslt_29), makePair(LAZ, 0, Rslt_29Args, newRef(glblProto42_1179)));
#endif
// call either at core: 1848
Term Rslt_31Args = makePair(APP, 0, m_1_3, SUB);
Term Rslt_31 = newTerm(VAR, 0, portLoc(2, Rslt_31Args));
Rslt_31Args = makePair(APP, 0, Rslt_29, Rslt_31Args);
#ifdef STRICT
pushRedex(Rslt_31Args, newRef(glblProto32_1183));
#else
swap(termLoc(Rslt_31), makePair(LAZ, 0, Rslt_31Args, newRef(glblProto32_1183)));
#endif
// link args to body
swap(r_seq_22, Rslt_31);

Term Rslt_31V = get(termLoc(Rslt_31));
apps = take(portLoc(1, Rslt_31V));
lams = take(portLoc(2, Rslt_31V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_31), makePair(LAZ, 0, apps, lams));
// call collisionDissoc-find at core: 1847
Term Rslt_32Args = makePair(APP, 0, newI60(0), SUB);
Term Rslt_32 = newTerm(VAR, 0, portLoc(2, Rslt_32Args));
Rslt_32Args = makePair(APP, 0, n_8_1, Rslt_32Args);
Rslt_32Args = makePair(APP, 0, k_2_1, Rslt_32Args);
Rslt_32Args = makePair(APP, 0, m_1_4, Rslt_32Args);
#ifdef STRICT
pushRedex(Rslt_32Args, glblFn1156);
#else
swap(termLoc(Rslt_32), makePair(LAZ, 0, Rslt_32Args, glblFn1156));
#endif

// call  at core: 1847
Term Rslt_33Args = makePair(APP, 0, Rslt_32, SUB);
Term Rslt_33 = newTerm(VAR, 0, portLoc(2, Rslt_33Args));
#ifdef STRICT
pushRedex(Rslt_33Args, seq_22);
#else
swap(termLoc(Rslt_33), makePair(LAZ, 0, Rslt_33Args, seq_22));
#endif

// call cond at core: 1842
Term Rslt_34Args = makePair(APP, 0, Rslt_33, SUB);
Term Rslt_34 = newTerm(VAR, 0, portLoc(2, Rslt_34Args));
Rslt_34Args = makePair(APP, 0, Rslt_19, Rslt_34Args);
Rslt_34Args = makePair(APP, 0, Rslt_11, Rslt_34Args);
#ifdef STRICT
pushRedex(Rslt_34Args, newRef(glblProto34_1185));
#else
swap(termLoc(Rslt_34), makePair(LAZ, 0, Rslt_34Args, newRef(glblProto34_1185)));
#endif
// link args to body
swap(r_seq_9, Rslt_34);

Term Rslt_34V = get(termLoc(Rslt_34));
apps = take(portLoc(1, Rslt_34V));
lams = take(portLoc(2, Rslt_34V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_34), makePair(LAZ, 0, apps, lams));
// call collisionCount at core: 1841
Term Rslt_35Args = makePair(APP, 0, m_1_5, SUB);
Term Rslt_35 = newTerm(VAR, 0, portLoc(2, Rslt_35Args));
#ifdef STRICT
pushRedex(Rslt_35Args, glblFn880);
#else
swap(termLoc(Rslt_35), makePair(LAZ, 0, Rslt_35Args, glblFn880));
#endif

// call  at core: 1841
Term Rslt_36Args = makePair(APP, 0, Rslt_35, SUB);
Term Rslt_36 = newTerm(VAR, 0, portLoc(2, Rslt_36Args));
#ifdef STRICT
pushRedex(Rslt_36Args, seq_9);
#else
swap(termLoc(Rslt_36), makePair(LAZ, 0, Rslt_36Args, seq_9));
#endif

// link args to body
swap(r_seq_5, Rslt_36);

Term Rslt_36V = get(termLoc(Rslt_36));
apps = take(portLoc(1, Rslt_36V));
lams = take(portLoc(2, Rslt_36V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_36), makePair(LAZ, 0, apps, lams));
pushRedex(args, seq_5);
return;
}
Term glblFn1167 = newRef(glblCFn1166);
Term glblFn1169;
void glblCFn1168(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  Term result = NUL;
    if (arityArgs.count == 1) {
  Term v_1 = arityArgs.args[0];

    if (termTag(v_1) == I60) {
      result = newI60(-2);
    } else {
      result = newI60((int64_t)((Value *)v_1)->refs);
    }
    dec_and_free(v_1, 1);
  
move(portLoc(2, args), result);
}
return;
}
Term glblFn1169 = newRef(glblCFn1168);
void glblGetSentinelVal1178(Term ref, Term args) {
Term callArgs = makePair(APP, 0, newI60(0), args);
callArgs = makePair(APP, 0, newI60(45), callArgs);
interact(callArgs, construct);
return;
}
Term glblVal1182 = newRef(glblGetSentinelVal1178);
Term glblVal1182;
Term glblRslt1181;
void glblProto20_1189(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(1868), args);
  args = makePair(APP, 0, glblStr199, args);
  interact(args, glblProto20);
}
}
Term glbl_EQ_1177;
void glbl_EQ_1176(Term ref, Term args) {
Term apps, lams, V;
Location vLoc;
Term seq_3 = makePair(LAM, 0, SUB, NUL);
Location r_seq_3 = portLoc(2, seq_3);
Term y_2 = newTerm(VAR, 0, portLoc(1, seq_3));
seq_3 = makePair(LAM, 0, SUB, seq_3);
Term x_1 = newTerm(VAR, 0, portLoc(1, seq_3));
// allocate args at core: 1868
// call type-num at core: 1868
Term Rslt_5Args = makePair(APP, 0, x_1, SUB);
Term Rslt_5 = newTerm(VAR, 0, portLoc(2, Rslt_5Args));
#ifdef STRICT
pushRedex(Rslt_5Args, glblFn11);
#else
swap(termLoc(Rslt_5), makePair(LAZ, 0, Rslt_5Args, glblFn11));
#endif

// call type-num at core: 1868
Term Rslt_6Args = makePair(APP, 0, y_2, SUB);
Term Rslt_6 = newTerm(VAR, 0, portLoc(2, Rslt_6Args));
#ifdef STRICT
pushRedex(Rslt_6Args, glblFn11);
#else
swap(termLoc(Rslt_6), makePair(LAZ, 0, Rslt_6Args, glblFn11));
#endif

// call = at core: 1868
Term Rslt_7Args = makePair(APP, 0, Rslt_6, SUB);
Term Rslt_7 = newTerm(VAR, 0, portLoc(2, Rslt_7Args));
Rslt_7Args = makePair(APP, 0, Rslt_5, Rslt_7Args);
#ifdef STRICT
pushRedex(Rslt_7Args, newRef(glblProto20_1189));
#else
swap(termLoc(Rslt_7), makePair(LAZ, 0, Rslt_7Args, newRef(glblProto20_1189)));
#endif
// link args to body
swap(r_seq_3, Rslt_7);

Term Rslt_7V = get(termLoc(Rslt_7));
apps = take(portLoc(1, Rslt_7V));
lams = take(portLoc(2, Rslt_7V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_7), makePair(LAZ, 0, apps, lams));
pushRedex(args, seq_3);
return;
}
Term glbl_EQ_1177 = newRef(glbl_EQ_1176);
Term glblcount1207;
void glblcount1206(Term ref, Term args) {
Term apps, lams, V;
Location vLoc;
Term seq_10 = makePair(LAM, 0, SUB, NUL);
Location r_seq_10 = portLoc(2, seq_10);
Term m_9 = newTerm(VAR, 0, portLoc(1, seq_10));
// allocate args at core: 1871
// call bmiCount at core: 1872
Term Rslt_12Args = makePair(APP, 0, m_9, SUB);
Term Rslt_12 = newTerm(VAR, 0, portLoc(2, Rslt_12Args));
#ifdef STRICT
pushRedex(Rslt_12Args, glblFn756);
#else
swap(termLoc(Rslt_12), makePair(LAZ, 0, Rslt_12Args, glblFn756));
#endif

// link args to body
swap(r_seq_10, Rslt_12);

Term Rslt_12V = get(termLoc(Rslt_12));
apps = take(portLoc(1, Rslt_12V));
lams = take(portLoc(2, Rslt_12V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_12), makePair(LAZ, 0, apps, lams));
pushRedex(args, seq_10);
return;
}
Term glblcount1207 = newRef(glblcount1206);
Term glblvec1205;
void glblvec1204(Term ref, Term args) {
Term apps, lams, V;
Location vLoc;
Term seq_15 = makePair(LAM, 0, SUB, NUL);
Location r_seq_15 = portLoc(2, seq_15);
Term m_14 = newTerm(VAR, 0, portLoc(1, seq_15));
// allocate args at core: 1874
// call bmiVec at core: 1875
Term Rslt_17Args = makePair(APP, 0, m_14, SUB);
Term Rslt_17 = newTerm(VAR, 0, portLoc(2, Rslt_17Args));
#ifdef STRICT
pushRedex(Rslt_17Args, glblFn922);
#else
swap(termLoc(Rslt_17), makePair(LAZ, 0, Rslt_17Args, glblFn922));
#endif

// link args to body
swap(r_seq_15, Rslt_17);

Term Rslt_17V = get(termLoc(Rslt_17));
apps = take(portLoc(1, Rslt_17V));
lams = take(portLoc(2, Rslt_17V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_17), makePair(LAZ, 0, apps, lams));
pushRedex(args, seq_15);
return;
}
Term glblvec1205 = newRef(glblvec1204);
Term glblhash_vec1203;
void glblhash_vec1202(Term ref, Term args) {
Term apps, lams, V;
Location vLoc;
Term seq_21 = makePair(LAM, 0, SUB, NUL);
Location r_seq_21 = portLoc(2, seq_21);
Term s_20 = newTerm(VAR, 0, portLoc(1, seq_21));
seq_21 = makePair(LAM, 0, SUB, seq_21);
Term m_19 = newTerm(VAR, 0, portLoc(1, seq_21));
// allocate args at core: 1877
// call bmiHashSeq at core: 1878
Term Rslt_23Args = makePair(APP, 0, s_20, SUB);
Term Rslt_23 = newTerm(VAR, 0, portLoc(2, Rslt_23Args));
Rslt_23Args = makePair(APP, 0, m_19, Rslt_23Args);
#ifdef STRICT
pushRedex(Rslt_23Args, glblFn958);
#else
swap(termLoc(Rslt_23), makePair(LAZ, 0, Rslt_23Args, glblFn958));
#endif

// link args to body
swap(r_seq_21, Rslt_23);

Term Rslt_23V = get(termLoc(Rslt_23));
apps = take(portLoc(1, Rslt_23V));
lams = take(portLoc(2, Rslt_23V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_23), makePair(LAZ, 0, apps, lams));
pushRedex(args, seq_21);
return;
}
Term glblhash_vec1203 = newRef(glblhash_vec1202);
void glblProto121_1211(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(1881), args);
  args = makePair(APP, 0, glblStr199, args);
  interact(args, glblProto121);
}
}
void glblProto42_1213(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(1881), args);
  args = makePair(APP, 0, glblStr199, args);
  interact(args, glblProto42);
}
}
Term glblvals1201;
void glblvals1200(Term ref, Term args) {
Term apps, lams, V;
Location vLoc;
Term seq_26 = makePair(LAM, 0, SUB, NUL);
Location r_seq_26 = portLoc(2, seq_26);
Term m_25 = newTerm(VAR, 0, portLoc(1, seq_26));
// allocate args at core: 1880
Term glblVal175_25;
glblVal175 = dupeArg(glblVal175, &glblVal175_25, 0);

// call hash-vec at core: 1881
Term Rslt_28Args = makePair(APP, 0, glblVal175_25, SUB);
Term Rslt_28 = newTerm(VAR, 0, portLoc(2, Rslt_28Args));
Rslt_28Args = makePair(APP, 0, m_25, Rslt_28Args);
#ifdef STRICT
pushRedex(Rslt_28Args, newRef(glblProto121_1211));
#else
swap(termLoc(Rslt_28), makePair(LAZ, 0, Rslt_28Args, newRef(glblProto121_1211)));
#endif
// call map at core: 1881
Term Rslt_29Args = makePair(APP, 0, glblFn990, SUB);
Term Rslt_29 = newTerm(VAR, 0, portLoc(2, Rslt_29Args));
Rslt_29Args = makePair(APP, 0, Rslt_28, Rslt_29Args);
#ifdef STRICT
pushRedex(Rslt_29Args, newRef(glblProto42_1213));
#else
swap(termLoc(Rslt_29), makePair(LAZ, 0, Rslt_29Args, newRef(glblProto42_1213)));
#endif
// link args to body
swap(r_seq_26, Rslt_29);

Term Rslt_29V = get(termLoc(Rslt_29));
apps = take(portLoc(1, Rslt_29V));
lams = take(portLoc(2, Rslt_29V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_29), makePair(LAZ, 0, apps, lams));
pushRedex(args, seq_26);
return;
}
Term glblvals1201 = newRef(glblvals1200);
Term glblcopyAssoc1199;
void glblcopyAssoc1198(Term ref, Term args) {
Term apps, lams, V;
Location vLoc;
Term seq_36 = makePair(LAM, 0, SUB, NUL);
Location r_seq_36 = portLoc(2, seq_36);
Term shift_35 = newTerm(VAR, 0, portLoc(1, seq_36));
seq_36 = makePair(LAM, 0, SUB, seq_36);
Term hash_34 = newTerm(VAR, 0, portLoc(1, seq_36));
seq_36 = makePair(LAM, 0, SUB, seq_36);
Term v_33 = newTerm(VAR, 0, portLoc(1, seq_36));
seq_36 = makePair(LAM, 0, SUB, seq_36);
Term k_32 = newTerm(VAR, 0, portLoc(1, seq_36));
seq_36 = makePair(LAM, 0, SUB, seq_36);
Term m_31 = newTerm(VAR, 0, portLoc(1, seq_36));
// allocate args at core: 1883
// call bmiCopyAssoc at core: 1884
Term Rslt_38Args = makePair(APP, 0, shift_35, SUB);
Term Rslt_38 = newTerm(VAR, 0, portLoc(2, Rslt_38Args));
Rslt_38Args = makePair(APP, 0, hash_34, Rslt_38Args);
Rslt_38Args = makePair(APP, 0, v_33, Rslt_38Args);
Rslt_38Args = makePair(APP, 0, k_32, Rslt_38Args);
Rslt_38Args = makePair(APP, 0, m_31, Rslt_38Args);
#ifdef STRICT
pushRedex(Rslt_38Args, glblFn997);
#else
swap(termLoc(Rslt_38), makePair(LAZ, 0, Rslt_38Args, glblFn997));
#endif

// link args to body
swap(r_seq_36, Rslt_38);

Term Rslt_38V = get(termLoc(Rslt_38));
apps = take(portLoc(1, Rslt_38V));
lams = take(portLoc(2, Rslt_38V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_38), makePair(LAZ, 0, apps, lams));
pushRedex(args, seq_36);
return;
}
Term glblcopyAssoc1199 = newRef(glblcopyAssoc1198);
void glblProto20_1217(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(1888), args);
  args = makePair(APP, 0, glblStr199, args);
  interact(args, glblProto20);
}
}
void glblProto34_1219(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(1888), args);
  args = makePair(APP, 0, glblStr199, args);
  interact(args, glblProto34);
}
}
Term glblassoc_STAR_1197;
void glblassoc_STAR_1196(Term ref, Term args) {
Term apps, lams, V;
Location vLoc;
Term seq_45 = makePair(LAM, 0, SUB, NUL);
Location r_seq_45 = portLoc(2, seq_45);
Term shift_44 = newTerm(VAR, 0, portLoc(1, seq_45));
seq_45 = makePair(LAM, 0, SUB, seq_45);
Term hash_43 = newTerm(VAR, 0, portLoc(1, seq_45));
seq_45 = makePair(LAM, 0, SUB, seq_45);
Term v_42 = newTerm(VAR, 0, portLoc(1, seq_45));
seq_45 = makePair(LAM, 0, SUB, seq_45);
Term k_41 = newTerm(VAR, 0, portLoc(1, seq_45));
seq_45 = makePair(LAM, 0, SUB, seq_45);
Term m_40 = newTerm(VAR, 0, portLoc(1, seq_45));
// allocate args at core: 1886

Term m_40_2;
m_40 = dupeArg(m_40, &m_40_2, 216);

Term m_40_1;
m_40 = dupeArg(m_40, &m_40_1, 217);

Term k_41_1;
k_41 = dupeArg(k_41, &k_41_1, 218);

Term v_42_1;
v_42 = dupeArg(v_42, &v_42_1, 219);

Term hash_43_1;
hash_43 = dupeArg(hash_43, &hash_43_1, 220);

Term shift_44_1;
shift_44 = dupeArg(shift_44, &shift_44_1, 221);
// call refs-count at core: 1888
Term Rslt_47Args = makePair(APP, 0, m_40, SUB);
Term Rslt_47 = newTerm(VAR, 0, portLoc(2, Rslt_47Args));
#ifdef STRICT
pushRedex(Rslt_47Args, glblFn1169);
#else
swap(termLoc(Rslt_47), makePair(LAZ, 0, Rslt_47Args, glblFn1169));
#endif

// call = at core: 1888
Term Rslt_48Args = makePair(APP, 0, Rslt_47, SUB);
Term Rslt_48 = newTerm(VAR, 0, portLoc(2, Rslt_48Args));
Rslt_48Args = makePair(APP, 0, newI60(3), Rslt_48Args);
#ifdef STRICT
pushRedex(Rslt_48Args, newRef(glblProto20_1217));
#else
swap(termLoc(Rslt_48), makePair(LAZ, 0, Rslt_48Args, newRef(glblProto20_1217)));
#endif
// call bmiMutateAssoc at core: 1889
Term Rslt_49Args = makePair(APP, 0, shift_44, SUB);
Term Rslt_49 = newTerm(VAR, 0, portLoc(2, Rslt_49Args));
Rslt_49Args = makePair(APP, 0, hash_43, Rslt_49Args);
Rslt_49Args = makePair(APP, 0, v_42, Rslt_49Args);
Rslt_49Args = makePair(APP, 0, k_41, Rslt_49Args);
Rslt_49Args = makePair(APP, 0, m_40_1, Rslt_49Args);
#ifdef STRICT
pushRedex(Rslt_49Args, glblFn1014);
#else
swap(termLoc(Rslt_49), makePair(LAZ, 0, Rslt_49Args, glblFn1014));
#endif

// call bmiCopyAssoc at core: 1890
Term Rslt_50Args = makePair(APP, 0, shift_44_1, SUB);
Term Rslt_50 = newTerm(VAR, 0, portLoc(2, Rslt_50Args));
Rslt_50Args = makePair(APP, 0, hash_43_1, Rslt_50Args);
Rslt_50Args = makePair(APP, 0, v_42_1, Rslt_50Args);
Rslt_50Args = makePair(APP, 0, k_41_1, Rslt_50Args);
Rslt_50Args = makePair(APP, 0, m_40_2, Rslt_50Args);
#ifdef STRICT
pushRedex(Rslt_50Args, glblFn997);
#else
swap(termLoc(Rslt_50), makePair(LAZ, 0, Rslt_50Args, glblFn997));
#endif

// call cond at core: 1888
Term Rslt_51Args = makePair(APP, 0, Rslt_50, SUB);
Term Rslt_51 = newTerm(VAR, 0, portLoc(2, Rslt_51Args));
Rslt_51Args = makePair(APP, 0, Rslt_49, Rslt_51Args);
Rslt_51Args = makePair(APP, 0, Rslt_48, Rslt_51Args);
#ifdef STRICT
pushRedex(Rslt_51Args, newRef(glblProto34_1219));
#else
swap(termLoc(Rslt_51), makePair(LAZ, 0, Rslt_51Args, newRef(glblProto34_1219)));
#endif
// link args to body
swap(r_seq_45, Rslt_51);

Term Rslt_51V = get(termLoc(Rslt_51));
apps = take(portLoc(1, Rslt_51V));
lams = take(portLoc(2, Rslt_51V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_51), makePair(LAZ, 0, apps, lams));
pushRedex(args, seq_45);
return;
}
Term glblassoc_STAR_1197 = newRef(glblassoc_STAR_1196);
Term glblget_STAR_1195;
void glblget_STAR_1194(Term ref, Term args) {
Term apps, lams, V;
Location vLoc;
Term seq_58 = makePair(LAM, 0, SUB, NUL);
Location r_seq_58 = portLoc(2, seq_58);
Term shift_57 = newTerm(VAR, 0, portLoc(1, seq_58));
seq_58 = makePair(LAM, 0, SUB, seq_58);
Term hash_56 = newTerm(VAR, 0, portLoc(1, seq_58));
seq_58 = makePair(LAM, 0, SUB, seq_58);
Term def_55 = newTerm(VAR, 0, portLoc(1, seq_58));
seq_58 = makePair(LAM, 0, SUB, seq_58);
Term k_54 = newTerm(VAR, 0, portLoc(1, seq_58));
seq_58 = makePair(LAM, 0, SUB, seq_58);
Term m_53 = newTerm(VAR, 0, portLoc(1, seq_58));
// allocate args at core: 1892
// call bmiGet at core: 1893
Term Rslt_60Args = makePair(APP, 0, shift_57, SUB);
Term Rslt_60 = newTerm(VAR, 0, portLoc(2, Rslt_60Args));
Rslt_60Args = makePair(APP, 0, hash_56, Rslt_60Args);
Rslt_60Args = makePair(APP, 0, def_55, Rslt_60Args);
Rslt_60Args = makePair(APP, 0, k_54, Rslt_60Args);
Rslt_60Args = makePair(APP, 0, m_53, Rslt_60Args);
#ifdef STRICT
pushRedex(Rslt_60Args, glblFn1049);
#else
swap(termLoc(Rslt_60), makePair(LAZ, 0, Rslt_60Args, glblFn1049));
#endif

// link args to body
swap(r_seq_58, Rslt_60);

Term Rslt_60V = get(termLoc(Rslt_60));
apps = take(portLoc(1, Rslt_60V));
lams = take(portLoc(2, Rslt_60V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_60), makePair(LAZ, 0, apps, lams));
pushRedex(args, seq_58);
return;
}
Term glblget_STAR_1195 = newRef(glblget_STAR_1194);
Term glbldissoc_STAR_1193;
void glbldissoc_STAR_1192(Term ref, Term args) {
Term apps, lams, V;
Location vLoc;
Term seq_66 = makePair(LAM, 0, SUB, NUL);
Location r_seq_66 = portLoc(2, seq_66);
Term shift_65 = newTerm(VAR, 0, portLoc(1, seq_66));
seq_66 = makePair(LAM, 0, SUB, seq_66);
Term hash_64 = newTerm(VAR, 0, portLoc(1, seq_66));
seq_66 = makePair(LAM, 0, SUB, seq_66);
Term k_63 = newTerm(VAR, 0, portLoc(1, seq_66));
seq_66 = makePair(LAM, 0, SUB, seq_66);
Term m_62 = newTerm(VAR, 0, portLoc(1, seq_66));
// allocate args at core: 1895
// call bmiDissoc at core: 1896
Term Rslt_68Args = makePair(APP, 0, shift_65, SUB);
Term Rslt_68 = newTerm(VAR, 0, portLoc(2, Rslt_68Args));
Rslt_68Args = makePair(APP, 0, hash_64, Rslt_68Args);
Rslt_68Args = makePair(APP, 0, k_63, Rslt_68Args);
Rslt_68Args = makePair(APP, 0, m_62, Rslt_68Args);
#ifdef STRICT
pushRedex(Rslt_68Args, glblFn1071);
#else
swap(termLoc(Rslt_68), makePair(LAZ, 0, Rslt_68Args, glblFn1071));
#endif

// link args to body
swap(r_seq_66, Rslt_68);

Term Rslt_68V = get(termLoc(Rslt_68));
apps = take(portLoc(1, Rslt_68V));
lams = take(portLoc(2, Rslt_68V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_68), makePair(LAZ, 0, apps, lams));
pushRedex(args, seq_66);
return;
}
Term glbldissoc_STAR_1193 = newRef(glbldissoc_STAR_1192);
Term glblcount1239;
void glblcount1238(Term ref, Term args) {
Term apps, lams, V;
Location vLoc;
Term seq_71 = makePair(LAM, 0, SUB, NUL);
Location r_seq_71 = portLoc(2, seq_71);
Term m_70 = newTerm(VAR, 0, portLoc(1, seq_71));
// allocate args at core: 1899
// call arrayNodeCount at core: 1900
Term Rslt_73Args = makePair(APP, 0, m_70, SUB);
Term Rslt_73 = newTerm(VAR, 0, portLoc(2, Rslt_73Args));
#ifdef STRICT
pushRedex(Rslt_73Args, glblFn758);
#else
swap(termLoc(Rslt_73), makePair(LAZ, 0, Rslt_73Args, glblFn758));
#endif

// link args to body
swap(r_seq_71, Rslt_73);

Term Rslt_73V = get(termLoc(Rslt_73));
apps = take(portLoc(1, Rslt_73V));
lams = take(portLoc(2, Rslt_73V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_73), makePair(LAZ, 0, apps, lams));
pushRedex(args, seq_71);
return;
}
Term glblcount1239 = newRef(glblcount1238);
Term glblvec1237;
void glblvec1236(Term ref, Term args) {
Term apps, lams, V;
Location vLoc;
Term seq_76 = makePair(LAM, 0, SUB, NUL);
Location r_seq_76 = portLoc(2, seq_76);
Term m_75 = newTerm(VAR, 0, portLoc(1, seq_76));
// allocate args at core: 1902
// call arrayNodeVec at core: 1903
Term Rslt_78Args = makePair(APP, 0, m_75, SUB);
Term Rslt_78 = newTerm(VAR, 0, portLoc(2, Rslt_78Args));
#ifdef STRICT
pushRedex(Rslt_78Args, glblFn940);
#else
swap(termLoc(Rslt_78), makePair(LAZ, 0, Rslt_78Args, glblFn940));
#endif

// link args to body
swap(r_seq_76, Rslt_78);

Term Rslt_78V = get(termLoc(Rslt_78));
apps = take(portLoc(1, Rslt_78V));
lams = take(portLoc(2, Rslt_78V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_78), makePair(LAZ, 0, apps, lams));
pushRedex(args, seq_76);
return;
}
Term glblvec1237 = newRef(glblvec1236);
Term glblhash_vec1235;
void glblhash_vec1234(Term ref, Term args) {
Term apps, lams, V;
Location vLoc;
Term seq_82 = makePair(LAM, 0, SUB, NUL);
Location r_seq_82 = portLoc(2, seq_82);
Term s_81 = newTerm(VAR, 0, portLoc(1, seq_82));
seq_82 = makePair(LAM, 0, SUB, seq_82);
Term m_80 = newTerm(VAR, 0, portLoc(1, seq_82));
// allocate args at core: 1905
// call arrayNodeHashSeq at core: 1906
Term Rslt_84Args = makePair(APP, 0, s_81, SUB);
Term Rslt_84 = newTerm(VAR, 0, portLoc(2, Rslt_84Args));
Rslt_84Args = makePair(APP, 0, m_80, Rslt_84Args);
#ifdef STRICT
pushRedex(Rslt_84Args, glblFn978);
#else
swap(termLoc(Rslt_84), makePair(LAZ, 0, Rslt_84Args, glblFn978));
#endif

// link args to body
swap(r_seq_82, Rslt_84);

Term Rslt_84V = get(termLoc(Rslt_84));
apps = take(portLoc(1, Rslt_84V));
lams = take(portLoc(2, Rslt_84V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_84), makePair(LAZ, 0, apps, lams));
pushRedex(args, seq_82);
return;
}
Term glblhash_vec1235 = newRef(glblhash_vec1234);
void glblProto121_1243(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(1909), args);
  args = makePair(APP, 0, glblStr199, args);
  interact(args, glblProto121);
}
}
void glblProto42_1245(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(1909), args);
  args = makePair(APP, 0, glblStr199, args);
  interact(args, glblProto42);
}
}
Term glblvals1233;
void glblvals1232(Term ref, Term args) {
Term apps, lams, V;
Location vLoc;
Term seq_87 = makePair(LAM, 0, SUB, NUL);
Location r_seq_87 = portLoc(2, seq_87);
Term m_86 = newTerm(VAR, 0, portLoc(1, seq_87));
// allocate args at core: 1908
Term glblVal175_26;
glblVal175 = dupeArg(glblVal175, &glblVal175_26, 0);

// call hash-vec at core: 1909
Term Rslt_89Args = makePair(APP, 0, glblVal175_26, SUB);
Term Rslt_89 = newTerm(VAR, 0, portLoc(2, Rslt_89Args));
Rslt_89Args = makePair(APP, 0, m_86, Rslt_89Args);
#ifdef STRICT
pushRedex(Rslt_89Args, newRef(glblProto121_1243));
#else
swap(termLoc(Rslt_89), makePair(LAZ, 0, Rslt_89Args, newRef(glblProto121_1243)));
#endif
// call map at core: 1909
Term Rslt_90Args = makePair(APP, 0, glblFn990, SUB);
Term Rslt_90 = newTerm(VAR, 0, portLoc(2, Rslt_90Args));
Rslt_90Args = makePair(APP, 0, Rslt_89, Rslt_90Args);
#ifdef STRICT
pushRedex(Rslt_90Args, newRef(glblProto42_1245));
#else
swap(termLoc(Rslt_90), makePair(LAZ, 0, Rslt_90Args, newRef(glblProto42_1245)));
#endif
// link args to body
swap(r_seq_87, Rslt_90);

Term Rslt_90V = get(termLoc(Rslt_90));
apps = take(portLoc(1, Rslt_90V));
lams = take(portLoc(2, Rslt_90V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_90), makePair(LAZ, 0, apps, lams));
pushRedex(args, seq_87);
return;
}
Term glblvals1233 = newRef(glblvals1232);
Term glblcopyAssoc1231;
void glblcopyAssoc1230(Term ref, Term args) {
Term apps, lams, V;
Location vLoc;
Term seq_97 = makePair(LAM, 0, SUB, NUL);
Location r_seq_97 = portLoc(2, seq_97);
Term shift_96 = newTerm(VAR, 0, portLoc(1, seq_97));
seq_97 = makePair(LAM, 0, SUB, seq_97);
Term hash_95 = newTerm(VAR, 0, portLoc(1, seq_97));
seq_97 = makePair(LAM, 0, SUB, seq_97);
Term v_94 = newTerm(VAR, 0, portLoc(1, seq_97));
seq_97 = makePair(LAM, 0, SUB, seq_97);
Term k_93 = newTerm(VAR, 0, portLoc(1, seq_97));
seq_97 = makePair(LAM, 0, SUB, seq_97);
Term m_92 = newTerm(VAR, 0, portLoc(1, seq_97));
// allocate args at core: 1911
// call arrayNodeCopyAssoc at core: 1912
Term Rslt_99Args = makePair(APP, 0, shift_96, SUB);
Term Rslt_99 = newTerm(VAR, 0, portLoc(2, Rslt_99Args));
Rslt_99Args = makePair(APP, 0, hash_95, Rslt_99Args);
Rslt_99Args = makePair(APP, 0, v_94, Rslt_99Args);
Rslt_99Args = makePair(APP, 0, k_93, Rslt_99Args);
Rslt_99Args = makePair(APP, 0, m_92, Rslt_99Args);
#ifdef STRICT
pushRedex(Rslt_99Args, glblFn1027);
#else
swap(termLoc(Rslt_99), makePair(LAZ, 0, Rslt_99Args, glblFn1027));
#endif

// link args to body
swap(r_seq_97, Rslt_99);

Term Rslt_99V = get(termLoc(Rslt_99));
apps = take(portLoc(1, Rslt_99V));
lams = take(portLoc(2, Rslt_99V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_99), makePair(LAZ, 0, apps, lams));
pushRedex(args, seq_97);
return;
}
Term glblcopyAssoc1231 = newRef(glblcopyAssoc1230);
void glblProto20_1249(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(1916), args);
  args = makePair(APP, 0, glblStr199, args);
  interact(args, glblProto20);
}
}
void glblProto34_1251(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(1916), args);
  args = makePair(APP, 0, glblStr199, args);
  interact(args, glblProto34);
}
}
Term glblassoc_STAR_1229;
void glblassoc_STAR_1228(Term ref, Term args) {
Term apps, lams, V;
Location vLoc;
Term seq_106 = makePair(LAM, 0, SUB, NUL);
Location r_seq_106 = portLoc(2, seq_106);
Term shift_105 = newTerm(VAR, 0, portLoc(1, seq_106));
seq_106 = makePair(LAM, 0, SUB, seq_106);
Term hash_104 = newTerm(VAR, 0, portLoc(1, seq_106));
seq_106 = makePair(LAM, 0, SUB, seq_106);
Term v_103 = newTerm(VAR, 0, portLoc(1, seq_106));
seq_106 = makePair(LAM, 0, SUB, seq_106);
Term k_102 = newTerm(VAR, 0, portLoc(1, seq_106));
seq_106 = makePair(LAM, 0, SUB, seq_106);
Term m_101 = newTerm(VAR, 0, portLoc(1, seq_106));
// allocate args at core: 1914

Term m_101_2;
m_101 = dupeArg(m_101, &m_101_2, 222);

Term m_101_1;
m_101 = dupeArg(m_101, &m_101_1, 223);

Term k_102_1;
k_102 = dupeArg(k_102, &k_102_1, 224);

Term v_103_1;
v_103 = dupeArg(v_103, &v_103_1, 225);

Term hash_104_1;
hash_104 = dupeArg(hash_104, &hash_104_1, 226);

Term shift_105_1;
shift_105 = dupeArg(shift_105, &shift_105_1, 227);
// call refs-count at core: 1916
Term Rslt_108Args = makePair(APP, 0, m_101, SUB);
Term Rslt_108 = newTerm(VAR, 0, portLoc(2, Rslt_108Args));
#ifdef STRICT
pushRedex(Rslt_108Args, glblFn1169);
#else
swap(termLoc(Rslt_108), makePair(LAZ, 0, Rslt_108Args, glblFn1169));
#endif

// call = at core: 1916
Term Rslt_109Args = makePair(APP, 0, Rslt_108, SUB);
Term Rslt_109 = newTerm(VAR, 0, portLoc(2, Rslt_109Args));
Rslt_109Args = makePair(APP, 0, newI60(3), Rslt_109Args);
#ifdef STRICT
pushRedex(Rslt_109Args, newRef(glblProto20_1249));
#else
swap(termLoc(Rslt_109), makePair(LAZ, 0, Rslt_109Args, newRef(glblProto20_1249)));
#endif
// call arrayNodeMutateAssoc at core: 1917
Term Rslt_110Args = makePair(APP, 0, shift_105, SUB);
Term Rslt_110 = newTerm(VAR, 0, portLoc(2, Rslt_110Args));
Rslt_110Args = makePair(APP, 0, hash_104, Rslt_110Args);
Rslt_110Args = makePair(APP, 0, v_103, Rslt_110Args);
Rslt_110Args = makePair(APP, 0, k_102, Rslt_110Args);
Rslt_110Args = makePair(APP, 0, m_101_1, Rslt_110Args);
#ifdef STRICT
pushRedex(Rslt_110Args, glblFn1038);
#else
swap(termLoc(Rslt_110), makePair(LAZ, 0, Rslt_110Args, glblFn1038));
#endif

// call arrayNodeCopyAssoc at core: 1918
Term Rslt_111Args = makePair(APP, 0, shift_105_1, SUB);
Term Rslt_111 = newTerm(VAR, 0, portLoc(2, Rslt_111Args));
Rslt_111Args = makePair(APP, 0, hash_104_1, Rslt_111Args);
Rslt_111Args = makePair(APP, 0, v_103_1, Rslt_111Args);
Rslt_111Args = makePair(APP, 0, k_102_1, Rslt_111Args);
Rslt_111Args = makePair(APP, 0, m_101_2, Rslt_111Args);
#ifdef STRICT
pushRedex(Rslt_111Args, glblFn1027);
#else
swap(termLoc(Rslt_111), makePair(LAZ, 0, Rslt_111Args, glblFn1027));
#endif

// call cond at core: 1916
Term Rslt_112Args = makePair(APP, 0, Rslt_111, SUB);
Term Rslt_112 = newTerm(VAR, 0, portLoc(2, Rslt_112Args));
Rslt_112Args = makePair(APP, 0, Rslt_110, Rslt_112Args);
Rslt_112Args = makePair(APP, 0, Rslt_109, Rslt_112Args);
#ifdef STRICT
pushRedex(Rslt_112Args, newRef(glblProto34_1251));
#else
swap(termLoc(Rslt_112), makePair(LAZ, 0, Rslt_112Args, newRef(glblProto34_1251)));
#endif
// link args to body
swap(r_seq_106, Rslt_112);

Term Rslt_112V = get(termLoc(Rslt_112));
apps = take(portLoc(1, Rslt_112V));
lams = take(portLoc(2, Rslt_112V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_112), makePair(LAZ, 0, apps, lams));
pushRedex(args, seq_106);
return;
}
Term glblassoc_STAR_1229 = newRef(glblassoc_STAR_1228);
Term glblget_STAR_1227;
void glblget_STAR_1226(Term ref, Term args) {
Term apps, lams, V;
Location vLoc;
Term seq_119 = makePair(LAM, 0, SUB, NUL);
Location r_seq_119 = portLoc(2, seq_119);
Term shift_118 = newTerm(VAR, 0, portLoc(1, seq_119));
seq_119 = makePair(LAM, 0, SUB, seq_119);
Term hash_117 = newTerm(VAR, 0, portLoc(1, seq_119));
seq_119 = makePair(LAM, 0, SUB, seq_119);
Term def_116 = newTerm(VAR, 0, portLoc(1, seq_119));
seq_119 = makePair(LAM, 0, SUB, seq_119);
Term k_115 = newTerm(VAR, 0, portLoc(1, seq_119));
seq_119 = makePair(LAM, 0, SUB, seq_119);
Term m_114 = newTerm(VAR, 0, portLoc(1, seq_119));
// allocate args at core: 1920
// call arrayNodeGet at core: 1921
Term Rslt_121Args = makePair(APP, 0, shift_118, SUB);
Term Rslt_121 = newTerm(VAR, 0, portLoc(2, Rslt_121Args));
Rslt_121Args = makePair(APP, 0, hash_117, Rslt_121Args);
Rslt_121Args = makePair(APP, 0, def_116, Rslt_121Args);
Rslt_121Args = makePair(APP, 0, k_115, Rslt_121Args);
Rslt_121Args = makePair(APP, 0, m_114, Rslt_121Args);
#ifdef STRICT
pushRedex(Rslt_121Args, glblFn1062);
#else
swap(termLoc(Rslt_121), makePair(LAZ, 0, Rslt_121Args, glblFn1062));
#endif

// link args to body
swap(r_seq_119, Rslt_121);

Term Rslt_121V = get(termLoc(Rslt_121));
apps = take(portLoc(1, Rslt_121V));
lams = take(portLoc(2, Rslt_121V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_121), makePair(LAZ, 0, apps, lams));
pushRedex(args, seq_119);
return;
}
Term glblget_STAR_1227 = newRef(glblget_STAR_1226);
Term glbldissoc_STAR_1225;
void glbldissoc_STAR_1224(Term ref, Term args) {
Term apps, lams, V;
Location vLoc;
Term seq_127 = makePair(LAM, 0, SUB, NUL);
Location r_seq_127 = portLoc(2, seq_127);
Term shift_126 = newTerm(VAR, 0, portLoc(1, seq_127));
seq_127 = makePair(LAM, 0, SUB, seq_127);
Term hash_125 = newTerm(VAR, 0, portLoc(1, seq_127));
seq_127 = makePair(LAM, 0, SUB, seq_127);
Term k_124 = newTerm(VAR, 0, portLoc(1, seq_127));
seq_127 = makePair(LAM, 0, SUB, seq_127);
Term m_123 = newTerm(VAR, 0, portLoc(1, seq_127));
// allocate args at core: 1923
// call arrayNodeDissoc at core: 1924
Term Rslt_129Args = makePair(APP, 0, shift_126, SUB);
Term Rslt_129 = newTerm(VAR, 0, portLoc(2, Rslt_129Args));
Rslt_129Args = makePair(APP, 0, hash_125, Rslt_129Args);
Rslt_129Args = makePair(APP, 0, k_124, Rslt_129Args);
Rslt_129Args = makePair(APP, 0, m_123, Rslt_129Args);
#ifdef STRICT
pushRedex(Rslt_129Args, glblFn1096);
#else
swap(termLoc(Rslt_129), makePair(LAZ, 0, Rslt_129Args, glblFn1096));
#endif

// link args to body
swap(r_seq_127, Rslt_129);

Term Rslt_129V = get(termLoc(Rslt_129));
apps = take(portLoc(1, Rslt_129V));
lams = take(portLoc(2, Rslt_129V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_129), makePair(LAZ, 0, apps, lams));
pushRedex(args, seq_127);
return;
}
Term glbldissoc_STAR_1225 = newRef(glbldissoc_STAR_1224);
Term glblcount1271;
void glblcount1270(Term ref, Term args) {
Term apps, lams, V;
Location vLoc;
Term seq_132 = makePair(LAM, 0, SUB, NUL);
Location r_seq_132 = portLoc(2, seq_132);
Term m_131 = newTerm(VAR, 0, portLoc(1, seq_132));
// allocate args at core: 1928
// call collisionCount at core: 1929
Term Rslt_134Args = makePair(APP, 0, m_131, SUB);
Term Rslt_134 = newTerm(VAR, 0, portLoc(2, Rslt_134Args));
#ifdef STRICT
pushRedex(Rslt_134Args, glblFn880);
#else
swap(termLoc(Rslt_134), makePair(LAZ, 0, Rslt_134Args, glblFn880));
#endif

// link args to body
swap(r_seq_132, Rslt_134);

Term Rslt_134V = get(termLoc(Rslt_134));
apps = take(portLoc(1, Rslt_134V));
lams = take(portLoc(2, Rslt_134V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_134), makePair(LAZ, 0, apps, lams));
pushRedex(args, seq_132);
return;
}
Term glblcount1271 = newRef(glblcount1270);
Term glblvec1269;
void glblvec1268(Term ref, Term args) {
Term apps, lams, V;
Location vLoc;
Term seq_137 = makePair(LAM, 0, SUB, NUL);
Location r_seq_137 = portLoc(2, seq_137);
Term m_136 = newTerm(VAR, 0, portLoc(1, seq_137));
// allocate args at core: 1931
Term glblVal175_27;
glblVal175 = dupeArg(glblVal175, &glblVal175_27, 0);

// call collisionVec at core: 1932
Term Rslt_139Args = makePair(APP, 0, glblVal175_27, SUB);
Term Rslt_139 = newTerm(VAR, 0, portLoc(2, Rslt_139Args));
Rslt_139Args = makePair(APP, 0, m_136, Rslt_139Args);
#ifdef STRICT
pushRedex(Rslt_139Args, glblFn902);
#else
swap(termLoc(Rslt_139), makePair(LAZ, 0, Rslt_139Args, glblFn902));
#endif

// link args to body
swap(r_seq_137, Rslt_139);

Term Rslt_139V = get(termLoc(Rslt_139));
apps = take(portLoc(1, Rslt_139V));
lams = take(portLoc(2, Rslt_139V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_139), makePair(LAZ, 0, apps, lams));
pushRedex(args, seq_137);
return;
}
Term glblvec1269 = newRef(glblvec1268);
Term glblhash_vec1267;
void glblhash_vec1266(Term ref, Term args) {
Term apps, lams, V;
Location vLoc;
Term seq_143 = makePair(LAM, 0, SUB, NUL);
Location r_seq_143 = portLoc(2, seq_143);
Term s_142 = newTerm(VAR, 0, portLoc(1, seq_143));
seq_143 = makePair(LAM, 0, SUB, seq_143);
Term m_141 = newTerm(VAR, 0, portLoc(1, seq_143));
// allocate args at core: 1934
// call collisionVec at core: 1939
Term Rslt_145Args = makePair(APP, 0, s_142, SUB);
Term Rslt_145 = newTerm(VAR, 0, portLoc(2, Rslt_145Args));
Rslt_145Args = makePair(APP, 0, m_141, Rslt_145Args);
#ifdef STRICT
pushRedex(Rslt_145Args, glblFn902);
#else
swap(termLoc(Rslt_145), makePair(LAZ, 0, Rslt_145Args, glblFn902));
#endif

// link args to body
swap(r_seq_143, Rslt_145);

Term Rslt_145V = get(termLoc(Rslt_145));
apps = take(portLoc(1, Rslt_145V));
lams = take(portLoc(2, Rslt_145V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_145), makePair(LAZ, 0, apps, lams));
pushRedex(args, seq_143);
return;
}
Term glblhash_vec1267 = newRef(glblhash_vec1266);
void glblProto121_1275(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(1942), args);
  args = makePair(APP, 0, glblStr199, args);
  interact(args, glblProto121);
}
}
void glblProto42_1277(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(1942), args);
  args = makePair(APP, 0, glblStr199, args);
  interact(args, glblProto42);
}
}
Term glblvals1265;
void glblvals1264(Term ref, Term args) {
Term apps, lams, V;
Location vLoc;
Term seq_148 = makePair(LAM, 0, SUB, NUL);
Location r_seq_148 = portLoc(2, seq_148);
Term m_147 = newTerm(VAR, 0, portLoc(1, seq_148));
// allocate args at core: 1941
Term glblVal175_28;
glblVal175 = dupeArg(glblVal175, &glblVal175_28, 0);

// call hash-vec at core: 1942
Term Rslt_150Args = makePair(APP, 0, glblVal175_28, SUB);
Term Rslt_150 = newTerm(VAR, 0, portLoc(2, Rslt_150Args));
Rslt_150Args = makePair(APP, 0, m_147, Rslt_150Args);
#ifdef STRICT
pushRedex(Rslt_150Args, newRef(glblProto121_1275));
#else
swap(termLoc(Rslt_150), makePair(LAZ, 0, Rslt_150Args, newRef(glblProto121_1275)));
#endif
// call map at core: 1942
Term Rslt_151Args = makePair(APP, 0, glblFn990, SUB);
Term Rslt_151 = newTerm(VAR, 0, portLoc(2, Rslt_151Args));
Rslt_151Args = makePair(APP, 0, Rslt_150, Rslt_151Args);
#ifdef STRICT
pushRedex(Rslt_151Args, newRef(glblProto42_1277));
#else
swap(termLoc(Rslt_151), makePair(LAZ, 0, Rslt_151Args, newRef(glblProto42_1277)));
#endif
// link args to body
swap(r_seq_148, Rslt_151);

Term Rslt_151V = get(termLoc(Rslt_151));
apps = take(portLoc(1, Rslt_151V));
lams = take(portLoc(2, Rslt_151V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_151), makePair(LAZ, 0, apps, lams));
pushRedex(args, seq_148);
return;
}
Term glblvals1265 = newRef(glblvals1264);
Term glblcopyAssoc1263;
void glblcopyAssoc1262(Term ref, Term args) {
Term apps, lams, V;
Location vLoc;
Term seq_158 = makePair(LAM, 0, SUB, NUL);
Location r_seq_158 = portLoc(2, seq_158);
Term shift_157 = newTerm(VAR, 0, portLoc(1, seq_158));
seq_158 = makePair(LAM, 0, SUB, seq_158);
Term hash_156 = newTerm(VAR, 0, portLoc(1, seq_158));
seq_158 = makePair(LAM, 0, SUB, seq_158);
Term v_155 = newTerm(VAR, 0, portLoc(1, seq_158));
seq_158 = makePair(LAM, 0, SUB, seq_158);
Term k_154 = newTerm(VAR, 0, portLoc(1, seq_158));
seq_158 = makePair(LAM, 0, SUB, seq_158);
Term m_153 = newTerm(VAR, 0, portLoc(1, seq_158));
// allocate args at core: 1944
// call collisionAssoc at core: 1945
Term Rslt_160Args = makePair(APP, 0, shift_157, SUB);
Term Rslt_160 = newTerm(VAR, 0, portLoc(2, Rslt_160Args));
Rslt_160Args = makePair(APP, 0, hash_156, Rslt_160Args);
Rslt_160Args = makePair(APP, 0, v_155, Rslt_160Args);
Rslt_160Args = makePair(APP, 0, k_154, Rslt_160Args);
Rslt_160Args = makePair(APP, 0, m_153, Rslt_160Args);
#ifdef STRICT
pushRedex(Rslt_160Args, glblFn1141);
#else
swap(termLoc(Rslt_160), makePair(LAZ, 0, Rslt_160Args, glblFn1141));
#endif

// link args to body
swap(r_seq_158, Rslt_160);

Term Rslt_160V = get(termLoc(Rslt_160));
apps = take(portLoc(1, Rslt_160V));
lams = take(portLoc(2, Rslt_160V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_160), makePair(LAZ, 0, apps, lams));
pushRedex(args, seq_158);
return;
}
Term glblcopyAssoc1263 = newRef(glblcopyAssoc1262);
Term glblassoc_STAR_1261;
void glblassoc_STAR_1260(Term ref, Term args) {
Term apps, lams, V;
Location vLoc;
Term seq_167 = makePair(LAM, 0, SUB, NUL);
Location r_seq_167 = portLoc(2, seq_167);
Term shift_166 = newTerm(VAR, 0, portLoc(1, seq_167));
seq_167 = makePair(LAM, 0, SUB, seq_167);
Term hash_165 = newTerm(VAR, 0, portLoc(1, seq_167));
seq_167 = makePair(LAM, 0, SUB, seq_167);
Term v_164 = newTerm(VAR, 0, portLoc(1, seq_167));
seq_167 = makePair(LAM, 0, SUB, seq_167);
Term k_163 = newTerm(VAR, 0, portLoc(1, seq_167));
seq_167 = makePair(LAM, 0, SUB, seq_167);
Term m_162 = newTerm(VAR, 0, portLoc(1, seq_167));
// allocate args at core: 1947
// call collisionAssoc at core: 1955
Term Rslt_169Args = makePair(APP, 0, shift_166, SUB);
Term Rslt_169 = newTerm(VAR, 0, portLoc(2, Rslt_169Args));
Rslt_169Args = makePair(APP, 0, hash_165, Rslt_169Args);
Rslt_169Args = makePair(APP, 0, v_164, Rslt_169Args);
Rslt_169Args = makePair(APP, 0, k_163, Rslt_169Args);
Rslt_169Args = makePair(APP, 0, m_162, Rslt_169Args);
#ifdef STRICT
pushRedex(Rslt_169Args, glblFn1141);
#else
swap(termLoc(Rslt_169), makePair(LAZ, 0, Rslt_169Args, glblFn1141));
#endif

// link args to body
swap(r_seq_167, Rslt_169);

Term Rslt_169V = get(termLoc(Rslt_169));
apps = take(portLoc(1, Rslt_169V));
lams = take(portLoc(2, Rslt_169V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_169), makePair(LAZ, 0, apps, lams));
pushRedex(args, seq_167);
return;
}
Term glblassoc_STAR_1261 = newRef(glblassoc_STAR_1260);
Term glblget_STAR_1259;
void glblget_STAR_1258(Term ref, Term args) {
Term apps, lams, V;
Location vLoc;
Term seq_176 = makePair(LAM, 0, SUB, NUL);
Location r_seq_176 = portLoc(2, seq_176);
Term shift_175 = newTerm(VAR, 0, portLoc(1, seq_176));
seq_176 = makePair(LAM, 0, SUB, seq_176);
Term hash_174 = newTerm(VAR, 0, portLoc(1, seq_176));
seq_176 = makePair(LAM, 0, SUB, seq_176);
Term def_173 = newTerm(VAR, 0, portLoc(1, seq_176));
seq_176 = makePair(LAM, 0, SUB, seq_176);
Term k_172 = newTerm(VAR, 0, portLoc(1, seq_176));
seq_176 = makePair(LAM, 0, SUB, seq_176);
Term m_171 = newTerm(VAR, 0, portLoc(1, seq_176));
// allocate args at core: 1957
// call collisionGet at core: 1958
Term Rslt_178Args = makePair(APP, 0, shift_175, SUB);
Term Rslt_178 = newTerm(VAR, 0, portLoc(2, Rslt_178Args));
Rslt_178Args = makePair(APP, 0, hash_174, Rslt_178Args);
Rslt_178Args = makePair(APP, 0, def_173, Rslt_178Args);
Rslt_178Args = makePair(APP, 0, k_172, Rslt_178Args);
Rslt_178Args = makePair(APP, 0, m_171, Rslt_178Args);
#ifdef STRICT
pushRedex(Rslt_178Args, glblFn1116);
#else
swap(termLoc(Rslt_178), makePair(LAZ, 0, Rslt_178Args, glblFn1116));
#endif

// link args to body
swap(r_seq_176, Rslt_178);

Term Rslt_178V = get(termLoc(Rslt_178));
apps = take(portLoc(1, Rslt_178V));
lams = take(portLoc(2, Rslt_178V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_178), makePair(LAZ, 0, apps, lams));
pushRedex(args, seq_176);
return;
}
Term glblget_STAR_1259 = newRef(glblget_STAR_1258);
Term glbldissoc_STAR_1257;
void glbldissoc_STAR_1256(Term ref, Term args) {
Term apps, lams, V;
Location vLoc;
Term seq_184 = makePair(LAM, 0, SUB, NUL);
Location r_seq_184 = portLoc(2, seq_184);
Term shift_183 = newTerm(VAR, 0, portLoc(1, seq_184));
seq_184 = makePair(LAM, 0, SUB, seq_184);
Term hash_182 = newTerm(VAR, 0, portLoc(1, seq_184));
seq_184 = makePair(LAM, 0, SUB, seq_184);
Term k_181 = newTerm(VAR, 0, portLoc(1, seq_184));
seq_184 = makePair(LAM, 0, SUB, seq_184);
Term m_180 = newTerm(VAR, 0, portLoc(1, seq_184));
// allocate args at core: 1960
// call collisionDissoc at core: 1961
Term Rslt_186Args = makePair(APP, 0, shift_183, SUB);
Term Rslt_186 = newTerm(VAR, 0, portLoc(2, Rslt_186Args));
Rslt_186Args = makePair(APP, 0, hash_182, Rslt_186Args);
Rslt_186Args = makePair(APP, 0, k_181, Rslt_186Args);
Rslt_186Args = makePair(APP, 0, m_180, Rslt_186Args);
#ifdef STRICT
pushRedex(Rslt_186Args, glblFn1167);
#else
swap(termLoc(Rslt_186), makePair(LAZ, 0, Rslt_186Args, glblFn1167));
#endif

// link args to body
swap(r_seq_184, Rslt_186);

Term Rslt_186V = get(termLoc(Rslt_186));
apps = take(portLoc(1, Rslt_186V));
lams = take(portLoc(2, Rslt_186V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_186), makePair(LAZ, 0, apps, lams));
pushRedex(args, seq_184);
return;
}
Term glbldissoc_STAR_1257 = newRef(glbldissoc_STAR_1256);
void glblProto64_1318(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(1965), args);
  args = makePair(APP, 0, glblStr199, args);
  interact(args, glblProto64);
}
}
void glblProto20_1320(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(1965), args);
  args = makePair(APP, 0, glblStr199, args);
  interact(args, glblProto20);
}
}
void glblProto28_1324(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(1965), args);
  args = makePair(APP, 0, glblStr199, args);
  interact(args, glblProto28);
}
}
Term glblempty_QM_1309;
void glblempty_QM_1308(Term ref, Term args) {
Term apps, lams, V;
Location vLoc;
Term seq_189 = makePair(LAM, 0, SUB, NUL);
Location r_seq_189 = portLoc(2, seq_189);
Term m_188 = newTerm(VAR, 0, portLoc(1, seq_189));
// allocate args at core: 1964

Term m_188_1;
m_188 = dupeArg(m_188, &m_188_1, 228);
Term glblVal25_15;
glblVal25 = dupeArg(glblVal25, &glblVal25_15, 0);

// call count at core: 1965
Term Rslt_191Args = makePair(APP, 0, m_188, SUB);
Term Rslt_191 = newTerm(VAR, 0, portLoc(2, Rslt_191Args));
#ifdef STRICT
pushRedex(Rslt_191Args, newRef(glblProto64_1318));
#else
swap(termLoc(Rslt_191), makePair(LAZ, 0, Rslt_191Args, newRef(glblProto64_1318)));
#endif
// call = at core: 1965
Term Rslt_192Args = makePair(APP, 0, Rslt_191, SUB);
Term Rslt_192 = newTerm(VAR, 0, portLoc(2, Rslt_192Args));
Rslt_192Args = makePair(APP, 0, newI60(0), Rslt_192Args);
#ifdef STRICT
pushRedex(Rslt_192Args, newRef(glblProto20_1320));
#else
swap(termLoc(Rslt_192), makePair(LAZ, 0, Rslt_192Args, newRef(glblProto20_1320)));
#endif
// call Some at core: 1966
Term Rslt_193Args = makePair(APP, 0, m_188_1, SUB);
Term Rslt_193 = newTerm(VAR, 0, portLoc(2, Rslt_193Args));
#ifdef STRICT
pushRedex(Rslt_193Args, glblVal25_15);
#else
swap(termLoc(Rslt_193), makePair(LAZ, 0, Rslt_193Args, glblVal25_15));
#endif

// call and at core: 1965
Term Rslt_194Args = makePair(APP, 0, Rslt_193, SUB);
Term Rslt_194 = newTerm(VAR, 0, portLoc(2, Rslt_194Args));
Rslt_194Args = makePair(APP, 0, Rslt_192, Rslt_194Args);
#ifdef STRICT
pushRedex(Rslt_194Args, newRef(glblProto28_1324));
#else
swap(termLoc(Rslt_194), makePair(LAZ, 0, Rslt_194Args, newRef(glblProto28_1324)));
#endif
// link args to body
swap(r_seq_189, Rslt_194);

Term Rslt_194V = get(termLoc(Rslt_194));
apps = take(portLoc(1, Rslt_194V));
lams = take(portLoc(2, Rslt_194V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_194), makePair(LAZ, 0, apps, lams));
pushRedex(args, seq_189);
return;
}
Term glblempty_QM_1309 = newRef(glblempty_QM_1308);
void glblProto64_1331(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(1965), args);
  args = makePair(APP, 0, glblStr199, args);
  interact(args, glblProto64);
}
}
void glblProto20_1334(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(1965), args);
  args = makePair(APP, 0, glblStr199, args);
  interact(args, glblProto20);
}
}
void glblProto28_1336(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(1965), args);
  args = makePair(APP, 0, glblStr199, args);
  interact(args, glblProto28);
}
}
Term glblempty_QM_1311;
void glblempty_QM_1310(Term ref, Term args) {
Term apps, lams, V;
Location vLoc;
Term seq_197 = makePair(LAM, 0, SUB, NUL);
Location r_seq_197 = portLoc(2, seq_197);
Term m_196 = newTerm(VAR, 0, portLoc(1, seq_197));
// allocate args at core: 1964

Term m_196_1;
m_196 = dupeArg(m_196, &m_196_1, 229);
Term glblVal25_16;
glblVal25 = dupeArg(glblVal25, &glblVal25_16, 0);

// call count at core: 1965
Term Rslt_199Args = makePair(APP, 0, m_196, SUB);
Term Rslt_199 = newTerm(VAR, 0, portLoc(2, Rslt_199Args));
#ifdef STRICT
pushRedex(Rslt_199Args, newRef(glblProto64_1331));
#else
swap(termLoc(Rslt_199), makePair(LAZ, 0, Rslt_199Args, newRef(glblProto64_1331)));
#endif
// call = at core: 1965
Term Rslt_200Args = makePair(APP, 0, Rslt_199, SUB);
Term Rslt_200 = newTerm(VAR, 0, portLoc(2, Rslt_200Args));
Rslt_200Args = makePair(APP, 0, newI60(0), Rslt_200Args);
#ifdef STRICT
pushRedex(Rslt_200Args, newRef(glblProto20_1334));
#else
swap(termLoc(Rslt_200), makePair(LAZ, 0, Rslt_200Args, newRef(glblProto20_1334)));
#endif
// call Some at core: 1966
Term Rslt_201Args = makePair(APP, 0, m_196_1, SUB);
Term Rslt_201 = newTerm(VAR, 0, portLoc(2, Rslt_201Args));
#ifdef STRICT
pushRedex(Rslt_201Args, glblVal25_16);
#else
swap(termLoc(Rslt_201), makePair(LAZ, 0, Rslt_201Args, glblVal25_16));
#endif

// call and at core: 1965
Term Rslt_202Args = makePair(APP, 0, Rslt_201, SUB);
Term Rslt_202 = newTerm(VAR, 0, portLoc(2, Rslt_202Args));
Rslt_202Args = makePair(APP, 0, Rslt_200, Rslt_202Args);
#ifdef STRICT
pushRedex(Rslt_202Args, newRef(glblProto28_1336));
#else
swap(termLoc(Rslt_202), makePair(LAZ, 0, Rslt_202Args, newRef(glblProto28_1336)));
#endif
// link args to body
swap(r_seq_197, Rslt_202);

Term Rslt_202V = get(termLoc(Rslt_202));
apps = take(portLoc(1, Rslt_202V));
lams = take(portLoc(2, Rslt_202V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_202), makePair(LAZ, 0, apps, lams));
pushRedex(args, seq_197);
return;
}
Term glblempty_QM_1311 = newRef(glblempty_QM_1310);
void glblProto64_1339(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(1965), args);
  args = makePair(APP, 0, glblStr199, args);
  interact(args, glblProto64);
}
}
void glblProto20_1341(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(1965), args);
  args = makePair(APP, 0, glblStr199, args);
  interact(args, glblProto20);
}
}
void glblProto28_1343(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(1965), args);
  args = makePair(APP, 0, glblStr199, args);
  interact(args, glblProto28);
}
}
Term glblempty_QM_1313;
void glblempty_QM_1312(Term ref, Term args) {
Term apps, lams, V;
Location vLoc;
Term seq_205 = makePair(LAM, 0, SUB, NUL);
Location r_seq_205 = portLoc(2, seq_205);
Term m_204 = newTerm(VAR, 0, portLoc(1, seq_205));
// allocate args at core: 1964

Term m_204_1;
m_204 = dupeArg(m_204, &m_204_1, 230);
Term glblVal25_17;
glblVal25 = dupeArg(glblVal25, &glblVal25_17, 0);

// call count at core: 1965
Term Rslt_207Args = makePair(APP, 0, m_204, SUB);
Term Rslt_207 = newTerm(VAR, 0, portLoc(2, Rslt_207Args));
#ifdef STRICT
pushRedex(Rslt_207Args, newRef(glblProto64_1339));
#else
swap(termLoc(Rslt_207), makePair(LAZ, 0, Rslt_207Args, newRef(glblProto64_1339)));
#endif
// call = at core: 1965
Term Rslt_208Args = makePair(APP, 0, Rslt_207, SUB);
Term Rslt_208 = newTerm(VAR, 0, portLoc(2, Rslt_208Args));
Rslt_208Args = makePair(APP, 0, newI60(0), Rslt_208Args);
#ifdef STRICT
pushRedex(Rslt_208Args, newRef(glblProto20_1341));
#else
swap(termLoc(Rslt_208), makePair(LAZ, 0, Rslt_208Args, newRef(glblProto20_1341)));
#endif
// call Some at core: 1966
Term Rslt_209Args = makePair(APP, 0, m_204_1, SUB);
Term Rslt_209 = newTerm(VAR, 0, portLoc(2, Rslt_209Args));
#ifdef STRICT
pushRedex(Rslt_209Args, glblVal25_17);
#else
swap(termLoc(Rslt_209), makePair(LAZ, 0, Rslt_209Args, glblVal25_17));
#endif

// call and at core: 1965
Term Rslt_210Args = makePair(APP, 0, Rslt_209, SUB);
Term Rslt_210 = newTerm(VAR, 0, portLoc(2, Rslt_210Args));
Rslt_210Args = makePair(APP, 0, Rslt_208, Rslt_210Args);
#ifdef STRICT
pushRedex(Rslt_210Args, newRef(glblProto28_1343));
#else
swap(termLoc(Rslt_210), makePair(LAZ, 0, Rslt_210Args, newRef(glblProto28_1343)));
#endif
// link args to body
swap(r_seq_205, Rslt_210);

Term Rslt_210V = get(termLoc(Rslt_210));
apps = take(portLoc(1, Rslt_210V));
lams = take(portLoc(2, Rslt_210V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_210), makePair(LAZ, 0, apps, lams));
pushRedex(args, seq_205);
return;
}
Term glblempty_QM_1313 = newRef(glblempty_QM_1312);
void glblProto64_1346(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(1971), args);
  args = makePair(APP, 0, glblStr199, args);
  interact(args, glblProto64);
}
}
void glblProto64_1348(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(1971), args);
  args = makePair(APP, 0, glblStr199, args);
  interact(args, glblProto64);
}
}
void glblProto20_1350(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(1971), args);
  args = makePair(APP, 0, glblStr199, args);
  interact(args, glblProto20);
}
}
void glblProto121_1352(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(1972), args);
  args = makePair(APP, 0, glblStr199, args);
  interact(args, glblProto121);
}
}
void glblProto98_1354(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(1975), args);
  args = makePair(APP, 0, glblStr199, args);
  interact(args, glblProto98);
}
}
void glblProto98_1356(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(1977), args);
  args = makePair(APP, 0, glblStr199, args);
  interact(args, glblProto98);
}
}
void glblProto119_1359(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(1979), args);
  args = makePair(APP, 0, glblStr199, args);
  interact(args, glblProto119);
}
}
void glblProto20_1364(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(1981), args);
  args = makePair(APP, 0, glblStr199, args);
  interact(args, glblProto20);
}
}
void glblProto46_1366(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(1979), args);
  args = makePair(APP, 0, glblStr199, args);
  interact(args, glblProto46);
}
}
void glblProto46_1370(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(1977), args);
  args = makePair(APP, 0, glblStr199, args);
  interact(args, glblProto46);
}
}
void glblProto46_1374(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(1975), args);
  args = makePair(APP, 0, glblStr199, args);
  interact(args, glblProto46);
}
}
void glblProto28_1377(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(1974), args);
  args = makePair(APP, 0, glblStr199, args);
  interact(args, glblProto28);
}
}
void glblProto72_1385(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(1972), args);
  args = makePair(APP, 0, glblStr199, args);
  interact(args, glblProto72);
}
}
void glblProto28_1387(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(1971), args);
  args = makePair(APP, 0, glblStr199, args);
  interact(args, glblProto28);
}
}
Term glbl_EQ_1303;
void glbl_EQ_1302(Term ref, Term args) {
Term apps, lams, V;
Location vLoc;
Term seq_214 = makePair(LAM, 0, SUB, NUL);
Location r_seq_214 = portLoc(2, seq_214);
Term m2_213 = newTerm(VAR, 0, portLoc(1, seq_214));
seq_214 = makePair(LAM, 0, SUB, seq_214);
Term m1_212 = newTerm(VAR, 0, portLoc(1, seq_214));
// allocate args at core: 1968

Term m1_212_1;
m1_212 = dupeArg(m1_212, &m1_212_1, 231);

Term m2_213_1;
m2_213 = dupeArg(m2_213, &m2_213_1, 232);
Term seq_224 = makePair(LAM, 0, SUB, NUL);
Location r_seq_224 = portLoc(2, seq_224);
Term kv_pair_223 = newTerm(VAR, 0, portLoc(1, seq_224));
seq_224 = makePair(LAM, 0, SUB, seq_224);
Term f_222 = newTerm(VAR, 0, portLoc(1, seq_224));
// allocate args at core: 1973

Term kv_pair_223_1;
kv_pair_223 = dupeArg(kv_pair_223, &kv_pair_223_1, 0);
Term seq_229 = makePair(LAM, 0, SUB, NUL);
Location r_seq_229 = portLoc(2, seq_229);
Term k_228 = newTerm(VAR, 0, portLoc(1, seq_229));
// allocate args at core: 1976
Term seq_234 = makePair(LAM, 0, SUB, NUL);
Location r_seq_234 = portLoc(2, seq_234);
Term v1_233 = newTerm(VAR, 0, portLoc(1, seq_234));
// allocate args at core: 1978
Term seq_239 = makePair(LAM, 0, SUB, NUL);
Location r_seq_239 = portLoc(2, seq_239);
Term v2_238 = newTerm(VAR, 0, portLoc(1, seq_239));
// allocate args at core: 1980
Term glblVal25_18;
glblVal25 = dupeArg(glblVal25, &glblVal25_18, 0);

Term glblVal175_29;
glblVal175 = dupeArg(glblVal175, &glblVal175_29, 0);

// call count at core: 1971
Term Rslt_216Args = makePair(APP, 0, m1_212, SUB);
Term Rslt_216 = newTerm(VAR, 0, portLoc(2, Rslt_216Args));
#ifdef STRICT
pushRedex(Rslt_216Args, newRef(glblProto64_1346));
#else
swap(termLoc(Rslt_216), makePair(LAZ, 0, Rslt_216Args, newRef(glblProto64_1346)));
#endif
// call count at core: 1971
Term Rslt_217Args = makePair(APP, 0, m2_213, SUB);
Term Rslt_217 = newTerm(VAR, 0, portLoc(2, Rslt_217Args));
#ifdef STRICT
pushRedex(Rslt_217Args, newRef(glblProto64_1348));
#else
swap(termLoc(Rslt_217), makePair(LAZ, 0, Rslt_217Args, newRef(glblProto64_1348)));
#endif
// call = at core: 1971
Term Rslt_218Args = makePair(APP, 0, Rslt_217, SUB);
Term Rslt_218 = newTerm(VAR, 0, portLoc(2, Rslt_218Args));
Rslt_218Args = makePair(APP, 0, Rslt_216, Rslt_218Args);
#ifdef STRICT
pushRedex(Rslt_218Args, newRef(glblProto20_1350));
#else
swap(termLoc(Rslt_218), makePair(LAZ, 0, Rslt_218Args, newRef(glblProto20_1350)));
#endif
// call hash-vec at core: 1972
Term Rslt_219Args = makePair(APP, 0, glblVal175_29, SUB);
Term Rslt_219 = newTerm(VAR, 0, portLoc(2, Rslt_219Args));
Rslt_219Args = makePair(APP, 0, m1_212_1, Rslt_219Args);
#ifdef STRICT
pushRedex(Rslt_219Args, newRef(glblProto121_1352));
#else
swap(termLoc(Rslt_219), makePair(LAZ, 0, Rslt_219Args, newRef(glblProto121_1352)));
#endif
// call Some at core: 1972
Term Rslt_220Args = makePair(APP, 0, newI60(1), SUB);
Term Rslt_220 = newTerm(VAR, 0, portLoc(2, Rslt_220Args));
#ifdef STRICT
pushRedex(Rslt_220Args, glblVal25_18);
#else
swap(termLoc(Rslt_220), makePair(LAZ, 0, Rslt_220Args, glblVal25_18));
#endif

// call nth at core: 1975
Term Rslt_226Args = makePair(APP, 0, newI60(0), SUB);
Term Rslt_226 = newTerm(VAR, 0, portLoc(2, Rslt_226Args));
Rslt_226Args = makePair(APP, 0, kv_pair_223, Rslt_226Args);
#ifdef STRICT
pushRedex(Rslt_226Args, newRef(glblProto98_1354));
#else
swap(termLoc(Rslt_226), makePair(LAZ, 0, Rslt_226Args, newRef(glblProto98_1354)));
#endif
// call nth at core: 1977
Term Rslt_231Args = makePair(APP, 0, newI60(1), SUB);
Term Rslt_231 = newTerm(VAR, 0, portLoc(2, Rslt_231Args));
Rslt_231Args = makePair(APP, 0, kv_pair_223_1, Rslt_231Args);
#ifdef STRICT
pushRedex(Rslt_231Args, newRef(glblProto98_1356));
#else
swap(termLoc(Rslt_231), makePair(LAZ, 0, Rslt_231Args, newRef(glblProto98_1356)));
#endif
// call get at core: 1979
Term Rslt_236Args = makePair(APP, 0, k_228, SUB);
Term Rslt_236 = newTerm(VAR, 0, portLoc(2, Rslt_236Args));
Rslt_236Args = makePair(APP, 0, m2_213_1, Rslt_236Args);
#ifdef STRICT
pushRedex(Rslt_236Args, newRef(glblProto119_1359));
#else
swap(termLoc(Rslt_236), makePair(LAZ, 0, Rslt_236Args, newRef(glblProto119_1359)));
#endif
// call = at core: 1981
Term Rslt_241Args = makePair(APP, 0, v2_238, SUB);
Term Rslt_241 = newTerm(VAR, 0, portLoc(2, Rslt_241Args));
Rslt_241Args = makePair(APP, 0, v1_233, Rslt_241Args);
#ifdef STRICT
pushRedex(Rslt_241Args, newRef(glblProto20_1364));
#else
swap(termLoc(Rslt_241), makePair(LAZ, 0, Rslt_241Args, newRef(glblProto20_1364)));
#endif
// link args to body
swap(r_seq_239, Rslt_241);

Term Rslt_241V = get(termLoc(Rslt_241));
apps = take(portLoc(1, Rslt_241V));
lams = take(portLoc(2, Rslt_241V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_241), makePair(LAZ, 0, apps, lams));
// call flat-map at core: 1979
Term Rslt_242Args = makePair(APP, 0, seq_239, SUB);
Term Rslt_242 = newTerm(VAR, 0, portLoc(2, Rslt_242Args));
Rslt_242Args = makePair(APP, 0, Rslt_236, Rslt_242Args);
#ifdef STRICT
pushRedex(Rslt_242Args, newRef(glblProto46_1366));
#else
swap(termLoc(Rslt_242), makePair(LAZ, 0, Rslt_242Args, newRef(glblProto46_1366)));
#endif
// link args to body
swap(r_seq_234, Rslt_242);

Term Rslt_242V = get(termLoc(Rslt_242));
apps = take(portLoc(1, Rslt_242V));
lams = take(portLoc(2, Rslt_242V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_242), makePair(LAZ, 0, apps, lams));
// call flat-map at core: 1977
Term Rslt_244Args = makePair(APP, 0, seq_234, SUB);
Term Rslt_244 = newTerm(VAR, 0, portLoc(2, Rslt_244Args));
Rslt_244Args = makePair(APP, 0, Rslt_231, Rslt_244Args);
#ifdef STRICT
pushRedex(Rslt_244Args, newRef(glblProto46_1370));
#else
swap(termLoc(Rslt_244), makePair(LAZ, 0, Rslt_244Args, newRef(glblProto46_1370)));
#endif
// link args to body
swap(r_seq_229, Rslt_244);

Term Rslt_244V = get(termLoc(Rslt_244));
apps = take(portLoc(1, Rslt_244V));
lams = take(portLoc(2, Rslt_244V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_244), makePair(LAZ, 0, apps, lams));
// call flat-map at core: 1975
Term Rslt_246Args = makePair(APP, 0, seq_229, SUB);
Term Rslt_246 = newTerm(VAR, 0, portLoc(2, Rslt_246Args));
Rslt_246Args = makePair(APP, 0, Rslt_226, Rslt_246Args);
#ifdef STRICT
pushRedex(Rslt_246Args, newRef(glblProto46_1374));
#else
swap(termLoc(Rslt_246), makePair(LAZ, 0, Rslt_246Args, newRef(glblProto46_1374)));
#endif
// call and at core: 1974
Term Rslt_248Args = makePair(APP, 0, Rslt_246, SUB);
Term Rslt_248 = newTerm(VAR, 0, portLoc(2, Rslt_248Args));
Rslt_248Args = makePair(APP, 0, f_222, Rslt_248Args);
#ifdef STRICT
pushRedex(Rslt_248Args, newRef(glblProto28_1377));
#else
swap(termLoc(Rslt_248), makePair(LAZ, 0, Rslt_248Args, newRef(glblProto28_1377)));
#endif
// link args to body
swap(r_seq_224, Rslt_248);

Term Rslt_248V = get(termLoc(Rslt_248));
apps = take(portLoc(1, Rslt_248V));
lams = take(portLoc(2, Rslt_248V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_248), makePair(LAZ, 0, apps, lams));
// call reduce at core: 1972
Term Rslt_249Args = makePair(APP, 0, seq_224, SUB);
Term Rslt_249 = newTerm(VAR, 0, portLoc(2, Rslt_249Args));
Rslt_249Args = makePair(APP, 0, Rslt_220, Rslt_249Args);
Rslt_249Args = makePair(APP, 0, Rslt_219, Rslt_249Args);
#ifdef STRICT
pushRedex(Rslt_249Args, newRef(glblProto72_1385));
#else
swap(termLoc(Rslt_249), makePair(LAZ, 0, Rslt_249Args, newRef(glblProto72_1385)));
#endif
// call and at core: 1971
Term Rslt_251Args = makePair(APP, 0, Rslt_249, SUB);
Term Rslt_251 = newTerm(VAR, 0, portLoc(2, Rslt_251Args));
Rslt_251Args = makePair(APP, 0, Rslt_218, Rslt_251Args);
#ifdef STRICT
pushRedex(Rslt_251Args, newRef(glblProto28_1387));
#else
swap(termLoc(Rslt_251), makePair(LAZ, 0, Rslt_251Args, newRef(glblProto28_1387)));
#endif
// link args to body
swap(r_seq_214, Rslt_251);

Term Rslt_251V = get(termLoc(Rslt_251));
apps = take(portLoc(1, Rslt_251V));
lams = take(portLoc(2, Rslt_251V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_251), makePair(LAZ, 0, apps, lams));
pushRedex(args, seq_214);
return;
}
Term glbl_EQ_1303 = newRef(glbl_EQ_1302);
void glblProto64_1391(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(1971), args);
  args = makePair(APP, 0, glblStr199, args);
  interact(args, glblProto64);
}
}
void glblProto64_1393(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(1971), args);
  args = makePair(APP, 0, glblStr199, args);
  interact(args, glblProto64);
}
}
void glblProto20_1395(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(1971), args);
  args = makePair(APP, 0, glblStr199, args);
  interact(args, glblProto20);
}
}
void glblProto121_1397(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(1972), args);
  args = makePair(APP, 0, glblStr199, args);
  interact(args, glblProto121);
}
}
void glblProto98_1399(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(1975), args);
  args = makePair(APP, 0, glblStr199, args);
  interact(args, glblProto98);
}
}
void glblProto98_1401(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(1977), args);
  args = makePair(APP, 0, glblStr199, args);
  interact(args, glblProto98);
}
}
void glblProto119_1403(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(1979), args);
  args = makePair(APP, 0, glblStr199, args);
  interact(args, glblProto119);
}
}
void glblProto20_1405(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(1981), args);
  args = makePair(APP, 0, glblStr199, args);
  interact(args, glblProto20);
}
}
void glblProto46_1407(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(1979), args);
  args = makePair(APP, 0, glblStr199, args);
  interact(args, glblProto46);
}
}
void glblProto46_1409(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(1977), args);
  args = makePair(APP, 0, glblStr199, args);
  interact(args, glblProto46);
}
}
void glblProto46_1411(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(1975), args);
  args = makePair(APP, 0, glblStr199, args);
  interact(args, glblProto46);
}
}
void glblProto28_1413(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(1974), args);
  args = makePair(APP, 0, glblStr199, args);
  interact(args, glblProto28);
}
}
void glblProto72_1415(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(1972), args);
  args = makePair(APP, 0, glblStr199, args);
  interact(args, glblProto72);
}
}
void glblProto28_1417(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(1971), args);
  args = makePair(APP, 0, glblStr199, args);
  interact(args, glblProto28);
}
}
Term glbl_EQ_1305;
void glbl_EQ_1304(Term ref, Term args) {
Term apps, lams, V;
Location vLoc;
Term seq_255 = makePair(LAM, 0, SUB, NUL);
Location r_seq_255 = portLoc(2, seq_255);
Term m2_254 = newTerm(VAR, 0, portLoc(1, seq_255));
seq_255 = makePair(LAM, 0, SUB, seq_255);
Term m1_253 = newTerm(VAR, 0, portLoc(1, seq_255));
// allocate args at core: 1968

Term m1_253_1;
m1_253 = dupeArg(m1_253, &m1_253_1, 233);

Term m2_254_1;
m2_254 = dupeArg(m2_254, &m2_254_1, 234);
Term seq_265 = makePair(LAM, 0, SUB, NUL);
Location r_seq_265 = portLoc(2, seq_265);
Term kv_pair_264 = newTerm(VAR, 0, portLoc(1, seq_265));
seq_265 = makePair(LAM, 0, SUB, seq_265);
Term f_263 = newTerm(VAR, 0, portLoc(1, seq_265));
// allocate args at core: 1973

Term kv_pair_264_1;
kv_pair_264 = dupeArg(kv_pair_264, &kv_pair_264_1, 0);
Term seq_270 = makePair(LAM, 0, SUB, NUL);
Location r_seq_270 = portLoc(2, seq_270);
Term k_269 = newTerm(VAR, 0, portLoc(1, seq_270));
// allocate args at core: 1976
Term seq_275 = makePair(LAM, 0, SUB, NUL);
Location r_seq_275 = portLoc(2, seq_275);
Term v1_274 = newTerm(VAR, 0, portLoc(1, seq_275));
// allocate args at core: 1978
Term seq_280 = makePair(LAM, 0, SUB, NUL);
Location r_seq_280 = portLoc(2, seq_280);
Term v2_279 = newTerm(VAR, 0, portLoc(1, seq_280));
// allocate args at core: 1980
Term glblVal25_19;
glblVal25 = dupeArg(glblVal25, &glblVal25_19, 0);

Term glblVal175_30;
glblVal175 = dupeArg(glblVal175, &glblVal175_30, 0);

// call count at core: 1971
Term Rslt_257Args = makePair(APP, 0, m1_253, SUB);
Term Rslt_257 = newTerm(VAR, 0, portLoc(2, Rslt_257Args));
#ifdef STRICT
pushRedex(Rslt_257Args, newRef(glblProto64_1391));
#else
swap(termLoc(Rslt_257), makePair(LAZ, 0, Rslt_257Args, newRef(glblProto64_1391)));
#endif
// call count at core: 1971
Term Rslt_258Args = makePair(APP, 0, m2_254, SUB);
Term Rslt_258 = newTerm(VAR, 0, portLoc(2, Rslt_258Args));
#ifdef STRICT
pushRedex(Rslt_258Args, newRef(glblProto64_1393));
#else
swap(termLoc(Rslt_258), makePair(LAZ, 0, Rslt_258Args, newRef(glblProto64_1393)));
#endif
// call = at core: 1971
Term Rslt_259Args = makePair(APP, 0, Rslt_258, SUB);
Term Rslt_259 = newTerm(VAR, 0, portLoc(2, Rslt_259Args));
Rslt_259Args = makePair(APP, 0, Rslt_257, Rslt_259Args);
#ifdef STRICT
pushRedex(Rslt_259Args, newRef(glblProto20_1395));
#else
swap(termLoc(Rslt_259), makePair(LAZ, 0, Rslt_259Args, newRef(glblProto20_1395)));
#endif
// call hash-vec at core: 1972
Term Rslt_260Args = makePair(APP, 0, glblVal175_30, SUB);
Term Rslt_260 = newTerm(VAR, 0, portLoc(2, Rslt_260Args));
Rslt_260Args = makePair(APP, 0, m1_253_1, Rslt_260Args);
#ifdef STRICT
pushRedex(Rslt_260Args, newRef(glblProto121_1397));
#else
swap(termLoc(Rslt_260), makePair(LAZ, 0, Rslt_260Args, newRef(glblProto121_1397)));
#endif
// call Some at core: 1972
Term Rslt_261Args = makePair(APP, 0, newI60(1), SUB);
Term Rslt_261 = newTerm(VAR, 0, portLoc(2, Rslt_261Args));
#ifdef STRICT
pushRedex(Rslt_261Args, glblVal25_19);
#else
swap(termLoc(Rslt_261), makePair(LAZ, 0, Rslt_261Args, glblVal25_19));
#endif

// call nth at core: 1975
Term Rslt_267Args = makePair(APP, 0, newI60(0), SUB);
Term Rslt_267 = newTerm(VAR, 0, portLoc(2, Rslt_267Args));
Rslt_267Args = makePair(APP, 0, kv_pair_264, Rslt_267Args);
#ifdef STRICT
pushRedex(Rslt_267Args, newRef(glblProto98_1399));
#else
swap(termLoc(Rslt_267), makePair(LAZ, 0, Rslt_267Args, newRef(glblProto98_1399)));
#endif
// call nth at core: 1977
Term Rslt_272Args = makePair(APP, 0, newI60(1), SUB);
Term Rslt_272 = newTerm(VAR, 0, portLoc(2, Rslt_272Args));
Rslt_272Args = makePair(APP, 0, kv_pair_264_1, Rslt_272Args);
#ifdef STRICT
pushRedex(Rslt_272Args, newRef(glblProto98_1401));
#else
swap(termLoc(Rslt_272), makePair(LAZ, 0, Rslt_272Args, newRef(glblProto98_1401)));
#endif
// call get at core: 1979
Term Rslt_277Args = makePair(APP, 0, k_269, SUB);
Term Rslt_277 = newTerm(VAR, 0, portLoc(2, Rslt_277Args));
Rslt_277Args = makePair(APP, 0, m2_254_1, Rslt_277Args);
#ifdef STRICT
pushRedex(Rslt_277Args, newRef(glblProto119_1403));
#else
swap(termLoc(Rslt_277), makePair(LAZ, 0, Rslt_277Args, newRef(glblProto119_1403)));
#endif
// call = at core: 1981
Term Rslt_282Args = makePair(APP, 0, v2_279, SUB);
Term Rslt_282 = newTerm(VAR, 0, portLoc(2, Rslt_282Args));
Rslt_282Args = makePair(APP, 0, v1_274, Rslt_282Args);
#ifdef STRICT
pushRedex(Rslt_282Args, newRef(glblProto20_1405));
#else
swap(termLoc(Rslt_282), makePair(LAZ, 0, Rslt_282Args, newRef(glblProto20_1405)));
#endif
// link args to body
swap(r_seq_280, Rslt_282);

Term Rslt_282V = get(termLoc(Rslt_282));
apps = take(portLoc(1, Rslt_282V));
lams = take(portLoc(2, Rslt_282V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_282), makePair(LAZ, 0, apps, lams));
// call flat-map at core: 1979
Term Rslt_283Args = makePair(APP, 0, seq_280, SUB);
Term Rslt_283 = newTerm(VAR, 0, portLoc(2, Rslt_283Args));
Rslt_283Args = makePair(APP, 0, Rslt_277, Rslt_283Args);
#ifdef STRICT
pushRedex(Rslt_283Args, newRef(glblProto46_1407));
#else
swap(termLoc(Rslt_283), makePair(LAZ, 0, Rslt_283Args, newRef(glblProto46_1407)));
#endif
// link args to body
swap(r_seq_275, Rslt_283);

Term Rslt_283V = get(termLoc(Rslt_283));
apps = take(portLoc(1, Rslt_283V));
lams = take(portLoc(2, Rslt_283V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_283), makePair(LAZ, 0, apps, lams));
// call flat-map at core: 1977
Term Rslt_285Args = makePair(APP, 0, seq_275, SUB);
Term Rslt_285 = newTerm(VAR, 0, portLoc(2, Rslt_285Args));
Rslt_285Args = makePair(APP, 0, Rslt_272, Rslt_285Args);
#ifdef STRICT
pushRedex(Rslt_285Args, newRef(glblProto46_1409));
#else
swap(termLoc(Rslt_285), makePair(LAZ, 0, Rslt_285Args, newRef(glblProto46_1409)));
#endif
// link args to body
swap(r_seq_270, Rslt_285);

Term Rslt_285V = get(termLoc(Rslt_285));
apps = take(portLoc(1, Rslt_285V));
lams = take(portLoc(2, Rslt_285V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_285), makePair(LAZ, 0, apps, lams));
// call flat-map at core: 1975
Term Rslt_287Args = makePair(APP, 0, seq_270, SUB);
Term Rslt_287 = newTerm(VAR, 0, portLoc(2, Rslt_287Args));
Rslt_287Args = makePair(APP, 0, Rslt_267, Rslt_287Args);
#ifdef STRICT
pushRedex(Rslt_287Args, newRef(glblProto46_1411));
#else
swap(termLoc(Rslt_287), makePair(LAZ, 0, Rslt_287Args, newRef(glblProto46_1411)));
#endif
// call and at core: 1974
Term Rslt_289Args = makePair(APP, 0, Rslt_287, SUB);
Term Rslt_289 = newTerm(VAR, 0, portLoc(2, Rslt_289Args));
Rslt_289Args = makePair(APP, 0, f_263, Rslt_289Args);
#ifdef STRICT
pushRedex(Rslt_289Args, newRef(glblProto28_1413));
#else
swap(termLoc(Rslt_289), makePair(LAZ, 0, Rslt_289Args, newRef(glblProto28_1413)));
#endif
// link args to body
swap(r_seq_265, Rslt_289);

Term Rslt_289V = get(termLoc(Rslt_289));
apps = take(portLoc(1, Rslt_289V));
lams = take(portLoc(2, Rslt_289V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_289), makePair(LAZ, 0, apps, lams));
// call reduce at core: 1972
Term Rslt_290Args = makePair(APP, 0, seq_265, SUB);
Term Rslt_290 = newTerm(VAR, 0, portLoc(2, Rslt_290Args));
Rslt_290Args = makePair(APP, 0, Rslt_261, Rslt_290Args);
Rslt_290Args = makePair(APP, 0, Rslt_260, Rslt_290Args);
#ifdef STRICT
pushRedex(Rslt_290Args, newRef(glblProto72_1415));
#else
swap(termLoc(Rslt_290), makePair(LAZ, 0, Rslt_290Args, newRef(glblProto72_1415)));
#endif
// call and at core: 1971
Term Rslt_292Args = makePair(APP, 0, Rslt_290, SUB);
Term Rslt_292 = newTerm(VAR, 0, portLoc(2, Rslt_292Args));
Rslt_292Args = makePair(APP, 0, Rslt_259, Rslt_292Args);
#ifdef STRICT
pushRedex(Rslt_292Args, newRef(glblProto28_1417));
#else
swap(termLoc(Rslt_292), makePair(LAZ, 0, Rslt_292Args, newRef(glblProto28_1417)));
#endif
// link args to body
swap(r_seq_255, Rslt_292);

Term Rslt_292V = get(termLoc(Rslt_292));
apps = take(portLoc(1, Rslt_292V));
lams = take(portLoc(2, Rslt_292V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_292), makePair(LAZ, 0, apps, lams));
pushRedex(args, seq_255);
return;
}
Term glbl_EQ_1305 = newRef(glbl_EQ_1304);
Term glblProto386;
Term glblProto1362;
Term glblProto1358;
void glblCons1375(Term ref, Term args) {
Term callArgs = makePair(APP, 0, newI60(3), args);
callArgs = makePair(APP, 0, newI60(46), callArgs);
interact(callArgs, construct);
return;
}
void glblEndOfList1328(Term ref, Term args) {
Term callArgs = makePair(APP, 0, newI60(0), args);
callArgs = makePair(APP, 0, newI60(47), callArgs);
interact(callArgs, construct);
return;
}
void glblProto64_1420(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(1971), args);
  args = makePair(APP, 0, glblStr199, args);
  interact(args, glblProto64);
}
}
void glblProto64_1422(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(1971), args);
  args = makePair(APP, 0, glblStr199, args);
  interact(args, glblProto64);
}
}
void glblProto20_1424(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(1971), args);
  args = makePair(APP, 0, glblStr199, args);
  interact(args, glblProto20);
}
}
void glblProto121_1426(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(1972), args);
  args = makePair(APP, 0, glblStr199, args);
  interact(args, glblProto121);
}
}
void glblProto98_1428(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(1975), args);
  args = makePair(APP, 0, glblStr199, args);
  interact(args, glblProto98);
}
}
void glblProto98_1430(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(1977), args);
  args = makePair(APP, 0, glblStr199, args);
  interact(args, glblProto98);
}
}
void glblProto119_1432(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(1979), args);
  args = makePair(APP, 0, glblStr199, args);
  interact(args, glblProto119);
}
}
void glblProto20_1434(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(1981), args);
  args = makePair(APP, 0, glblStr199, args);
  interact(args, glblProto20);
}
}
void glblProto46_1436(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(1979), args);
  args = makePair(APP, 0, glblStr199, args);
  interact(args, glblProto46);
}
}
void glblProto46_1438(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(1977), args);
  args = makePair(APP, 0, glblStr199, args);
  interact(args, glblProto46);
}
}
void glblProto46_1440(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(1975), args);
  args = makePair(APP, 0, glblStr199, args);
  interact(args, glblProto46);
}
}
void glblProto28_1442(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(1974), args);
  args = makePair(APP, 0, glblStr199, args);
  interact(args, glblProto28);
}
}
void glblProto72_1444(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(1972), args);
  args = makePair(APP, 0, glblStr199, args);
  interact(args, glblProto72);
}
}
void glblProto28_1446(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(1971), args);
  args = makePair(APP, 0, glblStr199, args);
  interact(args, glblProto28);
}
}
Term glbl_EQ_1307;
void glbl_EQ_1306(Term ref, Term args) {
Term apps, lams, V;
Location vLoc;
Term seq_296 = makePair(LAM, 0, SUB, NUL);
Location r_seq_296 = portLoc(2, seq_296);
Term m2_295 = newTerm(VAR, 0, portLoc(1, seq_296));
seq_296 = makePair(LAM, 0, SUB, seq_296);
Term m1_294 = newTerm(VAR, 0, portLoc(1, seq_296));
// allocate args at core: 1968

Term m1_294_1;
m1_294 = dupeArg(m1_294, &m1_294_1, 235);

Term m2_295_1;
m2_295 = dupeArg(m2_295, &m2_295_1, 236);
Term seq_306 = makePair(LAM, 0, SUB, NUL);
Location r_seq_306 = portLoc(2, seq_306);
Term kv_pair_305 = newTerm(VAR, 0, portLoc(1, seq_306));
seq_306 = makePair(LAM, 0, SUB, seq_306);
Term f_304 = newTerm(VAR, 0, portLoc(1, seq_306));
// allocate args at core: 1973

Term kv_pair_305_1;
kv_pair_305 = dupeArg(kv_pair_305, &kv_pair_305_1, 0);
Term seq_311 = makePair(LAM, 0, SUB, NUL);
Location r_seq_311 = portLoc(2, seq_311);
Term k_310 = newTerm(VAR, 0, portLoc(1, seq_311));
// allocate args at core: 1976
Term seq_316 = makePair(LAM, 0, SUB, NUL);
Location r_seq_316 = portLoc(2, seq_316);
Term v1_315 = newTerm(VAR, 0, portLoc(1, seq_316));
// allocate args at core: 1978
Term seq_321 = makePair(LAM, 0, SUB, NUL);
Location r_seq_321 = portLoc(2, seq_321);
Term v2_320 = newTerm(VAR, 0, portLoc(1, seq_321));
// allocate args at core: 1980
Term glblVal25_20;
glblVal25 = dupeArg(glblVal25, &glblVal25_20, 0);

Term glblVal175_31;
glblVal175 = dupeArg(glblVal175, &glblVal175_31, 0);

// call count at core: 1971
Term Rslt_298Args = makePair(APP, 0, m1_294, SUB);
Term Rslt_298 = newTerm(VAR, 0, portLoc(2, Rslt_298Args));
#ifdef STRICT
pushRedex(Rslt_298Args, newRef(glblProto64_1420));
#else
swap(termLoc(Rslt_298), makePair(LAZ, 0, Rslt_298Args, newRef(glblProto64_1420)));
#endif
// call count at core: 1971
Term Rslt_299Args = makePair(APP, 0, m2_295, SUB);
Term Rslt_299 = newTerm(VAR, 0, portLoc(2, Rslt_299Args));
#ifdef STRICT
pushRedex(Rslt_299Args, newRef(glblProto64_1422));
#else
swap(termLoc(Rslt_299), makePair(LAZ, 0, Rslt_299Args, newRef(glblProto64_1422)));
#endif
// call = at core: 1971
Term Rslt_300Args = makePair(APP, 0, Rslt_299, SUB);
Term Rslt_300 = newTerm(VAR, 0, portLoc(2, Rslt_300Args));
Rslt_300Args = makePair(APP, 0, Rslt_298, Rslt_300Args);
#ifdef STRICT
pushRedex(Rslt_300Args, newRef(glblProto20_1424));
#else
swap(termLoc(Rslt_300), makePair(LAZ, 0, Rslt_300Args, newRef(glblProto20_1424)));
#endif
// call hash-vec at core: 1972
Term Rslt_301Args = makePair(APP, 0, glblVal175_31, SUB);
Term Rslt_301 = newTerm(VAR, 0, portLoc(2, Rslt_301Args));
Rslt_301Args = makePair(APP, 0, m1_294_1, Rslt_301Args);
#ifdef STRICT
pushRedex(Rslt_301Args, newRef(glblProto121_1426));
#else
swap(termLoc(Rslt_301), makePair(LAZ, 0, Rslt_301Args, newRef(glblProto121_1426)));
#endif
// call Some at core: 1972
Term Rslt_302Args = makePair(APP, 0, newI60(1), SUB);
Term Rslt_302 = newTerm(VAR, 0, portLoc(2, Rslt_302Args));
#ifdef STRICT
pushRedex(Rslt_302Args, glblVal25_20);
#else
swap(termLoc(Rslt_302), makePair(LAZ, 0, Rslt_302Args, glblVal25_20));
#endif

// call nth at core: 1975
Term Rslt_308Args = makePair(APP, 0, newI60(0), SUB);
Term Rslt_308 = newTerm(VAR, 0, portLoc(2, Rslt_308Args));
Rslt_308Args = makePair(APP, 0, kv_pair_305, Rslt_308Args);
#ifdef STRICT
pushRedex(Rslt_308Args, newRef(glblProto98_1428));
#else
swap(termLoc(Rslt_308), makePair(LAZ, 0, Rslt_308Args, newRef(glblProto98_1428)));
#endif
// call nth at core: 1977
Term Rslt_313Args = makePair(APP, 0, newI60(1), SUB);
Term Rslt_313 = newTerm(VAR, 0, portLoc(2, Rslt_313Args));
Rslt_313Args = makePair(APP, 0, kv_pair_305_1, Rslt_313Args);
#ifdef STRICT
pushRedex(Rslt_313Args, newRef(glblProto98_1430));
#else
swap(termLoc(Rslt_313), makePair(LAZ, 0, Rslt_313Args, newRef(glblProto98_1430)));
#endif
// call get at core: 1979
Term Rslt_318Args = makePair(APP, 0, k_310, SUB);
Term Rslt_318 = newTerm(VAR, 0, portLoc(2, Rslt_318Args));
Rslt_318Args = makePair(APP, 0, m2_295_1, Rslt_318Args);
#ifdef STRICT
pushRedex(Rslt_318Args, newRef(glblProto119_1432));
#else
swap(termLoc(Rslt_318), makePair(LAZ, 0, Rslt_318Args, newRef(glblProto119_1432)));
#endif
// call = at core: 1981
Term Rslt_323Args = makePair(APP, 0, v2_320, SUB);
Term Rslt_323 = newTerm(VAR, 0, portLoc(2, Rslt_323Args));
Rslt_323Args = makePair(APP, 0, v1_315, Rslt_323Args);
#ifdef STRICT
pushRedex(Rslt_323Args, newRef(glblProto20_1434));
#else
swap(termLoc(Rslt_323), makePair(LAZ, 0, Rslt_323Args, newRef(glblProto20_1434)));
#endif
// link args to body
swap(r_seq_321, Rslt_323);

Term Rslt_323V = get(termLoc(Rslt_323));
apps = take(portLoc(1, Rslt_323V));
lams = take(portLoc(2, Rslt_323V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_323), makePair(LAZ, 0, apps, lams));
// call flat-map at core: 1979
Term Rslt_324Args = makePair(APP, 0, seq_321, SUB);
Term Rslt_324 = newTerm(VAR, 0, portLoc(2, Rslt_324Args));
Rslt_324Args = makePair(APP, 0, Rslt_318, Rslt_324Args);
#ifdef STRICT
pushRedex(Rslt_324Args, newRef(glblProto46_1436));
#else
swap(termLoc(Rslt_324), makePair(LAZ, 0, Rslt_324Args, newRef(glblProto46_1436)));
#endif
// link args to body
swap(r_seq_316, Rslt_324);

Term Rslt_324V = get(termLoc(Rslt_324));
apps = take(portLoc(1, Rslt_324V));
lams = take(portLoc(2, Rslt_324V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_324), makePair(LAZ, 0, apps, lams));
// call flat-map at core: 1977
Term Rslt_326Args = makePair(APP, 0, seq_316, SUB);
Term Rslt_326 = newTerm(VAR, 0, portLoc(2, Rslt_326Args));
Rslt_326Args = makePair(APP, 0, Rslt_313, Rslt_326Args);
#ifdef STRICT
pushRedex(Rslt_326Args, newRef(glblProto46_1438));
#else
swap(termLoc(Rslt_326), makePair(LAZ, 0, Rslt_326Args, newRef(glblProto46_1438)));
#endif
// link args to body
swap(r_seq_311, Rslt_326);

Term Rslt_326V = get(termLoc(Rslt_326));
apps = take(portLoc(1, Rslt_326V));
lams = take(portLoc(2, Rslt_326V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_326), makePair(LAZ, 0, apps, lams));
// call flat-map at core: 1975
Term Rslt_328Args = makePair(APP, 0, seq_311, SUB);
Term Rslt_328 = newTerm(VAR, 0, portLoc(2, Rslt_328Args));
Rslt_328Args = makePair(APP, 0, Rslt_308, Rslt_328Args);
#ifdef STRICT
pushRedex(Rslt_328Args, newRef(glblProto46_1440));
#else
swap(termLoc(Rslt_328), makePair(LAZ, 0, Rslt_328Args, newRef(glblProto46_1440)));
#endif
// call and at core: 1974
Term Rslt_330Args = makePair(APP, 0, Rslt_328, SUB);
Term Rslt_330 = newTerm(VAR, 0, portLoc(2, Rslt_330Args));
Rslt_330Args = makePair(APP, 0, f_304, Rslt_330Args);
#ifdef STRICT
pushRedex(Rslt_330Args, newRef(glblProto28_1442));
#else
swap(termLoc(Rslt_330), makePair(LAZ, 0, Rslt_330Args, newRef(glblProto28_1442)));
#endif
// link args to body
swap(r_seq_306, Rslt_330);

Term Rslt_330V = get(termLoc(Rslt_330));
apps = take(portLoc(1, Rslt_330V));
lams = take(portLoc(2, Rslt_330V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_330), makePair(LAZ, 0, apps, lams));
// call reduce at core: 1972
Term Rslt_331Args = makePair(APP, 0, seq_306, SUB);
Term Rslt_331 = newTerm(VAR, 0, portLoc(2, Rslt_331Args));
Rslt_331Args = makePair(APP, 0, Rslt_302, Rslt_331Args);
Rslt_331Args = makePair(APP, 0, Rslt_301, Rslt_331Args);
#ifdef STRICT
pushRedex(Rslt_331Args, newRef(glblProto72_1444));
#else
swap(termLoc(Rslt_331), makePair(LAZ, 0, Rslt_331Args, newRef(glblProto72_1444)));
#endif
// call and at core: 1971
Term Rslt_333Args = makePair(APP, 0, Rslt_331, SUB);
Term Rslt_333 = newTerm(VAR, 0, portLoc(2, Rslt_333Args));
Rslt_333Args = makePair(APP, 0, Rslt_300, Rslt_333Args);
#ifdef STRICT
pushRedex(Rslt_333Args, newRef(glblProto28_1446));
#else
swap(termLoc(Rslt_333), makePair(LAZ, 0, Rslt_333Args, newRef(glblProto28_1446)));
#endif
// link args to body
swap(r_seq_296, Rslt_333);

Term Rslt_333V = get(termLoc(Rslt_333));
apps = take(portLoc(1, Rslt_333V));
lams = take(portLoc(2, Rslt_333V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_333), makePair(LAZ, 0, apps, lams));
pushRedex(args, seq_296);
return;
}
Term glbl_EQ_1307 = newRef(glbl_EQ_1306);
void glblProto111_1449(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(1988), args);
  args = makePair(APP, 0, glblStr199, args);
  interact(args, glblProto111);
}
}
void glblProto104_1451(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(1987), args);
  args = makePair(APP, 0, glblStr199, args);
  interact(args, glblProto104);
}
}
Term glblassoc1297;
void glblassoc1296(Term ref, Term args) {
Term apps, lams, V;
Location vLoc;
Term seq_338 = makePair(LAM, 0, SUB, NUL);
Location r_seq_338 = portLoc(2, seq_338);
Term v_337 = newTerm(VAR, 0, portLoc(1, seq_338));
seq_338 = makePair(LAM, 0, SUB, seq_338);
Term k_336 = newTerm(VAR, 0, portLoc(1, seq_338));
seq_338 = makePair(LAM, 0, SUB, seq_338);
Term m_335 = newTerm(VAR, 0, portLoc(1, seq_338));
// allocate args at core: 1983

Term k_336_1;
k_336 = dupeArg(k_336, &k_336_1, 237);
Term seq_342 = makePair(LAM, 0, SUB, NUL);
Location r_seq_342 = portLoc(2, seq_342);
Term h_341 = newTerm(VAR, 0, portLoc(1, seq_342));
// allocate args at core: 1987
// call assoc* at core: 1988
Term Rslt_344Args = makePair(APP, 0, newI60(0), SUB);
Term Rslt_344 = newTerm(VAR, 0, portLoc(2, Rslt_344Args));
Rslt_344Args = makePair(APP, 0, h_341, Rslt_344Args);
Rslt_344Args = makePair(APP, 0, v_337, Rslt_344Args);
Rslt_344Args = makePair(APP, 0, k_336, Rslt_344Args);
Rslt_344Args = makePair(APP, 0, m_335, Rslt_344Args);
#ifdef STRICT
pushRedex(Rslt_344Args, newRef(glblProto111_1449));
#else
swap(termLoc(Rslt_344), makePair(LAZ, 0, Rslt_344Args, newRef(glblProto111_1449)));
#endif
// link args to body
swap(r_seq_342, Rslt_344);

Term Rslt_344V = get(termLoc(Rslt_344));
apps = take(portLoc(1, Rslt_344V));
lams = take(portLoc(2, Rslt_344V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_344), makePair(LAZ, 0, apps, lams));
// call sha1 at core: 1987
Term Rslt_345Args = makePair(APP, 0, k_336_1, SUB);
Term Rslt_345 = newTerm(VAR, 0, portLoc(2, Rslt_345Args));
#ifdef STRICT
pushRedex(Rslt_345Args, newRef(glblProto104_1451));
#else
swap(termLoc(Rslt_345), makePair(LAZ, 0, Rslt_345Args, newRef(glblProto104_1451)));
#endif
// call  at core: 1987
Term Rslt_346Args = makePair(APP, 0, Rslt_345, SUB);
Term Rslt_346 = newTerm(VAR, 0, portLoc(2, Rslt_346Args));
#ifdef STRICT
pushRedex(Rslt_346Args, seq_342);
#else
swap(termLoc(Rslt_346), makePair(LAZ, 0, Rslt_346Args, seq_342));
#endif

// link args to body
swap(r_seq_338, Rslt_346);

Term Rslt_346V = get(termLoc(Rslt_346));
apps = take(portLoc(1, Rslt_346V));
lams = take(portLoc(2, Rslt_346V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_346), makePair(LAZ, 0, apps, lams));
pushRedex(args, seq_338);
return;
}
Term glblassoc1297 = newRef(glblassoc1296);
void glblProto111_1454(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(1988), args);
  args = makePair(APP, 0, glblStr199, args);
  interact(args, glblProto111);
}
}
void glblProto104_1456(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(1987), args);
  args = makePair(APP, 0, glblStr199, args);
  interact(args, glblProto104);
}
}
Term glblassoc1299;
void glblassoc1298(Term ref, Term args) {
Term apps, lams, V;
Location vLoc;
Term seq_351 = makePair(LAM, 0, SUB, NUL);
Location r_seq_351 = portLoc(2, seq_351);
Term v_350 = newTerm(VAR, 0, portLoc(1, seq_351));
seq_351 = makePair(LAM, 0, SUB, seq_351);
Term k_349 = newTerm(VAR, 0, portLoc(1, seq_351));
seq_351 = makePair(LAM, 0, SUB, seq_351);
Term m_348 = newTerm(VAR, 0, portLoc(1, seq_351));
// allocate args at core: 1983

Term k_349_1;
k_349 = dupeArg(k_349, &k_349_1, 238);
Term seq_355 = makePair(LAM, 0, SUB, NUL);
Location r_seq_355 = portLoc(2, seq_355);
Term h_354 = newTerm(VAR, 0, portLoc(1, seq_355));
// allocate args at core: 1987
// call assoc* at core: 1988
Term Rslt_357Args = makePair(APP, 0, newI60(0), SUB);
Term Rslt_357 = newTerm(VAR, 0, portLoc(2, Rslt_357Args));
Rslt_357Args = makePair(APP, 0, h_354, Rslt_357Args);
Rslt_357Args = makePair(APP, 0, v_350, Rslt_357Args);
Rslt_357Args = makePair(APP, 0, k_349, Rslt_357Args);
Rslt_357Args = makePair(APP, 0, m_348, Rslt_357Args);
#ifdef STRICT
pushRedex(Rslt_357Args, newRef(glblProto111_1454));
#else
swap(termLoc(Rslt_357), makePair(LAZ, 0, Rslt_357Args, newRef(glblProto111_1454)));
#endif
// link args to body
swap(r_seq_355, Rslt_357);

Term Rslt_357V = get(termLoc(Rslt_357));
apps = take(portLoc(1, Rslt_357V));
lams = take(portLoc(2, Rslt_357V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_357), makePair(LAZ, 0, apps, lams));
// call sha1 at core: 1987
Term Rslt_358Args = makePair(APP, 0, k_349_1, SUB);
Term Rslt_358 = newTerm(VAR, 0, portLoc(2, Rslt_358Args));
#ifdef STRICT
pushRedex(Rslt_358Args, newRef(glblProto104_1456));
#else
swap(termLoc(Rslt_358), makePair(LAZ, 0, Rslt_358Args, newRef(glblProto104_1456)));
#endif
// call  at core: 1987
Term Rslt_359Args = makePair(APP, 0, Rslt_358, SUB);
Term Rslt_359 = newTerm(VAR, 0, portLoc(2, Rslt_359Args));
#ifdef STRICT
pushRedex(Rslt_359Args, seq_355);
#else
swap(termLoc(Rslt_359), makePair(LAZ, 0, Rslt_359Args, seq_355));
#endif

// link args to body
swap(r_seq_351, Rslt_359);

Term Rslt_359V = get(termLoc(Rslt_359));
apps = take(portLoc(1, Rslt_359V));
lams = take(portLoc(2, Rslt_359V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_359), makePair(LAZ, 0, apps, lams));
pushRedex(args, seq_351);
return;
}
Term glblassoc1299 = newRef(glblassoc1298);
void glblProto111_1459(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(1988), args);
  args = makePair(APP, 0, glblStr199, args);
  interact(args, glblProto111);
}
}
void glblProto104_1461(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(1987), args);
  args = makePair(APP, 0, glblStr199, args);
  interact(args, glblProto104);
}
}
Term glblassoc1301;
void glblassoc1300(Term ref, Term args) {
Term apps, lams, V;
Location vLoc;
Term seq_364 = makePair(LAM, 0, SUB, NUL);
Location r_seq_364 = portLoc(2, seq_364);
Term v_363 = newTerm(VAR, 0, portLoc(1, seq_364));
seq_364 = makePair(LAM, 0, SUB, seq_364);
Term k_362 = newTerm(VAR, 0, portLoc(1, seq_364));
seq_364 = makePair(LAM, 0, SUB, seq_364);
Term m_361 = newTerm(VAR, 0, portLoc(1, seq_364));
// allocate args at core: 1983

Term k_362_1;
k_362 = dupeArg(k_362, &k_362_1, 239);
Term seq_368 = makePair(LAM, 0, SUB, NUL);
Location r_seq_368 = portLoc(2, seq_368);
Term h_367 = newTerm(VAR, 0, portLoc(1, seq_368));
// allocate args at core: 1987
// call assoc* at core: 1988
Term Rslt_370Args = makePair(APP, 0, newI60(0), SUB);
Term Rslt_370 = newTerm(VAR, 0, portLoc(2, Rslt_370Args));
Rslt_370Args = makePair(APP, 0, h_367, Rslt_370Args);
Rslt_370Args = makePair(APP, 0, v_363, Rslt_370Args);
Rslt_370Args = makePair(APP, 0, k_362, Rslt_370Args);
Rslt_370Args = makePair(APP, 0, m_361, Rslt_370Args);
#ifdef STRICT
pushRedex(Rslt_370Args, newRef(glblProto111_1459));
#else
swap(termLoc(Rslt_370), makePair(LAZ, 0, Rslt_370Args, newRef(glblProto111_1459)));
#endif
// link args to body
swap(r_seq_368, Rslt_370);

Term Rslt_370V = get(termLoc(Rslt_370));
apps = take(portLoc(1, Rslt_370V));
lams = take(portLoc(2, Rslt_370V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_370), makePair(LAZ, 0, apps, lams));
// call sha1 at core: 1987
Term Rslt_371Args = makePair(APP, 0, k_362_1, SUB);
Term Rslt_371 = newTerm(VAR, 0, portLoc(2, Rslt_371Args));
#ifdef STRICT
pushRedex(Rslt_371Args, newRef(glblProto104_1461));
#else
swap(termLoc(Rslt_371), makePair(LAZ, 0, Rslt_371Args, newRef(glblProto104_1461)));
#endif
// call  at core: 1987
Term Rslt_372Args = makePair(APP, 0, Rslt_371, SUB);
Term Rslt_372 = newTerm(VAR, 0, portLoc(2, Rslt_372Args));
#ifdef STRICT
pushRedex(Rslt_372Args, seq_368);
#else
swap(termLoc(Rslt_372), makePair(LAZ, 0, Rslt_372Args, seq_368));
#endif

// link args to body
swap(r_seq_364, Rslt_372);

Term Rslt_372V = get(termLoc(Rslt_372));
apps = take(portLoc(1, Rslt_372V));
lams = take(portLoc(2, Rslt_372V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_372), makePair(LAZ, 0, apps, lams));
pushRedex(args, seq_364);
return;
}
Term glblassoc1301 = newRef(glblassoc1300);
void glblProto115_1464(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(1992), args);
  args = makePair(APP, 0, glblStr199, args);
  interact(args, glblProto115);
}
}
void glblProto104_1466(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(1991), args);
  args = makePair(APP, 0, glblStr199, args);
  interact(args, glblProto104);
}
}
Term glbldissoc1291;
void glbldissoc1290(Term ref, Term args) {
Term apps, lams, V;
Location vLoc;
Term seq_376 = makePair(LAM, 0, SUB, NUL);
Location r_seq_376 = portLoc(2, seq_376);
Term k_375 = newTerm(VAR, 0, portLoc(1, seq_376));
seq_376 = makePair(LAM, 0, SUB, seq_376);
Term m_374 = newTerm(VAR, 0, portLoc(1, seq_376));
// allocate args at core: 1990

Term k_375_1;
k_375 = dupeArg(k_375, &k_375_1, 240);
Term seq_380 = makePair(LAM, 0, SUB, NUL);
Location r_seq_380 = portLoc(2, seq_380);
Term h_379 = newTerm(VAR, 0, portLoc(1, seq_380));
// allocate args at core: 1991
// call dissoc* at core: 1992
Term Rslt_382Args = makePair(APP, 0, newI60(0), SUB);
Term Rslt_382 = newTerm(VAR, 0, portLoc(2, Rslt_382Args));
Rslt_382Args = makePair(APP, 0, h_379, Rslt_382Args);
Rslt_382Args = makePair(APP, 0, k_375, Rslt_382Args);
Rslt_382Args = makePair(APP, 0, m_374, Rslt_382Args);
#ifdef STRICT
pushRedex(Rslt_382Args, newRef(glblProto115_1464));
#else
swap(termLoc(Rslt_382), makePair(LAZ, 0, Rslt_382Args, newRef(glblProto115_1464)));
#endif
// link args to body
swap(r_seq_380, Rslt_382);

Term Rslt_382V = get(termLoc(Rslt_382));
apps = take(portLoc(1, Rslt_382V));
lams = take(portLoc(2, Rslt_382V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_382), makePair(LAZ, 0, apps, lams));
// call sha1 at core: 1991
Term Rslt_383Args = makePair(APP, 0, k_375_1, SUB);
Term Rslt_383 = newTerm(VAR, 0, portLoc(2, Rslt_383Args));
#ifdef STRICT
pushRedex(Rslt_383Args, newRef(glblProto104_1466));
#else
swap(termLoc(Rslt_383), makePair(LAZ, 0, Rslt_383Args, newRef(glblProto104_1466)));
#endif
// call  at core: 1991
Term Rslt_384Args = makePair(APP, 0, Rslt_383, SUB);
Term Rslt_384 = newTerm(VAR, 0, portLoc(2, Rslt_384Args));
#ifdef STRICT
pushRedex(Rslt_384Args, seq_380);
#else
swap(termLoc(Rslt_384), makePair(LAZ, 0, Rslt_384Args, seq_380));
#endif

// link args to body
swap(r_seq_376, Rslt_384);

Term Rslt_384V = get(termLoc(Rslt_384));
apps = take(portLoc(1, Rslt_384V));
lams = take(portLoc(2, Rslt_384V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_384), makePair(LAZ, 0, apps, lams));
pushRedex(args, seq_376);
return;
}
Term glbldissoc1291 = newRef(glbldissoc1290);
void glblProto115_1475(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(1992), args);
  args = makePair(APP, 0, glblStr199, args);
  interact(args, glblProto115);
}
}
void glblProto104_1479(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(1991), args);
  args = makePair(APP, 0, glblStr199, args);
  interact(args, glblProto104);
}
}
Term glbldissoc1293;
void glbldissoc1292(Term ref, Term args) {
Term apps, lams, V;
Location vLoc;
Term seq_388 = makePair(LAM, 0, SUB, NUL);
Location r_seq_388 = portLoc(2, seq_388);
Term k_387 = newTerm(VAR, 0, portLoc(1, seq_388));
seq_388 = makePair(LAM, 0, SUB, seq_388);
Term m_386 = newTerm(VAR, 0, portLoc(1, seq_388));
// allocate args at core: 1990

Term k_387_1;
k_387 = dupeArg(k_387, &k_387_1, 241);
Term seq_392 = makePair(LAM, 0, SUB, NUL);
Location r_seq_392 = portLoc(2, seq_392);
Term h_391 = newTerm(VAR, 0, portLoc(1, seq_392));
// allocate args at core: 1991
// call dissoc* at core: 1992
Term Rslt_394Args = makePair(APP, 0, newI60(0), SUB);
Term Rslt_394 = newTerm(VAR, 0, portLoc(2, Rslt_394Args));
Rslt_394Args = makePair(APP, 0, h_391, Rslt_394Args);
Rslt_394Args = makePair(APP, 0, k_387, Rslt_394Args);
Rslt_394Args = makePair(APP, 0, m_386, Rslt_394Args);
#ifdef STRICT
pushRedex(Rslt_394Args, newRef(glblProto115_1475));
#else
swap(termLoc(Rslt_394), makePair(LAZ, 0, Rslt_394Args, newRef(glblProto115_1475)));
#endif
// link args to body
swap(r_seq_392, Rslt_394);

Term Rslt_394V = get(termLoc(Rslt_394));
apps = take(portLoc(1, Rslt_394V));
lams = take(portLoc(2, Rslt_394V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_394), makePair(LAZ, 0, apps, lams));
// call sha1 at core: 1991
Term Rslt_395Args = makePair(APP, 0, k_387_1, SUB);
Term Rslt_395 = newTerm(VAR, 0, portLoc(2, Rslt_395Args));
#ifdef STRICT
pushRedex(Rslt_395Args, newRef(glblProto104_1479));
#else
swap(termLoc(Rslt_395), makePair(LAZ, 0, Rslt_395Args, newRef(glblProto104_1479)));
#endif
// call  at core: 1991
Term Rslt_396Args = makePair(APP, 0, Rslt_395, SUB);
Term Rslt_396 = newTerm(VAR, 0, portLoc(2, Rslt_396Args));
#ifdef STRICT
pushRedex(Rslt_396Args, seq_392);
#else
swap(termLoc(Rslt_396), makePair(LAZ, 0, Rslt_396Args, seq_392));
#endif

// link args to body
swap(r_seq_388, Rslt_396);

Term Rslt_396V = get(termLoc(Rslt_396));
apps = take(portLoc(1, Rslt_396V));
lams = take(portLoc(2, Rslt_396V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_396), makePair(LAZ, 0, apps, lams));
pushRedex(args, seq_388);
return;
}
Term glbldissoc1293 = newRef(glbldissoc1292);
Term glblProto1476;
Term glblProto1472;
void glblLocation1480(Term ref, Term args) {
Term callArgs = makePair(APP, 0, newI60(2), args);
callArgs = makePair(APP, 0, newI60(48), callArgs);
interact(callArgs, construct);
return;
}
void glblProto115_1487(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(1992), args);
  args = makePair(APP, 0, glblStr199, args);
  interact(args, glblProto115);
}
}
void glblProto104_1490(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(1991), args);
  args = makePair(APP, 0, glblStr199, args);
  interact(args, glblProto104);
}
}
Term glbldissoc1295;
void glbldissoc1294(Term ref, Term args) {
Term apps, lams, V;
Location vLoc;
Term seq_400 = makePair(LAM, 0, SUB, NUL);
Location r_seq_400 = portLoc(2, seq_400);
Term k_399 = newTerm(VAR, 0, portLoc(1, seq_400));
seq_400 = makePair(LAM, 0, SUB, seq_400);
Term m_398 = newTerm(VAR, 0, portLoc(1, seq_400));
// allocate args at core: 1990

Term k_399_1;
k_399 = dupeArg(k_399, &k_399_1, 242);
Term seq_404 = makePair(LAM, 0, SUB, NUL);
Location r_seq_404 = portLoc(2, seq_404);
Term h_403 = newTerm(VAR, 0, portLoc(1, seq_404));
// allocate args at core: 1991
// call dissoc* at core: 1992
Term Rslt_406Args = makePair(APP, 0, newI60(0), SUB);
Term Rslt_406 = newTerm(VAR, 0, portLoc(2, Rslt_406Args));
Rslt_406Args = makePair(APP, 0, h_403, Rslt_406Args);
Rslt_406Args = makePair(APP, 0, k_399, Rslt_406Args);
Rslt_406Args = makePair(APP, 0, m_398, Rslt_406Args);
#ifdef STRICT
pushRedex(Rslt_406Args, newRef(glblProto115_1487));
#else
swap(termLoc(Rslt_406), makePair(LAZ, 0, Rslt_406Args, newRef(glblProto115_1487)));
#endif
// link args to body
swap(r_seq_404, Rslt_406);

Term Rslt_406V = get(termLoc(Rslt_406));
apps = take(portLoc(1, Rslt_406V));
lams = take(portLoc(2, Rslt_406V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_406), makePair(LAZ, 0, apps, lams));
// call sha1 at core: 1991
Term Rslt_407Args = makePair(APP, 0, k_399_1, SUB);
Term Rslt_407 = newTerm(VAR, 0, portLoc(2, Rslt_407Args));
#ifdef STRICT
pushRedex(Rslt_407Args, newRef(glblProto104_1490));
#else
swap(termLoc(Rslt_407), makePair(LAZ, 0, Rslt_407Args, newRef(glblProto104_1490)));
#endif
// call  at core: 1991
Term Rslt_408Args = makePair(APP, 0, Rslt_407, SUB);
Term Rslt_408 = newTerm(VAR, 0, portLoc(2, Rslt_408Args));
#ifdef STRICT
pushRedex(Rslt_408Args, seq_404);
#else
swap(termLoc(Rslt_408), makePair(LAZ, 0, Rslt_408Args, seq_404));
#endif

// link args to body
swap(r_seq_400, Rslt_408);

Term Rslt_408V = get(termLoc(Rslt_408));
apps = take(portLoc(1, Rslt_408V));
lams = take(portLoc(2, Rslt_408V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_408), makePair(LAZ, 0, apps, lams));
pushRedex(args, seq_400);
return;
}
Term glbldissoc1295 = newRef(glbldissoc1294);
void glblProto20_1493(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(1998), args);
  args = makePair(APP, 0, glblStr199, args);
  interact(args, glblProto20);
}
}
void glblProto34_1497(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(1998), args);
  args = makePair(APP, 0, glblStr199, args);
  interact(args, glblProto34);
}
}
void glblProto113_1499(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(1997), args);
  args = makePair(APP, 0, glblStr199, args);
  interact(args, glblProto113);
}
}
void glblProto104_1505(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(1995), args);
  args = makePair(APP, 0, glblStr199, args);
  interact(args, glblProto104);
}
}
Term glblget1285;
void glblget1284(Term ref, Term args) {
Term apps, lams, V;
Location vLoc;
Term seq_412 = makePair(LAM, 0, SUB, NUL);
Location r_seq_412 = portLoc(2, seq_412);
Term k_411 = newTerm(VAR, 0, portLoc(1, seq_412));
seq_412 = makePair(LAM, 0, SUB, seq_412);
Term m_410 = newTerm(VAR, 0, portLoc(1, seq_412));
// allocate args at core: 1994

Term k_411_1;
k_411 = dupeArg(k_411, &k_411_1, 243);
Term seq_416 = makePair(LAM, 0, SUB, NUL);
Location r_seq_416 = portLoc(2, seq_416);
Term h_415 = newTerm(VAR, 0, portLoc(1, seq_416));
// allocate args at core: 1995
Term glblRslt1181_1;
glblRslt1181 = dupeArg(glblRslt1181, &glblRslt1181_1, 0);

Term seq_420 = makePair(LAM, 0, SUB, NUL);
Location r_seq_420 = portLoc(2, seq_420);
Term sentinel_419 = newTerm(VAR, 0, portLoc(1, seq_420));
// allocate args at core: 1996

Term sentinel_419_1;
sentinel_419 = dupeArg(sentinel_419, &sentinel_419_1, 0);
Term seq_424 = makePair(LAM, 0, SUB, NUL);
Location r_seq_424 = portLoc(2, seq_424);
Term r_423 = newTerm(VAR, 0, portLoc(1, seq_424));
// allocate args at core: 1997

Term r_423_1;
r_423 = dupeArg(r_423, &r_423_1, 0);
Term glblVal25_21;
glblVal25 = dupeArg(glblVal25, &glblVal25_21, 0);

Term glblRslt16_9;
glblRslt16 = dupeArg(glblRslt16, &glblRslt16_9, 0);

// call = at core: 1998
Term Rslt_426Args = makePair(APP, 0, r_423, SUB);
Term Rslt_426 = newTerm(VAR, 0, portLoc(2, Rslt_426Args));
Rslt_426Args = makePair(APP, 0, sentinel_419, Rslt_426Args);
#ifdef STRICT
pushRedex(Rslt_426Args, newRef(glblProto20_1493));
#else
swap(termLoc(Rslt_426), makePair(LAZ, 0, Rslt_426Args, newRef(glblProto20_1493)));
#endif
// call Some at core: 2000
Term Rslt_427Args = makePair(APP, 0, r_423_1, SUB);
Term Rslt_427 = newTerm(VAR, 0, portLoc(2, Rslt_427Args));
#ifdef STRICT
pushRedex(Rslt_427Args, glblVal25_21);
#else
swap(termLoc(Rslt_427), makePair(LAZ, 0, Rslt_427Args, glblVal25_21));
#endif

// call cond at core: 1998
Term Rslt_428Args = makePair(APP, 0, Rslt_427, SUB);
Term Rslt_428 = newTerm(VAR, 0, portLoc(2, Rslt_428Args));
Rslt_428Args = makePair(APP, 0, glblRslt16_9, Rslt_428Args);
Rslt_428Args = makePair(APP, 0, Rslt_426, Rslt_428Args);
#ifdef STRICT
pushRedex(Rslt_428Args, newRef(glblProto34_1497));
#else
swap(termLoc(Rslt_428), makePair(LAZ, 0, Rslt_428Args, newRef(glblProto34_1497)));
#endif
// link args to body
swap(r_seq_424, Rslt_428);

Term Rslt_428V = get(termLoc(Rslt_428));
apps = take(portLoc(1, Rslt_428V));
lams = take(portLoc(2, Rslt_428V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_428), makePair(LAZ, 0, apps, lams));
// call get* at core: 1997
Term Rslt_429Args = makePair(APP, 0, newI60(0), SUB);
Term Rslt_429 = newTerm(VAR, 0, portLoc(2, Rslt_429Args));
Rslt_429Args = makePair(APP, 0, h_415, Rslt_429Args);
Rslt_429Args = makePair(APP, 0, sentinel_419_1, Rslt_429Args);
Rslt_429Args = makePair(APP, 0, k_411, Rslt_429Args);
Rslt_429Args = makePair(APP, 0, m_410, Rslt_429Args);
#ifdef STRICT
pushRedex(Rslt_429Args, newRef(glblProto113_1499));
#else
swap(termLoc(Rslt_429), makePair(LAZ, 0, Rslt_429Args, newRef(glblProto113_1499)));
#endif
// call  at core: 1997
Term Rslt_430Args = makePair(APP, 0, Rslt_429, SUB);
Term Rslt_430 = newTerm(VAR, 0, portLoc(2, Rslt_430Args));
#ifdef STRICT
pushRedex(Rslt_430Args, seq_424);
#else
swap(termLoc(Rslt_430), makePair(LAZ, 0, Rslt_430Args, seq_424));
#endif

// link args to body
swap(r_seq_420, Rslt_430);

Term Rslt_430V = get(termLoc(Rslt_430));
apps = take(portLoc(1, Rslt_430V));
lams = take(portLoc(2, Rslt_430V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_430), makePair(LAZ, 0, apps, lams));
// call  at core: 1996
Term Rslt_431Args = makePair(APP, 0, glblRslt1181_1, SUB);
Term Rslt_431 = newTerm(VAR, 0, portLoc(2, Rslt_431Args));
#ifdef STRICT
pushRedex(Rslt_431Args, seq_420);
#else
swap(termLoc(Rslt_431), makePair(LAZ, 0, Rslt_431Args, seq_420));
#endif

// link args to body
swap(r_seq_416, Rslt_431);

Term Rslt_431V = get(termLoc(Rslt_431));
apps = take(portLoc(1, Rslt_431V));
lams = take(portLoc(2, Rslt_431V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_431), makePair(LAZ, 0, apps, lams));
// call sha1 at core: 1995
Term Rslt_432Args = makePair(APP, 0, k_411_1, SUB);
Term Rslt_432 = newTerm(VAR, 0, portLoc(2, Rslt_432Args));
#ifdef STRICT
pushRedex(Rslt_432Args, newRef(glblProto104_1505));
#else
swap(termLoc(Rslt_432), makePair(LAZ, 0, Rslt_432Args, newRef(glblProto104_1505)));
#endif
// call  at core: 1995
Term Rslt_433Args = makePair(APP, 0, Rslt_432, SUB);
Term Rslt_433 = newTerm(VAR, 0, portLoc(2, Rslt_433Args));
#ifdef STRICT
pushRedex(Rslt_433Args, seq_416);
#else
swap(termLoc(Rslt_433), makePair(LAZ, 0, Rslt_433Args, seq_416));
#endif

// link args to body
swap(r_seq_412, Rslt_433);

Term Rslt_433V = get(termLoc(Rslt_433));
apps = take(portLoc(1, Rslt_433V));
lams = take(portLoc(2, Rslt_433V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_433), makePair(LAZ, 0, apps, lams));
pushRedex(args, seq_412);
return;
}
Term glblget1285 = newRef(glblget1284);
Term glblProto1495;
void glblModule1500(Term ref, Term args) {
Term callArgs = makePair(APP, 0, newI60(1), args);
callArgs = makePair(APP, 0, newI60(49), callArgs);
interact(callArgs, construct);
return;
}
void glblProto20_1508(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(1998), args);
  args = makePair(APP, 0, glblStr199, args);
  interact(args, glblProto20);
}
}
void glblProto34_1514(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(1998), args);
  args = makePair(APP, 0, glblStr199, args);
  interact(args, glblProto34);
}
}
void glblProto113_1518(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(1997), args);
  args = makePair(APP, 0, glblStr199, args);
  interact(args, glblProto113);
}
}
void glblProto104_1522(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(1995), args);
  args = makePair(APP, 0, glblStr199, args);
  interact(args, glblProto104);
}
}
Term glblget1287;
void glblget1286(Term ref, Term args) {
Term apps, lams, V;
Location vLoc;
Term seq_437 = makePair(LAM, 0, SUB, NUL);
Location r_seq_437 = portLoc(2, seq_437);
Term k_436 = newTerm(VAR, 0, portLoc(1, seq_437));
seq_437 = makePair(LAM, 0, SUB, seq_437);
Term m_435 = newTerm(VAR, 0, portLoc(1, seq_437));
// allocate args at core: 1994

Term k_436_1;
k_436 = dupeArg(k_436, &k_436_1, 244);
Term seq_441 = makePair(LAM, 0, SUB, NUL);
Location r_seq_441 = portLoc(2, seq_441);
Term h_440 = newTerm(VAR, 0, portLoc(1, seq_441));
// allocate args at core: 1995
Term glblRslt1181_2;
glblRslt1181 = dupeArg(glblRslt1181, &glblRslt1181_2, 0);

Term seq_445 = makePair(LAM, 0, SUB, NUL);
Location r_seq_445 = portLoc(2, seq_445);
Term sentinel_444 = newTerm(VAR, 0, portLoc(1, seq_445));
// allocate args at core: 1996

Term sentinel_444_1;
sentinel_444 = dupeArg(sentinel_444, &sentinel_444_1, 0);
Term seq_449 = makePair(LAM, 0, SUB, NUL);
Location r_seq_449 = portLoc(2, seq_449);
Term r_448 = newTerm(VAR, 0, portLoc(1, seq_449));
// allocate args at core: 1997

Term r_448_1;
r_448 = dupeArg(r_448, &r_448_1, 0);
Term glblVal25_22;
glblVal25 = dupeArg(glblVal25, &glblVal25_22, 0);

Term glblRslt16_10;
glblRslt16 = dupeArg(glblRslt16, &glblRslt16_10, 0);

// call = at core: 1998
Term Rslt_451Args = makePair(APP, 0, r_448, SUB);
Term Rslt_451 = newTerm(VAR, 0, portLoc(2, Rslt_451Args));
Rslt_451Args = makePair(APP, 0, sentinel_444, Rslt_451Args);
#ifdef STRICT
pushRedex(Rslt_451Args, newRef(glblProto20_1508));
#else
swap(termLoc(Rslt_451), makePair(LAZ, 0, Rslt_451Args, newRef(glblProto20_1508)));
#endif
// call Some at core: 2000
Term Rslt_452Args = makePair(APP, 0, r_448_1, SUB);
Term Rslt_452 = newTerm(VAR, 0, portLoc(2, Rslt_452Args));
#ifdef STRICT
pushRedex(Rslt_452Args, glblVal25_22);
#else
swap(termLoc(Rslt_452), makePair(LAZ, 0, Rslt_452Args, glblVal25_22));
#endif

// call cond at core: 1998
Term Rslt_453Args = makePair(APP, 0, Rslt_452, SUB);
Term Rslt_453 = newTerm(VAR, 0, portLoc(2, Rslt_453Args));
Rslt_453Args = makePair(APP, 0, glblRslt16_10, Rslt_453Args);
Rslt_453Args = makePair(APP, 0, Rslt_451, Rslt_453Args);
#ifdef STRICT
pushRedex(Rslt_453Args, newRef(glblProto34_1514));
#else
swap(termLoc(Rslt_453), makePair(LAZ, 0, Rslt_453Args, newRef(glblProto34_1514)));
#endif
// link args to body
swap(r_seq_449, Rslt_453);

Term Rslt_453V = get(termLoc(Rslt_453));
apps = take(portLoc(1, Rslt_453V));
lams = take(portLoc(2, Rslt_453V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_453), makePair(LAZ, 0, apps, lams));
// call get* at core: 1997
Term Rslt_454Args = makePair(APP, 0, newI60(0), SUB);
Term Rslt_454 = newTerm(VAR, 0, portLoc(2, Rslt_454Args));
Rslt_454Args = makePair(APP, 0, h_440, Rslt_454Args);
Rslt_454Args = makePair(APP, 0, sentinel_444_1, Rslt_454Args);
Rslt_454Args = makePair(APP, 0, k_436, Rslt_454Args);
Rslt_454Args = makePair(APP, 0, m_435, Rslt_454Args);
#ifdef STRICT
pushRedex(Rslt_454Args, newRef(glblProto113_1518));
#else
swap(termLoc(Rslt_454), makePair(LAZ, 0, Rslt_454Args, newRef(glblProto113_1518)));
#endif
// call  at core: 1997
Term Rslt_455Args = makePair(APP, 0, Rslt_454, SUB);
Term Rslt_455 = newTerm(VAR, 0, portLoc(2, Rslt_455Args));
#ifdef STRICT
pushRedex(Rslt_455Args, seq_449);
#else
swap(termLoc(Rslt_455), makePair(LAZ, 0, Rslt_455Args, seq_449));
#endif

// link args to body
swap(r_seq_445, Rslt_455);

Term Rslt_455V = get(termLoc(Rslt_455));
apps = take(portLoc(1, Rslt_455V));
lams = take(portLoc(2, Rslt_455V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_455), makePair(LAZ, 0, apps, lams));
// call  at core: 1996
Term Rslt_456Args = makePair(APP, 0, glblRslt1181_2, SUB);
Term Rslt_456 = newTerm(VAR, 0, portLoc(2, Rslt_456Args));
#ifdef STRICT
pushRedex(Rslt_456Args, seq_445);
#else
swap(termLoc(Rslt_456), makePair(LAZ, 0, Rslt_456Args, seq_445));
#endif

// link args to body
swap(r_seq_441, Rslt_456);

Term Rslt_456V = get(termLoc(Rslt_456));
apps = take(portLoc(1, Rslt_456V));
lams = take(portLoc(2, Rslt_456V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_456), makePair(LAZ, 0, apps, lams));
// call sha1 at core: 1995
Term Rslt_457Args = makePair(APP, 0, k_436_1, SUB);
Term Rslt_457 = newTerm(VAR, 0, portLoc(2, Rslt_457Args));
#ifdef STRICT
pushRedex(Rslt_457Args, newRef(glblProto104_1522));
#else
swap(termLoc(Rslt_457), makePair(LAZ, 0, Rslt_457Args, newRef(glblProto104_1522)));
#endif
// call  at core: 1995
Term Rslt_458Args = makePair(APP, 0, Rslt_457, SUB);
Term Rslt_458 = newTerm(VAR, 0, portLoc(2, Rslt_458Args));
#ifdef STRICT
pushRedex(Rslt_458Args, seq_441);
#else
swap(termLoc(Rslt_458), makePair(LAZ, 0, Rslt_458Args, seq_441));
#endif

// link args to body
swap(r_seq_437, Rslt_458);

Term Rslt_458V = get(termLoc(Rslt_458));
apps = take(portLoc(1, Rslt_458V));
lams = take(portLoc(2, Rslt_458V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_458), makePair(LAZ, 0, apps, lams));
pushRedex(args, seq_437);
return;
}
Term glblget1287 = newRef(glblget1286);
Term glblProto1515;
Term glblProto1512;
Term glblProto1510;
void glblConstructor1520(Term ref, Term args) {
Term callArgs = makePair(APP, 0, newI60(3), args);
callArgs = makePair(APP, 0, newI60(50), callArgs);
interact(callArgs, construct);
return;
}
void glblProto20_1530(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(1998), args);
  args = makePair(APP, 0, glblStr199, args);
  interact(args, glblProto20);
}
}
void glblProto34_1533(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(1998), args);
  args = makePair(APP, 0, glblStr199, args);
  interact(args, glblProto34);
}
}
void glblProto113_1535(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(1997), args);
  args = makePair(APP, 0, glblStr199, args);
  interact(args, glblProto113);
}
}
void glblProto104_1537(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(1995), args);
  args = makePair(APP, 0, glblStr199, args);
  interact(args, glblProto104);
}
}
Term glblget1289;
void glblget1288(Term ref, Term args) {
Term apps, lams, V;
Location vLoc;
Term seq_462 = makePair(LAM, 0, SUB, NUL);
Location r_seq_462 = portLoc(2, seq_462);
Term k_461 = newTerm(VAR, 0, portLoc(1, seq_462));
seq_462 = makePair(LAM, 0, SUB, seq_462);
Term m_460 = newTerm(VAR, 0, portLoc(1, seq_462));
// allocate args at core: 1994

Term k_461_1;
k_461 = dupeArg(k_461, &k_461_1, 245);
Term seq_466 = makePair(LAM, 0, SUB, NUL);
Location r_seq_466 = portLoc(2, seq_466);
Term h_465 = newTerm(VAR, 0, portLoc(1, seq_466));
// allocate args at core: 1995
Term glblRslt1181_3;
glblRslt1181 = dupeArg(glblRslt1181, &glblRslt1181_3, 0);

Term seq_470 = makePair(LAM, 0, SUB, NUL);
Location r_seq_470 = portLoc(2, seq_470);
Term sentinel_469 = newTerm(VAR, 0, portLoc(1, seq_470));
// allocate args at core: 1996

Term sentinel_469_1;
sentinel_469 = dupeArg(sentinel_469, &sentinel_469_1, 0);
Term seq_474 = makePair(LAM, 0, SUB, NUL);
Location r_seq_474 = portLoc(2, seq_474);
Term r_473 = newTerm(VAR, 0, portLoc(1, seq_474));
// allocate args at core: 1997

Term r_473_1;
r_473 = dupeArg(r_473, &r_473_1, 0);
Term glblVal25_23;
glblVal25 = dupeArg(glblVal25, &glblVal25_23, 0);

Term glblRslt16_11;
glblRslt16 = dupeArg(glblRslt16, &glblRslt16_11, 0);

// call = at core: 1998
Term Rslt_476Args = makePair(APP, 0, r_473, SUB);
Term Rslt_476 = newTerm(VAR, 0, portLoc(2, Rslt_476Args));
Rslt_476Args = makePair(APP, 0, sentinel_469, Rslt_476Args);
#ifdef STRICT
pushRedex(Rslt_476Args, newRef(glblProto20_1530));
#else
swap(termLoc(Rslt_476), makePair(LAZ, 0, Rslt_476Args, newRef(glblProto20_1530)));
#endif
// call Some at core: 2000
Term Rslt_477Args = makePair(APP, 0, r_473_1, SUB);
Term Rslt_477 = newTerm(VAR, 0, portLoc(2, Rslt_477Args));
#ifdef STRICT
pushRedex(Rslt_477Args, glblVal25_23);
#else
swap(termLoc(Rslt_477), makePair(LAZ, 0, Rslt_477Args, glblVal25_23));
#endif

// call cond at core: 1998
Term Rslt_478Args = makePair(APP, 0, Rslt_477, SUB);
Term Rslt_478 = newTerm(VAR, 0, portLoc(2, Rslt_478Args));
Rslt_478Args = makePair(APP, 0, glblRslt16_11, Rslt_478Args);
Rslt_478Args = makePair(APP, 0, Rslt_476, Rslt_478Args);
#ifdef STRICT
pushRedex(Rslt_478Args, newRef(glblProto34_1533));
#else
swap(termLoc(Rslt_478), makePair(LAZ, 0, Rslt_478Args, newRef(glblProto34_1533)));
#endif
// link args to body
swap(r_seq_474, Rslt_478);

Term Rslt_478V = get(termLoc(Rslt_478));
apps = take(portLoc(1, Rslt_478V));
lams = take(portLoc(2, Rslt_478V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_478), makePair(LAZ, 0, apps, lams));
// call get* at core: 1997
Term Rslt_479Args = makePair(APP, 0, newI60(0), SUB);
Term Rslt_479 = newTerm(VAR, 0, portLoc(2, Rslt_479Args));
Rslt_479Args = makePair(APP, 0, h_465, Rslt_479Args);
Rslt_479Args = makePair(APP, 0, sentinel_469_1, Rslt_479Args);
Rslt_479Args = makePair(APP, 0, k_461, Rslt_479Args);
Rslt_479Args = makePair(APP, 0, m_460, Rslt_479Args);
#ifdef STRICT
pushRedex(Rslt_479Args, newRef(glblProto113_1535));
#else
swap(termLoc(Rslt_479), makePair(LAZ, 0, Rslt_479Args, newRef(glblProto113_1535)));
#endif
// call  at core: 1997
Term Rslt_480Args = makePair(APP, 0, Rslt_479, SUB);
Term Rslt_480 = newTerm(VAR, 0, portLoc(2, Rslt_480Args));
#ifdef STRICT
pushRedex(Rslt_480Args, seq_474);
#else
swap(termLoc(Rslt_480), makePair(LAZ, 0, Rslt_480Args, seq_474));
#endif

// link args to body
swap(r_seq_470, Rslt_480);

Term Rslt_480V = get(termLoc(Rslt_480));
apps = take(portLoc(1, Rslt_480V));
lams = take(portLoc(2, Rslt_480V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_480), makePair(LAZ, 0, apps, lams));
// call  at core: 1996
Term Rslt_481Args = makePair(APP, 0, glblRslt1181_3, SUB);
Term Rslt_481 = newTerm(VAR, 0, portLoc(2, Rslt_481Args));
#ifdef STRICT
pushRedex(Rslt_481Args, seq_470);
#else
swap(termLoc(Rslt_481), makePair(LAZ, 0, Rslt_481Args, seq_470));
#endif

// link args to body
swap(r_seq_466, Rslt_481);

Term Rslt_481V = get(termLoc(Rslt_481));
apps = take(portLoc(1, Rslt_481V));
lams = take(portLoc(2, Rslt_481V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_481), makePair(LAZ, 0, apps, lams));
// call sha1 at core: 1995
Term Rslt_482Args = makePair(APP, 0, k_461_1, SUB);
Term Rslt_482 = newTerm(VAR, 0, portLoc(2, Rslt_482Args));
#ifdef STRICT
pushRedex(Rslt_482Args, newRef(glblProto104_1537));
#else
swap(termLoc(Rslt_482), makePair(LAZ, 0, Rslt_482Args, newRef(glblProto104_1537)));
#endif
// call  at core: 1995
Term Rslt_483Args = makePair(APP, 0, Rslt_482, SUB);
Term Rslt_483 = newTerm(VAR, 0, portLoc(2, Rslt_483Args));
#ifdef STRICT
pushRedex(Rslt_483Args, seq_466);
#else
swap(termLoc(Rslt_483), makePair(LAZ, 0, Rslt_483Args, seq_466));
#endif

// link args to body
swap(r_seq_462, Rslt_483);

Term Rslt_483V = get(termLoc(Rslt_483));
apps = take(portLoc(1, Rslt_483V));
lams = take(portLoc(2, Rslt_483V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_483), makePair(LAZ, 0, apps, lams));
pushRedex(args, seq_462);
return;
}
Term glblget1289 = newRef(glblget1288);
Term glblVal1332 = newRef(glblEndOfList1328);
Term glblVal1332;
Term glblRslt1330;
Term glblrecurse1323;
void glblrecurse1322(Term ref, Term args) {
Term apps, lams, V;
Location vLoc;
Term seq_3 = makePair(LAM, 0, SUB, NUL);
Location r_seq_3 = portLoc(2, seq_3);
Term f_2 = newTerm(VAR, 0, portLoc(1, seq_3));
seq_3 = makePair(LAM, 0, SUB, seq_3);
Term l_1 = newTerm(VAR, 0, portLoc(1, seq_3));
// allocate args at core: 2004
swap(termLoc(f_2), ERA);
// link args to body
swap(r_seq_3, l_1);

V = get(r_seq_3);
apps = makePair(APP, 0, V, SUB);
lams = makePair(LAM, 0, SUB, NUL);
swap(portLoc(2, lams),newTerm(VAR, 0, portLoc(1, lams)));
vLoc = portLoc(2, apps);
swap(vLoc, makePair(LAZ, 0, apps, lams));
swap(r_seq_3, newTerm(VAR, 0, vLoc));
pushRedex(args, seq_3);
return;
}
Term glblrecurse1323 = newRef(glblrecurse1322);
Term glblcount1327;
void glblcount1325(Term ref, Term args) {
Term apps, lams, V;
Location vLoc;
Term seq_7 = makePair(LAM, 0, SUB, NUL);
Location r_seq_7 = portLoc(2, seq_7);
Term l_6 = newTerm(VAR, 0, portLoc(1, seq_7));
// allocate args at core: 2010
swap(termLoc(l_6), ERA);
// link args to body
swap(r_seq_7, newI60(0));

V = get(r_seq_7);
apps = makePair(APP, 0, V, SUB);
lams = makePair(LAM, 0, SUB, NUL);
swap(portLoc(2, lams),newTerm(VAR, 0, portLoc(1, lams)));
vLoc = portLoc(2, apps);
swap(vLoc, makePair(LAZ, 0, apps, lams));
swap(r_seq_7, newTerm(VAR, 0, vLoc));
pushRedex(args, seq_7);
return;
}
Term glblcount1327 = newRef(glblcount1325);
Term glblVal1389 = newRef(glblCons1375);
Term glblVal1389;
Term glblrecurse1369;
void glblrecurse1367(Term ref, Term args) {
Term apps, lams, V;
Location vLoc;
Term seq_3 = makePair(LAM, 0, SUB, NUL);
Location r_seq_3 = portLoc(2, seq_3);
Term f_2 = newTerm(VAR, 0, portLoc(1, seq_3));
seq_3 = makePair(LAM, 0, SUB, seq_3);
Term arg_1 = newTerm(VAR, 0, portLoc(1, seq_3));
// allocate args at core: 2017

Term arg_1_2;
arg_1 = dupeArg(arg_1, &arg_1_2, 246);

Term arg_1_1;
arg_1 = dupeArg(arg_1, &arg_1_1, 247);
Term arg_1_2Args = makePair(APP, 0, arg_1_2, SUB);
Term Rslt_8 = newTerm(VAR, 0, portLoc(2, arg_1_2Args));
arg_1_2Args = makePair(APP, 0, newI60(2), arg_1_2Args);
pushRedex(arg_1_2Args, accessField);
Term arg_1_1Args = makePair(APP, 0, arg_1_1, SUB);
Term Rslt_6 = newTerm(VAR, 0, portLoc(2, arg_1_1Args));
arg_1_1Args = makePair(APP, 0, newI60(1), arg_1_1Args);
pushRedex(arg_1_1Args, accessField);
Term arg_1Args = makePair(APP, 0, arg_1, SUB);
Term Rslt_5 = newTerm(VAR, 0, portLoc(2, arg_1Args));
arg_1Args = makePair(APP, 0, newI60(0), arg_1Args);
pushRedex(arg_1Args, accessField);
Term glblVal1389_1;
glblVal1389 = dupeArg(glblVal1389, &glblVal1389_1, 0);

// call f at core: 2017
Term Rslt_7Args = makePair(APP, 0, Rslt_6, SUB);
Term Rslt_7 = newTerm(VAR, 0, portLoc(2, Rslt_7Args));
#ifdef STRICT
pushRedex(Rslt_7Args, f_2);
#else
swap(termLoc(Rslt_7), makePair(LAZ, 0, Rslt_7Args, f_2));
#endif

// call Cons at core: 2018
Term Rslt_9Args = makePair(APP, 0, Rslt_8, SUB);
Term Rslt_9 = newTerm(VAR, 0, portLoc(2, Rslt_9Args));
Rslt_9Args = makePair(APP, 0, Rslt_7, Rslt_9Args);
Rslt_9Args = makePair(APP, 0, Rslt_5, Rslt_9Args);
#ifdef STRICT
pushRedex(Rslt_9Args, glblVal1389_1);
#else
swap(termLoc(Rslt_9), makePair(LAZ, 0, Rslt_9Args, glblVal1389_1));
#endif

// link args to body
swap(r_seq_3, Rslt_9);

Term Rslt_9V = get(termLoc(Rslt_9));
apps = take(portLoc(1, Rslt_9V));
lams = take(portLoc(2, Rslt_9V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_9), makePair(LAZ, 0, apps, lams));
pushRedex(args, seq_3);
return;
}
Term glblrecurse1369 = newRef(glblrecurse1367);
void glblProto386_1543(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(2024), args);
  args = makePair(APP, 0, glblStr199, args);
  interact(args, glblProto386);
}
}
Term glblcount1373;
void glblcount1371(Term ref, Term args) {
Term apps, lams, V;
Location vLoc;
Term seq_12 = makePair(LAM, 0, SUB, NUL);
Location r_seq_12 = portLoc(2, seq_12);
Term l_11 = newTerm(VAR, 0, portLoc(1, seq_12));
// allocate args at core: 2023
// call .len at core: 2024
Term Rslt_14Args = makePair(APP, 0, l_11, SUB);
Term Rslt_14 = newTerm(VAR, 0, portLoc(2, Rslt_14Args));
#ifdef STRICT
pushRedex(Rslt_14Args, newRef(glblProto386_1543));
#else
swap(termLoc(Rslt_14), makePair(LAZ, 0, Rslt_14Args, newRef(glblProto386_1543)));
#endif
// link args to body
swap(r_seq_12, Rslt_14);

Term Rslt_14V = get(termLoc(Rslt_14));
apps = take(portLoc(1, Rslt_14V));
lams = take(portLoc(2, Rslt_14V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_14), makePair(LAZ, 0, apps, lams));
pushRedex(args, seq_12);
return;
}
Term glblcount1373 = newRef(glblcount1371);
Term glblFld1379;
void glblFldFn1378(Term ref, Term args) {
args = makePair(APP, 0, newI60(0), args);
pushRedex(args, accessField);
return;
}
Term glblFld1379 = newRef(glblFldFn1378);
Term glblFld1382;
void glblFldFn1381(Term ref, Term args) {
args = makePair(APP, 0, newI60(1), args);
pushRedex(args, accessField);
return;
}
Term glblFld1382 = newRef(glblFldFn1381);
Term glblFld1384;
void glblFldFn1383(Term ref, Term args) {
args = makePair(APP, 0, newI60(2), args);
pushRedex(args, accessField);
return;
}
Term glblFld1384 = newRef(glblFldFn1383);
void glblProto64_1550(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(2028), args);
  args = makePair(APP, 0, glblStr199, args);
  interact(args, glblProto64);
}
}
Term glblFn1315;
void glblCFn1314(Term ref, Term args) {
Term apps, lams, V;
Location vLoc;
Term seq_3 = makePair(LAM, 0, SUB, NUL);
Location r_seq_3 = portLoc(2, seq_3);
Term l_2 = newTerm(VAR, 0, portLoc(1, seq_3));
seq_3 = makePair(LAM, 0, SUB, seq_3);
Term v_1 = newTerm(VAR, 0, portLoc(1, seq_3));
// allocate args at core: 2026

Term l_2_1;
l_2 = dupeArg(l_2, &l_2_1, 248);
Term glblVal1389_2;
glblVal1389 = dupeArg(glblVal1389, &glblVal1389_2, 0);

// call count at core: 2028
Term Rslt_5Args = makePair(APP, 0, l_2_1, SUB);
Term Rslt_5 = newTerm(VAR, 0, portLoc(2, Rslt_5Args));
#ifdef STRICT
pushRedex(Rslt_5Args, newRef(glblProto64_1550));
#else
swap(termLoc(Rslt_5), makePair(LAZ, 0, Rslt_5Args, newRef(glblProto64_1550)));
#endif
// call inc at core: 2028
Term Rslt_6Args = makePair(APP, 0, Rslt_5, SUB);
Term Rslt_6 = newTerm(VAR, 0, portLoc(2, Rslt_6Args));
#ifdef STRICT
pushRedex(Rslt_6Args, glblFn141);
#else
swap(termLoc(Rslt_6), makePair(LAZ, 0, Rslt_6Args, glblFn141));
#endif

// call Cons at core: 2028
Term Rslt_7Args = makePair(APP, 0, Rslt_6, SUB);
Term Rslt_7 = newTerm(VAR, 0, portLoc(2, Rslt_7Args));
Rslt_7Args = makePair(APP, 0, l_2, Rslt_7Args);
Rslt_7Args = makePair(APP, 0, v_1, Rslt_7Args);
#ifdef STRICT
pushRedex(Rslt_7Args, glblVal1389_2);
#else
swap(termLoc(Rslt_7), makePair(LAZ, 0, Rslt_7Args, glblVal1389_2));
#endif

// link args to body
swap(r_seq_3, Rslt_7);

Term Rslt_7V = get(termLoc(Rslt_7));
apps = take(portLoc(1, Rslt_7V));
lams = take(portLoc(2, Rslt_7V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_7), makePair(LAZ, 0, apps, lams));
pushRedex(args, seq_3);
return;
}
Term glblFn1315 = newRef(glblCFn1314);
Term glblFn1317;
void glblCFn1316(Term ref, Term args) {
Term apps, lams, V;
Location vLoc;
Term seq_2 = makePair(LAM, 0, SUB, NUL);
Location r_seq_2 = portLoc(2, seq_2);
Term vs_1 = newTerm(VAR, 0, portLoc(1, seq_2));
// allocate args at core: 2030
// call to-str at core: 2034
Term Rslt_4Args = makePair(APP, 0, vs_1, SUB);
Term Rslt_4 = newTerm(VAR, 0, portLoc(2, Rslt_4Args));
#ifdef STRICT
pushRedex(Rslt_4Args, glblFn656);
#else
swap(termLoc(Rslt_4), makePair(LAZ, 0, Rslt_4Args, glblFn656));
#endif

// link args to body
swap(r_seq_2, Rslt_4);

Term Rslt_4V = get(termLoc(Rslt_4));
apps = take(portLoc(1, Rslt_4V));
lams = take(portLoc(2, Rslt_4V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_4), makePair(LAZ, 0, apps, lams));
pushRedex(args, seq_2);
return;
}
Term glblFn1317 = newRef(glblCFn1316);
Value *(*type_name)(FnArity *, Value *) = noImpl1;
Value *(*zero)(FnArity *, Value *) = noImpl1;
Value *(*invoke0Args)(FnArity *, Value *) = noImpl1;
Value *(*invoke1Arg)(FnArity *, Value *,Value *) = noImpl2;
Value *(*invoke2Args)(FnArity *, Value *,Value *,Value *) = noImpl3;
Value *(*equalSTAR)(FnArity *, Value *,Value *) = noImpl2;
Value *(*hashSeq)(FnArity *, Value *,Value *) = noImpl2;
Value *(*count)(FnArity *, Value *) = noImpl1;
Value *(*vals)(FnArity *, Value *) = noImpl1;
Value *(*first)(FnArity *, Value *) = noImpl1;
Value *(*rest)(FnArity *, Value *) = noImpl1;
Value *(*seq)(FnArity *, Value *) = noImpl1;
Value *(*hasField)(FnArity *, Value *,Value *) = noImpl2;
Value *(*showFn)(FnArity *, Value *) = noImpl1;
Value *(*newHashSet)(FnArity *, Value *) = noImpl1;
Value *(*fn_apply)(FnArity *, Value *,Value *) = noImpl2;
Value *(*prValue)(FnArity *, Value *) = noImpl1;
Term glblVal1489 = newRef(glblLocation1480);
Term glblVal1489;
Term glblFld1483;
void glblFldFn1482(Term ref, Term args) {
args = makePair(APP, 0, newI60(0), args);
pushRedex(args, accessField);
return;
}
Term glblFld1483 = newRef(glblFldFn1482);
Term glblFld1486;
void glblFldFn1484(Term ref, Term args) {
args = makePair(APP, 0, newI60(1), args);
pushRedex(args, accessField);
return;
}
Term glblFld1486 = newRef(glblFldFn1484);
Term glblVal1504 = newRef(glblModule1500);
Term glblVal1504;
Term glblFld1503;
void glblFldFn1502(Term ref, Term args) {
args = makePair(APP, 0, newI60(0), args);
pushRedex(args, accessField);
return;
}
Term glblFld1503 = newRef(glblFldFn1502);
Term glblVal1531 = newRef(glblConstructor1520);
Term glblVal1531;
Term glblFld1523;
void glblFldFn1521(Term ref, Term args) {
args = makePair(APP, 0, newI60(0), args);
pushRedex(args, accessField);
return;
}
Term glblFld1523 = newRef(glblFldFn1521);
Term glblFld1526;
void glblFldFn1525(Term ref, Term args) {
args = makePair(APP, 0, newI60(1), args);
pushRedex(args, accessField);
return;
}
Term glblFld1526 = newRef(glblFldFn1525);
Term glblFld1529;
void glblFldFn1527(Term ref, Term args) {
args = makePair(APP, 0, newI60(2), args);
pushRedex(args, accessField);
return;
}
Term glblFld1529 = newRef(glblFldFn1527);
Term glblProto1548;
Term glblProto1650;
Term glblProto1614;
void glblInline1652(Term ref, Term args) {
Term callArgs = makePair(APP, 0, newI60(3), args);
callArgs = makePair(APP, 0, newI60(51), callArgs);
interact(callArgs, construct);
return;
}
Term glblProto1644;
void glblBlockComment1646(Term ref, Term args) {
Term callArgs = makePair(APP, 0, newI60(1), args);
callArgs = makePair(APP, 0, newI60(52), callArgs);
interact(callArgs, construct);
return;
}
Term glblProto1634;
Term glblProto1632;
Term glblProto1630;
void glblMatch1636(Term ref, Term args) {
Term callArgs = makePair(APP, 0, newI60(3), args);
callArgs = makePair(APP, 0, newI60(53), callArgs);
interact(callArgs, construct);
return;
}
Term glblProto1548;
Term glblProto1622;
void glblFieldGetter1624(Term ref, Term args) {
Term callArgs = makePair(APP, 0, newI60(2), args);
callArgs = makePair(APP, 0, newI60(54), callArgs);
interact(callArgs, construct);
return;
}
Term glblProto1614;
Term glblProto1612;
void glblTypeConstraint1616(Term ref, Term args) {
Term callArgs = makePair(APP, 0, newI60(2), args);
callArgs = makePair(APP, 0, newI60(55), callArgs);
interact(callArgs, construct);
return;
}
Term glblProto1606;
void glblSuperposition1608(Term ref, Term args) {
Term callArgs = makePair(APP, 0, newI60(1), args);
callArgs = makePair(APP, 0, newI60(56), callArgs);
interact(callArgs, construct);
return;
}
Term glblProto1548;
Term glblProto1594;
Term glblProto1592;
Term glblProto1510;
void glblFn1596(Term ref, Term args) {
Term callArgs = makePair(APP, 0, newI60(4), args);
callArgs = makePair(APP, 0, newI60(57), callArgs);
interact(callArgs, construct);
return;
}
Term glblProto1584;
Term glblProto1582;
void glblCall1586(Term ref, Term args) {
Term callArgs = makePair(APP, 0, newI60(2), args);
callArgs = makePair(APP, 0, newI60(58), callArgs);
interact(callArgs, construct);
return;
}
Term glblProto1548;
Term glblProto1562;
void glblStringLit1576(Term ref, Term args) {
Term callArgs = makePair(APP, 0, newI60(2), args);
callArgs = makePair(APP, 0, newI60(59), callArgs);
interact(callArgs, construct);
return;
}
Term glblProto1548;
Term glblProto1562;
void glblFloatLit1570(Term ref, Term args) {
Term callArgs = makePair(APP, 0, newI60(2), args);
callArgs = makePair(APP, 0, newI60(60), callArgs);
interact(callArgs, construct);
return;
}
Term glblProto1548;
Term glblProto1562;
void glblIntegerLit1564(Term ref, Term args) {
Term callArgs = makePair(APP, 0, newI60(2), args);
callArgs = makePair(APP, 0, newI60(61), callArgs);
interact(callArgs, construct);
return;
}
Term glblProto1548;
Term glblProto1510;
Term glblProto1545;
void glblSymbol1551(Term ref, Term args) {
Term callArgs = makePair(APP, 0, newI60(3), args);
callArgs = makePair(APP, 0, newI60(62), callArgs);
interact(callArgs, construct);
return;
}
Term glblVal1560 = newRef(glblSymbol1551);
Term glblVal1560;
Term glblFld1554;
void glblFldFn1553(Term ref, Term args) {
args = makePair(APP, 0, newI60(0), args);
pushRedex(args, accessField);
return;
}
Term glblFld1554 = newRef(glblFldFn1553);
Term glblFld1556;
void glblFldFn1555(Term ref, Term args) {
args = makePair(APP, 0, newI60(1), args);
pushRedex(args, accessField);
return;
}
Term glblFld1556 = newRef(glblFldFn1555);
Term glblFld1559;
void glblFldFn1558(Term ref, Term args) {
args = makePair(APP, 0, newI60(2), args);
pushRedex(args, accessField);
return;
}
Term glblFld1559 = newRef(glblFldFn1558);
Term glblVal1569 = newRef(glblIntegerLit1564);
Term glblVal1569;
Term glblFld1566;
void glblFldFn1565(Term ref, Term args) {
args = makePair(APP, 0, newI60(0), args);
pushRedex(args, accessField);
return;
}
Term glblFld1566 = newRef(glblFldFn1565);
Term glblFld1568;
void glblFldFn1567(Term ref, Term args) {
args = makePair(APP, 0, newI60(1), args);
pushRedex(args, accessField);
return;
}
Term glblFld1568 = newRef(glblFldFn1567);
Term glblVal1575 = newRef(glblFloatLit1570);
Term glblVal1575;
Term glblFld1572;
void glblFldFn1571(Term ref, Term args) {
args = makePair(APP, 0, newI60(0), args);
pushRedex(args, accessField);
return;
}
Term glblFld1572 = newRef(glblFldFn1571);
Term glblFld1574;
void glblFldFn1573(Term ref, Term args) {
args = makePair(APP, 0, newI60(1), args);
pushRedex(args, accessField);
return;
}
Term glblFld1574 = newRef(glblFldFn1573);
Term glblVal1581 = newRef(glblStringLit1576);
Term glblVal1581;
Term glblFld1578;
void glblFldFn1577(Term ref, Term args) {
args = makePair(APP, 0, newI60(0), args);
pushRedex(args, accessField);
return;
}
Term glblFld1578 = newRef(glblFldFn1577);
Term glblFld1580;
void glblFldFn1579(Term ref, Term args) {
args = makePair(APP, 0, newI60(1), args);
pushRedex(args, accessField);
return;
}
Term glblFld1580 = newRef(glblFldFn1579);
Term glblVal1591 = newRef(glblCall1586);
Term glblVal1591;
Term glblFld1588;
void glblFldFn1587(Term ref, Term args) {
args = makePair(APP, 0, newI60(0), args);
pushRedex(args, accessField);
return;
}
Term glblFld1588 = newRef(glblFldFn1587);
Term glblFld1590;
void glblFldFn1589(Term ref, Term args) {
args = makePair(APP, 0, newI60(1), args);
pushRedex(args, accessField);
return;
}
Term glblFld1590 = newRef(glblFldFn1589);
Term glblVal1605 = newRef(glblFn1596);
Term glblVal1605;
Term glblFld1598;
void glblFldFn1597(Term ref, Term args) {
args = makePair(APP, 0, newI60(0), args);
pushRedex(args, accessField);
return;
}
Term glblFld1598 = newRef(glblFldFn1597);
Term glblFld1600;
void glblFldFn1599(Term ref, Term args) {
args = makePair(APP, 0, newI60(1), args);
pushRedex(args, accessField);
return;
}
Term glblFld1600 = newRef(glblFldFn1599);
Term glblFld1602;
void glblFldFn1601(Term ref, Term args) {
args = makePair(APP, 0, newI60(2), args);
pushRedex(args, accessField);
return;
}
Term glblFld1602 = newRef(glblFldFn1601);
Term glblFld1604;
void glblFldFn1603(Term ref, Term args) {
args = makePair(APP, 0, newI60(3), args);
pushRedex(args, accessField);
return;
}
Term glblFld1604 = newRef(glblFldFn1603);
Term glblVal1611 = newRef(glblSuperposition1608);
Term glblVal1611;
Term glblFld1610;
void glblFldFn1609(Term ref, Term args) {
args = makePair(APP, 0, newI60(0), args);
pushRedex(args, accessField);
return;
}
Term glblFld1610 = newRef(glblFldFn1609);
Term glblVal1621 = newRef(glblTypeConstraint1616);
Term glblVal1621;
Term glblFld1618;
void glblFldFn1617(Term ref, Term args) {
args = makePair(APP, 0, newI60(0), args);
pushRedex(args, accessField);
return;
}
Term glblFld1618 = newRef(glblFldFn1617);
Term glblFld1620;
void glblFldFn1619(Term ref, Term args) {
args = makePair(APP, 0, newI60(1), args);
pushRedex(args, accessField);
return;
}
Term glblFld1620 = newRef(glblFldFn1619);
Term glblVal1629 = newRef(glblFieldGetter1624);
Term glblVal1629;
Term glblFld1626;
void glblFldFn1625(Term ref, Term args) {
args = makePair(APP, 0, newI60(0), args);
pushRedex(args, accessField);
return;
}
Term glblFld1626 = newRef(glblFldFn1625);
Term glblFld1628;
void glblFldFn1627(Term ref, Term args) {
args = makePair(APP, 0, newI60(1), args);
pushRedex(args, accessField);
return;
}
Term glblFld1628 = newRef(glblFldFn1627);
Term glblVal1643 = newRef(glblMatch1636);
Term glblVal1643;
Term glblFld1638;
void glblFldFn1637(Term ref, Term args) {
args = makePair(APP, 0, newI60(0), args);
pushRedex(args, accessField);
return;
}
Term glblFld1638 = newRef(glblFldFn1637);
Term glblFld1640;
void glblFldFn1639(Term ref, Term args) {
args = makePair(APP, 0, newI60(1), args);
pushRedex(args, accessField);
return;
}
Term glblFld1640 = newRef(glblFldFn1639);
Term glblFld1642;
void glblFldFn1641(Term ref, Term args) {
args = makePair(APP, 0, newI60(2), args);
pushRedex(args, accessField);
return;
}
Term glblFld1642 = newRef(glblFldFn1641);
Term glblVal1649 = newRef(glblBlockComment1646);
Term glblVal1649;
Term glblFld1648;
void glblFldFn1647(Term ref, Term args) {
args = makePair(APP, 0, newI60(0), args);
pushRedex(args, accessField);
return;
}
Term glblFld1648 = newRef(glblFldFn1647);
Term glblVal1659 = newRef(glblInline1652);
Term glblVal1659;
Term glblFld1654;
void glblFldFn1653(Term ref, Term args) {
args = makePair(APP, 0, newI60(0), args);
pushRedex(args, accessField);
return;
}
Term glblFld1654 = newRef(glblFldFn1653);
Term glblFld1656;
void glblFldFn1655(Term ref, Term args) {
args = makePair(APP, 0, newI60(1), args);
pushRedex(args, accessField);
return;
}
Term glblFld1656 = newRef(glblFldFn1655);
Term glblFld1658;
void glblFldFn1657(Term ref, Term args) {
args = makePair(APP, 0, newI60(2), args);
pushRedex(args, accessField);
return;
}
Term glblFld1658 = newRef(glblFldFn1657);
Term glblProto1548;
Term glblProto1594;
Term glblProto1592;
void glblMain1704(Term ref, Term args) {
Term callArgs = makePair(APP, 0, newI60(3), args);
callArgs = makePair(APP, 0, newI60(63), callArgs);
interact(callArgs, construct);
return;
}
Term glblProto1696;
Term glblProto1694;
void glblAddNs1698(Term ref, Term args) {
Term callArgs = makePair(APP, 0, newI60(2), args);
callArgs = makePair(APP, 0, newI60(64), callArgs);
interact(callArgs, construct);
return;
}
Term glblProto1686;
Term glblProto1668;
void glblExtendType1688(Term ref, Term args) {
Term callArgs = makePair(APP, 0, newI60(2), args);
callArgs = makePair(APP, 0, newI60(65), callArgs);
interact(callArgs, construct);
return;
}
Term glblProto1594;
Term glblProto1592;
Term glblProto1510;
void glblDefp1678(Term ref, Term args) {
Term callArgs = makePair(APP, 0, newI60(3), args);
callArgs = makePair(APP, 0, newI60(66), callArgs);
interact(callArgs, construct);
return;
}
Term glblProto1670;
Term glblProto1668;
void glblDefType1672(Term ref, Term args) {
Term callArgs = makePair(APP, 0, newI60(2), args);
callArgs = makePair(APP, 0, newI60(67), callArgs);
interact(callArgs, construct);
return;
}
Term glblProto1548;
Term glblProto1562;
Term glblProto1510;
void glblDefinition1660(Term ref, Term args) {
Term callArgs = makePair(APP, 0, newI60(3), args);
callArgs = makePair(APP, 0, newI60(68), callArgs);
interact(callArgs, construct);
return;
}
Term glblVal1667 = newRef(glblDefinition1660);
Term glblVal1667;
Term glblFld1662;
void glblFldFn1661(Term ref, Term args) {
args = makePair(APP, 0, newI60(0), args);
pushRedex(args, accessField);
return;
}
Term glblFld1662 = newRef(glblFldFn1661);
Term glblFld1664;
void glblFldFn1663(Term ref, Term args) {
args = makePair(APP, 0, newI60(1), args);
pushRedex(args, accessField);
return;
}
Term glblFld1664 = newRef(glblFldFn1663);
Term glblFld1666;
void glblFldFn1665(Term ref, Term args) {
args = makePair(APP, 0, newI60(2), args);
pushRedex(args, accessField);
return;
}
Term glblFld1666 = newRef(glblFldFn1665);
Term glblVal1677 = newRef(glblDefType1672);
Term glblVal1677;
Term glblFld1674;
void glblFldFn1673(Term ref, Term args) {
args = makePair(APP, 0, newI60(0), args);
pushRedex(args, accessField);
return;
}
Term glblFld1674 = newRef(glblFldFn1673);
Term glblFld1676;
void glblFldFn1675(Term ref, Term args) {
args = makePair(APP, 0, newI60(1), args);
pushRedex(args, accessField);
return;
}
Term glblFld1676 = newRef(glblFldFn1675);
Term glblVal1685 = newRef(glblDefp1678);
Term glblVal1685;
Term glblFld1680;
void glblFldFn1679(Term ref, Term args) {
args = makePair(APP, 0, newI60(0), args);
pushRedex(args, accessField);
return;
}
Term glblFld1680 = newRef(glblFldFn1679);
Term glblFld1682;
void glblFldFn1681(Term ref, Term args) {
args = makePair(APP, 0, newI60(1), args);
pushRedex(args, accessField);
return;
}
Term glblFld1682 = newRef(glblFldFn1681);
Term glblFld1684;
void glblFldFn1683(Term ref, Term args) {
args = makePair(APP, 0, newI60(2), args);
pushRedex(args, accessField);
return;
}
Term glblFld1684 = newRef(glblFldFn1683);
Term glblVal1693 = newRef(glblExtendType1688);
Term glblVal1693;
Term glblFld1690;
void glblFldFn1689(Term ref, Term args) {
args = makePair(APP, 0, newI60(0), args);
pushRedex(args, accessField);
return;
}
Term glblFld1690 = newRef(glblFldFn1689);
Term glblFld1692;
void glblFldFn1691(Term ref, Term args) {
args = makePair(APP, 0, newI60(1), args);
pushRedex(args, accessField);
return;
}
Term glblFld1692 = newRef(glblFldFn1691);
Term glblVal1703 = newRef(glblAddNs1698);
Term glblVal1703;
void glblreturns1712(Term ref, Term args) {
Term callArgs = makePair(APP, 0, newI60(0), args);
callArgs = makePair(APP, 0, newI60(69), callArgs);
interact(callArgs, construct);
return;
}
Term glblFld1700;
void glblFldFn1699(Term ref, Term args) {
args = makePair(APP, 0, newI60(0), args);
pushRedex(args, accessField);
return;
}
Term glblFld1700 = newRef(glblFldFn1699);
Term glblFld1702;
void glblFldFn1701(Term ref, Term args) {
args = makePair(APP, 0, newI60(1), args);
pushRedex(args, accessField);
return;
}
Term glblFld1702 = newRef(glblFldFn1701);
Term glblVal1711 = newRef(glblMain1704);
Term glblVal1711;
Term glblFld1706;
void glblFldFn1705(Term ref, Term args) {
args = makePair(APP, 0, newI60(0), args);
pushRedex(args, accessField);
return;
}
Term glblFld1706 = newRef(glblFldFn1705);
Term glblFld1708;
void glblFldFn1707(Term ref, Term args) {
args = makePair(APP, 0, newI60(1), args);
pushRedex(args, accessField);
return;
}
Term glblFld1708 = newRef(glblFldFn1707);
Term glblFld1710;
void glblFldFn1709(Term ref, Term args) {
args = makePair(APP, 0, newI60(2), args);
pushRedex(args, accessField);
return;
}
Term glblFld1710 = newRef(glblFldFn1709);
Term glblVal1714 = newRef(glblreturns1712);
Term glblVal1714;
Term glblRslt1713;
Term glblProto1878;
Term glblProto1876;
void glblParserState1880(Term ref, Term args) {
Term callArgs = makePair(APP, 0, newI60(2), args);
callArgs = makePair(APP, 0, newI60(70), callArgs);
interact(callArgs, construct);
return;
}
Term glblVal1885 = newRef(glblParserState1880);
Term glblVal1885;
Term glblFld1882;
void glblFldFn1881(Term ref, Term args) {
args = makePair(APP, 0, newI60(0), args);
pushRedex(args, accessField);
return;
}
Term glblFld1882 = newRef(glblFldFn1881);
Term glblFld1884;
void glblFldFn1883(Term ref, Term args) {
args = makePair(APP, 0, newI60(1), args);
pushRedex(args, accessField);
return;
}
Term glblFld1884 = newRef(glblFldFn1883);
Term glblProto1889;
Term glblProto1901;
void glblParserError1903(Term ref, Term args) {
Term callArgs = makePair(APP, 0, newI60(2), args);
callArgs = makePair(APP, 0, newI60(71), callArgs);
interact(callArgs, construct);
return;
}
Term glblProto1889;
void glblParserIgnore1897(Term ref, Term args) {
Term callArgs = makePair(APP, 0, newI60(1), args);
callArgs = makePair(APP, 0, newI60(72), callArgs);
interact(callArgs, construct);
return;
}
Term glblProto1889;
Term glblProto1562;
void glblParserMatch1891(Term ref, Term args) {
Term callArgs = makePair(APP, 0, newI60(2), args);
callArgs = makePair(APP, 0, newI60(73), callArgs);
interact(callArgs, construct);
return;
}
void glblParserFail1886(Term ref, Term args) {
Term callArgs = makePair(APP, 0, newI60(0), args);
callArgs = makePair(APP, 0, newI60(74), callArgs);
interact(callArgs, construct);
return;
}
Term glblVal1888 = newRef(glblParserFail1886);
Term glblVal1888;
Term glblRslt1887;
Term glblVal1896 = newRef(glblParserMatch1891);
Term glblVal1896;
Term glblFld1893;
void glblFldFn1892(Term ref, Term args) {
args = makePair(APP, 0, newI60(0), args);
pushRedex(args, accessField);
return;
}
Term glblFld1893 = newRef(glblFldFn1892);
Term glblFld1895;
void glblFldFn1894(Term ref, Term args) {
args = makePair(APP, 0, newI60(1), args);
pushRedex(args, accessField);
return;
}
Term glblFld1895 = newRef(glblFldFn1894);
Term glblVal1900 = newRef(glblParserIgnore1897);
Term glblVal1900;
Term glblFld1899;
void glblFldFn1898(Term ref, Term args) {
args = makePair(APP, 0, newI60(0), args);
pushRedex(args, accessField);
return;
}
Term glblFld1899 = newRef(glblFldFn1898);
Term glblVal1908 = newRef(glblParserError1903);
Term glblVal1908;
Term glblFld1905;
void glblFldFn1904(Term ref, Term args) {
args = makePair(APP, 0, newI60(0), args);
pushRedex(args, accessField);
return;
}
Term glblFld1905 = newRef(glblFldFn1904);
Term glblFld1907;
void glblFldFn1906(Term ref, Term args) {
args = makePair(APP, 0, newI60(1), args);
pushRedex(args, accessField);
return;
}
Term glblFld1907 = newRef(glblFldFn1906);
struct {TYPE_SIZE type;
        REFS_SIZE refs;
        int64_t *hash;
        Value *parent;
        int64_t len;
        char buffer[5];
} str12 = {StringBufferType, REFS_STATIC, 0, 0, 4, "file"};
Term glblStr1910 = newTerm_(VAL, (Term)&str12);
struct {TYPE_SIZE type;
        REFS_SIZE refs;
        int64_t *hash;
        Value *parent;
        int64_t len;
        char buffer[44];
} str13 = {StringBufferType, REFS_STATIC, 0, 0, 43, "/home/jim/toccata/interpreter/intrp-rdr.toc"};
Term glblStr1912 = newTerm_(VAL, (Term)&str13);
void glblProto107_1911(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(40), args);
  args = makePair(APP, 0, glblStr1912, args);
  interact(args, glblProto107);
}
}
struct {TYPE_SIZE type;
        REFS_SIZE refs;
        int64_t *hash;
        Value *parent;
        int64_t len;
        char buffer[5];
} str14 = {StringBufferType, REFS_STATIC, 0, 0, 4, "line"};
Term glblStr1909 = newTerm_(VAL, (Term)&str14);
void glblProto107_1913(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(40), args);
  args = makePair(APP, 0, glblStr1912, args);
  interact(args, glblProto107);
}
}
Term glblFn1716;
void glblCFn1715(Term ref, Term args) {
Term apps, lams, V;
Location vLoc;
Term seq_4 = makePair(LAM, 0, SUB, NUL);
Location r_seq_4 = portLoc(2, seq_4);
Term line_3 = newTerm(VAR, 0, portLoc(1, seq_4));
seq_4 = makePair(LAM, 0, SUB, seq_4);
Term file_2 = newTerm(VAR, 0, portLoc(1, seq_4));
seq_4 = makePair(LAM, 0, SUB, seq_4);
Term input_1 = newTerm(VAR, 0, portLoc(1, seq_4));
// allocate args at /home/jim/toccata/interpreter/intrp-rdr.toc: 39
Term glblVal106_7;
glblVal106 = dupeArg(glblVal106, &glblVal106_7, 0);

Term glblVal1885_1;
glblVal1885 = dupeArg(glblVal1885, &glblVal1885_1, 0);

// call assoc at /home/jim/toccata/interpreter/intrp-rdr.toc: 40
Term Rslt_6Args = makePair(APP, 0, file_2, SUB);
Term Rslt_6 = newTerm(VAR, 0, portLoc(2, Rslt_6Args));
Rslt_6Args = makePair(APP, 0, glblStr1910, Rslt_6Args);
Rslt_6Args = makePair(APP, 0, glblVal106_7, Rslt_6Args);
#ifdef STRICT
pushRedex(Rslt_6Args, newRef(glblProto107_1911));
#else
swap(termLoc(Rslt_6), makePair(LAZ, 0, Rslt_6Args, newRef(glblProto107_1911)));
#endif
// call assoc at /home/jim/toccata/interpreter/intrp-rdr.toc: 40
Term Rslt_7Args = makePair(APP, 0, line_3, SUB);
Term Rslt_7 = newTerm(VAR, 0, portLoc(2, Rslt_7Args));
Rslt_7Args = makePair(APP, 0, glblStr1909, Rslt_7Args);
Rslt_7Args = makePair(APP, 0, Rslt_6, Rslt_7Args);
#ifdef STRICT
pushRedex(Rslt_7Args, newRef(glblProto107_1913));
#else
swap(termLoc(Rslt_7), makePair(LAZ, 0, Rslt_7Args, newRef(glblProto107_1913)));
#endif
// call ParserState at /home/jim/toccata/interpreter/intrp-rdr.toc: 40
Term Rslt_8Args = makePair(APP, 0, Rslt_7, SUB);
Term Rslt_8 = newTerm(VAR, 0, portLoc(2, Rslt_8Args));
Rslt_8Args = makePair(APP, 0, input_1, Rslt_8Args);
#ifdef STRICT
pushRedex(Rslt_8Args, glblVal1885_1);
#else
swap(termLoc(Rslt_8), makePair(LAZ, 0, Rslt_8Args, glblVal1885_1));
#endif

// link args to body
swap(r_seq_4, Rslt_8);

Term Rslt_8V = get(termLoc(Rslt_8));
apps = take(portLoc(1, Rslt_8V));
lams = take(portLoc(2, Rslt_8V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_8), makePair(LAZ, 0, apps, lams));
pushRedex(args, seq_4);
return;
}
Term glblFn1716 = newRef(glblCFn1715);
void glblProto1878_1917(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(44), args);
  args = makePair(APP, 0, glblStr1912, args);
  interact(args, glblProto1878);
}
}
void glblProto119_1919(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(44), args);
  args = makePair(APP, 0, glblStr1912, args);
  interact(args, glblProto119);
}
}
void glblProto56_1921(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(44), args);
  args = makePair(APP, 0, glblStr1912, args);
  interact(args, glblProto56);
}
}
Term glblFn1718;
void glblCFn1717(Term ref, Term args) {
Term apps, lams, V;
Location vLoc;
Term seq_2 = makePair(LAM, 0, SUB, NUL);
Location r_seq_2 = portLoc(2, seq_2);
Term state_1 = newTerm(VAR, 0, portLoc(1, seq_2));
// allocate args at /home/jim/toccata/interpreter/intrp-rdr.toc: 43
// call .values at /home/jim/toccata/interpreter/intrp-rdr.toc: 44
Term Rslt_4Args = makePair(APP, 0, state_1, SUB);
Term Rslt_4 = newTerm(VAR, 0, portLoc(2, Rslt_4Args));
#ifdef STRICT
pushRedex(Rslt_4Args, newRef(glblProto1878_1917));
#else
swap(termLoc(Rslt_4), makePair(LAZ, 0, Rslt_4Args, newRef(glblProto1878_1917)));
#endif
// call get at /home/jim/toccata/interpreter/intrp-rdr.toc: 44
Term Rslt_5Args = makePair(APP, 0, glblStr1909, SUB);
Term Rslt_5 = newTerm(VAR, 0, portLoc(2, Rslt_5Args));
Rslt_5Args = makePair(APP, 0, Rslt_4, Rslt_5Args);
#ifdef STRICT
pushRedex(Rslt_5Args, newRef(glblProto119_1919));
#else
swap(termLoc(Rslt_5), makePair(LAZ, 0, Rslt_5Args, newRef(glblProto119_1919)));
#endif
// call extract at /home/jim/toccata/interpreter/intrp-rdr.toc: 44
Term Rslt_6Args = makePair(APP, 0, Rslt_5, SUB);
Term Rslt_6 = newTerm(VAR, 0, portLoc(2, Rslt_6Args));
#ifdef STRICT
pushRedex(Rslt_6Args, newRef(glblProto56_1921));
#else
swap(termLoc(Rslt_6), makePair(LAZ, 0, Rslt_6Args, newRef(glblProto56_1921)));
#endif
// link args to body
swap(r_seq_2, Rslt_6);

Term Rslt_6V = get(termLoc(Rslt_6));
apps = take(portLoc(1, Rslt_6V));
lams = take(portLoc(2, Rslt_6V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_6), makePair(LAZ, 0, apps, lams));
pushRedex(args, seq_2);
return;
}
Term glblFn1718 = newRef(glblCFn1717);
void glblProto28_1924(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(52), args);
  args = makePair(APP, 0, glblStr1912, args);
  interact(args, glblProto28);
}
}
Term glblFn1720;
void glblCFn1719(Term ref, Term args) {
Term apps, lams, V;
Location vLoc;
Term seq_2 = makePair(LAM, 0, SUB, NUL);
Location r_seq_2 = portLoc(2, seq_2);
Term c_1 = newTerm(VAR, 0, portLoc(1, seq_2));
// allocate args at /home/jim/toccata/interpreter/intrp-rdr.toc: 50
Term seq_6 = makePair(LAM, 0, SUB, NUL);
Location r_seq_6 = portLoc(2, seq_6);
Term n_5 = newTerm(VAR, 0, portLoc(1, seq_6));
// allocate args at /home/jim/toccata/interpreter/intrp-rdr.toc: 51

Term n_5_1;
n_5 = dupeArg(n_5, &n_5_1, 0);
// call <= at /home/jim/toccata/interpreter/intrp-rdr.toc: 52
Term Rslt_8Args = makePair(APP, 0, n_5, SUB);
Term Rslt_8 = newTerm(VAR, 0, portLoc(2, Rslt_8Args));
Rslt_8Args = makePair(APP, 0, newI60(48), Rslt_8Args);
#ifdef STRICT
pushRedex(Rslt_8Args, glblFn261);
#else
swap(termLoc(Rslt_8), makePair(LAZ, 0, Rslt_8Args, glblFn261));
#endif

// call <= at /home/jim/toccata/interpreter/intrp-rdr.toc: 52
Term Rslt_9Args = makePair(APP, 0, newI60(57), SUB);
Term Rslt_9 = newTerm(VAR, 0, portLoc(2, Rslt_9Args));
Rslt_9Args = makePair(APP, 0, n_5_1, Rslt_9Args);
#ifdef STRICT
pushRedex(Rslt_9Args, glblFn261);
#else
swap(termLoc(Rslt_9), makePair(LAZ, 0, Rslt_9Args, glblFn261));
#endif

// call and at /home/jim/toccata/interpreter/intrp-rdr.toc: 52
Term Rslt_10Args = makePair(APP, 0, Rslt_9, SUB);
Term Rslt_10 = newTerm(VAR, 0, portLoc(2, Rslt_10Args));
Rslt_10Args = makePair(APP, 0, Rslt_8, Rslt_10Args);
#ifdef STRICT
pushRedex(Rslt_10Args, newRef(glblProto28_1924));
#else
swap(termLoc(Rslt_10), makePair(LAZ, 0, Rslt_10Args, newRef(glblProto28_1924)));
#endif
// link args to body
swap(r_seq_6, Rslt_10);

Term Rslt_10V = get(termLoc(Rslt_10));
apps = take(portLoc(1, Rslt_10V));
lams = take(portLoc(2, Rslt_10V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_10), makePair(LAZ, 0, apps, lams));
// call char-code at /home/jim/toccata/interpreter/intrp-rdr.toc: 51
Term Rslt_11Args = makePair(APP, 0, c_1, SUB);
Term Rslt_11 = newTerm(VAR, 0, portLoc(2, Rslt_11Args));
#ifdef STRICT
pushRedex(Rslt_11Args, glblFn378);
#else
swap(termLoc(Rslt_11), makePair(LAZ, 0, Rslt_11Args, glblFn378));
#endif

// call  at /home/jim/toccata/interpreter/intrp-rdr.toc: 51
Term Rslt_12Args = makePair(APP, 0, Rslt_11, SUB);
Term Rslt_12 = newTerm(VAR, 0, portLoc(2, Rslt_12Args));
#ifdef STRICT
pushRedex(Rslt_12Args, seq_6);
#else
swap(termLoc(Rslt_12), makePair(LAZ, 0, Rslt_12Args, seq_6));
#endif

// link args to body
swap(r_seq_2, Rslt_12);

Term Rslt_12V = get(termLoc(Rslt_12));
apps = take(portLoc(1, Rslt_12V));
lams = take(portLoc(2, Rslt_12V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_12), makePair(LAZ, 0, apps, lams));
pushRedex(args, seq_2);
return;
}
Term glblFn1720 = newRef(glblCFn1719);
void glblProto28_1927(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(56), args);
  args = makePair(APP, 0, glblStr1912, args);
  interact(args, glblProto28);
}
}
void glblProto28_1929(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(57), args);
  args = makePair(APP, 0, glblStr1912, args);
  interact(args, glblProto28);
}
}
void glblProto30_1931(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(56), args);
  args = makePair(APP, 0, glblStr1912, args);
  interact(args, glblProto30);
}
}
Term glblFn1722;
void glblCFn1721(Term ref, Term args) {
Term apps, lams, V;
Location vLoc;
Term seq_2 = makePair(LAM, 0, SUB, NUL);
Location r_seq_2 = portLoc(2, seq_2);
Term c_1 = newTerm(VAR, 0, portLoc(1, seq_2));
// allocate args at /home/jim/toccata/interpreter/intrp-rdr.toc: 54
Term seq_6 = makePair(LAM, 0, SUB, NUL);
Location r_seq_6 = portLoc(2, seq_6);
Term n_5 = newTerm(VAR, 0, portLoc(1, seq_6));
// allocate args at /home/jim/toccata/interpreter/intrp-rdr.toc: 55

Term n_5_3;
n_5 = dupeArg(n_5, &n_5_3, 0);

Term n_5_2;
n_5 = dupeArg(n_5, &n_5_2, 0);

Term n_5_1;
n_5 = dupeArg(n_5, &n_5_1, 0);
// call <= at /home/jim/toccata/interpreter/intrp-rdr.toc: 56
Term Rslt_8Args = makePair(APP, 0, n_5, SUB);
Term Rslt_8 = newTerm(VAR, 0, portLoc(2, Rslt_8Args));
Rslt_8Args = makePair(APP, 0, newI60(65), Rslt_8Args);
#ifdef STRICT
pushRedex(Rslt_8Args, glblFn261);
#else
swap(termLoc(Rslt_8), makePair(LAZ, 0, Rslt_8Args, glblFn261));
#endif

// call <= at /home/jim/toccata/interpreter/intrp-rdr.toc: 56
Term Rslt_9Args = makePair(APP, 0, newI60(90), SUB);
Term Rslt_9 = newTerm(VAR, 0, portLoc(2, Rslt_9Args));
Rslt_9Args = makePair(APP, 0, n_5_1, Rslt_9Args);
#ifdef STRICT
pushRedex(Rslt_9Args, glblFn261);
#else
swap(termLoc(Rslt_9), makePair(LAZ, 0, Rslt_9Args, glblFn261));
#endif

// call and at /home/jim/toccata/interpreter/intrp-rdr.toc: 56
Term Rslt_10Args = makePair(APP, 0, Rslt_9, SUB);
Term Rslt_10 = newTerm(VAR, 0, portLoc(2, Rslt_10Args));
Rslt_10Args = makePair(APP, 0, Rslt_8, Rslt_10Args);
#ifdef STRICT
pushRedex(Rslt_10Args, newRef(glblProto28_1927));
#else
swap(termLoc(Rslt_10), makePair(LAZ, 0, Rslt_10Args, newRef(glblProto28_1927)));
#endif
// call <= at /home/jim/toccata/interpreter/intrp-rdr.toc: 57
Term Rslt_11Args = makePair(APP, 0, n_5_2, SUB);
Term Rslt_11 = newTerm(VAR, 0, portLoc(2, Rslt_11Args));
Rslt_11Args = makePair(APP, 0, newI60(97), Rslt_11Args);
#ifdef STRICT
pushRedex(Rslt_11Args, glblFn261);
#else
swap(termLoc(Rslt_11), makePair(LAZ, 0, Rslt_11Args, glblFn261));
#endif

// call <= at /home/jim/toccata/interpreter/intrp-rdr.toc: 57
Term Rslt_12Args = makePair(APP, 0, newI60(122), SUB);
Term Rslt_12 = newTerm(VAR, 0, portLoc(2, Rslt_12Args));
Rslt_12Args = makePair(APP, 0, n_5_3, Rslt_12Args);
#ifdef STRICT
pushRedex(Rslt_12Args, glblFn261);
#else
swap(termLoc(Rslt_12), makePair(LAZ, 0, Rslt_12Args, glblFn261));
#endif

// call and at /home/jim/toccata/interpreter/intrp-rdr.toc: 57
Term Rslt_13Args = makePair(APP, 0, Rslt_12, SUB);
Term Rslt_13 = newTerm(VAR, 0, portLoc(2, Rslt_13Args));
Rslt_13Args = makePair(APP, 0, Rslt_11, Rslt_13Args);
#ifdef STRICT
pushRedex(Rslt_13Args, newRef(glblProto28_1929));
#else
swap(termLoc(Rslt_13), makePair(LAZ, 0, Rslt_13Args, newRef(glblProto28_1929)));
#endif
// call or at /home/jim/toccata/interpreter/intrp-rdr.toc: 56
Term Rslt_14Args = makePair(APP, 0, Rslt_13, SUB);
Term Rslt_14 = newTerm(VAR, 0, portLoc(2, Rslt_14Args));
Rslt_14Args = makePair(APP, 0, Rslt_10, Rslt_14Args);
#ifdef STRICT
pushRedex(Rslt_14Args, newRef(glblProto30_1931));
#else
swap(termLoc(Rslt_14), makePair(LAZ, 0, Rslt_14Args, newRef(glblProto30_1931)));
#endif
// link args to body
swap(r_seq_6, Rslt_14);

Term Rslt_14V = get(termLoc(Rslt_14));
apps = take(portLoc(1, Rslt_14V));
lams = take(portLoc(2, Rslt_14V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_14), makePair(LAZ, 0, apps, lams));
// call char-code at /home/jim/toccata/interpreter/intrp-rdr.toc: 55
Term Rslt_15Args = makePair(APP, 0, c_1, SUB);
Term Rslt_15 = newTerm(VAR, 0, portLoc(2, Rslt_15Args));
#ifdef STRICT
pushRedex(Rslt_15Args, glblFn378);
#else
swap(termLoc(Rslt_15), makePair(LAZ, 0, Rslt_15Args, glblFn378));
#endif

// call  at /home/jim/toccata/interpreter/intrp-rdr.toc: 55
Term Rslt_16Args = makePair(APP, 0, Rslt_15, SUB);
Term Rslt_16 = newTerm(VAR, 0, portLoc(2, Rslt_16Args));
#ifdef STRICT
pushRedex(Rslt_16Args, seq_6);
#else
swap(termLoc(Rslt_16), makePair(LAZ, 0, Rslt_16Args, seq_6));
#endif

// link args to body
swap(r_seq_2, Rslt_16);

Term Rslt_16V = get(termLoc(Rslt_16));
apps = take(portLoc(1, Rslt_16V));
lams = take(portLoc(2, Rslt_16V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_16), makePair(LAZ, 0, apps, lams));
pushRedex(args, seq_2);
return;
}
Term glblFn1722 = newRef(glblCFn1721);
struct {TYPE_SIZE type;
        REFS_SIZE refs;
        int64_t *hash;
        Value *parent;
        int64_t len;
        char buffer[2];
} str15 = {StringBufferType, REFS_STATIC, 0, 0, 1, "."};
Term glblStr1933 = newTerm_(VAL, (Term)&str15);
struct {TYPE_SIZE type;
        REFS_SIZE refs;
        int64_t *hash;
        Value *parent;
        int64_t len;
        char buffer[2];
} str16 = {StringBufferType, REFS_STATIC, 0, 0, 1, "_"};
Term glblStr1935 = newTerm_(VAL, (Term)&str16);
struct {TYPE_SIZE type;
        REFS_SIZE refs;
        int64_t *hash;
        Value *parent;
        int64_t len;
        char buffer[2];
} str17 = {StringBufferType, REFS_STATIC, 0, 0, 1, "<"};
Term glblStr1936 = newTerm_(VAL, (Term)&str17);
struct {TYPE_SIZE type;
        REFS_SIZE refs;
        int64_t *hash;
        Value *parent;
        int64_t len;
        char buffer[2];
} str18 = {StringBufferType, REFS_STATIC, 0, 0, 1, ">"};
Term glblStr1937 = newTerm_(VAL, (Term)&str18);
struct {TYPE_SIZE type;
        REFS_SIZE refs;
        int64_t *hash;
        Value *parent;
        int64_t len;
        char buffer[2];
} str19 = {StringBufferType, REFS_STATIC, 0, 0, 1, "="};
Term glblStr1938 = newTerm_(VAL, (Term)&str19);
struct {TYPE_SIZE type;
        REFS_SIZE refs;
        int64_t *hash;
        Value *parent;
        int64_t len;
        char buffer[2];
} str20 = {StringBufferType, REFS_STATIC, 0, 0, 1, "+"};
Term glblStr1939 = newTerm_(VAL, (Term)&str20);
struct {TYPE_SIZE type;
        REFS_SIZE refs;
        int64_t *hash;
        Value *parent;
        int64_t len;
        char buffer[2];
} str21 = {StringBufferType, REFS_STATIC, 0, 0, 1, "-"};
Term glblStr1940 = newTerm_(VAL, (Term)&str21);
struct {TYPE_SIZE type;
        REFS_SIZE refs;
        int64_t *hash;
        Value *parent;
        int64_t len;
        char buffer[2];
} str22 = {StringBufferType, REFS_STATIC, 0, 0, 1, "*"};
Term glblStr1941 = newTerm_(VAL, (Term)&str22);
struct {TYPE_SIZE type;
        REFS_SIZE refs;
        int64_t *hash;
        Value *parent;
        int64_t len;
        char buffer[2];
} str23 = {StringBufferType, REFS_STATIC, 0, 0, 1, "/"};
Term glblStr1942 = newTerm_(VAL, (Term)&str23);
void glblProto30_1944(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(69), args);
  args = makePair(APP, 0, glblStr1912, args);
  interact(args, glblProto30);
}
}
void glblProto30_1947(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(68), args);
  args = makePair(APP, 0, glblStr1912, args);
  interact(args, glblProto30);
}
}
void glblProto30_1949(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(67), args);
  args = makePair(APP, 0, glblStr1912, args);
  interact(args, glblProto30);
}
}
void glblProto30_1951(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(66), args);
  args = makePair(APP, 0, glblStr1912, args);
  interact(args, glblProto30);
}
}
void glblProto30_1953(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(65), args);
  args = makePair(APP, 0, glblStr1912, args);
  interact(args, glblProto30);
}
}
void glblProto30_1956(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(64), args);
  args = makePair(APP, 0, glblStr1912, args);
  interact(args, glblProto30);
}
}
void glblProto30_1958(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(63), args);
  args = makePair(APP, 0, glblStr1912, args);
  interact(args, glblProto30);
}
}
void glblProto30_1960(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(62), args);
  args = makePair(APP, 0, glblStr1912, args);
  interact(args, glblProto30);
}
}
void glblProto30_1963(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(61), args);
  args = makePair(APP, 0, glblStr1912, args);
  interact(args, glblProto30);
}
}
Term glblFn1724;
void glblCFn1723(Term ref, Term args) {
Term apps, lams, V;
Location vLoc;
Term seq_2 = makePair(LAM, 0, SUB, NUL);
Location r_seq_2 = portLoc(2, seq_2);
Term c_1 = newTerm(VAR, 0, portLoc(1, seq_2));
// allocate args at /home/jim/toccata/interpreter/intrp-rdr.toc: 60

Term c_1_9;
c_1 = dupeArg(c_1, &c_1_9, 249);

Term c_1_8;
c_1 = dupeArg(c_1, &c_1_8, 250);

Term c_1_7;
c_1 = dupeArg(c_1, &c_1_7, 251);

Term c_1_6;
c_1 = dupeArg(c_1, &c_1_6, 252);

Term c_1_5;
c_1 = dupeArg(c_1, &c_1_5, 253);

Term c_1_4;
c_1 = dupeArg(c_1, &c_1_4, 254);

Term c_1_3;
c_1 = dupeArg(c_1, &c_1_3, 255);

Term c_1_2;
c_1 = dupeArg(c_1, &c_1_2, 256);

Term c_1_1;
c_1 = dupeArg(c_1, &c_1_1, 257);
// call is-alpha at /home/jim/toccata/interpreter/intrp-rdr.toc: 61
Term Rslt_4Args = makePair(APP, 0, c_1, SUB);
Term Rslt_4 = newTerm(VAR, 0, portLoc(2, Rslt_4Args));
#ifdef STRICT
pushRedex(Rslt_4Args, glblFn1722);
#else
swap(termLoc(Rslt_4), makePair(LAZ, 0, Rslt_4Args, glblFn1722));
#endif

// call str= at /home/jim/toccata/interpreter/intrp-rdr.toc: 62
Term Rslt_5Args = makePair(APP, 0, glblStr1933, SUB);
Term Rslt_5 = newTerm(VAR, 0, portLoc(2, Rslt_5Args));
Rslt_5Args = makePair(APP, 0, c_1_1, Rslt_5Args);
#ifdef STRICT
pushRedex(Rslt_5Args, glblFn363);
#else
swap(termLoc(Rslt_5), makePair(LAZ, 0, Rslt_5Args, glblFn363));
#endif

// call str= at /home/jim/toccata/interpreter/intrp-rdr.toc: 63
Term Rslt_6Args = makePair(APP, 0, glblStr1935, SUB);
Term Rslt_6 = newTerm(VAR, 0, portLoc(2, Rslt_6Args));
Rslt_6Args = makePair(APP, 0, c_1_2, Rslt_6Args);
#ifdef STRICT
pushRedex(Rslt_6Args, glblFn363);
#else
swap(termLoc(Rslt_6), makePair(LAZ, 0, Rslt_6Args, glblFn363));
#endif

// call str= at /home/jim/toccata/interpreter/intrp-rdr.toc: 64
Term Rslt_7Args = makePair(APP, 0, glblStr1936, SUB);
Term Rslt_7 = newTerm(VAR, 0, portLoc(2, Rslt_7Args));
Rslt_7Args = makePair(APP, 0, c_1_3, Rslt_7Args);
#ifdef STRICT
pushRedex(Rslt_7Args, glblFn363);
#else
swap(termLoc(Rslt_7), makePair(LAZ, 0, Rslt_7Args, glblFn363));
#endif

// call str= at /home/jim/toccata/interpreter/intrp-rdr.toc: 65
Term Rslt_8Args = makePair(APP, 0, glblStr1937, SUB);
Term Rslt_8 = newTerm(VAR, 0, portLoc(2, Rslt_8Args));
Rslt_8Args = makePair(APP, 0, c_1_4, Rslt_8Args);
#ifdef STRICT
pushRedex(Rslt_8Args, glblFn363);
#else
swap(termLoc(Rslt_8), makePair(LAZ, 0, Rslt_8Args, glblFn363));
#endif

// call str= at /home/jim/toccata/interpreter/intrp-rdr.toc: 66
Term Rslt_9Args = makePair(APP, 0, glblStr1938, SUB);
Term Rslt_9 = newTerm(VAR, 0, portLoc(2, Rslt_9Args));
Rslt_9Args = makePair(APP, 0, c_1_5, Rslt_9Args);
#ifdef STRICT
pushRedex(Rslt_9Args, glblFn363);
#else
swap(termLoc(Rslt_9), makePair(LAZ, 0, Rslt_9Args, glblFn363));
#endif

// call str= at /home/jim/toccata/interpreter/intrp-rdr.toc: 67
Term Rslt_10Args = makePair(APP, 0, glblStr1939, SUB);
Term Rslt_10 = newTerm(VAR, 0, portLoc(2, Rslt_10Args));
Rslt_10Args = makePair(APP, 0, c_1_6, Rslt_10Args);
#ifdef STRICT
pushRedex(Rslt_10Args, glblFn363);
#else
swap(termLoc(Rslt_10), makePair(LAZ, 0, Rslt_10Args, glblFn363));
#endif

// call str= at /home/jim/toccata/interpreter/intrp-rdr.toc: 68
Term Rslt_11Args = makePair(APP, 0, glblStr1940, SUB);
Term Rslt_11 = newTerm(VAR, 0, portLoc(2, Rslt_11Args));
Rslt_11Args = makePair(APP, 0, c_1_7, Rslt_11Args);
#ifdef STRICT
pushRedex(Rslt_11Args, glblFn363);
#else
swap(termLoc(Rslt_11), makePair(LAZ, 0, Rslt_11Args, glblFn363));
#endif

// call str= at /home/jim/toccata/interpreter/intrp-rdr.toc: 69
Term Rslt_12Args = makePair(APP, 0, glblStr1941, SUB);
Term Rslt_12 = newTerm(VAR, 0, portLoc(2, Rslt_12Args));
Rslt_12Args = makePair(APP, 0, c_1_8, Rslt_12Args);
#ifdef STRICT
pushRedex(Rslt_12Args, glblFn363);
#else
swap(termLoc(Rslt_12), makePair(LAZ, 0, Rslt_12Args, glblFn363));
#endif

// call str= at /home/jim/toccata/interpreter/intrp-rdr.toc: 70
Term Rslt_13Args = makePair(APP, 0, glblStr1942, SUB);
Term Rslt_13 = newTerm(VAR, 0, portLoc(2, Rslt_13Args));
Rslt_13Args = makePair(APP, 0, c_1_9, Rslt_13Args);
#ifdef STRICT
pushRedex(Rslt_13Args, glblFn363);
#else
swap(termLoc(Rslt_13), makePair(LAZ, 0, Rslt_13Args, glblFn363));
#endif

// call or at /home/jim/toccata/interpreter/intrp-rdr.toc: 69
Term Rslt_14Args = makePair(APP, 0, Rslt_13, SUB);
Term Rslt_14 = newTerm(VAR, 0, portLoc(2, Rslt_14Args));
Rslt_14Args = makePair(APP, 0, Rslt_12, Rslt_14Args);
#ifdef STRICT
pushRedex(Rslt_14Args, newRef(glblProto30_1944));
#else
swap(termLoc(Rslt_14), makePair(LAZ, 0, Rslt_14Args, newRef(glblProto30_1944)));
#endif
// call or at /home/jim/toccata/interpreter/intrp-rdr.toc: 68
Term Rslt_15Args = makePair(APP, 0, Rslt_14, SUB);
Term Rslt_15 = newTerm(VAR, 0, portLoc(2, Rslt_15Args));
Rslt_15Args = makePair(APP, 0, Rslt_11, Rslt_15Args);
#ifdef STRICT
pushRedex(Rslt_15Args, newRef(glblProto30_1947));
#else
swap(termLoc(Rslt_15), makePair(LAZ, 0, Rslt_15Args, newRef(glblProto30_1947)));
#endif
// call or at /home/jim/toccata/interpreter/intrp-rdr.toc: 67
Term Rslt_16Args = makePair(APP, 0, Rslt_15, SUB);
Term Rslt_16 = newTerm(VAR, 0, portLoc(2, Rslt_16Args));
Rslt_16Args = makePair(APP, 0, Rslt_10, Rslt_16Args);
#ifdef STRICT
pushRedex(Rslt_16Args, newRef(glblProto30_1949));
#else
swap(termLoc(Rslt_16), makePair(LAZ, 0, Rslt_16Args, newRef(glblProto30_1949)));
#endif
// call or at /home/jim/toccata/interpreter/intrp-rdr.toc: 66
Term Rslt_17Args = makePair(APP, 0, Rslt_16, SUB);
Term Rslt_17 = newTerm(VAR, 0, portLoc(2, Rslt_17Args));
Rslt_17Args = makePair(APP, 0, Rslt_9, Rslt_17Args);
#ifdef STRICT
pushRedex(Rslt_17Args, newRef(glblProto30_1951));
#else
swap(termLoc(Rslt_17), makePair(LAZ, 0, Rslt_17Args, newRef(glblProto30_1951)));
#endif
// call or at /home/jim/toccata/interpreter/intrp-rdr.toc: 65
Term Rslt_18Args = makePair(APP, 0, Rslt_17, SUB);
Term Rslt_18 = newTerm(VAR, 0, portLoc(2, Rslt_18Args));
Rslt_18Args = makePair(APP, 0, Rslt_8, Rslt_18Args);
#ifdef STRICT
pushRedex(Rslt_18Args, newRef(glblProto30_1953));
#else
swap(termLoc(Rslt_18), makePair(LAZ, 0, Rslt_18Args, newRef(glblProto30_1953)));
#endif
// call or at /home/jim/toccata/interpreter/intrp-rdr.toc: 64
Term Rslt_19Args = makePair(APP, 0, Rslt_18, SUB);
Term Rslt_19 = newTerm(VAR, 0, portLoc(2, Rslt_19Args));
Rslt_19Args = makePair(APP, 0, Rslt_7, Rslt_19Args);
#ifdef STRICT
pushRedex(Rslt_19Args, newRef(glblProto30_1956));
#else
swap(termLoc(Rslt_19), makePair(LAZ, 0, Rslt_19Args, newRef(glblProto30_1956)));
#endif
// call or at /home/jim/toccata/interpreter/intrp-rdr.toc: 63
Term Rslt_20Args = makePair(APP, 0, Rslt_19, SUB);
Term Rslt_20 = newTerm(VAR, 0, portLoc(2, Rslt_20Args));
Rslt_20Args = makePair(APP, 0, Rslt_6, Rslt_20Args);
#ifdef STRICT
pushRedex(Rslt_20Args, newRef(glblProto30_1958));
#else
swap(termLoc(Rslt_20), makePair(LAZ, 0, Rslt_20Args, newRef(glblProto30_1958)));
#endif
// call or at /home/jim/toccata/interpreter/intrp-rdr.toc: 62
Term Rslt_21Args = makePair(APP, 0, Rslt_20, SUB);
Term Rslt_21 = newTerm(VAR, 0, portLoc(2, Rslt_21Args));
Rslt_21Args = makePair(APP, 0, Rslt_5, Rslt_21Args);
#ifdef STRICT
pushRedex(Rslt_21Args, newRef(glblProto30_1960));
#else
swap(termLoc(Rslt_21), makePair(LAZ, 0, Rslt_21Args, newRef(glblProto30_1960)));
#endif
// call or at /home/jim/toccata/interpreter/intrp-rdr.toc: 61
Term Rslt_22Args = makePair(APP, 0, Rslt_21, SUB);
Term Rslt_22 = newTerm(VAR, 0, portLoc(2, Rslt_22Args));
Rslt_22Args = makePair(APP, 0, Rslt_4, Rslt_22Args);
#ifdef STRICT
pushRedex(Rslt_22Args, newRef(glblProto30_1963));
#else
swap(termLoc(Rslt_22), makePair(LAZ, 0, Rslt_22Args, newRef(glblProto30_1963)));
#endif
// link args to body
swap(r_seq_2, Rslt_22);

Term Rslt_22V = get(termLoc(Rslt_22));
apps = take(portLoc(1, Rslt_22V));
lams = take(portLoc(2, Rslt_22V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_22), makePair(LAZ, 0, apps, lams));
pushRedex(args, seq_2);
return;
}
Term glblFn1724 = newRef(glblCFn1723);
struct {TYPE_SIZE type;
        REFS_SIZE refs;
        int64_t *hash;
        Value *parent;
        int64_t len;
        char buffer[2];
} str24 = {StringBufferType, REFS_STATIC, 0, 0, 1, "?"};
Term glblStr1970 = newTerm_(VAL, (Term)&str24);
struct {TYPE_SIZE type;
        REFS_SIZE refs;
        int64_t *hash;
        Value *parent;
        int64_t len;
        char buffer[2];
} str25 = {StringBufferType, REFS_STATIC, 0, 0, 1, "!"};
Term glblStr1971 = newTerm_(VAL, (Term)&str25);
void glblProto30_1973(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(84), args);
  args = makePair(APP, 0, glblStr1912, args);
  interact(args, glblProto30);
}
}
void glblProto30_1975(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(83), args);
  args = makePair(APP, 0, glblStr1912, args);
  interact(args, glblProto30);
}
}
void glblProto30_1977(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(82), args);
  args = makePair(APP, 0, glblStr1912, args);
  interact(args, glblProto30);
}
}
void glblProto30_1979(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(81), args);
  args = makePair(APP, 0, glblStr1912, args);
  interact(args, glblProto30);
}
}
void glblProto30_1981(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(80), args);
  args = makePair(APP, 0, glblStr1912, args);
  interact(args, glblProto30);
}
}
void glblProto30_1983(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(79), args);
  args = makePair(APP, 0, glblStr1912, args);
  interact(args, glblProto30);
}
}
void glblProto30_1985(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(78), args);
  args = makePair(APP, 0, glblStr1912, args);
  interact(args, glblProto30);
}
}
void glblProto30_1988(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(77), args);
  args = makePair(APP, 0, glblStr1912, args);
  interact(args, glblProto30);
}
}
void glblProto30_1991(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(76), args);
  args = makePair(APP, 0, glblStr1912, args);
  interact(args, glblProto30);
}
}
void glblProto30_1993(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(75), args);
  args = makePair(APP, 0, glblStr1912, args);
  interact(args, glblProto30);
}
}
void glblProto30_1995(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(74), args);
  args = makePair(APP, 0, glblStr1912, args);
  interact(args, glblProto30);
}
}
Term glblFn1726;
void glblCFn1725(Term ref, Term args) {
Term apps, lams, V;
Location vLoc;
Term seq_2 = makePair(LAM, 0, SUB, NUL);
Location r_seq_2 = portLoc(2, seq_2);
Term c_1 = newTerm(VAR, 0, portLoc(1, seq_2));
// allocate args at /home/jim/toccata/interpreter/intrp-rdr.toc: 73

Term c_1_11;
c_1 = dupeArg(c_1, &c_1_11, 258);

Term c_1_10;
c_1 = dupeArg(c_1, &c_1_10, 259);

Term c_1_9;
c_1 = dupeArg(c_1, &c_1_9, 260);

Term c_1_8;
c_1 = dupeArg(c_1, &c_1_8, 261);

Term c_1_7;
c_1 = dupeArg(c_1, &c_1_7, 262);

Term c_1_6;
c_1 = dupeArg(c_1, &c_1_6, 263);

Term c_1_5;
c_1 = dupeArg(c_1, &c_1_5, 264);

Term c_1_4;
c_1 = dupeArg(c_1, &c_1_4, 265);

Term c_1_3;
c_1 = dupeArg(c_1, &c_1_3, 266);

Term c_1_2;
c_1 = dupeArg(c_1, &c_1_2, 267);

Term c_1_1;
c_1 = dupeArg(c_1, &c_1_1, 268);
// call is-alpha at /home/jim/toccata/interpreter/intrp-rdr.toc: 74
Term Rslt_4Args = makePair(APP, 0, c_1, SUB);
Term Rslt_4 = newTerm(VAR, 0, portLoc(2, Rslt_4Args));
#ifdef STRICT
pushRedex(Rslt_4Args, glblFn1722);
#else
swap(termLoc(Rslt_4), makePair(LAZ, 0, Rslt_4Args, glblFn1722));
#endif

// call is-digit at /home/jim/toccata/interpreter/intrp-rdr.toc: 75
Term Rslt_5Args = makePair(APP, 0, c_1_1, SUB);
Term Rslt_5 = newTerm(VAR, 0, portLoc(2, Rslt_5Args));
#ifdef STRICT
pushRedex(Rslt_5Args, glblFn1720);
#else
swap(termLoc(Rslt_5), makePair(LAZ, 0, Rslt_5Args, glblFn1720));
#endif

// call str= at /home/jim/toccata/interpreter/intrp-rdr.toc: 76
Term Rslt_6Args = makePair(APP, 0, glblStr1933, SUB);
Term Rslt_6 = newTerm(VAR, 0, portLoc(2, Rslt_6Args));
Rslt_6Args = makePair(APP, 0, c_1_2, Rslt_6Args);
#ifdef STRICT
pushRedex(Rslt_6Args, glblFn363);
#else
swap(termLoc(Rslt_6), makePair(LAZ, 0, Rslt_6Args, glblFn363));
#endif

// call str= at /home/jim/toccata/interpreter/intrp-rdr.toc: 77
Term Rslt_7Args = makePair(APP, 0, glblStr1935, SUB);
Term Rslt_7 = newTerm(VAR, 0, portLoc(2, Rslt_7Args));
Rslt_7Args = makePair(APP, 0, c_1_3, Rslt_7Args);
#ifdef STRICT
pushRedex(Rslt_7Args, glblFn363);
#else
swap(termLoc(Rslt_7), makePair(LAZ, 0, Rslt_7Args, glblFn363));
#endif

// call str= at /home/jim/toccata/interpreter/intrp-rdr.toc: 78
Term Rslt_8Args = makePair(APP, 0, glblStr1936, SUB);
Term Rslt_8 = newTerm(VAR, 0, portLoc(2, Rslt_8Args));
Rslt_8Args = makePair(APP, 0, c_1_4, Rslt_8Args);
#ifdef STRICT
pushRedex(Rslt_8Args, glblFn363);
#else
swap(termLoc(Rslt_8), makePair(LAZ, 0, Rslt_8Args, glblFn363));
#endif

// call str= at /home/jim/toccata/interpreter/intrp-rdr.toc: 79
Term Rslt_9Args = makePair(APP, 0, glblStr1937, SUB);
Term Rslt_9 = newTerm(VAR, 0, portLoc(2, Rslt_9Args));
Rslt_9Args = makePair(APP, 0, c_1_5, Rslt_9Args);
#ifdef STRICT
pushRedex(Rslt_9Args, glblFn363);
#else
swap(termLoc(Rslt_9), makePair(LAZ, 0, Rslt_9Args, glblFn363));
#endif

// call str= at /home/jim/toccata/interpreter/intrp-rdr.toc: 80
Term Rslt_10Args = makePair(APP, 0, glblStr1938, SUB);
Term Rslt_10 = newTerm(VAR, 0, portLoc(2, Rslt_10Args));
Rslt_10Args = makePair(APP, 0, c_1_6, Rslt_10Args);
#ifdef STRICT
pushRedex(Rslt_10Args, glblFn363);
#else
swap(termLoc(Rslt_10), makePair(LAZ, 0, Rslt_10Args, glblFn363));
#endif

// call str= at /home/jim/toccata/interpreter/intrp-rdr.toc: 81
Term Rslt_11Args = makePair(APP, 0, glblStr1939, SUB);
Term Rslt_11 = newTerm(VAR, 0, portLoc(2, Rslt_11Args));
Rslt_11Args = makePair(APP, 0, c_1_7, Rslt_11Args);
#ifdef STRICT
pushRedex(Rslt_11Args, glblFn363);
#else
swap(termLoc(Rslt_11), makePair(LAZ, 0, Rslt_11Args, glblFn363));
#endif

// call str= at /home/jim/toccata/interpreter/intrp-rdr.toc: 82
Term Rslt_12Args = makePair(APP, 0, glblStr1940, SUB);
Term Rslt_12 = newTerm(VAR, 0, portLoc(2, Rslt_12Args));
Rslt_12Args = makePair(APP, 0, c_1_8, Rslt_12Args);
#ifdef STRICT
pushRedex(Rslt_12Args, glblFn363);
#else
swap(termLoc(Rslt_12), makePair(LAZ, 0, Rslt_12Args, glblFn363));
#endif

// call str= at /home/jim/toccata/interpreter/intrp-rdr.toc: 83
Term Rslt_13Args = makePair(APP, 0, glblStr1941, SUB);
Term Rslt_13 = newTerm(VAR, 0, portLoc(2, Rslt_13Args));
Rslt_13Args = makePair(APP, 0, c_1_9, Rslt_13Args);
#ifdef STRICT
pushRedex(Rslt_13Args, glblFn363);
#else
swap(termLoc(Rslt_13), makePair(LAZ, 0, Rslt_13Args, glblFn363));
#endif

// call str= at /home/jim/toccata/interpreter/intrp-rdr.toc: 84
Term Rslt_14Args = makePair(APP, 0, glblStr1970, SUB);
Term Rslt_14 = newTerm(VAR, 0, portLoc(2, Rslt_14Args));
Rslt_14Args = makePair(APP, 0, c_1_10, Rslt_14Args);
#ifdef STRICT
pushRedex(Rslt_14Args, glblFn363);
#else
swap(termLoc(Rslt_14), makePair(LAZ, 0, Rslt_14Args, glblFn363));
#endif

// call str= at /home/jim/toccata/interpreter/intrp-rdr.toc: 85
Term Rslt_15Args = makePair(APP, 0, glblStr1971, SUB);
Term Rslt_15 = newTerm(VAR, 0, portLoc(2, Rslt_15Args));
Rslt_15Args = makePair(APP, 0, c_1_11, Rslt_15Args);
#ifdef STRICT
pushRedex(Rslt_15Args, glblFn363);
#else
swap(termLoc(Rslt_15), makePair(LAZ, 0, Rslt_15Args, glblFn363));
#endif

// call or at /home/jim/toccata/interpreter/intrp-rdr.toc: 84
Term Rslt_16Args = makePair(APP, 0, Rslt_15, SUB);
Term Rslt_16 = newTerm(VAR, 0, portLoc(2, Rslt_16Args));
Rslt_16Args = makePair(APP, 0, Rslt_14, Rslt_16Args);
#ifdef STRICT
pushRedex(Rslt_16Args, newRef(glblProto30_1973));
#else
swap(termLoc(Rslt_16), makePair(LAZ, 0, Rslt_16Args, newRef(glblProto30_1973)));
#endif
// call or at /home/jim/toccata/interpreter/intrp-rdr.toc: 83
Term Rslt_17Args = makePair(APP, 0, Rslt_16, SUB);
Term Rslt_17 = newTerm(VAR, 0, portLoc(2, Rslt_17Args));
Rslt_17Args = makePair(APP, 0, Rslt_13, Rslt_17Args);
#ifdef STRICT
pushRedex(Rslt_17Args, newRef(glblProto30_1975));
#else
swap(termLoc(Rslt_17), makePair(LAZ, 0, Rslt_17Args, newRef(glblProto30_1975)));
#endif
// call or at /home/jim/toccata/interpreter/intrp-rdr.toc: 82
Term Rslt_18Args = makePair(APP, 0, Rslt_17, SUB);
Term Rslt_18 = newTerm(VAR, 0, portLoc(2, Rslt_18Args));
Rslt_18Args = makePair(APP, 0, Rslt_12, Rslt_18Args);
#ifdef STRICT
pushRedex(Rslt_18Args, newRef(glblProto30_1977));
#else
swap(termLoc(Rslt_18), makePair(LAZ, 0, Rslt_18Args, newRef(glblProto30_1977)));
#endif
// call or at /home/jim/toccata/interpreter/intrp-rdr.toc: 81
Term Rslt_19Args = makePair(APP, 0, Rslt_18, SUB);
Term Rslt_19 = newTerm(VAR, 0, portLoc(2, Rslt_19Args));
Rslt_19Args = makePair(APP, 0, Rslt_11, Rslt_19Args);
#ifdef STRICT
pushRedex(Rslt_19Args, newRef(glblProto30_1979));
#else
swap(termLoc(Rslt_19), makePair(LAZ, 0, Rslt_19Args, newRef(glblProto30_1979)));
#endif
// call or at /home/jim/toccata/interpreter/intrp-rdr.toc: 80
Term Rslt_20Args = makePair(APP, 0, Rslt_19, SUB);
Term Rslt_20 = newTerm(VAR, 0, portLoc(2, Rslt_20Args));
Rslt_20Args = makePair(APP, 0, Rslt_10, Rslt_20Args);
#ifdef STRICT
pushRedex(Rslt_20Args, newRef(glblProto30_1981));
#else
swap(termLoc(Rslt_20), makePair(LAZ, 0, Rslt_20Args, newRef(glblProto30_1981)));
#endif
// call or at /home/jim/toccata/interpreter/intrp-rdr.toc: 79
Term Rslt_21Args = makePair(APP, 0, Rslt_20, SUB);
Term Rslt_21 = newTerm(VAR, 0, portLoc(2, Rslt_21Args));
Rslt_21Args = makePair(APP, 0, Rslt_9, Rslt_21Args);
#ifdef STRICT
pushRedex(Rslt_21Args, newRef(glblProto30_1983));
#else
swap(termLoc(Rslt_21), makePair(LAZ, 0, Rslt_21Args, newRef(glblProto30_1983)));
#endif
// call or at /home/jim/toccata/interpreter/intrp-rdr.toc: 78
Term Rslt_22Args = makePair(APP, 0, Rslt_21, SUB);
Term Rslt_22 = newTerm(VAR, 0, portLoc(2, Rslt_22Args));
Rslt_22Args = makePair(APP, 0, Rslt_8, Rslt_22Args);
#ifdef STRICT
pushRedex(Rslt_22Args, newRef(glblProto30_1985));
#else
swap(termLoc(Rslt_22), makePair(LAZ, 0, Rslt_22Args, newRef(glblProto30_1985)));
#endif
// call or at /home/jim/toccata/interpreter/intrp-rdr.toc: 77
Term Rslt_23Args = makePair(APP, 0, Rslt_22, SUB);
Term Rslt_23 = newTerm(VAR, 0, portLoc(2, Rslt_23Args));
Rslt_23Args = makePair(APP, 0, Rslt_7, Rslt_23Args);
#ifdef STRICT
pushRedex(Rslt_23Args, newRef(glblProto30_1988));
#else
swap(termLoc(Rslt_23), makePair(LAZ, 0, Rslt_23Args, newRef(glblProto30_1988)));
#endif
// call or at /home/jim/toccata/interpreter/intrp-rdr.toc: 76
Term Rslt_24Args = makePair(APP, 0, Rslt_23, SUB);
Term Rslt_24 = newTerm(VAR, 0, portLoc(2, Rslt_24Args));
Rslt_24Args = makePair(APP, 0, Rslt_6, Rslt_24Args);
#ifdef STRICT
pushRedex(Rslt_24Args, newRef(glblProto30_1991));
#else
swap(termLoc(Rslt_24), makePair(LAZ, 0, Rslt_24Args, newRef(glblProto30_1991)));
#endif
// call or at /home/jim/toccata/interpreter/intrp-rdr.toc: 75
Term Rslt_25Args = makePair(APP, 0, Rslt_24, SUB);
Term Rslt_25 = newTerm(VAR, 0, portLoc(2, Rslt_25Args));
Rslt_25Args = makePair(APP, 0, Rslt_5, Rslt_25Args);
#ifdef STRICT
pushRedex(Rslt_25Args, newRef(glblProto30_1993));
#else
swap(termLoc(Rslt_25), makePair(LAZ, 0, Rslt_25Args, newRef(glblProto30_1993)));
#endif
// call or at /home/jim/toccata/interpreter/intrp-rdr.toc: 74
Term Rslt_26Args = makePair(APP, 0, Rslt_25, SUB);
Term Rslt_26 = newTerm(VAR, 0, portLoc(2, Rslt_26Args));
Rslt_26Args = makePair(APP, 0, Rslt_4, Rslt_26Args);
#ifdef STRICT
pushRedex(Rslt_26Args, newRef(glblProto30_1995));
#else
swap(termLoc(Rslt_26), makePair(LAZ, 0, Rslt_26Args, newRef(glblProto30_1995)));
#endif
// link args to body
swap(r_seq_2, Rslt_26);

Term Rslt_26V = get(termLoc(Rslt_26));
apps = take(portLoc(1, Rslt_26V));
lams = take(portLoc(2, Rslt_26V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_26), makePair(LAZ, 0, apps, lams));
pushRedex(args, seq_2);
return;
}
Term glblFn1726 = newRef(glblCFn1725);
void glblProto30_1998(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(90), args);
  args = makePair(APP, 0, glblStr1912, args);
  interact(args, glblProto30);
}
}
Term glblFn1728;
void glblCFn1727(Term ref, Term args) {
Term apps, lams, V;
Location vLoc;
Term seq_2 = makePair(LAM, 0, SUB, NUL);
Location r_seq_2 = portLoc(2, seq_2);
Term c_1 = newTerm(VAR, 0, portLoc(1, seq_2));
// allocate args at /home/jim/toccata/interpreter/intrp-rdr.toc: 89

Term c_1_1;
c_1 = dupeArg(c_1, &c_1_1, 269);
// call is-digit at /home/jim/toccata/interpreter/intrp-rdr.toc: 90
Term Rslt_4Args = makePair(APP, 0, c_1, SUB);
Term Rslt_4 = newTerm(VAR, 0, portLoc(2, Rslt_4Args));
#ifdef STRICT
pushRedex(Rslt_4Args, glblFn1720);
#else
swap(termLoc(Rslt_4), makePair(LAZ, 0, Rslt_4Args, glblFn1720));
#endif

// call str= at /home/jim/toccata/interpreter/intrp-rdr.toc: 91
Term Rslt_5Args = makePair(APP, 0, glblStr1933, SUB);
Term Rslt_5 = newTerm(VAR, 0, portLoc(2, Rslt_5Args));
Rslt_5Args = makePair(APP, 0, c_1_1, Rslt_5Args);
#ifdef STRICT
pushRedex(Rslt_5Args, glblFn363);
#else
swap(termLoc(Rslt_5), makePair(LAZ, 0, Rslt_5Args, glblFn363));
#endif

// call or at /home/jim/toccata/interpreter/intrp-rdr.toc: 90
Term Rslt_6Args = makePair(APP, 0, Rslt_5, SUB);
Term Rslt_6 = newTerm(VAR, 0, portLoc(2, Rslt_6Args));
Rslt_6Args = makePair(APP, 0, Rslt_4, Rslt_6Args);
#ifdef STRICT
pushRedex(Rslt_6Args, newRef(glblProto30_1998));
#else
swap(termLoc(Rslt_6), makePair(LAZ, 0, Rslt_6Args, newRef(glblProto30_1998)));
#endif
// link args to body
swap(r_seq_2, Rslt_6);

Term Rslt_6V = get(termLoc(Rslt_6));
apps = take(portLoc(1, Rslt_6V));
lams = take(portLoc(2, Rslt_6V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_6), makePair(LAZ, 0, apps, lams));
pushRedex(args, seq_2);
return;
}
Term glblFn1728 = newRef(glblCFn1727);
void glblProto380_2001(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(101), args);
  args = makePair(APP, 0, glblStr1912, args);
  interact(args, glblProto380);
}
}
void glblProto34_2003(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(99), args);
  args = makePair(APP, 0, glblStr1912, args);
  interact(args, glblProto34);
}
}
void glblProto1876_2005(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(98), args);
  args = makePair(APP, 0, glblStr1912, args);
  interact(args, glblProto1876);
}
}
Term glblFn1730;
void glblCFn1729(Term ref, Term args) {
Term apps, lams, V;
Location vLoc;
Term seq_2 = makePair(LAM, 0, SUB, NUL);
Location r_seq_2 = portLoc(2, seq_2);
Term state_1 = newTerm(VAR, 0, portLoc(1, seq_2));
// allocate args at /home/jim/toccata/interpreter/intrp-rdr.toc: 97
Term seq_6 = makePair(LAM, 0, SUB, NUL);
Location r_seq_6 = portLoc(2, seq_6);
Term input_5 = newTerm(VAR, 0, portLoc(1, seq_6));
// allocate args at /home/jim/toccata/interpreter/intrp-rdr.toc: 98

Term input_5_1;
input_5 = dupeArg(input_5, &input_5_1, 0);
// call str= at /home/jim/toccata/interpreter/intrp-rdr.toc: 99
Term Rslt_8Args = makePair(APP, 0, glblStr402, SUB);
Term Rslt_8 = newTerm(VAR, 0, portLoc(2, Rslt_8Args));
Rslt_8Args = makePair(APP, 0, input_5, Rslt_8Args);
#ifdef STRICT
pushRedex(Rslt_8Args, glblFn363);
#else
swap(termLoc(Rslt_8), makePair(LAZ, 0, Rslt_8Args, glblFn363));
#endif

// call subs at /home/jim/toccata/interpreter/intrp-rdr.toc: 101
Term Rslt_9Args = makePair(APP, 0, newI60(1), SUB);
Term Rslt_9 = newTerm(VAR, 0, portLoc(2, Rslt_9Args));
Rslt_9Args = makePair(APP, 0, newI60(0), Rslt_9Args);
Rslt_9Args = makePair(APP, 0, input_5_1, Rslt_9Args);
#ifdef STRICT
pushRedex(Rslt_9Args, newRef(glblProto380_2001));
#else
swap(termLoc(Rslt_9), makePair(LAZ, 0, Rslt_9Args, newRef(glblProto380_2001)));
#endif
// call cond at /home/jim/toccata/interpreter/intrp-rdr.toc: 99
Term Rslt_10Args = makePair(APP, 0, Rslt_9, SUB);
Term Rslt_10 = newTerm(VAR, 0, portLoc(2, Rslt_10Args));
Rslt_10Args = makePair(APP, 0, glblStr402, Rslt_10Args);
Rslt_10Args = makePair(APP, 0, Rslt_8, Rslt_10Args);
#ifdef STRICT
pushRedex(Rslt_10Args, newRef(glblProto34_2003));
#else
swap(termLoc(Rslt_10), makePair(LAZ, 0, Rslt_10Args, newRef(glblProto34_2003)));
#endif
// link args to body
swap(r_seq_6, Rslt_10);

Term Rslt_10V = get(termLoc(Rslt_10));
apps = take(portLoc(1, Rslt_10V));
lams = take(portLoc(2, Rslt_10V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_10), makePair(LAZ, 0, apps, lams));
// call .input at /home/jim/toccata/interpreter/intrp-rdr.toc: 98
Term Rslt_11Args = makePair(APP, 0, state_1, SUB);
Term Rslt_11 = newTerm(VAR, 0, portLoc(2, Rslt_11Args));
#ifdef STRICT
pushRedex(Rslt_11Args, newRef(glblProto1876_2005));
#else
swap(termLoc(Rslt_11), makePair(LAZ, 0, Rslt_11Args, newRef(glblProto1876_2005)));
#endif
// call  at /home/jim/toccata/interpreter/intrp-rdr.toc: 98
Term Rslt_12Args = makePair(APP, 0, Rslt_11, SUB);
Term Rslt_12 = newTerm(VAR, 0, portLoc(2, Rslt_12Args));
#ifdef STRICT
pushRedex(Rslt_12Args, seq_6);
#else
swap(termLoc(Rslt_12), makePair(LAZ, 0, Rslt_12Args, seq_6));
#endif

// link args to body
swap(r_seq_2, Rslt_12);

Term Rslt_12V = get(termLoc(Rslt_12));
apps = take(portLoc(1, Rslt_12V));
lams = take(portLoc(2, Rslt_12V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_12), makePair(LAZ, 0, apps, lams));
pushRedex(args, seq_2);
return;
}
Term glblFn1730 = newRef(glblCFn1729);
void glblProto1878_2010(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(115), args);
  args = makePair(APP, 0, glblStr1912, args);
  interact(args, glblProto1878);
}
}
void glblProto107_2012(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(115), args);
  args = makePair(APP, 0, glblStr1912, args);
  interact(args, glblProto107);
}
}
void glblProto34_2014(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(111), args);
  args = makePair(APP, 0, glblStr1912, args);
  interact(args, glblProto34);
}
}
void glblProto64_2017(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(109), args);
  args = makePair(APP, 0, glblStr1912, args);
  interact(args, glblProto64);
}
}
void glblProto380_2019(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(109), args);
  args = makePair(APP, 0, glblStr1912, args);
  interact(args, glblProto380);
}
}
void glblProto380_2022(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(108), args);
  args = makePair(APP, 0, glblStr1912, args);
  interact(args, glblProto380);
}
}
void glblProto1876_2024(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(107), args);
  args = makePair(APP, 0, glblStr1912, args);
  interact(args, glblProto1876);
}
}
Term glblFn1732;
void glblCFn1731(Term ref, Term args) {
Term apps, lams, V;
Location vLoc;
Term seq_2 = makePair(LAM, 0, SUB, NUL);
Location r_seq_2 = portLoc(2, seq_2);
Term state_1 = newTerm(VAR, 0, portLoc(1, seq_2));
// allocate args at /home/jim/toccata/interpreter/intrp-rdr.toc: 106

Term state_1_2;
state_1 = dupeArg(state_1, &state_1_2, 270);

Term state_1_1;
state_1 = dupeArg(state_1, &state_1_1, 271);
Term seq_6 = makePair(LAM, 0, SUB, NUL);
Location r_seq_6 = portLoc(2, seq_6);
Term input_5 = newTerm(VAR, 0, portLoc(1, seq_6));
// allocate args at /home/jim/toccata/interpreter/intrp-rdr.toc: 107

Term input_5_2;
input_5 = dupeArg(input_5, &input_5_2, 0);

Term input_5_1;
input_5 = dupeArg(input_5, &input_5_1, 0);
Term seq_10 = makePair(LAM, 0, SUB, NUL);
Location r_seq_10 = portLoc(2, seq_10);
Term c_9 = newTerm(VAR, 0, portLoc(1, seq_10));
// allocate args at /home/jim/toccata/interpreter/intrp-rdr.toc: 108
Term seq_14 = makePair(LAM, 0, SUB, NUL);
Location r_seq_14 = portLoc(2, seq_14);
Term rest_input_13 = newTerm(VAR, 0, portLoc(1, seq_14));
// allocate args at /home/jim/toccata/interpreter/intrp-rdr.toc: 109
Term seq_18 = makePair(LAM, 0, SUB, NUL);
Location r_seq_18 = portLoc(2, seq_18);
Term line_17 = newTerm(VAR, 0, portLoc(1, seq_18));
// allocate args at /home/jim/toccata/interpreter/intrp-rdr.toc: 110

Term line_17_1;
line_17 = dupeArg(line_17, &line_17_1, 0);
Term seq_22 = makePair(LAM, 0, SUB, NUL);
Location r_seq_22 = portLoc(2, seq_22);
Term new_line_21 = newTerm(VAR, 0, portLoc(1, seq_22));
// allocate args at /home/jim/toccata/interpreter/intrp-rdr.toc: 111
Term glblVal1885_2;
glblVal1885 = dupeArg(glblVal1885, &glblVal1885_2, 0);

// call .values at /home/jim/toccata/interpreter/intrp-rdr.toc: 115
Term Rslt_24Args = makePair(APP, 0, state_1, SUB);
Term Rslt_24 = newTerm(VAR, 0, portLoc(2, Rslt_24Args));
#ifdef STRICT
pushRedex(Rslt_24Args, newRef(glblProto1878_2010));
#else
swap(termLoc(Rslt_24), makePair(LAZ, 0, Rslt_24Args, newRef(glblProto1878_2010)));
#endif
// call assoc at /home/jim/toccata/interpreter/intrp-rdr.toc: 115
Term Rslt_25Args = makePair(APP, 0, new_line_21, SUB);
Term Rslt_25 = newTerm(VAR, 0, portLoc(2, Rslt_25Args));
Rslt_25Args = makePair(APP, 0, glblStr1909, Rslt_25Args);
Rslt_25Args = makePair(APP, 0, Rslt_24, Rslt_25Args);
#ifdef STRICT
pushRedex(Rslt_25Args, newRef(glblProto107_2012));
#else
swap(termLoc(Rslt_25), makePair(LAZ, 0, Rslt_25Args, newRef(glblProto107_2012)));
#endif
// call ParserState at /home/jim/toccata/interpreter/intrp-rdr.toc: 114
Term Rslt_26Args = makePair(APP, 0, Rslt_25, SUB);
Term Rslt_26 = newTerm(VAR, 0, portLoc(2, Rslt_26Args));
Rslt_26Args = makePair(APP, 0, rest_input_13, Rslt_26Args);
#ifdef STRICT
pushRedex(Rslt_26Args, glblVal1885_2);
#else
swap(termLoc(Rslt_26), makePair(LAZ, 0, Rslt_26Args, glblVal1885_2));
#endif

// link args to body
swap(r_seq_22, Rslt_26);

Term Rslt_26V = get(termLoc(Rslt_26));
apps = take(portLoc(1, Rslt_26V));
lams = take(portLoc(2, Rslt_26V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_26), makePair(LAZ, 0, apps, lams));
// call str= at /home/jim/toccata/interpreter/intrp-rdr.toc: 111
Term Rslt_27Args = makePair(APP, 0, glblStr209, SUB);
Term Rslt_27 = newTerm(VAR, 0, portLoc(2, Rslt_27Args));
Rslt_27Args = makePair(APP, 0, c_9, Rslt_27Args);
#ifdef STRICT
pushRedex(Rslt_27Args, glblFn363);
#else
swap(termLoc(Rslt_27), makePair(LAZ, 0, Rslt_27Args, glblFn363));
#endif

Term Rslt_28 = makeOp(OP_ADD, line_17, newI60(1));
// call cond at /home/jim/toccata/interpreter/intrp-rdr.toc: 111
Term Rslt_29Args = makePair(APP, 0, line_17_1, SUB);
Term Rslt_29 = newTerm(VAR, 0, portLoc(2, Rslt_29Args));
Rslt_29Args = makePair(APP, 0, Rslt_28, Rslt_29Args);
Rslt_29Args = makePair(APP, 0, Rslt_27, Rslt_29Args);
#ifdef STRICT
pushRedex(Rslt_29Args, newRef(glblProto34_2014));
#else
swap(termLoc(Rslt_29), makePair(LAZ, 0, Rslt_29Args, newRef(glblProto34_2014)));
#endif
// call  at /home/jim/toccata/interpreter/intrp-rdr.toc: 111
Term Rslt_30Args = makePair(APP, 0, Rslt_29, SUB);
Term Rslt_30 = newTerm(VAR, 0, portLoc(2, Rslt_30Args));
#ifdef STRICT
pushRedex(Rslt_30Args, seq_22);
#else
swap(termLoc(Rslt_30), makePair(LAZ, 0, Rslt_30Args, seq_22));
#endif

// link args to body
swap(r_seq_18, Rslt_30);

Term Rslt_30V = get(termLoc(Rslt_30));
apps = take(portLoc(1, Rslt_30V));
lams = take(portLoc(2, Rslt_30V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_30), makePair(LAZ, 0, apps, lams));
// call state-line at /home/jim/toccata/interpreter/intrp-rdr.toc: 110
Term Rslt_31Args = makePair(APP, 0, state_1_1, SUB);
Term Rslt_31 = newTerm(VAR, 0, portLoc(2, Rslt_31Args));
#ifdef STRICT
pushRedex(Rslt_31Args, glblFn1718);
#else
swap(termLoc(Rslt_31), makePair(LAZ, 0, Rslt_31Args, glblFn1718));
#endif

// call  at /home/jim/toccata/interpreter/intrp-rdr.toc: 110
Term Rslt_32Args = makePair(APP, 0, Rslt_31, SUB);
Term Rslt_32 = newTerm(VAR, 0, portLoc(2, Rslt_32Args));
#ifdef STRICT
pushRedex(Rslt_32Args, seq_18);
#else
swap(termLoc(Rslt_32), makePair(LAZ, 0, Rslt_32Args, seq_18));
#endif

// link args to body
swap(r_seq_14, Rslt_32);

Term Rslt_32V = get(termLoc(Rslt_32));
apps = take(portLoc(1, Rslt_32V));
lams = take(portLoc(2, Rslt_32V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_32), makePair(LAZ, 0, apps, lams));
// call count at /home/jim/toccata/interpreter/intrp-rdr.toc: 109
Term Rslt_33Args = makePair(APP, 0, input_5_1, SUB);
Term Rslt_33 = newTerm(VAR, 0, portLoc(2, Rslt_33Args));
#ifdef STRICT
pushRedex(Rslt_33Args, newRef(glblProto64_2017));
#else
swap(termLoc(Rslt_33), makePair(LAZ, 0, Rslt_33Args, newRef(glblProto64_2017)));
#endif
// call subs at /home/jim/toccata/interpreter/intrp-rdr.toc: 109
Term Rslt_34Args = makePair(APP, 0, Rslt_33, SUB);
Term Rslt_34 = newTerm(VAR, 0, portLoc(2, Rslt_34Args));
Rslt_34Args = makePair(APP, 0, newI60(1), Rslt_34Args);
Rslt_34Args = makePair(APP, 0, input_5, Rslt_34Args);
#ifdef STRICT
pushRedex(Rslt_34Args, newRef(glblProto380_2019));
#else
swap(termLoc(Rslt_34), makePair(LAZ, 0, Rslt_34Args, newRef(glblProto380_2019)));
#endif
// call  at /home/jim/toccata/interpreter/intrp-rdr.toc: 109
Term Rslt_35Args = makePair(APP, 0, Rslt_34, SUB);
Term Rslt_35 = newTerm(VAR, 0, portLoc(2, Rslt_35Args));
#ifdef STRICT
pushRedex(Rslt_35Args, seq_14);
#else
swap(termLoc(Rslt_35), makePair(LAZ, 0, Rslt_35Args, seq_14));
#endif

// link args to body
swap(r_seq_10, Rslt_35);

Term Rslt_35V = get(termLoc(Rslt_35));
apps = take(portLoc(1, Rslt_35V));
lams = take(portLoc(2, Rslt_35V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_35), makePair(LAZ, 0, apps, lams));
// call subs at /home/jim/toccata/interpreter/intrp-rdr.toc: 108
Term Rslt_36Args = makePair(APP, 0, newI60(1), SUB);
Term Rslt_36 = newTerm(VAR, 0, portLoc(2, Rslt_36Args));
Rslt_36Args = makePair(APP, 0, newI60(0), Rslt_36Args);
Rslt_36Args = makePair(APP, 0, input_5_2, Rslt_36Args);
#ifdef STRICT
pushRedex(Rslt_36Args, newRef(glblProto380_2022));
#else
swap(termLoc(Rslt_36), makePair(LAZ, 0, Rslt_36Args, newRef(glblProto380_2022)));
#endif
// call  at /home/jim/toccata/interpreter/intrp-rdr.toc: 108
Term Rslt_37Args = makePair(APP, 0, Rslt_36, SUB);
Term Rslt_37 = newTerm(VAR, 0, portLoc(2, Rslt_37Args));
#ifdef STRICT
pushRedex(Rslt_37Args, seq_10);
#else
swap(termLoc(Rslt_37), makePair(LAZ, 0, Rslt_37Args, seq_10));
#endif

// link args to body
swap(r_seq_6, Rslt_37);

Term Rslt_37V = get(termLoc(Rslt_37));
apps = take(portLoc(1, Rslt_37V));
lams = take(portLoc(2, Rslt_37V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_37), makePair(LAZ, 0, apps, lams));
// call .input at /home/jim/toccata/interpreter/intrp-rdr.toc: 107
Term Rslt_38Args = makePair(APP, 0, state_1_2, SUB);
Term Rslt_38 = newTerm(VAR, 0, portLoc(2, Rslt_38Args));
#ifdef STRICT
pushRedex(Rslt_38Args, newRef(glblProto1876_2024));
#else
swap(termLoc(Rslt_38), makePair(LAZ, 0, Rslt_38Args, newRef(glblProto1876_2024)));
#endif
// call  at /home/jim/toccata/interpreter/intrp-rdr.toc: 107
Term Rslt_39Args = makePair(APP, 0, Rslt_38, SUB);
Term Rslt_39 = newTerm(VAR, 0, portLoc(2, Rslt_39Args));
#ifdef STRICT
pushRedex(Rslt_39Args, seq_6);
#else
swap(termLoc(Rslt_39), makePair(LAZ, 0, Rslt_39Args, seq_6));
#endif

// link args to body
swap(r_seq_2, Rslt_39);

Term Rslt_39V = get(termLoc(Rslt_39));
apps = take(portLoc(1, Rslt_39V));
lams = take(portLoc(2, Rslt_39V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_39), makePair(LAZ, 0, apps, lams));
pushRedex(args, seq_2);
return;
}
Term glblFn1732 = newRef(glblCFn1731);
void glblProto34_2027(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(122), args);
  args = makePair(APP, 0, glblStr1912, args);
  interact(args, glblProto34);
}
}
void glblProto34_2029(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(122), args);
  args = makePair(APP, 0, glblStr1912, args);
  interact(args, glblProto34);
}
}
Term glblFn1734;
void glblCFn1733(Term ref, Term args) {
Term apps, lams, V;
Location vLoc;
Term seq_2 = makePair(LAM, 0, SUB, NUL);
Location r_seq_2 = portLoc(2, seq_2);
Term state_1 = newTerm(VAR, 0, portLoc(1, seq_2));
// allocate args at /home/jim/toccata/interpreter/intrp-rdr.toc: 120

Term state_1_3;
state_1 = dupeArg(state_1, &state_1_3, 272);

Term state_1_2;
state_1 = dupeArg(state_1, &state_1_2, 273);

Term state_1_1;
state_1 = dupeArg(state_1, &state_1_1, 274);
Term seq_6 = makePair(LAM, 0, SUB, NUL);
Location r_seq_6 = portLoc(2, seq_6);
Term c_5 = newTerm(VAR, 0, portLoc(1, seq_6));
// allocate args at /home/jim/toccata/interpreter/intrp-rdr.toc: 121

Term c_5_1;
c_5 = dupeArg(c_5, &c_5_1, 0);
// call str= at /home/jim/toccata/interpreter/intrp-rdr.toc: 122
Term Rslt_8Args = makePair(APP, 0, glblStr402, SUB);
Term Rslt_8 = newTerm(VAR, 0, portLoc(2, Rslt_8Args));
Rslt_8Args = makePair(APP, 0, c_5, Rslt_8Args);
#ifdef STRICT
pushRedex(Rslt_8Args, glblFn363);
#else
swap(termLoc(Rslt_8), makePair(LAZ, 0, Rslt_8Args, glblFn363));
#endif

// call str= at /home/jim/toccata/interpreter/intrp-rdr.toc: 124
Term Rslt_9Args = makePair(APP, 0, glblStr209, SUB);
Term Rslt_9 = newTerm(VAR, 0, portLoc(2, Rslt_9Args));
Rslt_9Args = makePair(APP, 0, c_5_1, Rslt_9Args);
#ifdef STRICT
pushRedex(Rslt_9Args, glblFn363);
#else
swap(termLoc(Rslt_9), makePair(LAZ, 0, Rslt_9Args, glblFn363));
#endif

// call take-char at /home/jim/toccata/interpreter/intrp-rdr.toc: 126
Term Rslt_10Args = makePair(APP, 0, state_1_2, SUB);
Term Rslt_10 = newTerm(VAR, 0, portLoc(2, Rslt_10Args));
#ifdef STRICT
pushRedex(Rslt_10Args, glblFn1732);
#else
swap(termLoc(Rslt_10), makePair(LAZ, 0, Rslt_10Args, glblFn1732));
#endif

// call skip-comment at /home/jim/toccata/interpreter/intrp-rdr.toc: 120
Term Rslt_11Args = makePair(APP, 0, Rslt_10, SUB);
Term Rslt_11 = newTerm(VAR, 0, portLoc(2, Rslt_11Args));
#ifdef STRICT
pushRedex(Rslt_11Args, glblFn1734);
#else
swap(termLoc(Rslt_11), makePair(LAZ, 0, Rslt_11Args, glblFn1734));
#endif

// call cond at /home/jim/toccata/interpreter/intrp-rdr.toc: 122
Term Rslt_12Args = makePair(APP, 0, Rslt_11, SUB);
Term Rslt_12 = newTerm(VAR, 0, portLoc(2, Rslt_12Args));
Rslt_12Args = makePair(APP, 0, state_1_1, Rslt_12Args);
Rslt_12Args = makePair(APP, 0, Rslt_9, Rslt_12Args);
#ifdef STRICT
pushRedex(Rslt_12Args, newRef(glblProto34_2027));
#else
swap(termLoc(Rslt_12), makePair(LAZ, 0, Rslt_12Args, newRef(glblProto34_2027)));
#endif
// call cond at /home/jim/toccata/interpreter/intrp-rdr.toc: 122
Term Rslt_13Args = makePair(APP, 0, Rslt_12, SUB);
Term Rslt_13 = newTerm(VAR, 0, portLoc(2, Rslt_13Args));
Rslt_13Args = makePair(APP, 0, state_1, Rslt_13Args);
Rslt_13Args = makePair(APP, 0, Rslt_8, Rslt_13Args);
#ifdef STRICT
pushRedex(Rslt_13Args, newRef(glblProto34_2029));
#else
swap(termLoc(Rslt_13), makePair(LAZ, 0, Rslt_13Args, newRef(glblProto34_2029)));
#endif
// link args to body
swap(r_seq_6, Rslt_13);

Term Rslt_13V = get(termLoc(Rslt_13));
apps = take(portLoc(1, Rslt_13V));
lams = take(portLoc(2, Rslt_13V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_13), makePair(LAZ, 0, apps, lams));
// call peek-char at /home/jim/toccata/interpreter/intrp-rdr.toc: 121
Term Rslt_14Args = makePair(APP, 0, state_1_3, SUB);
Term Rslt_14 = newTerm(VAR, 0, portLoc(2, Rslt_14Args));
#ifdef STRICT
pushRedex(Rslt_14Args, glblFn1730);
#else
swap(termLoc(Rslt_14), makePair(LAZ, 0, Rslt_14Args, glblFn1730));
#endif

// call  at /home/jim/toccata/interpreter/intrp-rdr.toc: 121
Term Rslt_15Args = makePair(APP, 0, Rslt_14, SUB);
Term Rslt_15 = newTerm(VAR, 0, portLoc(2, Rslt_15Args));
#ifdef STRICT
pushRedex(Rslt_15Args, seq_6);
#else
swap(termLoc(Rslt_15), makePair(LAZ, 0, Rslt_15Args, seq_6));
#endif

// link args to body
swap(r_seq_2, Rslt_15);

Term Rslt_15V = get(termLoc(Rslt_15));
apps = take(portLoc(1, Rslt_15V));
lams = take(portLoc(2, Rslt_15V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_15), makePair(LAZ, 0, apps, lams));
pushRedex(args, seq_2);
return;
}
Term glblFn1734 = newRef(glblCFn1733);
struct {TYPE_SIZE type;
        REFS_SIZE refs;
        int64_t *hash;
        Value *parent;
        int64_t len;
        char buffer[2];
} str26 = {StringBufferType, REFS_STATIC, 0, 0, 1, ";"};
Term glblStr2033 = newTerm_(VAL, (Term)&str26);
struct {TYPE_SIZE type;
        REFS_SIZE refs;
        int64_t *hash;
        Value *parent;
        int64_t len;
        char buffer[2];
} str27 = {StringBufferType, REFS_STATIC, 0, 0, 1, "\t"};
Term glblStr2035 = newTerm_(VAL, (Term)&str27);
struct {TYPE_SIZE type;
        REFS_SIZE refs;
        int64_t *hash;
        Value *parent;
        int64_t len;
        char buffer[2];
} str28 = {StringBufferType, REFS_STATIC, 0, 0, 1, "\r"};
Term glblStr2037 = newTerm_(VAL, (Term)&str28);
void glblProto34_2039(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(132), args);
  args = makePair(APP, 0, glblStr1912, args);
  interact(args, glblProto34);
}
}
void glblProto34_2041(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(132), args);
  args = makePair(APP, 0, glblStr1912, args);
  interact(args, glblProto34);
}
}
void glblProto34_2043(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(132), args);
  args = makePair(APP, 0, glblStr1912, args);
  interact(args, glblProto34);
}
}
void glblProto34_2045(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(132), args);
  args = makePair(APP, 0, glblStr1912, args);
  interact(args, glblProto34);
}
}
void glblProto34_2047(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(132), args);
  args = makePair(APP, 0, glblStr1912, args);
  interact(args, glblProto34);
}
}
void glblProto34_2049(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(132), args);
  args = makePair(APP, 0, glblStr1912, args);
  interact(args, glblProto34);
}
}
Term glblFn1736;
void glblCFn1735(Term ref, Term args) {
Term apps, lams, V;
Location vLoc;
Term seq_2 = makePair(LAM, 0, SUB, NUL);
Location r_seq_2 = portLoc(2, seq_2);
Term state_1 = newTerm(VAR, 0, portLoc(1, seq_2));
// allocate args at /home/jim/toccata/interpreter/intrp-rdr.toc: 130

Term state_1_7;
state_1 = dupeArg(state_1, &state_1_7, 275);

Term state_1_6;
state_1 = dupeArg(state_1, &state_1_6, 276);

Term state_1_5;
state_1 = dupeArg(state_1, &state_1_5, 277);

Term state_1_4;
state_1 = dupeArg(state_1, &state_1_4, 278);

Term state_1_3;
state_1 = dupeArg(state_1, &state_1_3, 279);

Term state_1_2;
state_1 = dupeArg(state_1, &state_1_2, 280);

Term state_1_1;
state_1 = dupeArg(state_1, &state_1_1, 281);
Term seq_6 = makePair(LAM, 0, SUB, NUL);
Location r_seq_6 = portLoc(2, seq_6);
Term c_5 = newTerm(VAR, 0, portLoc(1, seq_6));
// allocate args at /home/jim/toccata/interpreter/intrp-rdr.toc: 131

Term c_5_5;
c_5 = dupeArg(c_5, &c_5_5, 0);

Term c_5_4;
c_5 = dupeArg(c_5, &c_5_4, 0);

Term c_5_3;
c_5 = dupeArg(c_5, &c_5_3, 0);

Term c_5_2;
c_5 = dupeArg(c_5, &c_5_2, 0);

Term c_5_1;
c_5 = dupeArg(c_5, &c_5_1, 0);
// call str= at /home/jim/toccata/interpreter/intrp-rdr.toc: 132
Term Rslt_8Args = makePair(APP, 0, glblStr402, SUB);
Term Rslt_8 = newTerm(VAR, 0, portLoc(2, Rslt_8Args));
Rslt_8Args = makePair(APP, 0, c_5, Rslt_8Args);
#ifdef STRICT
pushRedex(Rslt_8Args, glblFn363);
#else
swap(termLoc(Rslt_8), makePair(LAZ, 0, Rslt_8Args, glblFn363));
#endif

// call str= at /home/jim/toccata/interpreter/intrp-rdr.toc: 134
Term Rslt_9Args = makePair(APP, 0, glblStr2033, SUB);
Term Rslt_9 = newTerm(VAR, 0, portLoc(2, Rslt_9Args));
Rslt_9Args = makePair(APP, 0, c_5_1, Rslt_9Args);
#ifdef STRICT
pushRedex(Rslt_9Args, glblFn363);
#else
swap(termLoc(Rslt_9), makePair(LAZ, 0, Rslt_9Args, glblFn363));
#endif

// call take-char at /home/jim/toccata/interpreter/intrp-rdr.toc: 135
Term Rslt_10Args = makePair(APP, 0, state_1_1, SUB);
Term Rslt_10 = newTerm(VAR, 0, portLoc(2, Rslt_10Args));
#ifdef STRICT
pushRedex(Rslt_10Args, glblFn1732);
#else
swap(termLoc(Rslt_10), makePair(LAZ, 0, Rslt_10Args, glblFn1732));
#endif

// call skip-comment at /home/jim/toccata/interpreter/intrp-rdr.toc: 135
Term Rslt_11Args = makePair(APP, 0, Rslt_10, SUB);
Term Rslt_11 = newTerm(VAR, 0, portLoc(2, Rslt_11Args));
#ifdef STRICT
pushRedex(Rslt_11Args, glblFn1734);
#else
swap(termLoc(Rslt_11), makePair(LAZ, 0, Rslt_11Args, glblFn1734));
#endif

// call skip-whitespace at /home/jim/toccata/interpreter/intrp-rdr.toc: 130
Term Rslt_12Args = makePair(APP, 0, Rslt_11, SUB);
Term Rslt_12 = newTerm(VAR, 0, portLoc(2, Rslt_12Args));
#ifdef STRICT
pushRedex(Rslt_12Args, glblFn1736);
#else
swap(termLoc(Rslt_12), makePair(LAZ, 0, Rslt_12Args, glblFn1736));
#endif

// call str= at /home/jim/toccata/interpreter/intrp-rdr.toc: 136
Term Rslt_13Args = makePair(APP, 0, glblStr210, SUB);
Term Rslt_13 = newTerm(VAR, 0, portLoc(2, Rslt_13Args));
Rslt_13Args = makePair(APP, 0, c_5_2, Rslt_13Args);
#ifdef STRICT
pushRedex(Rslt_13Args, glblFn363);
#else
swap(termLoc(Rslt_13), makePair(LAZ, 0, Rslt_13Args, glblFn363));
#endif

// call take-char at /home/jim/toccata/interpreter/intrp-rdr.toc: 137
Term Rslt_14Args = makePair(APP, 0, state_1_2, SUB);
Term Rslt_14 = newTerm(VAR, 0, portLoc(2, Rslt_14Args));
#ifdef STRICT
pushRedex(Rslt_14Args, glblFn1732);
#else
swap(termLoc(Rslt_14), makePair(LAZ, 0, Rslt_14Args, glblFn1732));
#endif

// call skip-whitespace at /home/jim/toccata/interpreter/intrp-rdr.toc: 130
Term Rslt_15Args = makePair(APP, 0, Rslt_14, SUB);
Term Rslt_15 = newTerm(VAR, 0, portLoc(2, Rslt_15Args));
#ifdef STRICT
pushRedex(Rslt_15Args, glblFn1736);
#else
swap(termLoc(Rslt_15), makePair(LAZ, 0, Rslt_15Args, glblFn1736));
#endif

// call str= at /home/jim/toccata/interpreter/intrp-rdr.toc: 138
Term Rslt_16Args = makePair(APP, 0, glblStr2035, SUB);
Term Rslt_16 = newTerm(VAR, 0, portLoc(2, Rslt_16Args));
Rslt_16Args = makePair(APP, 0, c_5_3, Rslt_16Args);
#ifdef STRICT
pushRedex(Rslt_16Args, glblFn363);
#else
swap(termLoc(Rslt_16), makePair(LAZ, 0, Rslt_16Args, glblFn363));
#endif

// call take-char at /home/jim/toccata/interpreter/intrp-rdr.toc: 139
Term Rslt_17Args = makePair(APP, 0, state_1_3, SUB);
Term Rslt_17 = newTerm(VAR, 0, portLoc(2, Rslt_17Args));
#ifdef STRICT
pushRedex(Rslt_17Args, glblFn1732);
#else
swap(termLoc(Rslt_17), makePair(LAZ, 0, Rslt_17Args, glblFn1732));
#endif

// call skip-whitespace at /home/jim/toccata/interpreter/intrp-rdr.toc: 130
Term Rslt_18Args = makePair(APP, 0, Rslt_17, SUB);
Term Rslt_18 = newTerm(VAR, 0, portLoc(2, Rslt_18Args));
#ifdef STRICT
pushRedex(Rslt_18Args, glblFn1736);
#else
swap(termLoc(Rslt_18), makePair(LAZ, 0, Rslt_18Args, glblFn1736));
#endif

// call str= at /home/jim/toccata/interpreter/intrp-rdr.toc: 140
Term Rslt_19Args = makePair(APP, 0, glblStr209, SUB);
Term Rslt_19 = newTerm(VAR, 0, portLoc(2, Rslt_19Args));
Rslt_19Args = makePair(APP, 0, c_5_4, Rslt_19Args);
#ifdef STRICT
pushRedex(Rslt_19Args, glblFn363);
#else
swap(termLoc(Rslt_19), makePair(LAZ, 0, Rslt_19Args, glblFn363));
#endif

// call take-char at /home/jim/toccata/interpreter/intrp-rdr.toc: 141
Term Rslt_20Args = makePair(APP, 0, state_1_4, SUB);
Term Rslt_20 = newTerm(VAR, 0, portLoc(2, Rslt_20Args));
#ifdef STRICT
pushRedex(Rslt_20Args, glblFn1732);
#else
swap(termLoc(Rslt_20), makePair(LAZ, 0, Rslt_20Args, glblFn1732));
#endif

// call skip-whitespace at /home/jim/toccata/interpreter/intrp-rdr.toc: 130
Term Rslt_21Args = makePair(APP, 0, Rslt_20, SUB);
Term Rslt_21 = newTerm(VAR, 0, portLoc(2, Rslt_21Args));
#ifdef STRICT
pushRedex(Rslt_21Args, glblFn1736);
#else
swap(termLoc(Rslt_21), makePair(LAZ, 0, Rslt_21Args, glblFn1736));
#endif

// call str= at /home/jim/toccata/interpreter/intrp-rdr.toc: 142
Term Rslt_22Args = makePair(APP, 0, glblStr2037, SUB);
Term Rslt_22 = newTerm(VAR, 0, portLoc(2, Rslt_22Args));
Rslt_22Args = makePair(APP, 0, c_5_5, Rslt_22Args);
#ifdef STRICT
pushRedex(Rslt_22Args, glblFn363);
#else
swap(termLoc(Rslt_22), makePair(LAZ, 0, Rslt_22Args, glblFn363));
#endif

// call take-char at /home/jim/toccata/interpreter/intrp-rdr.toc: 143
Term Rslt_23Args = makePair(APP, 0, state_1_5, SUB);
Term Rslt_23 = newTerm(VAR, 0, portLoc(2, Rslt_23Args));
#ifdef STRICT
pushRedex(Rslt_23Args, glblFn1732);
#else
swap(termLoc(Rslt_23), makePair(LAZ, 0, Rslt_23Args, glblFn1732));
#endif

// call skip-whitespace at /home/jim/toccata/interpreter/intrp-rdr.toc: 130
Term Rslt_24Args = makePair(APP, 0, Rslt_23, SUB);
Term Rslt_24 = newTerm(VAR, 0, portLoc(2, Rslt_24Args));
#ifdef STRICT
pushRedex(Rslt_24Args, glblFn1736);
#else
swap(termLoc(Rslt_24), makePair(LAZ, 0, Rslt_24Args, glblFn1736));
#endif

// call cond at /home/jim/toccata/interpreter/intrp-rdr.toc: 132
Term Rslt_25Args = makePair(APP, 0, state_1_6, SUB);
Term Rslt_25 = newTerm(VAR, 0, portLoc(2, Rslt_25Args));
Rslt_25Args = makePair(APP, 0, Rslt_24, Rslt_25Args);
Rslt_25Args = makePair(APP, 0, Rslt_22, Rslt_25Args);
#ifdef STRICT
pushRedex(Rslt_25Args, newRef(glblProto34_2039));
#else
swap(termLoc(Rslt_25), makePair(LAZ, 0, Rslt_25Args, newRef(glblProto34_2039)));
#endif
// call cond at /home/jim/toccata/interpreter/intrp-rdr.toc: 132
Term Rslt_26Args = makePair(APP, 0, Rslt_25, SUB);
Term Rslt_26 = newTerm(VAR, 0, portLoc(2, Rslt_26Args));
Rslt_26Args = makePair(APP, 0, Rslt_21, Rslt_26Args);
Rslt_26Args = makePair(APP, 0, Rslt_19, Rslt_26Args);
#ifdef STRICT
pushRedex(Rslt_26Args, newRef(glblProto34_2041));
#else
swap(termLoc(Rslt_26), makePair(LAZ, 0, Rslt_26Args, newRef(glblProto34_2041)));
#endif
// call cond at /home/jim/toccata/interpreter/intrp-rdr.toc: 132
Term Rslt_27Args = makePair(APP, 0, Rslt_26, SUB);
Term Rslt_27 = newTerm(VAR, 0, portLoc(2, Rslt_27Args));
Rslt_27Args = makePair(APP, 0, Rslt_18, Rslt_27Args);
Rslt_27Args = makePair(APP, 0, Rslt_16, Rslt_27Args);
#ifdef STRICT
pushRedex(Rslt_27Args, newRef(glblProto34_2043));
#else
swap(termLoc(Rslt_27), makePair(LAZ, 0, Rslt_27Args, newRef(glblProto34_2043)));
#endif
// call cond at /home/jim/toccata/interpreter/intrp-rdr.toc: 132
Term Rslt_28Args = makePair(APP, 0, Rslt_27, SUB);
Term Rslt_28 = newTerm(VAR, 0, portLoc(2, Rslt_28Args));
Rslt_28Args = makePair(APP, 0, Rslt_15, Rslt_28Args);
Rslt_28Args = makePair(APP, 0, Rslt_13, Rslt_28Args);
#ifdef STRICT
pushRedex(Rslt_28Args, newRef(glblProto34_2045));
#else
swap(termLoc(Rslt_28), makePair(LAZ, 0, Rslt_28Args, newRef(glblProto34_2045)));
#endif
// call cond at /home/jim/toccata/interpreter/intrp-rdr.toc: 132
Term Rslt_29Args = makePair(APP, 0, Rslt_28, SUB);
Term Rslt_29 = newTerm(VAR, 0, portLoc(2, Rslt_29Args));
Rslt_29Args = makePair(APP, 0, Rslt_12, Rslt_29Args);
Rslt_29Args = makePair(APP, 0, Rslt_9, Rslt_29Args);
#ifdef STRICT
pushRedex(Rslt_29Args, newRef(glblProto34_2047));
#else
swap(termLoc(Rslt_29), makePair(LAZ, 0, Rslt_29Args, newRef(glblProto34_2047)));
#endif
// call cond at /home/jim/toccata/interpreter/intrp-rdr.toc: 132
Term Rslt_30Args = makePair(APP, 0, Rslt_29, SUB);
Term Rslt_30 = newTerm(VAR, 0, portLoc(2, Rslt_30Args));
Rslt_30Args = makePair(APP, 0, state_1, Rslt_30Args);
Rslt_30Args = makePair(APP, 0, Rslt_8, Rslt_30Args);
#ifdef STRICT
pushRedex(Rslt_30Args, newRef(glblProto34_2049));
#else
swap(termLoc(Rslt_30), makePair(LAZ, 0, Rslt_30Args, newRef(glblProto34_2049)));
#endif
// link args to body
swap(r_seq_6, Rslt_30);

Term Rslt_30V = get(termLoc(Rslt_30));
apps = take(portLoc(1, Rslt_30V));
lams = take(portLoc(2, Rslt_30V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_30), makePair(LAZ, 0, apps, lams));
// call peek-char at /home/jim/toccata/interpreter/intrp-rdr.toc: 131
Term Rslt_31Args = makePair(APP, 0, state_1_7, SUB);
Term Rslt_31 = newTerm(VAR, 0, portLoc(2, Rslt_31Args));
#ifdef STRICT
pushRedex(Rslt_31Args, glblFn1730);
#else
swap(termLoc(Rslt_31), makePair(LAZ, 0, Rslt_31Args, glblFn1730));
#endif

// call  at /home/jim/toccata/interpreter/intrp-rdr.toc: 131
Term Rslt_32Args = makePair(APP, 0, Rslt_31, SUB);
Term Rslt_32 = newTerm(VAR, 0, portLoc(2, Rslt_32Args));
#ifdef STRICT
pushRedex(Rslt_32Args, seq_6);
#else
swap(termLoc(Rslt_32), makePair(LAZ, 0, Rslt_32Args, seq_6));
#endif

// link args to body
swap(r_seq_2, Rslt_32);

Term Rslt_32V = get(termLoc(Rslt_32));
apps = take(portLoc(1, Rslt_32V));
lams = take(portLoc(2, Rslt_32V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_32), makePair(LAZ, 0, apps, lams));
pushRedex(args, seq_2);
return;
}
Term glblFn1736 = newRef(glblCFn1735);
void glblProto64_2052(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(153), args);
  args = makePair(APP, 0, glblStr1912, args);
  interact(args, glblProto64);
}
}
void glblProto380_2054(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(153), args);
  args = makePair(APP, 0, glblStr1912, args);
  interact(args, glblProto380);
}
}
void glblProto34_2056(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(150), args);
  args = makePair(APP, 0, glblStr1912, args);
  interact(args, glblProto34);
}
}
void glblProto34_2058(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(150), args);
  args = makePair(APP, 0, glblStr1912, args);
  interact(args, glblProto34);
}
}
void glblProto380_2060(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(149), args);
  args = makePair(APP, 0, glblStr1912, args);
  interact(args, glblProto380);
}
}
Term glblFn1738;
void glblCFn1737(Term ref, Term args) {
Term apps, lams, V;
Location vLoc;
Term seq_3 = makePair(LAM, 0, SUB, NUL);
Location r_seq_3 = portLoc(2, seq_3);
Term pred_2 = newTerm(VAR, 0, portLoc(1, seq_3));
seq_3 = makePair(LAM, 0, SUB, seq_3);
Term s_1 = newTerm(VAR, 0, portLoc(1, seq_3));
// allocate args at /home/jim/toccata/interpreter/intrp-rdr.toc: 148

Term s_1_2;
s_1 = dupeArg(s_1, &s_1_2, 282);

Term s_1_1;
s_1 = dupeArg(s_1, &s_1_1, 283);

Term pred_2_1;
pred_2 = dupeArg(pred_2, &pred_2_1, 284);
Term seq_7 = makePair(LAM, 0, SUB, NUL);
Location r_seq_7 = portLoc(2, seq_7);
Term c_6 = newTerm(VAR, 0, portLoc(1, seq_7));
// allocate args at /home/jim/toccata/interpreter/intrp-rdr.toc: 149

Term c_6_1;
c_6 = dupeArg(c_6, &c_6_1, 0);
// call str= at /home/jim/toccata/interpreter/intrp-rdr.toc: 150
Term Rslt_9Args = makePair(APP, 0, glblStr402, SUB);
Term Rslt_9 = newTerm(VAR, 0, portLoc(2, Rslt_9Args));
Rslt_9Args = makePair(APP, 0, c_6, Rslt_9Args);
#ifdef STRICT
pushRedex(Rslt_9Args, glblFn363);
#else
swap(termLoc(Rslt_9), makePair(LAZ, 0, Rslt_9Args, glblFn363));
#endif

// call pred at /home/jim/toccata/interpreter/intrp-rdr.toc: 148
Term Rslt_10Args = makePair(APP, 0, c_6_1, SUB);
Term Rslt_10 = newTerm(VAR, 0, portLoc(2, Rslt_10Args));
#ifdef STRICT
pushRedex(Rslt_10Args, pred_2);
#else
swap(termLoc(Rslt_10), makePair(LAZ, 0, Rslt_10Args, pred_2));
#endif

// call count at /home/jim/toccata/interpreter/intrp-rdr.toc: 153
Term Rslt_11Args = makePair(APP, 0, s_1_1, SUB);
Term Rslt_11 = newTerm(VAR, 0, portLoc(2, Rslt_11Args));
#ifdef STRICT
pushRedex(Rslt_11Args, newRef(glblProto64_2052));
#else
swap(termLoc(Rslt_11), makePair(LAZ, 0, Rslt_11Args, newRef(glblProto64_2052)));
#endif
// call subs at /home/jim/toccata/interpreter/intrp-rdr.toc: 153
Term Rslt_12Args = makePair(APP, 0, Rslt_11, SUB);
Term Rslt_12 = newTerm(VAR, 0, portLoc(2, Rslt_12Args));
Rslt_12Args = makePair(APP, 0, newI60(1), Rslt_12Args);
Rslt_12Args = makePair(APP, 0, s_1, Rslt_12Args);
#ifdef STRICT
pushRedex(Rslt_12Args, newRef(glblProto380_2054));
#else
swap(termLoc(Rslt_12), makePair(LAZ, 0, Rslt_12Args, newRef(glblProto380_2054)));
#endif
// call run-length at /home/jim/toccata/interpreter/intrp-rdr.toc: 148
Term Rslt_13Args = makePair(APP, 0, pred_2_1, SUB);
Term Rslt_13 = newTerm(VAR, 0, portLoc(2, Rslt_13Args));
Rslt_13Args = makePair(APP, 0, Rslt_12, Rslt_13Args);
#ifdef STRICT
pushRedex(Rslt_13Args, glblFn1738);
#else
swap(termLoc(Rslt_13), makePair(LAZ, 0, Rslt_13Args, glblFn1738));
#endif

Term Rslt_14 = makeOp(OP_ADD, newI60(1), Rslt_13);
// call cond at /home/jim/toccata/interpreter/intrp-rdr.toc: 150
Term Rslt_15Args = makePair(APP, 0, newI60(0), SUB);
Term Rslt_15 = newTerm(VAR, 0, portLoc(2, Rslt_15Args));
Rslt_15Args = makePair(APP, 0, Rslt_14, Rslt_15Args);
Rslt_15Args = makePair(APP, 0, Rslt_10, Rslt_15Args);
#ifdef STRICT
pushRedex(Rslt_15Args, newRef(glblProto34_2056));
#else
swap(termLoc(Rslt_15), makePair(LAZ, 0, Rslt_15Args, newRef(glblProto34_2056)));
#endif
// call cond at /home/jim/toccata/interpreter/intrp-rdr.toc: 150
Term Rslt_16Args = makePair(APP, 0, Rslt_15, SUB);
Term Rslt_16 = newTerm(VAR, 0, portLoc(2, Rslt_16Args));
Rslt_16Args = makePair(APP, 0, newI60(0), Rslt_16Args);
Rslt_16Args = makePair(APP, 0, Rslt_9, Rslt_16Args);
#ifdef STRICT
pushRedex(Rslt_16Args, newRef(glblProto34_2058));
#else
swap(termLoc(Rslt_16), makePair(LAZ, 0, Rslt_16Args, newRef(glblProto34_2058)));
#endif
// link args to body
swap(r_seq_7, Rslt_16);

Term Rslt_16V = get(termLoc(Rslt_16));
apps = take(portLoc(1, Rslt_16V));
lams = take(portLoc(2, Rslt_16V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_16), makePair(LAZ, 0, apps, lams));
// call subs at /home/jim/toccata/interpreter/intrp-rdr.toc: 149
Term Rslt_17Args = makePair(APP, 0, newI60(1), SUB);
Term Rslt_17 = newTerm(VAR, 0, portLoc(2, Rslt_17Args));
Rslt_17Args = makePair(APP, 0, newI60(0), Rslt_17Args);
Rslt_17Args = makePair(APP, 0, s_1_2, Rslt_17Args);
#ifdef STRICT
pushRedex(Rslt_17Args, newRef(glblProto380_2060));
#else
swap(termLoc(Rslt_17), makePair(LAZ, 0, Rslt_17Args, newRef(glblProto380_2060)));
#endif
// call  at /home/jim/toccata/interpreter/intrp-rdr.toc: 149
Term Rslt_18Args = makePair(APP, 0, Rslt_17, SUB);
Term Rslt_18 = newTerm(VAR, 0, portLoc(2, Rslt_18Args));
#ifdef STRICT
pushRedex(Rslt_18Args, seq_7);
#else
swap(termLoc(Rslt_18), makePair(LAZ, 0, Rslt_18Args, seq_7));
#endif

// link args to body
swap(r_seq_3, Rslt_18);

Term Rslt_18V = get(termLoc(Rslt_18));
apps = take(portLoc(1, Rslt_18V));
lams = take(portLoc(2, Rslt_18V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_18), makePair(LAZ, 0, apps, lams));
pushRedex(args, seq_3);
return;
}
Term glblFn1738 = newRef(glblCFn1737);
void glblProto1878_2063(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(164), args);
  args = makePair(APP, 0, glblStr1912, args);
  interact(args, glblProto1878);
}
}
void glblProto64_2065(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(163), args);
  args = makePair(APP, 0, glblStr1912, args);
  interact(args, glblProto64);
}
}
void glblProto380_2067(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(163), args);
  args = makePair(APP, 0, glblStr1912, args);
  interact(args, glblProto380);
}
}
void glblProto380_2069(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(162), args);
  args = makePair(APP, 0, glblStr1912, args);
  interact(args, glblProto380);
}
}
void glblProto1876_2071(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(160), args);
  args = makePair(APP, 0, glblStr1912, args);
  interact(args, glblProto1876);
}
}
Term glblFn1740;
void glblCFn1739(Term ref, Term args) {
Term apps, lams, V;
Location vLoc;
Term seq_3 = makePair(LAM, 0, SUB, NUL);
Location r_seq_3 = portLoc(2, seq_3);
Term pred_2 = newTerm(VAR, 0, portLoc(1, seq_3));
seq_3 = makePair(LAM, 0, SUB, seq_3);
Term state_1 = newTerm(VAR, 0, portLoc(1, seq_3));
// allocate args at /home/jim/toccata/interpreter/intrp-rdr.toc: 159

Term state_1_1;
state_1 = dupeArg(state_1, &state_1_1, 285);
Term seq_7 = makePair(LAM, 0, SUB, NUL);
Location r_seq_7 = portLoc(2, seq_7);
Term input_6 = newTerm(VAR, 0, portLoc(1, seq_7));
// allocate args at /home/jim/toccata/interpreter/intrp-rdr.toc: 160

Term input_6_3;
input_6 = dupeArg(input_6, &input_6_3, 0);

Term input_6_2;
input_6 = dupeArg(input_6, &input_6_2, 0);

Term input_6_1;
input_6 = dupeArg(input_6, &input_6_1, 0);
Term seq_11 = makePair(LAM, 0, SUB, NUL);
Location r_seq_11 = portLoc(2, seq_11);
Term len_10 = newTerm(VAR, 0, portLoc(1, seq_11));
// allocate args at /home/jim/toccata/interpreter/intrp-rdr.toc: 161

Term len_10_1;
len_10 = dupeArg(len_10, &len_10_1, 0);
Term seq_15 = makePair(LAM, 0, SUB, NUL);
Location r_seq_15 = portLoc(2, seq_15);
Term run_14 = newTerm(VAR, 0, portLoc(1, seq_15));
// allocate args at /home/jim/toccata/interpreter/intrp-rdr.toc: 162
Term seq_19 = makePair(LAM, 0, SUB, NUL);
Location r_seq_19 = portLoc(2, seq_19);
Term rest_input_18 = newTerm(VAR, 0, portLoc(1, seq_19));
// allocate args at /home/jim/toccata/interpreter/intrp-rdr.toc: 163
Term glblVal1885_3;
glblVal1885 = dupeArg(glblVal1885, &glblVal1885_3, 0);

Term glblVal175_32;
glblVal175 = dupeArg(glblVal175, &glblVal175_32, 0);

// call vect-conj at /home/jim/toccata/interpreter/intrp-rdr.toc: 164
Term Rslt_21Args = makePair(APP, 0, run_14, SUB);
Term Rslt_21 = newTerm(VAR, 0, portLoc(2, Rslt_21Args));
Rslt_21Args = makePair(APP, 0, glblVal175_32, Rslt_21Args);
#ifdef STRICT
pushRedex(Rslt_21Args, glblFn178);
#else
swap(termLoc(Rslt_21), makePair(LAZ, 0, Rslt_21Args, glblFn178));
#endif

// call .values at /home/jim/toccata/interpreter/intrp-rdr.toc: 164
Term Rslt_22Args = makePair(APP, 0, state_1, SUB);
Term Rslt_22 = newTerm(VAR, 0, portLoc(2, Rslt_22Args));
#ifdef STRICT
pushRedex(Rslt_22Args, newRef(glblProto1878_2063));
#else
swap(termLoc(Rslt_22), makePair(LAZ, 0, Rslt_22Args, newRef(glblProto1878_2063)));
#endif
// call ParserState at /home/jim/toccata/interpreter/intrp-rdr.toc: 164
Term Rslt_23Args = makePair(APP, 0, Rslt_22, SUB);
Term Rslt_23 = newTerm(VAR, 0, portLoc(2, Rslt_23Args));
Rslt_23Args = makePair(APP, 0, rest_input_18, Rslt_23Args);
#ifdef STRICT
pushRedex(Rslt_23Args, glblVal1885_3);
#else
swap(termLoc(Rslt_23), makePair(LAZ, 0, Rslt_23Args, glblVal1885_3));
#endif

// call vect-conj at /home/jim/toccata/interpreter/intrp-rdr.toc: 164
Term Rslt_24Args = makePair(APP, 0, Rslt_23, SUB);
Term Rslt_24 = newTerm(VAR, 0, portLoc(2, Rslt_24Args));
Rslt_24Args = makePair(APP, 0, Rslt_21, Rslt_24Args);
#ifdef STRICT
pushRedex(Rslt_24Args, glblFn178);
#else
swap(termLoc(Rslt_24), makePair(LAZ, 0, Rslt_24Args, glblFn178));
#endif

// link args to body
swap(r_seq_19, Rslt_24);

Term Rslt_24V = get(termLoc(Rslt_24));
apps = take(portLoc(1, Rslt_24V));
lams = take(portLoc(2, Rslt_24V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_24), makePair(LAZ, 0, apps, lams));
// call count at /home/jim/toccata/interpreter/intrp-rdr.toc: 163
Term Rslt_25Args = makePair(APP, 0, input_6_1, SUB);
Term Rslt_25 = newTerm(VAR, 0, portLoc(2, Rslt_25Args));
#ifdef STRICT
pushRedex(Rslt_25Args, newRef(glblProto64_2065));
#else
swap(termLoc(Rslt_25), makePair(LAZ, 0, Rslt_25Args, newRef(glblProto64_2065)));
#endif
// call subs at /home/jim/toccata/interpreter/intrp-rdr.toc: 163
Term Rslt_26Args = makePair(APP, 0, Rslt_25, SUB);
Term Rslt_26 = newTerm(VAR, 0, portLoc(2, Rslt_26Args));
Rslt_26Args = makePair(APP, 0, len_10, Rslt_26Args);
Rslt_26Args = makePair(APP, 0, input_6, Rslt_26Args);
#ifdef STRICT
pushRedex(Rslt_26Args, newRef(glblProto380_2067));
#else
swap(termLoc(Rslt_26), makePair(LAZ, 0, Rslt_26Args, newRef(glblProto380_2067)));
#endif
// call  at /home/jim/toccata/interpreter/intrp-rdr.toc: 163
Term Rslt_27Args = makePair(APP, 0, Rslt_26, SUB);
Term Rslt_27 = newTerm(VAR, 0, portLoc(2, Rslt_27Args));
#ifdef STRICT
pushRedex(Rslt_27Args, seq_19);
#else
swap(termLoc(Rslt_27), makePair(LAZ, 0, Rslt_27Args, seq_19));
#endif

// link args to body
swap(r_seq_15, Rslt_27);

Term Rslt_27V = get(termLoc(Rslt_27));
apps = take(portLoc(1, Rslt_27V));
lams = take(portLoc(2, Rslt_27V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_27), makePair(LAZ, 0, apps, lams));
// call subs at /home/jim/toccata/interpreter/intrp-rdr.toc: 162
Term Rslt_28Args = makePair(APP, 0, len_10_1, SUB);
Term Rslt_28 = newTerm(VAR, 0, portLoc(2, Rslt_28Args));
Rslt_28Args = makePair(APP, 0, newI60(0), Rslt_28Args);
Rslt_28Args = makePair(APP, 0, input_6_2, Rslt_28Args);
#ifdef STRICT
pushRedex(Rslt_28Args, newRef(glblProto380_2069));
#else
swap(termLoc(Rslt_28), makePair(LAZ, 0, Rslt_28Args, newRef(glblProto380_2069)));
#endif
// call  at /home/jim/toccata/interpreter/intrp-rdr.toc: 162
Term Rslt_29Args = makePair(APP, 0, Rslt_28, SUB);
Term Rslt_29 = newTerm(VAR, 0, portLoc(2, Rslt_29Args));
#ifdef STRICT
pushRedex(Rslt_29Args, seq_15);
#else
swap(termLoc(Rslt_29), makePair(LAZ, 0, Rslt_29Args, seq_15));
#endif

// link args to body
swap(r_seq_11, Rslt_29);

Term Rslt_29V = get(termLoc(Rslt_29));
apps = take(portLoc(1, Rslt_29V));
lams = take(portLoc(2, Rslt_29V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_29), makePair(LAZ, 0, apps, lams));
// call run-length at /home/jim/toccata/interpreter/intrp-rdr.toc: 161
Term Rslt_30Args = makePair(APP, 0, pred_2, SUB);
Term Rslt_30 = newTerm(VAR, 0, portLoc(2, Rslt_30Args));
Rslt_30Args = makePair(APP, 0, input_6_3, Rslt_30Args);
#ifdef STRICT
pushRedex(Rslt_30Args, glblFn1738);
#else
swap(termLoc(Rslt_30), makePair(LAZ, 0, Rslt_30Args, glblFn1738));
#endif

// call  at /home/jim/toccata/interpreter/intrp-rdr.toc: 161
Term Rslt_31Args = makePair(APP, 0, Rslt_30, SUB);
Term Rslt_31 = newTerm(VAR, 0, portLoc(2, Rslt_31Args));
#ifdef STRICT
pushRedex(Rslt_31Args, seq_11);
#else
swap(termLoc(Rslt_31), makePair(LAZ, 0, Rslt_31Args, seq_11));
#endif

// link args to body
swap(r_seq_7, Rslt_31);

Term Rslt_31V = get(termLoc(Rslt_31));
apps = take(portLoc(1, Rslt_31V));
lams = take(portLoc(2, Rslt_31V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_31), makePair(LAZ, 0, apps, lams));
// call .input at /home/jim/toccata/interpreter/intrp-rdr.toc: 160
Term Rslt_32Args = makePair(APP, 0, state_1_1, SUB);
Term Rslt_32 = newTerm(VAR, 0, portLoc(2, Rslt_32Args));
#ifdef STRICT
pushRedex(Rslt_32Args, newRef(glblProto1876_2071));
#else
swap(termLoc(Rslt_32), makePair(LAZ, 0, Rslt_32Args, newRef(glblProto1876_2071)));
#endif
// call  at /home/jim/toccata/interpreter/intrp-rdr.toc: 160
Term Rslt_33Args = makePair(APP, 0, Rslt_32, SUB);
Term Rslt_33 = newTerm(VAR, 0, portLoc(2, Rslt_33Args));
#ifdef STRICT
pushRedex(Rslt_33Args, seq_7);
#else
swap(termLoc(Rslt_33), makePair(LAZ, 0, Rslt_33Args, seq_7));
#endif

// link args to body
swap(r_seq_3, Rslt_33);

Term Rslt_33V = get(termLoc(Rslt_33));
apps = take(portLoc(1, Rslt_33V));
lams = take(portLoc(2, Rslt_33V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_33), makePair(LAZ, 0, apps, lams));
pushRedex(args, seq_3);
return;
}
Term glblFn1740 = newRef(glblCFn1739);
void glblProto76_2074(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(169), args);
  args = makePair(APP, 0, glblStr1912, args);
  interact(args, glblProto76);
}
}
void glblProto56_2076(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(169), args);
  args = makePair(APP, 0, glblStr1912, args);
  interact(args, glblProto56);
}
}
Term glblFn1742;
void glblCFn1741(Term ref, Term args) {
Term apps, lams, V;
Location vLoc;
Term seq_2 = makePair(LAM, 0, SUB, NUL);
Location r_seq_2 = portLoc(2, seq_2);
Term v_1 = newTerm(VAR, 0, portLoc(1, seq_2));
// allocate args at /home/jim/toccata/interpreter/intrp-rdr.toc: 168
// call first at /home/jim/toccata/interpreter/intrp-rdr.toc: 169
Term Rslt_4Args = makePair(APP, 0, v_1, SUB);
Term Rslt_4 = newTerm(VAR, 0, portLoc(2, Rslt_4Args));
#ifdef STRICT
pushRedex(Rslt_4Args, newRef(glblProto76_2074));
#else
swap(termLoc(Rslt_4), makePair(LAZ, 0, Rslt_4Args, newRef(glblProto76_2074)));
#endif
// call extract at /home/jim/toccata/interpreter/intrp-rdr.toc: 169
Term Rslt_5Args = makePair(APP, 0, Rslt_4, SUB);
Term Rslt_5 = newTerm(VAR, 0, portLoc(2, Rslt_5Args));
#ifdef STRICT
pushRedex(Rslt_5Args, newRef(glblProto56_2076));
#else
swap(termLoc(Rslt_5), makePair(LAZ, 0, Rslt_5Args, newRef(glblProto56_2076)));
#endif
// link args to body
swap(r_seq_2, Rslt_5);

Term Rslt_5V = get(termLoc(Rslt_5));
apps = take(portLoc(1, Rslt_5V));
lams = take(portLoc(2, Rslt_5V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_5), makePair(LAZ, 0, apps, lams));
pushRedex(args, seq_2);
return;
}
Term glblFn1742 = newRef(glblCFn1741);
void glblProto78_2079(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(172), args);
  args = makePair(APP, 0, glblStr1912, args);
  interact(args, glblProto78);
}
}
void glblProto76_2081(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(172), args);
  args = makePair(APP, 0, glblStr1912, args);
  interact(args, glblProto76);
}
}
void glblProto56_2083(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(172), args);
  args = makePair(APP, 0, glblStr1912, args);
  interact(args, glblProto56);
}
}
Term glblFn1744;
void glblCFn1743(Term ref, Term args) {
Term apps, lams, V;
Location vLoc;
Term seq_2 = makePair(LAM, 0, SUB, NUL);
Location r_seq_2 = portLoc(2, seq_2);
Term v_1 = newTerm(VAR, 0, portLoc(1, seq_2));
// allocate args at /home/jim/toccata/interpreter/intrp-rdr.toc: 171
// call rest at /home/jim/toccata/interpreter/intrp-rdr.toc: 172
Term Rslt_4Args = makePair(APP, 0, v_1, SUB);
Term Rslt_4 = newTerm(VAR, 0, portLoc(2, Rslt_4Args));
#ifdef STRICT
pushRedex(Rslt_4Args, newRef(glblProto78_2079));
#else
swap(termLoc(Rslt_4), makePair(LAZ, 0, Rslt_4Args, newRef(glblProto78_2079)));
#endif
// call first at /home/jim/toccata/interpreter/intrp-rdr.toc: 172
Term Rslt_5Args = makePair(APP, 0, Rslt_4, SUB);
Term Rslt_5 = newTerm(VAR, 0, portLoc(2, Rslt_5Args));
#ifdef STRICT
pushRedex(Rslt_5Args, newRef(glblProto76_2081));
#else
swap(termLoc(Rslt_5), makePair(LAZ, 0, Rslt_5Args, newRef(glblProto76_2081)));
#endif
// call extract at /home/jim/toccata/interpreter/intrp-rdr.toc: 172
Term Rslt_6Args = makePair(APP, 0, Rslt_5, SUB);
Term Rslt_6 = newTerm(VAR, 0, portLoc(2, Rslt_6Args));
#ifdef STRICT
pushRedex(Rslt_6Args, newRef(glblProto56_2083));
#else
swap(termLoc(Rslt_6), makePair(LAZ, 0, Rslt_6Args, newRef(glblProto56_2083)));
#endif
// link args to body
swap(r_seq_2, Rslt_6);

Term Rslt_6V = get(termLoc(Rslt_6));
apps = take(portLoc(1, Rslt_6V));
lams = take(portLoc(2, Rslt_6V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_6), makePair(LAZ, 0, apps, lams));
pushRedex(args, seq_2);
return;
}
Term glblFn1744 = newRef(glblCFn1743);
struct {TYPE_SIZE type;
        REFS_SIZE refs;
        int64_t *hash;
        Value *parent;
        int64_t len;
        char buffer[2];
} str29 = {StringBufferType, REFS_STATIC, 0, 0, 1, "\\"};
Term glblStr2087 = newTerm_(VAL, (Term)&str29);
struct {TYPE_SIZE type;
        REFS_SIZE refs;
        int64_t *hash;
        Value *parent;
        int64_t len;
        char buffer[2];
} str30 = {StringBufferType, REFS_STATIC, 0, 0, 1, "\""};
Term glblStr2088 = newTerm_(VAL, (Term)&str30);
void glblProto34_2089(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(182), args);
  args = makePair(APP, 0, glblStr1912, args);
  interact(args, glblProto34);
}
}
void glblProto34_2091(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(182), args);
  args = makePair(APP, 0, glblStr1912, args);
  interact(args, glblProto34);
}
}
void glblProto34_2093(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(182), args);
  args = makePair(APP, 0, glblStr1912, args);
  interact(args, glblProto34);
}
}
Term glblFn1746;
void glblCFn1745(Term ref, Term args) {
Term apps, lams, V;
Location vLoc;
Term seq_2 = makePair(LAM, 0, SUB, NUL);
Location r_seq_2 = portLoc(2, seq_2);
Term state_1 = newTerm(VAR, 0, portLoc(1, seq_2));
// allocate args at /home/jim/toccata/interpreter/intrp-rdr.toc: 180

Term state_1_4;
state_1 = dupeArg(state_1, &state_1_4, 286);

Term state_1_3;
state_1 = dupeArg(state_1, &state_1_3, 287);

Term state_1_2;
state_1 = dupeArg(state_1, &state_1_2, 288);

Term state_1_1;
state_1 = dupeArg(state_1, &state_1_1, 289);
Term seq_6 = makePair(LAM, 0, SUB, NUL);
Location r_seq_6 = portLoc(2, seq_6);
Term c_5 = newTerm(VAR, 0, portLoc(1, seq_6));
// allocate args at /home/jim/toccata/interpreter/intrp-rdr.toc: 181

Term c_5_2;
c_5 = dupeArg(c_5, &c_5_2, 0);

Term c_5_1;
c_5 = dupeArg(c_5, &c_5_1, 0);
// call str= at /home/jim/toccata/interpreter/intrp-rdr.toc: 182
Term Rslt_8Args = makePair(APP, 0, glblStr402, SUB);
Term Rslt_8 = newTerm(VAR, 0, portLoc(2, Rslt_8Args));
Rslt_8Args = makePair(APP, 0, c_5, Rslt_8Args);
#ifdef STRICT
pushRedex(Rslt_8Args, glblFn363);
#else
swap(termLoc(Rslt_8), makePair(LAZ, 0, Rslt_8Args, glblFn363));
#endif

// call str= at /home/jim/toccata/interpreter/intrp-rdr.toc: 184
Term Rslt_9Args = makePair(APP, 0, glblStr2087, SUB);
Term Rslt_9 = newTerm(VAR, 0, portLoc(2, Rslt_9Args));
Rslt_9Args = makePair(APP, 0, c_5_1, Rslt_9Args);
#ifdef STRICT
pushRedex(Rslt_9Args, glblFn363);
#else
swap(termLoc(Rslt_9), makePair(LAZ, 0, Rslt_9Args, glblFn363));
#endif

// call take-char at /home/jim/toccata/interpreter/intrp-rdr.toc: 185
Term Rslt_10Args = makePair(APP, 0, state_1_1, SUB);
Term Rslt_10 = newTerm(VAR, 0, portLoc(2, Rslt_10Args));
#ifdef STRICT
pushRedex(Rslt_10Args, glblFn1732);
#else
swap(termLoc(Rslt_10), makePair(LAZ, 0, Rslt_10Args, glblFn1732));
#endif

// call take-char at /home/jim/toccata/interpreter/intrp-rdr.toc: 185
Term Rslt_11Args = makePair(APP, 0, Rslt_10, SUB);
Term Rslt_11 = newTerm(VAR, 0, portLoc(2, Rslt_11Args));
#ifdef STRICT
pushRedex(Rslt_11Args, glblFn1732);
#else
swap(termLoc(Rslt_11), makePair(LAZ, 0, Rslt_11Args, glblFn1732));
#endif

// call read-string-content at /home/jim/toccata/interpreter/intrp-rdr.toc: 180
Term Rslt_12Args = makePair(APP, 0, Rslt_11, SUB);
Term Rslt_12 = newTerm(VAR, 0, portLoc(2, Rslt_12Args));
#ifdef STRICT
pushRedex(Rslt_12Args, glblFn1746);
#else
swap(termLoc(Rslt_12), makePair(LAZ, 0, Rslt_12Args, glblFn1746));
#endif

// call str= at /home/jim/toccata/interpreter/intrp-rdr.toc: 186
Term Rslt_13Args = makePair(APP, 0, glblStr2088, SUB);
Term Rslt_13 = newTerm(VAR, 0, portLoc(2, Rslt_13Args));
Rslt_13Args = makePair(APP, 0, c_5_2, Rslt_13Args);
#ifdef STRICT
pushRedex(Rslt_13Args, glblFn363);
#else
swap(termLoc(Rslt_13), makePair(LAZ, 0, Rslt_13Args, glblFn363));
#endif

// call take-char at /home/jim/toccata/interpreter/intrp-rdr.toc: 188
Term Rslt_14Args = makePair(APP, 0, state_1_3, SUB);
Term Rslt_14 = newTerm(VAR, 0, portLoc(2, Rslt_14Args));
#ifdef STRICT
pushRedex(Rslt_14Args, glblFn1732);
#else
swap(termLoc(Rslt_14), makePair(LAZ, 0, Rslt_14Args, glblFn1732));
#endif

// call read-string-content at /home/jim/toccata/interpreter/intrp-rdr.toc: 180
Term Rslt_15Args = makePair(APP, 0, Rslt_14, SUB);
Term Rslt_15 = newTerm(VAR, 0, portLoc(2, Rslt_15Args));
#ifdef STRICT
pushRedex(Rslt_15Args, glblFn1746);
#else
swap(termLoc(Rslt_15), makePair(LAZ, 0, Rslt_15Args, glblFn1746));
#endif

// call cond at /home/jim/toccata/interpreter/intrp-rdr.toc: 182
Term Rslt_16Args = makePair(APP, 0, Rslt_15, SUB);
Term Rslt_16 = newTerm(VAR, 0, portLoc(2, Rslt_16Args));
Rslt_16Args = makePair(APP, 0, state_1_2, Rslt_16Args);
Rslt_16Args = makePair(APP, 0, Rslt_13, Rslt_16Args);
#ifdef STRICT
pushRedex(Rslt_16Args, newRef(glblProto34_2089));
#else
swap(termLoc(Rslt_16), makePair(LAZ, 0, Rslt_16Args, newRef(glblProto34_2089)));
#endif
// call cond at /home/jim/toccata/interpreter/intrp-rdr.toc: 182
Term Rslt_17Args = makePair(APP, 0, Rslt_16, SUB);
Term Rslt_17 = newTerm(VAR, 0, portLoc(2, Rslt_17Args));
Rslt_17Args = makePair(APP, 0, Rslt_12, Rslt_17Args);
Rslt_17Args = makePair(APP, 0, Rslt_9, Rslt_17Args);
#ifdef STRICT
pushRedex(Rslt_17Args, newRef(glblProto34_2091));
#else
swap(termLoc(Rslt_17), makePair(LAZ, 0, Rslt_17Args, newRef(glblProto34_2091)));
#endif
// call cond at /home/jim/toccata/interpreter/intrp-rdr.toc: 182
Term Rslt_18Args = makePair(APP, 0, Rslt_17, SUB);
Term Rslt_18 = newTerm(VAR, 0, portLoc(2, Rslt_18Args));
Rslt_18Args = makePair(APP, 0, state_1, Rslt_18Args);
Rslt_18Args = makePair(APP, 0, Rslt_8, Rslt_18Args);
#ifdef STRICT
pushRedex(Rslt_18Args, newRef(glblProto34_2093));
#else
swap(termLoc(Rslt_18), makePair(LAZ, 0, Rslt_18Args, newRef(glblProto34_2093)));
#endif
// link args to body
swap(r_seq_6, Rslt_18);

Term Rslt_18V = get(termLoc(Rslt_18));
apps = take(portLoc(1, Rslt_18V));
lams = take(portLoc(2, Rslt_18V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_18), makePair(LAZ, 0, apps, lams));
// call peek-char at /home/jim/toccata/interpreter/intrp-rdr.toc: 181
Term Rslt_19Args = makePair(APP, 0, state_1_4, SUB);
Term Rslt_19 = newTerm(VAR, 0, portLoc(2, Rslt_19Args));
#ifdef STRICT
pushRedex(Rslt_19Args, glblFn1730);
#else
swap(termLoc(Rslt_19), makePair(LAZ, 0, Rslt_19Args, glblFn1730));
#endif

// call  at /home/jim/toccata/interpreter/intrp-rdr.toc: 181
Term Rslt_20Args = makePair(APP, 0, Rslt_19, SUB);
Term Rslt_20 = newTerm(VAR, 0, portLoc(2, Rslt_20Args));
#ifdef STRICT
pushRedex(Rslt_20Args, seq_6);
#else
swap(termLoc(Rslt_20), makePair(LAZ, 0, Rslt_20Args, seq_6));
#endif

// link args to body
swap(r_seq_2, Rslt_20);

Term Rslt_20V = get(termLoc(Rslt_20));
apps = take(portLoc(1, Rslt_20V));
lams = take(portLoc(2, Rslt_20V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_20), makePair(LAZ, 0, apps, lams));
pushRedex(args, seq_2);
return;
}
Term glblFn1746 = newRef(glblCFn1745);
void glblProto1876_2100(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(204), args);
  args = makePair(APP, 0, glblStr1912, args);
  interact(args, glblProto1876);
}
}
void glblProto380_2102(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(204), args);
  args = makePair(APP, 0, glblStr1912, args);
  interact(args, glblProto380);
}
}
void glblProto1876_2105(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(202), args);
  args = makePair(APP, 0, glblStr1912, args);
  interact(args, glblProto1876);
}
}
void glblProto64_2107(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(202), args);
  args = makePair(APP, 0, glblStr1912, args);
  interact(args, glblProto64);
}
}
void glblProto1876_2110(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(203), args);
  args = makePair(APP, 0, glblStr1912, args);
  interact(args, glblProto1876);
}
}
void glblProto64_2112(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(203), args);
  args = makePair(APP, 0, glblStr1912, args);
  interact(args, glblProto64);
}
}
void glblProto34_2114(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(196), args);
  args = makePair(APP, 0, glblStr1912, args);
  interact(args, glblProto34);
}
}
void glblProto34_2116(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(196), args);
  args = makePair(APP, 0, glblStr1912, args);
  interact(args, glblProto34);
}
}
Term glblFn1748;
void glblCFn1747(Term ref, Term args) {
Term apps, lams, V;
Location vLoc;
Term seq_2 = makePair(LAM, 0, SUB, NUL);
Location r_seq_2 = portLoc(2, seq_2);
Term state_1 = newTerm(VAR, 0, portLoc(1, seq_2));
// allocate args at /home/jim/toccata/interpreter/intrp-rdr.toc: 194

Term state_1_1;
state_1 = dupeArg(state_1, &state_1_1, 290);
Term seq_6 = makePair(LAM, 0, SUB, NUL);
Location r_seq_6 = portLoc(2, seq_6);
Term c_5 = newTerm(VAR, 0, portLoc(1, seq_6));
// allocate args at /home/jim/toccata/interpreter/intrp-rdr.toc: 195

Term c_5_1;
c_5 = dupeArg(c_5, &c_5_1, 0);
Term glblVal175_35;
glblVal175 = dupeArg(glblVal175, &glblVal175_35, 0);

Term seq_13 = makePair(LAM, 0, SUB, NUL);
Location r_seq_13 = portLoc(2, seq_13);
Term after_open_12 = newTerm(VAR, 0, portLoc(1, seq_13));
// allocate args at /home/jim/toccata/interpreter/intrp-rdr.toc: 199

Term after_open_12_2;
after_open_12 = dupeArg(after_open_12, &after_open_12_2, 0);

Term after_open_12_1;
after_open_12 = dupeArg(after_open_12, &after_open_12_1, 0);
Term seq_17 = makePair(LAM, 0, SUB, NUL);
Location r_seq_17 = portLoc(2, seq_17);
Term at_close_16 = newTerm(VAR, 0, portLoc(1, seq_17));
// allocate args at /home/jim/toccata/interpreter/intrp-rdr.toc: 200

Term at_close_16_1;
at_close_16 = dupeArg(at_close_16, &at_close_16_1, 0);
Term seq_21 = makePair(LAM, 0, SUB, NUL);
Location r_seq_21 = portLoc(2, seq_21);
Term content_len_20 = newTerm(VAR, 0, portLoc(1, seq_21));
// allocate args at /home/jim/toccata/interpreter/intrp-rdr.toc: 201
Term seq_25 = makePair(LAM, 0, SUB, NUL);
Location r_seq_25 = portLoc(2, seq_25);
Term content_24 = newTerm(VAR, 0, portLoc(1, seq_25));
// allocate args at /home/jim/toccata/interpreter/intrp-rdr.toc: 204
Term seq_29 = makePair(LAM, 0, SUB, NUL);
Location r_seq_29 = portLoc(2, seq_29);
Term end_state_28 = newTerm(VAR, 0, portLoc(1, seq_29));
// allocate args at /home/jim/toccata/interpreter/intrp-rdr.toc: 205
Term glblVal175_34;
glblVal175 = dupeArg(glblVal175, &glblVal175_34, 0);

Term glblVal175_33;
glblVal175 = dupeArg(glblVal175, &glblVal175_33, 0);

// call str= at /home/jim/toccata/interpreter/intrp-rdr.toc: 196
Term Rslt_8Args = makePair(APP, 0, glblStr402, SUB);
Term Rslt_8 = newTerm(VAR, 0, portLoc(2, Rslt_8Args));
Rslt_8Args = makePair(APP, 0, c_5, Rslt_8Args);
#ifdef STRICT
pushRedex(Rslt_8Args, glblFn363);
#else
swap(termLoc(Rslt_8), makePair(LAZ, 0, Rslt_8Args, glblFn363));
#endif

// call vect-conj at /home/jim/toccata/interpreter/intrp-rdr.toc: 197
Term Rslt_9Args = makePair(APP, 0, glblStr402, SUB);
Term Rslt_9 = newTerm(VAR, 0, portLoc(2, Rslt_9Args));
Rslt_9Args = makePair(APP, 0, glblVal175_33, Rslt_9Args);
#ifdef STRICT
pushRedex(Rslt_9Args, glblFn178);
#else
swap(termLoc(Rslt_9), makePair(LAZ, 0, Rslt_9Args, glblFn178));
#endif

// call str= at /home/jim/toccata/interpreter/intrp-rdr.toc: 198
Term Rslt_10Args = makePair(APP, 0, glblStr2088, SUB);
Term Rslt_10 = newTerm(VAR, 0, portLoc(2, Rslt_10Args));
Rslt_10Args = makePair(APP, 0, c_5_1, Rslt_10Args);
#ifdef STRICT
pushRedex(Rslt_10Args, glblFn363);
#else
swap(termLoc(Rslt_10), makePair(LAZ, 0, Rslt_10Args, glblFn363));
#endif

// call vect-conj at /home/jim/toccata/interpreter/intrp-rdr.toc: 206
Term Rslt_31Args = makePair(APP, 0, content_24, SUB);
Term Rslt_31 = newTerm(VAR, 0, portLoc(2, Rslt_31Args));
Rslt_31Args = makePair(APP, 0, glblVal175_34, Rslt_31Args);
#ifdef STRICT
pushRedex(Rslt_31Args, glblFn178);
#else
swap(termLoc(Rslt_31), makePair(LAZ, 0, Rslt_31Args, glblFn178));
#endif

// call vect-conj at /home/jim/toccata/interpreter/intrp-rdr.toc: 206
Term Rslt_32Args = makePair(APP, 0, end_state_28, SUB);
Term Rslt_32 = newTerm(VAR, 0, portLoc(2, Rslt_32Args));
Rslt_32Args = makePair(APP, 0, Rslt_31, Rslt_32Args);
#ifdef STRICT
pushRedex(Rslt_32Args, glblFn178);
#else
swap(termLoc(Rslt_32), makePair(LAZ, 0, Rslt_32Args, glblFn178));
#endif

// link args to body
swap(r_seq_29, Rslt_32);

Term Rslt_32V = get(termLoc(Rslt_32));
apps = take(portLoc(1, Rslt_32V));
lams = take(portLoc(2, Rslt_32V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_32), makePair(LAZ, 0, apps, lams));
// call take-char at /home/jim/toccata/interpreter/intrp-rdr.toc: 205
Term Rslt_33Args = makePair(APP, 0, at_close_16, SUB);
Term Rslt_33 = newTerm(VAR, 0, portLoc(2, Rslt_33Args));
#ifdef STRICT
pushRedex(Rslt_33Args, glblFn1732);
#else
swap(termLoc(Rslt_33), makePair(LAZ, 0, Rslt_33Args, glblFn1732));
#endif

// call  at /home/jim/toccata/interpreter/intrp-rdr.toc: 205
Term Rslt_34Args = makePair(APP, 0, Rslt_33, SUB);
Term Rslt_34 = newTerm(VAR, 0, portLoc(2, Rslt_34Args));
#ifdef STRICT
pushRedex(Rslt_34Args, seq_29);
#else
swap(termLoc(Rslt_34), makePair(LAZ, 0, Rslt_34Args, seq_29));
#endif

// link args to body
swap(r_seq_25, Rslt_34);

Term Rslt_34V = get(termLoc(Rslt_34));
apps = take(portLoc(1, Rslt_34V));
lams = take(portLoc(2, Rslt_34V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_34), makePair(LAZ, 0, apps, lams));
// call .input at /home/jim/toccata/interpreter/intrp-rdr.toc: 204
Term Rslt_35Args = makePair(APP, 0, after_open_12, SUB);
Term Rslt_35 = newTerm(VAR, 0, portLoc(2, Rslt_35Args));
#ifdef STRICT
pushRedex(Rslt_35Args, newRef(glblProto1876_2100));
#else
swap(termLoc(Rslt_35), makePair(LAZ, 0, Rslt_35Args, newRef(glblProto1876_2100)));
#endif
// call subs at /home/jim/toccata/interpreter/intrp-rdr.toc: 204
Term Rslt_36Args = makePair(APP, 0, content_len_20, SUB);
Term Rslt_36 = newTerm(VAR, 0, portLoc(2, Rslt_36Args));
Rslt_36Args = makePair(APP, 0, newI60(0), Rslt_36Args);
Rslt_36Args = makePair(APP, 0, Rslt_35, Rslt_36Args);
#ifdef STRICT
pushRedex(Rslt_36Args, newRef(glblProto380_2102));
#else
swap(termLoc(Rslt_36), makePair(LAZ, 0, Rslt_36Args, newRef(glblProto380_2102)));
#endif
// call  at /home/jim/toccata/interpreter/intrp-rdr.toc: 204
Term Rslt_37Args = makePair(APP, 0, Rslt_36, SUB);
Term Rslt_37 = newTerm(VAR, 0, portLoc(2, Rslt_37Args));
#ifdef STRICT
pushRedex(Rslt_37Args, seq_25);
#else
swap(termLoc(Rslt_37), makePair(LAZ, 0, Rslt_37Args, seq_25));
#endif

// link args to body
swap(r_seq_21, Rslt_37);

Term Rslt_37V = get(termLoc(Rslt_37));
apps = take(portLoc(1, Rslt_37V));
lams = take(portLoc(2, Rslt_37V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_37), makePair(LAZ, 0, apps, lams));
// call .input at /home/jim/toccata/interpreter/intrp-rdr.toc: 202
Term Rslt_38Args = makePair(APP, 0, after_open_12_1, SUB);
Term Rslt_38 = newTerm(VAR, 0, portLoc(2, Rslt_38Args));
#ifdef STRICT
pushRedex(Rslt_38Args, newRef(glblProto1876_2105));
#else
swap(termLoc(Rslt_38), makePair(LAZ, 0, Rslt_38Args, newRef(glblProto1876_2105)));
#endif
// call count at /home/jim/toccata/interpreter/intrp-rdr.toc: 202
Term Rslt_39Args = makePair(APP, 0, Rslt_38, SUB);
Term Rslt_39 = newTerm(VAR, 0, portLoc(2, Rslt_39Args));
#ifdef STRICT
pushRedex(Rslt_39Args, newRef(glblProto64_2107));
#else
swap(termLoc(Rslt_39), makePair(LAZ, 0, Rslt_39Args, newRef(glblProto64_2107)));
#endif
// call .input at /home/jim/toccata/interpreter/intrp-rdr.toc: 203
Term Rslt_40Args = makePair(APP, 0, at_close_16_1, SUB);
Term Rslt_40 = newTerm(VAR, 0, portLoc(2, Rslt_40Args));
#ifdef STRICT
pushRedex(Rslt_40Args, newRef(glblProto1876_2110));
#else
swap(termLoc(Rslt_40), makePair(LAZ, 0, Rslt_40Args, newRef(glblProto1876_2110)));
#endif
// call count at /home/jim/toccata/interpreter/intrp-rdr.toc: 203
Term Rslt_41Args = makePair(APP, 0, Rslt_40, SUB);
Term Rslt_41 = newTerm(VAR, 0, portLoc(2, Rslt_41Args));
#ifdef STRICT
pushRedex(Rslt_41Args, newRef(glblProto64_2112));
#else
swap(termLoc(Rslt_41), makePair(LAZ, 0, Rslt_41Args, newRef(glblProto64_2112)));
#endif
Term Rslt_42 = makeOp(OP_SUB, Rslt_39, Rslt_41);
// call  at /home/jim/toccata/interpreter/intrp-rdr.toc: 202
Term Rslt_43Args = makePair(APP, 0, Rslt_42, SUB);
Term Rslt_43 = newTerm(VAR, 0, portLoc(2, Rslt_43Args));
#ifdef STRICT
pushRedex(Rslt_43Args, seq_21);
#else
swap(termLoc(Rslt_43), makePair(LAZ, 0, Rslt_43Args, seq_21));
#endif

// link args to body
swap(r_seq_17, Rslt_43);

Term Rslt_43V = get(termLoc(Rslt_43));
apps = take(portLoc(1, Rslt_43V));
lams = take(portLoc(2, Rslt_43V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_43), makePair(LAZ, 0, apps, lams));
// call read-string-content at /home/jim/toccata/interpreter/intrp-rdr.toc: 200
Term Rslt_44Args = makePair(APP, 0, after_open_12_2, SUB);
Term Rslt_44 = newTerm(VAR, 0, portLoc(2, Rslt_44Args));
#ifdef STRICT
pushRedex(Rslt_44Args, glblFn1746);
#else
swap(termLoc(Rslt_44), makePair(LAZ, 0, Rslt_44Args, glblFn1746));
#endif

// call  at /home/jim/toccata/interpreter/intrp-rdr.toc: 200
Term Rslt_45Args = makePair(APP, 0, Rslt_44, SUB);
Term Rslt_45 = newTerm(VAR, 0, portLoc(2, Rslt_45Args));
#ifdef STRICT
pushRedex(Rslt_45Args, seq_17);
#else
swap(termLoc(Rslt_45), makePair(LAZ, 0, Rslt_45Args, seq_17));
#endif

// link args to body
swap(r_seq_13, Rslt_45);

Term Rslt_45V = get(termLoc(Rslt_45));
apps = take(portLoc(1, Rslt_45V));
lams = take(portLoc(2, Rslt_45V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_45), makePair(LAZ, 0, apps, lams));
// call take-char at /home/jim/toccata/interpreter/intrp-rdr.toc: 199
Term Rslt_46Args = makePair(APP, 0, state_1, SUB);
Term Rslt_46 = newTerm(VAR, 0, portLoc(2, Rslt_46Args));
#ifdef STRICT
pushRedex(Rslt_46Args, glblFn1732);
#else
swap(termLoc(Rslt_46), makePair(LAZ, 0, Rslt_46Args, glblFn1732));
#endif

// call  at /home/jim/toccata/interpreter/intrp-rdr.toc: 199
Term Rslt_47Args = makePair(APP, 0, Rslt_46, SUB);
Term Rslt_47 = newTerm(VAR, 0, portLoc(2, Rslt_47Args));
#ifdef STRICT
pushRedex(Rslt_47Args, seq_13);
#else
swap(termLoc(Rslt_47), makePair(LAZ, 0, Rslt_47Args, seq_13));
#endif

// call vect-conj at /home/jim/toccata/interpreter/intrp-rdr.toc: 207
Term Rslt_48Args = makePair(APP, 0, glblStr402, SUB);
Term Rslt_48 = newTerm(VAR, 0, portLoc(2, Rslt_48Args));
Rslt_48Args = makePair(APP, 0, glblVal175_35, Rslt_48Args);
#ifdef STRICT
pushRedex(Rslt_48Args, glblFn178);
#else
swap(termLoc(Rslt_48), makePair(LAZ, 0, Rslt_48Args, glblFn178));
#endif

// call cond at /home/jim/toccata/interpreter/intrp-rdr.toc: 196
Term Rslt_49Args = makePair(APP, 0, Rslt_48, SUB);
Term Rslt_49 = newTerm(VAR, 0, portLoc(2, Rslt_49Args));
Rslt_49Args = makePair(APP, 0, Rslt_47, Rslt_49Args);
Rslt_49Args = makePair(APP, 0, Rslt_10, Rslt_49Args);
#ifdef STRICT
pushRedex(Rslt_49Args, newRef(glblProto34_2114));
#else
swap(termLoc(Rslt_49), makePair(LAZ, 0, Rslt_49Args, newRef(glblProto34_2114)));
#endif
// call cond at /home/jim/toccata/interpreter/intrp-rdr.toc: 196
Term Rslt_50Args = makePair(APP, 0, Rslt_49, SUB);
Term Rslt_50 = newTerm(VAR, 0, portLoc(2, Rslt_50Args));
Rslt_50Args = makePair(APP, 0, Rslt_9, Rslt_50Args);
Rslt_50Args = makePair(APP, 0, Rslt_8, Rslt_50Args);
#ifdef STRICT
pushRedex(Rslt_50Args, newRef(glblProto34_2116));
#else
swap(termLoc(Rslt_50), makePair(LAZ, 0, Rslt_50Args, newRef(glblProto34_2116)));
#endif
// link args to body
swap(r_seq_6, Rslt_50);

Term Rslt_50V = get(termLoc(Rslt_50));
apps = take(portLoc(1, Rslt_50V));
lams = take(portLoc(2, Rslt_50V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_50), makePair(LAZ, 0, apps, lams));
// call peek-char at /home/jim/toccata/interpreter/intrp-rdr.toc: 195
Term Rslt_51Args = makePair(APP, 0, state_1_1, SUB);
Term Rslt_51 = newTerm(VAR, 0, portLoc(2, Rslt_51Args));
#ifdef STRICT
pushRedex(Rslt_51Args, glblFn1730);
#else
swap(termLoc(Rslt_51), makePair(LAZ, 0, Rslt_51Args, glblFn1730));
#endif

// call  at /home/jim/toccata/interpreter/intrp-rdr.toc: 195
Term Rslt_52Args = makePair(APP, 0, Rslt_51, SUB);
Term Rslt_52 = newTerm(VAR, 0, portLoc(2, Rslt_52Args));
#ifdef STRICT
pushRedex(Rslt_52Args, seq_6);
#else
swap(termLoc(Rslt_52), makePair(LAZ, 0, Rslt_52Args, seq_6));
#endif

// link args to body
swap(r_seq_2, Rslt_52);

Term Rslt_52V = get(termLoc(Rslt_52));
apps = take(portLoc(1, Rslt_52V));
lams = take(portLoc(2, Rslt_52V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_52), makePair(LAZ, 0, apps, lams));
pushRedex(args, seq_2);
return;
}
Term glblFn1748 = newRef(glblCFn1747);
void glblProto34_2120(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(220), args);
  args = makePair(APP, 0, glblStr1912, args);
  interact(args, glblProto34);
}
}
void glblProto34_2122(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(220), args);
  args = makePair(APP, 0, glblStr1912, args);
  interact(args, glblProto34);
}
}
Term glblFn1750;
void glblCFn1749(Term ref, Term args) {
Term apps, lams, V;
Location vLoc;
Term seq_2 = makePair(LAM, 0, SUB, NUL);
Location r_seq_2 = portLoc(2, seq_2);
Term state_1 = newTerm(VAR, 0, portLoc(1, seq_2));
// allocate args at /home/jim/toccata/interpreter/intrp-rdr.toc: 218

Term state_1_1;
state_1 = dupeArg(state_1, &state_1_1, 291);
Term seq_6 = makePair(LAM, 0, SUB, NUL);
Location r_seq_6 = portLoc(2, seq_6);
Term c_5 = newTerm(VAR, 0, portLoc(1, seq_6));
// allocate args at /home/jim/toccata/interpreter/intrp-rdr.toc: 219

Term c_5_1;
c_5 = dupeArg(c_5, &c_5_1, 0);
Term glblVal175_37;
glblVal175 = dupeArg(glblVal175, &glblVal175_37, 0);

Term seq_13 = makePair(LAM, 0, SUB, NUL);
Location r_seq_13 = portLoc(2, seq_13);
Term r_12 = newTerm(VAR, 0, portLoc(1, seq_13));
// allocate args at /home/jim/toccata/interpreter/intrp-rdr.toc: 223
Term glblVal175_36;
glblVal175 = dupeArg(glblVal175, &glblVal175_36, 0);

// call str= at /home/jim/toccata/interpreter/intrp-rdr.toc: 220
Term Rslt_8Args = makePair(APP, 0, glblStr402, SUB);
Term Rslt_8 = newTerm(VAR, 0, portLoc(2, Rslt_8Args));
Rslt_8Args = makePair(APP, 0, c_5, Rslt_8Args);
#ifdef STRICT
pushRedex(Rslt_8Args, glblFn363);
#else
swap(termLoc(Rslt_8), makePair(LAZ, 0, Rslt_8Args, glblFn363));
#endif

// call vect-conj at /home/jim/toccata/interpreter/intrp-rdr.toc: 221
Term Rslt_9Args = makePair(APP, 0, glblStr402, SUB);
Term Rslt_9 = newTerm(VAR, 0, portLoc(2, Rslt_9Args));
Rslt_9Args = makePair(APP, 0, glblVal175_36, Rslt_9Args);
#ifdef STRICT
pushRedex(Rslt_9Args, glblFn178);
#else
swap(termLoc(Rslt_9), makePair(LAZ, 0, Rslt_9Args, glblFn178));
#endif

// call symbol-start? at /home/jim/toccata/interpreter/intrp-rdr.toc: 222
Term Rslt_10Args = makePair(APP, 0, c_5_1, SUB);
Term Rslt_10 = newTerm(VAR, 0, portLoc(2, Rslt_10Args));
#ifdef STRICT
pushRedex(Rslt_10Args, glblFn1724);
#else
swap(termLoc(Rslt_10), makePair(LAZ, 0, Rslt_10Args, glblFn1724));
#endif

// link args to body
swap(r_seq_13, r_12);

V = get(r_seq_13);
apps = makePair(APP, 0, V, SUB);
lams = makePair(LAM, 0, SUB, NUL);
swap(portLoc(2, lams),newTerm(VAR, 0, portLoc(1, lams)));
vLoc = portLoc(2, apps);
swap(vLoc, makePair(LAZ, 0, apps, lams));
swap(r_seq_13, newTerm(VAR, 0, vLoc));
// call read-run at /home/jim/toccata/interpreter/intrp-rdr.toc: 223
Term Rslt_15Args = makePair(APP, 0, glblFn1726, SUB);
Term Rslt_15 = newTerm(VAR, 0, portLoc(2, Rslt_15Args));
Rslt_15Args = makePair(APP, 0, state_1, Rslt_15Args);
#ifdef STRICT
pushRedex(Rslt_15Args, glblFn1740);
#else
swap(termLoc(Rslt_15), makePair(LAZ, 0, Rslt_15Args, glblFn1740));
#endif

// call  at /home/jim/toccata/interpreter/intrp-rdr.toc: 223
Term Rslt_16Args = makePair(APP, 0, Rslt_15, SUB);
Term Rslt_16 = newTerm(VAR, 0, portLoc(2, Rslt_16Args));
#ifdef STRICT
pushRedex(Rslt_16Args, seq_13);
#else
swap(termLoc(Rslt_16), makePair(LAZ, 0, Rslt_16Args, seq_13));
#endif

// call vect-conj at /home/jim/toccata/interpreter/intrp-rdr.toc: 224
Term Rslt_17Args = makePair(APP, 0, glblStr402, SUB);
Term Rslt_17 = newTerm(VAR, 0, portLoc(2, Rslt_17Args));
Rslt_17Args = makePair(APP, 0, glblVal175_37, Rslt_17Args);
#ifdef STRICT
pushRedex(Rslt_17Args, glblFn178);
#else
swap(termLoc(Rslt_17), makePair(LAZ, 0, Rslt_17Args, glblFn178));
#endif

// call cond at /home/jim/toccata/interpreter/intrp-rdr.toc: 220
Term Rslt_18Args = makePair(APP, 0, Rslt_17, SUB);
Term Rslt_18 = newTerm(VAR, 0, portLoc(2, Rslt_18Args));
Rslt_18Args = makePair(APP, 0, Rslt_16, Rslt_18Args);
Rslt_18Args = makePair(APP, 0, Rslt_10, Rslt_18Args);
#ifdef STRICT
pushRedex(Rslt_18Args, newRef(glblProto34_2120));
#else
swap(termLoc(Rslt_18), makePair(LAZ, 0, Rslt_18Args, newRef(glblProto34_2120)));
#endif
// call cond at /home/jim/toccata/interpreter/intrp-rdr.toc: 220
Term Rslt_19Args = makePair(APP, 0, Rslt_18, SUB);
Term Rslt_19 = newTerm(VAR, 0, portLoc(2, Rslt_19Args));
Rslt_19Args = makePair(APP, 0, Rslt_9, Rslt_19Args);
Rslt_19Args = makePair(APP, 0, Rslt_8, Rslt_19Args);
#ifdef STRICT
pushRedex(Rslt_19Args, newRef(glblProto34_2122));
#else
swap(termLoc(Rslt_19), makePair(LAZ, 0, Rslt_19Args, newRef(glblProto34_2122)));
#endif
// link args to body
swap(r_seq_6, Rslt_19);

Term Rslt_19V = get(termLoc(Rslt_19));
apps = take(portLoc(1, Rslt_19V));
lams = take(portLoc(2, Rslt_19V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_19), makePair(LAZ, 0, apps, lams));
// call peek-char at /home/jim/toccata/interpreter/intrp-rdr.toc: 219
Term Rslt_20Args = makePair(APP, 0, state_1_1, SUB);
Term Rslt_20 = newTerm(VAR, 0, portLoc(2, Rslt_20Args));
#ifdef STRICT
pushRedex(Rslt_20Args, glblFn1730);
#else
swap(termLoc(Rslt_20), makePair(LAZ, 0, Rslt_20Args, glblFn1730));
#endif

// call  at /home/jim/toccata/interpreter/intrp-rdr.toc: 219
Term Rslt_21Args = makePair(APP, 0, Rslt_20, SUB);
Term Rslt_21 = newTerm(VAR, 0, portLoc(2, Rslt_21Args));
#ifdef STRICT
pushRedex(Rslt_21Args, seq_6);
#else
swap(termLoc(Rslt_21), makePair(LAZ, 0, Rslt_21Args, seq_6));
#endif

// link args to body
swap(r_seq_2, Rslt_21);

Term Rslt_21V = get(termLoc(Rslt_21));
apps = take(portLoc(1, Rslt_21V));
lams = take(portLoc(2, Rslt_21V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_21), makePair(LAZ, 0, apps, lams));
pushRedex(args, seq_2);
return;
}
Term glblFn1750 = newRef(glblCFn1749);
void glblProto34_2128(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(229), args);
  args = makePair(APP, 0, glblStr1912, args);
  interact(args, glblProto34);
}
}
void glblProto34_2130(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(229), args);
  args = makePair(APP, 0, glblStr1912, args);
  interact(args, glblProto34);
}
}
Term glblFn1752;
void glblCFn1751(Term ref, Term args) {
Term apps, lams, V;
Location vLoc;
Term seq_2 = makePair(LAM, 0, SUB, NUL);
Location r_seq_2 = portLoc(2, seq_2);
Term state_1 = newTerm(VAR, 0, portLoc(1, seq_2));
// allocate args at /home/jim/toccata/interpreter/intrp-rdr.toc: 227

Term state_1_1;
state_1 = dupeArg(state_1, &state_1_1, 292);
Term seq_6 = makePair(LAM, 0, SUB, NUL);
Location r_seq_6 = portLoc(2, seq_6);
Term c_5 = newTerm(VAR, 0, portLoc(1, seq_6));
// allocate args at /home/jim/toccata/interpreter/intrp-rdr.toc: 228

Term c_5_1;
c_5 = dupeArg(c_5, &c_5_1, 0);
Term glblVal175_39;
glblVal175 = dupeArg(glblVal175, &glblVal175_39, 0);

Term seq_13 = makePair(LAM, 0, SUB, NUL);
Location r_seq_13 = portLoc(2, seq_13);
Term r_12 = newTerm(VAR, 0, portLoc(1, seq_13));
// allocate args at /home/jim/toccata/interpreter/intrp-rdr.toc: 232
Term glblVal175_38;
glblVal175 = dupeArg(glblVal175, &glblVal175_38, 0);

// call str= at /home/jim/toccata/interpreter/intrp-rdr.toc: 229
Term Rslt_8Args = makePair(APP, 0, glblStr402, SUB);
Term Rslt_8 = newTerm(VAR, 0, portLoc(2, Rslt_8Args));
Rslt_8Args = makePair(APP, 0, c_5, Rslt_8Args);
#ifdef STRICT
pushRedex(Rslt_8Args, glblFn363);
#else
swap(termLoc(Rslt_8), makePair(LAZ, 0, Rslt_8Args, glblFn363));
#endif

// call vect-conj at /home/jim/toccata/interpreter/intrp-rdr.toc: 230
Term Rslt_9Args = makePair(APP, 0, glblStr402, SUB);
Term Rslt_9 = newTerm(VAR, 0, portLoc(2, Rslt_9Args));
Rslt_9Args = makePair(APP, 0, glblVal175_38, Rslt_9Args);
#ifdef STRICT
pushRedex(Rslt_9Args, glblFn178);
#else
swap(termLoc(Rslt_9), makePair(LAZ, 0, Rslt_9Args, glblFn178));
#endif

// call is-digit at /home/jim/toccata/interpreter/intrp-rdr.toc: 231
Term Rslt_10Args = makePair(APP, 0, c_5_1, SUB);
Term Rslt_10 = newTerm(VAR, 0, portLoc(2, Rslt_10Args));
#ifdef STRICT
pushRedex(Rslt_10Args, glblFn1720);
#else
swap(termLoc(Rslt_10), makePair(LAZ, 0, Rslt_10Args, glblFn1720));
#endif

// link args to body
swap(r_seq_13, r_12);

V = get(r_seq_13);
apps = makePair(APP, 0, V, SUB);
lams = makePair(LAM, 0, SUB, NUL);
swap(portLoc(2, lams),newTerm(VAR, 0, portLoc(1, lams)));
vLoc = portLoc(2, apps);
swap(vLoc, makePair(LAZ, 0, apps, lams));
swap(r_seq_13, newTerm(VAR, 0, vLoc));
// call read-run at /home/jim/toccata/interpreter/intrp-rdr.toc: 232
Term Rslt_15Args = makePair(APP, 0, glblFn1720, SUB);
Term Rslt_15 = newTerm(VAR, 0, portLoc(2, Rslt_15Args));
Rslt_15Args = makePair(APP, 0, state_1, Rslt_15Args);
#ifdef STRICT
pushRedex(Rslt_15Args, glblFn1740);
#else
swap(termLoc(Rslt_15), makePair(LAZ, 0, Rslt_15Args, glblFn1740));
#endif

// call  at /home/jim/toccata/interpreter/intrp-rdr.toc: 232
Term Rslt_16Args = makePair(APP, 0, Rslt_15, SUB);
Term Rslt_16 = newTerm(VAR, 0, portLoc(2, Rslt_16Args));
#ifdef STRICT
pushRedex(Rslt_16Args, seq_13);
#else
swap(termLoc(Rslt_16), makePair(LAZ, 0, Rslt_16Args, seq_13));
#endif

// call vect-conj at /home/jim/toccata/interpreter/intrp-rdr.toc: 233
Term Rslt_17Args = makePair(APP, 0, glblStr402, SUB);
Term Rslt_17 = newTerm(VAR, 0, portLoc(2, Rslt_17Args));
Rslt_17Args = makePair(APP, 0, glblVal175_39, Rslt_17Args);
#ifdef STRICT
pushRedex(Rslt_17Args, glblFn178);
#else
swap(termLoc(Rslt_17), makePair(LAZ, 0, Rslt_17Args, glblFn178));
#endif

// call cond at /home/jim/toccata/interpreter/intrp-rdr.toc: 229
Term Rslt_18Args = makePair(APP, 0, Rslt_17, SUB);
Term Rslt_18 = newTerm(VAR, 0, portLoc(2, Rslt_18Args));
Rslt_18Args = makePair(APP, 0, Rslt_16, Rslt_18Args);
Rslt_18Args = makePair(APP, 0, Rslt_10, Rslt_18Args);
#ifdef STRICT
pushRedex(Rslt_18Args, newRef(glblProto34_2128));
#else
swap(termLoc(Rslt_18), makePair(LAZ, 0, Rslt_18Args, newRef(glblProto34_2128)));
#endif
// call cond at /home/jim/toccata/interpreter/intrp-rdr.toc: 229
Term Rslt_19Args = makePair(APP, 0, Rslt_18, SUB);
Term Rslt_19 = newTerm(VAR, 0, portLoc(2, Rslt_19Args));
Rslt_19Args = makePair(APP, 0, Rslt_9, Rslt_19Args);
Rslt_19Args = makePair(APP, 0, Rslt_8, Rslt_19Args);
#ifdef STRICT
pushRedex(Rslt_19Args, newRef(glblProto34_2130));
#else
swap(termLoc(Rslt_19), makePair(LAZ, 0, Rslt_19Args, newRef(glblProto34_2130)));
#endif
// link args to body
swap(r_seq_6, Rslt_19);

Term Rslt_19V = get(termLoc(Rslt_19));
apps = take(portLoc(1, Rslt_19V));
lams = take(portLoc(2, Rslt_19V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_19), makePair(LAZ, 0, apps, lams));
// call peek-char at /home/jim/toccata/interpreter/intrp-rdr.toc: 228
Term Rslt_20Args = makePair(APP, 0, state_1_1, SUB);
Term Rslt_20 = newTerm(VAR, 0, portLoc(2, Rslt_20Args));
#ifdef STRICT
pushRedex(Rslt_20Args, glblFn1730);
#else
swap(termLoc(Rslt_20), makePair(LAZ, 0, Rslt_20Args, glblFn1730));
#endif

// call  at /home/jim/toccata/interpreter/intrp-rdr.toc: 228
Term Rslt_21Args = makePair(APP, 0, Rslt_20, SUB);
Term Rslt_21 = newTerm(VAR, 0, portLoc(2, Rslt_21Args));
#ifdef STRICT
pushRedex(Rslt_21Args, seq_6);
#else
swap(termLoc(Rslt_21), makePair(LAZ, 0, Rslt_21Args, seq_6));
#endif

// link args to body
swap(r_seq_2, Rslt_21);

Term Rslt_21V = get(termLoc(Rslt_21));
apps = take(portLoc(1, Rslt_21V));
lams = take(portLoc(2, Rslt_21V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_21), makePair(LAZ, 0, apps, lams));
pushRedex(args, seq_2);
return;
}
Term glblFn1752 = newRef(glblCFn1751);
void glblProto34_2136(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(239), args);
  args = makePair(APP, 0, glblStr1912, args);
  interact(args, glblProto34);
}
}
void glblProto34_2138(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(239), args);
  args = makePair(APP, 0, glblStr1912, args);
  interact(args, glblProto34);
}
}
Term glblFn1754;
void glblCFn1753(Term ref, Term args) {
Term apps, lams, V;
Location vLoc;
Term seq_2 = makePair(LAM, 0, SUB, NUL);
Location r_seq_2 = portLoc(2, seq_2);
Term state_1 = newTerm(VAR, 0, portLoc(1, seq_2));
// allocate args at /home/jim/toccata/interpreter/intrp-rdr.toc: 237

Term state_1_1;
state_1 = dupeArg(state_1, &state_1_1, 293);
Term seq_6 = makePair(LAM, 0, SUB, NUL);
Location r_seq_6 = portLoc(2, seq_6);
Term c_5 = newTerm(VAR, 0, portLoc(1, seq_6));
// allocate args at /home/jim/toccata/interpreter/intrp-rdr.toc: 238

Term c_5_1;
c_5 = dupeArg(c_5, &c_5_1, 0);
Term glblVal175_41;
glblVal175 = dupeArg(glblVal175, &glblVal175_41, 0);

Term seq_13 = makePair(LAM, 0, SUB, NUL);
Location r_seq_13 = portLoc(2, seq_13);
Term r_12 = newTerm(VAR, 0, portLoc(1, seq_13));
// allocate args at /home/jim/toccata/interpreter/intrp-rdr.toc: 242
Term glblVal175_40;
glblVal175 = dupeArg(glblVal175, &glblVal175_40, 0);

// call str= at /home/jim/toccata/interpreter/intrp-rdr.toc: 239
Term Rslt_8Args = makePair(APP, 0, glblStr402, SUB);
Term Rslt_8 = newTerm(VAR, 0, portLoc(2, Rslt_8Args));
Rslt_8Args = makePair(APP, 0, c_5, Rslt_8Args);
#ifdef STRICT
pushRedex(Rslt_8Args, glblFn363);
#else
swap(termLoc(Rslt_8), makePair(LAZ, 0, Rslt_8Args, glblFn363));
#endif

// call vect-conj at /home/jim/toccata/interpreter/intrp-rdr.toc: 240
Term Rslt_9Args = makePair(APP, 0, glblStr402, SUB);
Term Rslt_9 = newTerm(VAR, 0, portLoc(2, Rslt_9Args));
Rslt_9Args = makePair(APP, 0, glblVal175_40, Rslt_9Args);
#ifdef STRICT
pushRedex(Rslt_9Args, glblFn178);
#else
swap(termLoc(Rslt_9), makePair(LAZ, 0, Rslt_9Args, glblFn178));
#endif

// call is-digit at /home/jim/toccata/interpreter/intrp-rdr.toc: 241
Term Rslt_10Args = makePair(APP, 0, c_5_1, SUB);
Term Rslt_10 = newTerm(VAR, 0, portLoc(2, Rslt_10Args));
#ifdef STRICT
pushRedex(Rslt_10Args, glblFn1720);
#else
swap(termLoc(Rslt_10), makePair(LAZ, 0, Rslt_10Args, glblFn1720));
#endif

// link args to body
swap(r_seq_13, r_12);

V = get(r_seq_13);
apps = makePair(APP, 0, V, SUB);
lams = makePair(LAM, 0, SUB, NUL);
swap(portLoc(2, lams),newTerm(VAR, 0, portLoc(1, lams)));
vLoc = portLoc(2, apps);
swap(vLoc, makePair(LAZ, 0, apps, lams));
swap(r_seq_13, newTerm(VAR, 0, vLoc));
// call read-run at /home/jim/toccata/interpreter/intrp-rdr.toc: 242
Term Rslt_15Args = makePair(APP, 0, glblFn1728, SUB);
Term Rslt_15 = newTerm(VAR, 0, portLoc(2, Rslt_15Args));
Rslt_15Args = makePair(APP, 0, state_1, Rslt_15Args);
#ifdef STRICT
pushRedex(Rslt_15Args, glblFn1740);
#else
swap(termLoc(Rslt_15), makePair(LAZ, 0, Rslt_15Args, glblFn1740));
#endif

// call  at /home/jim/toccata/interpreter/intrp-rdr.toc: 242
Term Rslt_16Args = makePair(APP, 0, Rslt_15, SUB);
Term Rslt_16 = newTerm(VAR, 0, portLoc(2, Rslt_16Args));
#ifdef STRICT
pushRedex(Rslt_16Args, seq_13);
#else
swap(termLoc(Rslt_16), makePair(LAZ, 0, Rslt_16Args, seq_13));
#endif

// call vect-conj at /home/jim/toccata/interpreter/intrp-rdr.toc: 243
Term Rslt_17Args = makePair(APP, 0, glblStr402, SUB);
Term Rslt_17 = newTerm(VAR, 0, portLoc(2, Rslt_17Args));
Rslt_17Args = makePair(APP, 0, glblVal175_41, Rslt_17Args);
#ifdef STRICT
pushRedex(Rslt_17Args, glblFn178);
#else
swap(termLoc(Rslt_17), makePair(LAZ, 0, Rslt_17Args, glblFn178));
#endif

// call cond at /home/jim/toccata/interpreter/intrp-rdr.toc: 239
Term Rslt_18Args = makePair(APP, 0, Rslt_17, SUB);
Term Rslt_18 = newTerm(VAR, 0, portLoc(2, Rslt_18Args));
Rslt_18Args = makePair(APP, 0, Rslt_16, Rslt_18Args);
Rslt_18Args = makePair(APP, 0, Rslt_10, Rslt_18Args);
#ifdef STRICT
pushRedex(Rslt_18Args, newRef(glblProto34_2136));
#else
swap(termLoc(Rslt_18), makePair(LAZ, 0, Rslt_18Args, newRef(glblProto34_2136)));
#endif
// call cond at /home/jim/toccata/interpreter/intrp-rdr.toc: 239
Term Rslt_19Args = makePair(APP, 0, Rslt_18, SUB);
Term Rslt_19 = newTerm(VAR, 0, portLoc(2, Rslt_19Args));
Rslt_19Args = makePair(APP, 0, Rslt_9, Rslt_19Args);
Rslt_19Args = makePair(APP, 0, Rslt_8, Rslt_19Args);
#ifdef STRICT
pushRedex(Rslt_19Args, newRef(glblProto34_2138));
#else
swap(termLoc(Rslt_19), makePair(LAZ, 0, Rslt_19Args, newRef(glblProto34_2138)));
#endif
// link args to body
swap(r_seq_6, Rslt_19);

Term Rslt_19V = get(termLoc(Rslt_19));
apps = take(portLoc(1, Rslt_19V));
lams = take(portLoc(2, Rslt_19V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_19), makePair(LAZ, 0, apps, lams));
// call peek-char at /home/jim/toccata/interpreter/intrp-rdr.toc: 238
Term Rslt_20Args = makePair(APP, 0, state_1_1, SUB);
Term Rslt_20 = newTerm(VAR, 0, portLoc(2, Rslt_20Args));
#ifdef STRICT
pushRedex(Rslt_20Args, glblFn1730);
#else
swap(termLoc(Rslt_20), makePair(LAZ, 0, Rslt_20Args, glblFn1730));
#endif

// call  at /home/jim/toccata/interpreter/intrp-rdr.toc: 238
Term Rslt_21Args = makePair(APP, 0, Rslt_20, SUB);
Term Rslt_21 = newTerm(VAR, 0, portLoc(2, Rslt_21Args));
#ifdef STRICT
pushRedex(Rslt_21Args, seq_6);
#else
swap(termLoc(Rslt_21), makePair(LAZ, 0, Rslt_21Args, seq_6));
#endif

// link args to body
swap(r_seq_2, Rslt_21);

Term Rslt_21V = get(termLoc(Rslt_21));
apps = take(portLoc(1, Rslt_21V));
lams = take(portLoc(2, Rslt_21V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_21), makePair(LAZ, 0, apps, lams));
pushRedex(args, seq_2);
return;
}
Term glblFn1754 = newRef(glblCFn1753);
void glblProto78_2141(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(277), args);
  args = makePair(APP, 0, glblStr1912, args);
  interact(args, glblProto78);
}
}
void glblProto78_2143(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(277), args);
  args = makePair(APP, 0, glblStr1912, args);
  interact(args, glblProto78);
}
}
void glblProto76_2145(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(277), args);
  args = makePair(APP, 0, glblStr1912, args);
  interact(args, glblProto76);
}
}
void glblProto56_2148(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(277), args);
  args = makePair(APP, 0, glblStr1912, args);
  interact(args, glblProto56);
}
}
Term glblFn1756;
void glblCFn1755(Term ref, Term args) {
Term apps, lams, V;
Location vLoc;
Term seq_2 = makePair(LAM, 0, SUB, NUL);
Location r_seq_2 = portLoc(2, seq_2);
Term v_1 = newTerm(VAR, 0, portLoc(1, seq_2));
// allocate args at /home/jim/toccata/interpreter/intrp-rdr.toc: 276
// call rest at /home/jim/toccata/interpreter/intrp-rdr.toc: 277
Term Rslt_4Args = makePair(APP, 0, v_1, SUB);
Term Rslt_4 = newTerm(VAR, 0, portLoc(2, Rslt_4Args));
#ifdef STRICT
pushRedex(Rslt_4Args, newRef(glblProto78_2141));
#else
swap(termLoc(Rslt_4), makePair(LAZ, 0, Rslt_4Args, newRef(glblProto78_2141)));
#endif
// call rest at /home/jim/toccata/interpreter/intrp-rdr.toc: 277
Term Rslt_5Args = makePair(APP, 0, Rslt_4, SUB);
Term Rslt_5 = newTerm(VAR, 0, portLoc(2, Rslt_5Args));
#ifdef STRICT
pushRedex(Rslt_5Args, newRef(glblProto78_2143));
#else
swap(termLoc(Rslt_5), makePair(LAZ, 0, Rslt_5Args, newRef(glblProto78_2143)));
#endif
// call first at /home/jim/toccata/interpreter/intrp-rdr.toc: 277
Term Rslt_6Args = makePair(APP, 0, Rslt_5, SUB);
Term Rslt_6 = newTerm(VAR, 0, portLoc(2, Rslt_6Args));
#ifdef STRICT
pushRedex(Rslt_6Args, newRef(glblProto76_2145));
#else
swap(termLoc(Rslt_6), makePair(LAZ, 0, Rslt_6Args, newRef(glblProto76_2145)));
#endif
// call extract at /home/jim/toccata/interpreter/intrp-rdr.toc: 277
Term Rslt_7Args = makePair(APP, 0, Rslt_6, SUB);
Term Rslt_7 = newTerm(VAR, 0, portLoc(2, Rslt_7Args));
#ifdef STRICT
pushRedex(Rslt_7Args, newRef(glblProto56_2148));
#else
swap(termLoc(Rslt_7), makePair(LAZ, 0, Rslt_7Args, newRef(glblProto56_2148)));
#endif
// link args to body
swap(r_seq_2, Rslt_7);

Term Rslt_7V = get(termLoc(Rslt_7));
apps = take(portLoc(1, Rslt_7V));
lams = take(portLoc(2, Rslt_7V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_7), makePair(LAZ, 0, apps, lams));
pushRedex(args, seq_2);
return;
}
Term glblFn1756 = newRef(glblCFn1755);
Term glblProto1757;
Term glblresult_kind2150;
void glblresult_kind2147(Term ref, Term args) {
Term apps, lams, V;
Location vLoc;
Term seq_5 = makePair(LAM, 0, SUB, NUL);
Location r_seq_5 = portLoc(2, seq_5);
Term arg_4 = newTerm(VAR, 0, portLoc(1, seq_5));
// allocate args at /home/jim/toccata/interpreter/intrp-rdr.toc: 282
swap(termLoc(arg_4), ERA);
// link args to body
swap(r_seq_5, newI60(0));

V = get(r_seq_5);
apps = makePair(APP, 0, V, SUB);
lams = makePair(LAM, 0, SUB, NUL);
swap(portLoc(2, lams),newTerm(VAR, 0, portLoc(1, lams)));
vLoc = portLoc(2, apps);
swap(vLoc, makePair(LAZ, 0, apps, lams));
swap(r_seq_5, newTerm(VAR, 0, vLoc));
pushRedex(args, seq_5);
return;
}
Term glblresult_kind2150 = newRef(glblresult_kind2147);
Term glblresult_kind2154;
void glblresult_kind2152(Term ref, Term args) {
Term apps, lams, V;
Location vLoc;
Term seq_9 = makePair(LAM, 0, SUB, NUL);
Location r_seq_9 = portLoc(2, seq_9);
Term arg_8 = newTerm(VAR, 0, portLoc(1, seq_9));
// allocate args at /home/jim/toccata/interpreter/intrp-rdr.toc: 283
swap(termLoc(arg_8), ERA);
// link args to body
swap(r_seq_9, newI60(1));

V = get(r_seq_9);
apps = makePair(APP, 0, V, SUB);
lams = makePair(LAM, 0, SUB, NUL);
swap(portLoc(2, lams),newTerm(VAR, 0, portLoc(1, lams)));
vLoc = portLoc(2, apps);
swap(vLoc, makePair(LAZ, 0, apps, lams));
swap(r_seq_9, newTerm(VAR, 0, vLoc));
pushRedex(args, seq_9);
return;
}
Term glblresult_kind2154 = newRef(glblresult_kind2152);
Term glblresult_kind2156;
void glblresult_kind2155(Term ref, Term args) {
Term apps, lams, V;
Location vLoc;
Term seq_13 = makePair(LAM, 0, SUB, NUL);
Location r_seq_13 = portLoc(2, seq_13);
Term arg_12 = newTerm(VAR, 0, portLoc(1, seq_13));
// allocate args at /home/jim/toccata/interpreter/intrp-rdr.toc: 284
swap(termLoc(arg_12), ERA);
// link args to body
swap(r_seq_13, newI60(2));

V = get(r_seq_13);
apps = makePair(APP, 0, V, SUB);
lams = makePair(LAM, 0, SUB, NUL);
swap(portLoc(2, lams),newTerm(VAR, 0, portLoc(1, lams)));
vLoc = portLoc(2, apps);
swap(vLoc, makePair(LAZ, 0, apps, lams));
swap(r_seq_13, newTerm(VAR, 0, vLoc));
pushRedex(args, seq_13);
return;
}
Term glblresult_kind2156 = newRef(glblresult_kind2155);
Term glblresult_kind2160;
void glblresult_kind2158(Term ref, Term args) {
Term apps, lams, V;
Location vLoc;
Term seq_17 = makePair(LAM, 0, SUB, NUL);
Location r_seq_17 = portLoc(2, seq_17);
Term arg_16 = newTerm(VAR, 0, portLoc(1, seq_17));
// allocate args at /home/jim/toccata/interpreter/intrp-rdr.toc: 285
swap(termLoc(arg_16), ERA);
// link args to body
swap(r_seq_17, newI60(3));

V = get(r_seq_17);
apps = makePair(APP, 0, V, SUB);
lams = makePair(LAM, 0, SUB, NUL);
swap(portLoc(2, lams),newTerm(VAR, 0, portLoc(1, lams)));
vLoc = portLoc(2, apps);
swap(vLoc, makePair(LAZ, 0, apps, lams));
swap(r_seq_17, newTerm(VAR, 0, vLoc));
pushRedex(args, seq_17);
return;
}
Term glblresult_kind2160 = newRef(glblresult_kind2158);
Term glblProto1759;
Term glblexpr_kind2163;
void glblexpr_kind2162(Term ref, Term args) {
Term apps, lams, V;
Location vLoc;
Term seq_5 = makePair(LAM, 0, SUB, NUL);
Location r_seq_5 = portLoc(2, seq_5);
Term arg_4 = newTerm(VAR, 0, portLoc(1, seq_5));
// allocate args at /home/jim/toccata/interpreter/intrp-rdr.toc: 289
swap(termLoc(arg_4), ERA);
// link args to body
swap(r_seq_5, newI60(0));

V = get(r_seq_5);
apps = makePair(APP, 0, V, SUB);
lams = makePair(LAM, 0, SUB, NUL);
swap(portLoc(2, lams),newTerm(VAR, 0, portLoc(1, lams)));
vLoc = portLoc(2, apps);
swap(vLoc, makePair(LAZ, 0, apps, lams));
swap(r_seq_5, newTerm(VAR, 0, vLoc));
pushRedex(args, seq_5);
return;
}
Term glblexpr_kind2163 = newRef(glblexpr_kind2162);
Term glblexpr_kind2166;
void glblexpr_kind2164(Term ref, Term args) {
Term apps, lams, V;
Location vLoc;
Term seq_9 = makePair(LAM, 0, SUB, NUL);
Location r_seq_9 = portLoc(2, seq_9);
Term arg_8 = newTerm(VAR, 0, portLoc(1, seq_9));
// allocate args at /home/jim/toccata/interpreter/intrp-rdr.toc: 290
swap(termLoc(arg_8), ERA);
// link args to body
swap(r_seq_9, newI60(1));

V = get(r_seq_9);
apps = makePair(APP, 0, V, SUB);
lams = makePair(LAM, 0, SUB, NUL);
swap(portLoc(2, lams),newTerm(VAR, 0, portLoc(1, lams)));
vLoc = portLoc(2, apps);
swap(vLoc, makePair(LAZ, 0, apps, lams));
swap(r_seq_9, newTerm(VAR, 0, vLoc));
pushRedex(args, seq_9);
return;
}
Term glblexpr_kind2166 = newRef(glblexpr_kind2164);
Term glblexpr_kind2168;
void glblexpr_kind2167(Term ref, Term args) {
Term apps, lams, V;
Location vLoc;
Term seq_13 = makePair(LAM, 0, SUB, NUL);
Location r_seq_13 = portLoc(2, seq_13);
Term arg_12 = newTerm(VAR, 0, portLoc(1, seq_13));
// allocate args at /home/jim/toccata/interpreter/intrp-rdr.toc: 291
swap(termLoc(arg_12), ERA);
// link args to body
swap(r_seq_13, newI60(2));

V = get(r_seq_13);
apps = makePair(APP, 0, V, SUB);
lams = makePair(LAM, 0, SUB, NUL);
swap(portLoc(2, lams),newTerm(VAR, 0, portLoc(1, lams)));
vLoc = portLoc(2, apps);
swap(vLoc, makePair(LAZ, 0, apps, lams));
swap(r_seq_13, newTerm(VAR, 0, vLoc));
pushRedex(args, seq_13);
return;
}
Term glblexpr_kind2168 = newRef(glblexpr_kind2167);
void glblProto1878_2172(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(295), args);
  args = makePair(APP, 0, glblStr1912, args);
  interact(args, glblProto1878);
}
}
void glblProto119_2174(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(295), args);
  args = makePair(APP, 0, glblStr1912, args);
  interact(args, glblProto119);
}
}
void glblProto56_2176(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(295), args);
  args = makePair(APP, 0, glblStr1912, args);
  interact(args, glblProto56);
}
}
Term glblFn1762;
void glblCFn1761(Term ref, Term args) {
Term apps, lams, V;
Location vLoc;
Term seq_2 = makePair(LAM, 0, SUB, NUL);
Location r_seq_2 = portLoc(2, seq_2);
Term state_1 = newTerm(VAR, 0, portLoc(1, seq_2));
// allocate args at /home/jim/toccata/interpreter/intrp-rdr.toc: 294

Term state_1_1;
state_1 = dupeArg(state_1, &state_1_1, 294);
Term seq_6 = makePair(LAM, 0, SUB, NUL);
Location r_seq_6 = portLoc(2, seq_6);
Term file_5 = newTerm(VAR, 0, portLoc(1, seq_6));
// allocate args at /home/jim/toccata/interpreter/intrp-rdr.toc: 295
Term glblVal1489_1;
glblVal1489 = dupeArg(glblVal1489, &glblVal1489_1, 0);

// call state-line at /home/jim/toccata/interpreter/intrp-rdr.toc: 296
Term Rslt_8Args = makePair(APP, 0, state_1, SUB);
Term Rslt_8 = newTerm(VAR, 0, portLoc(2, Rslt_8Args));
#ifdef STRICT
pushRedex(Rslt_8Args, glblFn1718);
#else
swap(termLoc(Rslt_8), makePair(LAZ, 0, Rslt_8Args, glblFn1718));
#endif

// call ast/Location at /home/jim/toccata/interpreter/intrp-rdr.toc: 296
Term Rslt_9Args = makePair(APP, 0, Rslt_8, SUB);
Term Rslt_9 = newTerm(VAR, 0, portLoc(2, Rslt_9Args));
Rslt_9Args = makePair(APP, 0, file_5, Rslt_9Args);
#ifdef STRICT
pushRedex(Rslt_9Args, glblVal1489_1);
#else
swap(termLoc(Rslt_9), makePair(LAZ, 0, Rslt_9Args, glblVal1489_1));
#endif

// link args to body
swap(r_seq_6, Rslt_9);

Term Rslt_9V = get(termLoc(Rslt_9));
apps = take(portLoc(1, Rslt_9V));
lams = take(portLoc(2, Rslt_9V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_9), makePair(LAZ, 0, apps, lams));
// call .values at /home/jim/toccata/interpreter/intrp-rdr.toc: 295
Term Rslt_10Args = makePair(APP, 0, state_1_1, SUB);
Term Rslt_10 = newTerm(VAR, 0, portLoc(2, Rslt_10Args));
#ifdef STRICT
pushRedex(Rslt_10Args, newRef(glblProto1878_2172));
#else
swap(termLoc(Rslt_10), makePair(LAZ, 0, Rslt_10Args, newRef(glblProto1878_2172)));
#endif
// call get at /home/jim/toccata/interpreter/intrp-rdr.toc: 295
Term Rslt_11Args = makePair(APP, 0, glblStr1910, SUB);
Term Rslt_11 = newTerm(VAR, 0, portLoc(2, Rslt_11Args));
Rslt_11Args = makePair(APP, 0, Rslt_10, Rslt_11Args);
#ifdef STRICT
pushRedex(Rslt_11Args, newRef(glblProto119_2174));
#else
swap(termLoc(Rslt_11), makePair(LAZ, 0, Rslt_11Args, newRef(glblProto119_2174)));
#endif
// call extract at /home/jim/toccata/interpreter/intrp-rdr.toc: 295
Term Rslt_12Args = makePair(APP, 0, Rslt_11, SUB);
Term Rslt_12 = newTerm(VAR, 0, portLoc(2, Rslt_12Args));
#ifdef STRICT
pushRedex(Rslt_12Args, newRef(glblProto56_2176));
#else
swap(termLoc(Rslt_12), makePair(LAZ, 0, Rslt_12Args, newRef(glblProto56_2176)));
#endif
// call  at /home/jim/toccata/interpreter/intrp-rdr.toc: 295
Term Rslt_13Args = makePair(APP, 0, Rslt_12, SUB);
Term Rslt_13 = newTerm(VAR, 0, portLoc(2, Rslt_13Args));
#ifdef STRICT
pushRedex(Rslt_13Args, seq_6);
#else
swap(termLoc(Rslt_13), makePair(LAZ, 0, Rslt_13Args, seq_6));
#endif

// link args to body
swap(r_seq_2, Rslt_13);

Term Rslt_13V = get(termLoc(Rslt_13));
apps = take(portLoc(1, Rslt_13V));
lams = take(portLoc(2, Rslt_13V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_13), makePair(LAZ, 0, apps, lams));
pushRedex(args, seq_2);
return;
}
Term glblFn1762 = newRef(glblCFn1761);
void glblProto72_2179(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(300), args);
  args = makePair(APP, 0, glblStr1912, args);
  interact(args, glblProto72);
}
}
Term glblFn1764;
void glblCFn1763(Term ref, Term args) {
Term apps, lams, V;
Location vLoc;
Term seq_3 = makePair(LAM, 0, SUB, NUL);
Location r_seq_3 = portLoc(2, seq_3);
Term b_2 = newTerm(VAR, 0, portLoc(1, seq_3));
seq_3 = makePair(LAM, 0, SUB, seq_3);
Term a_1 = newTerm(VAR, 0, portLoc(1, seq_3));
// allocate args at /home/jim/toccata/interpreter/intrp-rdr.toc: 299
// call reduce at /home/jim/toccata/interpreter/intrp-rdr.toc: 300
Term Rslt_5Args = makePair(APP, 0, glblFn178, SUB);
Term Rslt_5 = newTerm(VAR, 0, portLoc(2, Rslt_5Args));
Rslt_5Args = makePair(APP, 0, a_1, Rslt_5Args);
Rslt_5Args = makePair(APP, 0, b_2, Rslt_5Args);
#ifdef STRICT
pushRedex(Rslt_5Args, newRef(glblProto72_2179));
#else
swap(termLoc(Rslt_5), makePair(LAZ, 0, Rslt_5Args, newRef(glblProto72_2179)));
#endif
// link args to body
swap(r_seq_3, Rslt_5);

Term Rslt_5V = get(termLoc(Rslt_5));
apps = take(portLoc(1, Rslt_5V));
lams = take(portLoc(2, Rslt_5V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_5), makePair(LAZ, 0, apps, lams));
pushRedex(args, seq_3);
return;
}
Term glblFn1764 = newRef(glblCFn1763);
void glblProto34_2184(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(310), args);
  args = makePair(APP, 0, glblStr1912, args);
  interact(args, glblProto34);
}
}
Term glblFn1766;
void glblCFn1765(Term ref, Term args) {
Term apps, lams, V;
Location vLoc;
Term seq_2 = makePair(LAM, 0, SUB, NUL);
Location r_seq_2 = portLoc(2, seq_2);
Term state_1 = newTerm(VAR, 0, portLoc(1, seq_2));
// allocate args at /home/jim/toccata/interpreter/intrp-rdr.toc: 304
Term seq_6 = makePair(LAM, 0, SUB, NUL);
Location r_seq_6 = portLoc(2, seq_6);
Term first_pair_5 = newTerm(VAR, 0, portLoc(1, seq_6));
// allocate args at /home/jim/toccata/interpreter/intrp-rdr.toc: 305

Term first_pair_5_1;
first_pair_5 = dupeArg(first_pair_5, &first_pair_5_1, 0);
Term seq_10 = makePair(LAM, 0, SUB, NUL);
Location r_seq_10 = portLoc(2, seq_10);
Term fst_9 = newTerm(VAR, 0, portLoc(1, seq_10));
// allocate args at /home/jim/toccata/interpreter/intrp-rdr.toc: 306

Term fst_9_1;
fst_9 = dupeArg(fst_9, &fst_9_1, 0);
Term seq_14 = makePair(LAM, 0, SUB, NUL);
Location r_seq_14 = portLoc(2, seq_14);
Term s2_13 = newTerm(VAR, 0, portLoc(1, seq_14));
// allocate args at /home/jim/toccata/interpreter/intrp-rdr.toc: 307

Term s2_13_2;
s2_13 = dupeArg(s2_13, &s2_13_2, 0);

Term s2_13_1;
s2_13 = dupeArg(s2_13, &s2_13_1, 0);
Term seq_18 = makePair(LAM, 0, SUB, NUL);
Location r_seq_18 = portLoc(2, seq_18);
Term c_17 = newTerm(VAR, 0, portLoc(1, seq_18));
// allocate args at /home/jim/toccata/interpreter/intrp-rdr.toc: 308
Term glblVal175_43;
glblVal175 = dupeArg(glblVal175, &glblVal175_43, 0);

Term seq_23 = makePair(LAM, 0, SUB, NUL);
Location r_seq_23 = portLoc(2, seq_23);
Term after_slash_22 = newTerm(VAR, 0, portLoc(1, seq_23));
// allocate args at /home/jim/toccata/interpreter/intrp-rdr.toc: 311
Term seq_27 = makePair(LAM, 0, SUB, NUL);
Location r_seq_27 = portLoc(2, seq_27);
Term name_pair_26 = newTerm(VAR, 0, portLoc(1, seq_27));
// allocate args at /home/jim/toccata/interpreter/intrp-rdr.toc: 312

Term name_pair_26_1;
name_pair_26 = dupeArg(name_pair_26, &name_pair_26_1, 0);
Term glblVal175_42;
glblVal175 = dupeArg(glblVal175, &glblVal175_42, 0);

// call str= at /home/jim/toccata/interpreter/intrp-rdr.toc: 310
Term Rslt_20Args = makePair(APP, 0, glblStr1942, SUB);
Term Rslt_20 = newTerm(VAR, 0, portLoc(2, Rslt_20Args));
Rslt_20Args = makePair(APP, 0, c_17, Rslt_20Args);
#ifdef STRICT
pushRedex(Rslt_20Args, glblFn363);
#else
swap(termLoc(Rslt_20), makePair(LAZ, 0, Rslt_20Args, glblFn363));
#endif

// call vect-conj at /home/jim/toccata/interpreter/intrp-rdr.toc: 313
Term Rslt_29Args = makePair(APP, 0, fst_9, SUB);
Term Rslt_29 = newTerm(VAR, 0, portLoc(2, Rslt_29Args));
Rslt_29Args = makePair(APP, 0, glblVal175_42, Rslt_29Args);
#ifdef STRICT
pushRedex(Rslt_29Args, glblFn178);
#else
swap(termLoc(Rslt_29), makePair(LAZ, 0, Rslt_29Args, glblFn178));
#endif

// call elt0 at /home/jim/toccata/interpreter/intrp-rdr.toc: 313
Term Rslt_30Args = makePair(APP, 0, name_pair_26, SUB);
Term Rslt_30 = newTerm(VAR, 0, portLoc(2, Rslt_30Args));
#ifdef STRICT
pushRedex(Rslt_30Args, glblFn1742);
#else
swap(termLoc(Rslt_30), makePair(LAZ, 0, Rslt_30Args, glblFn1742));
#endif

// call vect-conj at /home/jim/toccata/interpreter/intrp-rdr.toc: 313
Term Rslt_31Args = makePair(APP, 0, Rslt_30, SUB);
Term Rslt_31 = newTerm(VAR, 0, portLoc(2, Rslt_31Args));
Rslt_31Args = makePair(APP, 0, Rslt_29, Rslt_31Args);
#ifdef STRICT
pushRedex(Rslt_31Args, glblFn178);
#else
swap(termLoc(Rslt_31), makePair(LAZ, 0, Rslt_31Args, glblFn178));
#endif

// call elt1 at /home/jim/toccata/interpreter/intrp-rdr.toc: 313
Term Rslt_32Args = makePair(APP, 0, name_pair_26_1, SUB);
Term Rslt_32 = newTerm(VAR, 0, portLoc(2, Rslt_32Args));
#ifdef STRICT
pushRedex(Rslt_32Args, glblFn1744);
#else
swap(termLoc(Rslt_32), makePair(LAZ, 0, Rslt_32Args, glblFn1744));
#endif

// call vect-conj at /home/jim/toccata/interpreter/intrp-rdr.toc: 313
Term Rslt_33Args = makePair(APP, 0, Rslt_32, SUB);
Term Rslt_33 = newTerm(VAR, 0, portLoc(2, Rslt_33Args));
Rslt_33Args = makePair(APP, 0, Rslt_31, Rslt_33Args);
#ifdef STRICT
pushRedex(Rslt_33Args, glblFn178);
#else
swap(termLoc(Rslt_33), makePair(LAZ, 0, Rslt_33Args, glblFn178));
#endif

// link args to body
swap(r_seq_27, Rslt_33);

Term Rslt_33V = get(termLoc(Rslt_33));
apps = take(portLoc(1, Rslt_33V));
lams = take(portLoc(2, Rslt_33V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_33), makePair(LAZ, 0, apps, lams));
// call read-symbol at /home/jim/toccata/interpreter/intrp-rdr.toc: 312
Term Rslt_34Args = makePair(APP, 0, after_slash_22, SUB);
Term Rslt_34 = newTerm(VAR, 0, portLoc(2, Rslt_34Args));
#ifdef STRICT
pushRedex(Rslt_34Args, glblFn1750);
#else
swap(termLoc(Rslt_34), makePair(LAZ, 0, Rslt_34Args, glblFn1750));
#endif

// call  at /home/jim/toccata/interpreter/intrp-rdr.toc: 312
Term Rslt_35Args = makePair(APP, 0, Rslt_34, SUB);
Term Rslt_35 = newTerm(VAR, 0, portLoc(2, Rslt_35Args));
#ifdef STRICT
pushRedex(Rslt_35Args, seq_27);
#else
swap(termLoc(Rslt_35), makePair(LAZ, 0, Rslt_35Args, seq_27));
#endif

// link args to body
swap(r_seq_23, Rslt_35);

Term Rslt_35V = get(termLoc(Rslt_35));
apps = take(portLoc(1, Rslt_35V));
lams = take(portLoc(2, Rslt_35V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_35), makePair(LAZ, 0, apps, lams));
// call take-char at /home/jim/toccata/interpreter/intrp-rdr.toc: 311
Term Rslt_36Args = makePair(APP, 0, s2_13, SUB);
Term Rslt_36 = newTerm(VAR, 0, portLoc(2, Rslt_36Args));
#ifdef STRICT
pushRedex(Rslt_36Args, glblFn1732);
#else
swap(termLoc(Rslt_36), makePair(LAZ, 0, Rslt_36Args, glblFn1732));
#endif

// call  at /home/jim/toccata/interpreter/intrp-rdr.toc: 311
Term Rslt_37Args = makePair(APP, 0, Rslt_36, SUB);
Term Rslt_37 = newTerm(VAR, 0, portLoc(2, Rslt_37Args));
#ifdef STRICT
pushRedex(Rslt_37Args, seq_23);
#else
swap(termLoc(Rslt_37), makePair(LAZ, 0, Rslt_37Args, seq_23));
#endif

// call vect-conj at /home/jim/toccata/interpreter/intrp-rdr.toc: 314
Term Rslt_38Args = makePair(APP, 0, glblStr402, SUB);
Term Rslt_38 = newTerm(VAR, 0, portLoc(2, Rslt_38Args));
Rslt_38Args = makePair(APP, 0, glblVal175_43, Rslt_38Args);
#ifdef STRICT
pushRedex(Rslt_38Args, glblFn178);
#else
swap(termLoc(Rslt_38), makePair(LAZ, 0, Rslt_38Args, glblFn178));
#endif

// call vect-conj at /home/jim/toccata/interpreter/intrp-rdr.toc: 314
Term Rslt_39Args = makePair(APP, 0, fst_9_1, SUB);
Term Rslt_39 = newTerm(VAR, 0, portLoc(2, Rslt_39Args));
Rslt_39Args = makePair(APP, 0, Rslt_38, Rslt_39Args);
#ifdef STRICT
pushRedex(Rslt_39Args, glblFn178);
#else
swap(termLoc(Rslt_39), makePair(LAZ, 0, Rslt_39Args, glblFn178));
#endif

// call vect-conj at /home/jim/toccata/interpreter/intrp-rdr.toc: 314
Term Rslt_40Args = makePair(APP, 0, s2_13_1, SUB);
Term Rslt_40 = newTerm(VAR, 0, portLoc(2, Rslt_40Args));
Rslt_40Args = makePair(APP, 0, Rslt_39, Rslt_40Args);
#ifdef STRICT
pushRedex(Rslt_40Args, glblFn178);
#else
swap(termLoc(Rslt_40), makePair(LAZ, 0, Rslt_40Args, glblFn178));
#endif

// call cond at /home/jim/toccata/interpreter/intrp-rdr.toc: 310
Term Rslt_41Args = makePair(APP, 0, Rslt_40, SUB);
Term Rslt_41 = newTerm(VAR, 0, portLoc(2, Rslt_41Args));
Rslt_41Args = makePair(APP, 0, Rslt_37, Rslt_41Args);
Rslt_41Args = makePair(APP, 0, Rslt_20, Rslt_41Args);
#ifdef STRICT
pushRedex(Rslt_41Args, newRef(glblProto34_2184));
#else
swap(termLoc(Rslt_41), makePair(LAZ, 0, Rslt_41Args, newRef(glblProto34_2184)));
#endif
// link args to body
swap(r_seq_18, Rslt_41);

Term Rslt_41V = get(termLoc(Rslt_41));
apps = take(portLoc(1, Rslt_41V));
lams = take(portLoc(2, Rslt_41V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_41), makePair(LAZ, 0, apps, lams));
// call peek-char at /home/jim/toccata/interpreter/intrp-rdr.toc: 308
Term Rslt_42Args = makePair(APP, 0, s2_13_2, SUB);
Term Rslt_42 = newTerm(VAR, 0, portLoc(2, Rslt_42Args));
#ifdef STRICT
pushRedex(Rslt_42Args, glblFn1730);
#else
swap(termLoc(Rslt_42), makePair(LAZ, 0, Rslt_42Args, glblFn1730));
#endif

// call  at /home/jim/toccata/interpreter/intrp-rdr.toc: 308
Term Rslt_43Args = makePair(APP, 0, Rslt_42, SUB);
Term Rslt_43 = newTerm(VAR, 0, portLoc(2, Rslt_43Args));
#ifdef STRICT
pushRedex(Rslt_43Args, seq_18);
#else
swap(termLoc(Rslt_43), makePair(LAZ, 0, Rslt_43Args, seq_18));
#endif

// link args to body
swap(r_seq_14, Rslt_43);

Term Rslt_43V = get(termLoc(Rslt_43));
apps = take(portLoc(1, Rslt_43V));
lams = take(portLoc(2, Rslt_43V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_43), makePair(LAZ, 0, apps, lams));
// call elt1 at /home/jim/toccata/interpreter/intrp-rdr.toc: 307
Term Rslt_44Args = makePair(APP, 0, first_pair_5, SUB);
Term Rslt_44 = newTerm(VAR, 0, portLoc(2, Rslt_44Args));
#ifdef STRICT
pushRedex(Rslt_44Args, glblFn1744);
#else
swap(termLoc(Rslt_44), makePair(LAZ, 0, Rslt_44Args, glblFn1744));
#endif

// call  at /home/jim/toccata/interpreter/intrp-rdr.toc: 307
Term Rslt_45Args = makePair(APP, 0, Rslt_44, SUB);
Term Rslt_45 = newTerm(VAR, 0, portLoc(2, Rslt_45Args));
#ifdef STRICT
pushRedex(Rslt_45Args, seq_14);
#else
swap(termLoc(Rslt_45), makePair(LAZ, 0, Rslt_45Args, seq_14));
#endif

// link args to body
swap(r_seq_10, Rslt_45);

Term Rslt_45V = get(termLoc(Rslt_45));
apps = take(portLoc(1, Rslt_45V));
lams = take(portLoc(2, Rslt_45V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_45), makePair(LAZ, 0, apps, lams));
// call elt0 at /home/jim/toccata/interpreter/intrp-rdr.toc: 306
Term Rslt_46Args = makePair(APP, 0, first_pair_5_1, SUB);
Term Rslt_46 = newTerm(VAR, 0, portLoc(2, Rslt_46Args));
#ifdef STRICT
pushRedex(Rslt_46Args, glblFn1742);
#else
swap(termLoc(Rslt_46), makePair(LAZ, 0, Rslt_46Args, glblFn1742));
#endif

// call  at /home/jim/toccata/interpreter/intrp-rdr.toc: 306
Term Rslt_47Args = makePair(APP, 0, Rslt_46, SUB);
Term Rslt_47 = newTerm(VAR, 0, portLoc(2, Rslt_47Args));
#ifdef STRICT
pushRedex(Rslt_47Args, seq_10);
#else
swap(termLoc(Rslt_47), makePair(LAZ, 0, Rslt_47Args, seq_10));
#endif

// link args to body
swap(r_seq_6, Rslt_47);

Term Rslt_47V = get(termLoc(Rslt_47));
apps = take(portLoc(1, Rslt_47V));
lams = take(portLoc(2, Rslt_47V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_47), makePair(LAZ, 0, apps, lams));
// call read-symbol at /home/jim/toccata/interpreter/intrp-rdr.toc: 305
Term Rslt_48Args = makePair(APP, 0, state_1, SUB);
Term Rslt_48 = newTerm(VAR, 0, portLoc(2, Rslt_48Args));
#ifdef STRICT
pushRedex(Rslt_48Args, glblFn1750);
#else
swap(termLoc(Rslt_48), makePair(LAZ, 0, Rslt_48Args, glblFn1750));
#endif

// call  at /home/jim/toccata/interpreter/intrp-rdr.toc: 305
Term Rslt_49Args = makePair(APP, 0, Rslt_48, SUB);
Term Rslt_49 = newTerm(VAR, 0, portLoc(2, Rslt_49Args));
#ifdef STRICT
pushRedex(Rslt_49Args, seq_6);
#else
swap(termLoc(Rslt_49), makePair(LAZ, 0, Rslt_49Args, seq_6));
#endif

// link args to body
swap(r_seq_2, Rslt_49);

Term Rslt_49V = get(termLoc(Rslt_49));
apps = take(portLoc(1, Rslt_49V));
lams = take(portLoc(2, Rslt_49V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_49), makePair(LAZ, 0, apps, lams));
pushRedex(args, seq_2);
return;
}
Term glblFn1766 = newRef(glblCFn1765);
void glblProto34_2190(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(324), args);
  args = makePair(APP, 0, glblStr1912, args);
  interact(args, glblProto34);
}
}
Term glblFn1768;
void glblCFn1767(Term ref, Term args) {
Term apps, lams, V;
Location vLoc;
Term seq_2 = makePair(LAM, 0, SUB, NUL);
Location r_seq_2 = portLoc(2, seq_2);
Term state_1 = newTerm(VAR, 0, portLoc(1, seq_2));
// allocate args at /home/jim/toccata/interpreter/intrp-rdr.toc: 320
Term seq_6 = makePair(LAM, 0, SUB, NUL);
Location r_seq_6 = portLoc(2, seq_6);
Term s_5 = newTerm(VAR, 0, portLoc(1, seq_6));
// allocate args at /home/jim/toccata/interpreter/intrp-rdr.toc: 321

Term s_5_2;
s_5 = dupeArg(s_5, &s_5_2, 0);

Term s_5_1;
s_5 = dupeArg(s_5, &s_5_1, 0);
Term seq_10 = makePair(LAM, 0, SUB, NUL);
Location r_seq_10 = portLoc(2, seq_10);
Term c_9 = newTerm(VAR, 0, portLoc(1, seq_10));
// allocate args at /home/jim/toccata/interpreter/intrp-rdr.toc: 322
Term seq_44 = makePair(LAM, 0, SUB, NUL);
Location r_seq_44 = portLoc(2, seq_44);
Term nm_43 = newTerm(VAR, 0, portLoc(1, seq_44));
// allocate args at /home/jim/toccata/interpreter/intrp-rdr.toc: 330

Term nm_43_1;
nm_43 = dupeArg(nm_43, &nm_43_1, 0);
Term glblVal175_45;
glblVal175 = dupeArg(glblVal175, &glblVal175_45, 0);

Term seq_15 = makePair(LAM, 0, SUB, NUL);
Location r_seq_15 = portLoc(2, seq_15);
Term s2_14 = newTerm(VAR, 0, portLoc(1, seq_15));
// allocate args at /home/jim/toccata/interpreter/intrp-rdr.toc: 325
Term seq_19 = makePair(LAM, 0, SUB, NUL);
Location r_seq_19 = portLoc(2, seq_19);
Term inner_18 = newTerm(VAR, 0, portLoc(1, seq_19));
// allocate args at /home/jim/toccata/interpreter/intrp-rdr.toc: 326

Term inner_18_1;
inner_18 = dupeArg(inner_18, &inner_18_1, 0);
Term seq_23 = makePair(LAM, 0, SUB, NUL);
Location r_seq_23 = portLoc(2, seq_23);
Term s3_22 = newTerm(VAR, 0, portLoc(1, seq_23));
// allocate args at /home/jim/toccata/interpreter/intrp-rdr.toc: 327
Term seq_27 = makePair(LAM, 0, SUB, NUL);
Location r_seq_27 = portLoc(2, seq_27);
Term s4_26 = newTerm(VAR, 0, portLoc(1, seq_27));
// allocate args at /home/jim/toccata/interpreter/intrp-rdr.toc: 328
Term glblVal175_44;
glblVal175 = dupeArg(glblVal175, &glblVal175_44, 0);

// call str= at /home/jim/toccata/interpreter/intrp-rdr.toc: 324
Term Rslt_12Args = makePair(APP, 0, glblStr747, SUB);
Term Rslt_12 = newTerm(VAR, 0, portLoc(2, Rslt_12Args));
Rslt_12Args = makePair(APP, 0, c_9, Rslt_12Args);
#ifdef STRICT
pushRedex(Rslt_12Args, glblFn363);
#else
swap(termLoc(Rslt_12), makePair(LAZ, 0, Rslt_12Args, glblFn363));
#endif

// call elt0 at /home/jim/toccata/interpreter/intrp-rdr.toc: 329
Term Rslt_29Args = makePair(APP, 0, inner_18, SUB);
Term Rslt_29 = newTerm(VAR, 0, portLoc(2, Rslt_29Args));
#ifdef STRICT
pushRedex(Rslt_29Args, glblFn1742);
#else
swap(termLoc(Rslt_29), makePair(LAZ, 0, Rslt_29Args, glblFn1742));
#endif

// call vect-conj at /home/jim/toccata/interpreter/intrp-rdr.toc: 329
Term Rslt_30Args = makePair(APP, 0, Rslt_29, SUB);
Term Rslt_30 = newTerm(VAR, 0, portLoc(2, Rslt_30Args));
Rslt_30Args = makePair(APP, 0, glblVal175_44, Rslt_30Args);
#ifdef STRICT
pushRedex(Rslt_30Args, glblFn178);
#else
swap(termLoc(Rslt_30), makePair(LAZ, 0, Rslt_30Args, glblFn178));
#endif

// call vect-conj at /home/jim/toccata/interpreter/intrp-rdr.toc: 329
Term Rslt_31Args = makePair(APP, 0, s4_26, SUB);
Term Rslt_31 = newTerm(VAR, 0, portLoc(2, Rslt_31Args));
Rslt_31Args = makePair(APP, 0, Rslt_30, Rslt_31Args);
#ifdef STRICT
pushRedex(Rslt_31Args, glblFn178);
#else
swap(termLoc(Rslt_31), makePair(LAZ, 0, Rslt_31Args, glblFn178));
#endif

// link args to body
swap(r_seq_27, Rslt_31);

Term Rslt_31V = get(termLoc(Rslt_31));
apps = take(portLoc(1, Rslt_31V));
lams = take(portLoc(2, Rslt_31V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_31), makePair(LAZ, 0, apps, lams));
// call take-char at /home/jim/toccata/interpreter/intrp-rdr.toc: 328
Term Rslt_32Args = makePair(APP, 0, s3_22, SUB);
Term Rslt_32 = newTerm(VAR, 0, portLoc(2, Rslt_32Args));
#ifdef STRICT
pushRedex(Rslt_32Args, glblFn1732);
#else
swap(termLoc(Rslt_32), makePair(LAZ, 0, Rslt_32Args, glblFn1732));
#endif

// call  at /home/jim/toccata/interpreter/intrp-rdr.toc: 328
Term Rslt_33Args = makePair(APP, 0, Rslt_32, SUB);
Term Rslt_33 = newTerm(VAR, 0, portLoc(2, Rslt_33Args));
#ifdef STRICT
pushRedex(Rslt_33Args, seq_27);
#else
swap(termLoc(Rslt_33), makePair(LAZ, 0, Rslt_33Args, seq_27));
#endif

// link args to body
swap(r_seq_23, Rslt_33);

Term Rslt_33V = get(termLoc(Rslt_33));
apps = take(portLoc(1, Rslt_33V));
lams = take(portLoc(2, Rslt_33V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_33), makePair(LAZ, 0, apps, lams));
// call elt1 at /home/jim/toccata/interpreter/intrp-rdr.toc: 327
Term Rslt_34Args = makePair(APP, 0, inner_18_1, SUB);
Term Rslt_34 = newTerm(VAR, 0, portLoc(2, Rslt_34Args));
#ifdef STRICT
pushRedex(Rslt_34Args, glblFn1744);
#else
swap(termLoc(Rslt_34), makePair(LAZ, 0, Rslt_34Args, glblFn1744));
#endif

// call skip-whitespace at /home/jim/toccata/interpreter/intrp-rdr.toc: 327
Term Rslt_35Args = makePair(APP, 0, Rslt_34, SUB);
Term Rslt_35 = newTerm(VAR, 0, portLoc(2, Rslt_35Args));
#ifdef STRICT
pushRedex(Rslt_35Args, glblFn1736);
#else
swap(termLoc(Rslt_35), makePair(LAZ, 0, Rslt_35Args, glblFn1736));
#endif

// call  at /home/jim/toccata/interpreter/intrp-rdr.toc: 327
Term Rslt_36Args = makePair(APP, 0, Rslt_35, SUB);
Term Rslt_36 = newTerm(VAR, 0, portLoc(2, Rslt_36Args));
#ifdef STRICT
pushRedex(Rslt_36Args, seq_23);
#else
swap(termLoc(Rslt_36), makePair(LAZ, 0, Rslt_36Args, seq_23));
#endif

// link args to body
swap(r_seq_19, Rslt_36);

Term Rslt_36V = get(termLoc(Rslt_36));
apps = take(portLoc(1, Rslt_36V));
lams = take(portLoc(2, Rslt_36V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_36), makePair(LAZ, 0, apps, lams));
// call read-symbol at /home/jim/toccata/interpreter/intrp-rdr.toc: 326
Term Rslt_37Args = makePair(APP, 0, s2_14, SUB);
Term Rslt_37 = newTerm(VAR, 0, portLoc(2, Rslt_37Args));
#ifdef STRICT
pushRedex(Rslt_37Args, glblFn1750);
#else
swap(termLoc(Rslt_37), makePair(LAZ, 0, Rslt_37Args, glblFn1750));
#endif

// call  at /home/jim/toccata/interpreter/intrp-rdr.toc: 326
Term Rslt_38Args = makePair(APP, 0, Rslt_37, SUB);
Term Rslt_38 = newTerm(VAR, 0, portLoc(2, Rslt_38Args));
#ifdef STRICT
pushRedex(Rslt_38Args, seq_19);
#else
swap(termLoc(Rslt_38), makePair(LAZ, 0, Rslt_38Args, seq_19));
#endif

// link args to body
swap(r_seq_15, Rslt_38);

Term Rslt_38V = get(termLoc(Rslt_38));
apps = take(portLoc(1, Rslt_38V));
lams = take(portLoc(2, Rslt_38V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_38), makePair(LAZ, 0, apps, lams));
// call take-char at /home/jim/toccata/interpreter/intrp-rdr.toc: 325
Term Rslt_39Args = makePair(APP, 0, s_5, SUB);
Term Rslt_39 = newTerm(VAR, 0, portLoc(2, Rslt_39Args));
#ifdef STRICT
pushRedex(Rslt_39Args, glblFn1732);
#else
swap(termLoc(Rslt_39), makePair(LAZ, 0, Rslt_39Args, glblFn1732));
#endif

// call skip-whitespace at /home/jim/toccata/interpreter/intrp-rdr.toc: 325
Term Rslt_40Args = makePair(APP, 0, Rslt_39, SUB);
Term Rslt_40 = newTerm(VAR, 0, portLoc(2, Rslt_40Args));
#ifdef STRICT
pushRedex(Rslt_40Args, glblFn1736);
#else
swap(termLoc(Rslt_40), makePair(LAZ, 0, Rslt_40Args, glblFn1736));
#endif

// call  at /home/jim/toccata/interpreter/intrp-rdr.toc: 325
Term Rslt_41Args = makePair(APP, 0, Rslt_40, SUB);
Term Rslt_41 = newTerm(VAR, 0, portLoc(2, Rslt_41Args));
#ifdef STRICT
pushRedex(Rslt_41Args, seq_15);
#else
swap(termLoc(Rslt_41), makePair(LAZ, 0, Rslt_41Args, seq_15));
#endif

// call elt0 at /home/jim/toccata/interpreter/intrp-rdr.toc: 331
Term Rslt_46Args = makePair(APP, 0, nm_43, SUB);
Term Rslt_46 = newTerm(VAR, 0, portLoc(2, Rslt_46Args));
#ifdef STRICT
pushRedex(Rslt_46Args, glblFn1742);
#else
swap(termLoc(Rslt_46), makePair(LAZ, 0, Rslt_46Args, glblFn1742));
#endif

// call vect-conj at /home/jim/toccata/interpreter/intrp-rdr.toc: 331
Term Rslt_47Args = makePair(APP, 0, Rslt_46, SUB);
Term Rslt_47 = newTerm(VAR, 0, portLoc(2, Rslt_47Args));
Rslt_47Args = makePair(APP, 0, glblVal175_45, Rslt_47Args);
#ifdef STRICT
pushRedex(Rslt_47Args, glblFn178);
#else
swap(termLoc(Rslt_47), makePair(LAZ, 0, Rslt_47Args, glblFn178));
#endif

// call elt1 at /home/jim/toccata/interpreter/intrp-rdr.toc: 331
Term Rslt_48Args = makePair(APP, 0, nm_43_1, SUB);
Term Rslt_48 = newTerm(VAR, 0, portLoc(2, Rslt_48Args));
#ifdef STRICT
pushRedex(Rslt_48Args, glblFn1744);
#else
swap(termLoc(Rslt_48), makePair(LAZ, 0, Rslt_48Args, glblFn1744));
#endif

// call vect-conj at /home/jim/toccata/interpreter/intrp-rdr.toc: 331
Term Rslt_49Args = makePair(APP, 0, Rslt_48, SUB);
Term Rslt_49 = newTerm(VAR, 0, portLoc(2, Rslt_49Args));
Rslt_49Args = makePair(APP, 0, Rslt_47, Rslt_49Args);
#ifdef STRICT
pushRedex(Rslt_49Args, glblFn178);
#else
swap(termLoc(Rslt_49), makePair(LAZ, 0, Rslt_49Args, glblFn178));
#endif

// link args to body
swap(r_seq_44, Rslt_49);

Term Rslt_49V = get(termLoc(Rslt_49));
apps = take(portLoc(1, Rslt_49V));
lams = take(portLoc(2, Rslt_49V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_49), makePair(LAZ, 0, apps, lams));
// call read-symbol at /home/jim/toccata/interpreter/intrp-rdr.toc: 330
Term Rslt_50Args = makePair(APP, 0, s_5_1, SUB);
Term Rslt_50 = newTerm(VAR, 0, portLoc(2, Rslt_50Args));
#ifdef STRICT
pushRedex(Rslt_50Args, glblFn1750);
#else
swap(termLoc(Rslt_50), makePair(LAZ, 0, Rslt_50Args, glblFn1750));
#endif

// call  at /home/jim/toccata/interpreter/intrp-rdr.toc: 330
Term Rslt_51Args = makePair(APP, 0, Rslt_50, SUB);
Term Rslt_51 = newTerm(VAR, 0, portLoc(2, Rslt_51Args));
#ifdef STRICT
pushRedex(Rslt_51Args, seq_44);
#else
swap(termLoc(Rslt_51), makePair(LAZ, 0, Rslt_51Args, seq_44));
#endif

// call cond at /home/jim/toccata/interpreter/intrp-rdr.toc: 324
Term Rslt_52Args = makePair(APP, 0, Rslt_51, SUB);
Term Rslt_52 = newTerm(VAR, 0, portLoc(2, Rslt_52Args));
Rslt_52Args = makePair(APP, 0, Rslt_41, Rslt_52Args);
Rslt_52Args = makePair(APP, 0, Rslt_12, Rslt_52Args);
#ifdef STRICT
pushRedex(Rslt_52Args, newRef(glblProto34_2190));
#else
swap(termLoc(Rslt_52), makePair(LAZ, 0, Rslt_52Args, newRef(glblProto34_2190)));
#endif
// link args to body
swap(r_seq_10, Rslt_52);

Term Rslt_52V = get(termLoc(Rslt_52));
apps = take(portLoc(1, Rslt_52V));
lams = take(portLoc(2, Rslt_52V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_52), makePair(LAZ, 0, apps, lams));
// call peek-char at /home/jim/toccata/interpreter/intrp-rdr.toc: 322
Term Rslt_53Args = makePair(APP, 0, s_5_2, SUB);
Term Rslt_53 = newTerm(VAR, 0, portLoc(2, Rslt_53Args));
#ifdef STRICT
pushRedex(Rslt_53Args, glblFn1730);
#else
swap(termLoc(Rslt_53), makePair(LAZ, 0, Rslt_53Args, glblFn1730));
#endif

// call  at /home/jim/toccata/interpreter/intrp-rdr.toc: 322
Term Rslt_54Args = makePair(APP, 0, Rslt_53, SUB);
Term Rslt_54 = newTerm(VAR, 0, portLoc(2, Rslt_54Args));
#ifdef STRICT
pushRedex(Rslt_54Args, seq_10);
#else
swap(termLoc(Rslt_54), makePair(LAZ, 0, Rslt_54Args, seq_10));
#endif

// link args to body
swap(r_seq_6, Rslt_54);

Term Rslt_54V = get(termLoc(Rslt_54));
apps = take(portLoc(1, Rslt_54V));
lams = take(portLoc(2, Rslt_54V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_54), makePair(LAZ, 0, apps, lams));
// call skip-whitespace at /home/jim/toccata/interpreter/intrp-rdr.toc: 321
Term Rslt_55Args = makePair(APP, 0, state_1, SUB);
Term Rslt_55 = newTerm(VAR, 0, portLoc(2, Rslt_55Args));
#ifdef STRICT
pushRedex(Rslt_55Args, glblFn1736);
#else
swap(termLoc(Rslt_55), makePair(LAZ, 0, Rslt_55Args, glblFn1736));
#endif

// call  at /home/jim/toccata/interpreter/intrp-rdr.toc: 321
Term Rslt_56Args = makePair(APP, 0, Rslt_55, SUB);
Term Rslt_56 = newTerm(VAR, 0, portLoc(2, Rslt_56Args));
#ifdef STRICT
pushRedex(Rslt_56Args, seq_6);
#else
swap(termLoc(Rslt_56), makePair(LAZ, 0, Rslt_56Args, seq_6));
#endif

// link args to body
swap(r_seq_2, Rslt_56);

Term Rslt_56V = get(termLoc(Rslt_56));
apps = take(portLoc(1, Rslt_56V));
lams = take(portLoc(2, Rslt_56V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_56), makePair(LAZ, 0, apps, lams));
pushRedex(args, seq_2);
return;
}
Term glblFn1768 = newRef(glblCFn1767);
void glblProto380_2193(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(339), args);
  args = makePair(APP, 0, glblStr1912, args);
  interact(args, glblProto380);
}
}
void glblProto64_2195(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(341), args);
  args = makePair(APP, 0, glblStr1912, args);
  interact(args, glblProto64);
}
}
void glblProto380_2197(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(341), args);
  args = makePair(APP, 0, glblStr1912, args);
  interact(args, glblProto380);
}
}
void glblProto34_2200(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(337), args);
  args = makePair(APP, 0, glblStr1912, args);
  interact(args, glblProto34);
}
}
void glblProto34_2202(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(337), args);
  args = makePair(APP, 0, glblStr1912, args);
  interact(args, glblProto34);
}
}
Term glblFn1770;
void glblCFn1769(Term ref, Term args) {
Term apps, lams, V;
Location vLoc;
Term seq_2 = makePair(LAM, 0, SUB, NUL);
Location r_seq_2 = portLoc(2, seq_2);
Term s_1 = newTerm(VAR, 0, portLoc(1, seq_2));
// allocate args at /home/jim/toccata/interpreter/intrp-rdr.toc: 335

Term s_1_3;
s_1 = dupeArg(s_1, &s_1_3, 295);

Term s_1_2;
s_1 = dupeArg(s_1, &s_1_2, 296);

Term s_1_1;
s_1 = dupeArg(s_1, &s_1_1, 297);
Term glblVal25_24;
glblVal25 = dupeArg(glblVal25, &glblVal25_24, 0);

Term glblRslt16_12;
glblRslt16 = dupeArg(glblRslt16, &glblRslt16_12, 0);

// call str= at /home/jim/toccata/interpreter/intrp-rdr.toc: 337
Term Rslt_4Args = makePair(APP, 0, glblStr402, SUB);
Term Rslt_4 = newTerm(VAR, 0, portLoc(2, Rslt_4Args));
Rslt_4Args = makePair(APP, 0, s_1, Rslt_4Args);
#ifdef STRICT
pushRedex(Rslt_4Args, glblFn363);
#else
swap(termLoc(Rslt_4), makePair(LAZ, 0, Rslt_4Args, glblFn363));
#endif

// call subs at /home/jim/toccata/interpreter/intrp-rdr.toc: 339
Term Rslt_5Args = makePair(APP, 0, newI60(1), SUB);
Term Rslt_5 = newTerm(VAR, 0, portLoc(2, Rslt_5Args));
Rslt_5Args = makePair(APP, 0, newI60(0), Rslt_5Args);
Rslt_5Args = makePair(APP, 0, s_1_1, Rslt_5Args);
#ifdef STRICT
pushRedex(Rslt_5Args, newRef(glblProto380_2193));
#else
swap(termLoc(Rslt_5), makePair(LAZ, 0, Rslt_5Args, newRef(glblProto380_2193)));
#endif
// call str= at /home/jim/toccata/interpreter/intrp-rdr.toc: 339
Term Rslt_6Args = makePair(APP, 0, glblStr1933, SUB);
Term Rslt_6 = newTerm(VAR, 0, portLoc(2, Rslt_6Args));
Rslt_6Args = makePair(APP, 0, Rslt_5, Rslt_6Args);
#ifdef STRICT
pushRedex(Rslt_6Args, glblFn363);
#else
swap(termLoc(Rslt_6), makePair(LAZ, 0, Rslt_6Args, glblFn363));
#endif

// call Some at /home/jim/toccata/interpreter/intrp-rdr.toc: 340
Term Rslt_7Args = makePair(APP, 0, newI60(1), SUB);
Term Rslt_7 = newTerm(VAR, 0, portLoc(2, Rslt_7Args));
#ifdef STRICT
pushRedex(Rslt_7Args, glblVal25_24);
#else
swap(termLoc(Rslt_7), makePair(LAZ, 0, Rslt_7Args, glblVal25_24));
#endif

// call count at /home/jim/toccata/interpreter/intrp-rdr.toc: 341
Term Rslt_8Args = makePair(APP, 0, s_1_3, SUB);
Term Rslt_8 = newTerm(VAR, 0, portLoc(2, Rslt_8Args));
#ifdef STRICT
pushRedex(Rslt_8Args, newRef(glblProto64_2195));
#else
swap(termLoc(Rslt_8), makePair(LAZ, 0, Rslt_8Args, newRef(glblProto64_2195)));
#endif
// call subs at /home/jim/toccata/interpreter/intrp-rdr.toc: 341
Term Rslt_9Args = makePair(APP, 0, Rslt_8, SUB);
Term Rslt_9 = newTerm(VAR, 0, portLoc(2, Rslt_9Args));
Rslt_9Args = makePair(APP, 0, newI60(1), Rslt_9Args);
Rslt_9Args = makePair(APP, 0, s_1_2, Rslt_9Args);
#ifdef STRICT
pushRedex(Rslt_9Args, newRef(glblProto380_2197));
#else
swap(termLoc(Rslt_9), makePair(LAZ, 0, Rslt_9Args, newRef(glblProto380_2197)));
#endif
// call contains-dot at /home/jim/toccata/interpreter/intrp-rdr.toc: 335
Term Rslt_10Args = makePair(APP, 0, Rslt_9, SUB);
Term Rslt_10 = newTerm(VAR, 0, portLoc(2, Rslt_10Args));
#ifdef STRICT
pushRedex(Rslt_10Args, glblFn1770);
#else
swap(termLoc(Rslt_10), makePair(LAZ, 0, Rslt_10Args, glblFn1770));
#endif

// call cond at /home/jim/toccata/interpreter/intrp-rdr.toc: 337
Term Rslt_11Args = makePair(APP, 0, Rslt_10, SUB);
Term Rslt_11 = newTerm(VAR, 0, portLoc(2, Rslt_11Args));
Rslt_11Args = makePair(APP, 0, Rslt_7, Rslt_11Args);
Rslt_11Args = makePair(APP, 0, Rslt_6, Rslt_11Args);
#ifdef STRICT
pushRedex(Rslt_11Args, newRef(glblProto34_2200));
#else
swap(termLoc(Rslt_11), makePair(LAZ, 0, Rslt_11Args, newRef(glblProto34_2200)));
#endif
// call cond at /home/jim/toccata/interpreter/intrp-rdr.toc: 337
Term Rslt_12Args = makePair(APP, 0, Rslt_11, SUB);
Term Rslt_12 = newTerm(VAR, 0, portLoc(2, Rslt_12Args));
Rslt_12Args = makePair(APP, 0, glblRslt16_12, Rslt_12Args);
Rslt_12Args = makePair(APP, 0, Rslt_4, Rslt_12Args);
#ifdef STRICT
pushRedex(Rslt_12Args, newRef(glblProto34_2202));
#else
swap(termLoc(Rslt_12), makePair(LAZ, 0, Rslt_12Args, newRef(glblProto34_2202)));
#endif
// link args to body
swap(r_seq_2, Rslt_12);

Term Rslt_12V = get(termLoc(Rslt_12));
apps = take(portLoc(1, Rslt_12V));
lams = take(portLoc(2, Rslt_12V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_12), makePair(LAZ, 0, apps, lams));
pushRedex(args, seq_2);
return;
}
Term glblFn1770 = newRef(glblCFn1769);
void glblProto64_2205(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(353), args);
  args = makePair(APP, 0, glblStr1912, args);
  interact(args, glblProto64);
}
}
void glblProto380_2207(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(353), args);
  args = makePair(APP, 0, glblStr1912, args);
  interact(args, glblProto380);
}
}
void glblProto380_2209(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(352), args);
  args = makePair(APP, 0, glblStr1912, args);
  interact(args, glblProto380);
}
}
void glblProto34_2211(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(350), args);
  args = makePair(APP, 0, glblStr1912, args);
  interact(args, glblProto34);
}
}
Term glblFn1772;
void glblCFn1771(Term ref, Term args) {
Term apps, lams, V;
Location vLoc;
Term seq_3 = makePair(LAM, 0, SUB, NUL);
Location r_seq_3 = portLoc(2, seq_3);
Term acc_2 = newTerm(VAR, 0, portLoc(1, seq_3));
seq_3 = makePair(LAM, 0, SUB, seq_3);
Term s_1 = newTerm(VAR, 0, portLoc(1, seq_3));
// allocate args at /home/jim/toccata/interpreter/intrp-rdr.toc: 348

Term s_1_3;
s_1 = dupeArg(s_1, &s_1_3, 298);

Term s_1_2;
s_1 = dupeArg(s_1, &s_1_2, 299);

Term s_1_1;
s_1 = dupeArg(s_1, &s_1_1, 300);

Term acc_2_1;
acc_2 = dupeArg(acc_2, &acc_2_1, 301);
Term seq_8 = makePair(LAM, 0, SUB, NUL);
Location r_seq_8 = portLoc(2, seq_8);
Term d_7 = newTerm(VAR, 0, portLoc(1, seq_8));
// allocate args at /home/jim/toccata/interpreter/intrp-rdr.toc: 352
// call str= at /home/jim/toccata/interpreter/intrp-rdr.toc: 350
Term Rslt_5Args = makePair(APP, 0, glblStr402, SUB);
Term Rslt_5 = newTerm(VAR, 0, portLoc(2, Rslt_5Args));
Rslt_5Args = makePair(APP, 0, s_1, Rslt_5Args);
#ifdef STRICT
pushRedex(Rslt_5Args, glblFn363);
#else
swap(termLoc(Rslt_5), makePair(LAZ, 0, Rslt_5Args, glblFn363));
#endif

// call count at /home/jim/toccata/interpreter/intrp-rdr.toc: 353
Term Rslt_10Args = makePair(APP, 0, s_1_2, SUB);
Term Rslt_10 = newTerm(VAR, 0, portLoc(2, Rslt_10Args));
#ifdef STRICT
pushRedex(Rslt_10Args, newRef(glblProto64_2205));
#else
swap(termLoc(Rslt_10), makePair(LAZ, 0, Rslt_10Args, newRef(glblProto64_2205)));
#endif
// call subs at /home/jim/toccata/interpreter/intrp-rdr.toc: 353
Term Rslt_11Args = makePair(APP, 0, Rslt_10, SUB);
Term Rslt_11 = newTerm(VAR, 0, portLoc(2, Rslt_11Args));
Rslt_11Args = makePair(APP, 0, newI60(1), Rslt_11Args);
Rslt_11Args = makePair(APP, 0, s_1_1, Rslt_11Args);
#ifdef STRICT
pushRedex(Rslt_11Args, newRef(glblProto380_2207));
#else
swap(termLoc(Rslt_11), makePair(LAZ, 0, Rslt_11Args, newRef(glblProto380_2207)));
#endif
Term Rslt_12 = makeOp(OP_MUL, newI60(10), acc_2_1);
Term Rslt_13 = makeOp(OP_ADD, Rslt_12, d_7);
// call text-to-int-acc at /home/jim/toccata/interpreter/intrp-rdr.toc: 348
Term Rslt_14Args = makePair(APP, 0, Rslt_13, SUB);
Term Rslt_14 = newTerm(VAR, 0, portLoc(2, Rslt_14Args));
Rslt_14Args = makePair(APP, 0, Rslt_11, Rslt_14Args);
#ifdef STRICT
pushRedex(Rslt_14Args, glblFn1772);
#else
swap(termLoc(Rslt_14), makePair(LAZ, 0, Rslt_14Args, glblFn1772));
#endif

// link args to body
swap(r_seq_8, Rslt_14);

Term Rslt_14V = get(termLoc(Rslt_14));
apps = take(portLoc(1, Rslt_14V));
lams = take(portLoc(2, Rslt_14V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_14), makePair(LAZ, 0, apps, lams));
// call subs at /home/jim/toccata/interpreter/intrp-rdr.toc: 352
Term Rslt_15Args = makePair(APP, 0, newI60(1), SUB);
Term Rslt_15 = newTerm(VAR, 0, portLoc(2, Rslt_15Args));
Rslt_15Args = makePair(APP, 0, newI60(0), Rslt_15Args);
Rslt_15Args = makePair(APP, 0, s_1_3, Rslt_15Args);
#ifdef STRICT
pushRedex(Rslt_15Args, newRef(glblProto380_2209));
#else
swap(termLoc(Rslt_15), makePair(LAZ, 0, Rslt_15Args, newRef(glblProto380_2209)));
#endif
// call char-code at /home/jim/toccata/interpreter/intrp-rdr.toc: 352
Term Rslt_16Args = makePair(APP, 0, Rslt_15, SUB);
Term Rslt_16 = newTerm(VAR, 0, portLoc(2, Rslt_16Args));
#ifdef STRICT
pushRedex(Rslt_16Args, glblFn378);
#else
swap(termLoc(Rslt_16), makePair(LAZ, 0, Rslt_16Args, glblFn378));
#endif

Term Rslt_17 = makeOp(OP_SUB, Rslt_16, newI60(48));
// call  at /home/jim/toccata/interpreter/intrp-rdr.toc: 352
Term Rslt_18Args = makePair(APP, 0, Rslt_17, SUB);
Term Rslt_18 = newTerm(VAR, 0, portLoc(2, Rslt_18Args));
#ifdef STRICT
pushRedex(Rslt_18Args, seq_8);
#else
swap(termLoc(Rslt_18), makePair(LAZ, 0, Rslt_18Args, seq_8));
#endif

// call cond at /home/jim/toccata/interpreter/intrp-rdr.toc: 350
Term Rslt_19Args = makePair(APP, 0, Rslt_18, SUB);
Term Rslt_19 = newTerm(VAR, 0, portLoc(2, Rslt_19Args));
Rslt_19Args = makePair(APP, 0, acc_2, Rslt_19Args);
Rslt_19Args = makePair(APP, 0, Rslt_5, Rslt_19Args);
#ifdef STRICT
pushRedex(Rslt_19Args, newRef(glblProto34_2211));
#else
swap(termLoc(Rslt_19), makePair(LAZ, 0, Rslt_19Args, newRef(glblProto34_2211)));
#endif
// link args to body
swap(r_seq_3, Rslt_19);

Term Rslt_19V = get(termLoc(Rslt_19));
apps = take(portLoc(1, Rslt_19V));
lams = take(portLoc(2, Rslt_19V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_19), makePair(LAZ, 0, apps, lams));
pushRedex(args, seq_3);
return;
}
Term glblFn1772 = newRef(glblCFn1771);
Term glblFn1774;
void glblCFn1773(Term ref, Term args) {
Term apps, lams, V;
Location vLoc;
Term seq_2 = makePair(LAM, 0, SUB, NUL);
Location r_seq_2 = portLoc(2, seq_2);
Term s_1 = newTerm(VAR, 0, portLoc(1, seq_2));
// allocate args at /home/jim/toccata/interpreter/intrp-rdr.toc: 356
// call text-to-int-acc at /home/jim/toccata/interpreter/intrp-rdr.toc: 357
Term Rslt_4Args = makePair(APP, 0, newI60(0), SUB);
Term Rslt_4 = newTerm(VAR, 0, portLoc(2, Rslt_4Args));
Rslt_4Args = makePair(APP, 0, s_1, Rslt_4Args);
#ifdef STRICT
pushRedex(Rslt_4Args, glblFn1772);
#else
swap(termLoc(Rslt_4), makePair(LAZ, 0, Rslt_4Args, glblFn1772));
#endif

// link args to body
swap(r_seq_2, Rslt_4);

Term Rslt_4V = get(termLoc(Rslt_4));
apps = take(portLoc(1, Rslt_4V));
lams = take(portLoc(2, Rslt_4V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_4), makePair(LAZ, 0, apps, lams));
pushRedex(args, seq_2);
return;
}
Term glblFn1774 = newRef(glblCFn1773);
void glblProto64_2217(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(363), args);
  args = makePair(APP, 0, glblStr1912, args);
  interact(args, glblProto64);
}
}
void glblProto20_2219(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(363), args);
  args = makePair(APP, 0, glblStr1912, args);
  interact(args, glblProto20);
}
}
void glblProto78_2221(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(366), args);
  args = makePair(APP, 0, glblStr1912, args);
  interact(args, glblProto78);
}
}
struct {TYPE_SIZE type;
        REFS_SIZE refs;
        int64_t *hash;
        Value *parent;
        int64_t len;
        char buffer[10];
} str31 = {StringBufferType, REFS_STATIC, 0, 0, 9, "vect-conj"};
Term glblStr2215 = newTerm_(VAL, (Term)&str31);
void glblProto76_2225(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(365), args);
  args = makePair(APP, 0, glblStr1912, args);
  interact(args, glblProto76);
}
}
void glblProto56_2227(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(365), args);
  args = makePair(APP, 0, glblStr1912, args);
  interact(args, glblProto56);
}
}
void glblProto34_2229(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(363), args);
  args = makePair(APP, 0, glblStr1912, args);
  interact(args, glblProto34);
}
}
Term glblFn1776;
void glblCFn1775(Term ref, Term args) {
Term apps, lams, V;
Location vLoc;
Term seq_4 = makePair(LAM, 0, SUB, NUL);
Location r_seq_4 = portLoc(2, seq_4);
Term loc_3 = newTerm(VAR, 0, portLoc(1, seq_4));
seq_4 = makePair(LAM, 0, SUB, seq_4);
Term acc_2 = newTerm(VAR, 0, portLoc(1, seq_4));
seq_4 = makePair(LAM, 0, SUB, seq_4);
Term exprs_1 = newTerm(VAR, 0, portLoc(1, seq_4));
// allocate args at /home/jim/toccata/interpreter/intrp-rdr.toc: 361

Term exprs_1_2;
exprs_1 = dupeArg(exprs_1, &exprs_1_2, 302);

Term exprs_1_1;
exprs_1 = dupeArg(exprs_1, &exprs_1_1, 303);

Term acc_2_1;
acc_2 = dupeArg(acc_2, &acc_2_1, 304);

Term loc_3_1;
loc_3 = dupeArg(loc_3, &loc_3_1, 305);
Term seq_10 = makePair(LAM, 0, SUB, NUL);
Location r_seq_10 = portLoc(2, seq_10);
Term e_9 = newTerm(VAR, 0, portLoc(1, seq_10));
// allocate args at /home/jim/toccata/interpreter/intrp-rdr.toc: 365
Term glblVal175_46;
glblVal175 = dupeArg(glblVal175, &glblVal175_46, 0);

Term glblVal1560_1;
glblVal1560 = dupeArg(glblVal1560, &glblVal1560_1, 0);

Term glblVal1591_1;
glblVal1591 = dupeArg(glblVal1591, &glblVal1591_1, 0);

// call count at /home/jim/toccata/interpreter/intrp-rdr.toc: 363
Term Rslt_6Args = makePair(APP, 0, exprs_1, SUB);
Term Rslt_6 = newTerm(VAR, 0, portLoc(2, Rslt_6Args));
#ifdef STRICT
pushRedex(Rslt_6Args, newRef(glblProto64_2217));
#else
swap(termLoc(Rslt_6), makePair(LAZ, 0, Rslt_6Args, newRef(glblProto64_2217)));
#endif
// call = at /home/jim/toccata/interpreter/intrp-rdr.toc: 363
Term Rslt_7Args = makePair(APP, 0, newI60(0), SUB);
Term Rslt_7 = newTerm(VAR, 0, portLoc(2, Rslt_7Args));
Rslt_7Args = makePair(APP, 0, Rslt_6, Rslt_7Args);
#ifdef STRICT
pushRedex(Rslt_7Args, newRef(glblProto20_2219));
#else
swap(termLoc(Rslt_7), makePair(LAZ, 0, Rslt_7Args, newRef(glblProto20_2219)));
#endif
// call rest at /home/jim/toccata/interpreter/intrp-rdr.toc: 366
Term Rslt_12Args = makePair(APP, 0, exprs_1_1, SUB);
Term Rslt_12 = newTerm(VAR, 0, portLoc(2, Rslt_12Args));
#ifdef STRICT
pushRedex(Rslt_12Args, newRef(glblProto78_2221));
#else
swap(termLoc(Rslt_12), makePair(LAZ, 0, Rslt_12Args, newRef(glblProto78_2221)));
#endif
// call ast/Symbol at /home/jim/toccata/interpreter/intrp-rdr.toc: 367
Term Rslt_13Args = makePair(APP, 0, loc_3, SUB);
Term Rslt_13 = newTerm(VAR, 0, portLoc(2, Rslt_13Args));
Rslt_13Args = makePair(APP, 0, glblStr2215, Rslt_13Args);
Rslt_13Args = makePair(APP, 0, glblStr402, Rslt_13Args);
#ifdef STRICT
pushRedex(Rslt_13Args, glblVal1560_1);
#else
swap(termLoc(Rslt_13), makePair(LAZ, 0, Rslt_13Args, glblVal1560_1));
#endif

// call vect-conj at /home/jim/toccata/interpreter/intrp-rdr.toc: 367
Term Rslt_14Args = makePair(APP, 0, acc_2_1, SUB);
Term Rslt_14 = newTerm(VAR, 0, portLoc(2, Rslt_14Args));
Rslt_14Args = makePair(APP, 0, glblVal175_46, Rslt_14Args);
#ifdef STRICT
pushRedex(Rslt_14Args, glblFn178);
#else
swap(termLoc(Rslt_14), makePair(LAZ, 0, Rslt_14Args, glblFn178));
#endif

// call vect-conj at /home/jim/toccata/interpreter/intrp-rdr.toc: 367
Term Rslt_15Args = makePair(APP, 0, e_9, SUB);
Term Rslt_15 = newTerm(VAR, 0, portLoc(2, Rslt_15Args));
Rslt_15Args = makePair(APP, 0, Rslt_14, Rslt_15Args);
#ifdef STRICT
pushRedex(Rslt_15Args, glblFn178);
#else
swap(termLoc(Rslt_15), makePair(LAZ, 0, Rslt_15Args, glblFn178));
#endif

// call ast/Call at /home/jim/toccata/interpreter/intrp-rdr.toc: 367
Term Rslt_16Args = makePair(APP, 0, Rslt_15, SUB);
Term Rslt_16 = newTerm(VAR, 0, portLoc(2, Rslt_16Args));
Rslt_16Args = makePair(APP, 0, Rslt_13, Rslt_16Args);
#ifdef STRICT
pushRedex(Rslt_16Args, glblVal1591_1);
#else
swap(termLoc(Rslt_16), makePair(LAZ, 0, Rslt_16Args, glblVal1591_1));
#endif

// call fold-vec-acc at /home/jim/toccata/interpreter/intrp-rdr.toc: 361
Term Rslt_17Args = makePair(APP, 0, loc_3_1, SUB);
Term Rslt_17 = newTerm(VAR, 0, portLoc(2, Rslt_17Args));
Rslt_17Args = makePair(APP, 0, Rslt_16, Rslt_17Args);
Rslt_17Args = makePair(APP, 0, Rslt_12, Rslt_17Args);
#ifdef STRICT
pushRedex(Rslt_17Args, glblFn1776);
#else
swap(termLoc(Rslt_17), makePair(LAZ, 0, Rslt_17Args, glblFn1776));
#endif

// link args to body
swap(r_seq_10, Rslt_17);

Term Rslt_17V = get(termLoc(Rslt_17));
apps = take(portLoc(1, Rslt_17V));
lams = take(portLoc(2, Rslt_17V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_17), makePair(LAZ, 0, apps, lams));
// call first at /home/jim/toccata/interpreter/intrp-rdr.toc: 365
Term Rslt_18Args = makePair(APP, 0, exprs_1_2, SUB);
Term Rslt_18 = newTerm(VAR, 0, portLoc(2, Rslt_18Args));
#ifdef STRICT
pushRedex(Rslt_18Args, newRef(glblProto76_2225));
#else
swap(termLoc(Rslt_18), makePair(LAZ, 0, Rslt_18Args, newRef(glblProto76_2225)));
#endif
// call extract at /home/jim/toccata/interpreter/intrp-rdr.toc: 365
Term Rslt_19Args = makePair(APP, 0, Rslt_18, SUB);
Term Rslt_19 = newTerm(VAR, 0, portLoc(2, Rslt_19Args));
#ifdef STRICT
pushRedex(Rslt_19Args, newRef(glblProto56_2227));
#else
swap(termLoc(Rslt_19), makePair(LAZ, 0, Rslt_19Args, newRef(glblProto56_2227)));
#endif
// call  at /home/jim/toccata/interpreter/intrp-rdr.toc: 365
Term Rslt_20Args = makePair(APP, 0, Rslt_19, SUB);
Term Rslt_20 = newTerm(VAR, 0, portLoc(2, Rslt_20Args));
#ifdef STRICT
pushRedex(Rslt_20Args, seq_10);
#else
swap(termLoc(Rslt_20), makePair(LAZ, 0, Rslt_20Args, seq_10));
#endif

// call cond at /home/jim/toccata/interpreter/intrp-rdr.toc: 363
Term Rslt_21Args = makePair(APP, 0, Rslt_20, SUB);
Term Rslt_21 = newTerm(VAR, 0, portLoc(2, Rslt_21Args));
Rslt_21Args = makePair(APP, 0, acc_2, Rslt_21Args);
Rslt_21Args = makePair(APP, 0, Rslt_7, Rslt_21Args);
#ifdef STRICT
pushRedex(Rslt_21Args, newRef(glblProto34_2229));
#else
swap(termLoc(Rslt_21), makePair(LAZ, 0, Rslt_21Args, newRef(glblProto34_2229)));
#endif
// link args to body
swap(r_seq_4, Rslt_21);

Term Rslt_21V = get(termLoc(Rslt_21));
apps = take(portLoc(1, Rslt_21V));
lams = take(portLoc(2, Rslt_21V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_21), makePair(LAZ, 0, apps, lams));
pushRedex(args, seq_4);
return;
}
Term glblFn1776 = newRef(glblCFn1775);
struct {TYPE_SIZE type;
        REFS_SIZE refs;
        int64_t *hash;
        Value *parent;
        int64_t len;
        char buffer[13];
} str32 = {StringBufferType, REFS_STATIC, 0, 0, 12, "empty-vector"};
Term glblStr2223 = newTerm_(VAL, (Term)&str32);
Term glblFn1778;
void glblCFn1777(Term ref, Term args) {
Term apps, lams, V;
Location vLoc;
Term seq_3 = makePair(LAM, 0, SUB, NUL);
Location r_seq_3 = portLoc(2, seq_3);
Term loc_2 = newTerm(VAR, 0, portLoc(1, seq_3));
seq_3 = makePair(LAM, 0, SUB, seq_3);
Term exprs_1 = newTerm(VAR, 0, portLoc(1, seq_3));
// allocate args at /home/jim/toccata/interpreter/intrp-rdr.toc: 372

Term loc_2_1;
loc_2 = dupeArg(loc_2, &loc_2_1, 306);
Term glblVal1560_2;
glblVal1560 = dupeArg(glblVal1560, &glblVal1560_2, 0);

// call ast/Symbol at /home/jim/toccata/interpreter/intrp-rdr.toc: 373
Term Rslt_5Args = makePair(APP, 0, loc_2, SUB);
Term Rslt_5 = newTerm(VAR, 0, portLoc(2, Rslt_5Args));
Rslt_5Args = makePair(APP, 0, glblStr2223, Rslt_5Args);
Rslt_5Args = makePair(APP, 0, glblStr402, Rslt_5Args);
#ifdef STRICT
pushRedex(Rslt_5Args, glblVal1560_2);
#else
swap(termLoc(Rslt_5), makePair(LAZ, 0, Rslt_5Args, glblVal1560_2));
#endif

// call fold-vec-acc at /home/jim/toccata/interpreter/intrp-rdr.toc: 373
Term Rslt_6Args = makePair(APP, 0, loc_2_1, SUB);
Term Rslt_6 = newTerm(VAR, 0, portLoc(2, Rslt_6Args));
Rslt_6Args = makePair(APP, 0, Rslt_5, Rslt_6Args);
Rslt_6Args = makePair(APP, 0, exprs_1, Rslt_6Args);
#ifdef STRICT
pushRedex(Rslt_6Args, glblFn1776);
#else
swap(termLoc(Rslt_6), makePair(LAZ, 0, Rslt_6Args, glblFn1776));
#endif

// link args to body
swap(r_seq_3, Rslt_6);

Term Rslt_6V = get(termLoc(Rslt_6));
apps = take(portLoc(1, Rslt_6V));
lams = take(portLoc(2, Rslt_6V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_6), makePair(LAZ, 0, apps, lams));
pushRedex(args, seq_3);
return;
}
Term glblFn1778 = newRef(glblCFn1777);
void glblProto64_2235(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(377), args);
  args = makePair(APP, 0, glblStr1912, args);
  interact(args, glblProto64);
}
}
void glblProto20_2237(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(377), args);
  args = makePair(APP, 0, glblStr1912, args);
  interact(args, glblProto20);
}
}
void glblProto78_2239(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(380), args);
  args = makePair(APP, 0, glblStr1912, args);
  interact(args, glblProto78);
}
}
struct {TYPE_SIZE type;
        REFS_SIZE refs;
        int64_t *hash;
        Value *parent;
        int64_t len;
        char buffer[6];
} str33 = {StringBufferType, REFS_STATIC, 0, 0, 5, "assoc"};
Term glblStr2233 = newTerm_(VAL, (Term)&str33);
void glblProto76_2243(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(379), args);
  args = makePair(APP, 0, glblStr1912, args);
  interact(args, glblProto76);
}
}
void glblProto56_2245(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(379), args);
  args = makePair(APP, 0, glblStr1912, args);
  interact(args, glblProto56);
}
}
void glblProto34_2247(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(377), args);
  args = makePair(APP, 0, glblStr1912, args);
  interact(args, glblProto34);
}
}
Term glblFn1780;
void glblCFn1779(Term ref, Term args) {
Term apps, lams, V;
Location vLoc;
Term seq_4 = makePair(LAM, 0, SUB, NUL);
Location r_seq_4 = portLoc(2, seq_4);
Term loc_3 = newTerm(VAR, 0, portLoc(1, seq_4));
seq_4 = makePair(LAM, 0, SUB, seq_4);
Term acc_2 = newTerm(VAR, 0, portLoc(1, seq_4));
seq_4 = makePair(LAM, 0, SUB, seq_4);
Term pairs_1 = newTerm(VAR, 0, portLoc(1, seq_4));
// allocate args at /home/jim/toccata/interpreter/intrp-rdr.toc: 375

Term pairs_1_2;
pairs_1 = dupeArg(pairs_1, &pairs_1_2, 307);

Term pairs_1_1;
pairs_1 = dupeArg(pairs_1, &pairs_1_1, 308);

Term acc_2_1;
acc_2 = dupeArg(acc_2, &acc_2_1, 309);

Term loc_3_1;
loc_3 = dupeArg(loc_3, &loc_3_1, 310);
Term seq_10 = makePair(LAM, 0, SUB, NUL);
Location r_seq_10 = portLoc(2, seq_10);
Term kv_9 = newTerm(VAR, 0, portLoc(1, seq_10));
// allocate args at /home/jim/toccata/interpreter/intrp-rdr.toc: 379

Term kv_9_1;
kv_9 = dupeArg(kv_9, &kv_9_1, 0);
Term glblVal175_47;
glblVal175 = dupeArg(glblVal175, &glblVal175_47, 0);

Term glblVal1560_3;
glblVal1560 = dupeArg(glblVal1560, &glblVal1560_3, 0);

Term glblVal1591_2;
glblVal1591 = dupeArg(glblVal1591, &glblVal1591_2, 0);

// call count at /home/jim/toccata/interpreter/intrp-rdr.toc: 377
Term Rslt_6Args = makePair(APP, 0, pairs_1, SUB);
Term Rslt_6 = newTerm(VAR, 0, portLoc(2, Rslt_6Args));
#ifdef STRICT
pushRedex(Rslt_6Args, newRef(glblProto64_2235));
#else
swap(termLoc(Rslt_6), makePair(LAZ, 0, Rslt_6Args, newRef(glblProto64_2235)));
#endif
// call = at /home/jim/toccata/interpreter/intrp-rdr.toc: 377
Term Rslt_7Args = makePair(APP, 0, newI60(0), SUB);
Term Rslt_7 = newTerm(VAR, 0, portLoc(2, Rslt_7Args));
Rslt_7Args = makePair(APP, 0, Rslt_6, Rslt_7Args);
#ifdef STRICT
pushRedex(Rslt_7Args, newRef(glblProto20_2237));
#else
swap(termLoc(Rslt_7), makePair(LAZ, 0, Rslt_7Args, newRef(glblProto20_2237)));
#endif
// call rest at /home/jim/toccata/interpreter/intrp-rdr.toc: 380
Term Rslt_12Args = makePair(APP, 0, pairs_1_1, SUB);
Term Rslt_12 = newTerm(VAR, 0, portLoc(2, Rslt_12Args));
#ifdef STRICT
pushRedex(Rslt_12Args, newRef(glblProto78_2239));
#else
swap(termLoc(Rslt_12), makePair(LAZ, 0, Rslt_12Args, newRef(glblProto78_2239)));
#endif
// call ast/Symbol at /home/jim/toccata/interpreter/intrp-rdr.toc: 381
Term Rslt_13Args = makePair(APP, 0, loc_3, SUB);
Term Rslt_13 = newTerm(VAR, 0, portLoc(2, Rslt_13Args));
Rslt_13Args = makePair(APP, 0, glblStr2233, Rslt_13Args);
Rslt_13Args = makePair(APP, 0, glblStr402, Rslt_13Args);
#ifdef STRICT
pushRedex(Rslt_13Args, glblVal1560_3);
#else
swap(termLoc(Rslt_13), makePair(LAZ, 0, Rslt_13Args, glblVal1560_3));
#endif

// call vect-conj at /home/jim/toccata/interpreter/intrp-rdr.toc: 382
Term Rslt_14Args = makePair(APP, 0, acc_2_1, SUB);
Term Rslt_14 = newTerm(VAR, 0, portLoc(2, Rslt_14Args));
Rslt_14Args = makePair(APP, 0, glblVal175_47, Rslt_14Args);
#ifdef STRICT
pushRedex(Rslt_14Args, glblFn178);
#else
swap(termLoc(Rslt_14), makePair(LAZ, 0, Rslt_14Args, glblFn178));
#endif

// call elt0 at /home/jim/toccata/interpreter/intrp-rdr.toc: 382
Term Rslt_15Args = makePair(APP, 0, kv_9, SUB);
Term Rslt_15 = newTerm(VAR, 0, portLoc(2, Rslt_15Args));
#ifdef STRICT
pushRedex(Rslt_15Args, glblFn1742);
#else
swap(termLoc(Rslt_15), makePair(LAZ, 0, Rslt_15Args, glblFn1742));
#endif

// call vect-conj at /home/jim/toccata/interpreter/intrp-rdr.toc: 382
Term Rslt_16Args = makePair(APP, 0, Rslt_15, SUB);
Term Rslt_16 = newTerm(VAR, 0, portLoc(2, Rslt_16Args));
Rslt_16Args = makePair(APP, 0, Rslt_14, Rslt_16Args);
#ifdef STRICT
pushRedex(Rslt_16Args, glblFn178);
#else
swap(termLoc(Rslt_16), makePair(LAZ, 0, Rslt_16Args, glblFn178));
#endif

// call elt1 at /home/jim/toccata/interpreter/intrp-rdr.toc: 382
Term Rslt_17Args = makePair(APP, 0, kv_9_1, SUB);
Term Rslt_17 = newTerm(VAR, 0, portLoc(2, Rslt_17Args));
#ifdef STRICT
pushRedex(Rslt_17Args, glblFn1744);
#else
swap(termLoc(Rslt_17), makePair(LAZ, 0, Rslt_17Args, glblFn1744));
#endif

// call vect-conj at /home/jim/toccata/interpreter/intrp-rdr.toc: 382
Term Rslt_18Args = makePair(APP, 0, Rslt_17, SUB);
Term Rslt_18 = newTerm(VAR, 0, portLoc(2, Rslt_18Args));
Rslt_18Args = makePair(APP, 0, Rslt_16, Rslt_18Args);
#ifdef STRICT
pushRedex(Rslt_18Args, glblFn178);
#else
swap(termLoc(Rslt_18), makePair(LAZ, 0, Rslt_18Args, glblFn178));
#endif

// call ast/Call at /home/jim/toccata/interpreter/intrp-rdr.toc: 381
Term Rslt_19Args = makePair(APP, 0, Rslt_18, SUB);
Term Rslt_19 = newTerm(VAR, 0, portLoc(2, Rslt_19Args));
Rslt_19Args = makePair(APP, 0, Rslt_13, Rslt_19Args);
#ifdef STRICT
pushRedex(Rslt_19Args, glblVal1591_2);
#else
swap(termLoc(Rslt_19), makePair(LAZ, 0, Rslt_19Args, glblVal1591_2));
#endif

// call hash-fold-acc at /home/jim/toccata/interpreter/intrp-rdr.toc: 375
Term Rslt_20Args = makePair(APP, 0, loc_3_1, SUB);
Term Rslt_20 = newTerm(VAR, 0, portLoc(2, Rslt_20Args));
Rslt_20Args = makePair(APP, 0, Rslt_19, Rslt_20Args);
Rslt_20Args = makePair(APP, 0, Rslt_12, Rslt_20Args);
#ifdef STRICT
pushRedex(Rslt_20Args, glblFn1780);
#else
swap(termLoc(Rslt_20), makePair(LAZ, 0, Rslt_20Args, glblFn1780));
#endif

// link args to body
swap(r_seq_10, Rslt_20);

Term Rslt_20V = get(termLoc(Rslt_20));
apps = take(portLoc(1, Rslt_20V));
lams = take(portLoc(2, Rslt_20V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_20), makePair(LAZ, 0, apps, lams));
// call first at /home/jim/toccata/interpreter/intrp-rdr.toc: 379
Term Rslt_21Args = makePair(APP, 0, pairs_1_2, SUB);
Term Rslt_21 = newTerm(VAR, 0, portLoc(2, Rslt_21Args));
#ifdef STRICT
pushRedex(Rslt_21Args, newRef(glblProto76_2243));
#else
swap(termLoc(Rslt_21), makePair(LAZ, 0, Rslt_21Args, newRef(glblProto76_2243)));
#endif
// call extract at /home/jim/toccata/interpreter/intrp-rdr.toc: 379
Term Rslt_22Args = makePair(APP, 0, Rslt_21, SUB);
Term Rslt_22 = newTerm(VAR, 0, portLoc(2, Rslt_22Args));
#ifdef STRICT
pushRedex(Rslt_22Args, newRef(glblProto56_2245));
#else
swap(termLoc(Rslt_22), makePair(LAZ, 0, Rslt_22Args, newRef(glblProto56_2245)));
#endif
// call  at /home/jim/toccata/interpreter/intrp-rdr.toc: 379
Term Rslt_23Args = makePair(APP, 0, Rslt_22, SUB);
Term Rslt_23 = newTerm(VAR, 0, portLoc(2, Rslt_23Args));
#ifdef STRICT
pushRedex(Rslt_23Args, seq_10);
#else
swap(termLoc(Rslt_23), makePair(LAZ, 0, Rslt_23Args, seq_10));
#endif

// call cond at /home/jim/toccata/interpreter/intrp-rdr.toc: 377
Term Rslt_24Args = makePair(APP, 0, Rslt_23, SUB);
Term Rslt_24 = newTerm(VAR, 0, portLoc(2, Rslt_24Args));
Rslt_24Args = makePair(APP, 0, acc_2, Rslt_24Args);
Rslt_24Args = makePair(APP, 0, Rslt_7, Rslt_24Args);
#ifdef STRICT
pushRedex(Rslt_24Args, newRef(glblProto34_2247));
#else
swap(termLoc(Rslt_24), makePair(LAZ, 0, Rslt_24Args, newRef(glblProto34_2247)));
#endif
// link args to body
swap(r_seq_4, Rslt_24);

Term Rslt_24V = get(termLoc(Rslt_24));
apps = take(portLoc(1, Rslt_24V));
lams = take(portLoc(2, Rslt_24V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_24), makePair(LAZ, 0, apps, lams));
pushRedex(args, seq_4);
return;
}
Term glblFn1780 = newRef(glblCFn1779);
struct {TYPE_SIZE type;
        REFS_SIZE refs;
        int64_t *hash;
        Value *parent;
        int64_t len;
        char buffer[9];
} str34 = {StringBufferType, REFS_STATIC, 0, 0, 8, "emptyBMI"};
Term glblStr2241 = newTerm_(VAL, (Term)&str34);
Term glblFn1782;
void glblCFn1781(Term ref, Term args) {
Term apps, lams, V;
Location vLoc;
Term seq_3 = makePair(LAM, 0, SUB, NUL);
Location r_seq_3 = portLoc(2, seq_3);
Term loc_2 = newTerm(VAR, 0, portLoc(1, seq_3));
seq_3 = makePair(LAM, 0, SUB, seq_3);
Term pairs_1 = newTerm(VAR, 0, portLoc(1, seq_3));
// allocate args at /home/jim/toccata/interpreter/intrp-rdr.toc: 386

Term loc_2_1;
loc_2 = dupeArg(loc_2, &loc_2_1, 311);
Term glblVal1560_4;
glblVal1560 = dupeArg(glblVal1560, &glblVal1560_4, 0);

// call ast/Symbol at /home/jim/toccata/interpreter/intrp-rdr.toc: 387
Term Rslt_5Args = makePair(APP, 0, loc_2, SUB);
Term Rslt_5 = newTerm(VAR, 0, portLoc(2, Rslt_5Args));
Rslt_5Args = makePair(APP, 0, glblStr2241, Rslt_5Args);
Rslt_5Args = makePair(APP, 0, glblStr402, Rslt_5Args);
#ifdef STRICT
pushRedex(Rslt_5Args, glblVal1560_4);
#else
swap(termLoc(Rslt_5), makePair(LAZ, 0, Rslt_5Args, glblVal1560_4));
#endif

// call hash-fold-acc at /home/jim/toccata/interpreter/intrp-rdr.toc: 387
Term Rslt_6Args = makePair(APP, 0, loc_2_1, SUB);
Term Rslt_6 = newTerm(VAR, 0, portLoc(2, Rslt_6Args));
Rslt_6Args = makePair(APP, 0, Rslt_5, Rslt_6Args);
Rslt_6Args = makePair(APP, 0, pairs_1, Rslt_6Args);
#ifdef STRICT
pushRedex(Rslt_6Args, glblFn1780);
#else
swap(termLoc(Rslt_6), makePair(LAZ, 0, Rslt_6Args, glblFn1780));
#endif

// link args to body
swap(r_seq_3, Rslt_6);

Term Rslt_6V = get(termLoc(Rslt_6));
apps = take(portLoc(1, Rslt_6V));
lams = take(portLoc(2, Rslt_6V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_6), makePair(LAZ, 0, apps, lams));
pushRedex(args, seq_3);
return;
}
Term glblFn1782 = newRef(glblCFn1781);
void glblProto64_2253(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(392), args);
  args = makePair(APP, 0, glblStr1912, args);
  interact(args, glblProto64);
}
}
void glblProto20_2255(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(392), args);
  args = makePair(APP, 0, glblStr1912, args);
  interact(args, glblProto20);
}
}
void glblProto76_2257(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(393), args);
  args = makePair(APP, 0, glblStr1912, args);
  interact(args, glblProto76);
}
}
void glblProto56_2259(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(393), args);
  args = makePair(APP, 0, glblStr1912, args);
  interact(args, glblProto56);
}
}
struct {TYPE_SIZE type;
        REFS_SIZE refs;
        int64_t *hash;
        Value *parent;
        int64_t len;
        char buffer[5];
} str35 = {StringBufferType, REFS_STATIC, 0, 0, 4, "cond"};
Term glblStr2251 = newTerm_(VAL, (Term)&str35);
void glblProto78_2261(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(398), args);
  args = makePair(APP, 0, glblStr1912, args);
  interact(args, glblProto78);
}
}
void glblProto76_2263(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(396), args);
  args = makePair(APP, 0, glblStr1912, args);
  interact(args, glblProto76);
}
}
void glblProto56_2265(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(396), args);
  args = makePair(APP, 0, glblStr1912, args);
  interact(args, glblProto56);
}
}
void glblProto78_2267(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(395), args);
  args = makePair(APP, 0, glblStr1912, args);
  interact(args, glblProto78);
}
}
void glblProto76_2269(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(394), args);
  args = makePair(APP, 0, glblStr1912, args);
  interact(args, glblProto76);
}
}
void glblProto56_2271(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(394), args);
  args = makePair(APP, 0, glblStr1912, args);
  interact(args, glblProto56);
}
}
void glblProto34_2273(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(392), args);
  args = makePair(APP, 0, glblStr1912, args);
  interact(args, glblProto34);
}
}
Term glblFn1784;
void glblCFn1783(Term ref, Term args) {
Term apps, lams, V;
Location vLoc;
Term seq_3 = makePair(LAM, 0, SUB, NUL);
Location r_seq_3 = portLoc(2, seq_3);
Term loc_2 = newTerm(VAR, 0, portLoc(1, seq_3));
seq_3 = makePair(LAM, 0, SUB, seq_3);
Term r_1 = newTerm(VAR, 0, portLoc(1, seq_3));
// allocate args at /home/jim/toccata/interpreter/intrp-rdr.toc: 390

Term r_1_3;
r_1 = dupeArg(r_1, &r_1_3, 312);

Term r_1_2;
r_1 = dupeArg(r_1, &r_1_2, 313);

Term r_1_1;
r_1 = dupeArg(r_1, &r_1_1, 314);

Term loc_2_1;
loc_2 = dupeArg(loc_2, &loc_2_1, 315);
Term seq_11 = makePair(LAM, 0, SUB, NUL);
Location r_seq_11 = portLoc(2, seq_11);
Term t_10 = newTerm(VAR, 0, portLoc(1, seq_11));
// allocate args at /home/jim/toccata/interpreter/intrp-rdr.toc: 394
Term seq_15 = makePair(LAM, 0, SUB, NUL);
Location r_seq_15 = portLoc(2, seq_15);
Term r1_14 = newTerm(VAR, 0, portLoc(1, seq_15));
// allocate args at /home/jim/toccata/interpreter/intrp-rdr.toc: 395

Term r1_14_1;
r1_14 = dupeArg(r1_14, &r1_14_1, 0);
Term seq_19 = makePair(LAM, 0, SUB, NUL);
Location r_seq_19 = portLoc(2, seq_19);
Term v_18 = newTerm(VAR, 0, portLoc(1, seq_19));
// allocate args at /home/jim/toccata/interpreter/intrp-rdr.toc: 396
Term glblVal175_48;
glblVal175 = dupeArg(glblVal175, &glblVal175_48, 0);

Term glblVal1560_5;
glblVal1560 = dupeArg(glblVal1560, &glblVal1560_5, 0);

Term glblVal1591_3;
glblVal1591 = dupeArg(glblVal1591, &glblVal1591_3, 0);

// call count at /home/jim/toccata/interpreter/intrp-rdr.toc: 392
Term Rslt_5Args = makePair(APP, 0, r_1, SUB);
Term Rslt_5 = newTerm(VAR, 0, portLoc(2, Rslt_5Args));
#ifdef STRICT
pushRedex(Rslt_5Args, newRef(glblProto64_2253));
#else
swap(termLoc(Rslt_5), makePair(LAZ, 0, Rslt_5Args, newRef(glblProto64_2253)));
#endif
// call = at /home/jim/toccata/interpreter/intrp-rdr.toc: 392
Term Rslt_6Args = makePair(APP, 0, newI60(1), SUB);
Term Rslt_6 = newTerm(VAR, 0, portLoc(2, Rslt_6Args));
Rslt_6Args = makePair(APP, 0, Rslt_5, Rslt_6Args);
#ifdef STRICT
pushRedex(Rslt_6Args, newRef(glblProto20_2255));
#else
swap(termLoc(Rslt_6), makePair(LAZ, 0, Rslt_6Args, newRef(glblProto20_2255)));
#endif
// call first at /home/jim/toccata/interpreter/intrp-rdr.toc: 393
Term Rslt_7Args = makePair(APP, 0, r_1_1, SUB);
Term Rslt_7 = newTerm(VAR, 0, portLoc(2, Rslt_7Args));
#ifdef STRICT
pushRedex(Rslt_7Args, newRef(glblProto76_2257));
#else
swap(termLoc(Rslt_7), makePair(LAZ, 0, Rslt_7Args, newRef(glblProto76_2257)));
#endif
// call extract at /home/jim/toccata/interpreter/intrp-rdr.toc: 393
Term Rslt_8Args = makePair(APP, 0, Rslt_7, SUB);
Term Rslt_8 = newTerm(VAR, 0, portLoc(2, Rslt_8Args));
#ifdef STRICT
pushRedex(Rslt_8Args, newRef(glblProto56_2259));
#else
swap(termLoc(Rslt_8), makePair(LAZ, 0, Rslt_8Args, newRef(glblProto56_2259)));
#endif
// call ast/Symbol at /home/jim/toccata/interpreter/intrp-rdr.toc: 397
Term Rslt_21Args = makePair(APP, 0, loc_2, SUB);
Term Rslt_21 = newTerm(VAR, 0, portLoc(2, Rslt_21Args));
Rslt_21Args = makePair(APP, 0, glblStr2251, Rslt_21Args);
Rslt_21Args = makePair(APP, 0, glblStr402, Rslt_21Args);
#ifdef STRICT
pushRedex(Rslt_21Args, glblVal1560_5);
#else
swap(termLoc(Rslt_21), makePair(LAZ, 0, Rslt_21Args, glblVal1560_5));
#endif

// call vect-conj at /home/jim/toccata/interpreter/intrp-rdr.toc: 398
Term Rslt_22Args = makePair(APP, 0, t_10, SUB);
Term Rslt_22 = newTerm(VAR, 0, portLoc(2, Rslt_22Args));
Rslt_22Args = makePair(APP, 0, glblVal175_48, Rslt_22Args);
#ifdef STRICT
pushRedex(Rslt_22Args, glblFn178);
#else
swap(termLoc(Rslt_22), makePair(LAZ, 0, Rslt_22Args, glblFn178));
#endif

// call vect-conj at /home/jim/toccata/interpreter/intrp-rdr.toc: 398
Term Rslt_23Args = makePair(APP, 0, v_18, SUB);
Term Rslt_23 = newTerm(VAR, 0, portLoc(2, Rslt_23Args));
Rslt_23Args = makePair(APP, 0, Rslt_22, Rslt_23Args);
#ifdef STRICT
pushRedex(Rslt_23Args, glblFn178);
#else
swap(termLoc(Rslt_23), makePair(LAZ, 0, Rslt_23Args, glblFn178));
#endif

// call rest at /home/jim/toccata/interpreter/intrp-rdr.toc: 398
Term Rslt_24Args = makePair(APP, 0, r1_14, SUB);
Term Rslt_24 = newTerm(VAR, 0, portLoc(2, Rslt_24Args));
#ifdef STRICT
pushRedex(Rslt_24Args, newRef(glblProto78_2261));
#else
swap(termLoc(Rslt_24), makePair(LAZ, 0, Rslt_24Args, newRef(glblProto78_2261)));
#endif
// call cond-inner at /home/jim/toccata/interpreter/intrp-rdr.toc: 390
Term Rslt_25Args = makePair(APP, 0, loc_2_1, SUB);
Term Rslt_25 = newTerm(VAR, 0, portLoc(2, Rslt_25Args));
Rslt_25Args = makePair(APP, 0, Rslt_24, Rslt_25Args);
#ifdef STRICT
pushRedex(Rslt_25Args, glblFn1784);
#else
swap(termLoc(Rslt_25), makePair(LAZ, 0, Rslt_25Args, glblFn1784));
#endif

// call vect-conj at /home/jim/toccata/interpreter/intrp-rdr.toc: 398
Term Rslt_26Args = makePair(APP, 0, Rslt_25, SUB);
Term Rslt_26 = newTerm(VAR, 0, portLoc(2, Rslt_26Args));
Rslt_26Args = makePair(APP, 0, Rslt_23, Rslt_26Args);
#ifdef STRICT
pushRedex(Rslt_26Args, glblFn178);
#else
swap(termLoc(Rslt_26), makePair(LAZ, 0, Rslt_26Args, glblFn178));
#endif

// call ast/Call at /home/jim/toccata/interpreter/intrp-rdr.toc: 397
Term Rslt_27Args = makePair(APP, 0, Rslt_26, SUB);
Term Rslt_27 = newTerm(VAR, 0, portLoc(2, Rslt_27Args));
Rslt_27Args = makePair(APP, 0, Rslt_21, Rslt_27Args);
#ifdef STRICT
pushRedex(Rslt_27Args, glblVal1591_3);
#else
swap(termLoc(Rslt_27), makePair(LAZ, 0, Rslt_27Args, glblVal1591_3));
#endif

// link args to body
swap(r_seq_19, Rslt_27);

Term Rslt_27V = get(termLoc(Rslt_27));
apps = take(portLoc(1, Rslt_27V));
lams = take(portLoc(2, Rslt_27V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_27), makePair(LAZ, 0, apps, lams));
// call first at /home/jim/toccata/interpreter/intrp-rdr.toc: 396
Term Rslt_28Args = makePair(APP, 0, r1_14_1, SUB);
Term Rslt_28 = newTerm(VAR, 0, portLoc(2, Rslt_28Args));
#ifdef STRICT
pushRedex(Rslt_28Args, newRef(glblProto76_2263));
#else
swap(termLoc(Rslt_28), makePair(LAZ, 0, Rslt_28Args, newRef(glblProto76_2263)));
#endif
// call extract at /home/jim/toccata/interpreter/intrp-rdr.toc: 396
Term Rslt_29Args = makePair(APP, 0, Rslt_28, SUB);
Term Rslt_29 = newTerm(VAR, 0, portLoc(2, Rslt_29Args));
#ifdef STRICT
pushRedex(Rslt_29Args, newRef(glblProto56_2265));
#else
swap(termLoc(Rslt_29), makePair(LAZ, 0, Rslt_29Args, newRef(glblProto56_2265)));
#endif
// call  at /home/jim/toccata/interpreter/intrp-rdr.toc: 396
Term Rslt_30Args = makePair(APP, 0, Rslt_29, SUB);
Term Rslt_30 = newTerm(VAR, 0, portLoc(2, Rslt_30Args));
#ifdef STRICT
pushRedex(Rslt_30Args, seq_19);
#else
swap(termLoc(Rslt_30), makePair(LAZ, 0, Rslt_30Args, seq_19));
#endif

// link args to body
swap(r_seq_15, Rslt_30);

Term Rslt_30V = get(termLoc(Rslt_30));
apps = take(portLoc(1, Rslt_30V));
lams = take(portLoc(2, Rslt_30V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_30), makePair(LAZ, 0, apps, lams));
// call rest at /home/jim/toccata/interpreter/intrp-rdr.toc: 395
Term Rslt_31Args = makePair(APP, 0, r_1_2, SUB);
Term Rslt_31 = newTerm(VAR, 0, portLoc(2, Rslt_31Args));
#ifdef STRICT
pushRedex(Rslt_31Args, newRef(glblProto78_2267));
#else
swap(termLoc(Rslt_31), makePair(LAZ, 0, Rslt_31Args, newRef(glblProto78_2267)));
#endif
// call  at /home/jim/toccata/interpreter/intrp-rdr.toc: 395
Term Rslt_32Args = makePair(APP, 0, Rslt_31, SUB);
Term Rslt_32 = newTerm(VAR, 0, portLoc(2, Rslt_32Args));
#ifdef STRICT
pushRedex(Rslt_32Args, seq_15);
#else
swap(termLoc(Rslt_32), makePair(LAZ, 0, Rslt_32Args, seq_15));
#endif

// link args to body
swap(r_seq_11, Rslt_32);

Term Rslt_32V = get(termLoc(Rslt_32));
apps = take(portLoc(1, Rslt_32V));
lams = take(portLoc(2, Rslt_32V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_32), makePair(LAZ, 0, apps, lams));
// call first at /home/jim/toccata/interpreter/intrp-rdr.toc: 394
Term Rslt_33Args = makePair(APP, 0, r_1_3, SUB);
Term Rslt_33 = newTerm(VAR, 0, portLoc(2, Rslt_33Args));
#ifdef STRICT
pushRedex(Rslt_33Args, newRef(glblProto76_2269));
#else
swap(termLoc(Rslt_33), makePair(LAZ, 0, Rslt_33Args, newRef(glblProto76_2269)));
#endif
// call extract at /home/jim/toccata/interpreter/intrp-rdr.toc: 394
Term Rslt_34Args = makePair(APP, 0, Rslt_33, SUB);
Term Rslt_34 = newTerm(VAR, 0, portLoc(2, Rslt_34Args));
#ifdef STRICT
pushRedex(Rslt_34Args, newRef(glblProto56_2271));
#else
swap(termLoc(Rslt_34), makePair(LAZ, 0, Rslt_34Args, newRef(glblProto56_2271)));
#endif
// call  at /home/jim/toccata/interpreter/intrp-rdr.toc: 394
Term Rslt_35Args = makePair(APP, 0, Rslt_34, SUB);
Term Rslt_35 = newTerm(VAR, 0, portLoc(2, Rslt_35Args));
#ifdef STRICT
pushRedex(Rslt_35Args, seq_11);
#else
swap(termLoc(Rslt_35), makePair(LAZ, 0, Rslt_35Args, seq_11));
#endif

// call cond at /home/jim/toccata/interpreter/intrp-rdr.toc: 392
Term Rslt_36Args = makePair(APP, 0, Rslt_35, SUB);
Term Rslt_36 = newTerm(VAR, 0, portLoc(2, Rslt_36Args));
Rslt_36Args = makePair(APP, 0, Rslt_8, Rslt_36Args);
Rslt_36Args = makePair(APP, 0, Rslt_6, Rslt_36Args);
#ifdef STRICT
pushRedex(Rslt_36Args, newRef(glblProto34_2273));
#else
swap(termLoc(Rslt_36), makePair(LAZ, 0, Rslt_36Args, newRef(glblProto34_2273)));
#endif
// link args to body
swap(r_seq_3, Rslt_36);

Term Rslt_36V = get(termLoc(Rslt_36));
apps = take(portLoc(1, Rslt_36V));
lams = take(portLoc(2, Rslt_36V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_36), makePair(LAZ, 0, apps, lams));
pushRedex(args, seq_3);
return;
}
Term glblFn1784 = newRef(glblCFn1783);
void glblProto78_2278(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(404), args);
  args = makePair(APP, 0, glblStr1912, args);
  interact(args, glblProto78);
}
}
void glblProto76_2280(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(403), args);
  args = makePair(APP, 0, glblStr1912, args);
  interact(args, glblProto76);
}
}
void glblProto56_2282(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(403), args);
  args = makePair(APP, 0, glblStr1912, args);
  interact(args, glblProto56);
}
}
void glblProto78_2284(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(402), args);
  args = makePair(APP, 0, glblStr1912, args);
  interact(args, glblProto78);
}
}
void glblProto76_2286(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(401), args);
  args = makePair(APP, 0, glblStr1912, args);
  interact(args, glblProto76);
}
}
void glblProto56_2288(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(401), args);
  args = makePair(APP, 0, glblStr1912, args);
  interact(args, glblProto56);
}
}
Term glblFn1786;
void glblCFn1785(Term ref, Term args) {
Term apps, lams, V;
Location vLoc;
Term seq_3 = makePair(LAM, 0, SUB, NUL);
Location r_seq_3 = portLoc(2, seq_3);
Term loc_2 = newTerm(VAR, 0, portLoc(1, seq_3));
seq_3 = makePair(LAM, 0, SUB, seq_3);
Term exprs_1 = newTerm(VAR, 0, portLoc(1, seq_3));
// allocate args at /home/jim/toccata/interpreter/intrp-rdr.toc: 400

Term exprs_1_1;
exprs_1 = dupeArg(exprs_1, &exprs_1_1, 316);

Term loc_2_1;
loc_2 = dupeArg(loc_2, &loc_2_1, 317);
Term seq_7 = makePair(LAM, 0, SUB, NUL);
Location r_seq_7 = portLoc(2, seq_7);
Term t_6 = newTerm(VAR, 0, portLoc(1, seq_7));
// allocate args at /home/jim/toccata/interpreter/intrp-rdr.toc: 401
Term seq_11 = makePair(LAM, 0, SUB, NUL);
Location r_seq_11 = portLoc(2, seq_11);
Term r1_10 = newTerm(VAR, 0, portLoc(1, seq_11));
// allocate args at /home/jim/toccata/interpreter/intrp-rdr.toc: 402

Term r1_10_1;
r1_10 = dupeArg(r1_10, &r1_10_1, 0);
Term seq_15 = makePair(LAM, 0, SUB, NUL);
Location r_seq_15 = portLoc(2, seq_15);
Term v_14 = newTerm(VAR, 0, portLoc(1, seq_15));
// allocate args at /home/jim/toccata/interpreter/intrp-rdr.toc: 403
Term seq_19 = makePair(LAM, 0, SUB, NUL);
Location r_seq_19 = portLoc(2, seq_19);
Term r2_18 = newTerm(VAR, 0, portLoc(1, seq_19));
// allocate args at /home/jim/toccata/interpreter/intrp-rdr.toc: 404
Term glblVal175_49;
glblVal175 = dupeArg(glblVal175, &glblVal175_49, 0);

Term glblVal1560_6;
glblVal1560 = dupeArg(glblVal1560, &glblVal1560_6, 0);

Term glblVal1591_4;
glblVal1591 = dupeArg(glblVal1591, &glblVal1591_4, 0);

// call ast/Symbol at /home/jim/toccata/interpreter/intrp-rdr.toc: 405
Term Rslt_21Args = makePair(APP, 0, loc_2, SUB);
Term Rslt_21 = newTerm(VAR, 0, portLoc(2, Rslt_21Args));
Rslt_21Args = makePair(APP, 0, glblStr2251, Rslt_21Args);
Rslt_21Args = makePair(APP, 0, glblStr402, Rslt_21Args);
#ifdef STRICT
pushRedex(Rslt_21Args, glblVal1560_6);
#else
swap(termLoc(Rslt_21), makePair(LAZ, 0, Rslt_21Args, glblVal1560_6));
#endif

// call vect-conj at /home/jim/toccata/interpreter/intrp-rdr.toc: 405
Term Rslt_22Args = makePair(APP, 0, t_6, SUB);
Term Rslt_22 = newTerm(VAR, 0, portLoc(2, Rslt_22Args));
Rslt_22Args = makePair(APP, 0, glblVal175_49, Rslt_22Args);
#ifdef STRICT
pushRedex(Rslt_22Args, glblFn178);
#else
swap(termLoc(Rslt_22), makePair(LAZ, 0, Rslt_22Args, glblFn178));
#endif

// call vect-conj at /home/jim/toccata/interpreter/intrp-rdr.toc: 405
Term Rslt_23Args = makePair(APP, 0, v_14, SUB);
Term Rslt_23 = newTerm(VAR, 0, portLoc(2, Rslt_23Args));
Rslt_23Args = makePair(APP, 0, Rslt_22, Rslt_23Args);
#ifdef STRICT
pushRedex(Rslt_23Args, glblFn178);
#else
swap(termLoc(Rslt_23), makePair(LAZ, 0, Rslt_23Args, glblFn178));
#endif

// call cond-inner at /home/jim/toccata/interpreter/intrp-rdr.toc: 405
Term Rslt_24Args = makePair(APP, 0, loc_2_1, SUB);
Term Rslt_24 = newTerm(VAR, 0, portLoc(2, Rslt_24Args));
Rslt_24Args = makePair(APP, 0, r2_18, Rslt_24Args);
#ifdef STRICT
pushRedex(Rslt_24Args, glblFn1784);
#else
swap(termLoc(Rslt_24), makePair(LAZ, 0, Rslt_24Args, glblFn1784));
#endif

// call vect-conj at /home/jim/toccata/interpreter/intrp-rdr.toc: 405
Term Rslt_25Args = makePair(APP, 0, Rslt_24, SUB);
Term Rslt_25 = newTerm(VAR, 0, portLoc(2, Rslt_25Args));
Rslt_25Args = makePair(APP, 0, Rslt_23, Rslt_25Args);
#ifdef STRICT
pushRedex(Rslt_25Args, glblFn178);
#else
swap(termLoc(Rslt_25), makePair(LAZ, 0, Rslt_25Args, glblFn178));
#endif

// call ast/Call at /home/jim/toccata/interpreter/intrp-rdr.toc: 405
Term Rslt_26Args = makePair(APP, 0, Rslt_25, SUB);
Term Rslt_26 = newTerm(VAR, 0, portLoc(2, Rslt_26Args));
Rslt_26Args = makePair(APP, 0, Rslt_21, Rslt_26Args);
#ifdef STRICT
pushRedex(Rslt_26Args, glblVal1591_4);
#else
swap(termLoc(Rslt_26), makePair(LAZ, 0, Rslt_26Args, glblVal1591_4));
#endif

// link args to body
swap(r_seq_19, Rslt_26);

Term Rslt_26V = get(termLoc(Rslt_26));
apps = take(portLoc(1, Rslt_26V));
lams = take(portLoc(2, Rslt_26V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_26), makePair(LAZ, 0, apps, lams));
// call rest at /home/jim/toccata/interpreter/intrp-rdr.toc: 404
Term Rslt_27Args = makePair(APP, 0, r1_10, SUB);
Term Rslt_27 = newTerm(VAR, 0, portLoc(2, Rslt_27Args));
#ifdef STRICT
pushRedex(Rslt_27Args, newRef(glblProto78_2278));
#else
swap(termLoc(Rslt_27), makePair(LAZ, 0, Rslt_27Args, newRef(glblProto78_2278)));
#endif
// call  at /home/jim/toccata/interpreter/intrp-rdr.toc: 404
Term Rslt_28Args = makePair(APP, 0, Rslt_27, SUB);
Term Rslt_28 = newTerm(VAR, 0, portLoc(2, Rslt_28Args));
#ifdef STRICT
pushRedex(Rslt_28Args, seq_19);
#else
swap(termLoc(Rslt_28), makePair(LAZ, 0, Rslt_28Args, seq_19));
#endif

// link args to body
swap(r_seq_15, Rslt_28);

Term Rslt_28V = get(termLoc(Rslt_28));
apps = take(portLoc(1, Rslt_28V));
lams = take(portLoc(2, Rslt_28V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_28), makePair(LAZ, 0, apps, lams));
// call first at /home/jim/toccata/interpreter/intrp-rdr.toc: 403
Term Rslt_29Args = makePair(APP, 0, r1_10_1, SUB);
Term Rslt_29 = newTerm(VAR, 0, portLoc(2, Rslt_29Args));
#ifdef STRICT
pushRedex(Rslt_29Args, newRef(glblProto76_2280));
#else
swap(termLoc(Rslt_29), makePair(LAZ, 0, Rslt_29Args, newRef(glblProto76_2280)));
#endif
// call extract at /home/jim/toccata/interpreter/intrp-rdr.toc: 403
Term Rslt_30Args = makePair(APP, 0, Rslt_29, SUB);
Term Rslt_30 = newTerm(VAR, 0, portLoc(2, Rslt_30Args));
#ifdef STRICT
pushRedex(Rslt_30Args, newRef(glblProto56_2282));
#else
swap(termLoc(Rslt_30), makePair(LAZ, 0, Rslt_30Args, newRef(glblProto56_2282)));
#endif
// call  at /home/jim/toccata/interpreter/intrp-rdr.toc: 403
Term Rslt_31Args = makePair(APP, 0, Rslt_30, SUB);
Term Rslt_31 = newTerm(VAR, 0, portLoc(2, Rslt_31Args));
#ifdef STRICT
pushRedex(Rslt_31Args, seq_15);
#else
swap(termLoc(Rslt_31), makePair(LAZ, 0, Rslt_31Args, seq_15));
#endif

// link args to body
swap(r_seq_11, Rslt_31);

Term Rslt_31V = get(termLoc(Rslt_31));
apps = take(portLoc(1, Rslt_31V));
lams = take(portLoc(2, Rslt_31V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_31), makePair(LAZ, 0, apps, lams));
// call rest at /home/jim/toccata/interpreter/intrp-rdr.toc: 402
Term Rslt_32Args = makePair(APP, 0, exprs_1, SUB);
Term Rslt_32 = newTerm(VAR, 0, portLoc(2, Rslt_32Args));
#ifdef STRICT
pushRedex(Rslt_32Args, newRef(glblProto78_2284));
#else
swap(termLoc(Rslt_32), makePair(LAZ, 0, Rslt_32Args, newRef(glblProto78_2284)));
#endif
// call  at /home/jim/toccata/interpreter/intrp-rdr.toc: 402
Term Rslt_33Args = makePair(APP, 0, Rslt_32, SUB);
Term Rslt_33 = newTerm(VAR, 0, portLoc(2, Rslt_33Args));
#ifdef STRICT
pushRedex(Rslt_33Args, seq_11);
#else
swap(termLoc(Rslt_33), makePair(LAZ, 0, Rslt_33Args, seq_11));
#endif

// link args to body
swap(r_seq_7, Rslt_33);

Term Rslt_33V = get(termLoc(Rslt_33));
apps = take(portLoc(1, Rslt_33V));
lams = take(portLoc(2, Rslt_33V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_33), makePair(LAZ, 0, apps, lams));
// call first at /home/jim/toccata/interpreter/intrp-rdr.toc: 401
Term Rslt_34Args = makePair(APP, 0, exprs_1_1, SUB);
Term Rslt_34 = newTerm(VAR, 0, portLoc(2, Rslt_34Args));
#ifdef STRICT
pushRedex(Rslt_34Args, newRef(glblProto76_2286));
#else
swap(termLoc(Rslt_34), makePair(LAZ, 0, Rslt_34Args, newRef(glblProto76_2286)));
#endif
// call extract at /home/jim/toccata/interpreter/intrp-rdr.toc: 401
Term Rslt_35Args = makePair(APP, 0, Rslt_34, SUB);
Term Rslt_35 = newTerm(VAR, 0, portLoc(2, Rslt_35Args));
#ifdef STRICT
pushRedex(Rslt_35Args, newRef(glblProto56_2288));
#else
swap(termLoc(Rslt_35), makePair(LAZ, 0, Rslt_35Args, newRef(glblProto56_2288)));
#endif
// call  at /home/jim/toccata/interpreter/intrp-rdr.toc: 401
Term Rslt_36Args = makePair(APP, 0, Rslt_35, SUB);
Term Rslt_36 = newTerm(VAR, 0, portLoc(2, Rslt_36Args));
#ifdef STRICT
pushRedex(Rslt_36Args, seq_7);
#else
swap(termLoc(Rslt_36), makePair(LAZ, 0, Rslt_36Args, seq_7));
#endif

// link args to body
swap(r_seq_3, Rslt_36);

Term Rslt_36V = get(termLoc(Rslt_36));
apps = take(portLoc(1, Rslt_36V));
lams = take(portLoc(2, Rslt_36V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_36), makePair(LAZ, 0, apps, lams));
pushRedex(args, seq_3);
return;
}
Term glblFn1786 = newRef(glblCFn1785);
Term glblFn1788;
void glblCFn1787(Term ref, Term args) {
Term apps, lams, V;
Location vLoc;
Term seq_3 = makePair(LAM, 0, SUB, NUL);
Location r_seq_3 = portLoc(2, seq_3);
Term loc_2 = newTerm(VAR, 0, portLoc(1, seq_3));
seq_3 = makePair(LAM, 0, SUB, seq_3);
Term exprs_1 = newTerm(VAR, 0, portLoc(1, seq_3));
// allocate args at /home/jim/toccata/interpreter/intrp-rdr.toc: 409
// call cond-desugar-rec at /home/jim/toccata/interpreter/intrp-rdr.toc: 410
Term Rslt_5Args = makePair(APP, 0, loc_2, SUB);
Term Rslt_5 = newTerm(VAR, 0, portLoc(2, Rslt_5Args));
Rslt_5Args = makePair(APP, 0, exprs_1, Rslt_5Args);
#ifdef STRICT
pushRedex(Rslt_5Args, glblFn1786);
#else
swap(termLoc(Rslt_5), makePair(LAZ, 0, Rslt_5Args, glblFn1786));
#endif

// link args to body
swap(r_seq_3, Rslt_5);

Term Rslt_5V = get(termLoc(Rslt_5));
apps = take(portLoc(1, Rslt_5V));
lams = take(portLoc(2, Rslt_5V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_5), makePair(LAZ, 0, apps, lams));
pushRedex(args, seq_3);
return;
}
Term glblFn1788 = newRef(glblCFn1787);
void glblProto64_2293(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(414), args);
  args = makePair(APP, 0, glblStr1912, args);
  interact(args, glblProto64);
}
}
void glblProto20_2295(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(414), args);
  args = makePair(APP, 0, glblStr1912, args);
  interact(args, glblProto20);
}
}
void glblProto76_2297(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(415), args);
  args = makePair(APP, 0, glblStr1912, args);
  interact(args, glblProto76);
}
}
void glblProto56_2299(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(415), args);
  args = makePair(APP, 0, glblStr1912, args);
  interact(args, glblProto56);
}
}
void glblProto78_2301(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(418), args);
  args = makePair(APP, 0, glblStr1912, args);
  interact(args, glblProto78);
}
}
void glblProto76_2303(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(416), args);
  args = makePair(APP, 0, glblStr1912, args);
  interact(args, glblProto76);
}
}
void glblProto56_2305(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(416), args);
  args = makePair(APP, 0, glblStr1912, args);
  interact(args, glblProto56);
}
}
void glblProto34_2307(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(414), args);
  args = makePair(APP, 0, glblStr1912, args);
  interact(args, glblProto34);
}
}
Term glblFn1790;
void glblCFn1789(Term ref, Term args) {
Term apps, lams, V;
Location vLoc;
Term seq_4 = makePair(LAM, 0, SUB, NUL);
Location r_seq_4 = portLoc(2, seq_4);
Term op_name_3 = newTerm(VAR, 0, portLoc(1, seq_4));
seq_4 = makePair(LAM, 0, SUB, seq_4);
Term loc_2 = newTerm(VAR, 0, portLoc(1, seq_4));
seq_4 = makePair(LAM, 0, SUB, seq_4);
Term exprs_1 = newTerm(VAR, 0, portLoc(1, seq_4));
// allocate args at /home/jim/toccata/interpreter/intrp-rdr.toc: 412

Term exprs_1_3;
exprs_1 = dupeArg(exprs_1, &exprs_1_3, 318);

Term exprs_1_2;
exprs_1 = dupeArg(exprs_1, &exprs_1_2, 319);

Term exprs_1_1;
exprs_1 = dupeArg(exprs_1, &exprs_1_1, 320);

Term loc_2_1;
loc_2 = dupeArg(loc_2, &loc_2_1, 321);

Term op_name_3_1;
op_name_3 = dupeArg(op_name_3, &op_name_3_1, 322);
Term seq_12 = makePair(LAM, 0, SUB, NUL);
Location r_seq_12 = portLoc(2, seq_12);
Term a_11 = newTerm(VAR, 0, portLoc(1, seq_12));
// allocate args at /home/jim/toccata/interpreter/intrp-rdr.toc: 416
Term glblVal175_50;
glblVal175 = dupeArg(glblVal175, &glblVal175_50, 0);

Term glblVal1560_7;
glblVal1560 = dupeArg(glblVal1560, &glblVal1560_7, 0);

Term glblVal1591_5;
glblVal1591 = dupeArg(glblVal1591, &glblVal1591_5, 0);

// call count at /home/jim/toccata/interpreter/intrp-rdr.toc: 414
Term Rslt_6Args = makePair(APP, 0, exprs_1, SUB);
Term Rslt_6 = newTerm(VAR, 0, portLoc(2, Rslt_6Args));
#ifdef STRICT
pushRedex(Rslt_6Args, newRef(glblProto64_2293));
#else
swap(termLoc(Rslt_6), makePair(LAZ, 0, Rslt_6Args, newRef(glblProto64_2293)));
#endif
// call = at /home/jim/toccata/interpreter/intrp-rdr.toc: 414
Term Rslt_7Args = makePair(APP, 0, newI60(1), SUB);
Term Rslt_7 = newTerm(VAR, 0, portLoc(2, Rslt_7Args));
Rslt_7Args = makePair(APP, 0, Rslt_6, Rslt_7Args);
#ifdef STRICT
pushRedex(Rslt_7Args, newRef(glblProto20_2295));
#else
swap(termLoc(Rslt_7), makePair(LAZ, 0, Rslt_7Args, newRef(glblProto20_2295)));
#endif
// call first at /home/jim/toccata/interpreter/intrp-rdr.toc: 415
Term Rslt_8Args = makePair(APP, 0, exprs_1_1, SUB);
Term Rslt_8 = newTerm(VAR, 0, portLoc(2, Rslt_8Args));
#ifdef STRICT
pushRedex(Rslt_8Args, newRef(glblProto76_2297));
#else
swap(termLoc(Rslt_8), makePair(LAZ, 0, Rslt_8Args, newRef(glblProto76_2297)));
#endif
// call extract at /home/jim/toccata/interpreter/intrp-rdr.toc: 415
Term Rslt_9Args = makePair(APP, 0, Rslt_8, SUB);
Term Rslt_9 = newTerm(VAR, 0, portLoc(2, Rslt_9Args));
#ifdef STRICT
pushRedex(Rslt_9Args, newRef(glblProto56_2299));
#else
swap(termLoc(Rslt_9), makePair(LAZ, 0, Rslt_9Args, newRef(glblProto56_2299)));
#endif
// call ast/Symbol at /home/jim/toccata/interpreter/intrp-rdr.toc: 417
Term Rslt_14Args = makePair(APP, 0, loc_2, SUB);
Term Rslt_14 = newTerm(VAR, 0, portLoc(2, Rslt_14Args));
Rslt_14Args = makePair(APP, 0, op_name_3, Rslt_14Args);
Rslt_14Args = makePair(APP, 0, glblStr402, Rslt_14Args);
#ifdef STRICT
pushRedex(Rslt_14Args, glblVal1560_7);
#else
swap(termLoc(Rslt_14), makePair(LAZ, 0, Rslt_14Args, glblVal1560_7));
#endif

// call vect-conj at /home/jim/toccata/interpreter/intrp-rdr.toc: 418
Term Rslt_15Args = makePair(APP, 0, a_11, SUB);
Term Rslt_15 = newTerm(VAR, 0, portLoc(2, Rslt_15Args));
Rslt_15Args = makePair(APP, 0, glblVal175_50, Rslt_15Args);
#ifdef STRICT
pushRedex(Rslt_15Args, glblFn178);
#else
swap(termLoc(Rslt_15), makePair(LAZ, 0, Rslt_15Args, glblFn178));
#endif

// call rest at /home/jim/toccata/interpreter/intrp-rdr.toc: 418
Term Rslt_16Args = makePair(APP, 0, exprs_1_2, SUB);
Term Rslt_16 = newTerm(VAR, 0, portLoc(2, Rslt_16Args));
#ifdef STRICT
pushRedex(Rslt_16Args, newRef(glblProto78_2301));
#else
swap(termLoc(Rslt_16), makePair(LAZ, 0, Rslt_16Args, newRef(glblProto78_2301)));
#endif
// call and-or-desugar-rec at /home/jim/toccata/interpreter/intrp-rdr.toc: 412
Term Rslt_17Args = makePair(APP, 0, op_name_3_1, SUB);
Term Rslt_17 = newTerm(VAR, 0, portLoc(2, Rslt_17Args));
Rslt_17Args = makePair(APP, 0, loc_2_1, Rslt_17Args);
Rslt_17Args = makePair(APP, 0, Rslt_16, Rslt_17Args);
#ifdef STRICT
pushRedex(Rslt_17Args, glblFn1790);
#else
swap(termLoc(Rslt_17), makePair(LAZ, 0, Rslt_17Args, glblFn1790));
#endif

// call vect-conj at /home/jim/toccata/interpreter/intrp-rdr.toc: 418
Term Rslt_18Args = makePair(APP, 0, Rslt_17, SUB);
Term Rslt_18 = newTerm(VAR, 0, portLoc(2, Rslt_18Args));
Rslt_18Args = makePair(APP, 0, Rslt_15, Rslt_18Args);
#ifdef STRICT
pushRedex(Rslt_18Args, glblFn178);
#else
swap(termLoc(Rslt_18), makePair(LAZ, 0, Rslt_18Args, glblFn178));
#endif

// call ast/Call at /home/jim/toccata/interpreter/intrp-rdr.toc: 417
Term Rslt_19Args = makePair(APP, 0, Rslt_18, SUB);
Term Rslt_19 = newTerm(VAR, 0, portLoc(2, Rslt_19Args));
Rslt_19Args = makePair(APP, 0, Rslt_14, Rslt_19Args);
#ifdef STRICT
pushRedex(Rslt_19Args, glblVal1591_5);
#else
swap(termLoc(Rslt_19), makePair(LAZ, 0, Rslt_19Args, glblVal1591_5));
#endif

// link args to body
swap(r_seq_12, Rslt_19);

Term Rslt_19V = get(termLoc(Rslt_19));
apps = take(portLoc(1, Rslt_19V));
lams = take(portLoc(2, Rslt_19V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_19), makePair(LAZ, 0, apps, lams));
// call first at /home/jim/toccata/interpreter/intrp-rdr.toc: 416
Term Rslt_20Args = makePair(APP, 0, exprs_1_3, SUB);
Term Rslt_20 = newTerm(VAR, 0, portLoc(2, Rslt_20Args));
#ifdef STRICT
pushRedex(Rslt_20Args, newRef(glblProto76_2303));
#else
swap(termLoc(Rslt_20), makePair(LAZ, 0, Rslt_20Args, newRef(glblProto76_2303)));
#endif
// call extract at /home/jim/toccata/interpreter/intrp-rdr.toc: 416
Term Rslt_21Args = makePair(APP, 0, Rslt_20, SUB);
Term Rslt_21 = newTerm(VAR, 0, portLoc(2, Rslt_21Args));
#ifdef STRICT
pushRedex(Rslt_21Args, newRef(glblProto56_2305));
#else
swap(termLoc(Rslt_21), makePair(LAZ, 0, Rslt_21Args, newRef(glblProto56_2305)));
#endif
// call  at /home/jim/toccata/interpreter/intrp-rdr.toc: 416
Term Rslt_22Args = makePair(APP, 0, Rslt_21, SUB);
Term Rslt_22 = newTerm(VAR, 0, portLoc(2, Rslt_22Args));
#ifdef STRICT
pushRedex(Rslt_22Args, seq_12);
#else
swap(termLoc(Rslt_22), makePair(LAZ, 0, Rslt_22Args, seq_12));
#endif

// call cond at /home/jim/toccata/interpreter/intrp-rdr.toc: 414
Term Rslt_23Args = makePair(APP, 0, Rslt_22, SUB);
Term Rslt_23 = newTerm(VAR, 0, portLoc(2, Rslt_23Args));
Rslt_23Args = makePair(APP, 0, Rslt_9, Rslt_23Args);
Rslt_23Args = makePair(APP, 0, Rslt_7, Rslt_23Args);
#ifdef STRICT
pushRedex(Rslt_23Args, newRef(glblProto34_2307));
#else
swap(termLoc(Rslt_23), makePair(LAZ, 0, Rslt_23Args, newRef(glblProto34_2307)));
#endif
// link args to body
swap(r_seq_4, Rslt_23);

Term Rslt_23V = get(termLoc(Rslt_23));
apps = take(portLoc(1, Rslt_23V));
lams = take(portLoc(2, Rslt_23V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_23), makePair(LAZ, 0, apps, lams));
pushRedex(args, seq_4);
return;
}
Term glblFn1790 = newRef(glblCFn1789);
Term glblFn1792;
void glblCFn1791(Term ref, Term args) {
Term apps, lams, V;
Location vLoc;
Term seq_4 = makePair(LAM, 0, SUB, NUL);
Location r_seq_4 = portLoc(2, seq_4);
Term op_name_3 = newTerm(VAR, 0, portLoc(1, seq_4));
seq_4 = makePair(LAM, 0, SUB, seq_4);
Term loc_2 = newTerm(VAR, 0, portLoc(1, seq_4));
seq_4 = makePair(LAM, 0, SUB, seq_4);
Term exprs_1 = newTerm(VAR, 0, portLoc(1, seq_4));
// allocate args at /home/jim/toccata/interpreter/intrp-rdr.toc: 421
// call and-or-desugar-rec at /home/jim/toccata/interpreter/intrp-rdr.toc: 422
Term Rslt_6Args = makePair(APP, 0, op_name_3, SUB);
Term Rslt_6 = newTerm(VAR, 0, portLoc(2, Rslt_6Args));
Rslt_6Args = makePair(APP, 0, loc_2, Rslt_6Args);
Rslt_6Args = makePair(APP, 0, exprs_1, Rslt_6Args);
#ifdef STRICT
pushRedex(Rslt_6Args, glblFn1790);
#else
swap(termLoc(Rslt_6), makePair(LAZ, 0, Rslt_6Args, glblFn1790));
#endif

// link args to body
swap(r_seq_4, Rslt_6);

Term Rslt_6V = get(termLoc(Rslt_6));
apps = take(portLoc(1, Rslt_6V));
lams = take(portLoc(2, Rslt_6V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_6), makePair(LAZ, 0, apps, lams));
pushRedex(args, seq_4);
return;
}
Term glblFn1792 = newRef(glblCFn1791);
Term glblFn1794;
void glblCFn1793(Term ref, Term args) {
Term apps, lams, V;
Location vLoc;
Term seq_4 = makePair(LAM, 0, SUB, NUL);
Location r_seq_4 = portLoc(2, seq_4);
Term fn_name_3 = newTerm(VAR, 0, portLoc(1, seq_4));
seq_4 = makePair(LAM, 0, SUB, seq_4);
Term loc_2 = newTerm(VAR, 0, portLoc(1, seq_4));
seq_4 = makePair(LAM, 0, SUB, seq_4);
Term exprs_1 = newTerm(VAR, 0, portLoc(1, seq_4));
// allocate args at /home/jim/toccata/interpreter/intrp-rdr.toc: 425

Term loc_2_1;
loc_2 = dupeArg(loc_2, &loc_2_1, 323);
Term glblVal175_51;
glblVal175 = dupeArg(glblVal175, &glblVal175_51, 0);

Term glblVal1560_8;
glblVal1560 = dupeArg(glblVal1560, &glblVal1560_8, 0);

Term glblVal1591_6;
glblVal1591 = dupeArg(glblVal1591, &glblVal1591_6, 0);

// call ast/Symbol at /home/jim/toccata/interpreter/intrp-rdr.toc: 426
Term Rslt_6Args = makePair(APP, 0, loc_2, SUB);
Term Rslt_6 = newTerm(VAR, 0, portLoc(2, Rslt_6Args));
Rslt_6Args = makePair(APP, 0, fn_name_3, Rslt_6Args);
Rslt_6Args = makePair(APP, 0, glblStr402, Rslt_6Args);
#ifdef STRICT
pushRedex(Rslt_6Args, glblVal1560_8);
#else
swap(termLoc(Rslt_6), makePair(LAZ, 0, Rslt_6Args, glblVal1560_8));
#endif

// call fold-vec at /home/jim/toccata/interpreter/intrp-rdr.toc: 426
Term Rslt_7Args = makePair(APP, 0, loc_2_1, SUB);
Term Rslt_7 = newTerm(VAR, 0, portLoc(2, Rslt_7Args));
Rslt_7Args = makePair(APP, 0, exprs_1, Rslt_7Args);
#ifdef STRICT
pushRedex(Rslt_7Args, glblFn1778);
#else
swap(termLoc(Rslt_7), makePair(LAZ, 0, Rslt_7Args, glblFn1778));
#endif

// call vect-conj at /home/jim/toccata/interpreter/intrp-rdr.toc: 426
Term Rslt_8Args = makePair(APP, 0, Rslt_7, SUB);
Term Rslt_8 = newTerm(VAR, 0, portLoc(2, Rslt_8Args));
Rslt_8Args = makePair(APP, 0, glblVal175_51, Rslt_8Args);
#ifdef STRICT
pushRedex(Rslt_8Args, glblFn178);
#else
swap(termLoc(Rslt_8), makePair(LAZ, 0, Rslt_8Args, glblFn178));
#endif

// call ast/Call at /home/jim/toccata/interpreter/intrp-rdr.toc: 426
Term Rslt_9Args = makePair(APP, 0, Rslt_8, SUB);
Term Rslt_9 = newTerm(VAR, 0, portLoc(2, Rslt_9Args));
Rslt_9Args = makePair(APP, 0, Rslt_6, Rslt_9Args);
#ifdef STRICT
pushRedex(Rslt_9Args, glblVal1591_6);
#else
swap(termLoc(Rslt_9), makePair(LAZ, 0, Rslt_9Args, glblVal1591_6));
#endif

// link args to body
swap(r_seq_4, Rslt_9);

Term Rslt_9V = get(termLoc(Rslt_9));
apps = take(portLoc(1, Rslt_9V));
lams = take(portLoc(2, Rslt_9V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_9), makePair(LAZ, 0, apps, lams));
pushRedex(args, seq_4);
return;
}
Term glblFn1794 = newRef(glblCFn1793);
void glblProto20_2314(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(436), args);
  args = makePair(APP, 0, glblStr1912, args);
  interact(args, glblProto20);
}
}
void glblProto1582_2316(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(437), args);
  args = makePair(APP, 0, glblStr1912, args);
  interact(args, glblProto1582);
}
}
void glblProto20_2318(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(440), args);
  args = makePair(APP, 0, glblStr1912, args);
  interact(args, glblProto20);
}
}
void glblProto380_2320(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(443), args);
  args = makePair(APP, 0, glblStr1912, args);
  interact(args, glblProto380);
}
}
void glblProto64_2322(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(444), args);
  args = makePair(APP, 0, glblStr1912, args);
  interact(args, glblProto64);
}
}
void glblProto380_2324(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(444), args);
  args = makePair(APP, 0, glblStr1912, args);
  interact(args, glblProto380);
}
}
void glblProto34_2326(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(443), args);
  args = makePair(APP, 0, glblStr1912, args);
  interact(args, glblProto34);
}
}
void glblProto1510_2328(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(441), args);
  args = makePair(APP, 0, glblStr1912, args);
  interact(args, glblProto1510);
}
}
void glblProto34_2330(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(440), args);
  args = makePair(APP, 0, glblStr1912, args);
  interact(args, glblProto34);
}
}
void glblProto34_2332(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(436), args);
  args = makePair(APP, 0, glblStr1912, args);
  interact(args, glblProto34);
}
}
void glblProto1759_2334(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(434), args);
  args = makePair(APP, 0, glblStr1912, args);
  interact(args, glblProto1759);
}
}
Term glblFn1796;
void glblCFn1795(Term ref, Term args) {
Term apps, lams, V;
Location vLoc;
Term seq_4 = makePair(LAM, 0, SUB, NUL);
Location r_seq_4 = portLoc(2, seq_4);
Term loc_3 = newTerm(VAR, 0, portLoc(1, seq_4));
seq_4 = makePair(LAM, 0, SUB, seq_4);
Term threaded_2 = newTerm(VAR, 0, portLoc(1, seq_4));
seq_4 = makePair(LAM, 0, SUB, seq_4);
Term step_1 = newTerm(VAR, 0, portLoc(1, seq_4));
// allocate args at /home/jim/toccata/interpreter/intrp-rdr.toc: 433

Term step_1_4;
step_1 = dupeArg(step_1, &step_1_4, 324);

Term step_1_3;
step_1 = dupeArg(step_1, &step_1_3, 325);

Term step_1_2;
step_1 = dupeArg(step_1, &step_1_2, 326);

Term step_1_1;
step_1 = dupeArg(step_1, &step_1_1, 327);

Term threaded_2_2;
threaded_2 = dupeArg(threaded_2, &threaded_2_2, 328);

Term threaded_2_1;
threaded_2 = dupeArg(threaded_2, &threaded_2_1, 329);
Term seq_8 = makePair(LAM, 0, SUB, NUL);
Location r_seq_8 = portLoc(2, seq_8);
Term k_7 = newTerm(VAR, 0, portLoc(1, seq_8));
// allocate args at /home/jim/toccata/interpreter/intrp-rdr.toc: 434

Term k_7_1;
k_7 = dupeArg(k_7, &k_7_1, 0);
Term glblVal175_54;
glblVal175 = dupeArg(glblVal175, &glblVal175_54, 0);

Term glblVal1591_10;
glblVal1591 = dupeArg(glblVal1591, &glblVal1591_10, 0);

Term seq_24 = makePair(LAM, 0, SUB, NUL);
Location r_seq_24 = portLoc(2, seq_24);
Term nm_23 = newTerm(VAR, 0, portLoc(1, seq_24));
// allocate args at /home/jim/toccata/interpreter/intrp-rdr.toc: 441

Term nm_23_2;
nm_23 = dupeArg(nm_23, &nm_23_2, 0);

Term nm_23_1;
nm_23 = dupeArg(nm_23, &nm_23_1, 0);
Term glblVal175_53;
glblVal175 = dupeArg(glblVal175, &glblVal175_53, 0);

Term glblVal1591_9;
glblVal1591 = dupeArg(glblVal1591, &glblVal1591_9, 0);

Term glblVal175_52;
glblVal175 = dupeArg(glblVal175, &glblVal175_52, 0);

Term glblVal1629_1;
glblVal1629 = dupeArg(glblVal1629, &glblVal1629_1, 0);

Term glblVal1591_8;
glblVal1591 = dupeArg(glblVal1591, &glblVal1591_8, 0);

Term seq_13 = makePair(LAM, 0, SUB, NUL);
Location r_seq_13 = portLoc(2, seq_13);
Term op_12 = newTerm(VAR, 0, portLoc(1, seq_13));
// allocate args at /home/jim/toccata/interpreter/intrp-rdr.toc: 437
Term glblVal1591_7;
glblVal1591 = dupeArg(glblVal1591, &glblVal1591_7, 0);

// call = at /home/jim/toccata/interpreter/intrp-rdr.toc: 436
Term Rslt_10Args = makePair(APP, 0, newI60(0), SUB);
Term Rslt_10 = newTerm(VAR, 0, portLoc(2, Rslt_10Args));
Rslt_10Args = makePair(APP, 0, k_7, Rslt_10Args);
#ifdef STRICT
pushRedex(Rslt_10Args, newRef(glblProto20_2314));
#else
swap(termLoc(Rslt_10), makePair(LAZ, 0, Rslt_10Args, newRef(glblProto20_2314)));
#endif
// call ast/Call at /home/jim/toccata/interpreter/intrp-rdr.toc: 438
Term Rslt_18Args = makePair(APP, 0, op_12, SUB);
Term Rslt_18 = newTerm(VAR, 0, portLoc(2, Rslt_18Args));
#ifdef STRICT
pushRedex(Rslt_18Args, glblVal1591_7);
#else
swap(termLoc(Rslt_18), makePair(LAZ, 0, Rslt_18Args, glblVal1591_7));
#endif

// link args to body
swap(r_seq_13, Rslt_18);

Term Rslt_18V = get(termLoc(Rslt_18));
apps = take(portLoc(1, Rslt_18V));
lams = take(portLoc(2, Rslt_18V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_18), makePair(LAZ, 0, apps, lams));
// call .operator at /home/jim/toccata/interpreter/intrp-rdr.toc: 437
Term Rslt_19Args = makePair(APP, 0, step_1, SUB);
Term Rslt_19 = newTerm(VAR, 0, portLoc(2, Rslt_19Args));
#ifdef STRICT
pushRedex(Rslt_19Args, newRef(glblProto1582_2316));
#else
swap(termLoc(Rslt_19), makePair(LAZ, 0, Rslt_19Args, newRef(glblProto1582_2316)));
#endif
// call  at /home/jim/toccata/interpreter/intrp-rdr.toc: 437
Term Rslt_20Args = makePair(APP, 0, Rslt_19, SUB);
Term Rslt_20 = newTerm(VAR, 0, portLoc(2, Rslt_20Args));
#ifdef STRICT
pushRedex(Rslt_20Args, seq_13);
#else
swap(termLoc(Rslt_20), makePair(LAZ, 0, Rslt_20Args, seq_13));
#endif

// call = at /home/jim/toccata/interpreter/intrp-rdr.toc: 440
Term Rslt_21Args = makePair(APP, 0, newI60(1), SUB);
Term Rslt_21 = newTerm(VAR, 0, portLoc(2, Rslt_21Args));
Rslt_21Args = makePair(APP, 0, k_7_1, Rslt_21Args);
#ifdef STRICT
pushRedex(Rslt_21Args, newRef(glblProto20_2318));
#else
swap(termLoc(Rslt_21), makePair(LAZ, 0, Rslt_21Args, newRef(glblProto20_2318)));
#endif
// call subs at /home/jim/toccata/interpreter/intrp-rdr.toc: 443
Term Rslt_26Args = makePair(APP, 0, newI60(1), SUB);
Term Rslt_26 = newTerm(VAR, 0, portLoc(2, Rslt_26Args));
Rslt_26Args = makePair(APP, 0, newI60(0), Rslt_26Args);
Rslt_26Args = makePair(APP, 0, nm_23, Rslt_26Args);
#ifdef STRICT
pushRedex(Rslt_26Args, newRef(glblProto380_2320));
#else
swap(termLoc(Rslt_26), makePair(LAZ, 0, Rslt_26Args, newRef(glblProto380_2320)));
#endif
// call str= at /home/jim/toccata/interpreter/intrp-rdr.toc: 443
Term Rslt_27Args = makePair(APP, 0, glblStr1933, SUB);
Term Rslt_27 = newTerm(VAR, 0, portLoc(2, Rslt_27Args));
Rslt_27Args = makePair(APP, 0, Rslt_26, Rslt_27Args);
#ifdef STRICT
pushRedex(Rslt_27Args, glblFn363);
#else
swap(termLoc(Rslt_27), makePair(LAZ, 0, Rslt_27Args, glblFn363));
#endif

// call count at /home/jim/toccata/interpreter/intrp-rdr.toc: 444
Term Rslt_28Args = makePair(APP, 0, nm_23_2, SUB);
Term Rslt_28 = newTerm(VAR, 0, portLoc(2, Rslt_28Args));
#ifdef STRICT
pushRedex(Rslt_28Args, newRef(glblProto64_2322));
#else
swap(termLoc(Rslt_28), makePair(LAZ, 0, Rslt_28Args, newRef(glblProto64_2322)));
#endif
// call subs at /home/jim/toccata/interpreter/intrp-rdr.toc: 444
Term Rslt_29Args = makePair(APP, 0, Rslt_28, SUB);
Term Rslt_29 = newTerm(VAR, 0, portLoc(2, Rslt_29Args));
Rslt_29Args = makePair(APP, 0, newI60(1), Rslt_29Args);
Rslt_29Args = makePair(APP, 0, nm_23_1, Rslt_29Args);
#ifdef STRICT
pushRedex(Rslt_29Args, newRef(glblProto380_2324));
#else
swap(termLoc(Rslt_29), makePair(LAZ, 0, Rslt_29Args, newRef(glblProto380_2324)));
#endif
// call ast/FieldGetter at /home/jim/toccata/interpreter/intrp-rdr.toc: 444
Term Rslt_30Args = makePair(APP, 0, loc_3, SUB);
Term Rslt_30 = newTerm(VAR, 0, portLoc(2, Rslt_30Args));
Rslt_30Args = makePair(APP, 0, Rslt_29, Rslt_30Args);
#ifdef STRICT
pushRedex(Rslt_30Args, glblVal1629_1);
#else
swap(termLoc(Rslt_30), makePair(LAZ, 0, Rslt_30Args, glblVal1629_1));
#endif

// call vect-conj at /home/jim/toccata/interpreter/intrp-rdr.toc: 444
Term Rslt_31Args = makePair(APP, 0, threaded_2, SUB);
Term Rslt_31 = newTerm(VAR, 0, portLoc(2, Rslt_31Args));
Rslt_31Args = makePair(APP, 0, glblVal175_52, Rslt_31Args);
#ifdef STRICT
pushRedex(Rslt_31Args, glblFn178);
#else
swap(termLoc(Rslt_31), makePair(LAZ, 0, Rslt_31Args, glblFn178));
#endif

// call ast/Call at /home/jim/toccata/interpreter/intrp-rdr.toc: 444
Term Rslt_32Args = makePair(APP, 0, Rslt_31, SUB);
Term Rslt_32 = newTerm(VAR, 0, portLoc(2, Rslt_32Args));
Rslt_32Args = makePair(APP, 0, Rslt_30, Rslt_32Args);
#ifdef STRICT
pushRedex(Rslt_32Args, glblVal1591_8);
#else
swap(termLoc(Rslt_32), makePair(LAZ, 0, Rslt_32Args, glblVal1591_8));
#endif

// call vect-conj at /home/jim/toccata/interpreter/intrp-rdr.toc: 445
Term Rslt_33Args = makePair(APP, 0, threaded_2_1, SUB);
Term Rslt_33 = newTerm(VAR, 0, portLoc(2, Rslt_33Args));
Rslt_33Args = makePair(APP, 0, glblVal175_53, Rslt_33Args);
#ifdef STRICT
pushRedex(Rslt_33Args, glblFn178);
#else
swap(termLoc(Rslt_33), makePair(LAZ, 0, Rslt_33Args, glblFn178));
#endif

// call ast/Call at /home/jim/toccata/interpreter/intrp-rdr.toc: 445
Term Rslt_34Args = makePair(APP, 0, Rslt_33, SUB);
Term Rslt_34 = newTerm(VAR, 0, portLoc(2, Rslt_34Args));
Rslt_34Args = makePair(APP, 0, step_1_1, Rslt_34Args);
#ifdef STRICT
pushRedex(Rslt_34Args, glblVal1591_9);
#else
swap(termLoc(Rslt_34), makePair(LAZ, 0, Rslt_34Args, glblVal1591_9));
#endif

// call cond at /home/jim/toccata/interpreter/intrp-rdr.toc: 443
Term Rslt_35Args = makePair(APP, 0, Rslt_34, SUB);
Term Rslt_35 = newTerm(VAR, 0, portLoc(2, Rslt_35Args));
Rslt_35Args = makePair(APP, 0, Rslt_32, Rslt_35Args);
Rslt_35Args = makePair(APP, 0, Rslt_27, Rslt_35Args);
#ifdef STRICT
pushRedex(Rslt_35Args, newRef(glblProto34_2326));
#else
swap(termLoc(Rslt_35), makePair(LAZ, 0, Rslt_35Args, newRef(glblProto34_2326)));
#endif
// link args to body
swap(r_seq_24, Rslt_35);

Term Rslt_35V = get(termLoc(Rslt_35));
apps = take(portLoc(1, Rslt_35V));
lams = take(portLoc(2, Rslt_35V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_35), makePair(LAZ, 0, apps, lams));
// call .name at /home/jim/toccata/interpreter/intrp-rdr.toc: 441
Term Rslt_36Args = makePair(APP, 0, step_1_2, SUB);
Term Rslt_36 = newTerm(VAR, 0, portLoc(2, Rslt_36Args));
#ifdef STRICT
pushRedex(Rslt_36Args, newRef(glblProto1510_2328));
#else
swap(termLoc(Rslt_36), makePair(LAZ, 0, Rslt_36Args, newRef(glblProto1510_2328)));
#endif
// call  at /home/jim/toccata/interpreter/intrp-rdr.toc: 441
Term Rslt_37Args = makePair(APP, 0, Rslt_36, SUB);
Term Rslt_37 = newTerm(VAR, 0, portLoc(2, Rslt_37Args));
#ifdef STRICT
pushRedex(Rslt_37Args, seq_24);
#else
swap(termLoc(Rslt_37), makePair(LAZ, 0, Rslt_37Args, seq_24));
#endif

// call vect-conj at /home/jim/toccata/interpreter/intrp-rdr.toc: 446
Term Rslt_38Args = makePair(APP, 0, threaded_2_2, SUB);
Term Rslt_38 = newTerm(VAR, 0, portLoc(2, Rslt_38Args));
Rslt_38Args = makePair(APP, 0, glblVal175_54, Rslt_38Args);
#ifdef STRICT
pushRedex(Rslt_38Args, glblFn178);
#else
swap(termLoc(Rslt_38), makePair(LAZ, 0, Rslt_38Args, glblFn178));
#endif

// call ast/Call at /home/jim/toccata/interpreter/intrp-rdr.toc: 446
Term Rslt_39Args = makePair(APP, 0, Rslt_38, SUB);
Term Rslt_39 = newTerm(VAR, 0, portLoc(2, Rslt_39Args));
Rslt_39Args = makePair(APP, 0, step_1_3, Rslt_39Args);
#ifdef STRICT
pushRedex(Rslt_39Args, glblVal1591_10);
#else
swap(termLoc(Rslt_39), makePair(LAZ, 0, Rslt_39Args, glblVal1591_10));
#endif

// call cond at /home/jim/toccata/interpreter/intrp-rdr.toc: 440
Term Rslt_40Args = makePair(APP, 0, Rslt_39, SUB);
Term Rslt_40 = newTerm(VAR, 0, portLoc(2, Rslt_40Args));
Rslt_40Args = makePair(APP, 0, Rslt_37, Rslt_40Args);
Rslt_40Args = makePair(APP, 0, Rslt_21, Rslt_40Args);
#ifdef STRICT
pushRedex(Rslt_40Args, newRef(glblProto34_2330));
#else
swap(termLoc(Rslt_40), makePair(LAZ, 0, Rslt_40Args, newRef(glblProto34_2330)));
#endif
// call cond at /home/jim/toccata/interpreter/intrp-rdr.toc: 436
Term Rslt_41Args = makePair(APP, 0, Rslt_40, SUB);
Term Rslt_41 = newTerm(VAR, 0, portLoc(2, Rslt_41Args));
Rslt_41Args = makePair(APP, 0, Rslt_20, Rslt_41Args);
Rslt_41Args = makePair(APP, 0, Rslt_10, Rslt_41Args);
#ifdef STRICT
pushRedex(Rslt_41Args, newRef(glblProto34_2332));
#else
swap(termLoc(Rslt_41), makePair(LAZ, 0, Rslt_41Args, newRef(glblProto34_2332)));
#endif
// link args to body
swap(r_seq_8, Rslt_41);

Term Rslt_41V = get(termLoc(Rslt_41));
apps = take(portLoc(1, Rslt_41V));
lams = take(portLoc(2, Rslt_41V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_41), makePair(LAZ, 0, apps, lams));
// call expr-kind at /home/jim/toccata/interpreter/intrp-rdr.toc: 434
Term Rslt_42Args = makePair(APP, 0, step_1_4, SUB);
Term Rslt_42 = newTerm(VAR, 0, portLoc(2, Rslt_42Args));
#ifdef STRICT
pushRedex(Rslt_42Args, newRef(glblProto1759_2334));
#else
swap(termLoc(Rslt_42), makePair(LAZ, 0, Rslt_42Args, newRef(glblProto1759_2334)));
#endif
// call  at /home/jim/toccata/interpreter/intrp-rdr.toc: 434
Term Rslt_43Args = makePair(APP, 0, Rslt_42, SUB);
Term Rslt_43 = newTerm(VAR, 0, portLoc(2, Rslt_43Args));
#ifdef STRICT
pushRedex(Rslt_43Args, seq_8);
#else
swap(termLoc(Rslt_43), makePair(LAZ, 0, Rslt_43Args, seq_8));
#endif

// link args to body
swap(r_seq_4, Rslt_43);

Term Rslt_43V = get(termLoc(Rslt_43));
apps = take(portLoc(1, Rslt_43V));
lams = take(portLoc(2, Rslt_43V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_43), makePair(LAZ, 0, apps, lams));
pushRedex(args, seq_4);
return;
}
Term glblFn1796 = newRef(glblCFn1795);
void glblProto64_2337(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(450), args);
  args = makePair(APP, 0, glblStr1912, args);
  interact(args, glblProto64);
}
}
void glblProto20_2339(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(450), args);
  args = makePair(APP, 0, glblStr1912, args);
  interact(args, glblProto20);
}
}
void glblProto78_2341(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(453), args);
  args = makePair(APP, 0, glblStr1912, args);
  interact(args, glblProto78);
}
}
void glblProto76_2343(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(452), args);
  args = makePair(APP, 0, glblStr1912, args);
  interact(args, glblProto76);
}
}
void glblProto56_2345(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(452), args);
  args = makePair(APP, 0, glblStr1912, args);
  interact(args, glblProto56);
}
}
void glblProto34_2347(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(450), args);
  args = makePair(APP, 0, glblStr1912, args);
  interact(args, glblProto34);
}
}
Term glblFn1798;
void glblCFn1797(Term ref, Term args) {
Term apps, lams, V;
Location vLoc;
Term seq_4 = makePair(LAM, 0, SUB, NUL);
Location r_seq_4 = portLoc(2, seq_4);
Term loc_3 = newTerm(VAR, 0, portLoc(1, seq_4));
seq_4 = makePair(LAM, 0, SUB, seq_4);
Term threaded_2 = newTerm(VAR, 0, portLoc(1, seq_4));
seq_4 = makePair(LAM, 0, SUB, seq_4);
Term steps_1 = newTerm(VAR, 0, portLoc(1, seq_4));
// allocate args at /home/jim/toccata/interpreter/intrp-rdr.toc: 448

Term steps_1_2;
steps_1 = dupeArg(steps_1, &steps_1_2, 330);

Term steps_1_1;
steps_1 = dupeArg(steps_1, &steps_1_1, 331);

Term threaded_2_1;
threaded_2 = dupeArg(threaded_2, &threaded_2_1, 332);

Term loc_3_1;
loc_3 = dupeArg(loc_3, &loc_3_1, 333);
Term seq_10 = makePair(LAM, 0, SUB, NUL);
Location r_seq_10 = portLoc(2, seq_10);
Term step_9 = newTerm(VAR, 0, portLoc(1, seq_10));
// allocate args at /home/jim/toccata/interpreter/intrp-rdr.toc: 452
// call count at /home/jim/toccata/interpreter/intrp-rdr.toc: 450
Term Rslt_6Args = makePair(APP, 0, steps_1, SUB);
Term Rslt_6 = newTerm(VAR, 0, portLoc(2, Rslt_6Args));
#ifdef STRICT
pushRedex(Rslt_6Args, newRef(glblProto64_2337));
#else
swap(termLoc(Rslt_6), makePair(LAZ, 0, Rslt_6Args, newRef(glblProto64_2337)));
#endif
// call = at /home/jim/toccata/interpreter/intrp-rdr.toc: 450
Term Rslt_7Args = makePair(APP, 0, newI60(0), SUB);
Term Rslt_7 = newTerm(VAR, 0, portLoc(2, Rslt_7Args));
Rslt_7Args = makePair(APP, 0, Rslt_6, Rslt_7Args);
#ifdef STRICT
pushRedex(Rslt_7Args, newRef(glblProto20_2339));
#else
swap(termLoc(Rslt_7), makePair(LAZ, 0, Rslt_7Args, newRef(glblProto20_2339)));
#endif
// call rest at /home/jim/toccata/interpreter/intrp-rdr.toc: 453
Term Rslt_12Args = makePair(APP, 0, steps_1_1, SUB);
Term Rslt_12 = newTerm(VAR, 0, portLoc(2, Rslt_12Args));
#ifdef STRICT
pushRedex(Rslt_12Args, newRef(glblProto78_2341));
#else
swap(termLoc(Rslt_12), makePair(LAZ, 0, Rslt_12Args, newRef(glblProto78_2341)));
#endif
// call thread-step at /home/jim/toccata/interpreter/intrp-rdr.toc: 453
Term Rslt_13Args = makePair(APP, 0, loc_3, SUB);
Term Rslt_13 = newTerm(VAR, 0, portLoc(2, Rslt_13Args));
Rslt_13Args = makePair(APP, 0, threaded_2_1, Rslt_13Args);
Rslt_13Args = makePair(APP, 0, step_9, Rslt_13Args);
#ifdef STRICT
pushRedex(Rslt_13Args, glblFn1796);
#else
swap(termLoc(Rslt_13), makePair(LAZ, 0, Rslt_13Args, glblFn1796));
#endif

// call threading-rec at /home/jim/toccata/interpreter/intrp-rdr.toc: 448
Term Rslt_14Args = makePair(APP, 0, loc_3_1, SUB);
Term Rslt_14 = newTerm(VAR, 0, portLoc(2, Rslt_14Args));
Rslt_14Args = makePair(APP, 0, Rslt_13, Rslt_14Args);
Rslt_14Args = makePair(APP, 0, Rslt_12, Rslt_14Args);
#ifdef STRICT
pushRedex(Rslt_14Args, glblFn1798);
#else
swap(termLoc(Rslt_14), makePair(LAZ, 0, Rslt_14Args, glblFn1798));
#endif

// link args to body
swap(r_seq_10, Rslt_14);

Term Rslt_14V = get(termLoc(Rslt_14));
apps = take(portLoc(1, Rslt_14V));
lams = take(portLoc(2, Rslt_14V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_14), makePair(LAZ, 0, apps, lams));
// call first at /home/jim/toccata/interpreter/intrp-rdr.toc: 452
Term Rslt_15Args = makePair(APP, 0, steps_1_2, SUB);
Term Rslt_15 = newTerm(VAR, 0, portLoc(2, Rslt_15Args));
#ifdef STRICT
pushRedex(Rslt_15Args, newRef(glblProto76_2343));
#else
swap(termLoc(Rslt_15), makePair(LAZ, 0, Rslt_15Args, newRef(glblProto76_2343)));
#endif
// call extract at /home/jim/toccata/interpreter/intrp-rdr.toc: 452
Term Rslt_16Args = makePair(APP, 0, Rslt_15, SUB);
Term Rslt_16 = newTerm(VAR, 0, portLoc(2, Rslt_16Args));
#ifdef STRICT
pushRedex(Rslt_16Args, newRef(glblProto56_2345));
#else
swap(termLoc(Rslt_16), makePair(LAZ, 0, Rslt_16Args, newRef(glblProto56_2345)));
#endif
// call  at /home/jim/toccata/interpreter/intrp-rdr.toc: 452
Term Rslt_17Args = makePair(APP, 0, Rslt_16, SUB);
Term Rslt_17 = newTerm(VAR, 0, portLoc(2, Rslt_17Args));
#ifdef STRICT
pushRedex(Rslt_17Args, seq_10);
#else
swap(termLoc(Rslt_17), makePair(LAZ, 0, Rslt_17Args, seq_10));
#endif

// call cond at /home/jim/toccata/interpreter/intrp-rdr.toc: 450
Term Rslt_18Args = makePair(APP, 0, Rslt_17, SUB);
Term Rslt_18 = newTerm(VAR, 0, portLoc(2, Rslt_18Args));
Rslt_18Args = makePair(APP, 0, threaded_2, Rslt_18Args);
Rslt_18Args = makePair(APP, 0, Rslt_7, Rslt_18Args);
#ifdef STRICT
pushRedex(Rslt_18Args, newRef(glblProto34_2347));
#else
swap(termLoc(Rslt_18), makePair(LAZ, 0, Rslt_18Args, newRef(glblProto34_2347)));
#endif
// link args to body
swap(r_seq_4, Rslt_18);

Term Rslt_18V = get(termLoc(Rslt_18));
apps = take(portLoc(1, Rslt_18V));
lams = take(portLoc(2, Rslt_18V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_18), makePair(LAZ, 0, apps, lams));
pushRedex(args, seq_4);
return;
}
Term glblFn1798 = newRef(glblCFn1797);
Term glblFn1800;
void glblCFn1799(Term ref, Term args) {
Term apps, lams, V;
Location vLoc;
Term seq_4 = makePair(LAM, 0, SUB, NUL);
Location r_seq_4 = portLoc(2, seq_4);
Term loc_3 = newTerm(VAR, 0, portLoc(1, seq_4));
seq_4 = makePair(LAM, 0, SUB, seq_4);
Term steps_2 = newTerm(VAR, 0, portLoc(1, seq_4));
seq_4 = makePair(LAM, 0, SUB, seq_4);
Term initial_1 = newTerm(VAR, 0, portLoc(1, seq_4));
// allocate args at /home/jim/toccata/interpreter/intrp-rdr.toc: 456
// call threading-rec at /home/jim/toccata/interpreter/intrp-rdr.toc: 457
Term Rslt_6Args = makePair(APP, 0, loc_3, SUB);
Term Rslt_6 = newTerm(VAR, 0, portLoc(2, Rslt_6Args));
Rslt_6Args = makePair(APP, 0, initial_1, Rslt_6Args);
Rslt_6Args = makePair(APP, 0, steps_2, Rslt_6Args);
#ifdef STRICT
pushRedex(Rslt_6Args, glblFn1798);
#else
swap(termLoc(Rslt_6), makePair(LAZ, 0, Rslt_6Args, glblFn1798));
#endif

// link args to body
swap(r_seq_4, Rslt_6);

Term Rslt_6V = get(termLoc(Rslt_6));
apps = take(portLoc(1, Rslt_6V));
lams = take(portLoc(2, Rslt_6V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_6), makePair(LAZ, 0, apps, lams));
pushRedex(args, seq_4);
return;
}
Term glblFn1800 = newRef(glblCFn1799);
void glblProto64_2353(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(461), args);
  args = makePair(APP, 0, glblStr1912, args);
  interact(args, glblProto64);
}
}
void glblProto20_2355(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(461), args);
  args = makePair(APP, 0, glblStr1912, args);
  interact(args, glblProto20);
}
}
void glblProto76_2357(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(462), args);
  args = makePair(APP, 0, glblStr1912, args);
  interact(args, glblProto76);
}
}
void glblProto56_2359(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(462), args);
  args = makePair(APP, 0, glblStr1912, args);
  interact(args, glblProto56);
}
}
void glblProto78_2361(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(465), args);
  args = makePair(APP, 0, glblStr1912, args);
  interact(args, glblProto78);
}
}
void glblProto76_2363(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(464), args);
  args = makePair(APP, 0, glblStr1912, args);
  interact(args, glblProto76);
}
}
void glblProto56_2365(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(464), args);
  args = makePair(APP, 0, glblStr1912, args);
  interact(args, glblProto56);
}
}
void glblProto34_2367(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(461), args);
  args = makePair(APP, 0, glblStr1912, args);
  interact(args, glblProto34);
}
}
Term glblFn1802;
void glblCFn1801(Term ref, Term args) {
Term apps, lams, V;
Location vLoc;
Term seq_4 = makePair(LAM, 0, SUB, NUL);
Location r_seq_4 = portLoc(2, seq_4);
Term loc_3 = newTerm(VAR, 0, portLoc(1, seq_4));
seq_4 = makePair(LAM, 0, SUB, seq_4);
Term body_vec_2 = newTerm(VAR, 0, portLoc(1, seq_4));
seq_4 = makePair(LAM, 0, SUB, seq_4);
Term pairs_1 = newTerm(VAR, 0, portLoc(1, seq_4));
// allocate args at /home/jim/toccata/interpreter/intrp-rdr.toc: 459

Term pairs_1_3;
pairs_1 = dupeArg(pairs_1, &pairs_1_3, 334);

Term pairs_1_2;
pairs_1 = dupeArg(pairs_1, &pairs_1_2, 335);

Term pairs_1_1;
pairs_1 = dupeArg(pairs_1, &pairs_1_1, 336);

Term body_vec_2_1;
body_vec_2 = dupeArg(body_vec_2, &body_vec_2_1, 337);

Term loc_3_2;
loc_3 = dupeArg(loc_3, &loc_3_2, 338);

Term loc_3_1;
loc_3 = dupeArg(loc_3, &loc_3_1, 339);
Term seq_23 = makePair(LAM, 0, SUB, NUL);
Location r_seq_23 = portLoc(2, seq_23);
Term pair_22 = newTerm(VAR, 0, portLoc(1, seq_23));
// allocate args at /home/jim/toccata/interpreter/intrp-rdr.toc: 464

Term pair_22_1;
pair_22 = dupeArg(pair_22, &pair_22_1, 0);
Term seq_27 = makePair(LAM, 0, SUB, NUL);
Location r_seq_27 = portLoc(2, seq_27);
Term inner_26 = newTerm(VAR, 0, portLoc(1, seq_27));
// allocate args at /home/jim/toccata/interpreter/intrp-rdr.toc: 465
Term glblVal175_59;
glblVal175 = dupeArg(glblVal175, &glblVal175_59, 0);

Term glblVal175_58;
glblVal175 = dupeArg(glblVal175, &glblVal175_58, 0);

Term glblVal175_57;
glblVal175 = dupeArg(glblVal175, &glblVal175_57, 0);

Term glblVal1605_2;
glblVal1605 = dupeArg(glblVal1605, &glblVal1605_2, 0);

Term glblVal1591_12;
glblVal1591 = dupeArg(glblVal1591, &glblVal1591_12, 0);

Term seq_10 = makePair(LAM, 0, SUB, NUL);
Location r_seq_10 = portLoc(2, seq_10);
Term pair_9 = newTerm(VAR, 0, portLoc(1, seq_10));
// allocate args at /home/jim/toccata/interpreter/intrp-rdr.toc: 462

Term pair_9_1;
pair_9 = dupeArg(pair_9, &pair_9_1, 0);
Term glblVal175_56;
glblVal175 = dupeArg(glblVal175, &glblVal175_56, 0);

Term glblVal175_55;
glblVal175 = dupeArg(glblVal175, &glblVal175_55, 0);

Term glblVal1605_1;
glblVal1605 = dupeArg(glblVal1605, &glblVal1605_1, 0);

Term glblVal1591_11;
glblVal1591 = dupeArg(glblVal1591, &glblVal1591_11, 0);

// call count at /home/jim/toccata/interpreter/intrp-rdr.toc: 461
Term Rslt_6Args = makePair(APP, 0, pairs_1, SUB);
Term Rslt_6 = newTerm(VAR, 0, portLoc(2, Rslt_6Args));
#ifdef STRICT
pushRedex(Rslt_6Args, newRef(glblProto64_2353));
#else
swap(termLoc(Rslt_6), makePair(LAZ, 0, Rslt_6Args, newRef(glblProto64_2353)));
#endif
// call = at /home/jim/toccata/interpreter/intrp-rdr.toc: 461
Term Rslt_7Args = makePair(APP, 0, newI60(1), SUB);
Term Rslt_7 = newTerm(VAR, 0, portLoc(2, Rslt_7Args));
Rslt_7Args = makePair(APP, 0, Rslt_6, Rslt_7Args);
#ifdef STRICT
pushRedex(Rslt_7Args, newRef(glblProto20_2355));
#else
swap(termLoc(Rslt_7), makePair(LAZ, 0, Rslt_7Args, newRef(glblProto20_2355)));
#endif
// call elt0 at /home/jim/toccata/interpreter/intrp-rdr.toc: 463
Term Rslt_12Args = makePair(APP, 0, pair_9, SUB);
Term Rslt_12 = newTerm(VAR, 0, portLoc(2, Rslt_12Args));
#ifdef STRICT
pushRedex(Rslt_12Args, glblFn1742);
#else
swap(termLoc(Rslt_12), makePair(LAZ, 0, Rslt_12Args, glblFn1742));
#endif

// call vect-conj at /home/jim/toccata/interpreter/intrp-rdr.toc: 463
Term Rslt_13Args = makePair(APP, 0, Rslt_12, SUB);
Term Rslt_13 = newTerm(VAR, 0, portLoc(2, Rslt_13Args));
Rslt_13Args = makePair(APP, 0, glblVal175_55, Rslt_13Args);
#ifdef STRICT
pushRedex(Rslt_13Args, glblFn178);
#else
swap(termLoc(Rslt_13), makePair(LAZ, 0, Rslt_13Args, glblFn178));
#endif

// call ast/Fn at /home/jim/toccata/interpreter/intrp-rdr.toc: 463
Term Rslt_14Args = makePair(APP, 0, loc_3, SUB);
Term Rslt_14 = newTerm(VAR, 0, portLoc(2, Rslt_14Args));
Rslt_14Args = makePair(APP, 0, body_vec_2, Rslt_14Args);
Rslt_14Args = makePair(APP, 0, Rslt_13, Rslt_14Args);
Rslt_14Args = makePair(APP, 0, glblStr402, Rslt_14Args);
#ifdef STRICT
pushRedex(Rslt_14Args, glblVal1605_1);
#else
swap(termLoc(Rslt_14), makePair(LAZ, 0, Rslt_14Args, glblVal1605_1));
#endif

// call elt1 at /home/jim/toccata/interpreter/intrp-rdr.toc: 463
Term Rslt_15Args = makePair(APP, 0, pair_9_1, SUB);
Term Rslt_15 = newTerm(VAR, 0, portLoc(2, Rslt_15Args));
#ifdef STRICT
pushRedex(Rslt_15Args, glblFn1744);
#else
swap(termLoc(Rslt_15), makePair(LAZ, 0, Rslt_15Args, glblFn1744));
#endif

// call vect-conj at /home/jim/toccata/interpreter/intrp-rdr.toc: 463
Term Rslt_16Args = makePair(APP, 0, Rslt_15, SUB);
Term Rslt_16 = newTerm(VAR, 0, portLoc(2, Rslt_16Args));
Rslt_16Args = makePair(APP, 0, glblVal175_56, Rslt_16Args);
#ifdef STRICT
pushRedex(Rslt_16Args, glblFn178);
#else
swap(termLoc(Rslt_16), makePair(LAZ, 0, Rslt_16Args, glblFn178));
#endif

// call ast/Call at /home/jim/toccata/interpreter/intrp-rdr.toc: 463
Term Rslt_17Args = makePair(APP, 0, Rslt_16, SUB);
Term Rslt_17 = newTerm(VAR, 0, portLoc(2, Rslt_17Args));
Rslt_17Args = makePair(APP, 0, Rslt_14, Rslt_17Args);
#ifdef STRICT
pushRedex(Rslt_17Args, glblVal1591_11);
#else
swap(termLoc(Rslt_17), makePair(LAZ, 0, Rslt_17Args, glblVal1591_11));
#endif

// link args to body
swap(r_seq_10, Rslt_17);

Term Rslt_17V = get(termLoc(Rslt_17));
apps = take(portLoc(1, Rslt_17V));
lams = take(portLoc(2, Rslt_17V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_17), makePair(LAZ, 0, apps, lams));
// call first at /home/jim/toccata/interpreter/intrp-rdr.toc: 462
Term Rslt_18Args = makePair(APP, 0, pairs_1_1, SUB);
Term Rslt_18 = newTerm(VAR, 0, portLoc(2, Rslt_18Args));
#ifdef STRICT
pushRedex(Rslt_18Args, newRef(glblProto76_2357));
#else
swap(termLoc(Rslt_18), makePair(LAZ, 0, Rslt_18Args, newRef(glblProto76_2357)));
#endif
// call extract at /home/jim/toccata/interpreter/intrp-rdr.toc: 462
Term Rslt_19Args = makePair(APP, 0, Rslt_18, SUB);
Term Rslt_19 = newTerm(VAR, 0, portLoc(2, Rslt_19Args));
#ifdef STRICT
pushRedex(Rslt_19Args, newRef(glblProto56_2359));
#else
swap(termLoc(Rslt_19), makePair(LAZ, 0, Rslt_19Args, newRef(glblProto56_2359)));
#endif
// call  at /home/jim/toccata/interpreter/intrp-rdr.toc: 462
Term Rslt_20Args = makePair(APP, 0, Rslt_19, SUB);
Term Rslt_20 = newTerm(VAR, 0, portLoc(2, Rslt_20Args));
#ifdef STRICT
pushRedex(Rslt_20Args, seq_10);
#else
swap(termLoc(Rslt_20), makePair(LAZ, 0, Rslt_20Args, seq_10));
#endif

// call elt0 at /home/jim/toccata/interpreter/intrp-rdr.toc: 466
Term Rslt_29Args = makePair(APP, 0, pair_22, SUB);
Term Rslt_29 = newTerm(VAR, 0, portLoc(2, Rslt_29Args));
#ifdef STRICT
pushRedex(Rslt_29Args, glblFn1742);
#else
swap(termLoc(Rslt_29), makePair(LAZ, 0, Rslt_29Args, glblFn1742));
#endif

// call vect-conj at /home/jim/toccata/interpreter/intrp-rdr.toc: 466
Term Rslt_30Args = makePair(APP, 0, Rslt_29, SUB);
Term Rslt_30 = newTerm(VAR, 0, portLoc(2, Rslt_30Args));
Rslt_30Args = makePair(APP, 0, glblVal175_57, Rslt_30Args);
#ifdef STRICT
pushRedex(Rslt_30Args, glblFn178);
#else
swap(termLoc(Rslt_30), makePair(LAZ, 0, Rslt_30Args, glblFn178));
#endif

// call vect-conj at /home/jim/toccata/interpreter/intrp-rdr.toc: 466
Term Rslt_31Args = makePair(APP, 0, inner_26, SUB);
Term Rslt_31 = newTerm(VAR, 0, portLoc(2, Rslt_31Args));
Rslt_31Args = makePair(APP, 0, glblVal175_58, Rslt_31Args);
#ifdef STRICT
pushRedex(Rslt_31Args, glblFn178);
#else
swap(termLoc(Rslt_31), makePair(LAZ, 0, Rslt_31Args, glblFn178));
#endif

// call ast/Fn at /home/jim/toccata/interpreter/intrp-rdr.toc: 466
Term Rslt_32Args = makePair(APP, 0, loc_3_1, SUB);
Term Rslt_32 = newTerm(VAR, 0, portLoc(2, Rslt_32Args));
Rslt_32Args = makePair(APP, 0, Rslt_31, Rslt_32Args);
Rslt_32Args = makePair(APP, 0, Rslt_30, Rslt_32Args);
Rslt_32Args = makePair(APP, 0, glblStr402, Rslt_32Args);
#ifdef STRICT
pushRedex(Rslt_32Args, glblVal1605_2);
#else
swap(termLoc(Rslt_32), makePair(LAZ, 0, Rslt_32Args, glblVal1605_2));
#endif

// call elt1 at /home/jim/toccata/interpreter/intrp-rdr.toc: 466
Term Rslt_33Args = makePair(APP, 0, pair_22_1, SUB);
Term Rslt_33 = newTerm(VAR, 0, portLoc(2, Rslt_33Args));
#ifdef STRICT
pushRedex(Rslt_33Args, glblFn1744);
#else
swap(termLoc(Rslt_33), makePair(LAZ, 0, Rslt_33Args, glblFn1744));
#endif

// call vect-conj at /home/jim/toccata/interpreter/intrp-rdr.toc: 466
Term Rslt_34Args = makePair(APP, 0, Rslt_33, SUB);
Term Rslt_34 = newTerm(VAR, 0, portLoc(2, Rslt_34Args));
Rslt_34Args = makePair(APP, 0, glblVal175_59, Rslt_34Args);
#ifdef STRICT
pushRedex(Rslt_34Args, glblFn178);
#else
swap(termLoc(Rslt_34), makePair(LAZ, 0, Rslt_34Args, glblFn178));
#endif

// call ast/Call at /home/jim/toccata/interpreter/intrp-rdr.toc: 466
Term Rslt_35Args = makePair(APP, 0, Rslt_34, SUB);
Term Rslt_35 = newTerm(VAR, 0, portLoc(2, Rslt_35Args));
Rslt_35Args = makePair(APP, 0, Rslt_32, Rslt_35Args);
#ifdef STRICT
pushRedex(Rslt_35Args, glblVal1591_12);
#else
swap(termLoc(Rslt_35), makePair(LAZ, 0, Rslt_35Args, glblVal1591_12));
#endif

// link args to body
swap(r_seq_27, Rslt_35);

Term Rslt_35V = get(termLoc(Rslt_35));
apps = take(portLoc(1, Rslt_35V));
lams = take(portLoc(2, Rslt_35V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_35), makePair(LAZ, 0, apps, lams));
// call rest at /home/jim/toccata/interpreter/intrp-rdr.toc: 465
Term Rslt_36Args = makePair(APP, 0, pairs_1_2, SUB);
Term Rslt_36 = newTerm(VAR, 0, portLoc(2, Rslt_36Args));
#ifdef STRICT
pushRedex(Rslt_36Args, newRef(glblProto78_2361));
#else
swap(termLoc(Rslt_36), makePair(LAZ, 0, Rslt_36Args, newRef(glblProto78_2361)));
#endif
// call let-desugar-rec at /home/jim/toccata/interpreter/intrp-rdr.toc: 459
Term Rslt_37Args = makePair(APP, 0, loc_3_2, SUB);
Term Rslt_37 = newTerm(VAR, 0, portLoc(2, Rslt_37Args));
Rslt_37Args = makePair(APP, 0, body_vec_2_1, Rslt_37Args);
Rslt_37Args = makePair(APP, 0, Rslt_36, Rslt_37Args);
#ifdef STRICT
pushRedex(Rslt_37Args, glblFn1802);
#else
swap(termLoc(Rslt_37), makePair(LAZ, 0, Rslt_37Args, glblFn1802));
#endif

// call  at /home/jim/toccata/interpreter/intrp-rdr.toc: 459
Term Rslt_38Args = makePair(APP, 0, Rslt_37, SUB);
Term Rslt_38 = newTerm(VAR, 0, portLoc(2, Rslt_38Args));
#ifdef STRICT
pushRedex(Rslt_38Args, seq_27);
#else
swap(termLoc(Rslt_38), makePair(LAZ, 0, Rslt_38Args, seq_27));
#endif

// link args to body
swap(r_seq_23, Rslt_38);

Term Rslt_38V = get(termLoc(Rslt_38));
apps = take(portLoc(1, Rslt_38V));
lams = take(portLoc(2, Rslt_38V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_38), makePair(LAZ, 0, apps, lams));
// call first at /home/jim/toccata/interpreter/intrp-rdr.toc: 464
Term Rslt_39Args = makePair(APP, 0, pairs_1_3, SUB);
Term Rslt_39 = newTerm(VAR, 0, portLoc(2, Rslt_39Args));
#ifdef STRICT
pushRedex(Rslt_39Args, newRef(glblProto76_2363));
#else
swap(termLoc(Rslt_39), makePair(LAZ, 0, Rslt_39Args, newRef(glblProto76_2363)));
#endif
// call extract at /home/jim/toccata/interpreter/intrp-rdr.toc: 464
Term Rslt_40Args = makePair(APP, 0, Rslt_39, SUB);
Term Rslt_40 = newTerm(VAR, 0, portLoc(2, Rslt_40Args));
#ifdef STRICT
pushRedex(Rslt_40Args, newRef(glblProto56_2365));
#else
swap(termLoc(Rslt_40), makePair(LAZ, 0, Rslt_40Args, newRef(glblProto56_2365)));
#endif
// call  at /home/jim/toccata/interpreter/intrp-rdr.toc: 464
Term Rslt_41Args = makePair(APP, 0, Rslt_40, SUB);
Term Rslt_41 = newTerm(VAR, 0, portLoc(2, Rslt_41Args));
#ifdef STRICT
pushRedex(Rslt_41Args, seq_23);
#else
swap(termLoc(Rslt_41), makePair(LAZ, 0, Rslt_41Args, seq_23));
#endif

// call cond at /home/jim/toccata/interpreter/intrp-rdr.toc: 461
Term Rslt_42Args = makePair(APP, 0, Rslt_41, SUB);
Term Rslt_42 = newTerm(VAR, 0, portLoc(2, Rslt_42Args));
Rslt_42Args = makePair(APP, 0, Rslt_20, Rslt_42Args);
Rslt_42Args = makePair(APP, 0, Rslt_7, Rslt_42Args);
#ifdef STRICT
pushRedex(Rslt_42Args, newRef(glblProto34_2367));
#else
swap(termLoc(Rslt_42), makePair(LAZ, 0, Rslt_42Args, newRef(glblProto34_2367)));
#endif
// link args to body
swap(r_seq_4, Rslt_42);

Term Rslt_42V = get(termLoc(Rslt_42));
apps = take(portLoc(1, Rslt_42V));
lams = take(portLoc(2, Rslt_42V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_42), makePair(LAZ, 0, apps, lams));
pushRedex(args, seq_4);
return;
}
Term glblFn1802 = newRef(glblCFn1801);
Term glblFn1804;
void glblCFn1803(Term ref, Term args) {
Term apps, lams, V;
Location vLoc;
Term seq_4 = makePair(LAM, 0, SUB, NUL);
Location r_seq_4 = portLoc(2, seq_4);
Term loc_3 = newTerm(VAR, 0, portLoc(1, seq_4));
seq_4 = makePair(LAM, 0, SUB, seq_4);
Term body_vec_2 = newTerm(VAR, 0, portLoc(1, seq_4));
seq_4 = makePair(LAM, 0, SUB, seq_4);
Term pairs_1 = newTerm(VAR, 0, portLoc(1, seq_4));
// allocate args at /home/jim/toccata/interpreter/intrp-rdr.toc: 470
// call let-desugar-rec at /home/jim/toccata/interpreter/intrp-rdr.toc: 471
Term Rslt_6Args = makePair(APP, 0, loc_3, SUB);
Term Rslt_6 = newTerm(VAR, 0, portLoc(2, Rslt_6Args));
Rslt_6Args = makePair(APP, 0, body_vec_2, Rslt_6Args);
Rslt_6Args = makePair(APP, 0, pairs_1, Rslt_6Args);
#ifdef STRICT
pushRedex(Rslt_6Args, glblFn1802);
#else
swap(termLoc(Rslt_6), makePair(LAZ, 0, Rslt_6Args, glblFn1802));
#endif

// link args to body
swap(r_seq_4, Rslt_6);

Term Rslt_6V = get(termLoc(Rslt_6));
apps = take(portLoc(1, Rslt_6V));
lams = take(portLoc(2, Rslt_6V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_6), makePair(LAZ, 0, apps, lams));
pushRedex(args, seq_4);
return;
}
Term glblFn1804 = newRef(glblCFn1803);
void glblProto34_2372(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(483), args);
  args = makePair(APP, 0, glblStr1912, args);
  interact(args, glblProto34);
}
}
Term glblFn1806;
void glblCFn1805(Term ref, Term args) {
Term apps, lams, V;
Location vLoc;
Term seq_3 = makePair(LAM, 0, SUB, NUL);
Location r_seq_3 = portLoc(2, seq_3);
Term acc_2 = newTerm(VAR, 0, portLoc(1, seq_3));
seq_3 = makePair(LAM, 0, SUB, seq_3);
Term state_1 = newTerm(VAR, 0, portLoc(1, seq_3));
// allocate args at /home/jim/toccata/interpreter/intrp-rdr.toc: 479

Term acc_2_1;
acc_2 = dupeArg(acc_2, &acc_2_1, 340);
Term seq_7 = makePair(LAM, 0, SUB, NUL);
Location r_seq_7 = portLoc(2, seq_7);
Term s_6 = newTerm(VAR, 0, portLoc(1, seq_7));
// allocate args at /home/jim/toccata/interpreter/intrp-rdr.toc: 480

Term s_6_2;
s_6 = dupeArg(s_6, &s_6_2, 0);

Term s_6_1;
s_6 = dupeArg(s_6, &s_6_1, 0);
Term seq_11 = makePair(LAM, 0, SUB, NUL);
Location r_seq_11 = portLoc(2, seq_11);
Term c_10 = newTerm(VAR, 0, portLoc(1, seq_11));
// allocate args at /home/jim/toccata/interpreter/intrp-rdr.toc: 481
Term seq_19 = makePair(LAM, 0, SUB, NUL);
Location r_seq_19 = portLoc(2, seq_19);
Term nm_18 = newTerm(VAR, 0, portLoc(1, seq_19));
// allocate args at /home/jim/toccata/interpreter/intrp-rdr.toc: 485

Term nm_18_1;
nm_18 = dupeArg(nm_18, &nm_18_1, 0);
Term glblVal175_60;
glblVal175 = dupeArg(glblVal175, &glblVal175_60, 0);

// call str= at /home/jim/toccata/interpreter/intrp-rdr.toc: 483
Term Rslt_13Args = makePair(APP, 0, glblStr746, SUB);
Term Rslt_13 = newTerm(VAR, 0, portLoc(2, Rslt_13Args));
Rslt_13Args = makePair(APP, 0, c_10, Rslt_13Args);
#ifdef STRICT
pushRedex(Rslt_13Args, glblFn363);
#else
swap(termLoc(Rslt_13), makePair(LAZ, 0, Rslt_13Args, glblFn363));
#endif

// call vect-conj at /home/jim/toccata/interpreter/intrp-rdr.toc: 484
Term Rslt_14Args = makePair(APP, 0, acc_2, SUB);
Term Rslt_14 = newTerm(VAR, 0, portLoc(2, Rslt_14Args));
Rslt_14Args = makePair(APP, 0, glblVal175_60, Rslt_14Args);
#ifdef STRICT
pushRedex(Rslt_14Args, glblFn178);
#else
swap(termLoc(Rslt_14), makePair(LAZ, 0, Rslt_14Args, glblFn178));
#endif

// call take-char at /home/jim/toccata/interpreter/intrp-rdr.toc: 484
Term Rslt_15Args = makePair(APP, 0, s_6, SUB);
Term Rslt_15 = newTerm(VAR, 0, portLoc(2, Rslt_15Args));
#ifdef STRICT
pushRedex(Rslt_15Args, glblFn1732);
#else
swap(termLoc(Rslt_15), makePair(LAZ, 0, Rslt_15Args, glblFn1732));
#endif

// call vect-conj at /home/jim/toccata/interpreter/intrp-rdr.toc: 484
Term Rslt_16Args = makePair(APP, 0, Rslt_15, SUB);
Term Rslt_16 = newTerm(VAR, 0, portLoc(2, Rslt_16Args));
Rslt_16Args = makePair(APP, 0, Rslt_14, Rslt_16Args);
#ifdef STRICT
pushRedex(Rslt_16Args, glblFn178);
#else
swap(termLoc(Rslt_16), makePair(LAZ, 0, Rslt_16Args, glblFn178));
#endif

// call elt1 at /home/jim/toccata/interpreter/intrp-rdr.toc: 486
Term Rslt_21Args = makePair(APP, 0, nm_18, SUB);
Term Rslt_21 = newTerm(VAR, 0, portLoc(2, Rslt_21Args));
#ifdef STRICT
pushRedex(Rslt_21Args, glblFn1744);
#else
swap(termLoc(Rslt_21), makePair(LAZ, 0, Rslt_21Args, glblFn1744));
#endif

// call elt0 at /home/jim/toccata/interpreter/intrp-rdr.toc: 486
Term Rslt_22Args = makePair(APP, 0, nm_18_1, SUB);
Term Rslt_22 = newTerm(VAR, 0, portLoc(2, Rslt_22Args));
#ifdef STRICT
pushRedex(Rslt_22Args, glblFn1742);
#else
swap(termLoc(Rslt_22), makePair(LAZ, 0, Rslt_22Args, glblFn1742));
#endif

// call vect-conj at /home/jim/toccata/interpreter/intrp-rdr.toc: 486
Term Rslt_23Args = makePair(APP, 0, Rslt_22, SUB);
Term Rslt_23 = newTerm(VAR, 0, portLoc(2, Rslt_23Args));
Rslt_23Args = makePair(APP, 0, acc_2_1, Rslt_23Args);
#ifdef STRICT
pushRedex(Rslt_23Args, glblFn178);
#else
swap(termLoc(Rslt_23), makePair(LAZ, 0, Rslt_23Args, glblFn178));
#endif

// call read-param-list-acc at /home/jim/toccata/interpreter/intrp-rdr.toc: 479
Term Rslt_24Args = makePair(APP, 0, Rslt_23, SUB);
Term Rslt_24 = newTerm(VAR, 0, portLoc(2, Rslt_24Args));
Rslt_24Args = makePair(APP, 0, Rslt_21, Rslt_24Args);
#ifdef STRICT
pushRedex(Rslt_24Args, glblFn1806);
#else
swap(termLoc(Rslt_24), makePair(LAZ, 0, Rslt_24Args, glblFn1806));
#endif

// link args to body
swap(r_seq_19, Rslt_24);

Term Rslt_24V = get(termLoc(Rslt_24));
apps = take(portLoc(1, Rslt_24V));
lams = take(portLoc(2, Rslt_24V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_24), makePair(LAZ, 0, apps, lams));
// call read-symbol at /home/jim/toccata/interpreter/intrp-rdr.toc: 485
Term Rslt_25Args = makePair(APP, 0, s_6_1, SUB);
Term Rslt_25 = newTerm(VAR, 0, portLoc(2, Rslt_25Args));
#ifdef STRICT
pushRedex(Rslt_25Args, glblFn1750);
#else
swap(termLoc(Rslt_25), makePair(LAZ, 0, Rslt_25Args, glblFn1750));
#endif

// call  at /home/jim/toccata/interpreter/intrp-rdr.toc: 485
Term Rslt_26Args = makePair(APP, 0, Rslt_25, SUB);
Term Rslt_26 = newTerm(VAR, 0, portLoc(2, Rslt_26Args));
#ifdef STRICT
pushRedex(Rslt_26Args, seq_19);
#else
swap(termLoc(Rslt_26), makePair(LAZ, 0, Rslt_26Args, seq_19));
#endif

// call cond at /home/jim/toccata/interpreter/intrp-rdr.toc: 483
Term Rslt_27Args = makePair(APP, 0, Rslt_26, SUB);
Term Rslt_27 = newTerm(VAR, 0, portLoc(2, Rslt_27Args));
Rslt_27Args = makePair(APP, 0, Rslt_16, Rslt_27Args);
Rslt_27Args = makePair(APP, 0, Rslt_13, Rslt_27Args);
#ifdef STRICT
pushRedex(Rslt_27Args, newRef(glblProto34_2372));
#else
swap(termLoc(Rslt_27), makePair(LAZ, 0, Rslt_27Args, newRef(glblProto34_2372)));
#endif
// link args to body
swap(r_seq_11, Rslt_27);

Term Rslt_27V = get(termLoc(Rslt_27));
apps = take(portLoc(1, Rslt_27V));
lams = take(portLoc(2, Rslt_27V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_27), makePair(LAZ, 0, apps, lams));
// call peek-char at /home/jim/toccata/interpreter/intrp-rdr.toc: 481
Term Rslt_28Args = makePair(APP, 0, s_6_2, SUB);
Term Rslt_28 = newTerm(VAR, 0, portLoc(2, Rslt_28Args));
#ifdef STRICT
pushRedex(Rslt_28Args, glblFn1730);
#else
swap(termLoc(Rslt_28), makePair(LAZ, 0, Rslt_28Args, glblFn1730));
#endif

// call  at /home/jim/toccata/interpreter/intrp-rdr.toc: 481
Term Rslt_29Args = makePair(APP, 0, Rslt_28, SUB);
Term Rslt_29 = newTerm(VAR, 0, portLoc(2, Rslt_29Args));
#ifdef STRICT
pushRedex(Rslt_29Args, seq_11);
#else
swap(termLoc(Rslt_29), makePair(LAZ, 0, Rslt_29Args, seq_11));
#endif

// link args to body
swap(r_seq_7, Rslt_29);

Term Rslt_29V = get(termLoc(Rslt_29));
apps = take(portLoc(1, Rslt_29V));
lams = take(portLoc(2, Rslt_29V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_29), makePair(LAZ, 0, apps, lams));
// call skip-whitespace at /home/jim/toccata/interpreter/intrp-rdr.toc: 480
Term Rslt_30Args = makePair(APP, 0, state_1, SUB);
Term Rslt_30 = newTerm(VAR, 0, portLoc(2, Rslt_30Args));
#ifdef STRICT
pushRedex(Rslt_30Args, glblFn1736);
#else
swap(termLoc(Rslt_30), makePair(LAZ, 0, Rslt_30Args, glblFn1736));
#endif

// call  at /home/jim/toccata/interpreter/intrp-rdr.toc: 480
Term Rslt_31Args = makePair(APP, 0, Rslt_30, SUB);
Term Rslt_31 = newTerm(VAR, 0, portLoc(2, Rslt_31Args));
#ifdef STRICT
pushRedex(Rslt_31Args, seq_7);
#else
swap(termLoc(Rslt_31), makePair(LAZ, 0, Rslt_31Args, seq_7));
#endif

// link args to body
swap(r_seq_3, Rslt_31);

Term Rslt_31V = get(termLoc(Rslt_31));
apps = take(portLoc(1, Rslt_31V));
lams = take(portLoc(2, Rslt_31V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_31), makePair(LAZ, 0, apps, lams));
pushRedex(args, seq_3);
return;
}
Term glblFn1806 = newRef(glblCFn1805);
Term glblFn1808;
void glblCFn1807(Term ref, Term args) {
Term apps, lams, V;
Location vLoc;
Term seq_2 = makePair(LAM, 0, SUB, NUL);
Location r_seq_2 = portLoc(2, seq_2);
Term state_1 = newTerm(VAR, 0, portLoc(1, seq_2));
// allocate args at /home/jim/toccata/interpreter/intrp-rdr.toc: 488
Term glblVal175_61;
glblVal175 = dupeArg(glblVal175, &glblVal175_61, 0);

// call read-param-list-acc at /home/jim/toccata/interpreter/intrp-rdr.toc: 489
Term Rslt_4Args = makePair(APP, 0, glblVal175_61, SUB);
Term Rslt_4 = newTerm(VAR, 0, portLoc(2, Rslt_4Args));
Rslt_4Args = makePair(APP, 0, state_1, Rslt_4Args);
#ifdef STRICT
pushRedex(Rslt_4Args, glblFn1806);
#else
swap(termLoc(Rslt_4), makePair(LAZ, 0, Rslt_4Args, glblFn1806));
#endif

// link args to body
swap(r_seq_2, Rslt_4);

Term Rslt_4V = get(termLoc(Rslt_4));
apps = take(portLoc(1, Rslt_4V));
lams = take(portLoc(2, Rslt_4V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_4), makePair(LAZ, 0, apps, lams));
pushRedex(args, seq_2);
return;
}
Term glblFn1808 = newRef(glblCFn1807);
Term glblFn1810;
void glblCFn1809(Term ref, Term args) {
Term apps, lams, V;
Location vLoc;
Term seq_2 = makePair(LAM, 0, SUB, NUL);
Location r_seq_2 = portLoc(2, seq_2);
Term state_1 = newTerm(VAR, 0, portLoc(1, seq_2));
// allocate args at /home/jim/toccata/interpreter/intrp-rdr.toc: 492
Term seq_6 = makePair(LAM, 0, SUB, NUL);
Location r_seq_6 = portLoc(2, seq_6);
Term s_5 = newTerm(VAR, 0, portLoc(1, seq_6));
// allocate args at /home/jim/toccata/interpreter/intrp-rdr.toc: 493
Term seq_10 = makePair(LAM, 0, SUB, NUL);
Location r_seq_10 = portLoc(2, seq_10);
Term nm_9 = newTerm(VAR, 0, portLoc(1, seq_10));
// allocate args at /home/jim/toccata/interpreter/intrp-rdr.toc: 494

Term nm_9_1;
nm_9 = dupeArg(nm_9, &nm_9_1, 0);
Term seq_14 = makePair(LAM, 0, SUB, NUL);
Location r_seq_14 = portLoc(2, seq_14);
Term sym_13 = newTerm(VAR, 0, portLoc(1, seq_14));
// allocate args at /home/jim/toccata/interpreter/intrp-rdr.toc: 495
Term seq_18 = makePair(LAM, 0, SUB, NUL);
Location r_seq_18 = portLoc(2, seq_18);
Term te_17 = newTerm(VAR, 0, portLoc(1, seq_18));
// allocate args at /home/jim/toccata/interpreter/intrp-rdr.toc: 496

Term te_17_1;
te_17 = dupeArg(te_17, &te_17_1, 0);
Term glblVal1621_1;
glblVal1621 = dupeArg(glblVal1621, &glblVal1621_1, 0);

Term glblVal175_62;
glblVal175 = dupeArg(glblVal175, &glblVal175_62, 0);

// call elt0 at /home/jim/toccata/interpreter/intrp-rdr.toc: 497
Term Rslt_20Args = makePair(APP, 0, te_17, SUB);
Term Rslt_20 = newTerm(VAR, 0, portLoc(2, Rslt_20Args));
#ifdef STRICT
pushRedex(Rslt_20Args, glblFn1742);
#else
swap(termLoc(Rslt_20), makePair(LAZ, 0, Rslt_20Args, glblFn1742));
#endif

// call ast/TypeConstraint at /home/jim/toccata/interpreter/intrp-rdr.toc: 497
Term Rslt_21Args = makePair(APP, 0, Rslt_20, SUB);
Term Rslt_21 = newTerm(VAR, 0, portLoc(2, Rslt_21Args));
Rslt_21Args = makePair(APP, 0, sym_13, Rslt_21Args);
#ifdef STRICT
pushRedex(Rslt_21Args, glblVal1621_1);
#else
swap(termLoc(Rslt_21), makePair(LAZ, 0, Rslt_21Args, glblVal1621_1));
#endif

// call vect-conj at /home/jim/toccata/interpreter/intrp-rdr.toc: 497
Term Rslt_22Args = makePair(APP, 0, Rslt_21, SUB);
Term Rslt_22 = newTerm(VAR, 0, portLoc(2, Rslt_22Args));
Rslt_22Args = makePair(APP, 0, glblVal175_62, Rslt_22Args);
#ifdef STRICT
pushRedex(Rslt_22Args, glblFn178);
#else
swap(termLoc(Rslt_22), makePair(LAZ, 0, Rslt_22Args, glblFn178));
#endif

// call elt1 at /home/jim/toccata/interpreter/intrp-rdr.toc: 497
Term Rslt_23Args = makePair(APP, 0, te_17_1, SUB);
Term Rslt_23 = newTerm(VAR, 0, portLoc(2, Rslt_23Args));
#ifdef STRICT
pushRedex(Rslt_23Args, glblFn1744);
#else
swap(termLoc(Rslt_23), makePair(LAZ, 0, Rslt_23Args, glblFn1744));
#endif

// call vect-conj at /home/jim/toccata/interpreter/intrp-rdr.toc: 497
Term Rslt_24Args = makePair(APP, 0, Rslt_23, SUB);
Term Rslt_24 = newTerm(VAR, 0, portLoc(2, Rslt_24Args));
Rslt_24Args = makePair(APP, 0, Rslt_22, Rslt_24Args);
#ifdef STRICT
pushRedex(Rslt_24Args, glblFn178);
#else
swap(termLoc(Rslt_24), makePair(LAZ, 0, Rslt_24Args, glblFn178));
#endif

// link args to body
swap(r_seq_18, Rslt_24);

Term Rslt_24V = get(termLoc(Rslt_24));
apps = take(portLoc(1, Rslt_24V));
lams = take(portLoc(2, Rslt_24V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_24), makePair(LAZ, 0, apps, lams));
// call elt1 at /home/jim/toccata/interpreter/intrp-rdr.toc: 496
Term Rslt_25Args = makePair(APP, 0, nm_9, SUB);
Term Rslt_25 = newTerm(VAR, 0, portLoc(2, Rslt_25Args));
#ifdef STRICT
pushRedex(Rslt_25Args, glblFn1744);
#else
swap(termLoc(Rslt_25), makePair(LAZ, 0, Rslt_25Args, glblFn1744));
#endif

// call skip-whitespace at /home/jim/toccata/interpreter/intrp-rdr.toc: 496
Term Rslt_26Args = makePair(APP, 0, Rslt_25, SUB);
Term Rslt_26 = newTerm(VAR, 0, portLoc(2, Rslt_26Args));
#ifdef STRICT
pushRedex(Rslt_26Args, glblFn1736);
#else
swap(termLoc(Rslt_26), makePair(LAZ, 0, Rslt_26Args, glblFn1736));
#endif

// call read-type-expr at /home/jim/toccata/interpreter/intrp-rdr.toc: 496
Term Rslt_27Args = makePair(APP, 0, Rslt_26, SUB);
Term Rslt_27 = newTerm(VAR, 0, portLoc(2, Rslt_27Args));
#ifdef STRICT
pushRedex(Rslt_27Args, glblFn1768);
#else
swap(termLoc(Rslt_27), makePair(LAZ, 0, Rslt_27Args, glblFn1768));
#endif

// call  at /home/jim/toccata/interpreter/intrp-rdr.toc: 496
Term Rslt_28Args = makePair(APP, 0, Rslt_27, SUB);
Term Rslt_28 = newTerm(VAR, 0, portLoc(2, Rslt_28Args));
#ifdef STRICT
pushRedex(Rslt_28Args, seq_18);
#else
swap(termLoc(Rslt_28), makePair(LAZ, 0, Rslt_28Args, seq_18));
#endif

// link args to body
swap(r_seq_14, Rslt_28);

Term Rslt_28V = get(termLoc(Rslt_28));
apps = take(portLoc(1, Rslt_28V));
lams = take(portLoc(2, Rslt_28V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_28), makePair(LAZ, 0, apps, lams));
// call elt0 at /home/jim/toccata/interpreter/intrp-rdr.toc: 495
Term Rslt_29Args = makePair(APP, 0, nm_9_1, SUB);
Term Rslt_29 = newTerm(VAR, 0, portLoc(2, Rslt_29Args));
#ifdef STRICT
pushRedex(Rslt_29Args, glblFn1742);
#else
swap(termLoc(Rslt_29), makePair(LAZ, 0, Rslt_29Args, glblFn1742));
#endif

// call  at /home/jim/toccata/interpreter/intrp-rdr.toc: 495
Term Rslt_30Args = makePair(APP, 0, Rslt_29, SUB);
Term Rslt_30 = newTerm(VAR, 0, portLoc(2, Rslt_30Args));
#ifdef STRICT
pushRedex(Rslt_30Args, seq_14);
#else
swap(termLoc(Rslt_30), makePair(LAZ, 0, Rslt_30Args, seq_14));
#endif

// link args to body
swap(r_seq_10, Rslt_30);

Term Rslt_30V = get(termLoc(Rslt_30));
apps = take(portLoc(1, Rslt_30V));
lams = take(portLoc(2, Rslt_30V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_30), makePair(LAZ, 0, apps, lams));
// call read-symbol at /home/jim/toccata/interpreter/intrp-rdr.toc: 494
Term Rslt_31Args = makePair(APP, 0, s_5, SUB);
Term Rslt_31 = newTerm(VAR, 0, portLoc(2, Rslt_31Args));
#ifdef STRICT
pushRedex(Rslt_31Args, glblFn1750);
#else
swap(termLoc(Rslt_31), makePair(LAZ, 0, Rslt_31Args, glblFn1750));
#endif

// call  at /home/jim/toccata/interpreter/intrp-rdr.toc: 494
Term Rslt_32Args = makePair(APP, 0, Rslt_31, SUB);
Term Rslt_32 = newTerm(VAR, 0, portLoc(2, Rslt_32Args));
#ifdef STRICT
pushRedex(Rslt_32Args, seq_10);
#else
swap(termLoc(Rslt_32), makePair(LAZ, 0, Rslt_32Args, seq_10));
#endif

// link args to body
swap(r_seq_6, Rslt_32);

Term Rslt_32V = get(termLoc(Rslt_32));
apps = take(portLoc(1, Rslt_32V));
lams = take(portLoc(2, Rslt_32V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_32), makePair(LAZ, 0, apps, lams));
// call take-char at /home/jim/toccata/interpreter/intrp-rdr.toc: 493
Term Rslt_33Args = makePair(APP, 0, state_1, SUB);
Term Rslt_33 = newTerm(VAR, 0, portLoc(2, Rslt_33Args));
#ifdef STRICT
pushRedex(Rslt_33Args, glblFn1732);
#else
swap(termLoc(Rslt_33), makePair(LAZ, 0, Rslt_33Args, glblFn1732));
#endif

// call skip-whitespace at /home/jim/toccata/interpreter/intrp-rdr.toc: 493
Term Rslt_34Args = makePair(APP, 0, Rslt_33, SUB);
Term Rslt_34 = newTerm(VAR, 0, portLoc(2, Rslt_34Args));
#ifdef STRICT
pushRedex(Rslt_34Args, glblFn1736);
#else
swap(termLoc(Rslt_34), makePair(LAZ, 0, Rslt_34Args, glblFn1736));
#endif

// call  at /home/jim/toccata/interpreter/intrp-rdr.toc: 493
Term Rslt_35Args = makePair(APP, 0, Rslt_34, SUB);
Term Rslt_35 = newTerm(VAR, 0, portLoc(2, Rslt_35Args));
#ifdef STRICT
pushRedex(Rslt_35Args, seq_6);
#else
swap(termLoc(Rslt_35), makePair(LAZ, 0, Rslt_35Args, seq_6));
#endif

// link args to body
swap(r_seq_2, Rslt_35);

Term Rslt_35V = get(termLoc(Rslt_35));
apps = take(portLoc(1, Rslt_35V));
lams = take(portLoc(2, Rslt_35V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_35), makePair(LAZ, 0, apps, lams));
pushRedex(args, seq_2);
return;
}
Term glblFn1810 = newRef(glblCFn1809);
void glblProto34_2377(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(516), args);
  args = makePair(APP, 0, glblStr1912, args);
  interact(args, glblProto34);
}
}
Term glblFn1812;
void glblCFn1811(Term ref, Term args) {
Term apps, lams, V;
Location vLoc;
Term seq_2 = makePair(LAM, 0, SUB, NUL);
Location r_seq_2 = portLoc(2, seq_2);
Term state_1 = newTerm(VAR, 0, portLoc(1, seq_2));
// allocate args at /home/jim/toccata/interpreter/intrp-rdr.toc: 510
Term seq_6 = makePair(LAM, 0, SUB, NUL);
Location r_seq_6 = portLoc(2, seq_6);
Term r_5 = newTerm(VAR, 0, portLoc(1, seq_6));
// allocate args at /home/jim/toccata/interpreter/intrp-rdr.toc: 511

Term r_5_1;
r_5 = dupeArg(r_5, &r_5_1, 0);
Term seq_10 = makePair(LAM, 0, SUB, NUL);
Location r_seq_10 = portLoc(2, seq_10);
Term text_9 = newTerm(VAR, 0, portLoc(1, seq_10));
// allocate args at /home/jim/toccata/interpreter/intrp-rdr.toc: 512

Term text_9_2;
text_9 = dupeArg(text_9, &text_9_2, 0);

Term text_9_1;
text_9 = dupeArg(text_9, &text_9_1, 0);
Term seq_14 = makePair(LAM, 0, SUB, NUL);
Location r_seq_14 = portLoc(2, seq_14);
Term s2_13 = newTerm(VAR, 0, portLoc(1, seq_14));
// allocate args at /home/jim/toccata/interpreter/intrp-rdr.toc: 513

Term s2_13_2;
s2_13 = dupeArg(s2_13, &s2_13_2, 0);

Term s2_13_1;
s2_13 = dupeArg(s2_13, &s2_13_1, 0);
Term seq_18 = makePair(LAM, 0, SUB, NUL);
Location r_seq_18 = portLoc(2, seq_18);
Term loc_17 = newTerm(VAR, 0, portLoc(1, seq_18));
// allocate args at /home/jim/toccata/interpreter/intrp-rdr.toc: 514

Term loc_17_1;
loc_17 = dupeArg(loc_17, &loc_17_1, 0);
Term seq_25 = makePair(LAM, 0, SUB, NUL);
Location r_seq_25 = portLoc(2, seq_25);
Term n_24 = newTerm(VAR, 0, portLoc(1, seq_25));
// allocate args at /home/jim/toccata/interpreter/intrp-rdr.toc: 518
Term glblVal1569_1;
glblVal1569 = dupeArg(glblVal1569, &glblVal1569_1, 0);

Term glblVal1896_2;
glblVal1896 = dupeArg(glblVal1896, &glblVal1896_2, 0);

Term glblVal1575_1;
glblVal1575 = dupeArg(glblVal1575, &glblVal1575_1, 0);

Term glblVal1896_1;
glblVal1896 = dupeArg(glblVal1896, &glblVal1896_1, 0);

// call contains-dot at /home/jim/toccata/interpreter/intrp-rdr.toc: 516
Term Rslt_20Args = makePair(APP, 0, text_9, SUB);
Term Rslt_20 = newTerm(VAR, 0, portLoc(2, Rslt_20Args));
#ifdef STRICT
pushRedex(Rslt_20Args, glblFn1770);
#else
swap(termLoc(Rslt_20), makePair(LAZ, 0, Rslt_20Args, glblFn1770));
#endif

// call ast/FloatLit at /home/jim/toccata/interpreter/intrp-rdr.toc: 517
Term Rslt_21Args = makePair(APP, 0, loc_17, SUB);
Term Rslt_21 = newTerm(VAR, 0, portLoc(2, Rslt_21Args));
Rslt_21Args = makePair(APP, 0, text_9_1, Rslt_21Args);
#ifdef STRICT
pushRedex(Rslt_21Args, glblVal1575_1);
#else
swap(termLoc(Rslt_21), makePair(LAZ, 0, Rslt_21Args, glblVal1575_1));
#endif

// call ParserMatch at /home/jim/toccata/interpreter/intrp-rdr.toc: 517
Term Rslt_22Args = makePair(APP, 0, s2_13, SUB);
Term Rslt_22 = newTerm(VAR, 0, portLoc(2, Rslt_22Args));
Rslt_22Args = makePair(APP, 0, Rslt_21, Rslt_22Args);
#ifdef STRICT
pushRedex(Rslt_22Args, glblVal1896_1);
#else
swap(termLoc(Rslt_22), makePair(LAZ, 0, Rslt_22Args, glblVal1896_1));
#endif

// call ast/IntegerLit at /home/jim/toccata/interpreter/intrp-rdr.toc: 519
Term Rslt_27Args = makePair(APP, 0, loc_17_1, SUB);
Term Rslt_27 = newTerm(VAR, 0, portLoc(2, Rslt_27Args));
Rslt_27Args = makePair(APP, 0, n_24, Rslt_27Args);
#ifdef STRICT
pushRedex(Rslt_27Args, glblVal1569_1);
#else
swap(termLoc(Rslt_27), makePair(LAZ, 0, Rslt_27Args, glblVal1569_1));
#endif

// call ParserMatch at /home/jim/toccata/interpreter/intrp-rdr.toc: 519
Term Rslt_28Args = makePair(APP, 0, s2_13_1, SUB);
Term Rslt_28 = newTerm(VAR, 0, portLoc(2, Rslt_28Args));
Rslt_28Args = makePair(APP, 0, Rslt_27, Rslt_28Args);
#ifdef STRICT
pushRedex(Rslt_28Args, glblVal1896_2);
#else
swap(termLoc(Rslt_28), makePair(LAZ, 0, Rslt_28Args, glblVal1896_2));
#endif

// link args to body
swap(r_seq_25, Rslt_28);

Term Rslt_28V = get(termLoc(Rslt_28));
apps = take(portLoc(1, Rslt_28V));
lams = take(portLoc(2, Rslt_28V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_28), makePair(LAZ, 0, apps, lams));
// call text-to-int at /home/jim/toccata/interpreter/intrp-rdr.toc: 518
Term Rslt_29Args = makePair(APP, 0, text_9_2, SUB);
Term Rslt_29 = newTerm(VAR, 0, portLoc(2, Rslt_29Args));
#ifdef STRICT
pushRedex(Rslt_29Args, glblFn1774);
#else
swap(termLoc(Rslt_29), makePair(LAZ, 0, Rslt_29Args, glblFn1774));
#endif

// call  at /home/jim/toccata/interpreter/intrp-rdr.toc: 518
Term Rslt_30Args = makePair(APP, 0, Rslt_29, SUB);
Term Rslt_30 = newTerm(VAR, 0, portLoc(2, Rslt_30Args));
#ifdef STRICT
pushRedex(Rslt_30Args, seq_25);
#else
swap(termLoc(Rslt_30), makePair(LAZ, 0, Rslt_30Args, seq_25));
#endif

// call cond at /home/jim/toccata/interpreter/intrp-rdr.toc: 516
Term Rslt_31Args = makePair(APP, 0, Rslt_30, SUB);
Term Rslt_31 = newTerm(VAR, 0, portLoc(2, Rslt_31Args));
Rslt_31Args = makePair(APP, 0, Rslt_22, Rslt_31Args);
Rslt_31Args = makePair(APP, 0, Rslt_20, Rslt_31Args);
#ifdef STRICT
pushRedex(Rslt_31Args, newRef(glblProto34_2377));
#else
swap(termLoc(Rslt_31), makePair(LAZ, 0, Rslt_31Args, newRef(glblProto34_2377)));
#endif
// link args to body
swap(r_seq_18, Rslt_31);

Term Rslt_31V = get(termLoc(Rslt_31));
apps = take(portLoc(1, Rslt_31V));
lams = take(portLoc(2, Rslt_31V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_31), makePair(LAZ, 0, apps, lams));
// call make-loc at /home/jim/toccata/interpreter/intrp-rdr.toc: 514
Term Rslt_32Args = makePair(APP, 0, s2_13_2, SUB);
Term Rslt_32 = newTerm(VAR, 0, portLoc(2, Rslt_32Args));
#ifdef STRICT
pushRedex(Rslt_32Args, glblFn1762);
#else
swap(termLoc(Rslt_32), makePair(LAZ, 0, Rslt_32Args, glblFn1762));
#endif

// call  at /home/jim/toccata/interpreter/intrp-rdr.toc: 514
Term Rslt_33Args = makePair(APP, 0, Rslt_32, SUB);
Term Rslt_33 = newTerm(VAR, 0, portLoc(2, Rslt_33Args));
#ifdef STRICT
pushRedex(Rslt_33Args, seq_18);
#else
swap(termLoc(Rslt_33), makePair(LAZ, 0, Rslt_33Args, seq_18));
#endif

// link args to body
swap(r_seq_14, Rslt_33);

Term Rslt_33V = get(termLoc(Rslt_33));
apps = take(portLoc(1, Rslt_33V));
lams = take(portLoc(2, Rslt_33V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_33), makePair(LAZ, 0, apps, lams));
// call elt1 at /home/jim/toccata/interpreter/intrp-rdr.toc: 513
Term Rslt_34Args = makePair(APP, 0, r_5, SUB);
Term Rslt_34 = newTerm(VAR, 0, portLoc(2, Rslt_34Args));
#ifdef STRICT
pushRedex(Rslt_34Args, glblFn1744);
#else
swap(termLoc(Rslt_34), makePair(LAZ, 0, Rslt_34Args, glblFn1744));
#endif

// call  at /home/jim/toccata/interpreter/intrp-rdr.toc: 513
Term Rslt_35Args = makePair(APP, 0, Rslt_34, SUB);
Term Rslt_35 = newTerm(VAR, 0, portLoc(2, Rslt_35Args));
#ifdef STRICT
pushRedex(Rslt_35Args, seq_14);
#else
swap(termLoc(Rslt_35), makePair(LAZ, 0, Rslt_35Args, seq_14));
#endif

// link args to body
swap(r_seq_10, Rslt_35);

Term Rslt_35V = get(termLoc(Rslt_35));
apps = take(portLoc(1, Rslt_35V));
lams = take(portLoc(2, Rslt_35V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_35), makePair(LAZ, 0, apps, lams));
// call elt0 at /home/jim/toccata/interpreter/intrp-rdr.toc: 512
Term Rslt_36Args = makePair(APP, 0, r_5_1, SUB);
Term Rslt_36 = newTerm(VAR, 0, portLoc(2, Rslt_36Args));
#ifdef STRICT
pushRedex(Rslt_36Args, glblFn1742);
#else
swap(termLoc(Rslt_36), makePair(LAZ, 0, Rslt_36Args, glblFn1742));
#endif

// call  at /home/jim/toccata/interpreter/intrp-rdr.toc: 512
Term Rslt_37Args = makePair(APP, 0, Rslt_36, SUB);
Term Rslt_37 = newTerm(VAR, 0, portLoc(2, Rslt_37Args));
#ifdef STRICT
pushRedex(Rslt_37Args, seq_10);
#else
swap(termLoc(Rslt_37), makePair(LAZ, 0, Rslt_37Args, seq_10));
#endif

// link args to body
swap(r_seq_6, Rslt_37);

Term Rslt_37V = get(termLoc(Rslt_37));
apps = take(portLoc(1, Rslt_37V));
lams = take(portLoc(2, Rslt_37V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_37), makePair(LAZ, 0, apps, lams));
// call read-float at /home/jim/toccata/interpreter/intrp-rdr.toc: 511
Term Rslt_38Args = makePair(APP, 0, state_1, SUB);
Term Rslt_38 = newTerm(VAR, 0, portLoc(2, Rslt_38Args));
#ifdef STRICT
pushRedex(Rslt_38Args, glblFn1754);
#else
swap(termLoc(Rslt_38), makePair(LAZ, 0, Rslt_38Args, glblFn1754));
#endif

// call  at /home/jim/toccata/interpreter/intrp-rdr.toc: 511
Term Rslt_39Args = makePair(APP, 0, Rslt_38, SUB);
Term Rslt_39 = newTerm(VAR, 0, portLoc(2, Rslt_39Args));
#ifdef STRICT
pushRedex(Rslt_39Args, seq_6);
#else
swap(termLoc(Rslt_39), makePair(LAZ, 0, Rslt_39Args, seq_6));
#endif

// link args to body
swap(r_seq_2, Rslt_39);

Term Rslt_39V = get(termLoc(Rslt_39));
apps = take(portLoc(1, Rslt_39V));
lams = take(portLoc(2, Rslt_39V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_39), makePair(LAZ, 0, apps, lams));
pushRedex(args, seq_2);
return;
}
Term glblFn1812 = newRef(glblCFn1811);
Term glblFn1814;
void glblCFn1813(Term ref, Term args) {
Term apps, lams, V;
Location vLoc;
Term seq_2 = makePair(LAM, 0, SUB, NUL);
Location r_seq_2 = portLoc(2, seq_2);
Term state_1 = newTerm(VAR, 0, portLoc(1, seq_2));
// allocate args at /home/jim/toccata/interpreter/intrp-rdr.toc: 522
Term seq_6 = makePair(LAM, 0, SUB, NUL);
Location r_seq_6 = portLoc(2, seq_6);
Term r_5 = newTerm(VAR, 0, portLoc(1, seq_6));
// allocate args at /home/jim/toccata/interpreter/intrp-rdr.toc: 523

Term r_5_1;
r_5 = dupeArg(r_5, &r_5_1, 0);
Term seq_10 = makePair(LAM, 0, SUB, NUL);
Location r_seq_10 = portLoc(2, seq_10);
Term content_9 = newTerm(VAR, 0, portLoc(1, seq_10));
// allocate args at /home/jim/toccata/interpreter/intrp-rdr.toc: 524
Term seq_14 = makePair(LAM, 0, SUB, NUL);
Location r_seq_14 = portLoc(2, seq_14);
Term s2_13 = newTerm(VAR, 0, portLoc(1, seq_14));
// allocate args at /home/jim/toccata/interpreter/intrp-rdr.toc: 525

Term s2_13_1;
s2_13 = dupeArg(s2_13, &s2_13_1, 0);
Term glblVal1581_1;
glblVal1581 = dupeArg(glblVal1581, &glblVal1581_1, 0);

Term glblVal1896_3;
glblVal1896 = dupeArg(glblVal1896, &glblVal1896_3, 0);

// call make-loc at /home/jim/toccata/interpreter/intrp-rdr.toc: 526
Term Rslt_16Args = makePair(APP, 0, s2_13, SUB);
Term Rslt_16 = newTerm(VAR, 0, portLoc(2, Rslt_16Args));
#ifdef STRICT
pushRedex(Rslt_16Args, glblFn1762);
#else
swap(termLoc(Rslt_16), makePair(LAZ, 0, Rslt_16Args, glblFn1762));
#endif

// call ast/StringLit at /home/jim/toccata/interpreter/intrp-rdr.toc: 526
Term Rslt_17Args = makePair(APP, 0, Rslt_16, SUB);
Term Rslt_17 = newTerm(VAR, 0, portLoc(2, Rslt_17Args));
Rslt_17Args = makePair(APP, 0, content_9, Rslt_17Args);
#ifdef STRICT
pushRedex(Rslt_17Args, glblVal1581_1);
#else
swap(termLoc(Rslt_17), makePair(LAZ, 0, Rslt_17Args, glblVal1581_1));
#endif

// call ParserMatch at /home/jim/toccata/interpreter/intrp-rdr.toc: 526
Term Rslt_18Args = makePair(APP, 0, s2_13_1, SUB);
Term Rslt_18 = newTerm(VAR, 0, portLoc(2, Rslt_18Args));
Rslt_18Args = makePair(APP, 0, Rslt_17, Rslt_18Args);
#ifdef STRICT
pushRedex(Rslt_18Args, glblVal1896_3);
#else
swap(termLoc(Rslt_18), makePair(LAZ, 0, Rslt_18Args, glblVal1896_3));
#endif

// link args to body
swap(r_seq_14, Rslt_18);

Term Rslt_18V = get(termLoc(Rslt_18));
apps = take(portLoc(1, Rslt_18V));
lams = take(portLoc(2, Rslt_18V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_18), makePair(LAZ, 0, apps, lams));
// call elt1 at /home/jim/toccata/interpreter/intrp-rdr.toc: 525
Term Rslt_19Args = makePair(APP, 0, r_5, SUB);
Term Rslt_19 = newTerm(VAR, 0, portLoc(2, Rslt_19Args));
#ifdef STRICT
pushRedex(Rslt_19Args, glblFn1744);
#else
swap(termLoc(Rslt_19), makePair(LAZ, 0, Rslt_19Args, glblFn1744));
#endif

// call  at /home/jim/toccata/interpreter/intrp-rdr.toc: 525
Term Rslt_20Args = makePair(APP, 0, Rslt_19, SUB);
Term Rslt_20 = newTerm(VAR, 0, portLoc(2, Rslt_20Args));
#ifdef STRICT
pushRedex(Rslt_20Args, seq_14);
#else
swap(termLoc(Rslt_20), makePair(LAZ, 0, Rslt_20Args, seq_14));
#endif

// link args to body
swap(r_seq_10, Rslt_20);

Term Rslt_20V = get(termLoc(Rslt_20));
apps = take(portLoc(1, Rslt_20V));
lams = take(portLoc(2, Rslt_20V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_20), makePair(LAZ, 0, apps, lams));
// call elt0 at /home/jim/toccata/interpreter/intrp-rdr.toc: 524
Term Rslt_21Args = makePair(APP, 0, r_5_1, SUB);
Term Rslt_21 = newTerm(VAR, 0, portLoc(2, Rslt_21Args));
#ifdef STRICT
pushRedex(Rslt_21Args, glblFn1742);
#else
swap(termLoc(Rslt_21), makePair(LAZ, 0, Rslt_21Args, glblFn1742));
#endif

// call  at /home/jim/toccata/interpreter/intrp-rdr.toc: 524
Term Rslt_22Args = makePair(APP, 0, Rslt_21, SUB);
Term Rslt_22 = newTerm(VAR, 0, portLoc(2, Rslt_22Args));
#ifdef STRICT
pushRedex(Rslt_22Args, seq_10);
#else
swap(termLoc(Rslt_22), makePair(LAZ, 0, Rslt_22Args, seq_10));
#endif

// link args to body
swap(r_seq_6, Rslt_22);

Term Rslt_22V = get(termLoc(Rslt_22));
apps = take(portLoc(1, Rslt_22V));
lams = take(portLoc(2, Rslt_22V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_22), makePair(LAZ, 0, apps, lams));
// call read-string at /home/jim/toccata/interpreter/intrp-rdr.toc: 523
Term Rslt_23Args = makePair(APP, 0, state_1, SUB);
Term Rslt_23 = newTerm(VAR, 0, portLoc(2, Rslt_23Args));
#ifdef STRICT
pushRedex(Rslt_23Args, glblFn1748);
#else
swap(termLoc(Rslt_23), makePair(LAZ, 0, Rslt_23Args, glblFn1748));
#endif

// call  at /home/jim/toccata/interpreter/intrp-rdr.toc: 523
Term Rslt_24Args = makePair(APP, 0, Rslt_23, SUB);
Term Rslt_24 = newTerm(VAR, 0, portLoc(2, Rslt_24Args));
#ifdef STRICT
pushRedex(Rslt_24Args, seq_6);
#else
swap(termLoc(Rslt_24), makePair(LAZ, 0, Rslt_24Args, seq_6));
#endif

// link args to body
swap(r_seq_2, Rslt_24);

Term Rslt_24V = get(termLoc(Rslt_24));
apps = take(portLoc(1, Rslt_24V));
lams = take(portLoc(2, Rslt_24V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_24), makePair(LAZ, 0, apps, lams));
pushRedex(args, seq_2);
return;
}
Term glblFn1814 = newRef(glblCFn1813);
Term glblFn1816;
void glblCFn1815(Term ref, Term args) {
Term apps, lams, V;
Location vLoc;
Term seq_2 = makePair(LAM, 0, SUB, NUL);
Location r_seq_2 = portLoc(2, seq_2);
Term state_1 = newTerm(VAR, 0, portLoc(1, seq_2));
// allocate args at /home/jim/toccata/interpreter/intrp-rdr.toc: 529
Term seq_6 = makePair(LAM, 0, SUB, NUL);
Location r_seq_6 = portLoc(2, seq_6);
Term r_5 = newTerm(VAR, 0, portLoc(1, seq_6));
// allocate args at /home/jim/toccata/interpreter/intrp-rdr.toc: 530

Term r_5_1;
r_5 = dupeArg(r_5, &r_5_1, 0);
Term seq_10 = makePair(LAM, 0, SUB, NUL);
Location r_seq_10 = portLoc(2, seq_10);
Term text_9 = newTerm(VAR, 0, portLoc(1, seq_10));
// allocate args at /home/jim/toccata/interpreter/intrp-rdr.toc: 531
Term seq_14 = makePair(LAM, 0, SUB, NUL);
Location r_seq_14 = portLoc(2, seq_14);
Term s2_13 = newTerm(VAR, 0, portLoc(1, seq_14));
// allocate args at /home/jim/toccata/interpreter/intrp-rdr.toc: 532

Term s2_13_1;
s2_13 = dupeArg(s2_13, &s2_13_1, 0);
Term glblVal1581_2;
glblVal1581 = dupeArg(glblVal1581, &glblVal1581_2, 0);

Term glblVal1896_4;
glblVal1896 = dupeArg(glblVal1896, &glblVal1896_4, 0);

// call make-loc at /home/jim/toccata/interpreter/intrp-rdr.toc: 533
Term Rslt_16Args = makePair(APP, 0, s2_13, SUB);
Term Rslt_16 = newTerm(VAR, 0, portLoc(2, Rslt_16Args));
#ifdef STRICT
pushRedex(Rslt_16Args, glblFn1762);
#else
swap(termLoc(Rslt_16), makePair(LAZ, 0, Rslt_16Args, glblFn1762));
#endif

// call ast/StringLit at /home/jim/toccata/interpreter/intrp-rdr.toc: 533
Term Rslt_17Args = makePair(APP, 0, Rslt_16, SUB);
Term Rslt_17 = newTerm(VAR, 0, portLoc(2, Rslt_17Args));
Rslt_17Args = makePair(APP, 0, text_9, Rslt_17Args);
#ifdef STRICT
pushRedex(Rslt_17Args, glblVal1581_2);
#else
swap(termLoc(Rslt_17), makePair(LAZ, 0, Rslt_17Args, glblVal1581_2));
#endif

// call ParserMatch at /home/jim/toccata/interpreter/intrp-rdr.toc: 533
Term Rslt_18Args = makePair(APP, 0, s2_13_1, SUB);
Term Rslt_18 = newTerm(VAR, 0, portLoc(2, Rslt_18Args));
Rslt_18Args = makePair(APP, 0, Rslt_17, Rslt_18Args);
#ifdef STRICT
pushRedex(Rslt_18Args, glblVal1896_4);
#else
swap(termLoc(Rslt_18), makePair(LAZ, 0, Rslt_18Args, glblVal1896_4));
#endif

// link args to body
swap(r_seq_14, Rslt_18);

Term Rslt_18V = get(termLoc(Rslt_18));
apps = take(portLoc(1, Rslt_18V));
lams = take(portLoc(2, Rslt_18V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_18), makePair(LAZ, 0, apps, lams));
// call elt1 at /home/jim/toccata/interpreter/intrp-rdr.toc: 532
Term Rslt_19Args = makePair(APP, 0, r_5, SUB);
Term Rslt_19 = newTerm(VAR, 0, portLoc(2, Rslt_19Args));
#ifdef STRICT
pushRedex(Rslt_19Args, glblFn1744);
#else
swap(termLoc(Rslt_19), makePair(LAZ, 0, Rslt_19Args, glblFn1744));
#endif

// call  at /home/jim/toccata/interpreter/intrp-rdr.toc: 532
Term Rslt_20Args = makePair(APP, 0, Rslt_19, SUB);
Term Rslt_20 = newTerm(VAR, 0, portLoc(2, Rslt_20Args));
#ifdef STRICT
pushRedex(Rslt_20Args, seq_14);
#else
swap(termLoc(Rslt_20), makePair(LAZ, 0, Rslt_20Args, seq_14));
#endif

// link args to body
swap(r_seq_10, Rslt_20);

Term Rslt_20V = get(termLoc(Rslt_20));
apps = take(portLoc(1, Rslt_20V));
lams = take(portLoc(2, Rslt_20V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_20), makePair(LAZ, 0, apps, lams));
// call elt0 at /home/jim/toccata/interpreter/intrp-rdr.toc: 531
Term Rslt_21Args = makePair(APP, 0, r_5_1, SUB);
Term Rslt_21 = newTerm(VAR, 0, portLoc(2, Rslt_21Args));
#ifdef STRICT
pushRedex(Rslt_21Args, glblFn1742);
#else
swap(termLoc(Rslt_21), makePair(LAZ, 0, Rslt_21Args, glblFn1742));
#endif

// call  at /home/jim/toccata/interpreter/intrp-rdr.toc: 531
Term Rslt_22Args = makePair(APP, 0, Rslt_21, SUB);
Term Rslt_22 = newTerm(VAR, 0, portLoc(2, Rslt_22Args));
#ifdef STRICT
pushRedex(Rslt_22Args, seq_10);
#else
swap(termLoc(Rslt_22), makePair(LAZ, 0, Rslt_22Args, seq_10));
#endif

// link args to body
swap(r_seq_6, Rslt_22);

Term Rslt_22V = get(termLoc(Rslt_22));
apps = take(portLoc(1, Rslt_22V));
lams = take(portLoc(2, Rslt_22V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_22), makePair(LAZ, 0, apps, lams));
// call read-symbol at /home/jim/toccata/interpreter/intrp-rdr.toc: 530
Term Rslt_23Args = makePair(APP, 0, state_1, SUB);
Term Rslt_23 = newTerm(VAR, 0, portLoc(2, Rslt_23Args));
#ifdef STRICT
pushRedex(Rslt_23Args, glblFn1750);
#else
swap(termLoc(Rslt_23), makePair(LAZ, 0, Rslt_23Args, glblFn1750));
#endif

// call  at /home/jim/toccata/interpreter/intrp-rdr.toc: 530
Term Rslt_24Args = makePair(APP, 0, Rslt_23, SUB);
Term Rslt_24 = newTerm(VAR, 0, portLoc(2, Rslt_24Args));
#ifdef STRICT
pushRedex(Rslt_24Args, seq_6);
#else
swap(termLoc(Rslt_24), makePair(LAZ, 0, Rslt_24Args, seq_6));
#endif

// link args to body
swap(r_seq_2, Rslt_24);

Term Rslt_24V = get(termLoc(Rslt_24));
apps = take(portLoc(1, Rslt_24V));
lams = take(portLoc(2, Rslt_24V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_24), makePair(LAZ, 0, apps, lams));
pushRedex(args, seq_2);
return;
}
Term glblFn1816 = newRef(glblCFn1815);
Term glblFn1818;
void glblCFn1817(Term ref, Term args) {
Term apps, lams, V;
Location vLoc;
Term seq_2 = makePair(LAM, 0, SUB, NUL);
Location r_seq_2 = portLoc(2, seq_2);
Term state_1 = newTerm(VAR, 0, portLoc(1, seq_2));
// allocate args at /home/jim/toccata/interpreter/intrp-rdr.toc: 536
Term seq_6 = makePair(LAM, 0, SUB, NUL);
Location r_seq_6 = portLoc(2, seq_6);
Term r_5 = newTerm(VAR, 0, portLoc(1, seq_6));
// allocate args at /home/jim/toccata/interpreter/intrp-rdr.toc: 537

Term r_5_3;
r_5 = dupeArg(r_5, &r_5_3, 0);

Term r_5_2;
r_5 = dupeArg(r_5, &r_5_2, 0);

Term r_5_1;
r_5 = dupeArg(r_5, &r_5_1, 0);
Term glblVal1560_9;
glblVal1560 = dupeArg(glblVal1560, &glblVal1560_9, 0);

Term glblVal1896_5;
glblVal1896 = dupeArg(glblVal1896, &glblVal1896_5, 0);

// call elt0 at /home/jim/toccata/interpreter/intrp-rdr.toc: 538
Term Rslt_8Args = makePair(APP, 0, r_5, SUB);
Term Rslt_8 = newTerm(VAR, 0, portLoc(2, Rslt_8Args));
#ifdef STRICT
pushRedex(Rslt_8Args, glblFn1742);
#else
swap(termLoc(Rslt_8), makePair(LAZ, 0, Rslt_8Args, glblFn1742));
#endif

// call elt1 at /home/jim/toccata/interpreter/intrp-rdr.toc: 538
Term Rslt_9Args = makePair(APP, 0, r_5_1, SUB);
Term Rslt_9 = newTerm(VAR, 0, portLoc(2, Rslt_9Args));
#ifdef STRICT
pushRedex(Rslt_9Args, glblFn1744);
#else
swap(termLoc(Rslt_9), makePair(LAZ, 0, Rslt_9Args, glblFn1744));
#endif

// call elt2 at /home/jim/toccata/interpreter/intrp-rdr.toc: 538
Term Rslt_10Args = makePair(APP, 0, r_5_2, SUB);
Term Rslt_10 = newTerm(VAR, 0, portLoc(2, Rslt_10Args));
#ifdef STRICT
pushRedex(Rslt_10Args, glblFn1756);
#else
swap(termLoc(Rslt_10), makePair(LAZ, 0, Rslt_10Args, glblFn1756));
#endif

// call make-loc at /home/jim/toccata/interpreter/intrp-rdr.toc: 538
Term Rslt_11Args = makePair(APP, 0, Rslt_10, SUB);
Term Rslt_11 = newTerm(VAR, 0, portLoc(2, Rslt_11Args));
#ifdef STRICT
pushRedex(Rslt_11Args, glblFn1762);
#else
swap(termLoc(Rslt_11), makePair(LAZ, 0, Rslt_11Args, glblFn1762));
#endif

// call ast/Symbol at /home/jim/toccata/interpreter/intrp-rdr.toc: 538
Term Rslt_12Args = makePair(APP, 0, Rslt_11, SUB);
Term Rslt_12 = newTerm(VAR, 0, portLoc(2, Rslt_12Args));
Rslt_12Args = makePair(APP, 0, Rslt_9, Rslt_12Args);
Rslt_12Args = makePair(APP, 0, Rslt_8, Rslt_12Args);
#ifdef STRICT
pushRedex(Rslt_12Args, glblVal1560_9);
#else
swap(termLoc(Rslt_12), makePair(LAZ, 0, Rslt_12Args, glblVal1560_9));
#endif

// call elt2 at /home/jim/toccata/interpreter/intrp-rdr.toc: 539
Term Rslt_13Args = makePair(APP, 0, r_5_3, SUB);
Term Rslt_13 = newTerm(VAR, 0, portLoc(2, Rslt_13Args));
#ifdef STRICT
pushRedex(Rslt_13Args, glblFn1756);
#else
swap(termLoc(Rslt_13), makePair(LAZ, 0, Rslt_13Args, glblFn1756));
#endif

// call ParserMatch at /home/jim/toccata/interpreter/intrp-rdr.toc: 538
Term Rslt_14Args = makePair(APP, 0, Rslt_13, SUB);
Term Rslt_14 = newTerm(VAR, 0, portLoc(2, Rslt_14Args));
Rslt_14Args = makePair(APP, 0, Rslt_12, Rslt_14Args);
#ifdef STRICT
pushRedex(Rslt_14Args, glblVal1896_5);
#else
swap(termLoc(Rslt_14), makePair(LAZ, 0, Rslt_14Args, glblVal1896_5));
#endif

// link args to body
swap(r_seq_6, Rslt_14);

Term Rslt_14V = get(termLoc(Rslt_14));
apps = take(portLoc(1, Rslt_14V));
lams = take(portLoc(2, Rslt_14V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_14), makePair(LAZ, 0, apps, lams));
// call read-full-symbol at /home/jim/toccata/interpreter/intrp-rdr.toc: 537
Term Rslt_15Args = makePair(APP, 0, state_1, SUB);
Term Rslt_15 = newTerm(VAR, 0, portLoc(2, Rslt_15Args));
#ifdef STRICT
pushRedex(Rslt_15Args, glblFn1766);
#else
swap(termLoc(Rslt_15), makePair(LAZ, 0, Rslt_15Args, glblFn1766));
#endif

// call  at /home/jim/toccata/interpreter/intrp-rdr.toc: 537
Term Rslt_16Args = makePair(APP, 0, Rslt_15, SUB);
Term Rslt_16 = newTerm(VAR, 0, portLoc(2, Rslt_16Args));
#ifdef STRICT
pushRedex(Rslt_16Args, seq_6);
#else
swap(termLoc(Rslt_16), makePair(LAZ, 0, Rslt_16Args, seq_6));
#endif

// link args to body
swap(r_seq_2, Rslt_16);

Term Rslt_16V = get(termLoc(Rslt_16));
apps = take(portLoc(1, Rslt_16V));
lams = take(portLoc(2, Rslt_16V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_16), makePair(LAZ, 0, apps, lams));
pushRedex(args, seq_2);
return;
}
Term glblFn1818 = newRef(glblCFn1817);
Term glblVal1819;
struct {TYPE_SIZE type;
        REFS_SIZE refs;
        int64_t *hash;
        Value *parent;
        int64_t len;
        char buffer[24];
} str36 = {StringBufferType, REFS_STATIC, 0, 0, 23, "unexpected end of input"};
Term glblStr2384 = newTerm_(VAL, (Term)&str36);
void glblProto20_2385(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(561), args);
  args = makePair(APP, 0, glblStr1912, args);
  interact(args, glblProto20);
}
}
void glblProto1889_2387(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(563), args);
  args = makePair(APP, 0, glblStr1912, args);
  interact(args, glblProto1889);
}
}
void glblProto1562_2389(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(563), args);
  args = makePair(APP, 0, glblStr1912, args);
  interact(args, glblProto1562);
}
}
void glblProto34_2391(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(561), args);
  args = makePair(APP, 0, glblStr1912, args);
  interact(args, glblProto34);
}
}
void glblProto1757_2393(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(559), args);
  args = makePair(APP, 0, glblStr1912, args);
  interact(args, glblProto1757);
}
}
void glblProto34_2395(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(554), args);
  args = makePair(APP, 0, glblStr1912, args);
  interact(args, glblProto34);
}
}
void glblProto34_2397(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(554), args);
  args = makePair(APP, 0, glblStr1912, args);
  interact(args, glblProto34);
}
}
Term glblFn1821;
void glblCFn1820(Term ref, Term args) {
Term apps, lams, V;
Location vLoc;
Term seq_4 = makePair(LAM, 0, SUB, NUL);
Location r_seq_4 = portLoc(2, seq_4);
Term acc_3 = newTerm(VAR, 0, portLoc(1, seq_4));
seq_4 = makePair(LAM, 0, SUB, seq_4);
Term delim_2 = newTerm(VAR, 0, portLoc(1, seq_4));
seq_4 = makePair(LAM, 0, SUB, seq_4);
Term state_1 = newTerm(VAR, 0, portLoc(1, seq_4));
// allocate args at /home/jim/toccata/interpreter/intrp-rdr.toc: 550

Term delim_2_1;
delim_2 = dupeArg(delim_2, &delim_2_1, 341);

Term acc_3_1;
acc_3 = dupeArg(acc_3, &acc_3_1, 342);
Term seq_8 = makePair(LAM, 0, SUB, NUL);
Location r_seq_8 = portLoc(2, seq_8);
Term s_7 = newTerm(VAR, 0, portLoc(1, seq_8));
// allocate args at /home/jim/toccata/interpreter/intrp-rdr.toc: 551

Term s_7_3;
s_7 = dupeArg(s_7, &s_7_3, 0);

Term s_7_2;
s_7 = dupeArg(s_7, &s_7_2, 0);

Term s_7_1;
s_7 = dupeArg(s_7, &s_7_1, 0);
Term seq_12 = makePair(LAM, 0, SUB, NUL);
Location r_seq_12 = portLoc(2, seq_12);
Term c_11 = newTerm(VAR, 0, portLoc(1, seq_12));
// allocate args at /home/jim/toccata/interpreter/intrp-rdr.toc: 552

Term c_11_1;
c_11 = dupeArg(c_11, &c_11_1, 0);
Term glblVal1819_1;
glblVal1819 = dupeArg(glblVal1819, &glblVal1819_1, 0);

Term seq_21 = makePair(LAM, 0, SUB, NUL);
Location r_seq_21 = portLoc(2, seq_21);
Term r_20 = newTerm(VAR, 0, portLoc(1, seq_21));
// allocate args at /home/jim/toccata/interpreter/intrp-rdr.toc: 558

Term r_20_3;
r_20 = dupeArg(r_20, &r_20_3, 0);

Term r_20_2;
r_20 = dupeArg(r_20, &r_20_2, 0);

Term r_20_1;
r_20 = dupeArg(r_20, &r_20_1, 0);
Term seq_25 = makePair(LAM, 0, SUB, NUL);
Location r_seq_25 = portLoc(2, seq_25);
Term k_24 = newTerm(VAR, 0, portLoc(1, seq_25));
// allocate args at /home/jim/toccata/interpreter/intrp-rdr.toc: 559
Term glblVal1908_1;
glblVal1908 = dupeArg(glblVal1908, &glblVal1908_1, 0);

Term glblVal1896_6;
glblVal1896 = dupeArg(glblVal1896, &glblVal1896_6, 0);

// call str= at /home/jim/toccata/interpreter/intrp-rdr.toc: 554
Term Rslt_14Args = makePair(APP, 0, delim_2, SUB);
Term Rslt_14 = newTerm(VAR, 0, portLoc(2, Rslt_14Args));
Rslt_14Args = makePair(APP, 0, c_11, Rslt_14Args);
#ifdef STRICT
pushRedex(Rslt_14Args, glblFn363);
#else
swap(termLoc(Rslt_14), makePair(LAZ, 0, Rslt_14Args, glblFn363));
#endif

// call take-char at /home/jim/toccata/interpreter/intrp-rdr.toc: 555
Term Rslt_15Args = makePair(APP, 0, s_7, SUB);
Term Rslt_15 = newTerm(VAR, 0, portLoc(2, Rslt_15Args));
#ifdef STRICT
pushRedex(Rslt_15Args, glblFn1732);
#else
swap(termLoc(Rslt_15), makePair(LAZ, 0, Rslt_15Args, glblFn1732));
#endif

// call ParserMatch at /home/jim/toccata/interpreter/intrp-rdr.toc: 555
Term Rslt_16Args = makePair(APP, 0, Rslt_15, SUB);
Term Rslt_16 = newTerm(VAR, 0, portLoc(2, Rslt_16Args));
Rslt_16Args = makePair(APP, 0, acc_3, Rslt_16Args);
#ifdef STRICT
pushRedex(Rslt_16Args, glblVal1896_6);
#else
swap(termLoc(Rslt_16), makePair(LAZ, 0, Rslt_16Args, glblVal1896_6));
#endif

// call str= at /home/jim/toccata/interpreter/intrp-rdr.toc: 556
Term Rslt_17Args = makePair(APP, 0, glblStr402, SUB);
Term Rslt_17 = newTerm(VAR, 0, portLoc(2, Rslt_17Args));
Rslt_17Args = makePair(APP, 0, c_11_1, Rslt_17Args);
#ifdef STRICT
pushRedex(Rslt_17Args, glblFn363);
#else
swap(termLoc(Rslt_17), makePair(LAZ, 0, Rslt_17Args, glblFn363));
#endif

// call ParserError at /home/jim/toccata/interpreter/intrp-rdr.toc: 557
Term Rslt_18Args = makePair(APP, 0, s_7_1, SUB);
Term Rslt_18 = newTerm(VAR, 0, portLoc(2, Rslt_18Args));
Rslt_18Args = makePair(APP, 0, glblStr2384, Rslt_18Args);
#ifdef STRICT
pushRedex(Rslt_18Args, glblVal1908_1);
#else
swap(termLoc(Rslt_18), makePair(LAZ, 0, Rslt_18Args, glblVal1908_1));
#endif

// call = at /home/jim/toccata/interpreter/intrp-rdr.toc: 561
Term Rslt_27Args = makePair(APP, 0, newI60(2), SUB);
Term Rslt_27 = newTerm(VAR, 0, portLoc(2, Rslt_27Args));
Rslt_27Args = makePair(APP, 0, k_24, Rslt_27Args);
#ifdef STRICT
pushRedex(Rslt_27Args, newRef(glblProto20_2385));
#else
swap(termLoc(Rslt_27), makePair(LAZ, 0, Rslt_27Args, newRef(glblProto20_2385)));
#endif
// call .state at /home/jim/toccata/interpreter/intrp-rdr.toc: 563
Term Rslt_28Args = makePair(APP, 0, r_20_1, SUB);
Term Rslt_28 = newTerm(VAR, 0, portLoc(2, Rslt_28Args));
#ifdef STRICT
pushRedex(Rslt_28Args, newRef(glblProto1889_2387));
#else
swap(termLoc(Rslt_28), makePair(LAZ, 0, Rslt_28Args, newRef(glblProto1889_2387)));
#endif
// call .value at /home/jim/toccata/interpreter/intrp-rdr.toc: 563
Term Rslt_29Args = makePair(APP, 0, r_20_2, SUB);
Term Rslt_29 = newTerm(VAR, 0, portLoc(2, Rslt_29Args));
#ifdef STRICT
pushRedex(Rslt_29Args, newRef(glblProto1562_2389));
#else
swap(termLoc(Rslt_29), makePair(LAZ, 0, Rslt_29Args, newRef(glblProto1562_2389)));
#endif
// call vect-conj at /home/jim/toccata/interpreter/intrp-rdr.toc: 563
Term Rslt_30Args = makePair(APP, 0, Rslt_29, SUB);
Term Rslt_30 = newTerm(VAR, 0, portLoc(2, Rslt_30Args));
Rslt_30Args = makePair(APP, 0, acc_3_1, Rslt_30Args);
#ifdef STRICT
pushRedex(Rslt_30Args, glblFn178);
#else
swap(termLoc(Rslt_30), makePair(LAZ, 0, Rslt_30Args, glblFn178));
#endif

// call parse-expr-list-acc at /home/jim/toccata/interpreter/intrp-rdr.toc: 550
Term Rslt_31Args = makePair(APP, 0, Rslt_30, SUB);
Term Rslt_31 = newTerm(VAR, 0, portLoc(2, Rslt_31Args));
Rslt_31Args = makePair(APP, 0, delim_2_1, Rslt_31Args);
Rslt_31Args = makePair(APP, 0, Rslt_28, Rslt_31Args);
#ifdef STRICT
pushRedex(Rslt_31Args, glblFn1821);
#else
swap(termLoc(Rslt_31), makePair(LAZ, 0, Rslt_31Args, glblFn1821));
#endif

// call cond at /home/jim/toccata/interpreter/intrp-rdr.toc: 561
Term Rslt_32Args = makePair(APP, 0, Rslt_31, SUB);
Term Rslt_32 = newTerm(VAR, 0, portLoc(2, Rslt_32Args));
Rslt_32Args = makePair(APP, 0, r_20, Rslt_32Args);
Rslt_32Args = makePair(APP, 0, Rslt_27, Rslt_32Args);
#ifdef STRICT
pushRedex(Rslt_32Args, newRef(glblProto34_2391));
#else
swap(termLoc(Rslt_32), makePair(LAZ, 0, Rslt_32Args, newRef(glblProto34_2391)));
#endif
// link args to body
swap(r_seq_25, Rslt_32);

Term Rslt_32V = get(termLoc(Rslt_32));
apps = take(portLoc(1, Rslt_32V));
lams = take(portLoc(2, Rslt_32V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_32), makePair(LAZ, 0, apps, lams));
// call result-kind at /home/jim/toccata/interpreter/intrp-rdr.toc: 559
Term Rslt_33Args = makePair(APP, 0, r_20_3, SUB);
Term Rslt_33 = newTerm(VAR, 0, portLoc(2, Rslt_33Args));
#ifdef STRICT
pushRedex(Rslt_33Args, newRef(glblProto1757_2393));
#else
swap(termLoc(Rslt_33), makePair(LAZ, 0, Rslt_33Args, newRef(glblProto1757_2393)));
#endif
// call  at /home/jim/toccata/interpreter/intrp-rdr.toc: 559
Term Rslt_34Args = makePair(APP, 0, Rslt_33, SUB);
Term Rslt_34 = newTerm(VAR, 0, portLoc(2, Rslt_34Args));
#ifdef STRICT
pushRedex(Rslt_34Args, seq_25);
#else
swap(termLoc(Rslt_34), makePair(LAZ, 0, Rslt_34Args, seq_25));
#endif

// link args to body
swap(r_seq_21, Rslt_34);

Term Rslt_34V = get(termLoc(Rslt_34));
apps = take(portLoc(1, Rslt_34V));
lams = take(portLoc(2, Rslt_34V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_34), makePair(LAZ, 0, apps, lams));
// call parse-expr at /home/jim/toccata/interpreter/intrp-rdr.toc: 548
Term Rslt_35Args = makePair(APP, 0, s_7_2, SUB);
Term Rslt_35 = newTerm(VAR, 0, portLoc(2, Rslt_35Args));
#ifdef STRICT
pushRedex(Rslt_35Args, glblVal1819_1);
#else
swap(termLoc(Rslt_35), makePair(LAZ, 0, Rslt_35Args, glblVal1819_1));
#endif

// call  at /home/jim/toccata/interpreter/intrp-rdr.toc: 548
Term Rslt_36Args = makePair(APP, 0, Rslt_35, SUB);
Term Rslt_36 = newTerm(VAR, 0, portLoc(2, Rslt_36Args));
#ifdef STRICT
pushRedex(Rslt_36Args, seq_21);
#else
swap(termLoc(Rslt_36), makePair(LAZ, 0, Rslt_36Args, seq_21));
#endif

// call cond at /home/jim/toccata/interpreter/intrp-rdr.toc: 554
Term Rslt_37Args = makePair(APP, 0, Rslt_36, SUB);
Term Rslt_37 = newTerm(VAR, 0, portLoc(2, Rslt_37Args));
Rslt_37Args = makePair(APP, 0, Rslt_18, Rslt_37Args);
Rslt_37Args = makePair(APP, 0, Rslt_17, Rslt_37Args);
#ifdef STRICT
pushRedex(Rslt_37Args, newRef(glblProto34_2395));
#else
swap(termLoc(Rslt_37), makePair(LAZ, 0, Rslt_37Args, newRef(glblProto34_2395)));
#endif
// call cond at /home/jim/toccata/interpreter/intrp-rdr.toc: 554
Term Rslt_38Args = makePair(APP, 0, Rslt_37, SUB);
Term Rslt_38 = newTerm(VAR, 0, portLoc(2, Rslt_38Args));
Rslt_38Args = makePair(APP, 0, Rslt_16, Rslt_38Args);
Rslt_38Args = makePair(APP, 0, Rslt_14, Rslt_38Args);
#ifdef STRICT
pushRedex(Rslt_38Args, newRef(glblProto34_2397));
#else
swap(termLoc(Rslt_38), makePair(LAZ, 0, Rslt_38Args, newRef(glblProto34_2397)));
#endif
// link args to body
swap(r_seq_12, Rslt_38);

Term Rslt_38V = get(termLoc(Rslt_38));
apps = take(portLoc(1, Rslt_38V));
lams = take(portLoc(2, Rslt_38V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_38), makePair(LAZ, 0, apps, lams));
// call peek-char at /home/jim/toccata/interpreter/intrp-rdr.toc: 552
Term Rslt_39Args = makePair(APP, 0, s_7_3, SUB);
Term Rslt_39 = newTerm(VAR, 0, portLoc(2, Rslt_39Args));
#ifdef STRICT
pushRedex(Rslt_39Args, glblFn1730);
#else
swap(termLoc(Rslt_39), makePair(LAZ, 0, Rslt_39Args, glblFn1730));
#endif

// call  at /home/jim/toccata/interpreter/intrp-rdr.toc: 552
Term Rslt_40Args = makePair(APP, 0, Rslt_39, SUB);
Term Rslt_40 = newTerm(VAR, 0, portLoc(2, Rslt_40Args));
#ifdef STRICT
pushRedex(Rslt_40Args, seq_12);
#else
swap(termLoc(Rslt_40), makePair(LAZ, 0, Rslt_40Args, seq_12));
#endif

// link args to body
swap(r_seq_8, Rslt_40);

Term Rslt_40V = get(termLoc(Rslt_40));
apps = take(portLoc(1, Rslt_40V));
lams = take(portLoc(2, Rslt_40V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_40), makePair(LAZ, 0, apps, lams));
// call skip-whitespace at /home/jim/toccata/interpreter/intrp-rdr.toc: 551
Term Rslt_41Args = makePair(APP, 0, state_1, SUB);
Term Rslt_41 = newTerm(VAR, 0, portLoc(2, Rslt_41Args));
#ifdef STRICT
pushRedex(Rslt_41Args, glblFn1736);
#else
swap(termLoc(Rslt_41), makePair(LAZ, 0, Rslt_41Args, glblFn1736));
#endif

// call  at /home/jim/toccata/interpreter/intrp-rdr.toc: 551
Term Rslt_42Args = makePair(APP, 0, Rslt_41, SUB);
Term Rslt_42 = newTerm(VAR, 0, portLoc(2, Rslt_42Args));
#ifdef STRICT
pushRedex(Rslt_42Args, seq_8);
#else
swap(termLoc(Rslt_42), makePair(LAZ, 0, Rslt_42Args, seq_8));
#endif

// link args to body
swap(r_seq_4, Rslt_42);

Term Rslt_42V = get(termLoc(Rslt_42));
apps = take(portLoc(1, Rslt_42V));
lams = take(portLoc(2, Rslt_42V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_42), makePair(LAZ, 0, apps, lams));
pushRedex(args, seq_4);
return;
}
Term glblFn1821 = newRef(glblCFn1820);
Term glblFn1823;
void glblCFn1822(Term ref, Term args) {
Term apps, lams, V;
Location vLoc;
Term seq_3 = makePair(LAM, 0, SUB, NUL);
Location r_seq_3 = portLoc(2, seq_3);
Term delim_2 = newTerm(VAR, 0, portLoc(1, seq_3));
seq_3 = makePair(LAM, 0, SUB, seq_3);
Term state_1 = newTerm(VAR, 0, portLoc(1, seq_3));
// allocate args at /home/jim/toccata/interpreter/intrp-rdr.toc: 567
Term glblVal175_63;
glblVal175 = dupeArg(glblVal175, &glblVal175_63, 0);

// call parse-expr-list-acc at /home/jim/toccata/interpreter/intrp-rdr.toc: 568
Term Rslt_5Args = makePair(APP, 0, glblVal175_63, SUB);
Term Rslt_5 = newTerm(VAR, 0, portLoc(2, Rslt_5Args));
Rslt_5Args = makePair(APP, 0, delim_2, Rslt_5Args);
Rslt_5Args = makePair(APP, 0, state_1, Rslt_5Args);
#ifdef STRICT
pushRedex(Rslt_5Args, glblFn1821);
#else
swap(termLoc(Rslt_5), makePair(LAZ, 0, Rslt_5Args, glblFn1821));
#endif

// link args to body
swap(r_seq_3, Rslt_5);

Term Rslt_5V = get(termLoc(Rslt_5));
apps = take(portLoc(1, Rslt_5V));
lams = take(portLoc(2, Rslt_5V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_5), makePair(LAZ, 0, apps, lams));
pushRedex(args, seq_3);
return;
}
Term glblFn1823 = newRef(glblCFn1822);
void glblProto1889_2403(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(580), args);
  args = makePair(APP, 0, glblStr1912, args);
  interact(args, glblProto1889);
}
}
void glblProto1562_2405(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(580), args);
  args = makePair(APP, 0, glblStr1912, args);
  interact(args, glblProto1562);
}
}
void glblProto34_2407(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(574), args);
  args = makePair(APP, 0, glblStr1912, args);
  interact(args, glblProto34);
}
}
void glblProto34_2409(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(574), args);
  args = makePair(APP, 0, glblStr1912, args);
  interact(args, glblProto34);
}
}
Term glblFn1825;
void glblCFn1824(Term ref, Term args) {
Term apps, lams, V;
Location vLoc;
Term seq_4 = makePair(LAM, 0, SUB, NUL);
Location r_seq_4 = portLoc(2, seq_4);
Term acc_3 = newTerm(VAR, 0, portLoc(1, seq_4));
seq_4 = makePair(LAM, 0, SUB, seq_4);
Term tcs_2 = newTerm(VAR, 0, portLoc(1, seq_4));
seq_4 = makePair(LAM, 0, SUB, seq_4);
Term state_1 = newTerm(VAR, 0, portLoc(1, seq_4));
// allocate args at /home/jim/toccata/interpreter/intrp-rdr.toc: 570

Term tcs_2_2;
tcs_2 = dupeArg(tcs_2, &tcs_2_2, 343);

Term tcs_2_1;
tcs_2 = dupeArg(tcs_2, &tcs_2_1, 344);

Term acc_3_2;
acc_3 = dupeArg(acc_3, &acc_3_2, 345);

Term acc_3_1;
acc_3 = dupeArg(acc_3, &acc_3_1, 346);
Term seq_8 = makePair(LAM, 0, SUB, NUL);
Location r_seq_8 = portLoc(2, seq_8);
Term s_7 = newTerm(VAR, 0, portLoc(1, seq_8));
// allocate args at /home/jim/toccata/interpreter/intrp-rdr.toc: 571

Term s_7_3;
s_7 = dupeArg(s_7, &s_7_3, 0);

Term s_7_2;
s_7 = dupeArg(s_7, &s_7_2, 0);

Term s_7_1;
s_7 = dupeArg(s_7, &s_7_1, 0);
Term seq_12 = makePair(LAM, 0, SUB, NUL);
Location r_seq_12 = portLoc(2, seq_12);
Term c_11 = newTerm(VAR, 0, portLoc(1, seq_12));
// allocate args at /home/jim/toccata/interpreter/intrp-rdr.toc: 572

Term c_11_1;
c_11 = dupeArg(c_11, &c_11_1, 0);
Term glblVal1819_2;
glblVal1819 = dupeArg(glblVal1819, &glblVal1819_2, 0);

Term seq_32 = makePair(LAM, 0, SUB, NUL);
Location r_seq_32 = portLoc(2, seq_32);
Term r_31 = newTerm(VAR, 0, portLoc(1, seq_32));
// allocate args at /home/jim/toccata/interpreter/intrp-rdr.toc: 579

Term r_31_1;
r_31 = dupeArg(r_31, &r_31_1, 0);
Term seq_22 = makePair(LAM, 0, SUB, NUL);
Location r_seq_22 = portLoc(2, seq_22);
Term tc_21 = newTerm(VAR, 0, portLoc(1, seq_22));
// allocate args at /home/jim/toccata/interpreter/intrp-rdr.toc: 577

Term tc_21_1;
tc_21 = dupeArg(tc_21, &tc_21_1, 0);
Term glblVal175_64;
glblVal175 = dupeArg(glblVal175, &glblVal175_64, 0);

// call str= at /home/jim/toccata/interpreter/intrp-rdr.toc: 574
Term Rslt_14Args = makePair(APP, 0, glblStr254, SUB);
Term Rslt_14 = newTerm(VAR, 0, portLoc(2, Rslt_14Args));
Rslt_14Args = makePair(APP, 0, c_11, Rslt_14Args);
#ifdef STRICT
pushRedex(Rslt_14Args, glblFn363);
#else
swap(termLoc(Rslt_14), makePair(LAZ, 0, Rslt_14Args, glblFn363));
#endif

// call vect-conj at /home/jim/toccata/interpreter/intrp-rdr.toc: 575
Term Rslt_15Args = makePair(APP, 0, tcs_2, SUB);
Term Rslt_15 = newTerm(VAR, 0, portLoc(2, Rslt_15Args));
Rslt_15Args = makePair(APP, 0, glblVal175_64, Rslt_15Args);
#ifdef STRICT
pushRedex(Rslt_15Args, glblFn178);
#else
swap(termLoc(Rslt_15), makePair(LAZ, 0, Rslt_15Args, glblFn178));
#endif

// call vect-conj at /home/jim/toccata/interpreter/intrp-rdr.toc: 575
Term Rslt_16Args = makePair(APP, 0, acc_3, SUB);
Term Rslt_16 = newTerm(VAR, 0, portLoc(2, Rslt_16Args));
Rslt_16Args = makePair(APP, 0, Rslt_15, Rslt_16Args);
#ifdef STRICT
pushRedex(Rslt_16Args, glblFn178);
#else
swap(termLoc(Rslt_16), makePair(LAZ, 0, Rslt_16Args, glblFn178));
#endif

// call take-char at /home/jim/toccata/interpreter/intrp-rdr.toc: 575
Term Rslt_17Args = makePair(APP, 0, s_7, SUB);
Term Rslt_17 = newTerm(VAR, 0, portLoc(2, Rslt_17Args));
#ifdef STRICT
pushRedex(Rslt_17Args, glblFn1732);
#else
swap(termLoc(Rslt_17), makePair(LAZ, 0, Rslt_17Args, glblFn1732));
#endif

// call vect-conj at /home/jim/toccata/interpreter/intrp-rdr.toc: 575
Term Rslt_18Args = makePair(APP, 0, Rslt_17, SUB);
Term Rslt_18 = newTerm(VAR, 0, portLoc(2, Rslt_18Args));
Rslt_18Args = makePair(APP, 0, Rslt_16, Rslt_18Args);
#ifdef STRICT
pushRedex(Rslt_18Args, glblFn178);
#else
swap(termLoc(Rslt_18), makePair(LAZ, 0, Rslt_18Args, glblFn178));
#endif

// call str= at /home/jim/toccata/interpreter/intrp-rdr.toc: 576
Term Rslt_19Args = makePair(APP, 0, glblStr1971, SUB);
Term Rslt_19 = newTerm(VAR, 0, portLoc(2, Rslt_19Args));
Rslt_19Args = makePair(APP, 0, c_11_1, Rslt_19Args);
#ifdef STRICT
pushRedex(Rslt_19Args, glblFn363);
#else
swap(termLoc(Rslt_19), makePair(LAZ, 0, Rslt_19Args, glblFn363));
#endif

// call elt1 at /home/jim/toccata/interpreter/intrp-rdr.toc: 578
Term Rslt_24Args = makePair(APP, 0, tc_21, SUB);
Term Rslt_24 = newTerm(VAR, 0, portLoc(2, Rslt_24Args));
#ifdef STRICT
pushRedex(Rslt_24Args, glblFn1744);
#else
swap(termLoc(Rslt_24), makePair(LAZ, 0, Rslt_24Args, glblFn1744));
#endif

// call elt0 at /home/jim/toccata/interpreter/intrp-rdr.toc: 578
Term Rslt_25Args = makePair(APP, 0, tc_21_1, SUB);
Term Rslt_25 = newTerm(VAR, 0, portLoc(2, Rslt_25Args));
#ifdef STRICT
pushRedex(Rslt_25Args, glblFn1742);
#else
swap(termLoc(Rslt_25), makePair(LAZ, 0, Rslt_25Args, glblFn1742));
#endif

// call vect-conj at /home/jim/toccata/interpreter/intrp-rdr.toc: 578
Term Rslt_26Args = makePair(APP, 0, Rslt_25, SUB);
Term Rslt_26 = newTerm(VAR, 0, portLoc(2, Rslt_26Args));
Rslt_26Args = makePair(APP, 0, tcs_2_1, Rslt_26Args);
#ifdef STRICT
pushRedex(Rslt_26Args, glblFn178);
#else
swap(termLoc(Rslt_26), makePair(LAZ, 0, Rslt_26Args, glblFn178));
#endif

// call parse-body-acc at /home/jim/toccata/interpreter/intrp-rdr.toc: 570
Term Rslt_27Args = makePair(APP, 0, acc_3_1, SUB);
Term Rslt_27 = newTerm(VAR, 0, portLoc(2, Rslt_27Args));
Rslt_27Args = makePair(APP, 0, Rslt_26, Rslt_27Args);
Rslt_27Args = makePair(APP, 0, Rslt_24, Rslt_27Args);
#ifdef STRICT
pushRedex(Rslt_27Args, glblFn1825);
#else
swap(termLoc(Rslt_27), makePair(LAZ, 0, Rslt_27Args, glblFn1825));
#endif

// link args to body
swap(r_seq_22, Rslt_27);

Term Rslt_27V = get(termLoc(Rslt_27));
apps = take(portLoc(1, Rslt_27V));
lams = take(portLoc(2, Rslt_27V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_27), makePair(LAZ, 0, apps, lams));
// call parse-type-constraint at /home/jim/toccata/interpreter/intrp-rdr.toc: 577
Term Rslt_28Args = makePair(APP, 0, s_7_1, SUB);
Term Rslt_28 = newTerm(VAR, 0, portLoc(2, Rslt_28Args));
#ifdef STRICT
pushRedex(Rslt_28Args, glblFn1810);
#else
swap(termLoc(Rslt_28), makePair(LAZ, 0, Rslt_28Args, glblFn1810));
#endif

// call  at /home/jim/toccata/interpreter/intrp-rdr.toc: 577
Term Rslt_29Args = makePair(APP, 0, Rslt_28, SUB);
Term Rslt_29 = newTerm(VAR, 0, portLoc(2, Rslt_29Args));
#ifdef STRICT
pushRedex(Rslt_29Args, seq_22);
#else
swap(termLoc(Rslt_29), makePair(LAZ, 0, Rslt_29Args, seq_22));
#endif

// call .state at /home/jim/toccata/interpreter/intrp-rdr.toc: 580
Term Rslt_34Args = makePair(APP, 0, r_31, SUB);
Term Rslt_34 = newTerm(VAR, 0, portLoc(2, Rslt_34Args));
#ifdef STRICT
pushRedex(Rslt_34Args, newRef(glblProto1889_2403));
#else
swap(termLoc(Rslt_34), makePair(LAZ, 0, Rslt_34Args, newRef(glblProto1889_2403)));
#endif
// call .value at /home/jim/toccata/interpreter/intrp-rdr.toc: 580
Term Rslt_35Args = makePair(APP, 0, r_31_1, SUB);
Term Rslt_35 = newTerm(VAR, 0, portLoc(2, Rslt_35Args));
#ifdef STRICT
pushRedex(Rslt_35Args, newRef(glblProto1562_2405));
#else
swap(termLoc(Rslt_35), makePair(LAZ, 0, Rslt_35Args, newRef(glblProto1562_2405)));
#endif
// call vect-conj at /home/jim/toccata/interpreter/intrp-rdr.toc: 580
Term Rslt_36Args = makePair(APP, 0, Rslt_35, SUB);
Term Rslt_36 = newTerm(VAR, 0, portLoc(2, Rslt_36Args));
Rslt_36Args = makePair(APP, 0, acc_3_2, Rslt_36Args);
#ifdef STRICT
pushRedex(Rslt_36Args, glblFn178);
#else
swap(termLoc(Rslt_36), makePair(LAZ, 0, Rslt_36Args, glblFn178));
#endif

// call parse-body-acc at /home/jim/toccata/interpreter/intrp-rdr.toc: 570
Term Rslt_37Args = makePair(APP, 0, Rslt_36, SUB);
Term Rslt_37 = newTerm(VAR, 0, portLoc(2, Rslt_37Args));
Rslt_37Args = makePair(APP, 0, tcs_2_2, Rslt_37Args);
Rslt_37Args = makePair(APP, 0, Rslt_34, Rslt_37Args);
#ifdef STRICT
pushRedex(Rslt_37Args, glblFn1825);
#else
swap(termLoc(Rslt_37), makePair(LAZ, 0, Rslt_37Args, glblFn1825));
#endif

// link args to body
swap(r_seq_32, Rslt_37);

Term Rslt_37V = get(termLoc(Rslt_37));
apps = take(portLoc(1, Rslt_37V));
lams = take(portLoc(2, Rslt_37V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_37), makePair(LAZ, 0, apps, lams));
// call parse-expr at /home/jim/toccata/interpreter/intrp-rdr.toc: 548
Term Rslt_38Args = makePair(APP, 0, s_7_2, SUB);
Term Rslt_38 = newTerm(VAR, 0, portLoc(2, Rslt_38Args));
#ifdef STRICT
pushRedex(Rslt_38Args, glblVal1819_2);
#else
swap(termLoc(Rslt_38), makePair(LAZ, 0, Rslt_38Args, glblVal1819_2));
#endif

// call  at /home/jim/toccata/interpreter/intrp-rdr.toc: 548
Term Rslt_39Args = makePair(APP, 0, Rslt_38, SUB);
Term Rslt_39 = newTerm(VAR, 0, portLoc(2, Rslt_39Args));
#ifdef STRICT
pushRedex(Rslt_39Args, seq_32);
#else
swap(termLoc(Rslt_39), makePair(LAZ, 0, Rslt_39Args, seq_32));
#endif

// call cond at /home/jim/toccata/interpreter/intrp-rdr.toc: 574
Term Rslt_40Args = makePair(APP, 0, Rslt_39, SUB);
Term Rslt_40 = newTerm(VAR, 0, portLoc(2, Rslt_40Args));
Rslt_40Args = makePair(APP, 0, Rslt_29, Rslt_40Args);
Rslt_40Args = makePair(APP, 0, Rslt_19, Rslt_40Args);
#ifdef STRICT
pushRedex(Rslt_40Args, newRef(glblProto34_2407));
#else
swap(termLoc(Rslt_40), makePair(LAZ, 0, Rslt_40Args, newRef(glblProto34_2407)));
#endif
// call cond at /home/jim/toccata/interpreter/intrp-rdr.toc: 574
Term Rslt_41Args = makePair(APP, 0, Rslt_40, SUB);
Term Rslt_41 = newTerm(VAR, 0, portLoc(2, Rslt_41Args));
Rslt_41Args = makePair(APP, 0, Rslt_18, Rslt_41Args);
Rslt_41Args = makePair(APP, 0, Rslt_14, Rslt_41Args);
#ifdef STRICT
pushRedex(Rslt_41Args, newRef(glblProto34_2409));
#else
swap(termLoc(Rslt_41), makePair(LAZ, 0, Rslt_41Args, newRef(glblProto34_2409)));
#endif
// link args to body
swap(r_seq_12, Rslt_41);

Term Rslt_41V = get(termLoc(Rslt_41));
apps = take(portLoc(1, Rslt_41V));
lams = take(portLoc(2, Rslt_41V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_41), makePair(LAZ, 0, apps, lams));
// call peek-char at /home/jim/toccata/interpreter/intrp-rdr.toc: 572
Term Rslt_42Args = makePair(APP, 0, s_7_3, SUB);
Term Rslt_42 = newTerm(VAR, 0, portLoc(2, Rslt_42Args));
#ifdef STRICT
pushRedex(Rslt_42Args, glblFn1730);
#else
swap(termLoc(Rslt_42), makePair(LAZ, 0, Rslt_42Args, glblFn1730));
#endif

// call  at /home/jim/toccata/interpreter/intrp-rdr.toc: 572
Term Rslt_43Args = makePair(APP, 0, Rslt_42, SUB);
Term Rslt_43 = newTerm(VAR, 0, portLoc(2, Rslt_43Args));
#ifdef STRICT
pushRedex(Rslt_43Args, seq_12);
#else
swap(termLoc(Rslt_43), makePair(LAZ, 0, Rslt_43Args, seq_12));
#endif

// link args to body
swap(r_seq_8, Rslt_43);

Term Rslt_43V = get(termLoc(Rslt_43));
apps = take(portLoc(1, Rslt_43V));
lams = take(portLoc(2, Rslt_43V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_43), makePair(LAZ, 0, apps, lams));
// call skip-whitespace at /home/jim/toccata/interpreter/intrp-rdr.toc: 571
Term Rslt_44Args = makePair(APP, 0, state_1, SUB);
Term Rslt_44 = newTerm(VAR, 0, portLoc(2, Rslt_44Args));
#ifdef STRICT
pushRedex(Rslt_44Args, glblFn1736);
#else
swap(termLoc(Rslt_44), makePair(LAZ, 0, Rslt_44Args, glblFn1736));
#endif

// call  at /home/jim/toccata/interpreter/intrp-rdr.toc: 571
Term Rslt_45Args = makePair(APP, 0, Rslt_44, SUB);
Term Rslt_45 = newTerm(VAR, 0, portLoc(2, Rslt_45Args));
#ifdef STRICT
pushRedex(Rslt_45Args, seq_8);
#else
swap(termLoc(Rslt_45), makePair(LAZ, 0, Rslt_45Args, seq_8));
#endif

// link args to body
swap(r_seq_4, Rslt_45);

Term Rslt_45V = get(termLoc(Rslt_45));
apps = take(portLoc(1, Rslt_45V));
lams = take(portLoc(2, Rslt_45V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_45), makePair(LAZ, 0, apps, lams));
pushRedex(args, seq_4);
return;
}
Term glblFn1825 = newRef(glblCFn1824);
Term glblFn1827;
void glblCFn1826(Term ref, Term args) {
Term apps, lams, V;
Location vLoc;
Term seq_2 = makePair(LAM, 0, SUB, NUL);
Location r_seq_2 = portLoc(2, seq_2);
Term state_1 = newTerm(VAR, 0, portLoc(1, seq_2));
// allocate args at /home/jim/toccata/interpreter/intrp-rdr.toc: 584
Term glblVal175_66;
glblVal175 = dupeArg(glblVal175, &glblVal175_66, 0);

Term glblVal175_65;
glblVal175 = dupeArg(glblVal175, &glblVal175_65, 0);

// call parse-body-acc at /home/jim/toccata/interpreter/intrp-rdr.toc: 585
Term Rslt_4Args = makePair(APP, 0, glblVal175_66, SUB);
Term Rslt_4 = newTerm(VAR, 0, portLoc(2, Rslt_4Args));
Rslt_4Args = makePair(APP, 0, glblVal175_65, Rslt_4Args);
Rslt_4Args = makePair(APP, 0, state_1, Rslt_4Args);
#ifdef STRICT
pushRedex(Rslt_4Args, glblFn1825);
#else
swap(termLoc(Rslt_4), makePair(LAZ, 0, Rslt_4Args, glblFn1825));
#endif

// link args to body
swap(r_seq_2, Rslt_4);

Term Rslt_4V = get(termLoc(Rslt_4));
apps = take(portLoc(1, Rslt_4V));
lams = take(portLoc(2, Rslt_4V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_4), makePair(LAZ, 0, apps, lams));
pushRedex(args, seq_2);
return;
}
Term glblFn1827 = newRef(glblCFn1826);
void glblProto1889_2414(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(596), args);
  args = makePair(APP, 0, glblStr1912, args);
  interact(args, glblProto1889);
}
}
void glblProto1562_2416(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(597), args);
  args = makePair(APP, 0, glblStr1912, args);
  interact(args, glblProto1562);
}
}
void glblProto34_2418(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(591), args);
  args = makePair(APP, 0, glblStr1912, args);
  interact(args, glblProto34);
}
}
Term glblFn1829;
void glblCFn1828(Term ref, Term args) {
Term apps, lams, V;
Location vLoc;
Term seq_3 = makePair(LAM, 0, SUB, NUL);
Location r_seq_3 = portLoc(2, seq_3);
Term acc_2 = newTerm(VAR, 0, portLoc(1, seq_3));
seq_3 = makePair(LAM, 0, SUB, seq_3);
Term state_1 = newTerm(VAR, 0, portLoc(1, seq_3));
// allocate args at /home/jim/toccata/interpreter/intrp-rdr.toc: 587

Term acc_2_1;
acc_2 = dupeArg(acc_2, &acc_2_1, 347);
Term seq_7 = makePair(LAM, 0, SUB, NUL);
Location r_seq_7 = portLoc(2, seq_7);
Term s_6 = newTerm(VAR, 0, portLoc(1, seq_7));
// allocate args at /home/jim/toccata/interpreter/intrp-rdr.toc: 588

Term s_6_2;
s_6 = dupeArg(s_6, &s_6_2, 0);

Term s_6_1;
s_6 = dupeArg(s_6, &s_6_1, 0);
Term seq_11 = makePair(LAM, 0, SUB, NUL);
Location r_seq_11 = portLoc(2, seq_11);
Term c_10 = newTerm(VAR, 0, portLoc(1, seq_11));
// allocate args at /home/jim/toccata/interpreter/intrp-rdr.toc: 589
Term seq_19 = makePair(LAM, 0, SUB, NUL);
Location r_seq_19 = portLoc(2, seq_19);
Term nm_18 = newTerm(VAR, 0, portLoc(1, seq_19));
// allocate args at /home/jim/toccata/interpreter/intrp-rdr.toc: 593

Term nm_18_1;
nm_18 = dupeArg(nm_18, &nm_18_1, 0);
Term seq_23 = makePair(LAM, 0, SUB, NUL);
Location r_seq_23 = portLoc(2, seq_23);
Term name_22 = newTerm(VAR, 0, portLoc(1, seq_23));
// allocate args at /home/jim/toccata/interpreter/intrp-rdr.toc: 594
Term glblVal1819_3;
glblVal1819 = dupeArg(glblVal1819, &glblVal1819_3, 0);

Term seq_27 = makePair(LAM, 0, SUB, NUL);
Location r_seq_27 = portLoc(2, seq_27);
Term er_26 = newTerm(VAR, 0, portLoc(1, seq_27));
// allocate args at /home/jim/toccata/interpreter/intrp-rdr.toc: 595

Term er_26_1;
er_26 = dupeArg(er_26, &er_26_1, 0);
Term glblVal175_68;
glblVal175 = dupeArg(glblVal175, &glblVal175_68, 0);

Term glblVal175_67;
glblVal175 = dupeArg(glblVal175, &glblVal175_67, 0);

// call str= at /home/jim/toccata/interpreter/intrp-rdr.toc: 591
Term Rslt_13Args = makePair(APP, 0, glblStr746, SUB);
Term Rslt_13 = newTerm(VAR, 0, portLoc(2, Rslt_13Args));
Rslt_13Args = makePair(APP, 0, c_10, Rslt_13Args);
#ifdef STRICT
pushRedex(Rslt_13Args, glblFn363);
#else
swap(termLoc(Rslt_13), makePair(LAZ, 0, Rslt_13Args, glblFn363));
#endif

// call vect-conj at /home/jim/toccata/interpreter/intrp-rdr.toc: 592
Term Rslt_14Args = makePair(APP, 0, acc_2, SUB);
Term Rslt_14 = newTerm(VAR, 0, portLoc(2, Rslt_14Args));
Rslt_14Args = makePair(APP, 0, glblVal175_67, Rslt_14Args);
#ifdef STRICT
pushRedex(Rslt_14Args, glblFn178);
#else
swap(termLoc(Rslt_14), makePair(LAZ, 0, Rslt_14Args, glblFn178));
#endif

// call take-char at /home/jim/toccata/interpreter/intrp-rdr.toc: 592
Term Rslt_15Args = makePair(APP, 0, s_6, SUB);
Term Rslt_15 = newTerm(VAR, 0, portLoc(2, Rslt_15Args));
#ifdef STRICT
pushRedex(Rslt_15Args, glblFn1732);
#else
swap(termLoc(Rslt_15), makePair(LAZ, 0, Rslt_15Args, glblFn1732));
#endif

// call vect-conj at /home/jim/toccata/interpreter/intrp-rdr.toc: 592
Term Rslt_16Args = makePair(APP, 0, Rslt_15, SUB);
Term Rslt_16 = newTerm(VAR, 0, portLoc(2, Rslt_16Args));
Rslt_16Args = makePair(APP, 0, Rslt_14, Rslt_16Args);
#ifdef STRICT
pushRedex(Rslt_16Args, glblFn178);
#else
swap(termLoc(Rslt_16), makePair(LAZ, 0, Rslt_16Args, glblFn178));
#endif

// call .state at /home/jim/toccata/interpreter/intrp-rdr.toc: 596
Term Rslt_29Args = makePair(APP, 0, er_26, SUB);
Term Rslt_29 = newTerm(VAR, 0, portLoc(2, Rslt_29Args));
#ifdef STRICT
pushRedex(Rslt_29Args, newRef(glblProto1889_2414));
#else
swap(termLoc(Rslt_29), makePair(LAZ, 0, Rslt_29Args, newRef(glblProto1889_2414)));
#endif
// call vect-conj at /home/jim/toccata/interpreter/intrp-rdr.toc: 597
Term Rslt_30Args = makePair(APP, 0, name_22, SUB);
Term Rslt_30 = newTerm(VAR, 0, portLoc(2, Rslt_30Args));
Rslt_30Args = makePair(APP, 0, glblVal175_68, Rslt_30Args);
#ifdef STRICT
pushRedex(Rslt_30Args, glblFn178);
#else
swap(termLoc(Rslt_30), makePair(LAZ, 0, Rslt_30Args, glblFn178));
#endif

// call .value at /home/jim/toccata/interpreter/intrp-rdr.toc: 597
Term Rslt_31Args = makePair(APP, 0, er_26_1, SUB);
Term Rslt_31 = newTerm(VAR, 0, portLoc(2, Rslt_31Args));
#ifdef STRICT
pushRedex(Rslt_31Args, newRef(glblProto1562_2416));
#else
swap(termLoc(Rslt_31), makePair(LAZ, 0, Rslt_31Args, newRef(glblProto1562_2416)));
#endif
// call vect-conj at /home/jim/toccata/interpreter/intrp-rdr.toc: 597
Term Rslt_32Args = makePair(APP, 0, Rslt_31, SUB);
Term Rslt_32 = newTerm(VAR, 0, portLoc(2, Rslt_32Args));
Rslt_32Args = makePair(APP, 0, Rslt_30, Rslt_32Args);
#ifdef STRICT
pushRedex(Rslt_32Args, glblFn178);
#else
swap(termLoc(Rslt_32), makePair(LAZ, 0, Rslt_32Args, glblFn178));
#endif

// call vect-conj at /home/jim/toccata/interpreter/intrp-rdr.toc: 597
Term Rslt_33Args = makePair(APP, 0, Rslt_32, SUB);
Term Rslt_33 = newTerm(VAR, 0, portLoc(2, Rslt_33Args));
Rslt_33Args = makePair(APP, 0, acc_2_1, Rslt_33Args);
#ifdef STRICT
pushRedex(Rslt_33Args, glblFn178);
#else
swap(termLoc(Rslt_33), makePair(LAZ, 0, Rslt_33Args, glblFn178));
#endif

// call read-let-bindings-acc at /home/jim/toccata/interpreter/intrp-rdr.toc: 587
Term Rslt_34Args = makePair(APP, 0, Rslt_33, SUB);
Term Rslt_34 = newTerm(VAR, 0, portLoc(2, Rslt_34Args));
Rslt_34Args = makePair(APP, 0, Rslt_29, Rslt_34Args);
#ifdef STRICT
pushRedex(Rslt_34Args, glblFn1829);
#else
swap(termLoc(Rslt_34), makePair(LAZ, 0, Rslt_34Args, glblFn1829));
#endif

// link args to body
swap(r_seq_27, Rslt_34);

Term Rslt_34V = get(termLoc(Rslt_34));
apps = take(portLoc(1, Rslt_34V));
lams = take(portLoc(2, Rslt_34V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_34), makePair(LAZ, 0, apps, lams));
// call elt1 at /home/jim/toccata/interpreter/intrp-rdr.toc: 595
Term Rslt_35Args = makePair(APP, 0, nm_18, SUB);
Term Rslt_35 = newTerm(VAR, 0, portLoc(2, Rslt_35Args));
#ifdef STRICT
pushRedex(Rslt_35Args, glblFn1744);
#else
swap(termLoc(Rslt_35), makePair(LAZ, 0, Rslt_35Args, glblFn1744));
#endif

// call skip-whitespace at /home/jim/toccata/interpreter/intrp-rdr.toc: 595
Term Rslt_36Args = makePair(APP, 0, Rslt_35, SUB);
Term Rslt_36 = newTerm(VAR, 0, portLoc(2, Rslt_36Args));
#ifdef STRICT
pushRedex(Rslt_36Args, glblFn1736);
#else
swap(termLoc(Rslt_36), makePair(LAZ, 0, Rslt_36Args, glblFn1736));
#endif

// call parse-expr at /home/jim/toccata/interpreter/intrp-rdr.toc: 548
Term Rslt_37Args = makePair(APP, 0, Rslt_36, SUB);
Term Rslt_37 = newTerm(VAR, 0, portLoc(2, Rslt_37Args));
#ifdef STRICT
pushRedex(Rslt_37Args, glblVal1819_3);
#else
swap(termLoc(Rslt_37), makePair(LAZ, 0, Rslt_37Args, glblVal1819_3));
#endif

// call  at /home/jim/toccata/interpreter/intrp-rdr.toc: 548
Term Rslt_38Args = makePair(APP, 0, Rslt_37, SUB);
Term Rslt_38 = newTerm(VAR, 0, portLoc(2, Rslt_38Args));
#ifdef STRICT
pushRedex(Rslt_38Args, seq_27);
#else
swap(termLoc(Rslt_38), makePair(LAZ, 0, Rslt_38Args, seq_27));
#endif

// link args to body
swap(r_seq_23, Rslt_38);

Term Rslt_38V = get(termLoc(Rslt_38));
apps = take(portLoc(1, Rslt_38V));
lams = take(portLoc(2, Rslt_38V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_38), makePair(LAZ, 0, apps, lams));
// call elt0 at /home/jim/toccata/interpreter/intrp-rdr.toc: 594
Term Rslt_39Args = makePair(APP, 0, nm_18_1, SUB);
Term Rslt_39 = newTerm(VAR, 0, portLoc(2, Rslt_39Args));
#ifdef STRICT
pushRedex(Rslt_39Args, glblFn1742);
#else
swap(termLoc(Rslt_39), makePair(LAZ, 0, Rslt_39Args, glblFn1742));
#endif

// call  at /home/jim/toccata/interpreter/intrp-rdr.toc: 594
Term Rslt_40Args = makePair(APP, 0, Rslt_39, SUB);
Term Rslt_40 = newTerm(VAR, 0, portLoc(2, Rslt_40Args));
#ifdef STRICT
pushRedex(Rslt_40Args, seq_23);
#else
swap(termLoc(Rslt_40), makePair(LAZ, 0, Rslt_40Args, seq_23));
#endif

// link args to body
swap(r_seq_19, Rslt_40);

Term Rslt_40V = get(termLoc(Rslt_40));
apps = take(portLoc(1, Rslt_40V));
lams = take(portLoc(2, Rslt_40V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_40), makePair(LAZ, 0, apps, lams));
// call read-symbol at /home/jim/toccata/interpreter/intrp-rdr.toc: 593
Term Rslt_41Args = makePair(APP, 0, s_6_1, SUB);
Term Rslt_41 = newTerm(VAR, 0, portLoc(2, Rslt_41Args));
#ifdef STRICT
pushRedex(Rslt_41Args, glblFn1750);
#else
swap(termLoc(Rslt_41), makePair(LAZ, 0, Rslt_41Args, glblFn1750));
#endif

// call  at /home/jim/toccata/interpreter/intrp-rdr.toc: 593
Term Rslt_42Args = makePair(APP, 0, Rslt_41, SUB);
Term Rslt_42 = newTerm(VAR, 0, portLoc(2, Rslt_42Args));
#ifdef STRICT
pushRedex(Rslt_42Args, seq_19);
#else
swap(termLoc(Rslt_42), makePair(LAZ, 0, Rslt_42Args, seq_19));
#endif

// call cond at /home/jim/toccata/interpreter/intrp-rdr.toc: 591
Term Rslt_43Args = makePair(APP, 0, Rslt_42, SUB);
Term Rslt_43 = newTerm(VAR, 0, portLoc(2, Rslt_43Args));
Rslt_43Args = makePair(APP, 0, Rslt_16, Rslt_43Args);
Rslt_43Args = makePair(APP, 0, Rslt_13, Rslt_43Args);
#ifdef STRICT
pushRedex(Rslt_43Args, newRef(glblProto34_2418));
#else
swap(termLoc(Rslt_43), makePair(LAZ, 0, Rslt_43Args, newRef(glblProto34_2418)));
#endif
// link args to body
swap(r_seq_11, Rslt_43);

Term Rslt_43V = get(termLoc(Rslt_43));
apps = take(portLoc(1, Rslt_43V));
lams = take(portLoc(2, Rslt_43V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_43), makePair(LAZ, 0, apps, lams));
// call peek-char at /home/jim/toccata/interpreter/intrp-rdr.toc: 589
Term Rslt_44Args = makePair(APP, 0, s_6_2, SUB);
Term Rslt_44 = newTerm(VAR, 0, portLoc(2, Rslt_44Args));
#ifdef STRICT
pushRedex(Rslt_44Args, glblFn1730);
#else
swap(termLoc(Rslt_44), makePair(LAZ, 0, Rslt_44Args, glblFn1730));
#endif

// call  at /home/jim/toccata/interpreter/intrp-rdr.toc: 589
Term Rslt_45Args = makePair(APP, 0, Rslt_44, SUB);
Term Rslt_45 = newTerm(VAR, 0, portLoc(2, Rslt_45Args));
#ifdef STRICT
pushRedex(Rslt_45Args, seq_11);
#else
swap(termLoc(Rslt_45), makePair(LAZ, 0, Rslt_45Args, seq_11));
#endif

// link args to body
swap(r_seq_7, Rslt_45);

Term Rslt_45V = get(termLoc(Rslt_45));
apps = take(portLoc(1, Rslt_45V));
lams = take(portLoc(2, Rslt_45V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_45), makePair(LAZ, 0, apps, lams));
// call skip-whitespace at /home/jim/toccata/interpreter/intrp-rdr.toc: 588
Term Rslt_46Args = makePair(APP, 0, state_1, SUB);
Term Rslt_46 = newTerm(VAR, 0, portLoc(2, Rslt_46Args));
#ifdef STRICT
pushRedex(Rslt_46Args, glblFn1736);
#else
swap(termLoc(Rslt_46), makePair(LAZ, 0, Rslt_46Args, glblFn1736));
#endif

// call  at /home/jim/toccata/interpreter/intrp-rdr.toc: 588
Term Rslt_47Args = makePair(APP, 0, Rslt_46, SUB);
Term Rslt_47 = newTerm(VAR, 0, portLoc(2, Rslt_47Args));
#ifdef STRICT
pushRedex(Rslt_47Args, seq_7);
#else
swap(termLoc(Rslt_47), makePair(LAZ, 0, Rslt_47Args, seq_7));
#endif

// link args to body
swap(r_seq_3, Rslt_47);

Term Rslt_47V = get(termLoc(Rslt_47));
apps = take(portLoc(1, Rslt_47V));
lams = take(portLoc(2, Rslt_47V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_47), makePair(LAZ, 0, apps, lams));
pushRedex(args, seq_3);
return;
}
Term glblFn1829 = newRef(glblCFn1828);
Term glblFn1831;
void glblCFn1830(Term ref, Term args) {
Term apps, lams, V;
Location vLoc;
Term seq_2 = makePair(LAM, 0, SUB, NUL);
Location r_seq_2 = portLoc(2, seq_2);
Term state_1 = newTerm(VAR, 0, portLoc(1, seq_2));
// allocate args at /home/jim/toccata/interpreter/intrp-rdr.toc: 602
Term glblVal175_69;
glblVal175 = dupeArg(glblVal175, &glblVal175_69, 0);

// call read-let-bindings-acc at /home/jim/toccata/interpreter/intrp-rdr.toc: 603
Term Rslt_4Args = makePair(APP, 0, glblVal175_69, SUB);
Term Rslt_4 = newTerm(VAR, 0, portLoc(2, Rslt_4Args));
Rslt_4Args = makePair(APP, 0, state_1, Rslt_4Args);
#ifdef STRICT
pushRedex(Rslt_4Args, glblFn1829);
#else
swap(termLoc(Rslt_4), makePair(LAZ, 0, Rslt_4Args, glblFn1829));
#endif

// link args to body
swap(r_seq_2, Rslt_4);

Term Rslt_4V = get(termLoc(Rslt_4));
apps = take(portLoc(1, Rslt_4V));
lams = take(portLoc(2, Rslt_4V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_4), makePair(LAZ, 0, apps, lams));
pushRedex(args, seq_2);
return;
}
Term glblFn1831 = newRef(glblCFn1830);
struct {TYPE_SIZE type;
        REFS_SIZE refs;
        int64_t *hash;
        Value *parent;
        int64_t len;
        char buffer[2];
} str37 = {StringBufferType, REFS_STATIC, 0, 0, 1, "}"};
Term glblStr2422 = newTerm_(VAL, (Term)&str37);
void glblProto1889_2423(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(614), args);
  args = makePair(APP, 0, glblStr1912, args);
  interact(args, glblProto1889);
}
}
void glblProto1562_2425(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(615), args);
  args = makePair(APP, 0, glblStr1912, args);
  interact(args, glblProto1562);
}
}
void glblProto1889_2427(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(613), args);
  args = makePair(APP, 0, glblStr1912, args);
  interact(args, glblProto1889);
}
}
void glblProto1562_2429(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(612), args);
  args = makePair(APP, 0, glblStr1912, args);
  interact(args, glblProto1562);
}
}
void glblProto34_2431(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(609), args);
  args = makePair(APP, 0, glblStr1912, args);
  interact(args, glblProto34);
}
}
Term glblFn1833;
void glblCFn1832(Term ref, Term args) {
Term apps, lams, V;
Location vLoc;
Term seq_3 = makePair(LAM, 0, SUB, NUL);
Location r_seq_3 = portLoc(2, seq_3);
Term acc_2 = newTerm(VAR, 0, portLoc(1, seq_3));
seq_3 = makePair(LAM, 0, SUB, seq_3);
Term state_1 = newTerm(VAR, 0, portLoc(1, seq_3));
// allocate args at /home/jim/toccata/interpreter/intrp-rdr.toc: 605

Term acc_2_1;
acc_2 = dupeArg(acc_2, &acc_2_1, 348);
Term seq_7 = makePair(LAM, 0, SUB, NUL);
Location r_seq_7 = portLoc(2, seq_7);
Term s_6 = newTerm(VAR, 0, portLoc(1, seq_7));
// allocate args at /home/jim/toccata/interpreter/intrp-rdr.toc: 606

Term s_6_2;
s_6 = dupeArg(s_6, &s_6_2, 0);

Term s_6_1;
s_6 = dupeArg(s_6, &s_6_1, 0);
Term seq_11 = makePair(LAM, 0, SUB, NUL);
Location r_seq_11 = portLoc(2, seq_11);
Term c_10 = newTerm(VAR, 0, portLoc(1, seq_11));
// allocate args at /home/jim/toccata/interpreter/intrp-rdr.toc: 607
Term glblVal1819_5;
glblVal1819 = dupeArg(glblVal1819, &glblVal1819_5, 0);

Term seq_19 = makePair(LAM, 0, SUB, NUL);
Location r_seq_19 = portLoc(2, seq_19);
Term kr_18 = newTerm(VAR, 0, portLoc(1, seq_19));
// allocate args at /home/jim/toccata/interpreter/intrp-rdr.toc: 611

Term kr_18_1;
kr_18 = dupeArg(kr_18, &kr_18_1, 0);
Term seq_23 = makePair(LAM, 0, SUB, NUL);
Location r_seq_23 = portLoc(2, seq_23);
Term k_22 = newTerm(VAR, 0, portLoc(1, seq_23));
// allocate args at /home/jim/toccata/interpreter/intrp-rdr.toc: 612
Term glblVal1819_4;
glblVal1819 = dupeArg(glblVal1819, &glblVal1819_4, 0);

Term seq_27 = makePair(LAM, 0, SUB, NUL);
Location r_seq_27 = portLoc(2, seq_27);
Term vr_26 = newTerm(VAR, 0, portLoc(1, seq_27));
// allocate args at /home/jim/toccata/interpreter/intrp-rdr.toc: 613

Term vr_26_1;
vr_26 = dupeArg(vr_26, &vr_26_1, 0);
Term glblVal175_71;
glblVal175 = dupeArg(glblVal175, &glblVal175_71, 0);

Term glblVal175_70;
glblVal175 = dupeArg(glblVal175, &glblVal175_70, 0);

// call str= at /home/jim/toccata/interpreter/intrp-rdr.toc: 609
Term Rslt_13Args = makePair(APP, 0, glblStr2422, SUB);
Term Rslt_13 = newTerm(VAR, 0, portLoc(2, Rslt_13Args));
Rslt_13Args = makePair(APP, 0, c_10, Rslt_13Args);
#ifdef STRICT
pushRedex(Rslt_13Args, glblFn363);
#else
swap(termLoc(Rslt_13), makePair(LAZ, 0, Rslt_13Args, glblFn363));
#endif

// call vect-conj at /home/jim/toccata/interpreter/intrp-rdr.toc: 610
Term Rslt_14Args = makePair(APP, 0, acc_2, SUB);
Term Rslt_14 = newTerm(VAR, 0, portLoc(2, Rslt_14Args));
Rslt_14Args = makePair(APP, 0, glblVal175_70, Rslt_14Args);
#ifdef STRICT
pushRedex(Rslt_14Args, glblFn178);
#else
swap(termLoc(Rslt_14), makePair(LAZ, 0, Rslt_14Args, glblFn178));
#endif

// call take-char at /home/jim/toccata/interpreter/intrp-rdr.toc: 610
Term Rslt_15Args = makePair(APP, 0, s_6, SUB);
Term Rslt_15 = newTerm(VAR, 0, portLoc(2, Rslt_15Args));
#ifdef STRICT
pushRedex(Rslt_15Args, glblFn1732);
#else
swap(termLoc(Rslt_15), makePair(LAZ, 0, Rslt_15Args, glblFn1732));
#endif

// call vect-conj at /home/jim/toccata/interpreter/intrp-rdr.toc: 610
Term Rslt_16Args = makePair(APP, 0, Rslt_15, SUB);
Term Rslt_16 = newTerm(VAR, 0, portLoc(2, Rslt_16Args));
Rslt_16Args = makePair(APP, 0, Rslt_14, Rslt_16Args);
#ifdef STRICT
pushRedex(Rslt_16Args, glblFn178);
#else
swap(termLoc(Rslt_16), makePair(LAZ, 0, Rslt_16Args, glblFn178));
#endif

// call .state at /home/jim/toccata/interpreter/intrp-rdr.toc: 614
Term Rslt_29Args = makePair(APP, 0, vr_26, SUB);
Term Rslt_29 = newTerm(VAR, 0, portLoc(2, Rslt_29Args));
#ifdef STRICT
pushRedex(Rslt_29Args, newRef(glblProto1889_2423));
#else
swap(termLoc(Rslt_29), makePair(LAZ, 0, Rslt_29Args, newRef(glblProto1889_2423)));
#endif
// call vect-conj at /home/jim/toccata/interpreter/intrp-rdr.toc: 615
Term Rslt_30Args = makePair(APP, 0, k_22, SUB);
Term Rslt_30 = newTerm(VAR, 0, portLoc(2, Rslt_30Args));
Rslt_30Args = makePair(APP, 0, glblVal175_71, Rslt_30Args);
#ifdef STRICT
pushRedex(Rslt_30Args, glblFn178);
#else
swap(termLoc(Rslt_30), makePair(LAZ, 0, Rslt_30Args, glblFn178));
#endif

// call .value at /home/jim/toccata/interpreter/intrp-rdr.toc: 615
Term Rslt_31Args = makePair(APP, 0, vr_26_1, SUB);
Term Rslt_31 = newTerm(VAR, 0, portLoc(2, Rslt_31Args));
#ifdef STRICT
pushRedex(Rslt_31Args, newRef(glblProto1562_2425));
#else
swap(termLoc(Rslt_31), makePair(LAZ, 0, Rslt_31Args, newRef(glblProto1562_2425)));
#endif
// call vect-conj at /home/jim/toccata/interpreter/intrp-rdr.toc: 615
Term Rslt_32Args = makePair(APP, 0, Rslt_31, SUB);
Term Rslt_32 = newTerm(VAR, 0, portLoc(2, Rslt_32Args));
Rslt_32Args = makePair(APP, 0, Rslt_30, Rslt_32Args);
#ifdef STRICT
pushRedex(Rslt_32Args, glblFn178);
#else
swap(termLoc(Rslt_32), makePair(LAZ, 0, Rslt_32Args, glblFn178));
#endif

// call vect-conj at /home/jim/toccata/interpreter/intrp-rdr.toc: 615
Term Rslt_33Args = makePair(APP, 0, Rslt_32, SUB);
Term Rslt_33 = newTerm(VAR, 0, portLoc(2, Rslt_33Args));
Rslt_33Args = makePair(APP, 0, acc_2_1, Rslt_33Args);
#ifdef STRICT
pushRedex(Rslt_33Args, glblFn178);
#else
swap(termLoc(Rslt_33), makePair(LAZ, 0, Rslt_33Args, glblFn178));
#endif

// call read-hash-pairs-acc at /home/jim/toccata/interpreter/intrp-rdr.toc: 605
Term Rslt_34Args = makePair(APP, 0, Rslt_33, SUB);
Term Rslt_34 = newTerm(VAR, 0, portLoc(2, Rslt_34Args));
Rslt_34Args = makePair(APP, 0, Rslt_29, Rslt_34Args);
#ifdef STRICT
pushRedex(Rslt_34Args, glblFn1833);
#else
swap(termLoc(Rslt_34), makePair(LAZ, 0, Rslt_34Args, glblFn1833));
#endif

// link args to body
swap(r_seq_27, Rslt_34);

Term Rslt_34V = get(termLoc(Rslt_34));
apps = take(portLoc(1, Rslt_34V));
lams = take(portLoc(2, Rslt_34V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_34), makePair(LAZ, 0, apps, lams));
// call .state at /home/jim/toccata/interpreter/intrp-rdr.toc: 613
Term Rslt_35Args = makePair(APP, 0, kr_18, SUB);
Term Rslt_35 = newTerm(VAR, 0, portLoc(2, Rslt_35Args));
#ifdef STRICT
pushRedex(Rslt_35Args, newRef(glblProto1889_2427));
#else
swap(termLoc(Rslt_35), makePair(LAZ, 0, Rslt_35Args, newRef(glblProto1889_2427)));
#endif
// call parse-expr at /home/jim/toccata/interpreter/intrp-rdr.toc: 548
Term Rslt_36Args = makePair(APP, 0, Rslt_35, SUB);
Term Rslt_36 = newTerm(VAR, 0, portLoc(2, Rslt_36Args));
#ifdef STRICT
pushRedex(Rslt_36Args, glblVal1819_4);
#else
swap(termLoc(Rslt_36), makePair(LAZ, 0, Rslt_36Args, glblVal1819_4));
#endif

// call  at /home/jim/toccata/interpreter/intrp-rdr.toc: 548
Term Rslt_37Args = makePair(APP, 0, Rslt_36, SUB);
Term Rslt_37 = newTerm(VAR, 0, portLoc(2, Rslt_37Args));
#ifdef STRICT
pushRedex(Rslt_37Args, seq_27);
#else
swap(termLoc(Rslt_37), makePair(LAZ, 0, Rslt_37Args, seq_27));
#endif

// link args to body
swap(r_seq_23, Rslt_37);

Term Rslt_37V = get(termLoc(Rslt_37));
apps = take(portLoc(1, Rslt_37V));
lams = take(portLoc(2, Rslt_37V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_37), makePair(LAZ, 0, apps, lams));
// call .value at /home/jim/toccata/interpreter/intrp-rdr.toc: 612
Term Rslt_38Args = makePair(APP, 0, kr_18_1, SUB);
Term Rslt_38 = newTerm(VAR, 0, portLoc(2, Rslt_38Args));
#ifdef STRICT
pushRedex(Rslt_38Args, newRef(glblProto1562_2429));
#else
swap(termLoc(Rslt_38), makePair(LAZ, 0, Rslt_38Args, newRef(glblProto1562_2429)));
#endif
// call  at /home/jim/toccata/interpreter/intrp-rdr.toc: 612
Term Rslt_39Args = makePair(APP, 0, Rslt_38, SUB);
Term Rslt_39 = newTerm(VAR, 0, portLoc(2, Rslt_39Args));
#ifdef STRICT
pushRedex(Rslt_39Args, seq_23);
#else
swap(termLoc(Rslt_39), makePair(LAZ, 0, Rslt_39Args, seq_23));
#endif

// link args to body
swap(r_seq_19, Rslt_39);

Term Rslt_39V = get(termLoc(Rslt_39));
apps = take(portLoc(1, Rslt_39V));
lams = take(portLoc(2, Rslt_39V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_39), makePair(LAZ, 0, apps, lams));
// call parse-expr at /home/jim/toccata/interpreter/intrp-rdr.toc: 548
Term Rslt_40Args = makePair(APP, 0, s_6_1, SUB);
Term Rslt_40 = newTerm(VAR, 0, portLoc(2, Rslt_40Args));
#ifdef STRICT
pushRedex(Rslt_40Args, glblVal1819_5);
#else
swap(termLoc(Rslt_40), makePair(LAZ, 0, Rslt_40Args, glblVal1819_5));
#endif

// call  at /home/jim/toccata/interpreter/intrp-rdr.toc: 548
Term Rslt_41Args = makePair(APP, 0, Rslt_40, SUB);
Term Rslt_41 = newTerm(VAR, 0, portLoc(2, Rslt_41Args));
#ifdef STRICT
pushRedex(Rslt_41Args, seq_19);
#else
swap(termLoc(Rslt_41), makePair(LAZ, 0, Rslt_41Args, seq_19));
#endif

// call cond at /home/jim/toccata/interpreter/intrp-rdr.toc: 609
Term Rslt_42Args = makePair(APP, 0, Rslt_41, SUB);
Term Rslt_42 = newTerm(VAR, 0, portLoc(2, Rslt_42Args));
Rslt_42Args = makePair(APP, 0, Rslt_16, Rslt_42Args);
Rslt_42Args = makePair(APP, 0, Rslt_13, Rslt_42Args);
#ifdef STRICT
pushRedex(Rslt_42Args, newRef(glblProto34_2431));
#else
swap(termLoc(Rslt_42), makePair(LAZ, 0, Rslt_42Args, newRef(glblProto34_2431)));
#endif
// link args to body
swap(r_seq_11, Rslt_42);

Term Rslt_42V = get(termLoc(Rslt_42));
apps = take(portLoc(1, Rslt_42V));
lams = take(portLoc(2, Rslt_42V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_42), makePair(LAZ, 0, apps, lams));
// call peek-char at /home/jim/toccata/interpreter/intrp-rdr.toc: 607
Term Rslt_43Args = makePair(APP, 0, s_6_2, SUB);
Term Rslt_43 = newTerm(VAR, 0, portLoc(2, Rslt_43Args));
#ifdef STRICT
pushRedex(Rslt_43Args, glblFn1730);
#else
swap(termLoc(Rslt_43), makePair(LAZ, 0, Rslt_43Args, glblFn1730));
#endif

// call  at /home/jim/toccata/interpreter/intrp-rdr.toc: 607
Term Rslt_44Args = makePair(APP, 0, Rslt_43, SUB);
Term Rslt_44 = newTerm(VAR, 0, portLoc(2, Rslt_44Args));
#ifdef STRICT
pushRedex(Rslt_44Args, seq_11);
#else
swap(termLoc(Rslt_44), makePair(LAZ, 0, Rslt_44Args, seq_11));
#endif

// link args to body
swap(r_seq_7, Rslt_44);

Term Rslt_44V = get(termLoc(Rslt_44));
apps = take(portLoc(1, Rslt_44V));
lams = take(portLoc(2, Rslt_44V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_44), makePair(LAZ, 0, apps, lams));
// call skip-whitespace at /home/jim/toccata/interpreter/intrp-rdr.toc: 606
Term Rslt_45Args = makePair(APP, 0, state_1, SUB);
Term Rslt_45 = newTerm(VAR, 0, portLoc(2, Rslt_45Args));
#ifdef STRICT
pushRedex(Rslt_45Args, glblFn1736);
#else
swap(termLoc(Rslt_45), makePair(LAZ, 0, Rslt_45Args, glblFn1736));
#endif

// call  at /home/jim/toccata/interpreter/intrp-rdr.toc: 606
Term Rslt_46Args = makePair(APP, 0, Rslt_45, SUB);
Term Rslt_46 = newTerm(VAR, 0, portLoc(2, Rslt_46Args));
#ifdef STRICT
pushRedex(Rslt_46Args, seq_7);
#else
swap(termLoc(Rslt_46), makePair(LAZ, 0, Rslt_46Args, seq_7));
#endif

// link args to body
swap(r_seq_3, Rslt_46);

Term Rslt_46V = get(termLoc(Rslt_46));
apps = take(portLoc(1, Rslt_46V));
lams = take(portLoc(2, Rslt_46V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_46), makePair(LAZ, 0, apps, lams));
pushRedex(args, seq_3);
return;
}
Term glblFn1833 = newRef(glblCFn1832);
Term glblFn1835;
void glblCFn1834(Term ref, Term args) {
Term apps, lams, V;
Location vLoc;
Term seq_2 = makePair(LAM, 0, SUB, NUL);
Location r_seq_2 = portLoc(2, seq_2);
Term state_1 = newTerm(VAR, 0, portLoc(1, seq_2));
// allocate args at /home/jim/toccata/interpreter/intrp-rdr.toc: 620
Term glblVal175_72;
glblVal175 = dupeArg(glblVal175, &glblVal175_72, 0);

// call read-hash-pairs-acc at /home/jim/toccata/interpreter/intrp-rdr.toc: 621
Term Rslt_4Args = makePair(APP, 0, glblVal175_72, SUB);
Term Rslt_4 = newTerm(VAR, 0, portLoc(2, Rslt_4Args));
Rslt_4Args = makePair(APP, 0, state_1, Rslt_4Args);
#ifdef STRICT
pushRedex(Rslt_4Args, glblFn1833);
#else
swap(termLoc(Rslt_4), makePair(LAZ, 0, Rslt_4Args, glblFn1833));
#endif

// link args to body
swap(r_seq_2, Rslt_4);

Term Rslt_4V = get(termLoc(Rslt_4));
apps = take(portLoc(1, Rslt_4V));
lams = take(portLoc(2, Rslt_4V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_4), makePair(LAZ, 0, apps, lams));
pushRedex(args, seq_2);
return;
}
Term glblFn1835 = newRef(glblCFn1834);
struct {TYPE_SIZE type;
        REFS_SIZE refs;
        int64_t *hash;
        Value *parent;
        int64_t len;
        char buffer[37];
} str38 = {StringBufferType, REFS_STATIC, 0, 0, 36, "unexpected end of input in threading"};
Term glblStr2437 = newTerm_(VAL, (Term)&str38);
void glblProto20_2438(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(634), args);
  args = makePair(APP, 0, glblStr1912, args);
  interact(args, glblProto20);
}
}
void glblProto1889_2440(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(636), args);
  args = makePair(APP, 0, glblStr1912, args);
  interact(args, glblProto1889);
}
}
void glblProto1562_2442(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(637), args);
  args = makePair(APP, 0, glblStr1912, args);
  interact(args, glblProto1562);
}
}
void glblProto34_2444(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(634), args);
  args = makePair(APP, 0, glblStr1912, args);
  interact(args, glblProto34);
}
}
void glblProto1757_2446(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(632), args);
  args = makePair(APP, 0, glblStr1912, args);
  interact(args, glblProto1757);
}
}
void glblProto34_2448(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(627), args);
  args = makePair(APP, 0, glblStr1912, args);
  interact(args, glblProto34);
}
}
void glblProto34_2450(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(627), args);
  args = makePair(APP, 0, glblStr1912, args);
  interact(args, glblProto34);
}
}
Term glblFn1837;
void glblCFn1836(Term ref, Term args) {
Term apps, lams, V;
Location vLoc;
Term seq_3 = makePair(LAM, 0, SUB, NUL);
Location r_seq_3 = portLoc(2, seq_3);
Term acc_2 = newTerm(VAR, 0, portLoc(1, seq_3));
seq_3 = makePair(LAM, 0, SUB, seq_3);
Term state_1 = newTerm(VAR, 0, portLoc(1, seq_3));
// allocate args at /home/jim/toccata/interpreter/intrp-rdr.toc: 623

Term acc_2_1;
acc_2 = dupeArg(acc_2, &acc_2_1, 349);
Term seq_7 = makePair(LAM, 0, SUB, NUL);
Location r_seq_7 = portLoc(2, seq_7);
Term s_6 = newTerm(VAR, 0, portLoc(1, seq_7));
// allocate args at /home/jim/toccata/interpreter/intrp-rdr.toc: 624

Term s_6_3;
s_6 = dupeArg(s_6, &s_6_3, 0);

Term s_6_2;
s_6 = dupeArg(s_6, &s_6_2, 0);

Term s_6_1;
s_6 = dupeArg(s_6, &s_6_1, 0);
Term seq_11 = makePair(LAM, 0, SUB, NUL);
Location r_seq_11 = portLoc(2, seq_11);
Term c_10 = newTerm(VAR, 0, portLoc(1, seq_11));
// allocate args at /home/jim/toccata/interpreter/intrp-rdr.toc: 625

Term c_10_1;
c_10 = dupeArg(c_10, &c_10_1, 0);
Term glblVal1819_6;
glblVal1819 = dupeArg(glblVal1819, &glblVal1819_6, 0);

Term seq_20 = makePair(LAM, 0, SUB, NUL);
Location r_seq_20 = portLoc(2, seq_20);
Term r_19 = newTerm(VAR, 0, portLoc(1, seq_20));
// allocate args at /home/jim/toccata/interpreter/intrp-rdr.toc: 631

Term r_19_3;
r_19 = dupeArg(r_19, &r_19_3, 0);

Term r_19_2;
r_19 = dupeArg(r_19, &r_19_2, 0);

Term r_19_1;
r_19 = dupeArg(r_19, &r_19_1, 0);
Term seq_24 = makePair(LAM, 0, SUB, NUL);
Location r_seq_24 = portLoc(2, seq_24);
Term k_23 = newTerm(VAR, 0, portLoc(1, seq_24));
// allocate args at /home/jim/toccata/interpreter/intrp-rdr.toc: 632
Term glblVal1908_2;
glblVal1908 = dupeArg(glblVal1908, &glblVal1908_2, 0);

Term glblVal1896_7;
glblVal1896 = dupeArg(glblVal1896, &glblVal1896_7, 0);

// call str= at /home/jim/toccata/interpreter/intrp-rdr.toc: 627
Term Rslt_13Args = makePair(APP, 0, glblStr254, SUB);
Term Rslt_13 = newTerm(VAR, 0, portLoc(2, Rslt_13Args));
Rslt_13Args = makePair(APP, 0, c_10, Rslt_13Args);
#ifdef STRICT
pushRedex(Rslt_13Args, glblFn363);
#else
swap(termLoc(Rslt_13), makePair(LAZ, 0, Rslt_13Args, glblFn363));
#endif

// call take-char at /home/jim/toccata/interpreter/intrp-rdr.toc: 628
Term Rslt_14Args = makePair(APP, 0, s_6, SUB);
Term Rslt_14 = newTerm(VAR, 0, portLoc(2, Rslt_14Args));
#ifdef STRICT
pushRedex(Rslt_14Args, glblFn1732);
#else
swap(termLoc(Rslt_14), makePair(LAZ, 0, Rslt_14Args, glblFn1732));
#endif

// call ParserMatch at /home/jim/toccata/interpreter/intrp-rdr.toc: 628
Term Rslt_15Args = makePair(APP, 0, Rslt_14, SUB);
Term Rslt_15 = newTerm(VAR, 0, portLoc(2, Rslt_15Args));
Rslt_15Args = makePair(APP, 0, acc_2, Rslt_15Args);
#ifdef STRICT
pushRedex(Rslt_15Args, glblVal1896_7);
#else
swap(termLoc(Rslt_15), makePair(LAZ, 0, Rslt_15Args, glblVal1896_7));
#endif

// call str= at /home/jim/toccata/interpreter/intrp-rdr.toc: 629
Term Rslt_16Args = makePair(APP, 0, glblStr402, SUB);
Term Rslt_16 = newTerm(VAR, 0, portLoc(2, Rslt_16Args));
Rslt_16Args = makePair(APP, 0, c_10_1, Rslt_16Args);
#ifdef STRICT
pushRedex(Rslt_16Args, glblFn363);
#else
swap(termLoc(Rslt_16), makePair(LAZ, 0, Rslt_16Args, glblFn363));
#endif

// call ParserError at /home/jim/toccata/interpreter/intrp-rdr.toc: 630
Term Rslt_17Args = makePair(APP, 0, s_6_1, SUB);
Term Rslt_17 = newTerm(VAR, 0, portLoc(2, Rslt_17Args));
Rslt_17Args = makePair(APP, 0, glblStr2437, Rslt_17Args);
#ifdef STRICT
pushRedex(Rslt_17Args, glblVal1908_2);
#else
swap(termLoc(Rslt_17), makePair(LAZ, 0, Rslt_17Args, glblVal1908_2));
#endif

// call = at /home/jim/toccata/interpreter/intrp-rdr.toc: 634
Term Rslt_26Args = makePair(APP, 0, newI60(2), SUB);
Term Rslt_26 = newTerm(VAR, 0, portLoc(2, Rslt_26Args));
Rslt_26Args = makePair(APP, 0, k_23, Rslt_26Args);
#ifdef STRICT
pushRedex(Rslt_26Args, newRef(glblProto20_2438));
#else
swap(termLoc(Rslt_26), makePair(LAZ, 0, Rslt_26Args, newRef(glblProto20_2438)));
#endif
// call .state at /home/jim/toccata/interpreter/intrp-rdr.toc: 636
Term Rslt_27Args = makePair(APP, 0, r_19_1, SUB);
Term Rslt_27 = newTerm(VAR, 0, portLoc(2, Rslt_27Args));
#ifdef STRICT
pushRedex(Rslt_27Args, newRef(glblProto1889_2440));
#else
swap(termLoc(Rslt_27), makePair(LAZ, 0, Rslt_27Args, newRef(glblProto1889_2440)));
#endif
// call .value at /home/jim/toccata/interpreter/intrp-rdr.toc: 637
Term Rslt_28Args = makePair(APP, 0, r_19_2, SUB);
Term Rslt_28 = newTerm(VAR, 0, portLoc(2, Rslt_28Args));
#ifdef STRICT
pushRedex(Rslt_28Args, newRef(glblProto1562_2442));
#else
swap(termLoc(Rslt_28), makePair(LAZ, 0, Rslt_28Args, newRef(glblProto1562_2442)));
#endif
// call vect-conj at /home/jim/toccata/interpreter/intrp-rdr.toc: 637
Term Rslt_29Args = makePair(APP, 0, Rslt_28, SUB);
Term Rslt_29 = newTerm(VAR, 0, portLoc(2, Rslt_29Args));
Rslt_29Args = makePair(APP, 0, acc_2_1, Rslt_29Args);
#ifdef STRICT
pushRedex(Rslt_29Args, glblFn178);
#else
swap(termLoc(Rslt_29), makePair(LAZ, 0, Rslt_29Args, glblFn178));
#endif

// call parse-threading-steps-acc at /home/jim/toccata/interpreter/intrp-rdr.toc: 623
Term Rslt_30Args = makePair(APP, 0, Rslt_29, SUB);
Term Rslt_30 = newTerm(VAR, 0, portLoc(2, Rslt_30Args));
Rslt_30Args = makePair(APP, 0, Rslt_27, Rslt_30Args);
#ifdef STRICT
pushRedex(Rslt_30Args, glblFn1837);
#else
swap(termLoc(Rslt_30), makePair(LAZ, 0, Rslt_30Args, glblFn1837));
#endif

// call cond at /home/jim/toccata/interpreter/intrp-rdr.toc: 634
Term Rslt_31Args = makePair(APP, 0, Rslt_30, SUB);
Term Rslt_31 = newTerm(VAR, 0, portLoc(2, Rslt_31Args));
Rslt_31Args = makePair(APP, 0, r_19, Rslt_31Args);
Rslt_31Args = makePair(APP, 0, Rslt_26, Rslt_31Args);
#ifdef STRICT
pushRedex(Rslt_31Args, newRef(glblProto34_2444));
#else
swap(termLoc(Rslt_31), makePair(LAZ, 0, Rslt_31Args, newRef(glblProto34_2444)));
#endif
// link args to body
swap(r_seq_24, Rslt_31);

Term Rslt_31V = get(termLoc(Rslt_31));
apps = take(portLoc(1, Rslt_31V));
lams = take(portLoc(2, Rslt_31V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_31), makePair(LAZ, 0, apps, lams));
// call result-kind at /home/jim/toccata/interpreter/intrp-rdr.toc: 632
Term Rslt_32Args = makePair(APP, 0, r_19_3, SUB);
Term Rslt_32 = newTerm(VAR, 0, portLoc(2, Rslt_32Args));
#ifdef STRICT
pushRedex(Rslt_32Args, newRef(glblProto1757_2446));
#else
swap(termLoc(Rslt_32), makePair(LAZ, 0, Rslt_32Args, newRef(glblProto1757_2446)));
#endif
// call  at /home/jim/toccata/interpreter/intrp-rdr.toc: 632
Term Rslt_33Args = makePair(APP, 0, Rslt_32, SUB);
Term Rslt_33 = newTerm(VAR, 0, portLoc(2, Rslt_33Args));
#ifdef STRICT
pushRedex(Rslt_33Args, seq_24);
#else
swap(termLoc(Rslt_33), makePair(LAZ, 0, Rslt_33Args, seq_24));
#endif

// link args to body
swap(r_seq_20, Rslt_33);

Term Rslt_33V = get(termLoc(Rslt_33));
apps = take(portLoc(1, Rslt_33V));
lams = take(portLoc(2, Rslt_33V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_33), makePair(LAZ, 0, apps, lams));
// call parse-expr at /home/jim/toccata/interpreter/intrp-rdr.toc: 548
Term Rslt_34Args = makePair(APP, 0, s_6_2, SUB);
Term Rslt_34 = newTerm(VAR, 0, portLoc(2, Rslt_34Args));
#ifdef STRICT
pushRedex(Rslt_34Args, glblVal1819_6);
#else
swap(termLoc(Rslt_34), makePair(LAZ, 0, Rslt_34Args, glblVal1819_6));
#endif

// call  at /home/jim/toccata/interpreter/intrp-rdr.toc: 548
Term Rslt_35Args = makePair(APP, 0, Rslt_34, SUB);
Term Rslt_35 = newTerm(VAR, 0, portLoc(2, Rslt_35Args));
#ifdef STRICT
pushRedex(Rslt_35Args, seq_20);
#else
swap(termLoc(Rslt_35), makePair(LAZ, 0, Rslt_35Args, seq_20));
#endif

// call cond at /home/jim/toccata/interpreter/intrp-rdr.toc: 627
Term Rslt_36Args = makePair(APP, 0, Rslt_35, SUB);
Term Rslt_36 = newTerm(VAR, 0, portLoc(2, Rslt_36Args));
Rslt_36Args = makePair(APP, 0, Rslt_17, Rslt_36Args);
Rslt_36Args = makePair(APP, 0, Rslt_16, Rslt_36Args);
#ifdef STRICT
pushRedex(Rslt_36Args, newRef(glblProto34_2448));
#else
swap(termLoc(Rslt_36), makePair(LAZ, 0, Rslt_36Args, newRef(glblProto34_2448)));
#endif
// call cond at /home/jim/toccata/interpreter/intrp-rdr.toc: 627
Term Rslt_37Args = makePair(APP, 0, Rslt_36, SUB);
Term Rslt_37 = newTerm(VAR, 0, portLoc(2, Rslt_37Args));
Rslt_37Args = makePair(APP, 0, Rslt_15, Rslt_37Args);
Rslt_37Args = makePair(APP, 0, Rslt_13, Rslt_37Args);
#ifdef STRICT
pushRedex(Rslt_37Args, newRef(glblProto34_2450));
#else
swap(termLoc(Rslt_37), makePair(LAZ, 0, Rslt_37Args, newRef(glblProto34_2450)));
#endif
// link args to body
swap(r_seq_11, Rslt_37);

Term Rslt_37V = get(termLoc(Rslt_37));
apps = take(portLoc(1, Rslt_37V));
lams = take(portLoc(2, Rslt_37V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_37), makePair(LAZ, 0, apps, lams));
// call peek-char at /home/jim/toccata/interpreter/intrp-rdr.toc: 625
Term Rslt_38Args = makePair(APP, 0, s_6_3, SUB);
Term Rslt_38 = newTerm(VAR, 0, portLoc(2, Rslt_38Args));
#ifdef STRICT
pushRedex(Rslt_38Args, glblFn1730);
#else
swap(termLoc(Rslt_38), makePair(LAZ, 0, Rslt_38Args, glblFn1730));
#endif

// call  at /home/jim/toccata/interpreter/intrp-rdr.toc: 625
Term Rslt_39Args = makePair(APP, 0, Rslt_38, SUB);
Term Rslt_39 = newTerm(VAR, 0, portLoc(2, Rslt_39Args));
#ifdef STRICT
pushRedex(Rslt_39Args, seq_11);
#else
swap(termLoc(Rslt_39), makePair(LAZ, 0, Rslt_39Args, seq_11));
#endif

// link args to body
swap(r_seq_7, Rslt_39);

Term Rslt_39V = get(termLoc(Rslt_39));
apps = take(portLoc(1, Rslt_39V));
lams = take(portLoc(2, Rslt_39V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_39), makePair(LAZ, 0, apps, lams));
// call skip-whitespace at /home/jim/toccata/interpreter/intrp-rdr.toc: 624
Term Rslt_40Args = makePair(APP, 0, state_1, SUB);
Term Rslt_40 = newTerm(VAR, 0, portLoc(2, Rslt_40Args));
#ifdef STRICT
pushRedex(Rslt_40Args, glblFn1736);
#else
swap(termLoc(Rslt_40), makePair(LAZ, 0, Rslt_40Args, glblFn1736));
#endif

// call  at /home/jim/toccata/interpreter/intrp-rdr.toc: 624
Term Rslt_41Args = makePair(APP, 0, Rslt_40, SUB);
Term Rslt_41 = newTerm(VAR, 0, portLoc(2, Rslt_41Args));
#ifdef STRICT
pushRedex(Rslt_41Args, seq_7);
#else
swap(termLoc(Rslt_41), makePair(LAZ, 0, Rslt_41Args, seq_7));
#endif

// link args to body
swap(r_seq_3, Rslt_41);

Term Rslt_41V = get(termLoc(Rslt_41));
apps = take(portLoc(1, Rslt_41V));
lams = take(portLoc(2, Rslt_41V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_41), makePair(LAZ, 0, apps, lams));
pushRedex(args, seq_3);
return;
}
Term glblFn1837 = newRef(glblCFn1836);
Term glblFn1839;
void glblCFn1838(Term ref, Term args) {
Term apps, lams, V;
Location vLoc;
Term seq_2 = makePair(LAM, 0, SUB, NUL);
Location r_seq_2 = portLoc(2, seq_2);
Term state_1 = newTerm(VAR, 0, portLoc(1, seq_2));
// allocate args at /home/jim/toccata/interpreter/intrp-rdr.toc: 641
Term glblVal175_73;
glblVal175 = dupeArg(glblVal175, &glblVal175_73, 0);

// call parse-threading-steps-acc at /home/jim/toccata/interpreter/intrp-rdr.toc: 642
Term Rslt_4Args = makePair(APP, 0, glblVal175_73, SUB);
Term Rslt_4 = newTerm(VAR, 0, portLoc(2, Rslt_4Args));
Rslt_4Args = makePair(APP, 0, state_1, Rslt_4Args);
#ifdef STRICT
pushRedex(Rslt_4Args, glblFn1837);
#else
swap(termLoc(Rslt_4), makePair(LAZ, 0, Rslt_4Args, glblFn1837));
#endif

// link args to body
swap(r_seq_2, Rslt_4);

Term Rslt_4V = get(termLoc(Rslt_4));
apps = take(portLoc(1, Rslt_4V));
lams = take(portLoc(2, Rslt_4V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_4), makePair(LAZ, 0, apps, lams));
pushRedex(args, seq_2);
return;
}
Term glblFn1839 = newRef(glblCFn1838);
void glblProto20_2455(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(650), args);
  args = makePair(APP, 0, glblStr1912, args);
  interact(args, glblProto20);
}
}
void glblProto1562_2457(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(652), args);
  args = makePair(APP, 0, glblStr1912, args);
  interact(args, glblProto1562);
}
}
void glblProto1889_2459(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(652), args);
  args = makePair(APP, 0, glblStr1912, args);
  interact(args, glblProto1889);
}
}
void glblProto34_2461(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(650), args);
  args = makePair(APP, 0, glblStr1912, args);
  interact(args, glblProto34);
}
}
void glblProto1757_2463(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(648), args);
  args = makePair(APP, 0, glblStr1912, args);
  interact(args, glblProto1757);
}
}
Term glblFn1841;
void glblCFn1840(Term ref, Term args) {
Term apps, lams, V;
Location vLoc;
Term seq_3 = makePair(LAM, 0, SUB, NUL);
Location r_seq_3 = portLoc(2, seq_3);
Term op_2 = newTerm(VAR, 0, portLoc(1, seq_3));
seq_3 = makePair(LAM, 0, SUB, seq_3);
Term state_1 = newTerm(VAR, 0, portLoc(1, seq_3));
// allocate args at /home/jim/toccata/interpreter/intrp-rdr.toc: 646
Term seq_7 = makePair(LAM, 0, SUB, NUL);
Location r_seq_7 = portLoc(2, seq_7);
Term args_6 = newTerm(VAR, 0, portLoc(1, seq_7));
// allocate args at /home/jim/toccata/interpreter/intrp-rdr.toc: 647

Term args_6_3;
args_6 = dupeArg(args_6, &args_6_3, 0);

Term args_6_2;
args_6 = dupeArg(args_6, &args_6_2, 0);

Term args_6_1;
args_6 = dupeArg(args_6, &args_6_1, 0);
Term seq_11 = makePair(LAM, 0, SUB, NUL);
Location r_seq_11 = portLoc(2, seq_11);
Term k_10 = newTerm(VAR, 0, portLoc(1, seq_11));
// allocate args at /home/jim/toccata/interpreter/intrp-rdr.toc: 648
Term glblVal1591_13;
glblVal1591 = dupeArg(glblVal1591, &glblVal1591_13, 0);

Term glblVal1896_8;
glblVal1896 = dupeArg(glblVal1896, &glblVal1896_8, 0);

// call = at /home/jim/toccata/interpreter/intrp-rdr.toc: 650
Term Rslt_13Args = makePair(APP, 0, newI60(2), SUB);
Term Rslt_13 = newTerm(VAR, 0, portLoc(2, Rslt_13Args));
Rslt_13Args = makePair(APP, 0, k_10, Rslt_13Args);
#ifdef STRICT
pushRedex(Rslt_13Args, newRef(glblProto20_2455));
#else
swap(termLoc(Rslt_13), makePair(LAZ, 0, Rslt_13Args, newRef(glblProto20_2455)));
#endif
// call .value at /home/jim/toccata/interpreter/intrp-rdr.toc: 652
Term Rslt_14Args = makePair(APP, 0, args_6_1, SUB);
Term Rslt_14 = newTerm(VAR, 0, portLoc(2, Rslt_14Args));
#ifdef STRICT
pushRedex(Rslt_14Args, newRef(glblProto1562_2457));
#else
swap(termLoc(Rslt_14), makePair(LAZ, 0, Rslt_14Args, newRef(glblProto1562_2457)));
#endif
// call ast/Call at /home/jim/toccata/interpreter/intrp-rdr.toc: 652
Term Rslt_15Args = makePair(APP, 0, Rslt_14, SUB);
Term Rslt_15 = newTerm(VAR, 0, portLoc(2, Rslt_15Args));
Rslt_15Args = makePair(APP, 0, op_2, Rslt_15Args);
#ifdef STRICT
pushRedex(Rslt_15Args, glblVal1591_13);
#else
swap(termLoc(Rslt_15), makePair(LAZ, 0, Rslt_15Args, glblVal1591_13));
#endif

// call .state at /home/jim/toccata/interpreter/intrp-rdr.toc: 652
Term Rslt_16Args = makePair(APP, 0, args_6_2, SUB);
Term Rslt_16 = newTerm(VAR, 0, portLoc(2, Rslt_16Args));
#ifdef STRICT
pushRedex(Rslt_16Args, newRef(glblProto1889_2459));
#else
swap(termLoc(Rslt_16), makePair(LAZ, 0, Rslt_16Args, newRef(glblProto1889_2459)));
#endif
// call ParserMatch at /home/jim/toccata/interpreter/intrp-rdr.toc: 652
Term Rslt_17Args = makePair(APP, 0, Rslt_16, SUB);
Term Rslt_17 = newTerm(VAR, 0, portLoc(2, Rslt_17Args));
Rslt_17Args = makePair(APP, 0, Rslt_15, Rslt_17Args);
#ifdef STRICT
pushRedex(Rslt_17Args, glblVal1896_8);
#else
swap(termLoc(Rslt_17), makePair(LAZ, 0, Rslt_17Args, glblVal1896_8));
#endif

// call cond at /home/jim/toccata/interpreter/intrp-rdr.toc: 650
Term Rslt_18Args = makePair(APP, 0, Rslt_17, SUB);
Term Rslt_18 = newTerm(VAR, 0, portLoc(2, Rslt_18Args));
Rslt_18Args = makePair(APP, 0, args_6, Rslt_18Args);
Rslt_18Args = makePair(APP, 0, Rslt_13, Rslt_18Args);
#ifdef STRICT
pushRedex(Rslt_18Args, newRef(glblProto34_2461));
#else
swap(termLoc(Rslt_18), makePair(LAZ, 0, Rslt_18Args, newRef(glblProto34_2461)));
#endif
// link args to body
swap(r_seq_11, Rslt_18);

Term Rslt_18V = get(termLoc(Rslt_18));
apps = take(portLoc(1, Rslt_18V));
lams = take(portLoc(2, Rslt_18V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_18), makePair(LAZ, 0, apps, lams));
// call result-kind at /home/jim/toccata/interpreter/intrp-rdr.toc: 648
Term Rslt_19Args = makePair(APP, 0, args_6_3, SUB);
Term Rslt_19 = newTerm(VAR, 0, portLoc(2, Rslt_19Args));
#ifdef STRICT
pushRedex(Rslt_19Args, newRef(glblProto1757_2463));
#else
swap(termLoc(Rslt_19), makePair(LAZ, 0, Rslt_19Args, newRef(glblProto1757_2463)));
#endif
// call  at /home/jim/toccata/interpreter/intrp-rdr.toc: 648
Term Rslt_20Args = makePair(APP, 0, Rslt_19, SUB);
Term Rslt_20 = newTerm(VAR, 0, portLoc(2, Rslt_20Args));
#ifdef STRICT
pushRedex(Rslt_20Args, seq_11);
#else
swap(termLoc(Rslt_20), makePair(LAZ, 0, Rslt_20Args, seq_11));
#endif

// link args to body
swap(r_seq_7, Rslt_20);

Term Rslt_20V = get(termLoc(Rslt_20));
apps = take(portLoc(1, Rslt_20V));
lams = take(portLoc(2, Rslt_20V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_20), makePair(LAZ, 0, apps, lams));
// call parse-expr-list at /home/jim/toccata/interpreter/intrp-rdr.toc: 647
Term Rslt_21Args = makePair(APP, 0, glblStr254, SUB);
Term Rslt_21 = newTerm(VAR, 0, portLoc(2, Rslt_21Args));
Rslt_21Args = makePair(APP, 0, state_1, Rslt_21Args);
#ifdef STRICT
pushRedex(Rslt_21Args, glblFn1823);
#else
swap(termLoc(Rslt_21), makePair(LAZ, 0, Rslt_21Args, glblFn1823));
#endif

// call  at /home/jim/toccata/interpreter/intrp-rdr.toc: 647
Term Rslt_22Args = makePair(APP, 0, Rslt_21, SUB);
Term Rslt_22 = newTerm(VAR, 0, portLoc(2, Rslt_22Args));
#ifdef STRICT
pushRedex(Rslt_22Args, seq_7);
#else
swap(termLoc(Rslt_22), makePair(LAZ, 0, Rslt_22Args, seq_7));
#endif

// link args to body
swap(r_seq_3, Rslt_22);

Term Rslt_22V = get(termLoc(Rslt_22));
apps = take(portLoc(1, Rslt_22V));
lams = take(portLoc(2, Rslt_22V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_22), makePair(LAZ, 0, apps, lams));
pushRedex(args, seq_3);
return;
}
Term glblFn1841 = newRef(glblCFn1840);
void glblProto20_2467(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(660), args);
  args = makePair(APP, 0, glblStr1912, args);
  interact(args, glblProto20);
}
}
void glblProto1562_2469(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(662), args);
  args = makePair(APP, 0, glblStr1912, args);
  interact(args, glblProto1562);
}
}
void glblProto1889_2471(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(662), args);
  args = makePair(APP, 0, glblStr1912, args);
  interact(args, glblProto1889);
}
}
void glblProto1889_2473(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(663), args);
  args = makePair(APP, 0, glblStr1912, args);
  interact(args, glblProto1889);
}
}
void glblProto34_2475(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(660), args);
  args = makePair(APP, 0, glblStr1912, args);
  interact(args, glblProto34);
}
}
void glblProto1757_2477(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(658), args);
  args = makePair(APP, 0, glblStr1912, args);
  interact(args, glblProto1757);
}
}
Term glblFn1843;
void glblCFn1842(Term ref, Term args) {
Term apps, lams, V;
Location vLoc;
Term seq_2 = makePair(LAM, 0, SUB, NUL);
Location r_seq_2 = portLoc(2, seq_2);
Term state_1 = newTerm(VAR, 0, portLoc(1, seq_2));
// allocate args at /home/jim/toccata/interpreter/intrp-rdr.toc: 655
Term seq_6 = makePair(LAM, 0, SUB, NUL);
Location r_seq_6 = portLoc(2, seq_6);
Term s_5 = newTerm(VAR, 0, portLoc(1, seq_6));
// allocate args at /home/jim/toccata/interpreter/intrp-rdr.toc: 656
Term seq_10 = makePair(LAM, 0, SUB, NUL);
Location r_seq_10 = portLoc(2, seq_10);
Term exprs_9 = newTerm(VAR, 0, portLoc(1, seq_10));
// allocate args at /home/jim/toccata/interpreter/intrp-rdr.toc: 657

Term exprs_9_4;
exprs_9 = dupeArg(exprs_9, &exprs_9_4, 0);

Term exprs_9_3;
exprs_9 = dupeArg(exprs_9, &exprs_9_3, 0);

Term exprs_9_2;
exprs_9 = dupeArg(exprs_9, &exprs_9_2, 0);

Term exprs_9_1;
exprs_9 = dupeArg(exprs_9, &exprs_9_1, 0);
Term seq_14 = makePair(LAM, 0, SUB, NUL);
Location r_seq_14 = portLoc(2, seq_14);
Term k_13 = newTerm(VAR, 0, portLoc(1, seq_14));
// allocate args at /home/jim/toccata/interpreter/intrp-rdr.toc: 658
Term glblVal1896_9;
glblVal1896 = dupeArg(glblVal1896, &glblVal1896_9, 0);

// call = at /home/jim/toccata/interpreter/intrp-rdr.toc: 660
Term Rslt_16Args = makePair(APP, 0, newI60(2), SUB);
Term Rslt_16 = newTerm(VAR, 0, portLoc(2, Rslt_16Args));
Rslt_16Args = makePair(APP, 0, k_13, Rslt_16Args);
#ifdef STRICT
pushRedex(Rslt_16Args, newRef(glblProto20_2467));
#else
swap(termLoc(Rslt_16), makePair(LAZ, 0, Rslt_16Args, newRef(glblProto20_2467)));
#endif
// call .value at /home/jim/toccata/interpreter/intrp-rdr.toc: 662
Term Rslt_17Args = makePair(APP, 0, exprs_9_1, SUB);
Term Rslt_17 = newTerm(VAR, 0, portLoc(2, Rslt_17Args));
#ifdef STRICT
pushRedex(Rslt_17Args, newRef(glblProto1562_2469));
#else
swap(termLoc(Rslt_17), makePair(LAZ, 0, Rslt_17Args, newRef(glblProto1562_2469)));
#endif
// call .state at /home/jim/toccata/interpreter/intrp-rdr.toc: 662
Term Rslt_18Args = makePair(APP, 0, exprs_9_2, SUB);
Term Rslt_18 = newTerm(VAR, 0, portLoc(2, Rslt_18Args));
#ifdef STRICT
pushRedex(Rslt_18Args, newRef(glblProto1889_2471));
#else
swap(termLoc(Rslt_18), makePair(LAZ, 0, Rslt_18Args, newRef(glblProto1889_2471)));
#endif
// call make-loc at /home/jim/toccata/interpreter/intrp-rdr.toc: 662
Term Rslt_19Args = makePair(APP, 0, Rslt_18, SUB);
Term Rslt_19 = newTerm(VAR, 0, portLoc(2, Rslt_19Args));
#ifdef STRICT
pushRedex(Rslt_19Args, glblFn1762);
#else
swap(termLoc(Rslt_19), makePair(LAZ, 0, Rslt_19Args, glblFn1762));
#endif

// call fold-vec at /home/jim/toccata/interpreter/intrp-rdr.toc: 662
Term Rslt_20Args = makePair(APP, 0, Rslt_19, SUB);
Term Rslt_20 = newTerm(VAR, 0, portLoc(2, Rslt_20Args));
Rslt_20Args = makePair(APP, 0, Rslt_17, Rslt_20Args);
#ifdef STRICT
pushRedex(Rslt_20Args, glblFn1778);
#else
swap(termLoc(Rslt_20), makePair(LAZ, 0, Rslt_20Args, glblFn1778));
#endif

// call .state at /home/jim/toccata/interpreter/intrp-rdr.toc: 663
Term Rslt_21Args = makePair(APP, 0, exprs_9_3, SUB);
Term Rslt_21 = newTerm(VAR, 0, portLoc(2, Rslt_21Args));
#ifdef STRICT
pushRedex(Rslt_21Args, newRef(glblProto1889_2473));
#else
swap(termLoc(Rslt_21), makePair(LAZ, 0, Rslt_21Args, newRef(glblProto1889_2473)));
#endif
// call ParserMatch at /home/jim/toccata/interpreter/intrp-rdr.toc: 662
Term Rslt_22Args = makePair(APP, 0, Rslt_21, SUB);
Term Rslt_22 = newTerm(VAR, 0, portLoc(2, Rslt_22Args));
Rslt_22Args = makePair(APP, 0, Rslt_20, Rslt_22Args);
#ifdef STRICT
pushRedex(Rslt_22Args, glblVal1896_9);
#else
swap(termLoc(Rslt_22), makePair(LAZ, 0, Rslt_22Args, glblVal1896_9));
#endif

// call cond at /home/jim/toccata/interpreter/intrp-rdr.toc: 660
Term Rslt_23Args = makePair(APP, 0, Rslt_22, SUB);
Term Rslt_23 = newTerm(VAR, 0, portLoc(2, Rslt_23Args));
Rslt_23Args = makePair(APP, 0, exprs_9, Rslt_23Args);
Rslt_23Args = makePair(APP, 0, Rslt_16, Rslt_23Args);
#ifdef STRICT
pushRedex(Rslt_23Args, newRef(glblProto34_2475));
#else
swap(termLoc(Rslt_23), makePair(LAZ, 0, Rslt_23Args, newRef(glblProto34_2475)));
#endif
// link args to body
swap(r_seq_14, Rslt_23);

Term Rslt_23V = get(termLoc(Rslt_23));
apps = take(portLoc(1, Rslt_23V));
lams = take(portLoc(2, Rslt_23V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_23), makePair(LAZ, 0, apps, lams));
// call result-kind at /home/jim/toccata/interpreter/intrp-rdr.toc: 658
Term Rslt_24Args = makePair(APP, 0, exprs_9_4, SUB);
Term Rslt_24 = newTerm(VAR, 0, portLoc(2, Rslt_24Args));
#ifdef STRICT
pushRedex(Rslt_24Args, newRef(glblProto1757_2477));
#else
swap(termLoc(Rslt_24), makePair(LAZ, 0, Rslt_24Args, newRef(glblProto1757_2477)));
#endif
// call  at /home/jim/toccata/interpreter/intrp-rdr.toc: 658
Term Rslt_25Args = makePair(APP, 0, Rslt_24, SUB);
Term Rslt_25 = newTerm(VAR, 0, portLoc(2, Rslt_25Args));
#ifdef STRICT
pushRedex(Rslt_25Args, seq_14);
#else
swap(termLoc(Rslt_25), makePair(LAZ, 0, Rslt_25Args, seq_14));
#endif

// link args to body
swap(r_seq_10, Rslt_25);

Term Rslt_25V = get(termLoc(Rslt_25));
apps = take(portLoc(1, Rslt_25V));
lams = take(portLoc(2, Rslt_25V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_25), makePair(LAZ, 0, apps, lams));
// call parse-expr-list at /home/jim/toccata/interpreter/intrp-rdr.toc: 657
Term Rslt_26Args = makePair(APP, 0, glblStr746, SUB);
Term Rslt_26 = newTerm(VAR, 0, portLoc(2, Rslt_26Args));
Rslt_26Args = makePair(APP, 0, s_5, Rslt_26Args);
#ifdef STRICT
pushRedex(Rslt_26Args, glblFn1823);
#else
swap(termLoc(Rslt_26), makePair(LAZ, 0, Rslt_26Args, glblFn1823));
#endif

// call  at /home/jim/toccata/interpreter/intrp-rdr.toc: 657
Term Rslt_27Args = makePair(APP, 0, Rslt_26, SUB);
Term Rslt_27 = newTerm(VAR, 0, portLoc(2, Rslt_27Args));
#ifdef STRICT
pushRedex(Rslt_27Args, seq_10);
#else
swap(termLoc(Rslt_27), makePair(LAZ, 0, Rslt_27Args, seq_10));
#endif

// link args to body
swap(r_seq_6, Rslt_27);

Term Rslt_27V = get(termLoc(Rslt_27));
apps = take(portLoc(1, Rslt_27V));
lams = take(portLoc(2, Rslt_27V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_27), makePair(LAZ, 0, apps, lams));
// call take-char at /home/jim/toccata/interpreter/intrp-rdr.toc: 656
Term Rslt_28Args = makePair(APP, 0, state_1, SUB);
Term Rslt_28 = newTerm(VAR, 0, portLoc(2, Rslt_28Args));
#ifdef STRICT
pushRedex(Rslt_28Args, glblFn1732);
#else
swap(termLoc(Rslt_28), makePair(LAZ, 0, Rslt_28Args, glblFn1732));
#endif

// call skip-whitespace at /home/jim/toccata/interpreter/intrp-rdr.toc: 656
Term Rslt_29Args = makePair(APP, 0, Rslt_28, SUB);
Term Rslt_29 = newTerm(VAR, 0, portLoc(2, Rslt_29Args));
#ifdef STRICT
pushRedex(Rslt_29Args, glblFn1736);
#else
swap(termLoc(Rslt_29), makePair(LAZ, 0, Rslt_29Args, glblFn1736));
#endif

// call  at /home/jim/toccata/interpreter/intrp-rdr.toc: 656
Term Rslt_30Args = makePair(APP, 0, Rslt_29, SUB);
Term Rslt_30 = newTerm(VAR, 0, portLoc(2, Rslt_30Args));
#ifdef STRICT
pushRedex(Rslt_30Args, seq_6);
#else
swap(termLoc(Rslt_30), makePair(LAZ, 0, Rslt_30Args, seq_6));
#endif

// link args to body
swap(r_seq_2, Rslt_30);

Term Rslt_30V = get(termLoc(Rslt_30));
apps = take(portLoc(1, Rslt_30V));
lams = take(portLoc(2, Rslt_30V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_30), makePair(LAZ, 0, apps, lams));
pushRedex(args, seq_2);
return;
}
Term glblFn1843 = newRef(glblCFn1842);
Term glblFn1845;
void glblCFn1844(Term ref, Term args) {
Term apps, lams, V;
Location vLoc;
Term seq_2 = makePair(LAM, 0, SUB, NUL);
Location r_seq_2 = portLoc(2, seq_2);
Term state_1 = newTerm(VAR, 0, portLoc(1, seq_2));
// allocate args at /home/jim/toccata/interpreter/intrp-rdr.toc: 666
Term seq_6 = makePair(LAM, 0, SUB, NUL);
Location r_seq_6 = portLoc(2, seq_6);
Term s_5 = newTerm(VAR, 0, portLoc(1, seq_6));
// allocate args at /home/jim/toccata/interpreter/intrp-rdr.toc: 667
Term seq_10 = makePair(LAM, 0, SUB, NUL);
Location r_seq_10 = portLoc(2, seq_10);
Term pairs_9 = newTerm(VAR, 0, portLoc(1, seq_10));
// allocate args at /home/jim/toccata/interpreter/intrp-rdr.toc: 668

Term pairs_9_2;
pairs_9 = dupeArg(pairs_9, &pairs_9_2, 0);

Term pairs_9_1;
pairs_9 = dupeArg(pairs_9, &pairs_9_1, 0);
Term glblVal1896_10;
glblVal1896 = dupeArg(glblVal1896, &glblVal1896_10, 0);

// call elt0 at /home/jim/toccata/interpreter/intrp-rdr.toc: 669
Term Rslt_12Args = makePair(APP, 0, pairs_9, SUB);
Term Rslt_12 = newTerm(VAR, 0, portLoc(2, Rslt_12Args));
#ifdef STRICT
pushRedex(Rslt_12Args, glblFn1742);
#else
swap(termLoc(Rslt_12), makePair(LAZ, 0, Rslt_12Args, glblFn1742));
#endif

// call elt1 at /home/jim/toccata/interpreter/intrp-rdr.toc: 669
Term Rslt_13Args = makePair(APP, 0, pairs_9_1, SUB);
Term Rslt_13 = newTerm(VAR, 0, portLoc(2, Rslt_13Args));
#ifdef STRICT
pushRedex(Rslt_13Args, glblFn1744);
#else
swap(termLoc(Rslt_13), makePair(LAZ, 0, Rslt_13Args, glblFn1744));
#endif

// call make-loc at /home/jim/toccata/interpreter/intrp-rdr.toc: 669
Term Rslt_14Args = makePair(APP, 0, Rslt_13, SUB);
Term Rslt_14 = newTerm(VAR, 0, portLoc(2, Rslt_14Args));
#ifdef STRICT
pushRedex(Rslt_14Args, glblFn1762);
#else
swap(termLoc(Rslt_14), makePair(LAZ, 0, Rslt_14Args, glblFn1762));
#endif

// call hash-fold at /home/jim/toccata/interpreter/intrp-rdr.toc: 669
Term Rslt_15Args = makePair(APP, 0, Rslt_14, SUB);
Term Rslt_15 = newTerm(VAR, 0, portLoc(2, Rslt_15Args));
Rslt_15Args = makePair(APP, 0, Rslt_12, Rslt_15Args);
#ifdef STRICT
pushRedex(Rslt_15Args, glblFn1782);
#else
swap(termLoc(Rslt_15), makePair(LAZ, 0, Rslt_15Args, glblFn1782));
#endif

// call elt1 at /home/jim/toccata/interpreter/intrp-rdr.toc: 670
Term Rslt_16Args = makePair(APP, 0, pairs_9_2, SUB);
Term Rslt_16 = newTerm(VAR, 0, portLoc(2, Rslt_16Args));
#ifdef STRICT
pushRedex(Rslt_16Args, glblFn1744);
#else
swap(termLoc(Rslt_16), makePair(LAZ, 0, Rslt_16Args, glblFn1744));
#endif

// call ParserMatch at /home/jim/toccata/interpreter/intrp-rdr.toc: 669
Term Rslt_17Args = makePair(APP, 0, Rslt_16, SUB);
Term Rslt_17 = newTerm(VAR, 0, portLoc(2, Rslt_17Args));
Rslt_17Args = makePair(APP, 0, Rslt_15, Rslt_17Args);
#ifdef STRICT
pushRedex(Rslt_17Args, glblVal1896_10);
#else
swap(termLoc(Rslt_17), makePair(LAZ, 0, Rslt_17Args, glblVal1896_10));
#endif

// link args to body
swap(r_seq_10, Rslt_17);

Term Rslt_17V = get(termLoc(Rslt_17));
apps = take(portLoc(1, Rslt_17V));
lams = take(portLoc(2, Rslt_17V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_17), makePair(LAZ, 0, apps, lams));
// call read-hash-pairs at /home/jim/toccata/interpreter/intrp-rdr.toc: 668
Term Rslt_18Args = makePair(APP, 0, s_5, SUB);
Term Rslt_18 = newTerm(VAR, 0, portLoc(2, Rslt_18Args));
#ifdef STRICT
pushRedex(Rslt_18Args, glblFn1835);
#else
swap(termLoc(Rslt_18), makePair(LAZ, 0, Rslt_18Args, glblFn1835));
#endif

// call  at /home/jim/toccata/interpreter/intrp-rdr.toc: 668
Term Rslt_19Args = makePair(APP, 0, Rslt_18, SUB);
Term Rslt_19 = newTerm(VAR, 0, portLoc(2, Rslt_19Args));
#ifdef STRICT
pushRedex(Rslt_19Args, seq_10);
#else
swap(termLoc(Rslt_19), makePair(LAZ, 0, Rslt_19Args, seq_10));
#endif

// link args to body
swap(r_seq_6, Rslt_19);

Term Rslt_19V = get(termLoc(Rslt_19));
apps = take(portLoc(1, Rslt_19V));
lams = take(portLoc(2, Rslt_19V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_19), makePair(LAZ, 0, apps, lams));
// call take-char at /home/jim/toccata/interpreter/intrp-rdr.toc: 667
Term Rslt_20Args = makePair(APP, 0, state_1, SUB);
Term Rslt_20 = newTerm(VAR, 0, portLoc(2, Rslt_20Args));
#ifdef STRICT
pushRedex(Rslt_20Args, glblFn1732);
#else
swap(termLoc(Rslt_20), makePair(LAZ, 0, Rslt_20Args, glblFn1732));
#endif

// call skip-whitespace at /home/jim/toccata/interpreter/intrp-rdr.toc: 667
Term Rslt_21Args = makePair(APP, 0, Rslt_20, SUB);
Term Rslt_21 = newTerm(VAR, 0, portLoc(2, Rslt_21Args));
#ifdef STRICT
pushRedex(Rslt_21Args, glblFn1736);
#else
swap(termLoc(Rslt_21), makePair(LAZ, 0, Rslt_21Args, glblFn1736));
#endif

// call  at /home/jim/toccata/interpreter/intrp-rdr.toc: 667
Term Rslt_22Args = makePair(APP, 0, Rslt_21, SUB);
Term Rslt_22 = newTerm(VAR, 0, portLoc(2, Rslt_22Args));
#ifdef STRICT
pushRedex(Rslt_22Args, seq_6);
#else
swap(termLoc(Rslt_22), makePair(LAZ, 0, Rslt_22Args, seq_6));
#endif

// link args to body
swap(r_seq_2, Rslt_22);

Term Rslt_22V = get(termLoc(Rslt_22));
apps = take(portLoc(1, Rslt_22V));
lams = take(portLoc(2, Rslt_22V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_22), makePair(LAZ, 0, apps, lams));
pushRedex(args, seq_2);
return;
}
Term glblFn1845 = newRef(glblCFn1844);
void glblProto20_2482(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(678), args);
  args = makePair(APP, 0, glblStr1912, args);
  interact(args, glblProto20);
}
}
void glblProto1562_2484(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(680), args);
  args = makePair(APP, 0, glblStr1912, args);
  interact(args, glblProto1562);
}
}
void glblProto1889_2486(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(680), args);
  args = makePair(APP, 0, glblStr1912, args);
  interact(args, glblProto1889);
}
}
void glblProto1889_2488(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(681), args);
  args = makePair(APP, 0, glblStr1912, args);
  interact(args, glblProto1889);
}
}
void glblProto34_2490(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(678), args);
  args = makePair(APP, 0, glblStr1912, args);
  interact(args, glblProto34);
}
}
void glblProto1757_2492(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(676), args);
  args = makePair(APP, 0, glblStr1912, args);
  interact(args, glblProto1757);
}
}
Term glblFn1847;
void glblCFn1846(Term ref, Term args) {
Term apps, lams, V;
Location vLoc;
Term seq_2 = makePair(LAM, 0, SUB, NUL);
Location r_seq_2 = portLoc(2, seq_2);
Term state_1 = newTerm(VAR, 0, portLoc(1, seq_2));
// allocate args at /home/jim/toccata/interpreter/intrp-rdr.toc: 674
Term seq_6 = makePair(LAM, 0, SUB, NUL);
Location r_seq_6 = portLoc(2, seq_6);
Term exprs_5 = newTerm(VAR, 0, portLoc(1, seq_6));
// allocate args at /home/jim/toccata/interpreter/intrp-rdr.toc: 675

Term exprs_5_4;
exprs_5 = dupeArg(exprs_5, &exprs_5_4, 0);

Term exprs_5_3;
exprs_5 = dupeArg(exprs_5, &exprs_5_3, 0);

Term exprs_5_2;
exprs_5 = dupeArg(exprs_5, &exprs_5_2, 0);

Term exprs_5_1;
exprs_5 = dupeArg(exprs_5, &exprs_5_1, 0);
Term seq_10 = makePair(LAM, 0, SUB, NUL);
Location r_seq_10 = portLoc(2, seq_10);
Term k_9 = newTerm(VAR, 0, portLoc(1, seq_10));
// allocate args at /home/jim/toccata/interpreter/intrp-rdr.toc: 676
Term glblVal1896_11;
glblVal1896 = dupeArg(glblVal1896, &glblVal1896_11, 0);

// call = at /home/jim/toccata/interpreter/intrp-rdr.toc: 678
Term Rslt_12Args = makePair(APP, 0, newI60(2), SUB);
Term Rslt_12 = newTerm(VAR, 0, portLoc(2, Rslt_12Args));
Rslt_12Args = makePair(APP, 0, k_9, Rslt_12Args);
#ifdef STRICT
pushRedex(Rslt_12Args, newRef(glblProto20_2482));
#else
swap(termLoc(Rslt_12), makePair(LAZ, 0, Rslt_12Args, newRef(glblProto20_2482)));
#endif
// call .value at /home/jim/toccata/interpreter/intrp-rdr.toc: 680
Term Rslt_13Args = makePair(APP, 0, exprs_5_1, SUB);
Term Rslt_13 = newTerm(VAR, 0, portLoc(2, Rslt_13Args));
#ifdef STRICT
pushRedex(Rslt_13Args, newRef(glblProto1562_2484));
#else
swap(termLoc(Rslt_13), makePair(LAZ, 0, Rslt_13Args, newRef(glblProto1562_2484)));
#endif
// call .state at /home/jim/toccata/interpreter/intrp-rdr.toc: 680
Term Rslt_14Args = makePair(APP, 0, exprs_5_2, SUB);
Term Rslt_14 = newTerm(VAR, 0, portLoc(2, Rslt_14Args));
#ifdef STRICT
pushRedex(Rslt_14Args, newRef(glblProto1889_2486));
#else
swap(termLoc(Rslt_14), makePair(LAZ, 0, Rslt_14Args, newRef(glblProto1889_2486)));
#endif
// call make-loc at /home/jim/toccata/interpreter/intrp-rdr.toc: 680
Term Rslt_15Args = makePair(APP, 0, Rslt_14, SUB);
Term Rslt_15 = newTerm(VAR, 0, portLoc(2, Rslt_15Args));
#ifdef STRICT
pushRedex(Rslt_15Args, glblFn1762);
#else
swap(termLoc(Rslt_15), makePair(LAZ, 0, Rslt_15Args, glblFn1762));
#endif

// call cond-desugar at /home/jim/toccata/interpreter/intrp-rdr.toc: 680
Term Rslt_16Args = makePair(APP, 0, Rslt_15, SUB);
Term Rslt_16 = newTerm(VAR, 0, portLoc(2, Rslt_16Args));
Rslt_16Args = makePair(APP, 0, Rslt_13, Rslt_16Args);
#ifdef STRICT
pushRedex(Rslt_16Args, glblFn1788);
#else
swap(termLoc(Rslt_16), makePair(LAZ, 0, Rslt_16Args, glblFn1788));
#endif

// call .state at /home/jim/toccata/interpreter/intrp-rdr.toc: 681
Term Rslt_17Args = makePair(APP, 0, exprs_5_3, SUB);
Term Rslt_17 = newTerm(VAR, 0, portLoc(2, Rslt_17Args));
#ifdef STRICT
pushRedex(Rslt_17Args, newRef(glblProto1889_2488));
#else
swap(termLoc(Rslt_17), makePair(LAZ, 0, Rslt_17Args, newRef(glblProto1889_2488)));
#endif
// call ParserMatch at /home/jim/toccata/interpreter/intrp-rdr.toc: 680
Term Rslt_18Args = makePair(APP, 0, Rslt_17, SUB);
Term Rslt_18 = newTerm(VAR, 0, portLoc(2, Rslt_18Args));
Rslt_18Args = makePair(APP, 0, Rslt_16, Rslt_18Args);
#ifdef STRICT
pushRedex(Rslt_18Args, glblVal1896_11);
#else
swap(termLoc(Rslt_18), makePair(LAZ, 0, Rslt_18Args, glblVal1896_11));
#endif

// call cond at /home/jim/toccata/interpreter/intrp-rdr.toc: 678
Term Rslt_19Args = makePair(APP, 0, Rslt_18, SUB);
Term Rslt_19 = newTerm(VAR, 0, portLoc(2, Rslt_19Args));
Rslt_19Args = makePair(APP, 0, exprs_5, Rslt_19Args);
Rslt_19Args = makePair(APP, 0, Rslt_12, Rslt_19Args);
#ifdef STRICT
pushRedex(Rslt_19Args, newRef(glblProto34_2490));
#else
swap(termLoc(Rslt_19), makePair(LAZ, 0, Rslt_19Args, newRef(glblProto34_2490)));
#endif
// link args to body
swap(r_seq_10, Rslt_19);

Term Rslt_19V = get(termLoc(Rslt_19));
apps = take(portLoc(1, Rslt_19V));
lams = take(portLoc(2, Rslt_19V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_19), makePair(LAZ, 0, apps, lams));
// call result-kind at /home/jim/toccata/interpreter/intrp-rdr.toc: 676
Term Rslt_20Args = makePair(APP, 0, exprs_5_4, SUB);
Term Rslt_20 = newTerm(VAR, 0, portLoc(2, Rslt_20Args));
#ifdef STRICT
pushRedex(Rslt_20Args, newRef(glblProto1757_2492));
#else
swap(termLoc(Rslt_20), makePair(LAZ, 0, Rslt_20Args, newRef(glblProto1757_2492)));
#endif
// call  at /home/jim/toccata/interpreter/intrp-rdr.toc: 676
Term Rslt_21Args = makePair(APP, 0, Rslt_20, SUB);
Term Rslt_21 = newTerm(VAR, 0, portLoc(2, Rslt_21Args));
#ifdef STRICT
pushRedex(Rslt_21Args, seq_10);
#else
swap(termLoc(Rslt_21), makePair(LAZ, 0, Rslt_21Args, seq_10));
#endif

// link args to body
swap(r_seq_6, Rslt_21);

Term Rslt_21V = get(termLoc(Rslt_21));
apps = take(portLoc(1, Rslt_21V));
lams = take(portLoc(2, Rslt_21V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_21), makePair(LAZ, 0, apps, lams));
// call parse-expr-list at /home/jim/toccata/interpreter/intrp-rdr.toc: 675
Term Rslt_22Args = makePair(APP, 0, glblStr254, SUB);
Term Rslt_22 = newTerm(VAR, 0, portLoc(2, Rslt_22Args));
Rslt_22Args = makePair(APP, 0, state_1, Rslt_22Args);
#ifdef STRICT
pushRedex(Rslt_22Args, glblFn1823);
#else
swap(termLoc(Rslt_22), makePair(LAZ, 0, Rslt_22Args, glblFn1823));
#endif

// call  at /home/jim/toccata/interpreter/intrp-rdr.toc: 675
Term Rslt_23Args = makePair(APP, 0, Rslt_22, SUB);
Term Rslt_23 = newTerm(VAR, 0, portLoc(2, Rslt_23Args));
#ifdef STRICT
pushRedex(Rslt_23Args, seq_6);
#else
swap(termLoc(Rslt_23), makePair(LAZ, 0, Rslt_23Args, seq_6));
#endif

// link args to body
swap(r_seq_2, Rslt_23);

Term Rslt_23V = get(termLoc(Rslt_23));
apps = take(portLoc(1, Rslt_23V));
lams = take(portLoc(2, Rslt_23V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_23), makePair(LAZ, 0, apps, lams));
pushRedex(args, seq_2);
return;
}
Term glblFn1847 = newRef(glblCFn1846);
void glblProto20_2496(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(687), args);
  args = makePair(APP, 0, glblStr1912, args);
  interact(args, glblProto20);
}
}
void glblProto1562_2498(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(689), args);
  args = makePair(APP, 0, glblStr1912, args);
  interact(args, glblProto1562);
}
}
void glblProto1889_2501(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(690), args);
  args = makePair(APP, 0, glblStr1912, args);
  interact(args, glblProto1889);
}
}
void glblProto1889_2503(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(691), args);
  args = makePair(APP, 0, glblStr1912, args);
  interact(args, glblProto1889);
}
}
void glblProto34_2505(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(687), args);
  args = makePair(APP, 0, glblStr1912, args);
  interact(args, glblProto34);
}
}
void glblProto1757_2507(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(685), args);
  args = makePair(APP, 0, glblStr1912, args);
  interact(args, glblProto1757);
}
}
Term glblFn1849;
void glblCFn1848(Term ref, Term args) {
Term apps, lams, V;
Location vLoc;
Term seq_3 = makePair(LAM, 0, SUB, NUL);
Location r_seq_3 = portLoc(2, seq_3);
Term op_name_2 = newTerm(VAR, 0, portLoc(1, seq_3));
seq_3 = makePair(LAM, 0, SUB, seq_3);
Term state_1 = newTerm(VAR, 0, portLoc(1, seq_3));
// allocate args at /home/jim/toccata/interpreter/intrp-rdr.toc: 683
Term seq_7 = makePair(LAM, 0, SUB, NUL);
Location r_seq_7 = portLoc(2, seq_7);
Term exprs_6 = newTerm(VAR, 0, portLoc(1, seq_7));
// allocate args at /home/jim/toccata/interpreter/intrp-rdr.toc: 684

Term exprs_6_4;
exprs_6 = dupeArg(exprs_6, &exprs_6_4, 0);

Term exprs_6_3;
exprs_6 = dupeArg(exprs_6, &exprs_6_3, 0);

Term exprs_6_2;
exprs_6 = dupeArg(exprs_6, &exprs_6_2, 0);

Term exprs_6_1;
exprs_6 = dupeArg(exprs_6, &exprs_6_1, 0);
Term seq_11 = makePair(LAM, 0, SUB, NUL);
Location r_seq_11 = portLoc(2, seq_11);
Term k_10 = newTerm(VAR, 0, portLoc(1, seq_11));
// allocate args at /home/jim/toccata/interpreter/intrp-rdr.toc: 685
Term glblVal1896_12;
glblVal1896 = dupeArg(glblVal1896, &glblVal1896_12, 0);

// call = at /home/jim/toccata/interpreter/intrp-rdr.toc: 687
Term Rslt_13Args = makePair(APP, 0, newI60(2), SUB);
Term Rslt_13 = newTerm(VAR, 0, portLoc(2, Rslt_13Args));
Rslt_13Args = makePair(APP, 0, k_10, Rslt_13Args);
#ifdef STRICT
pushRedex(Rslt_13Args, newRef(glblProto20_2496));
#else
swap(termLoc(Rslt_13), makePair(LAZ, 0, Rslt_13Args, newRef(glblProto20_2496)));
#endif
// call .value at /home/jim/toccata/interpreter/intrp-rdr.toc: 689
Term Rslt_14Args = makePair(APP, 0, exprs_6_1, SUB);
Term Rslt_14 = newTerm(VAR, 0, portLoc(2, Rslt_14Args));
#ifdef STRICT
pushRedex(Rslt_14Args, newRef(glblProto1562_2498));
#else
swap(termLoc(Rslt_14), makePair(LAZ, 0, Rslt_14Args, newRef(glblProto1562_2498)));
#endif
// call .state at /home/jim/toccata/interpreter/intrp-rdr.toc: 690
Term Rslt_15Args = makePair(APP, 0, exprs_6_2, SUB);
Term Rslt_15 = newTerm(VAR, 0, portLoc(2, Rslt_15Args));
#ifdef STRICT
pushRedex(Rslt_15Args, newRef(glblProto1889_2501));
#else
swap(termLoc(Rslt_15), makePair(LAZ, 0, Rslt_15Args, newRef(glblProto1889_2501)));
#endif
// call make-loc at /home/jim/toccata/interpreter/intrp-rdr.toc: 690
Term Rslt_16Args = makePair(APP, 0, Rslt_15, SUB);
Term Rslt_16 = newTerm(VAR, 0, portLoc(2, Rslt_16Args));
#ifdef STRICT
pushRedex(Rslt_16Args, glblFn1762);
#else
swap(termLoc(Rslt_16), makePair(LAZ, 0, Rslt_16Args, glblFn1762));
#endif

// call and-or-desugar at /home/jim/toccata/interpreter/intrp-rdr.toc: 689
Term Rslt_17Args = makePair(APP, 0, op_name_2, SUB);
Term Rslt_17 = newTerm(VAR, 0, portLoc(2, Rslt_17Args));
Rslt_17Args = makePair(APP, 0, Rslt_16, Rslt_17Args);
Rslt_17Args = makePair(APP, 0, Rslt_14, Rslt_17Args);
#ifdef STRICT
pushRedex(Rslt_17Args, glblFn1792);
#else
swap(termLoc(Rslt_17), makePair(LAZ, 0, Rslt_17Args, glblFn1792));
#endif

// call .state at /home/jim/toccata/interpreter/intrp-rdr.toc: 691
Term Rslt_18Args = makePair(APP, 0, exprs_6_3, SUB);
Term Rslt_18 = newTerm(VAR, 0, portLoc(2, Rslt_18Args));
#ifdef STRICT
pushRedex(Rslt_18Args, newRef(glblProto1889_2503));
#else
swap(termLoc(Rslt_18), makePair(LAZ, 0, Rslt_18Args, newRef(glblProto1889_2503)));
#endif
// call ParserMatch at /home/jim/toccata/interpreter/intrp-rdr.toc: 689
Term Rslt_19Args = makePair(APP, 0, Rslt_18, SUB);
Term Rslt_19 = newTerm(VAR, 0, portLoc(2, Rslt_19Args));
Rslt_19Args = makePair(APP, 0, Rslt_17, Rslt_19Args);
#ifdef STRICT
pushRedex(Rslt_19Args, glblVal1896_12);
#else
swap(termLoc(Rslt_19), makePair(LAZ, 0, Rslt_19Args, glblVal1896_12));
#endif

// call cond at /home/jim/toccata/interpreter/intrp-rdr.toc: 687
Term Rslt_20Args = makePair(APP, 0, Rslt_19, SUB);
Term Rslt_20 = newTerm(VAR, 0, portLoc(2, Rslt_20Args));
Rslt_20Args = makePair(APP, 0, exprs_6, Rslt_20Args);
Rslt_20Args = makePair(APP, 0, Rslt_13, Rslt_20Args);
#ifdef STRICT
pushRedex(Rslt_20Args, newRef(glblProto34_2505));
#else
swap(termLoc(Rslt_20), makePair(LAZ, 0, Rslt_20Args, newRef(glblProto34_2505)));
#endif
// link args to body
swap(r_seq_11, Rslt_20);

Term Rslt_20V = get(termLoc(Rslt_20));
apps = take(portLoc(1, Rslt_20V));
lams = take(portLoc(2, Rslt_20V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_20), makePair(LAZ, 0, apps, lams));
// call result-kind at /home/jim/toccata/interpreter/intrp-rdr.toc: 685
Term Rslt_21Args = makePair(APP, 0, exprs_6_4, SUB);
Term Rslt_21 = newTerm(VAR, 0, portLoc(2, Rslt_21Args));
#ifdef STRICT
pushRedex(Rslt_21Args, newRef(glblProto1757_2507));
#else
swap(termLoc(Rslt_21), makePair(LAZ, 0, Rslt_21Args, newRef(glblProto1757_2507)));
#endif
// call  at /home/jim/toccata/interpreter/intrp-rdr.toc: 685
Term Rslt_22Args = makePair(APP, 0, Rslt_21, SUB);
Term Rslt_22 = newTerm(VAR, 0, portLoc(2, Rslt_22Args));
#ifdef STRICT
pushRedex(Rslt_22Args, seq_11);
#else
swap(termLoc(Rslt_22), makePair(LAZ, 0, Rslt_22Args, seq_11));
#endif

// link args to body
swap(r_seq_7, Rslt_22);

Term Rslt_22V = get(termLoc(Rslt_22));
apps = take(portLoc(1, Rslt_22V));
lams = take(portLoc(2, Rslt_22V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_22), makePair(LAZ, 0, apps, lams));
// call parse-expr-list at /home/jim/toccata/interpreter/intrp-rdr.toc: 684
Term Rslt_23Args = makePair(APP, 0, glblStr254, SUB);
Term Rslt_23 = newTerm(VAR, 0, portLoc(2, Rslt_23Args));
Rslt_23Args = makePair(APP, 0, state_1, Rslt_23Args);
#ifdef STRICT
pushRedex(Rslt_23Args, glblFn1823);
#else
swap(termLoc(Rslt_23), makePair(LAZ, 0, Rslt_23Args, glblFn1823));
#endif

// call  at /home/jim/toccata/interpreter/intrp-rdr.toc: 684
Term Rslt_24Args = makePair(APP, 0, Rslt_23, SUB);
Term Rslt_24 = newTerm(VAR, 0, portLoc(2, Rslt_24Args));
#ifdef STRICT
pushRedex(Rslt_24Args, seq_7);
#else
swap(termLoc(Rslt_24), makePair(LAZ, 0, Rslt_24Args, seq_7));
#endif

// link args to body
swap(r_seq_3, Rslt_24);

Term Rslt_24V = get(termLoc(Rslt_24));
apps = take(portLoc(1, Rslt_24V));
lams = take(portLoc(2, Rslt_24V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_24), makePair(LAZ, 0, apps, lams));
pushRedex(args, seq_3);
return;
}
Term glblFn1849 = newRef(glblCFn1848);
struct {TYPE_SIZE type;
        REFS_SIZE refs;
        int64_t *hash;
        Value *parent;
        int64_t len;
        char buffer[4];
} str39 = {StringBufferType, REFS_STATIC, 0, 0, 3, "and"};
Term glblStr2500 = newTerm_(VAL, (Term)&str39);
Term glblFn1851;
void glblCFn1850(Term ref, Term args) {
Term apps, lams, V;
Location vLoc;
Term seq_2 = makePair(LAM, 0, SUB, NUL);
Location r_seq_2 = portLoc(2, seq_2);
Term state_1 = newTerm(VAR, 0, portLoc(1, seq_2));
// allocate args at /home/jim/toccata/interpreter/intrp-rdr.toc: 694
// call parse-and-or at /home/jim/toccata/interpreter/intrp-rdr.toc: 695
Term Rslt_4Args = makePair(APP, 0, glblStr2500, SUB);
Term Rslt_4 = newTerm(VAR, 0, portLoc(2, Rslt_4Args));
Rslt_4Args = makePair(APP, 0, state_1, Rslt_4Args);
#ifdef STRICT
pushRedex(Rslt_4Args, glblFn1849);
#else
swap(termLoc(Rslt_4), makePair(LAZ, 0, Rslt_4Args, glblFn1849));
#endif

// link args to body
swap(r_seq_2, Rslt_4);

Term Rslt_4V = get(termLoc(Rslt_4));
apps = take(portLoc(1, Rslt_4V));
lams = take(portLoc(2, Rslt_4V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_4), makePair(LAZ, 0, apps, lams));
pushRedex(args, seq_2);
return;
}
Term glblFn1851 = newRef(glblCFn1850);
struct {TYPE_SIZE type;
        REFS_SIZE refs;
        int64_t *hash;
        Value *parent;
        int64_t len;
        char buffer[3];
} str40 = {StringBufferType, REFS_STATIC, 0, 0, 2, "or"};
Term glblStr2510 = newTerm_(VAL, (Term)&str40);
Term glblFn1853;
void glblCFn1852(Term ref, Term args) {
Term apps, lams, V;
Location vLoc;
Term seq_2 = makePair(LAM, 0, SUB, NUL);
Location r_seq_2 = portLoc(2, seq_2);
Term state_1 = newTerm(VAR, 0, portLoc(1, seq_2));
// allocate args at /home/jim/toccata/interpreter/intrp-rdr.toc: 697
// call parse-and-or at /home/jim/toccata/interpreter/intrp-rdr.toc: 698
Term Rslt_4Args = makePair(APP, 0, glblStr2510, SUB);
Term Rslt_4 = newTerm(VAR, 0, portLoc(2, Rslt_4Args));
Rslt_4Args = makePair(APP, 0, state_1, Rslt_4Args);
#ifdef STRICT
pushRedex(Rslt_4Args, glblFn1849);
#else
swap(termLoc(Rslt_4), makePair(LAZ, 0, Rslt_4Args, glblFn1849));
#endif

// link args to body
swap(r_seq_2, Rslt_4);

Term Rslt_4V = get(termLoc(Rslt_4));
apps = take(portLoc(1, Rslt_4V));
lams = take(portLoc(2, Rslt_4V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_4), makePair(LAZ, 0, apps, lams));
pushRedex(args, seq_2);
return;
}
Term glblFn1853 = newRef(glblCFn1852);
void glblProto20_2514(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(704), args);
  args = makePair(APP, 0, glblStr1912, args);
  interact(args, glblProto20);
}
}
void glblProto1562_2517(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(706), args);
  args = makePair(APP, 0, glblStr1912, args);
  interact(args, glblProto1562);
}
}
void glblProto1889_2519(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(707), args);
  args = makePair(APP, 0, glblStr1912, args);
  interact(args, glblProto1889);
}
}
void glblProto1889_2521(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(709), args);
  args = makePair(APP, 0, glblStr1912, args);
  interact(args, glblProto1889);
}
}
void glblProto34_2523(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(704), args);
  args = makePair(APP, 0, glblStr1912, args);
  interact(args, glblProto34);
}
}
void glblProto1757_2525(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(702), args);
  args = makePair(APP, 0, glblStr1912, args);
  interact(args, glblProto1757);
}
}
Term glblFn1855;
void glblCFn1854(Term ref, Term args) {
Term apps, lams, V;
Location vLoc;
Term seq_3 = makePair(LAM, 0, SUB, NUL);
Location r_seq_3 = portLoc(2, seq_3);
Term fn_name_2 = newTerm(VAR, 0, portLoc(1, seq_3));
seq_3 = makePair(LAM, 0, SUB, seq_3);
Term state_1 = newTerm(VAR, 0, portLoc(1, seq_3));
// allocate args at /home/jim/toccata/interpreter/intrp-rdr.toc: 700
Term seq_7 = makePair(LAM, 0, SUB, NUL);
Location r_seq_7 = portLoc(2, seq_7);
Term exprs_6 = newTerm(VAR, 0, portLoc(1, seq_7));
// allocate args at /home/jim/toccata/interpreter/intrp-rdr.toc: 701

Term exprs_6_4;
exprs_6 = dupeArg(exprs_6, &exprs_6_4, 0);

Term exprs_6_3;
exprs_6 = dupeArg(exprs_6, &exprs_6_3, 0);

Term exprs_6_2;
exprs_6 = dupeArg(exprs_6, &exprs_6_2, 0);

Term exprs_6_1;
exprs_6 = dupeArg(exprs_6, &exprs_6_1, 0);
Term seq_11 = makePair(LAM, 0, SUB, NUL);
Location r_seq_11 = portLoc(2, seq_11);
Term k_10 = newTerm(VAR, 0, portLoc(1, seq_11));
// allocate args at /home/jim/toccata/interpreter/intrp-rdr.toc: 702
Term glblVal1896_13;
glblVal1896 = dupeArg(glblVal1896, &glblVal1896_13, 0);

// call = at /home/jim/toccata/interpreter/intrp-rdr.toc: 704
Term Rslt_13Args = makePair(APP, 0, newI60(2), SUB);
Term Rslt_13 = newTerm(VAR, 0, portLoc(2, Rslt_13Args));
Rslt_13Args = makePair(APP, 0, k_10, Rslt_13Args);
#ifdef STRICT
pushRedex(Rslt_13Args, newRef(glblProto20_2514));
#else
swap(termLoc(Rslt_13), makePair(LAZ, 0, Rslt_13Args, newRef(glblProto20_2514)));
#endif
// call .value at /home/jim/toccata/interpreter/intrp-rdr.toc: 706
Term Rslt_14Args = makePair(APP, 0, exprs_6_1, SUB);
Term Rslt_14 = newTerm(VAR, 0, portLoc(2, Rslt_14Args));
#ifdef STRICT
pushRedex(Rslt_14Args, newRef(glblProto1562_2517));
#else
swap(termLoc(Rslt_14), makePair(LAZ, 0, Rslt_14Args, newRef(glblProto1562_2517)));
#endif
// call .state at /home/jim/toccata/interpreter/intrp-rdr.toc: 707
Term Rslt_15Args = makePair(APP, 0, exprs_6_2, SUB);
Term Rslt_15 = newTerm(VAR, 0, portLoc(2, Rslt_15Args));
#ifdef STRICT
pushRedex(Rslt_15Args, newRef(glblProto1889_2519));
#else
swap(termLoc(Rslt_15), makePair(LAZ, 0, Rslt_15Args, newRef(glblProto1889_2519)));
#endif
// call make-loc at /home/jim/toccata/interpreter/intrp-rdr.toc: 707
Term Rslt_16Args = makePair(APP, 0, Rslt_15, SUB);
Term Rslt_16 = newTerm(VAR, 0, portLoc(2, Rslt_16Args));
#ifdef STRICT
pushRedex(Rslt_16Args, glblFn1762);
#else
swap(termLoc(Rslt_16), makePair(LAZ, 0, Rslt_16Args, glblFn1762));
#endif

// call str-println-desugar at /home/jim/toccata/interpreter/intrp-rdr.toc: 706
Term Rslt_17Args = makePair(APP, 0, fn_name_2, SUB);
Term Rslt_17 = newTerm(VAR, 0, portLoc(2, Rslt_17Args));
Rslt_17Args = makePair(APP, 0, Rslt_16, Rslt_17Args);
Rslt_17Args = makePair(APP, 0, Rslt_14, Rslt_17Args);
#ifdef STRICT
pushRedex(Rslt_17Args, glblFn1794);
#else
swap(termLoc(Rslt_17), makePair(LAZ, 0, Rslt_17Args, glblFn1794));
#endif

// call .state at /home/jim/toccata/interpreter/intrp-rdr.toc: 709
Term Rslt_18Args = makePair(APP, 0, exprs_6_3, SUB);
Term Rslt_18 = newTerm(VAR, 0, portLoc(2, Rslt_18Args));
#ifdef STRICT
pushRedex(Rslt_18Args, newRef(glblProto1889_2521));
#else
swap(termLoc(Rslt_18), makePair(LAZ, 0, Rslt_18Args, newRef(glblProto1889_2521)));
#endif
// call ParserMatch at /home/jim/toccata/interpreter/intrp-rdr.toc: 706
Term Rslt_19Args = makePair(APP, 0, Rslt_18, SUB);
Term Rslt_19 = newTerm(VAR, 0, portLoc(2, Rslt_19Args));
Rslt_19Args = makePair(APP, 0, Rslt_17, Rslt_19Args);
#ifdef STRICT
pushRedex(Rslt_19Args, glblVal1896_13);
#else
swap(termLoc(Rslt_19), makePair(LAZ, 0, Rslt_19Args, glblVal1896_13));
#endif

// call cond at /home/jim/toccata/interpreter/intrp-rdr.toc: 704
Term Rslt_20Args = makePair(APP, 0, Rslt_19, SUB);
Term Rslt_20 = newTerm(VAR, 0, portLoc(2, Rslt_20Args));
Rslt_20Args = makePair(APP, 0, exprs_6, Rslt_20Args);
Rslt_20Args = makePair(APP, 0, Rslt_13, Rslt_20Args);
#ifdef STRICT
pushRedex(Rslt_20Args, newRef(glblProto34_2523));
#else
swap(termLoc(Rslt_20), makePair(LAZ, 0, Rslt_20Args, newRef(glblProto34_2523)));
#endif
// link args to body
swap(r_seq_11, Rslt_20);

Term Rslt_20V = get(termLoc(Rslt_20));
apps = take(portLoc(1, Rslt_20V));
lams = take(portLoc(2, Rslt_20V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_20), makePair(LAZ, 0, apps, lams));
// call result-kind at /home/jim/toccata/interpreter/intrp-rdr.toc: 702
Term Rslt_21Args = makePair(APP, 0, exprs_6_4, SUB);
Term Rslt_21 = newTerm(VAR, 0, portLoc(2, Rslt_21Args));
#ifdef STRICT
pushRedex(Rslt_21Args, newRef(glblProto1757_2525));
#else
swap(termLoc(Rslt_21), makePair(LAZ, 0, Rslt_21Args, newRef(glblProto1757_2525)));
#endif
// call  at /home/jim/toccata/interpreter/intrp-rdr.toc: 702
Term Rslt_22Args = makePair(APP, 0, Rslt_21, SUB);
Term Rslt_22 = newTerm(VAR, 0, portLoc(2, Rslt_22Args));
#ifdef STRICT
pushRedex(Rslt_22Args, seq_11);
#else
swap(termLoc(Rslt_22), makePair(LAZ, 0, Rslt_22Args, seq_11));
#endif

// link args to body
swap(r_seq_7, Rslt_22);

Term Rslt_22V = get(termLoc(Rslt_22));
apps = take(portLoc(1, Rslt_22V));
lams = take(portLoc(2, Rslt_22V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_22), makePair(LAZ, 0, apps, lams));
// call parse-expr-list at /home/jim/toccata/interpreter/intrp-rdr.toc: 701
Term Rslt_23Args = makePair(APP, 0, glblStr254, SUB);
Term Rslt_23 = newTerm(VAR, 0, portLoc(2, Rslt_23Args));
Rslt_23Args = makePair(APP, 0, state_1, Rslt_23Args);
#ifdef STRICT
pushRedex(Rslt_23Args, glblFn1823);
#else
swap(termLoc(Rslt_23), makePair(LAZ, 0, Rslt_23Args, glblFn1823));
#endif

// call  at /home/jim/toccata/interpreter/intrp-rdr.toc: 701
Term Rslt_24Args = makePair(APP, 0, Rslt_23, SUB);
Term Rslt_24 = newTerm(VAR, 0, portLoc(2, Rslt_24Args));
#ifdef STRICT
pushRedex(Rslt_24Args, seq_7);
#else
swap(termLoc(Rslt_24), makePair(LAZ, 0, Rslt_24Args, seq_7));
#endif

// link args to body
swap(r_seq_3, Rslt_24);

Term Rslt_24V = get(termLoc(Rslt_24));
apps = take(portLoc(1, Rslt_24V));
lams = take(portLoc(2, Rslt_24V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_24), makePair(LAZ, 0, apps, lams));
pushRedex(args, seq_3);
return;
}
Term glblFn1855 = newRef(glblCFn1854);
struct {TYPE_SIZE type;
        REFS_SIZE refs;
        int64_t *hash;
        Value *parent;
        int64_t len;
        char buffer[5];
} str41 = {StringBufferType, REFS_STATIC, 0, 0, 4, "str*"};
Term glblStr2516 = newTerm_(VAL, (Term)&str41);
Term glblFn1857;
void glblCFn1856(Term ref, Term args) {
Term apps, lams, V;
Location vLoc;
Term seq_2 = makePair(LAM, 0, SUB, NUL);
Location r_seq_2 = portLoc(2, seq_2);
Term state_1 = newTerm(VAR, 0, portLoc(1, seq_2));
// allocate args at /home/jim/toccata/interpreter/intrp-rdr.toc: 712
// call parse-str-println at /home/jim/toccata/interpreter/intrp-rdr.toc: 713
Term Rslt_4Args = makePair(APP, 0, glblStr2516, SUB);
Term Rslt_4 = newTerm(VAR, 0, portLoc(2, Rslt_4Args));
Rslt_4Args = makePair(APP, 0, state_1, Rslt_4Args);
#ifdef STRICT
pushRedex(Rslt_4Args, glblFn1855);
#else
swap(termLoc(Rslt_4), makePair(LAZ, 0, Rslt_4Args, glblFn1855));
#endif

// link args to body
swap(r_seq_2, Rslt_4);

Term Rslt_4V = get(termLoc(Rslt_4));
apps = take(portLoc(1, Rslt_4V));
lams = take(portLoc(2, Rslt_4V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_4), makePair(LAZ, 0, apps, lams));
pushRedex(args, seq_2);
return;
}
Term glblFn1857 = newRef(glblCFn1856);
struct {TYPE_SIZE type;
        REFS_SIZE refs;
        int64_t *hash;
        Value *parent;
        int64_t len;
        char buffer[9];
} str42 = {StringBufferType, REFS_STATIC, 0, 0, 8, "println*"};
Term glblStr2527 = newTerm_(VAL, (Term)&str42);
Term glblFn1859;
void glblCFn1858(Term ref, Term args) {
Term apps, lams, V;
Location vLoc;
Term seq_2 = makePair(LAM, 0, SUB, NUL);
Location r_seq_2 = portLoc(2, seq_2);
Term state_1 = newTerm(VAR, 0, portLoc(1, seq_2));
// allocate args at /home/jim/toccata/interpreter/intrp-rdr.toc: 715
// call parse-str-println at /home/jim/toccata/interpreter/intrp-rdr.toc: 716
Term Rslt_4Args = makePair(APP, 0, glblStr2527, SUB);
Term Rslt_4 = newTerm(VAR, 0, portLoc(2, Rslt_4Args));
Rslt_4Args = makePair(APP, 0, state_1, Rslt_4Args);
#ifdef STRICT
pushRedex(Rslt_4Args, glblFn1855);
#else
swap(termLoc(Rslt_4), makePair(LAZ, 0, Rslt_4Args, glblFn1855));
#endif

// link args to body
swap(r_seq_2, Rslt_4);

Term Rslt_4V = get(termLoc(Rslt_4));
apps = take(portLoc(1, Rslt_4V));
lams = take(portLoc(2, Rslt_4V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_4), makePair(LAZ, 0, apps, lams));
pushRedex(args, seq_2);
return;
}
Term glblFn1859 = newRef(glblCFn1858);
void glblProto20_2532(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(724), args);
  args = makePair(APP, 0, glblStr1912, args);
  interact(args, glblProto20);
}
}
void glblProto20_2534(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(731), args);
  args = makePair(APP, 0, glblStr1912, args);
  interact(args, glblProto20);
}
}
void glblProto1562_2536(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(733), args);
  args = makePair(APP, 0, glblStr1912, args);
  interact(args, glblProto1562);
}
}
void glblProto64_2538(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(733), args);
  args = makePair(APP, 0, glblStr1912, args);
  interact(args, glblProto64);
}
}
void glblProto20_2540(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(733), args);
  args = makePair(APP, 0, glblStr1912, args);
  interact(args, glblProto20);
}
}
struct {TYPE_SIZE type;
        REFS_SIZE refs;
        int64_t *hash;
        Value *parent;
        int64_t len;
        char buffer[26];
} str43 = {StringBufferType, REFS_STATIC, 0, 0, 25, "threading with zero steps"};
Term glblStr2531 = newTerm_(VAL, (Term)&str43);
void glblProto1889_2543(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(734), args);
  args = makePair(APP, 0, glblStr1912, args);
  interact(args, glblProto1889);
}
}
void glblProto1562_2545(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(735), args);
  args = makePair(APP, 0, glblStr1912, args);
  interact(args, glblProto1562);
}
}
void glblProto1889_2548(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(736), args);
  args = makePair(APP, 0, glblStr1912, args);
  interact(args, glblProto1889);
}
}
void glblProto1889_2550(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(737), args);
  args = makePair(APP, 0, glblStr1912, args);
  interact(args, glblProto1889);
}
}
void glblProto34_2552(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(733), args);
  args = makePair(APP, 0, glblStr1912, args);
  interact(args, glblProto34);
}
}
void glblProto34_2555(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(731), args);
  args = makePair(APP, 0, glblStr1912, args);
  interact(args, glblProto34);
}
}
void glblProto1757_2557(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(729), args);
  args = makePair(APP, 0, glblStr1912, args);
  interact(args, glblProto1757);
}
}
void glblProto1889_2560(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(727), args);
  args = makePair(APP, 0, glblStr1912, args);
  interact(args, glblProto1889);
}
}
void glblProto1562_2562(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(726), args);
  args = makePair(APP, 0, glblStr1912, args);
  interact(args, glblProto1562);
}
}
void glblProto34_2564(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(724), args);
  args = makePair(APP, 0, glblStr1912, args);
  interact(args, glblProto34);
}
}
void glblProto1757_2566(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(722), args);
  args = makePair(APP, 0, glblStr1912, args);
  interact(args, glblProto1757);
}
}
Term glblFn1861;
void glblCFn1860(Term ref, Term args) {
Term apps, lams, V;
Location vLoc;
Term seq_2 = makePair(LAM, 0, SUB, NUL);
Location r_seq_2 = portLoc(2, seq_2);
Term state_1 = newTerm(VAR, 0, portLoc(1, seq_2));
// allocate args at /home/jim/toccata/interpreter/intrp-rdr.toc: 720
Term glblVal1819_7;
glblVal1819 = dupeArg(glblVal1819, &glblVal1819_7, 0);

Term seq_6 = makePair(LAM, 0, SUB, NUL);
Location r_seq_6 = portLoc(2, seq_6);
Term initial_res_5 = newTerm(VAR, 0, portLoc(1, seq_6));
// allocate args at /home/jim/toccata/interpreter/intrp-rdr.toc: 721

Term initial_res_5_3;
initial_res_5 = dupeArg(initial_res_5, &initial_res_5_3, 0);

Term initial_res_5_2;
initial_res_5 = dupeArg(initial_res_5, &initial_res_5_2, 0);

Term initial_res_5_1;
initial_res_5 = dupeArg(initial_res_5, &initial_res_5_1, 0);
Term seq_10 = makePair(LAM, 0, SUB, NUL);
Location r_seq_10 = portLoc(2, seq_10);
Term k_9 = newTerm(VAR, 0, portLoc(1, seq_10));
// allocate args at /home/jim/toccata/interpreter/intrp-rdr.toc: 722
Term seq_15 = makePair(LAM, 0, SUB, NUL);
Location r_seq_15 = portLoc(2, seq_15);
Term initial_14 = newTerm(VAR, 0, portLoc(1, seq_15));
// allocate args at /home/jim/toccata/interpreter/intrp-rdr.toc: 726
Term seq_19 = makePair(LAM, 0, SUB, NUL);
Location r_seq_19 = portLoc(2, seq_19);
Term s2_18 = newTerm(VAR, 0, portLoc(1, seq_19));
// allocate args at /home/jim/toccata/interpreter/intrp-rdr.toc: 727
Term seq_23 = makePair(LAM, 0, SUB, NUL);
Location r_seq_23 = portLoc(2, seq_23);
Term steps_22 = newTerm(VAR, 0, portLoc(1, seq_23));
// allocate args at /home/jim/toccata/interpreter/intrp-rdr.toc: 728

Term steps_22_6;
steps_22 = dupeArg(steps_22, &steps_22_6, 0);

Term steps_22_5;
steps_22 = dupeArg(steps_22, &steps_22_5, 0);

Term steps_22_4;
steps_22 = dupeArg(steps_22, &steps_22_4, 0);

Term steps_22_3;
steps_22 = dupeArg(steps_22, &steps_22_3, 0);

Term steps_22_2;
steps_22 = dupeArg(steps_22, &steps_22_2, 0);

Term steps_22_1;
steps_22 = dupeArg(steps_22, &steps_22_1, 0);
Term seq_27 = makePair(LAM, 0, SUB, NUL);
Location r_seq_27 = portLoc(2, seq_27);
Term sk_26 = newTerm(VAR, 0, portLoc(1, seq_27));
// allocate args at /home/jim/toccata/interpreter/intrp-rdr.toc: 729
Term glblVal1896_14;
glblVal1896 = dupeArg(glblVal1896, &glblVal1896_14, 0);

Term glblVal1908_3;
glblVal1908 = dupeArg(glblVal1908, &glblVal1908_3, 0);

// call = at /home/jim/toccata/interpreter/intrp-rdr.toc: 724
Term Rslt_12Args = makePair(APP, 0, newI60(2), SUB);
Term Rslt_12 = newTerm(VAR, 0, portLoc(2, Rslt_12Args));
Rslt_12Args = makePair(APP, 0, k_9, Rslt_12Args);
#ifdef STRICT
pushRedex(Rslt_12Args, newRef(glblProto20_2532));
#else
swap(termLoc(Rslt_12), makePair(LAZ, 0, Rslt_12Args, newRef(glblProto20_2532)));
#endif
// call = at /home/jim/toccata/interpreter/intrp-rdr.toc: 731
Term Rslt_29Args = makePair(APP, 0, newI60(2), SUB);
Term Rslt_29 = newTerm(VAR, 0, portLoc(2, Rslt_29Args));
Rslt_29Args = makePair(APP, 0, sk_26, Rslt_29Args);
#ifdef STRICT
pushRedex(Rslt_29Args, newRef(glblProto20_2534));
#else
swap(termLoc(Rslt_29), makePair(LAZ, 0, Rslt_29Args, newRef(glblProto20_2534)));
#endif
// call .value at /home/jim/toccata/interpreter/intrp-rdr.toc: 733
Term Rslt_30Args = makePair(APP, 0, steps_22, SUB);
Term Rslt_30 = newTerm(VAR, 0, portLoc(2, Rslt_30Args));
#ifdef STRICT
pushRedex(Rslt_30Args, newRef(glblProto1562_2536));
#else
swap(termLoc(Rslt_30), makePair(LAZ, 0, Rslt_30Args, newRef(glblProto1562_2536)));
#endif
// call count at /home/jim/toccata/interpreter/intrp-rdr.toc: 733
Term Rslt_31Args = makePair(APP, 0, Rslt_30, SUB);
Term Rslt_31 = newTerm(VAR, 0, portLoc(2, Rslt_31Args));
#ifdef STRICT
pushRedex(Rslt_31Args, newRef(glblProto64_2538));
#else
swap(termLoc(Rslt_31), makePair(LAZ, 0, Rslt_31Args, newRef(glblProto64_2538)));
#endif
// call = at /home/jim/toccata/interpreter/intrp-rdr.toc: 733
Term Rslt_32Args = makePair(APP, 0, newI60(0), SUB);
Term Rslt_32 = newTerm(VAR, 0, portLoc(2, Rslt_32Args));
Rslt_32Args = makePair(APP, 0, Rslt_31, Rslt_32Args);
#ifdef STRICT
pushRedex(Rslt_32Args, newRef(glblProto20_2540));
#else
swap(termLoc(Rslt_32), makePair(LAZ, 0, Rslt_32Args, newRef(glblProto20_2540)));
#endif
// call .state at /home/jim/toccata/interpreter/intrp-rdr.toc: 734
Term Rslt_33Args = makePair(APP, 0, steps_22_1, SUB);
Term Rslt_33 = newTerm(VAR, 0, portLoc(2, Rslt_33Args));
#ifdef STRICT
pushRedex(Rslt_33Args, newRef(glblProto1889_2543));
#else
swap(termLoc(Rslt_33), makePair(LAZ, 0, Rslt_33Args, newRef(glblProto1889_2543)));
#endif
// call ParserError at /home/jim/toccata/interpreter/intrp-rdr.toc: 734
Term Rslt_34Args = makePair(APP, 0, Rslt_33, SUB);
Term Rslt_34 = newTerm(VAR, 0, portLoc(2, Rslt_34Args));
Rslt_34Args = makePair(APP, 0, glblStr2531, Rslt_34Args);
#ifdef STRICT
pushRedex(Rslt_34Args, glblVal1908_3);
#else
swap(termLoc(Rslt_34), makePair(LAZ, 0, Rslt_34Args, glblVal1908_3));
#endif

// call .value at /home/jim/toccata/interpreter/intrp-rdr.toc: 735
Term Rslt_35Args = makePair(APP, 0, steps_22_2, SUB);
Term Rslt_35 = newTerm(VAR, 0, portLoc(2, Rslt_35Args));
#ifdef STRICT
pushRedex(Rslt_35Args, newRef(glblProto1562_2545));
#else
swap(termLoc(Rslt_35), makePair(LAZ, 0, Rslt_35Args, newRef(glblProto1562_2545)));
#endif
// call .state at /home/jim/toccata/interpreter/intrp-rdr.toc: 736
Term Rslt_36Args = makePair(APP, 0, steps_22_3, SUB);
Term Rslt_36 = newTerm(VAR, 0, portLoc(2, Rslt_36Args));
#ifdef STRICT
pushRedex(Rslt_36Args, newRef(glblProto1889_2548));
#else
swap(termLoc(Rslt_36), makePair(LAZ, 0, Rslt_36Args, newRef(glblProto1889_2548)));
#endif
// call make-loc at /home/jim/toccata/interpreter/intrp-rdr.toc: 736
Term Rslt_37Args = makePair(APP, 0, Rslt_36, SUB);
Term Rslt_37 = newTerm(VAR, 0, portLoc(2, Rslt_37Args));
#ifdef STRICT
pushRedex(Rslt_37Args, glblFn1762);
#else
swap(termLoc(Rslt_37), makePair(LAZ, 0, Rslt_37Args, glblFn1762));
#endif

// call threading at /home/jim/toccata/interpreter/intrp-rdr.toc: 735
Term Rslt_38Args = makePair(APP, 0, Rslt_37, SUB);
Term Rslt_38 = newTerm(VAR, 0, portLoc(2, Rslt_38Args));
Rslt_38Args = makePair(APP, 0, Rslt_35, Rslt_38Args);
Rslt_38Args = makePair(APP, 0, initial_14, Rslt_38Args);
#ifdef STRICT
pushRedex(Rslt_38Args, glblFn1800);
#else
swap(termLoc(Rslt_38), makePair(LAZ, 0, Rslt_38Args, glblFn1800));
#endif

// call .state at /home/jim/toccata/interpreter/intrp-rdr.toc: 737
Term Rslt_39Args = makePair(APP, 0, steps_22_4, SUB);
Term Rslt_39 = newTerm(VAR, 0, portLoc(2, Rslt_39Args));
#ifdef STRICT
pushRedex(Rslt_39Args, newRef(glblProto1889_2550));
#else
swap(termLoc(Rslt_39), makePair(LAZ, 0, Rslt_39Args, newRef(glblProto1889_2550)));
#endif
// call ParserMatch at /home/jim/toccata/interpreter/intrp-rdr.toc: 735
Term Rslt_40Args = makePair(APP, 0, Rslt_39, SUB);
Term Rslt_40 = newTerm(VAR, 0, portLoc(2, Rslt_40Args));
Rslt_40Args = makePair(APP, 0, Rslt_38, Rslt_40Args);
#ifdef STRICT
pushRedex(Rslt_40Args, glblVal1896_14);
#else
swap(termLoc(Rslt_40), makePair(LAZ, 0, Rslt_40Args, glblVal1896_14));
#endif

// call cond at /home/jim/toccata/interpreter/intrp-rdr.toc: 733
Term Rslt_41Args = makePair(APP, 0, Rslt_40, SUB);
Term Rslt_41 = newTerm(VAR, 0, portLoc(2, Rslt_41Args));
Rslt_41Args = makePair(APP, 0, Rslt_34, Rslt_41Args);
Rslt_41Args = makePair(APP, 0, Rslt_32, Rslt_41Args);
#ifdef STRICT
pushRedex(Rslt_41Args, newRef(glblProto34_2552));
#else
swap(termLoc(Rslt_41), makePair(LAZ, 0, Rslt_41Args, newRef(glblProto34_2552)));
#endif
// call cond at /home/jim/toccata/interpreter/intrp-rdr.toc: 731
Term Rslt_42Args = makePair(APP, 0, steps_22_5, SUB);
Term Rslt_42 = newTerm(VAR, 0, portLoc(2, Rslt_42Args));
Rslt_42Args = makePair(APP, 0, Rslt_41, Rslt_42Args);
Rslt_42Args = makePair(APP, 0, Rslt_29, Rslt_42Args);
#ifdef STRICT
pushRedex(Rslt_42Args, newRef(glblProto34_2555));
#else
swap(termLoc(Rslt_42), makePair(LAZ, 0, Rslt_42Args, newRef(glblProto34_2555)));
#endif
// link args to body
swap(r_seq_27, Rslt_42);

Term Rslt_42V = get(termLoc(Rslt_42));
apps = take(portLoc(1, Rslt_42V));
lams = take(portLoc(2, Rslt_42V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_42), makePair(LAZ, 0, apps, lams));
// call result-kind at /home/jim/toccata/interpreter/intrp-rdr.toc: 729
Term Rslt_43Args = makePair(APP, 0, steps_22_6, SUB);
Term Rslt_43 = newTerm(VAR, 0, portLoc(2, Rslt_43Args));
#ifdef STRICT
pushRedex(Rslt_43Args, newRef(glblProto1757_2557));
#else
swap(termLoc(Rslt_43), makePair(LAZ, 0, Rslt_43Args, newRef(glblProto1757_2557)));
#endif
// call  at /home/jim/toccata/interpreter/intrp-rdr.toc: 729
Term Rslt_44Args = makePair(APP, 0, Rslt_43, SUB);
Term Rslt_44 = newTerm(VAR, 0, portLoc(2, Rslt_44Args));
#ifdef STRICT
pushRedex(Rslt_44Args, seq_27);
#else
swap(termLoc(Rslt_44), makePair(LAZ, 0, Rslt_44Args, seq_27));
#endif

// link args to body
swap(r_seq_23, Rslt_44);

Term Rslt_44V = get(termLoc(Rslt_44));
apps = take(portLoc(1, Rslt_44V));
lams = take(portLoc(2, Rslt_44V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_44), makePair(LAZ, 0, apps, lams));
// call parse-threading-steps at /home/jim/toccata/interpreter/intrp-rdr.toc: 728
Term Rslt_45Args = makePair(APP, 0, s2_18, SUB);
Term Rslt_45 = newTerm(VAR, 0, portLoc(2, Rslt_45Args));
#ifdef STRICT
pushRedex(Rslt_45Args, glblFn1839);
#else
swap(termLoc(Rslt_45), makePair(LAZ, 0, Rslt_45Args, glblFn1839));
#endif

// call  at /home/jim/toccata/interpreter/intrp-rdr.toc: 728
Term Rslt_46Args = makePair(APP, 0, Rslt_45, SUB);
Term Rslt_46 = newTerm(VAR, 0, portLoc(2, Rslt_46Args));
#ifdef STRICT
pushRedex(Rslt_46Args, seq_23);
#else
swap(termLoc(Rslt_46), makePair(LAZ, 0, Rslt_46Args, seq_23));
#endif

// link args to body
swap(r_seq_19, Rslt_46);

Term Rslt_46V = get(termLoc(Rslt_46));
apps = take(portLoc(1, Rslt_46V));
lams = take(portLoc(2, Rslt_46V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_46), makePair(LAZ, 0, apps, lams));
// call .state at /home/jim/toccata/interpreter/intrp-rdr.toc: 727
Term Rslt_47Args = makePair(APP, 0, initial_res_5_1, SUB);
Term Rslt_47 = newTerm(VAR, 0, portLoc(2, Rslt_47Args));
#ifdef STRICT
pushRedex(Rslt_47Args, newRef(glblProto1889_2560));
#else
swap(termLoc(Rslt_47), makePair(LAZ, 0, Rslt_47Args, newRef(glblProto1889_2560)));
#endif
// call  at /home/jim/toccata/interpreter/intrp-rdr.toc: 727
Term Rslt_48Args = makePair(APP, 0, Rslt_47, SUB);
Term Rslt_48 = newTerm(VAR, 0, portLoc(2, Rslt_48Args));
#ifdef STRICT
pushRedex(Rslt_48Args, seq_19);
#else
swap(termLoc(Rslt_48), makePair(LAZ, 0, Rslt_48Args, seq_19));
#endif

// link args to body
swap(r_seq_15, Rslt_48);

Term Rslt_48V = get(termLoc(Rslt_48));
apps = take(portLoc(1, Rslt_48V));
lams = take(portLoc(2, Rslt_48V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_48), makePair(LAZ, 0, apps, lams));
// call .value at /home/jim/toccata/interpreter/intrp-rdr.toc: 726
Term Rslt_49Args = makePair(APP, 0, initial_res_5_2, SUB);
Term Rslt_49 = newTerm(VAR, 0, portLoc(2, Rslt_49Args));
#ifdef STRICT
pushRedex(Rslt_49Args, newRef(glblProto1562_2562));
#else
swap(termLoc(Rslt_49), makePair(LAZ, 0, Rslt_49Args, newRef(glblProto1562_2562)));
#endif
// call  at /home/jim/toccata/interpreter/intrp-rdr.toc: 726
Term Rslt_50Args = makePair(APP, 0, Rslt_49, SUB);
Term Rslt_50 = newTerm(VAR, 0, portLoc(2, Rslt_50Args));
#ifdef STRICT
pushRedex(Rslt_50Args, seq_15);
#else
swap(termLoc(Rslt_50), makePair(LAZ, 0, Rslt_50Args, seq_15));
#endif

// call cond at /home/jim/toccata/interpreter/intrp-rdr.toc: 724
Term Rslt_51Args = makePair(APP, 0, Rslt_50, SUB);
Term Rslt_51 = newTerm(VAR, 0, portLoc(2, Rslt_51Args));
Rslt_51Args = makePair(APP, 0, initial_res_5, Rslt_51Args);
Rslt_51Args = makePair(APP, 0, Rslt_12, Rslt_51Args);
#ifdef STRICT
pushRedex(Rslt_51Args, newRef(glblProto34_2564));
#else
swap(termLoc(Rslt_51), makePair(LAZ, 0, Rslt_51Args, newRef(glblProto34_2564)));
#endif
// link args to body
swap(r_seq_10, Rslt_51);

Term Rslt_51V = get(termLoc(Rslt_51));
apps = take(portLoc(1, Rslt_51V));
lams = take(portLoc(2, Rslt_51V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_51), makePair(LAZ, 0, apps, lams));
// call result-kind at /home/jim/toccata/interpreter/intrp-rdr.toc: 722
Term Rslt_52Args = makePair(APP, 0, initial_res_5_3, SUB);
Term Rslt_52 = newTerm(VAR, 0, portLoc(2, Rslt_52Args));
#ifdef STRICT
pushRedex(Rslt_52Args, newRef(glblProto1757_2566));
#else
swap(termLoc(Rslt_52), makePair(LAZ, 0, Rslt_52Args, newRef(glblProto1757_2566)));
#endif
// call  at /home/jim/toccata/interpreter/intrp-rdr.toc: 722
Term Rslt_53Args = makePair(APP, 0, Rslt_52, SUB);
Term Rslt_53 = newTerm(VAR, 0, portLoc(2, Rslt_53Args));
#ifdef STRICT
pushRedex(Rslt_53Args, seq_10);
#else
swap(termLoc(Rslt_53), makePair(LAZ, 0, Rslt_53Args, seq_10));
#endif

// link args to body
swap(r_seq_6, Rslt_53);

Term Rslt_53V = get(termLoc(Rslt_53));
apps = take(portLoc(1, Rslt_53V));
lams = take(portLoc(2, Rslt_53V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_53), makePair(LAZ, 0, apps, lams));
// call parse-expr at /home/jim/toccata/interpreter/intrp-rdr.toc: 548
Term Rslt_54Args = makePair(APP, 0, state_1, SUB);
Term Rslt_54 = newTerm(VAR, 0, portLoc(2, Rslt_54Args));
#ifdef STRICT
pushRedex(Rslt_54Args, glblVal1819_7);
#else
swap(termLoc(Rslt_54), makePair(LAZ, 0, Rslt_54Args, glblVal1819_7));
#endif

// call  at /home/jim/toccata/interpreter/intrp-rdr.toc: 548
Term Rslt_55Args = makePair(APP, 0, Rslt_54, SUB);
Term Rslt_55 = newTerm(VAR, 0, portLoc(2, Rslt_55Args));
#ifdef STRICT
pushRedex(Rslt_55Args, seq_6);
#else
swap(termLoc(Rslt_55), makePair(LAZ, 0, Rslt_55Args, seq_6));
#endif

// link args to body
swap(r_seq_2, Rslt_55);

Term Rslt_55V = get(termLoc(Rslt_55));
apps = take(portLoc(1, Rslt_55V));
lams = take(portLoc(2, Rslt_55V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_55), makePair(LAZ, 0, apps, lams));
pushRedex(args, seq_2);
return;
}
Term glblFn1861 = newRef(glblCFn1860);
struct {TYPE_SIZE type;
        REFS_SIZE refs;
        int64_t *hash;
        Value *parent;
        int64_t len;
        char buffer[4];
} str44 = {StringBufferType, REFS_STATIC, 0, 0, 3, "str"};
Term glblStr2547 = newTerm_(VAL, (Term)&str44);
struct {TYPE_SIZE type;
        REFS_SIZE refs;
        int64_t *hash;
        Value *parent;
        int64_t len;
        char buffer[8];
} str45 = {StringBufferType, REFS_STATIC, 0, 0, 7, "println"};
Term glblStr2553 = newTerm_(VAL, (Term)&str45);
struct {TYPE_SIZE type;
        REFS_SIZE refs;
        int64_t *hash;
        Value *parent;
        int64_t len;
        char buffer[3];
} str46 = {StringBufferType, REFS_STATIC, 0, 0, 2, "->"};
Term glblStr2559 = newTerm_(VAL, (Term)&str46);
void glblProto34_2569(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(751), args);
  args = makePair(APP, 0, glblStr1912, args);
  interact(args, glblProto34);
}
}
void glblProto34_2571(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(749), args);
  args = makePair(APP, 0, glblStr1912, args);
  interact(args, glblProto34);
}
}
void glblProto34_2573(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(747), args);
  args = makePair(APP, 0, glblStr1912, args);
  interact(args, glblProto34);
}
}
void glblProto34_2575(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(745), args);
  args = makePair(APP, 0, glblStr1912, args);
  interact(args, glblProto34);
}
}
Term glblFn1863;
void glblCFn1862(Term ref, Term args) {
Term apps, lams, V;
Location vLoc;
Term seq_4 = makePair(LAM, 0, SUB, NUL);
Location r_seq_4 = portLoc(2, seq_4);
Term s2_3 = newTerm(VAR, 0, portLoc(1, seq_4));
seq_4 = makePair(LAM, 0, SUB, seq_4);
Term name_2 = newTerm(VAR, 0, portLoc(1, seq_4));
seq_4 = makePair(LAM, 0, SUB, seq_4);
Term ns_1 = newTerm(VAR, 0, portLoc(1, seq_4));
// allocate args at /home/jim/toccata/interpreter/intrp-rdr.toc: 743

Term name_2_4;
name_2 = dupeArg(name_2, &name_2_4, 350);

Term name_2_3;
name_2 = dupeArg(name_2, &name_2_3, 351);

Term name_2_2;
name_2 = dupeArg(name_2, &name_2_2, 352);

Term name_2_1;
name_2 = dupeArg(name_2, &name_2_1, 353);

Term s2_3_5;
s2_3 = dupeArg(s2_3, &s2_3_5, 354);

Term s2_3_4;
s2_3 = dupeArg(s2_3, &s2_3_4, 355);

Term s2_3_3;
s2_3 = dupeArg(s2_3, &s2_3_3, 356);

Term s2_3_2;
s2_3 = dupeArg(s2_3, &s2_3_2, 357);

Term s2_3_1;
s2_3 = dupeArg(s2_3, &s2_3_1, 358);
Term glblVal1560_10;
glblVal1560 = dupeArg(glblVal1560, &glblVal1560_10, 0);

Term seq_16 = makePair(LAM, 0, SUB, NUL);
Location r_seq_16 = portLoc(2, seq_16);
Term op_15 = newTerm(VAR, 0, portLoc(1, seq_16));
// allocate args at /home/jim/toccata/interpreter/intrp-rdr.toc: 752
// call str= at /home/jim/toccata/interpreter/intrp-rdr.toc: 745
Term Rslt_6Args = makePair(APP, 0, glblStr2510, SUB);
Term Rslt_6 = newTerm(VAR, 0, portLoc(2, Rslt_6Args));
Rslt_6Args = makePair(APP, 0, name_2, Rslt_6Args);
#ifdef STRICT
pushRedex(Rslt_6Args, glblFn363);
#else
swap(termLoc(Rslt_6), makePair(LAZ, 0, Rslt_6Args, glblFn363));
#endif

// call parse-or at /home/jim/toccata/interpreter/intrp-rdr.toc: 745
Term Rslt_7Args = makePair(APP, 0, s2_3, SUB);
Term Rslt_7 = newTerm(VAR, 0, portLoc(2, Rslt_7Args));
#ifdef STRICT
pushRedex(Rslt_7Args, glblFn1853);
#else
swap(termLoc(Rslt_7), makePair(LAZ, 0, Rslt_7Args, glblFn1853));
#endif

// call str= at /home/jim/toccata/interpreter/intrp-rdr.toc: 747
Term Rslt_8Args = makePair(APP, 0, glblStr2547, SUB);
Term Rslt_8 = newTerm(VAR, 0, portLoc(2, Rslt_8Args));
Rslt_8Args = makePair(APP, 0, name_2_1, Rslt_8Args);
#ifdef STRICT
pushRedex(Rslt_8Args, glblFn363);
#else
swap(termLoc(Rslt_8), makePair(LAZ, 0, Rslt_8Args, glblFn363));
#endif

// call parse-str at /home/jim/toccata/interpreter/intrp-rdr.toc: 747
Term Rslt_9Args = makePair(APP, 0, s2_3_1, SUB);
Term Rslt_9 = newTerm(VAR, 0, portLoc(2, Rslt_9Args));
#ifdef STRICT
pushRedex(Rslt_9Args, glblFn1857);
#else
swap(termLoc(Rslt_9), makePair(LAZ, 0, Rslt_9Args, glblFn1857));
#endif

// call str= at /home/jim/toccata/interpreter/intrp-rdr.toc: 749
Term Rslt_10Args = makePair(APP, 0, glblStr2553, SUB);
Term Rslt_10 = newTerm(VAR, 0, portLoc(2, Rslt_10Args));
Rslt_10Args = makePair(APP, 0, name_2_2, Rslt_10Args);
#ifdef STRICT
pushRedex(Rslt_10Args, glblFn363);
#else
swap(termLoc(Rslt_10), makePair(LAZ, 0, Rslt_10Args, glblFn363));
#endif

// call parse-println at /home/jim/toccata/interpreter/intrp-rdr.toc: 749
Term Rslt_11Args = makePair(APP, 0, s2_3_2, SUB);
Term Rslt_11 = newTerm(VAR, 0, portLoc(2, Rslt_11Args));
#ifdef STRICT
pushRedex(Rslt_11Args, glblFn1859);
#else
swap(termLoc(Rslt_11), makePair(LAZ, 0, Rslt_11Args, glblFn1859));
#endif

// call str= at /home/jim/toccata/interpreter/intrp-rdr.toc: 751
Term Rslt_12Args = makePair(APP, 0, glblStr2559, SUB);
Term Rslt_12 = newTerm(VAR, 0, portLoc(2, Rslt_12Args));
Rslt_12Args = makePair(APP, 0, name_2_3, Rslt_12Args);
#ifdef STRICT
pushRedex(Rslt_12Args, glblFn363);
#else
swap(termLoc(Rslt_12), makePair(LAZ, 0, Rslt_12Args, glblFn363));
#endif

// call parse-threading at /home/jim/toccata/interpreter/intrp-rdr.toc: 751
Term Rslt_13Args = makePair(APP, 0, s2_3_3, SUB);
Term Rslt_13 = newTerm(VAR, 0, portLoc(2, Rslt_13Args));
#ifdef STRICT
pushRedex(Rslt_13Args, glblFn1861);
#else
swap(termLoc(Rslt_13), makePair(LAZ, 0, Rslt_13Args, glblFn1861));
#endif

// call parse-call-args at /home/jim/toccata/interpreter/intrp-rdr.toc: 753
Term Rslt_18Args = makePair(APP, 0, op_15, SUB);
Term Rslt_18 = newTerm(VAR, 0, portLoc(2, Rslt_18Args));
Rslt_18Args = makePair(APP, 0, s2_3_4, Rslt_18Args);
#ifdef STRICT
pushRedex(Rslt_18Args, glblFn1841);
#else
swap(termLoc(Rslt_18), makePair(LAZ, 0, Rslt_18Args, glblFn1841));
#endif

// link args to body
swap(r_seq_16, Rslt_18);

Term Rslt_18V = get(termLoc(Rslt_18));
apps = take(portLoc(1, Rslt_18V));
lams = take(portLoc(2, Rslt_18V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_18), makePair(LAZ, 0, apps, lams));
// call make-loc at /home/jim/toccata/interpreter/intrp-rdr.toc: 752
Term Rslt_19Args = makePair(APP, 0, s2_3_5, SUB);
Term Rslt_19 = newTerm(VAR, 0, portLoc(2, Rslt_19Args));
#ifdef STRICT
pushRedex(Rslt_19Args, glblFn1762);
#else
swap(termLoc(Rslt_19), makePair(LAZ, 0, Rslt_19Args, glblFn1762));
#endif

// call ast/Symbol at /home/jim/toccata/interpreter/intrp-rdr.toc: 752
Term Rslt_20Args = makePair(APP, 0, Rslt_19, SUB);
Term Rslt_20 = newTerm(VAR, 0, portLoc(2, Rslt_20Args));
Rslt_20Args = makePair(APP, 0, name_2_4, Rslt_20Args);
Rslt_20Args = makePair(APP, 0, ns_1, Rslt_20Args);
#ifdef STRICT
pushRedex(Rslt_20Args, glblVal1560_10);
#else
swap(termLoc(Rslt_20), makePair(LAZ, 0, Rslt_20Args, glblVal1560_10));
#endif

// call  at /home/jim/toccata/interpreter/intrp-rdr.toc: 752
Term Rslt_21Args = makePair(APP, 0, Rslt_20, SUB);
Term Rslt_21 = newTerm(VAR, 0, portLoc(2, Rslt_21Args));
#ifdef STRICT
pushRedex(Rslt_21Args, seq_16);
#else
swap(termLoc(Rslt_21), makePair(LAZ, 0, Rslt_21Args, seq_16));
#endif

// call cond at /home/jim/toccata/interpreter/intrp-rdr.toc: 751
Term Rslt_22Args = makePair(APP, 0, Rslt_21, SUB);
Term Rslt_22 = newTerm(VAR, 0, portLoc(2, Rslt_22Args));
Rslt_22Args = makePair(APP, 0, Rslt_13, Rslt_22Args);
Rslt_22Args = makePair(APP, 0, Rslt_12, Rslt_22Args);
#ifdef STRICT
pushRedex(Rslt_22Args, newRef(glblProto34_2569));
#else
swap(termLoc(Rslt_22), makePair(LAZ, 0, Rslt_22Args, newRef(glblProto34_2569)));
#endif
// call cond at /home/jim/toccata/interpreter/intrp-rdr.toc: 749
Term Rslt_23Args = makePair(APP, 0, Rslt_22, SUB);
Term Rslt_23 = newTerm(VAR, 0, portLoc(2, Rslt_23Args));
Rslt_23Args = makePair(APP, 0, Rslt_11, Rslt_23Args);
Rslt_23Args = makePair(APP, 0, Rslt_10, Rslt_23Args);
#ifdef STRICT
pushRedex(Rslt_23Args, newRef(glblProto34_2571));
#else
swap(termLoc(Rslt_23), makePair(LAZ, 0, Rslt_23Args, newRef(glblProto34_2571)));
#endif
// call cond at /home/jim/toccata/interpreter/intrp-rdr.toc: 747
Term Rslt_24Args = makePair(APP, 0, Rslt_23, SUB);
Term Rslt_24 = newTerm(VAR, 0, portLoc(2, Rslt_24Args));
Rslt_24Args = makePair(APP, 0, Rslt_9, Rslt_24Args);
Rslt_24Args = makePair(APP, 0, Rslt_8, Rslt_24Args);
#ifdef STRICT
pushRedex(Rslt_24Args, newRef(glblProto34_2573));
#else
swap(termLoc(Rslt_24), makePair(LAZ, 0, Rslt_24Args, newRef(glblProto34_2573)));
#endif
// call cond at /home/jim/toccata/interpreter/intrp-rdr.toc: 745
Term Rslt_25Args = makePair(APP, 0, Rslt_24, SUB);
Term Rslt_25 = newTerm(VAR, 0, portLoc(2, Rslt_25Args));
Rslt_25Args = makePair(APP, 0, Rslt_7, Rslt_25Args);
Rslt_25Args = makePair(APP, 0, Rslt_6, Rslt_25Args);
#ifdef STRICT
pushRedex(Rslt_25Args, newRef(glblProto34_2575));
#else
swap(termLoc(Rslt_25), makePair(LAZ, 0, Rslt_25Args, newRef(glblProto34_2575)));
#endif
// link args to body
swap(r_seq_4, Rslt_25);

Term Rslt_25V = get(termLoc(Rslt_25));
apps = take(portLoc(1, Rslt_25V));
lams = take(portLoc(2, Rslt_25V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_25), makePair(LAZ, 0, apps, lams));
pushRedex(args, seq_4);
return;
}
Term glblFn1863 = newRef(glblCFn1862);
Term glblFn1865;
void glblCFn1864(Term ref, Term args) {
Term apps, lams, V;
Location vLoc;
Term seq_3 = makePair(LAM, 0, SUB, NUL);
Location r_seq_3 = portLoc(2, seq_3);
Term name_2 = newTerm(VAR, 0, portLoc(1, seq_3));
seq_3 = makePair(LAM, 0, SUB, seq_3);
Term state_1 = newTerm(VAR, 0, portLoc(1, seq_3));
// allocate args at /home/jim/toccata/interpreter/intrp-rdr.toc: 757
Term seq_7 = makePair(LAM, 0, SUB, NUL);
Location r_seq_7 = portLoc(2, seq_7);
Term s_6 = newTerm(VAR, 0, portLoc(1, seq_7));
// allocate args at /home/jim/toccata/interpreter/intrp-rdr.toc: 758
Term seq_11 = makePair(LAM, 0, SUB, NUL);
Location r_seq_11 = portLoc(2, seq_11);
Term params_10 = newTerm(VAR, 0, portLoc(1, seq_11));
// allocate args at /home/jim/toccata/interpreter/intrp-rdr.toc: 759

Term params_10_1;
params_10 = dupeArg(params_10, &params_10_1, 0);
Term seq_15 = makePair(LAM, 0, SUB, NUL);
Location r_seq_15 = portLoc(2, seq_15);
Term body_14 = newTerm(VAR, 0, portLoc(1, seq_15));
// allocate args at /home/jim/toccata/interpreter/intrp-rdr.toc: 760

Term body_14_2;
body_14 = dupeArg(body_14, &body_14_2, 0);

Term body_14_1;
body_14 = dupeArg(body_14, &body_14_1, 0);
Term seq_19 = makePair(LAM, 0, SUB, NUL);
Location r_seq_19 = portLoc(2, seq_19);
Term body_vec_18 = newTerm(VAR, 0, portLoc(1, seq_19));
// allocate args at /home/jim/toccata/interpreter/intrp-rdr.toc: 761
Term seq_23 = makePair(LAM, 0, SUB, NUL);
Location r_seq_23 = portLoc(2, seq_23);
Term s3_22 = newTerm(VAR, 0, portLoc(1, seq_23));
// allocate args at /home/jim/toccata/interpreter/intrp-rdr.toc: 762

Term s3_22_1;
s3_22 = dupeArg(s3_22, &s3_22_1, 0);
Term glblVal1605_3;
glblVal1605 = dupeArg(glblVal1605, &glblVal1605_3, 0);

Term glblVal1896_15;
glblVal1896 = dupeArg(glblVal1896, &glblVal1896_15, 0);

// call elt0 at /home/jim/toccata/interpreter/intrp-rdr.toc: 763
Term Rslt_25Args = makePair(APP, 0, params_10, SUB);
Term Rslt_25 = newTerm(VAR, 0, portLoc(2, Rslt_25Args));
#ifdef STRICT
pushRedex(Rslt_25Args, glblFn1742);
#else
swap(termLoc(Rslt_25), makePair(LAZ, 0, Rslt_25Args, glblFn1742));
#endif

// call make-loc at /home/jim/toccata/interpreter/intrp-rdr.toc: 763
Term Rslt_26Args = makePair(APP, 0, s3_22, SUB);
Term Rslt_26 = newTerm(VAR, 0, portLoc(2, Rslt_26Args));
#ifdef STRICT
pushRedex(Rslt_26Args, glblFn1762);
#else
swap(termLoc(Rslt_26), makePair(LAZ, 0, Rslt_26Args, glblFn1762));
#endif

// call ast/Fn at /home/jim/toccata/interpreter/intrp-rdr.toc: 763
Term Rslt_27Args = makePair(APP, 0, Rslt_26, SUB);
Term Rslt_27 = newTerm(VAR, 0, portLoc(2, Rslt_27Args));
Rslt_27Args = makePair(APP, 0, body_vec_18, Rslt_27Args);
Rslt_27Args = makePair(APP, 0, Rslt_25, Rslt_27Args);
Rslt_27Args = makePair(APP, 0, name_2, Rslt_27Args);
#ifdef STRICT
pushRedex(Rslt_27Args, glblVal1605_3);
#else
swap(termLoc(Rslt_27), makePair(LAZ, 0, Rslt_27Args, glblVal1605_3));
#endif

// call ParserMatch at /home/jim/toccata/interpreter/intrp-rdr.toc: 763
Term Rslt_28Args = makePair(APP, 0, s3_22_1, SUB);
Term Rslt_28 = newTerm(VAR, 0, portLoc(2, Rslt_28Args));
Rslt_28Args = makePair(APP, 0, Rslt_27, Rslt_28Args);
#ifdef STRICT
pushRedex(Rslt_28Args, glblVal1896_15);
#else
swap(termLoc(Rslt_28), makePair(LAZ, 0, Rslt_28Args, glblVal1896_15));
#endif

// link args to body
swap(r_seq_23, Rslt_28);

Term Rslt_28V = get(termLoc(Rslt_28));
apps = take(portLoc(1, Rslt_28V));
lams = take(portLoc(2, Rslt_28V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_28), makePair(LAZ, 0, apps, lams));
// call elt2 at /home/jim/toccata/interpreter/intrp-rdr.toc: 762
Term Rslt_29Args = makePair(APP, 0, body_14, SUB);
Term Rslt_29 = newTerm(VAR, 0, portLoc(2, Rslt_29Args));
#ifdef STRICT
pushRedex(Rslt_29Args, glblFn1756);
#else
swap(termLoc(Rslt_29), makePair(LAZ, 0, Rslt_29Args, glblFn1756));
#endif

// call  at /home/jim/toccata/interpreter/intrp-rdr.toc: 762
Term Rslt_30Args = makePair(APP, 0, Rslt_29, SUB);
Term Rslt_30 = newTerm(VAR, 0, portLoc(2, Rslt_30Args));
#ifdef STRICT
pushRedex(Rslt_30Args, seq_23);
#else
swap(termLoc(Rslt_30), makePair(LAZ, 0, Rslt_30Args, seq_23));
#endif

// link args to body
swap(r_seq_19, Rslt_30);

Term Rslt_30V = get(termLoc(Rslt_30));
apps = take(portLoc(1, Rslt_30V));
lams = take(portLoc(2, Rslt_30V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_30), makePair(LAZ, 0, apps, lams));
// call elt0 at /home/jim/toccata/interpreter/intrp-rdr.toc: 761
Term Rslt_31Args = makePair(APP, 0, body_14_1, SUB);
Term Rslt_31 = newTerm(VAR, 0, portLoc(2, Rslt_31Args));
#ifdef STRICT
pushRedex(Rslt_31Args, glblFn1742);
#else
swap(termLoc(Rslt_31), makePair(LAZ, 0, Rslt_31Args, glblFn1742));
#endif

// call elt1 at /home/jim/toccata/interpreter/intrp-rdr.toc: 761
Term Rslt_32Args = makePair(APP, 0, body_14_2, SUB);
Term Rslt_32 = newTerm(VAR, 0, portLoc(2, Rslt_32Args));
#ifdef STRICT
pushRedex(Rslt_32Args, glblFn1744);
#else
swap(termLoc(Rslt_32), makePair(LAZ, 0, Rslt_32Args, glblFn1744));
#endif

// call vect-concat at /home/jim/toccata/interpreter/intrp-rdr.toc: 761
Term Rslt_33Args = makePair(APP, 0, Rslt_32, SUB);
Term Rslt_33 = newTerm(VAR, 0, portLoc(2, Rslt_33Args));
Rslt_33Args = makePair(APP, 0, Rslt_31, Rslt_33Args);
#ifdef STRICT
pushRedex(Rslt_33Args, glblFn1764);
#else
swap(termLoc(Rslt_33), makePair(LAZ, 0, Rslt_33Args, glblFn1764));
#endif

// call  at /home/jim/toccata/interpreter/intrp-rdr.toc: 761
Term Rslt_34Args = makePair(APP, 0, Rslt_33, SUB);
Term Rslt_34 = newTerm(VAR, 0, portLoc(2, Rslt_34Args));
#ifdef STRICT
pushRedex(Rslt_34Args, seq_19);
#else
swap(termLoc(Rslt_34), makePair(LAZ, 0, Rslt_34Args, seq_19));
#endif

// link args to body
swap(r_seq_15, Rslt_34);

Term Rslt_34V = get(termLoc(Rslt_34));
apps = take(portLoc(1, Rslt_34V));
lams = take(portLoc(2, Rslt_34V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_34), makePair(LAZ, 0, apps, lams));
// call elt1 at /home/jim/toccata/interpreter/intrp-rdr.toc: 760
Term Rslt_35Args = makePair(APP, 0, params_10_1, SUB);
Term Rslt_35 = newTerm(VAR, 0, portLoc(2, Rslt_35Args));
#ifdef STRICT
pushRedex(Rslt_35Args, glblFn1744);
#else
swap(termLoc(Rslt_35), makePair(LAZ, 0, Rslt_35Args, glblFn1744));
#endif

// call parse-body at /home/jim/toccata/interpreter/intrp-rdr.toc: 760
Term Rslt_36Args = makePair(APP, 0, Rslt_35, SUB);
Term Rslt_36 = newTerm(VAR, 0, portLoc(2, Rslt_36Args));
#ifdef STRICT
pushRedex(Rslt_36Args, glblFn1827);
#else
swap(termLoc(Rslt_36), makePair(LAZ, 0, Rslt_36Args, glblFn1827));
#endif

// call  at /home/jim/toccata/interpreter/intrp-rdr.toc: 760
Term Rslt_37Args = makePair(APP, 0, Rslt_36, SUB);
Term Rslt_37 = newTerm(VAR, 0, portLoc(2, Rslt_37Args));
#ifdef STRICT
pushRedex(Rslt_37Args, seq_15);
#else
swap(termLoc(Rslt_37), makePair(LAZ, 0, Rslt_37Args, seq_15));
#endif

// link args to body
swap(r_seq_11, Rslt_37);

Term Rslt_37V = get(termLoc(Rslt_37));
apps = take(portLoc(1, Rslt_37V));
lams = take(portLoc(2, Rslt_37V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_37), makePair(LAZ, 0, apps, lams));
// call read-param-list at /home/jim/toccata/interpreter/intrp-rdr.toc: 759
Term Rslt_38Args = makePair(APP, 0, s_6, SUB);
Term Rslt_38 = newTerm(VAR, 0, portLoc(2, Rslt_38Args));
#ifdef STRICT
pushRedex(Rslt_38Args, glblFn1808);
#else
swap(termLoc(Rslt_38), makePair(LAZ, 0, Rslt_38Args, glblFn1808));
#endif

// call  at /home/jim/toccata/interpreter/intrp-rdr.toc: 759
Term Rslt_39Args = makePair(APP, 0, Rslt_38, SUB);
Term Rslt_39 = newTerm(VAR, 0, portLoc(2, Rslt_39Args));
#ifdef STRICT
pushRedex(Rslt_39Args, seq_11);
#else
swap(termLoc(Rslt_39), makePair(LAZ, 0, Rslt_39Args, seq_11));
#endif

// link args to body
swap(r_seq_7, Rslt_39);

Term Rslt_39V = get(termLoc(Rslt_39));
apps = take(portLoc(1, Rslt_39V));
lams = take(portLoc(2, Rslt_39V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_39), makePair(LAZ, 0, apps, lams));
// call take-char at /home/jim/toccata/interpreter/intrp-rdr.toc: 758
Term Rslt_40Args = makePair(APP, 0, state_1, SUB);
Term Rslt_40 = newTerm(VAR, 0, portLoc(2, Rslt_40Args));
#ifdef STRICT
pushRedex(Rslt_40Args, glblFn1732);
#else
swap(termLoc(Rslt_40), makePair(LAZ, 0, Rslt_40Args, glblFn1732));
#endif

// call skip-whitespace at /home/jim/toccata/interpreter/intrp-rdr.toc: 758
Term Rslt_41Args = makePair(APP, 0, Rslt_40, SUB);
Term Rslt_41 = newTerm(VAR, 0, portLoc(2, Rslt_41Args));
#ifdef STRICT
pushRedex(Rslt_41Args, glblFn1736);
#else
swap(termLoc(Rslt_41), makePair(LAZ, 0, Rslt_41Args, glblFn1736));
#endif

// call  at /home/jim/toccata/interpreter/intrp-rdr.toc: 758
Term Rslt_42Args = makePair(APP, 0, Rslt_41, SUB);
Term Rslt_42 = newTerm(VAR, 0, portLoc(2, Rslt_42Args));
#ifdef STRICT
pushRedex(Rslt_42Args, seq_7);
#else
swap(termLoc(Rslt_42), makePair(LAZ, 0, Rslt_42Args, seq_7));
#endif

// link args to body
swap(r_seq_3, Rslt_42);

Term Rslt_42V = get(termLoc(Rslt_42));
apps = take(portLoc(1, Rslt_42V));
lams = take(portLoc(2, Rslt_42V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_42), makePair(LAZ, 0, apps, lams));
pushRedex(args, seq_3);
return;
}
Term glblFn1865 = newRef(glblCFn1864);
void glblProto34_2581(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(772), args);
  args = makePair(APP, 0, glblStr1912, args);
  interact(args, glblProto34);
}
}
Term glblFn1867;
void glblCFn1866(Term ref, Term args) {
Term apps, lams, V;
Location vLoc;
Term seq_2 = makePair(LAM, 0, SUB, NUL);
Location r_seq_2 = portLoc(2, seq_2);
Term state_1 = newTerm(VAR, 0, portLoc(1, seq_2));
// allocate args at /home/jim/toccata/interpreter/intrp-rdr.toc: 768
Term seq_6 = makePair(LAM, 0, SUB, NUL);
Location r_seq_6 = portLoc(2, seq_6);
Term s_5 = newTerm(VAR, 0, portLoc(1, seq_6));
// allocate args at /home/jim/toccata/interpreter/intrp-rdr.toc: 769

Term s_5_2;
s_5 = dupeArg(s_5, &s_5_2, 0);

Term s_5_1;
s_5 = dupeArg(s_5, &s_5_1, 0);
Term seq_10 = makePair(LAM, 0, SUB, NUL);
Location r_seq_10 = portLoc(2, seq_10);
Term c_9 = newTerm(VAR, 0, portLoc(1, seq_10));
// allocate args at /home/jim/toccata/interpreter/intrp-rdr.toc: 770
Term seq_16 = makePair(LAM, 0, SUB, NUL);
Location r_seq_16 = portLoc(2, seq_16);
Term nm_15 = newTerm(VAR, 0, portLoc(1, seq_16));
// allocate args at /home/jim/toccata/interpreter/intrp-rdr.toc: 774

Term nm_15_1;
nm_15 = dupeArg(nm_15, &nm_15_1, 0);
// call str= at /home/jim/toccata/interpreter/intrp-rdr.toc: 772
Term Rslt_12Args = makePair(APP, 0, glblStr747, SUB);
Term Rslt_12 = newTerm(VAR, 0, portLoc(2, Rslt_12Args));
Rslt_12Args = makePair(APP, 0, c_9, Rslt_12Args);
#ifdef STRICT
pushRedex(Rslt_12Args, glblFn363);
#else
swap(termLoc(Rslt_12), makePair(LAZ, 0, Rslt_12Args, glblFn363));
#endif

// call parse-fn-body at /home/jim/toccata/interpreter/intrp-rdr.toc: 773
Term Rslt_13Args = makePair(APP, 0, glblStr402, SUB);
Term Rslt_13 = newTerm(VAR, 0, portLoc(2, Rslt_13Args));
Rslt_13Args = makePair(APP, 0, s_5, Rslt_13Args);
#ifdef STRICT
pushRedex(Rslt_13Args, glblFn1865);
#else
swap(termLoc(Rslt_13), makePair(LAZ, 0, Rslt_13Args, glblFn1865));
#endif

// call elt1 at /home/jim/toccata/interpreter/intrp-rdr.toc: 775
Term Rslt_18Args = makePair(APP, 0, nm_15, SUB);
Term Rslt_18 = newTerm(VAR, 0, portLoc(2, Rslt_18Args));
#ifdef STRICT
pushRedex(Rslt_18Args, glblFn1744);
#else
swap(termLoc(Rslt_18), makePair(LAZ, 0, Rslt_18Args, glblFn1744));
#endif

// call skip-whitespace at /home/jim/toccata/interpreter/intrp-rdr.toc: 775
Term Rslt_19Args = makePair(APP, 0, Rslt_18, SUB);
Term Rslt_19 = newTerm(VAR, 0, portLoc(2, Rslt_19Args));
#ifdef STRICT
pushRedex(Rslt_19Args, glblFn1736);
#else
swap(termLoc(Rslt_19), makePair(LAZ, 0, Rslt_19Args, glblFn1736));
#endif

// call elt0 at /home/jim/toccata/interpreter/intrp-rdr.toc: 775
Term Rslt_20Args = makePair(APP, 0, nm_15_1, SUB);
Term Rslt_20 = newTerm(VAR, 0, portLoc(2, Rslt_20Args));
#ifdef STRICT
pushRedex(Rslt_20Args, glblFn1742);
#else
swap(termLoc(Rslt_20), makePair(LAZ, 0, Rslt_20Args, glblFn1742));
#endif

// call parse-fn-body at /home/jim/toccata/interpreter/intrp-rdr.toc: 775
Term Rslt_21Args = makePair(APP, 0, Rslt_20, SUB);
Term Rslt_21 = newTerm(VAR, 0, portLoc(2, Rslt_21Args));
Rslt_21Args = makePair(APP, 0, Rslt_19, Rslt_21Args);
#ifdef STRICT
pushRedex(Rslt_21Args, glblFn1865);
#else
swap(termLoc(Rslt_21), makePair(LAZ, 0, Rslt_21Args, glblFn1865));
#endif

// link args to body
swap(r_seq_16, Rslt_21);

Term Rslt_21V = get(termLoc(Rslt_21));
apps = take(portLoc(1, Rslt_21V));
lams = take(portLoc(2, Rslt_21V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_21), makePair(LAZ, 0, apps, lams));
// call read-symbol at /home/jim/toccata/interpreter/intrp-rdr.toc: 774
Term Rslt_22Args = makePair(APP, 0, s_5_1, SUB);
Term Rslt_22 = newTerm(VAR, 0, portLoc(2, Rslt_22Args));
#ifdef STRICT
pushRedex(Rslt_22Args, glblFn1750);
#else
swap(termLoc(Rslt_22), makePair(LAZ, 0, Rslt_22Args, glblFn1750));
#endif

// call  at /home/jim/toccata/interpreter/intrp-rdr.toc: 774
Term Rslt_23Args = makePair(APP, 0, Rslt_22, SUB);
Term Rslt_23 = newTerm(VAR, 0, portLoc(2, Rslt_23Args));
#ifdef STRICT
pushRedex(Rslt_23Args, seq_16);
#else
swap(termLoc(Rslt_23), makePair(LAZ, 0, Rslt_23Args, seq_16));
#endif

// call cond at /home/jim/toccata/interpreter/intrp-rdr.toc: 772
Term Rslt_24Args = makePair(APP, 0, Rslt_23, SUB);
Term Rslt_24 = newTerm(VAR, 0, portLoc(2, Rslt_24Args));
Rslt_24Args = makePair(APP, 0, Rslt_13, Rslt_24Args);
Rslt_24Args = makePair(APP, 0, Rslt_12, Rslt_24Args);
#ifdef STRICT
pushRedex(Rslt_24Args, newRef(glblProto34_2581));
#else
swap(termLoc(Rslt_24), makePair(LAZ, 0, Rslt_24Args, newRef(glblProto34_2581)));
#endif
// link args to body
swap(r_seq_10, Rslt_24);

Term Rslt_24V = get(termLoc(Rslt_24));
apps = take(portLoc(1, Rslt_24V));
lams = take(portLoc(2, Rslt_24V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_24), makePair(LAZ, 0, apps, lams));
// call peek-char at /home/jim/toccata/interpreter/intrp-rdr.toc: 770
Term Rslt_25Args = makePair(APP, 0, s_5_2, SUB);
Term Rslt_25 = newTerm(VAR, 0, portLoc(2, Rslt_25Args));
#ifdef STRICT
pushRedex(Rslt_25Args, glblFn1730);
#else
swap(termLoc(Rslt_25), makePair(LAZ, 0, Rslt_25Args, glblFn1730));
#endif

// call  at /home/jim/toccata/interpreter/intrp-rdr.toc: 770
Term Rslt_26Args = makePair(APP, 0, Rslt_25, SUB);
Term Rslt_26 = newTerm(VAR, 0, portLoc(2, Rslt_26Args));
#ifdef STRICT
pushRedex(Rslt_26Args, seq_10);
#else
swap(termLoc(Rslt_26), makePair(LAZ, 0, Rslt_26Args, seq_10));
#endif

// link args to body
swap(r_seq_6, Rslt_26);

Term Rslt_26V = get(termLoc(Rslt_26));
apps = take(portLoc(1, Rslt_26V));
lams = take(portLoc(2, Rslt_26V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_26), makePair(LAZ, 0, apps, lams));
// call skip-whitespace at /home/jim/toccata/interpreter/intrp-rdr.toc: 769
Term Rslt_27Args = makePair(APP, 0, state_1, SUB);
Term Rslt_27 = newTerm(VAR, 0, portLoc(2, Rslt_27Args));
#ifdef STRICT
pushRedex(Rslt_27Args, glblFn1736);
#else
swap(termLoc(Rslt_27), makePair(LAZ, 0, Rslt_27Args, glblFn1736));
#endif

// call  at /home/jim/toccata/interpreter/intrp-rdr.toc: 769
Term Rslt_28Args = makePair(APP, 0, Rslt_27, SUB);
Term Rslt_28 = newTerm(VAR, 0, portLoc(2, Rslt_28Args));
#ifdef STRICT
pushRedex(Rslt_28Args, seq_6);
#else
swap(termLoc(Rslt_28), makePair(LAZ, 0, Rslt_28Args, seq_6));
#endif

// link args to body
swap(r_seq_2, Rslt_28);

Term Rslt_28V = get(termLoc(Rslt_28));
apps = take(portLoc(1, Rslt_28V));
lams = take(portLoc(2, Rslt_28V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_28), makePair(LAZ, 0, apps, lams));
pushRedex(args, seq_2);
return;
}
Term glblFn1867 = newRef(glblCFn1866);
Term glblFn1869;
void glblCFn1868(Term ref, Term args) {
Term apps, lams, V;
Location vLoc;
Term seq_2 = makePair(LAM, 0, SUB, NUL);
Location r_seq_2 = portLoc(2, seq_2);
Term state_1 = newTerm(VAR, 0, portLoc(1, seq_2));
// allocate args at /home/jim/toccata/interpreter/intrp-rdr.toc: 779
Term seq_6 = makePair(LAM, 0, SUB, NUL);
Location r_seq_6 = portLoc(2, seq_6);
Term s_5 = newTerm(VAR, 0, portLoc(1, seq_6));
// allocate args at /home/jim/toccata/interpreter/intrp-rdr.toc: 780
Term seq_10 = makePair(LAM, 0, SUB, NUL);
Location r_seq_10 = portLoc(2, seq_10);
Term s2_9 = newTerm(VAR, 0, portLoc(1, seq_10));
// allocate args at /home/jim/toccata/interpreter/intrp-rdr.toc: 781
Term seq_14 = makePair(LAM, 0, SUB, NUL);
Location r_seq_14 = portLoc(2, seq_14);
Term bindings_13 = newTerm(VAR, 0, portLoc(1, seq_14));
// allocate args at /home/jim/toccata/interpreter/intrp-rdr.toc: 782

Term bindings_13_1;
bindings_13 = dupeArg(bindings_13, &bindings_13_1, 0);
Term seq_18 = makePair(LAM, 0, SUB, NUL);
Location r_seq_18 = portLoc(2, seq_18);
Term body_17 = newTerm(VAR, 0, portLoc(1, seq_18));
// allocate args at /home/jim/toccata/interpreter/intrp-rdr.toc: 783

Term body_17_2;
body_17 = dupeArg(body_17, &body_17_2, 0);

Term body_17_1;
body_17 = dupeArg(body_17, &body_17_1, 0);
Term seq_22 = makePair(LAM, 0, SUB, NUL);
Location r_seq_22 = portLoc(2, seq_22);
Term inner_body_21 = newTerm(VAR, 0, portLoc(1, seq_22));
// allocate args at /home/jim/toccata/interpreter/intrp-rdr.toc: 784
Term seq_26 = makePair(LAM, 0, SUB, NUL);
Location r_seq_26 = portLoc(2, seq_26);
Term s4_25 = newTerm(VAR, 0, portLoc(1, seq_26));
// allocate args at /home/jim/toccata/interpreter/intrp-rdr.toc: 785

Term s4_25_1;
s4_25 = dupeArg(s4_25, &s4_25_1, 0);
Term glblVal1896_16;
glblVal1896 = dupeArg(glblVal1896, &glblVal1896_16, 0);

// call elt0 at /home/jim/toccata/interpreter/intrp-rdr.toc: 786
Term Rslt_28Args = makePair(APP, 0, bindings_13, SUB);
Term Rslt_28 = newTerm(VAR, 0, portLoc(2, Rslt_28Args));
#ifdef STRICT
pushRedex(Rslt_28Args, glblFn1742);
#else
swap(termLoc(Rslt_28), makePair(LAZ, 0, Rslt_28Args, glblFn1742));
#endif

// call make-loc at /home/jim/toccata/interpreter/intrp-rdr.toc: 787
Term Rslt_29Args = makePair(APP, 0, s4_25, SUB);
Term Rslt_29 = newTerm(VAR, 0, portLoc(2, Rslt_29Args));
#ifdef STRICT
pushRedex(Rslt_29Args, glblFn1762);
#else
swap(termLoc(Rslt_29), makePair(LAZ, 0, Rslt_29Args, glblFn1762));
#endif

// call let-desugar at /home/jim/toccata/interpreter/intrp-rdr.toc: 786
Term Rslt_30Args = makePair(APP, 0, Rslt_29, SUB);
Term Rslt_30 = newTerm(VAR, 0, portLoc(2, Rslt_30Args));
Rslt_30Args = makePair(APP, 0, inner_body_21, Rslt_30Args);
Rslt_30Args = makePair(APP, 0, Rslt_28, Rslt_30Args);
#ifdef STRICT
pushRedex(Rslt_30Args, glblFn1804);
#else
swap(termLoc(Rslt_30), makePair(LAZ, 0, Rslt_30Args, glblFn1804));
#endif

// call ParserMatch at /home/jim/toccata/interpreter/intrp-rdr.toc: 786
Term Rslt_31Args = makePair(APP, 0, s4_25_1, SUB);
Term Rslt_31 = newTerm(VAR, 0, portLoc(2, Rslt_31Args));
Rslt_31Args = makePair(APP, 0, Rslt_30, Rslt_31Args);
#ifdef STRICT
pushRedex(Rslt_31Args, glblVal1896_16);
#else
swap(termLoc(Rslt_31), makePair(LAZ, 0, Rslt_31Args, glblVal1896_16));
#endif

// link args to body
swap(r_seq_26, Rslt_31);

Term Rslt_31V = get(termLoc(Rslt_31));
apps = take(portLoc(1, Rslt_31V));
lams = take(portLoc(2, Rslt_31V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_31), makePair(LAZ, 0, apps, lams));
// call elt2 at /home/jim/toccata/interpreter/intrp-rdr.toc: 785
Term Rslt_32Args = makePair(APP, 0, body_17, SUB);
Term Rslt_32 = newTerm(VAR, 0, portLoc(2, Rslt_32Args));
#ifdef STRICT
pushRedex(Rslt_32Args, glblFn1756);
#else
swap(termLoc(Rslt_32), makePair(LAZ, 0, Rslt_32Args, glblFn1756));
#endif

// call  at /home/jim/toccata/interpreter/intrp-rdr.toc: 785
Term Rslt_33Args = makePair(APP, 0, Rslt_32, SUB);
Term Rslt_33 = newTerm(VAR, 0, portLoc(2, Rslt_33Args));
#ifdef STRICT
pushRedex(Rslt_33Args, seq_26);
#else
swap(termLoc(Rslt_33), makePair(LAZ, 0, Rslt_33Args, seq_26));
#endif

// link args to body
swap(r_seq_22, Rslt_33);

Term Rslt_33V = get(termLoc(Rslt_33));
apps = take(portLoc(1, Rslt_33V));
lams = take(portLoc(2, Rslt_33V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_33), makePair(LAZ, 0, apps, lams));
// call elt0 at /home/jim/toccata/interpreter/intrp-rdr.toc: 784
Term Rslt_34Args = makePair(APP, 0, body_17_1, SUB);
Term Rslt_34 = newTerm(VAR, 0, portLoc(2, Rslt_34Args));
#ifdef STRICT
pushRedex(Rslt_34Args, glblFn1742);
#else
swap(termLoc(Rslt_34), makePair(LAZ, 0, Rslt_34Args, glblFn1742));
#endif

// call elt1 at /home/jim/toccata/interpreter/intrp-rdr.toc: 784
Term Rslt_35Args = makePair(APP, 0, body_17_2, SUB);
Term Rslt_35 = newTerm(VAR, 0, portLoc(2, Rslt_35Args));
#ifdef STRICT
pushRedex(Rslt_35Args, glblFn1744);
#else
swap(termLoc(Rslt_35), makePair(LAZ, 0, Rslt_35Args, glblFn1744));
#endif

// call vect-concat at /home/jim/toccata/interpreter/intrp-rdr.toc: 784
Term Rslt_36Args = makePair(APP, 0, Rslt_35, SUB);
Term Rslt_36 = newTerm(VAR, 0, portLoc(2, Rslt_36Args));
Rslt_36Args = makePair(APP, 0, Rslt_34, Rslt_36Args);
#ifdef STRICT
pushRedex(Rslt_36Args, glblFn1764);
#else
swap(termLoc(Rslt_36), makePair(LAZ, 0, Rslt_36Args, glblFn1764));
#endif

// call  at /home/jim/toccata/interpreter/intrp-rdr.toc: 784
Term Rslt_37Args = makePair(APP, 0, Rslt_36, SUB);
Term Rslt_37 = newTerm(VAR, 0, portLoc(2, Rslt_37Args));
#ifdef STRICT
pushRedex(Rslt_37Args, seq_22);
#else
swap(termLoc(Rslt_37), makePair(LAZ, 0, Rslt_37Args, seq_22));
#endif

// link args to body
swap(r_seq_18, Rslt_37);

Term Rslt_37V = get(termLoc(Rslt_37));
apps = take(portLoc(1, Rslt_37V));
lams = take(portLoc(2, Rslt_37V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_37), makePair(LAZ, 0, apps, lams));
// call elt1 at /home/jim/toccata/interpreter/intrp-rdr.toc: 783
Term Rslt_38Args = makePair(APP, 0, bindings_13_1, SUB);
Term Rslt_38 = newTerm(VAR, 0, portLoc(2, Rslt_38Args));
#ifdef STRICT
pushRedex(Rslt_38Args, glblFn1744);
#else
swap(termLoc(Rslt_38), makePair(LAZ, 0, Rslt_38Args, glblFn1744));
#endif

// call parse-body at /home/jim/toccata/interpreter/intrp-rdr.toc: 783
Term Rslt_39Args = makePair(APP, 0, Rslt_38, SUB);
Term Rslt_39 = newTerm(VAR, 0, portLoc(2, Rslt_39Args));
#ifdef STRICT
pushRedex(Rslt_39Args, glblFn1827);
#else
swap(termLoc(Rslt_39), makePair(LAZ, 0, Rslt_39Args, glblFn1827));
#endif

// call  at /home/jim/toccata/interpreter/intrp-rdr.toc: 783
Term Rslt_40Args = makePair(APP, 0, Rslt_39, SUB);
Term Rslt_40 = newTerm(VAR, 0, portLoc(2, Rslt_40Args));
#ifdef STRICT
pushRedex(Rslt_40Args, seq_18);
#else
swap(termLoc(Rslt_40), makePair(LAZ, 0, Rslt_40Args, seq_18));
#endif

// link args to body
swap(r_seq_14, Rslt_40);

Term Rslt_40V = get(termLoc(Rslt_40));
apps = take(portLoc(1, Rslt_40V));
lams = take(portLoc(2, Rslt_40V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_40), makePair(LAZ, 0, apps, lams));
// call read-let-bindings at /home/jim/toccata/interpreter/intrp-rdr.toc: 782
Term Rslt_41Args = makePair(APP, 0, s2_9, SUB);
Term Rslt_41 = newTerm(VAR, 0, portLoc(2, Rslt_41Args));
#ifdef STRICT
pushRedex(Rslt_41Args, glblFn1831);
#else
swap(termLoc(Rslt_41), makePair(LAZ, 0, Rslt_41Args, glblFn1831));
#endif

// call  at /home/jim/toccata/interpreter/intrp-rdr.toc: 782
Term Rslt_42Args = makePair(APP, 0, Rslt_41, SUB);
Term Rslt_42 = newTerm(VAR, 0, portLoc(2, Rslt_42Args));
#ifdef STRICT
pushRedex(Rslt_42Args, seq_14);
#else
swap(termLoc(Rslt_42), makePair(LAZ, 0, Rslt_42Args, seq_14));
#endif

// link args to body
swap(r_seq_10, Rslt_42);

Term Rslt_42V = get(termLoc(Rslt_42));
apps = take(portLoc(1, Rslt_42V));
lams = take(portLoc(2, Rslt_42V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_42), makePair(LAZ, 0, apps, lams));
// call take-char at /home/jim/toccata/interpreter/intrp-rdr.toc: 781
Term Rslt_43Args = makePair(APP, 0, s_5, SUB);
Term Rslt_43 = newTerm(VAR, 0, portLoc(2, Rslt_43Args));
#ifdef STRICT
pushRedex(Rslt_43Args, glblFn1732);
#else
swap(termLoc(Rslt_43), makePair(LAZ, 0, Rslt_43Args, glblFn1732));
#endif

// call skip-whitespace at /home/jim/toccata/interpreter/intrp-rdr.toc: 781
Term Rslt_44Args = makePair(APP, 0, Rslt_43, SUB);
Term Rslt_44 = newTerm(VAR, 0, portLoc(2, Rslt_44Args));
#ifdef STRICT
pushRedex(Rslt_44Args, glblFn1736);
#else
swap(termLoc(Rslt_44), makePair(LAZ, 0, Rslt_44Args, glblFn1736));
#endif

// call  at /home/jim/toccata/interpreter/intrp-rdr.toc: 781
Term Rslt_45Args = makePair(APP, 0, Rslt_44, SUB);
Term Rslt_45 = newTerm(VAR, 0, portLoc(2, Rslt_45Args));
#ifdef STRICT
pushRedex(Rslt_45Args, seq_10);
#else
swap(termLoc(Rslt_45), makePair(LAZ, 0, Rslt_45Args, seq_10));
#endif

// link args to body
swap(r_seq_6, Rslt_45);

Term Rslt_45V = get(termLoc(Rslt_45));
apps = take(portLoc(1, Rslt_45V));
lams = take(portLoc(2, Rslt_45V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_45), makePair(LAZ, 0, apps, lams));
// call skip-whitespace at /home/jim/toccata/interpreter/intrp-rdr.toc: 780
Term Rslt_46Args = makePair(APP, 0, state_1, SUB);
Term Rslt_46 = newTerm(VAR, 0, portLoc(2, Rslt_46Args));
#ifdef STRICT
pushRedex(Rslt_46Args, glblFn1736);
#else
swap(termLoc(Rslt_46), makePair(LAZ, 0, Rslt_46Args, glblFn1736));
#endif

// call  at /home/jim/toccata/interpreter/intrp-rdr.toc: 780
Term Rslt_47Args = makePair(APP, 0, Rslt_46, SUB);
Term Rslt_47 = newTerm(VAR, 0, portLoc(2, Rslt_47Args));
#ifdef STRICT
pushRedex(Rslt_47Args, seq_6);
#else
swap(termLoc(Rslt_47), makePair(LAZ, 0, Rslt_47Args, seq_6));
#endif

// link args to body
swap(r_seq_2, Rslt_47);

Term Rslt_47V = get(termLoc(Rslt_47));
apps = take(portLoc(1, Rslt_47V));
lams = take(portLoc(2, Rslt_47V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_47), makePair(LAZ, 0, apps, lams));
pushRedex(args, seq_2);
return;
}
Term glblFn1869 = newRef(glblCFn1868);
struct {TYPE_SIZE type;
        REFS_SIZE refs;
        int64_t *hash;
        Value *parent;
        int64_t len;
        char buffer[3];
} str47 = {StringBufferType, REFS_STATIC, 0, 0, 2, "fn"};
Term glblStr2584 = newTerm_(VAL, (Term)&str47);
struct {TYPE_SIZE type;
        REFS_SIZE refs;
        int64_t *hash;
        Value *parent;
        int64_t len;
        char buffer[4];
} str48 = {StringBufferType, REFS_STATIC, 0, 0, 3, "let"};
Term glblStr2586 = newTerm_(VAL, (Term)&str48);
void glblProto34_2589(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(798), args);
  args = makePair(APP, 0, glblStr1912, args);
  interact(args, glblProto34);
}
}
void glblProto34_2591(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(796), args);
  args = makePair(APP, 0, glblStr1912, args);
  interact(args, glblProto34);
}
}
void glblProto34_2593(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(794), args);
  args = makePair(APP, 0, glblStr1912, args);
  interact(args, glblProto34);
}
}
void glblProto34_2595(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(792), args);
  args = makePair(APP, 0, glblStr1912, args);
  interact(args, glblProto34);
}
}
Term glblFn1871;
void glblCFn1870(Term ref, Term args) {
Term apps, lams, V;
Location vLoc;
Term seq_4 = makePair(LAM, 0, SUB, NUL);
Location r_seq_4 = portLoc(2, seq_4);
Term s2_3 = newTerm(VAR, 0, portLoc(1, seq_4));
seq_4 = makePair(LAM, 0, SUB, seq_4);
Term name_2 = newTerm(VAR, 0, portLoc(1, seq_4));
seq_4 = makePair(LAM, 0, SUB, seq_4);
Term ns_1 = newTerm(VAR, 0, portLoc(1, seq_4));
// allocate args at /home/jim/toccata/interpreter/intrp-rdr.toc: 790

Term name_2_4;
name_2 = dupeArg(name_2, &name_2_4, 359);

Term name_2_3;
name_2 = dupeArg(name_2, &name_2_3, 360);

Term name_2_2;
name_2 = dupeArg(name_2, &name_2_2, 361);

Term name_2_1;
name_2 = dupeArg(name_2, &name_2_1, 362);

Term s2_3_4;
s2_3 = dupeArg(s2_3, &s2_3_4, 363);

Term s2_3_3;
s2_3 = dupeArg(s2_3, &s2_3_3, 364);

Term s2_3_2;
s2_3 = dupeArg(s2_3, &s2_3_2, 365);

Term s2_3_1;
s2_3 = dupeArg(s2_3, &s2_3_1, 366);
// call str= at /home/jim/toccata/interpreter/intrp-rdr.toc: 792
Term Rslt_6Args = makePair(APP, 0, glblStr2584, SUB);
Term Rslt_6 = newTerm(VAR, 0, portLoc(2, Rslt_6Args));
Rslt_6Args = makePair(APP, 0, name_2, Rslt_6Args);
#ifdef STRICT
pushRedex(Rslt_6Args, glblFn363);
#else
swap(termLoc(Rslt_6), makePair(LAZ, 0, Rslt_6Args, glblFn363));
#endif

// call parse-fn at /home/jim/toccata/interpreter/intrp-rdr.toc: 792
Term Rslt_7Args = makePair(APP, 0, s2_3, SUB);
Term Rslt_7 = newTerm(VAR, 0, portLoc(2, Rslt_7Args));
#ifdef STRICT
pushRedex(Rslt_7Args, glblFn1867);
#else
swap(termLoc(Rslt_7), makePair(LAZ, 0, Rslt_7Args, glblFn1867));
#endif

// call str= at /home/jim/toccata/interpreter/intrp-rdr.toc: 794
Term Rslt_8Args = makePair(APP, 0, glblStr2586, SUB);
Term Rslt_8 = newTerm(VAR, 0, portLoc(2, Rslt_8Args));
Rslt_8Args = makePair(APP, 0, name_2_1, Rslt_8Args);
#ifdef STRICT
pushRedex(Rslt_8Args, glblFn363);
#else
swap(termLoc(Rslt_8), makePair(LAZ, 0, Rslt_8Args, glblFn363));
#endif

// call parse-let at /home/jim/toccata/interpreter/intrp-rdr.toc: 794
Term Rslt_9Args = makePair(APP, 0, s2_3_1, SUB);
Term Rslt_9 = newTerm(VAR, 0, portLoc(2, Rslt_9Args));
#ifdef STRICT
pushRedex(Rslt_9Args, glblFn1869);
#else
swap(termLoc(Rslt_9), makePair(LAZ, 0, Rslt_9Args, glblFn1869));
#endif

// call str= at /home/jim/toccata/interpreter/intrp-rdr.toc: 796
Term Rslt_10Args = makePair(APP, 0, glblStr2251, SUB);
Term Rslt_10 = newTerm(VAR, 0, portLoc(2, Rslt_10Args));
Rslt_10Args = makePair(APP, 0, name_2_2, Rslt_10Args);
#ifdef STRICT
pushRedex(Rslt_10Args, glblFn363);
#else
swap(termLoc(Rslt_10), makePair(LAZ, 0, Rslt_10Args, glblFn363));
#endif

// call parse-cond at /home/jim/toccata/interpreter/intrp-rdr.toc: 796
Term Rslt_11Args = makePair(APP, 0, s2_3_2, SUB);
Term Rslt_11 = newTerm(VAR, 0, portLoc(2, Rslt_11Args));
#ifdef STRICT
pushRedex(Rslt_11Args, glblFn1847);
#else
swap(termLoc(Rslt_11), makePair(LAZ, 0, Rslt_11Args, glblFn1847));
#endif

// call str= at /home/jim/toccata/interpreter/intrp-rdr.toc: 798
Term Rslt_12Args = makePair(APP, 0, glblStr2500, SUB);
Term Rslt_12 = newTerm(VAR, 0, portLoc(2, Rslt_12Args));
Rslt_12Args = makePair(APP, 0, name_2_3, Rslt_12Args);
#ifdef STRICT
pushRedex(Rslt_12Args, glblFn363);
#else
swap(termLoc(Rslt_12), makePair(LAZ, 0, Rslt_12Args, glblFn363));
#endif

// call parse-and at /home/jim/toccata/interpreter/intrp-rdr.toc: 798
Term Rslt_13Args = makePair(APP, 0, s2_3_3, SUB);
Term Rslt_13 = newTerm(VAR, 0, portLoc(2, Rslt_13Args));
#ifdef STRICT
pushRedex(Rslt_13Args, glblFn1851);
#else
swap(termLoc(Rslt_13), makePair(LAZ, 0, Rslt_13Args, glblFn1851));
#endif

// call dispatch-special-2 at /home/jim/toccata/interpreter/intrp-rdr.toc: 799
Term Rslt_14Args = makePair(APP, 0, s2_3_4, SUB);
Term Rslt_14 = newTerm(VAR, 0, portLoc(2, Rslt_14Args));
Rslt_14Args = makePair(APP, 0, name_2_4, Rslt_14Args);
Rslt_14Args = makePair(APP, 0, ns_1, Rslt_14Args);
#ifdef STRICT
pushRedex(Rslt_14Args, glblFn1863);
#else
swap(termLoc(Rslt_14), makePair(LAZ, 0, Rslt_14Args, glblFn1863));
#endif

// call cond at /home/jim/toccata/interpreter/intrp-rdr.toc: 798
Term Rslt_15Args = makePair(APP, 0, Rslt_14, SUB);
Term Rslt_15 = newTerm(VAR, 0, portLoc(2, Rslt_15Args));
Rslt_15Args = makePair(APP, 0, Rslt_13, Rslt_15Args);
Rslt_15Args = makePair(APP, 0, Rslt_12, Rslt_15Args);
#ifdef STRICT
pushRedex(Rslt_15Args, newRef(glblProto34_2589));
#else
swap(termLoc(Rslt_15), makePair(LAZ, 0, Rslt_15Args, newRef(glblProto34_2589)));
#endif
// call cond at /home/jim/toccata/interpreter/intrp-rdr.toc: 796
Term Rslt_16Args = makePair(APP, 0, Rslt_15, SUB);
Term Rslt_16 = newTerm(VAR, 0, portLoc(2, Rslt_16Args));
Rslt_16Args = makePair(APP, 0, Rslt_11, Rslt_16Args);
Rslt_16Args = makePair(APP, 0, Rslt_10, Rslt_16Args);
#ifdef STRICT
pushRedex(Rslt_16Args, newRef(glblProto34_2591));
#else
swap(termLoc(Rslt_16), makePair(LAZ, 0, Rslt_16Args, newRef(glblProto34_2591)));
#endif
// call cond at /home/jim/toccata/interpreter/intrp-rdr.toc: 794
Term Rslt_17Args = makePair(APP, 0, Rslt_16, SUB);
Term Rslt_17 = newTerm(VAR, 0, portLoc(2, Rslt_17Args));
Rslt_17Args = makePair(APP, 0, Rslt_9, Rslt_17Args);
Rslt_17Args = makePair(APP, 0, Rslt_8, Rslt_17Args);
#ifdef STRICT
pushRedex(Rslt_17Args, newRef(glblProto34_2593));
#else
swap(termLoc(Rslt_17), makePair(LAZ, 0, Rslt_17Args, newRef(glblProto34_2593)));
#endif
// call cond at /home/jim/toccata/interpreter/intrp-rdr.toc: 792
Term Rslt_18Args = makePair(APP, 0, Rslt_17, SUB);
Term Rslt_18 = newTerm(VAR, 0, portLoc(2, Rslt_18Args));
Rslt_18Args = makePair(APP, 0, Rslt_7, Rslt_18Args);
Rslt_18Args = makePair(APP, 0, Rslt_6, Rslt_18Args);
#ifdef STRICT
pushRedex(Rslt_18Args, newRef(glblProto34_2595));
#else
swap(termLoc(Rslt_18), makePair(LAZ, 0, Rslt_18Args, newRef(glblProto34_2595)));
#endif
// link args to body
swap(r_seq_4, Rslt_18);

Term Rslt_18V = get(termLoc(Rslt_18));
apps = take(portLoc(1, Rslt_18V));
lams = take(portLoc(2, Rslt_18V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_18), makePair(LAZ, 0, apps, lams));
pushRedex(args, seq_4);
return;
}
Term glblFn1871 = newRef(glblCFn1870);
struct {TYPE_SIZE type;
        REFS_SIZE refs;
        int64_t *hash;
        Value *parent;
        int64_t len;
        char buffer[32];
} str49 = {StringBufferType, REFS_STATIC, 0, 0, 31, "unexpected end of input in call"};
Term glblStr2599 = newTerm_(VAL, (Term)&str49);
struct {TYPE_SIZE type;
        REFS_SIZE refs;
        int64_t *hash;
        Value *parent;
        int64_t len;
        char buffer[11];
} str50 = {StringBufferType, REFS_STATIC, 0, 0, 10, "empty call"};
Term glblStr2601 = newTerm_(VAL, (Term)&str50);
void glblProto20_2602(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(818), args);
  args = makePair(APP, 0, glblStr1912, args);
  interact(args, glblProto20);
}
}
void glblProto1889_2604(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(820), args);
  args = makePair(APP, 0, glblStr1912, args);
  interact(args, glblProto1889);
}
}
void glblProto1562_2607(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(820), args);
  args = makePair(APP, 0, glblStr1912, args);
  interact(args, glblProto1562);
}
}
void glblProto34_2609(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(818), args);
  args = makePair(APP, 0, glblStr1912, args);
  interact(args, glblProto34);
}
}
void glblProto1757_2612(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(816), args);
  args = makePair(APP, 0, glblStr1912, args);
  interact(args, glblProto1757);
}
}
void glblProto34_2615(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(808), args);
  args = makePair(APP, 0, glblStr1912, args);
  interact(args, glblProto34);
}
}
void glblProto34_2617(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(808), args);
  args = makePair(APP, 0, glblStr1912, args);
  interact(args, glblProto34);
}
}
void glblProto34_2619(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(808), args);
  args = makePair(APP, 0, glblStr1912, args);
  interact(args, glblProto34);
}
}
Term glblFn1873;
void glblCFn1872(Term ref, Term args) {
Term apps, lams, V;
Location vLoc;
Term seq_2 = makePair(LAM, 0, SUB, NUL);
Location r_seq_2 = portLoc(2, seq_2);
Term state_1 = newTerm(VAR, 0, portLoc(1, seq_2));
// allocate args at /home/jim/toccata/interpreter/intrp-rdr.toc: 804
Term seq_6 = makePair(LAM, 0, SUB, NUL);
Location r_seq_6 = portLoc(2, seq_6);
Term s_5 = newTerm(VAR, 0, portLoc(1, seq_6));
// allocate args at /home/jim/toccata/interpreter/intrp-rdr.toc: 805

Term s_5_4;
s_5 = dupeArg(s_5, &s_5_4, 0);

Term s_5_3;
s_5 = dupeArg(s_5, &s_5_3, 0);

Term s_5_2;
s_5 = dupeArg(s_5, &s_5_2, 0);

Term s_5_1;
s_5 = dupeArg(s_5, &s_5_1, 0);
Term seq_10 = makePair(LAM, 0, SUB, NUL);
Location r_seq_10 = portLoc(2, seq_10);
Term c_9 = newTerm(VAR, 0, portLoc(1, seq_10));
// allocate args at /home/jim/toccata/interpreter/intrp-rdr.toc: 806

Term c_9_2;
c_9 = dupeArg(c_9, &c_9_2, 0);

Term c_9_1;
c_9 = dupeArg(c_9, &c_9_1, 0);
Term glblVal1819_8;
glblVal1819 = dupeArg(glblVal1819, &glblVal1819_8, 0);

Term seq_29 = makePair(LAM, 0, SUB, NUL);
Location r_seq_29 = portLoc(2, seq_29);
Term op_res_28 = newTerm(VAR, 0, portLoc(1, seq_29));
// allocate args at /home/jim/toccata/interpreter/intrp-rdr.toc: 815

Term op_res_28_3;
op_res_28 = dupeArg(op_res_28, &op_res_28_3, 0);

Term op_res_28_2;
op_res_28 = dupeArg(op_res_28, &op_res_28_2, 0);

Term op_res_28_1;
op_res_28 = dupeArg(op_res_28, &op_res_28_1, 0);
Term seq_33 = makePair(LAM, 0, SUB, NUL);
Location r_seq_33 = portLoc(2, seq_33);
Term k_32 = newTerm(VAR, 0, portLoc(1, seq_33));
// allocate args at /home/jim/toccata/interpreter/intrp-rdr.toc: 816
Term seq_19 = makePair(LAM, 0, SUB, NUL);
Location r_seq_19 = portLoc(2, seq_19);
Term sym_18 = newTerm(VAR, 0, portLoc(1, seq_19));
// allocate args at /home/jim/toccata/interpreter/intrp-rdr.toc: 813

Term sym_18_2;
sym_18 = dupeArg(sym_18, &sym_18_2, 0);

Term sym_18_1;
sym_18 = dupeArg(sym_18, &sym_18_1, 0);
Term glblVal1908_5;
glblVal1908 = dupeArg(glblVal1908, &glblVal1908_5, 0);

Term glblVal1908_4;
glblVal1908 = dupeArg(glblVal1908, &glblVal1908_4, 0);

// call str= at /home/jim/toccata/interpreter/intrp-rdr.toc: 808
Term Rslt_12Args = makePair(APP, 0, glblStr402, SUB);
Term Rslt_12 = newTerm(VAR, 0, portLoc(2, Rslt_12Args));
Rslt_12Args = makePair(APP, 0, c_9, Rslt_12Args);
#ifdef STRICT
pushRedex(Rslt_12Args, glblFn363);
#else
swap(termLoc(Rslt_12), makePair(LAZ, 0, Rslt_12Args, glblFn363));
#endif

// call ParserError at /home/jim/toccata/interpreter/intrp-rdr.toc: 809
Term Rslt_13Args = makePair(APP, 0, s_5, SUB);
Term Rslt_13 = newTerm(VAR, 0, portLoc(2, Rslt_13Args));
Rslt_13Args = makePair(APP, 0, glblStr2599, Rslt_13Args);
#ifdef STRICT
pushRedex(Rslt_13Args, glblVal1908_4);
#else
swap(termLoc(Rslt_13), makePair(LAZ, 0, Rslt_13Args, glblVal1908_4));
#endif

// call str= at /home/jim/toccata/interpreter/intrp-rdr.toc: 810
Term Rslt_14Args = makePair(APP, 0, glblStr254, SUB);
Term Rslt_14 = newTerm(VAR, 0, portLoc(2, Rslt_14Args));
Rslt_14Args = makePair(APP, 0, c_9_1, Rslt_14Args);
#ifdef STRICT
pushRedex(Rslt_14Args, glblFn363);
#else
swap(termLoc(Rslt_14), makePair(LAZ, 0, Rslt_14Args, glblFn363));
#endif

// call ParserError at /home/jim/toccata/interpreter/intrp-rdr.toc: 811
Term Rslt_15Args = makePair(APP, 0, s_5_1, SUB);
Term Rslt_15 = newTerm(VAR, 0, portLoc(2, Rslt_15Args));
Rslt_15Args = makePair(APP, 0, glblStr2601, Rslt_15Args);
#ifdef STRICT
pushRedex(Rslt_15Args, glblVal1908_5);
#else
swap(termLoc(Rslt_15), makePair(LAZ, 0, Rslt_15Args, glblVal1908_5));
#endif

// call symbol-start? at /home/jim/toccata/interpreter/intrp-rdr.toc: 812
Term Rslt_16Args = makePair(APP, 0, c_9_2, SUB);
Term Rslt_16 = newTerm(VAR, 0, portLoc(2, Rslt_16Args));
#ifdef STRICT
pushRedex(Rslt_16Args, glblFn1724);
#else
swap(termLoc(Rslt_16), makePair(LAZ, 0, Rslt_16Args, glblFn1724));
#endif

// call elt0 at /home/jim/toccata/interpreter/intrp-rdr.toc: 814
Term Rslt_21Args = makePair(APP, 0, sym_18, SUB);
Term Rslt_21 = newTerm(VAR, 0, portLoc(2, Rslt_21Args));
#ifdef STRICT
pushRedex(Rslt_21Args, glblFn1742);
#else
swap(termLoc(Rslt_21), makePair(LAZ, 0, Rslt_21Args, glblFn1742));
#endif

// call elt1 at /home/jim/toccata/interpreter/intrp-rdr.toc: 814
Term Rslt_22Args = makePair(APP, 0, sym_18_1, SUB);
Term Rslt_22 = newTerm(VAR, 0, portLoc(2, Rslt_22Args));
#ifdef STRICT
pushRedex(Rslt_22Args, glblFn1744);
#else
swap(termLoc(Rslt_22), makePair(LAZ, 0, Rslt_22Args, glblFn1744));
#endif

// call elt2 at /home/jim/toccata/interpreter/intrp-rdr.toc: 814
Term Rslt_23Args = makePair(APP, 0, sym_18_2, SUB);
Term Rslt_23 = newTerm(VAR, 0, portLoc(2, Rslt_23Args));
#ifdef STRICT
pushRedex(Rslt_23Args, glblFn1756);
#else
swap(termLoc(Rslt_23), makePair(LAZ, 0, Rslt_23Args, glblFn1756));
#endif

// call dispatch-special at /home/jim/toccata/interpreter/intrp-rdr.toc: 814
Term Rslt_24Args = makePair(APP, 0, Rslt_23, SUB);
Term Rslt_24 = newTerm(VAR, 0, portLoc(2, Rslt_24Args));
Rslt_24Args = makePair(APP, 0, Rslt_22, Rslt_24Args);
Rslt_24Args = makePair(APP, 0, Rslt_21, Rslt_24Args);
#ifdef STRICT
pushRedex(Rslt_24Args, glblFn1871);
#else
swap(termLoc(Rslt_24), makePair(LAZ, 0, Rslt_24Args, glblFn1871));
#endif

// link args to body
swap(r_seq_19, Rslt_24);

Term Rslt_24V = get(termLoc(Rslt_24));
apps = take(portLoc(1, Rslt_24V));
lams = take(portLoc(2, Rslt_24V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_24), makePair(LAZ, 0, apps, lams));
// call read-full-symbol at /home/jim/toccata/interpreter/intrp-rdr.toc: 813
Term Rslt_25Args = makePair(APP, 0, s_5_2, SUB);
Term Rslt_25 = newTerm(VAR, 0, portLoc(2, Rslt_25Args));
#ifdef STRICT
pushRedex(Rslt_25Args, glblFn1766);
#else
swap(termLoc(Rslt_25), makePair(LAZ, 0, Rslt_25Args, glblFn1766));
#endif

// call  at /home/jim/toccata/interpreter/intrp-rdr.toc: 813
Term Rslt_26Args = makePair(APP, 0, Rslt_25, SUB);
Term Rslt_26 = newTerm(VAR, 0, portLoc(2, Rslt_26Args));
#ifdef STRICT
pushRedex(Rslt_26Args, seq_19);
#else
swap(termLoc(Rslt_26), makePair(LAZ, 0, Rslt_26Args, seq_19));
#endif

// call = at /home/jim/toccata/interpreter/intrp-rdr.toc: 818
Term Rslt_35Args = makePair(APP, 0, newI60(2), SUB);
Term Rslt_35 = newTerm(VAR, 0, portLoc(2, Rslt_35Args));
Rslt_35Args = makePair(APP, 0, k_32, Rslt_35Args);
#ifdef STRICT
pushRedex(Rslt_35Args, newRef(glblProto20_2602));
#else
swap(termLoc(Rslt_35), makePair(LAZ, 0, Rslt_35Args, newRef(glblProto20_2602)));
#endif
// call .state at /home/jim/toccata/interpreter/intrp-rdr.toc: 820
Term Rslt_36Args = makePair(APP, 0, op_res_28_1, SUB);
Term Rslt_36 = newTerm(VAR, 0, portLoc(2, Rslt_36Args));
#ifdef STRICT
pushRedex(Rslt_36Args, newRef(glblProto1889_2604));
#else
swap(termLoc(Rslt_36), makePair(LAZ, 0, Rslt_36Args, newRef(glblProto1889_2604)));
#endif
// call .value at /home/jim/toccata/interpreter/intrp-rdr.toc: 820
Term Rslt_37Args = makePair(APP, 0, op_res_28_2, SUB);
Term Rslt_37 = newTerm(VAR, 0, portLoc(2, Rslt_37Args));
#ifdef STRICT
pushRedex(Rslt_37Args, newRef(glblProto1562_2607));
#else
swap(termLoc(Rslt_37), makePair(LAZ, 0, Rslt_37Args, newRef(glblProto1562_2607)));
#endif
// call parse-call-args at /home/jim/toccata/interpreter/intrp-rdr.toc: 820
Term Rslt_38Args = makePair(APP, 0, Rslt_37, SUB);
Term Rslt_38 = newTerm(VAR, 0, portLoc(2, Rslt_38Args));
Rslt_38Args = makePair(APP, 0, Rslt_36, Rslt_38Args);
#ifdef STRICT
pushRedex(Rslt_38Args, glblFn1841);
#else
swap(termLoc(Rslt_38), makePair(LAZ, 0, Rslt_38Args, glblFn1841));
#endif

// call cond at /home/jim/toccata/interpreter/intrp-rdr.toc: 818
Term Rslt_39Args = makePair(APP, 0, Rslt_38, SUB);
Term Rslt_39 = newTerm(VAR, 0, portLoc(2, Rslt_39Args));
Rslt_39Args = makePair(APP, 0, op_res_28, Rslt_39Args);
Rslt_39Args = makePair(APP, 0, Rslt_35, Rslt_39Args);
#ifdef STRICT
pushRedex(Rslt_39Args, newRef(glblProto34_2609));
#else
swap(termLoc(Rslt_39), makePair(LAZ, 0, Rslt_39Args, newRef(glblProto34_2609)));
#endif
// link args to body
swap(r_seq_33, Rslt_39);

Term Rslt_39V = get(termLoc(Rslt_39));
apps = take(portLoc(1, Rslt_39V));
lams = take(portLoc(2, Rslt_39V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_39), makePair(LAZ, 0, apps, lams));
// call result-kind at /home/jim/toccata/interpreter/intrp-rdr.toc: 816
Term Rslt_40Args = makePair(APP, 0, op_res_28_3, SUB);
Term Rslt_40 = newTerm(VAR, 0, portLoc(2, Rslt_40Args));
#ifdef STRICT
pushRedex(Rslt_40Args, newRef(glblProto1757_2612));
#else
swap(termLoc(Rslt_40), makePair(LAZ, 0, Rslt_40Args, newRef(glblProto1757_2612)));
#endif
// call  at /home/jim/toccata/interpreter/intrp-rdr.toc: 816
Term Rslt_41Args = makePair(APP, 0, Rslt_40, SUB);
Term Rslt_41 = newTerm(VAR, 0, portLoc(2, Rslt_41Args));
#ifdef STRICT
pushRedex(Rslt_41Args, seq_33);
#else
swap(termLoc(Rslt_41), makePair(LAZ, 0, Rslt_41Args, seq_33));
#endif

// link args to body
swap(r_seq_29, Rslt_41);

Term Rslt_41V = get(termLoc(Rslt_41));
apps = take(portLoc(1, Rslt_41V));
lams = take(portLoc(2, Rslt_41V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_41), makePair(LAZ, 0, apps, lams));
// call parse-expr at /home/jim/toccata/interpreter/intrp-rdr.toc: 548
Term Rslt_42Args = makePair(APP, 0, s_5_3, SUB);
Term Rslt_42 = newTerm(VAR, 0, portLoc(2, Rslt_42Args));
#ifdef STRICT
pushRedex(Rslt_42Args, glblVal1819_8);
#else
swap(termLoc(Rslt_42), makePair(LAZ, 0, Rslt_42Args, glblVal1819_8));
#endif

// call  at /home/jim/toccata/interpreter/intrp-rdr.toc: 548
Term Rslt_43Args = makePair(APP, 0, Rslt_42, SUB);
Term Rslt_43 = newTerm(VAR, 0, portLoc(2, Rslt_43Args));
#ifdef STRICT
pushRedex(Rslt_43Args, seq_29);
#else
swap(termLoc(Rslt_43), makePair(LAZ, 0, Rslt_43Args, seq_29));
#endif

// call cond at /home/jim/toccata/interpreter/intrp-rdr.toc: 808
Term Rslt_44Args = makePair(APP, 0, Rslt_43, SUB);
Term Rslt_44 = newTerm(VAR, 0, portLoc(2, Rslt_44Args));
Rslt_44Args = makePair(APP, 0, Rslt_26, Rslt_44Args);
Rslt_44Args = makePair(APP, 0, Rslt_16, Rslt_44Args);
#ifdef STRICT
pushRedex(Rslt_44Args, newRef(glblProto34_2615));
#else
swap(termLoc(Rslt_44), makePair(LAZ, 0, Rslt_44Args, newRef(glblProto34_2615)));
#endif
// call cond at /home/jim/toccata/interpreter/intrp-rdr.toc: 808
Term Rslt_45Args = makePair(APP, 0, Rslt_44, SUB);
Term Rslt_45 = newTerm(VAR, 0, portLoc(2, Rslt_45Args));
Rslt_45Args = makePair(APP, 0, Rslt_15, Rslt_45Args);
Rslt_45Args = makePair(APP, 0, Rslt_14, Rslt_45Args);
#ifdef STRICT
pushRedex(Rslt_45Args, newRef(glblProto34_2617));
#else
swap(termLoc(Rslt_45), makePair(LAZ, 0, Rslt_45Args, newRef(glblProto34_2617)));
#endif
// call cond at /home/jim/toccata/interpreter/intrp-rdr.toc: 808
Term Rslt_46Args = makePair(APP, 0, Rslt_45, SUB);
Term Rslt_46 = newTerm(VAR, 0, portLoc(2, Rslt_46Args));
Rslt_46Args = makePair(APP, 0, Rslt_13, Rslt_46Args);
Rslt_46Args = makePair(APP, 0, Rslt_12, Rslt_46Args);
#ifdef STRICT
pushRedex(Rslt_46Args, newRef(glblProto34_2619));
#else
swap(termLoc(Rslt_46), makePair(LAZ, 0, Rslt_46Args, newRef(glblProto34_2619)));
#endif
// link args to body
swap(r_seq_10, Rslt_46);

Term Rslt_46V = get(termLoc(Rslt_46));
apps = take(portLoc(1, Rslt_46V));
lams = take(portLoc(2, Rslt_46V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_46), makePair(LAZ, 0, apps, lams));
// call peek-char at /home/jim/toccata/interpreter/intrp-rdr.toc: 806
Term Rslt_47Args = makePair(APP, 0, s_5_4, SUB);
Term Rslt_47 = newTerm(VAR, 0, portLoc(2, Rslt_47Args));
#ifdef STRICT
pushRedex(Rslt_47Args, glblFn1730);
#else
swap(termLoc(Rslt_47), makePair(LAZ, 0, Rslt_47Args, glblFn1730));
#endif

// call  at /home/jim/toccata/interpreter/intrp-rdr.toc: 806
Term Rslt_48Args = makePair(APP, 0, Rslt_47, SUB);
Term Rslt_48 = newTerm(VAR, 0, portLoc(2, Rslt_48Args));
#ifdef STRICT
pushRedex(Rslt_48Args, seq_10);
#else
swap(termLoc(Rslt_48), makePair(LAZ, 0, Rslt_48Args, seq_10));
#endif

// link args to body
swap(r_seq_6, Rslt_48);

Term Rslt_48V = get(termLoc(Rslt_48));
apps = take(portLoc(1, Rslt_48V));
lams = take(portLoc(2, Rslt_48V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_48), makePair(LAZ, 0, apps, lams));
// call take-char at /home/jim/toccata/interpreter/intrp-rdr.toc: 805
Term Rslt_49Args = makePair(APP, 0, state_1, SUB);
Term Rslt_49 = newTerm(VAR, 0, portLoc(2, Rslt_49Args));
#ifdef STRICT
pushRedex(Rslt_49Args, glblFn1732);
#else
swap(termLoc(Rslt_49), makePair(LAZ, 0, Rslt_49Args, glblFn1732));
#endif

// call skip-whitespace at /home/jim/toccata/interpreter/intrp-rdr.toc: 805
Term Rslt_50Args = makePair(APP, 0, Rslt_49, SUB);
Term Rslt_50 = newTerm(VAR, 0, portLoc(2, Rslt_50Args));
#ifdef STRICT
pushRedex(Rslt_50Args, glblFn1736);
#else
swap(termLoc(Rslt_50), makePair(LAZ, 0, Rslt_50Args, glblFn1736));
#endif

// call  at /home/jim/toccata/interpreter/intrp-rdr.toc: 805
Term Rslt_51Args = makePair(APP, 0, Rslt_50, SUB);
Term Rslt_51 = newTerm(VAR, 0, portLoc(2, Rslt_51Args));
#ifdef STRICT
pushRedex(Rslt_51Args, seq_6);
#else
swap(termLoc(Rslt_51), makePair(LAZ, 0, Rslt_51Args, seq_6));
#endif

// link args to body
swap(r_seq_2, Rslt_51);

Term Rslt_51V = get(termLoc(Rslt_51));
apps = take(portLoc(1, Rslt_51V));
lams = take(portLoc(2, Rslt_51V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_51), makePair(LAZ, 0, apps, lams));
pushRedex(args, seq_2);
return;
}
Term glblFn1873 = newRef(glblCFn1872);
struct {TYPE_SIZE type;
        REFS_SIZE refs;
        int64_t *hash;
        Value *parent;
        int64_t len;
        char buffer[2];
} str51 = {StringBufferType, REFS_STATIC, 0, 0, 1, "("};
Term glblStr2614 = newTerm_(VAL, (Term)&str51);
struct {TYPE_SIZE type;
        REFS_SIZE refs;
        int64_t *hash;
        Value *parent;
        int64_t len;
        char buffer[2];
} str52 = {StringBufferType, REFS_STATIC, 0, 0, 1, "{"};
Term glblStr2623 = newTerm_(VAL, (Term)&str52);
struct {TYPE_SIZE type;
        REFS_SIZE refs;
        int64_t *hash;
        Value *parent;
        int64_t len;
        char buffer[2];
} str53 = {StringBufferType, REFS_STATIC, 0, 0, 1, "'"};
Term glblStr2625 = newTerm_(VAL, (Term)&str53);
struct {TYPE_SIZE type;
        REFS_SIZE refs;
        int64_t *hash;
        Value *parent;
        int64_t len;
        char buffer[21];
} str54 = {StringBufferType, REFS_STATIC, 0, 0, 20, "unexpected character"};
Term glblStr2626 = newTerm_(VAL, (Term)&str54);
void glblProto34_2629(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(827), args);
  args = makePair(APP, 0, glblStr1912, args);
  interact(args, glblProto34);
}
}
void glblProto34_2631(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(827), args);
  args = makePair(APP, 0, glblStr1912, args);
  interact(args, glblProto34);
}
}
void glblProto34_2633(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(827), args);
  args = makePair(APP, 0, glblStr1912, args);
  interact(args, glblProto34);
}
}
void glblProto34_2635(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(827), args);
  args = makePair(APP, 0, glblStr1912, args);
  interact(args, glblProto34);
}
}
void glblProto34_2637(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(827), args);
  args = makePair(APP, 0, glblStr1912, args);
  interact(args, glblProto34);
}
}
void glblProto34_2639(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(827), args);
  args = makePair(APP, 0, glblStr1912, args);
  interact(args, glblProto34);
}
}
void glblProto34_2641(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(827), args);
  args = makePair(APP, 0, glblStr1912, args);
  interact(args, glblProto34);
}
}
void glblProto34_2643(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(827), args);
  args = makePair(APP, 0, glblStr1912, args);
  interact(args, glblProto34);
}
}
Term glblVal1819;
void glblCFn1874(Term ref, Term args) {
Term apps, lams, V;
Location vLoc;
Term seq_2 = makePair(LAM, 0, SUB, NUL);
Location r_seq_2 = portLoc(2, seq_2);
Term state_1 = newTerm(VAR, 0, portLoc(1, seq_2));
// allocate args at /home/jim/toccata/interpreter/intrp-rdr.toc: 823
Term seq_6 = makePair(LAM, 0, SUB, NUL);
Location r_seq_6 = portLoc(2, seq_6);
Term s_5 = newTerm(VAR, 0, portLoc(1, seq_6));
// allocate args at /home/jim/toccata/interpreter/intrp-rdr.toc: 824

Term s_5_9;
s_5 = dupeArg(s_5, &s_5_9, 0);

Term s_5_8;
s_5 = dupeArg(s_5, &s_5_8, 0);

Term s_5_7;
s_5 = dupeArg(s_5, &s_5_7, 0);

Term s_5_6;
s_5 = dupeArg(s_5, &s_5_6, 0);

Term s_5_5;
s_5 = dupeArg(s_5, &s_5_5, 0);

Term s_5_4;
s_5 = dupeArg(s_5, &s_5_4, 0);

Term s_5_3;
s_5 = dupeArg(s_5, &s_5_3, 0);

Term s_5_2;
s_5 = dupeArg(s_5, &s_5_2, 0);

Term s_5_1;
s_5 = dupeArg(s_5, &s_5_1, 0);
Term seq_10 = makePair(LAM, 0, SUB, NUL);
Location r_seq_10 = portLoc(2, seq_10);
Term c_9 = newTerm(VAR, 0, portLoc(1, seq_10));
// allocate args at /home/jim/toccata/interpreter/intrp-rdr.toc: 825

Term c_9_7;
c_9 = dupeArg(c_9, &c_9_7, 0);

Term c_9_6;
c_9 = dupeArg(c_9, &c_9_6, 0);

Term c_9_5;
c_9 = dupeArg(c_9, &c_9_5, 0);

Term c_9_4;
c_9 = dupeArg(c_9, &c_9_4, 0);

Term c_9_3;
c_9 = dupeArg(c_9, &c_9_3, 0);

Term c_9_2;
c_9 = dupeArg(c_9, &c_9_2, 0);

Term c_9_1;
c_9 = dupeArg(c_9, &c_9_1, 0);
Term glblVal1908_7;
glblVal1908 = dupeArg(glblVal1908, &glblVal1908_7, 0);

Term glblVal1908_6;
glblVal1908 = dupeArg(glblVal1908, &glblVal1908_6, 0);

// call str= at /home/jim/toccata/interpreter/intrp-rdr.toc: 827
Term Rslt_12Args = makePair(APP, 0, glblStr402, SUB);
Term Rslt_12 = newTerm(VAR, 0, portLoc(2, Rslt_12Args));
Rslt_12Args = makePair(APP, 0, c_9, Rslt_12Args);
#ifdef STRICT
pushRedex(Rslt_12Args, glblFn363);
#else
swap(termLoc(Rslt_12), makePair(LAZ, 0, Rslt_12Args, glblFn363));
#endif

// call ParserError at /home/jim/toccata/interpreter/intrp-rdr.toc: 828
Term Rslt_13Args = makePair(APP, 0, s_5, SUB);
Term Rslt_13 = newTerm(VAR, 0, portLoc(2, Rslt_13Args));
Rslt_13Args = makePair(APP, 0, glblStr2384, Rslt_13Args);
#ifdef STRICT
pushRedex(Rslt_13Args, glblVal1908_6);
#else
swap(termLoc(Rslt_13), makePair(LAZ, 0, Rslt_13Args, glblVal1908_6));
#endif

// call str= at /home/jim/toccata/interpreter/intrp-rdr.toc: 829
Term Rslt_14Args = makePair(APP, 0, glblStr2614, SUB);
Term Rslt_14 = newTerm(VAR, 0, portLoc(2, Rslt_14Args));
Rslt_14Args = makePair(APP, 0, c_9_1, Rslt_14Args);
#ifdef STRICT
pushRedex(Rslt_14Args, glblFn363);
#else
swap(termLoc(Rslt_14), makePair(LAZ, 0, Rslt_14Args, glblFn363));
#endif

// call take-char at /home/jim/toccata/interpreter/intrp-rdr.toc: 830
Term Rslt_15Args = makePair(APP, 0, s_5_1, SUB);
Term Rslt_15 = newTerm(VAR, 0, portLoc(2, Rslt_15Args));
#ifdef STRICT
pushRedex(Rslt_15Args, glblFn1732);
#else
swap(termLoc(Rslt_15), makePair(LAZ, 0, Rslt_15Args, glblFn1732));
#endif

// call parse-call at /home/jim/toccata/interpreter/intrp-rdr.toc: 830
Term Rslt_16Args = makePair(APP, 0, Rslt_15, SUB);
Term Rslt_16 = newTerm(VAR, 0, portLoc(2, Rslt_16Args));
#ifdef STRICT
pushRedex(Rslt_16Args, glblFn1873);
#else
swap(termLoc(Rslt_16), makePair(LAZ, 0, Rslt_16Args, glblFn1873));
#endif

// call str= at /home/jim/toccata/interpreter/intrp-rdr.toc: 831
Term Rslt_17Args = makePair(APP, 0, glblStr747, SUB);
Term Rslt_17 = newTerm(VAR, 0, portLoc(2, Rslt_17Args));
Rslt_17Args = makePair(APP, 0, c_9_2, Rslt_17Args);
#ifdef STRICT
pushRedex(Rslt_17Args, glblFn363);
#else
swap(termLoc(Rslt_17), makePair(LAZ, 0, Rslt_17Args, glblFn363));
#endif

// call take-char at /home/jim/toccata/interpreter/intrp-rdr.toc: 832
Term Rslt_18Args = makePair(APP, 0, s_5_2, SUB);
Term Rslt_18 = newTerm(VAR, 0, portLoc(2, Rslt_18Args));
#ifdef STRICT
pushRedex(Rslt_18Args, glblFn1732);
#else
swap(termLoc(Rslt_18), makePair(LAZ, 0, Rslt_18Args, glblFn1732));
#endif

// call parse-vector at /home/jim/toccata/interpreter/intrp-rdr.toc: 832
Term Rslt_19Args = makePair(APP, 0, Rslt_18, SUB);
Term Rslt_19 = newTerm(VAR, 0, portLoc(2, Rslt_19Args));
#ifdef STRICT
pushRedex(Rslt_19Args, glblFn1843);
#else
swap(termLoc(Rslt_19), makePair(LAZ, 0, Rslt_19Args, glblFn1843));
#endif

// call str= at /home/jim/toccata/interpreter/intrp-rdr.toc: 833
Term Rslt_20Args = makePair(APP, 0, glblStr2623, SUB);
Term Rslt_20 = newTerm(VAR, 0, portLoc(2, Rslt_20Args));
Rslt_20Args = makePair(APP, 0, c_9_3, Rslt_20Args);
#ifdef STRICT
pushRedex(Rslt_20Args, glblFn363);
#else
swap(termLoc(Rslt_20), makePair(LAZ, 0, Rslt_20Args, glblFn363));
#endif

// call take-char at /home/jim/toccata/interpreter/intrp-rdr.toc: 834
Term Rslt_21Args = makePair(APP, 0, s_5_3, SUB);
Term Rslt_21 = newTerm(VAR, 0, portLoc(2, Rslt_21Args));
#ifdef STRICT
pushRedex(Rslt_21Args, glblFn1732);
#else
swap(termLoc(Rslt_21), makePair(LAZ, 0, Rslt_21Args, glblFn1732));
#endif

// call parse-hash at /home/jim/toccata/interpreter/intrp-rdr.toc: 834
Term Rslt_22Args = makePair(APP, 0, Rslt_21, SUB);
Term Rslt_22 = newTerm(VAR, 0, portLoc(2, Rslt_22Args));
#ifdef STRICT
pushRedex(Rslt_22Args, glblFn1845);
#else
swap(termLoc(Rslt_22), makePair(LAZ, 0, Rslt_22Args, glblFn1845));
#endif

// call str= at /home/jim/toccata/interpreter/intrp-rdr.toc: 835
Term Rslt_23Args = makePair(APP, 0, glblStr2088, SUB);
Term Rslt_23 = newTerm(VAR, 0, portLoc(2, Rslt_23Args));
Rslt_23Args = makePair(APP, 0, c_9_4, Rslt_23Args);
#ifdef STRICT
pushRedex(Rslt_23Args, glblFn363);
#else
swap(termLoc(Rslt_23), makePair(LAZ, 0, Rslt_23Args, glblFn363));
#endif

// call parse-string-lit at /home/jim/toccata/interpreter/intrp-rdr.toc: 836
Term Rslt_24Args = makePair(APP, 0, s_5_4, SUB);
Term Rslt_24 = newTerm(VAR, 0, portLoc(2, Rslt_24Args));
#ifdef STRICT
pushRedex(Rslt_24Args, glblFn1814);
#else
swap(termLoc(Rslt_24), makePair(LAZ, 0, Rslt_24Args, glblFn1814));
#endif

// call str= at /home/jim/toccata/interpreter/intrp-rdr.toc: 837
Term Rslt_25Args = makePair(APP, 0, glblStr2625, SUB);
Term Rslt_25 = newTerm(VAR, 0, portLoc(2, Rslt_25Args));
Rslt_25Args = makePair(APP, 0, c_9_5, Rslt_25Args);
#ifdef STRICT
pushRedex(Rslt_25Args, glblFn363);
#else
swap(termLoc(Rslt_25), makePair(LAZ, 0, Rslt_25Args, glblFn363));
#endif

// call take-char at /home/jim/toccata/interpreter/intrp-rdr.toc: 838
Term Rslt_26Args = makePair(APP, 0, s_5_5, SUB);
Term Rslt_26 = newTerm(VAR, 0, portLoc(2, Rslt_26Args));
#ifdef STRICT
pushRedex(Rslt_26Args, glblFn1732);
#else
swap(termLoc(Rslt_26), makePair(LAZ, 0, Rslt_26Args, glblFn1732));
#endif

// call parse-quoted-sym at /home/jim/toccata/interpreter/intrp-rdr.toc: 838
Term Rslt_27Args = makePair(APP, 0, Rslt_26, SUB);
Term Rslt_27 = newTerm(VAR, 0, portLoc(2, Rslt_27Args));
#ifdef STRICT
pushRedex(Rslt_27Args, glblFn1816);
#else
swap(termLoc(Rslt_27), makePair(LAZ, 0, Rslt_27Args, glblFn1816));
#endif

// call is-digit at /home/jim/toccata/interpreter/intrp-rdr.toc: 839
Term Rslt_28Args = makePair(APP, 0, c_9_6, SUB);
Term Rslt_28 = newTerm(VAR, 0, portLoc(2, Rslt_28Args));
#ifdef STRICT
pushRedex(Rslt_28Args, glblFn1720);
#else
swap(termLoc(Rslt_28), makePair(LAZ, 0, Rslt_28Args, glblFn1720));
#endif

// call parse-number at /home/jim/toccata/interpreter/intrp-rdr.toc: 840
Term Rslt_29Args = makePair(APP, 0, s_5_6, SUB);
Term Rslt_29 = newTerm(VAR, 0, portLoc(2, Rslt_29Args));
#ifdef STRICT
pushRedex(Rslt_29Args, glblFn1812);
#else
swap(termLoc(Rslt_29), makePair(LAZ, 0, Rslt_29Args, glblFn1812));
#endif

// call symbol-start? at /home/jim/toccata/interpreter/intrp-rdr.toc: 841
Term Rslt_30Args = makePair(APP, 0, c_9_7, SUB);
Term Rslt_30 = newTerm(VAR, 0, portLoc(2, Rslt_30Args));
#ifdef STRICT
pushRedex(Rslt_30Args, glblFn1724);
#else
swap(termLoc(Rslt_30), makePair(LAZ, 0, Rslt_30Args, glblFn1724));
#endif

// call parse-symbol at /home/jim/toccata/interpreter/intrp-rdr.toc: 842
Term Rslt_31Args = makePair(APP, 0, s_5_7, SUB);
Term Rslt_31 = newTerm(VAR, 0, portLoc(2, Rslt_31Args));
#ifdef STRICT
pushRedex(Rslt_31Args, glblFn1818);
#else
swap(termLoc(Rslt_31), makePair(LAZ, 0, Rslt_31Args, glblFn1818));
#endif

// call ParserError at /home/jim/toccata/interpreter/intrp-rdr.toc: 843
Term Rslt_32Args = makePair(APP, 0, s_5_8, SUB);
Term Rslt_32 = newTerm(VAR, 0, portLoc(2, Rslt_32Args));
Rslt_32Args = makePair(APP, 0, glblStr2626, Rslt_32Args);
#ifdef STRICT
pushRedex(Rslt_32Args, glblVal1908_7);
#else
swap(termLoc(Rslt_32), makePair(LAZ, 0, Rslt_32Args, glblVal1908_7));
#endif

// call cond at /home/jim/toccata/interpreter/intrp-rdr.toc: 827
Term Rslt_33Args = makePair(APP, 0, Rslt_32, SUB);
Term Rslt_33 = newTerm(VAR, 0, portLoc(2, Rslt_33Args));
Rslt_33Args = makePair(APP, 0, Rslt_31, Rslt_33Args);
Rslt_33Args = makePair(APP, 0, Rslt_30, Rslt_33Args);
#ifdef STRICT
pushRedex(Rslt_33Args, newRef(glblProto34_2629));
#else
swap(termLoc(Rslt_33), makePair(LAZ, 0, Rslt_33Args, newRef(glblProto34_2629)));
#endif
// call cond at /home/jim/toccata/interpreter/intrp-rdr.toc: 827
Term Rslt_34Args = makePair(APP, 0, Rslt_33, SUB);
Term Rslt_34 = newTerm(VAR, 0, portLoc(2, Rslt_34Args));
Rslt_34Args = makePair(APP, 0, Rslt_29, Rslt_34Args);
Rslt_34Args = makePair(APP, 0, Rslt_28, Rslt_34Args);
#ifdef STRICT
pushRedex(Rslt_34Args, newRef(glblProto34_2631));
#else
swap(termLoc(Rslt_34), makePair(LAZ, 0, Rslt_34Args, newRef(glblProto34_2631)));
#endif
// call cond at /home/jim/toccata/interpreter/intrp-rdr.toc: 827
Term Rslt_35Args = makePair(APP, 0, Rslt_34, SUB);
Term Rslt_35 = newTerm(VAR, 0, portLoc(2, Rslt_35Args));
Rslt_35Args = makePair(APP, 0, Rslt_27, Rslt_35Args);
Rslt_35Args = makePair(APP, 0, Rslt_25, Rslt_35Args);
#ifdef STRICT
pushRedex(Rslt_35Args, newRef(glblProto34_2633));
#else
swap(termLoc(Rslt_35), makePair(LAZ, 0, Rslt_35Args, newRef(glblProto34_2633)));
#endif
// call cond at /home/jim/toccata/interpreter/intrp-rdr.toc: 827
Term Rslt_36Args = makePair(APP, 0, Rslt_35, SUB);
Term Rslt_36 = newTerm(VAR, 0, portLoc(2, Rslt_36Args));
Rslt_36Args = makePair(APP, 0, Rslt_24, Rslt_36Args);
Rslt_36Args = makePair(APP, 0, Rslt_23, Rslt_36Args);
#ifdef STRICT
pushRedex(Rslt_36Args, newRef(glblProto34_2635));
#else
swap(termLoc(Rslt_36), makePair(LAZ, 0, Rslt_36Args, newRef(glblProto34_2635)));
#endif
// call cond at /home/jim/toccata/interpreter/intrp-rdr.toc: 827
Term Rslt_37Args = makePair(APP, 0, Rslt_36, SUB);
Term Rslt_37 = newTerm(VAR, 0, portLoc(2, Rslt_37Args));
Rslt_37Args = makePair(APP, 0, Rslt_22, Rslt_37Args);
Rslt_37Args = makePair(APP, 0, Rslt_20, Rslt_37Args);
#ifdef STRICT
pushRedex(Rslt_37Args, newRef(glblProto34_2637));
#else
swap(termLoc(Rslt_37), makePair(LAZ, 0, Rslt_37Args, newRef(glblProto34_2637)));
#endif
// call cond at /home/jim/toccata/interpreter/intrp-rdr.toc: 827
Term Rslt_38Args = makePair(APP, 0, Rslt_37, SUB);
Term Rslt_38 = newTerm(VAR, 0, portLoc(2, Rslt_38Args));
Rslt_38Args = makePair(APP, 0, Rslt_19, Rslt_38Args);
Rslt_38Args = makePair(APP, 0, Rslt_17, Rslt_38Args);
#ifdef STRICT
pushRedex(Rslt_38Args, newRef(glblProto34_2639));
#else
swap(termLoc(Rslt_38), makePair(LAZ, 0, Rslt_38Args, newRef(glblProto34_2639)));
#endif
// call cond at /home/jim/toccata/interpreter/intrp-rdr.toc: 827
Term Rslt_39Args = makePair(APP, 0, Rslt_38, SUB);
Term Rslt_39 = newTerm(VAR, 0, portLoc(2, Rslt_39Args));
Rslt_39Args = makePair(APP, 0, Rslt_16, Rslt_39Args);
Rslt_39Args = makePair(APP, 0, Rslt_14, Rslt_39Args);
#ifdef STRICT
pushRedex(Rslt_39Args, newRef(glblProto34_2641));
#else
swap(termLoc(Rslt_39), makePair(LAZ, 0, Rslt_39Args, newRef(glblProto34_2641)));
#endif
// call cond at /home/jim/toccata/interpreter/intrp-rdr.toc: 827
Term Rslt_40Args = makePair(APP, 0, Rslt_39, SUB);
Term Rslt_40 = newTerm(VAR, 0, portLoc(2, Rslt_40Args));
Rslt_40Args = makePair(APP, 0, Rslt_13, Rslt_40Args);
Rslt_40Args = makePair(APP, 0, Rslt_12, Rslt_40Args);
#ifdef STRICT
pushRedex(Rslt_40Args, newRef(glblProto34_2643));
#else
swap(termLoc(Rslt_40), makePair(LAZ, 0, Rslt_40Args, newRef(glblProto34_2643)));
#endif
// link args to body
swap(r_seq_10, Rslt_40);

Term Rslt_40V = get(termLoc(Rslt_40));
apps = take(portLoc(1, Rslt_40V));
lams = take(portLoc(2, Rslt_40V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_40), makePair(LAZ, 0, apps, lams));
// call peek-char at /home/jim/toccata/interpreter/intrp-rdr.toc: 825
Term Rslt_41Args = makePair(APP, 0, s_5_9, SUB);
Term Rslt_41 = newTerm(VAR, 0, portLoc(2, Rslt_41Args));
#ifdef STRICT
pushRedex(Rslt_41Args, glblFn1730);
#else
swap(termLoc(Rslt_41), makePair(LAZ, 0, Rslt_41Args, glblFn1730));
#endif

// call  at /home/jim/toccata/interpreter/intrp-rdr.toc: 825
Term Rslt_42Args = makePair(APP, 0, Rslt_41, SUB);
Term Rslt_42 = newTerm(VAR, 0, portLoc(2, Rslt_42Args));
#ifdef STRICT
pushRedex(Rslt_42Args, seq_10);
#else
swap(termLoc(Rslt_42), makePair(LAZ, 0, Rslt_42Args, seq_10));
#endif

// link args to body
swap(r_seq_6, Rslt_42);

Term Rslt_42V = get(termLoc(Rslt_42));
apps = take(portLoc(1, Rslt_42V));
lams = take(portLoc(2, Rslt_42V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_42), makePair(LAZ, 0, apps, lams));
// call skip-whitespace at /home/jim/toccata/interpreter/intrp-rdr.toc: 824
Term Rslt_43Args = makePair(APP, 0, state_1, SUB);
Term Rslt_43 = newTerm(VAR, 0, portLoc(2, Rslt_43Args));
#ifdef STRICT
pushRedex(Rslt_43Args, glblFn1736);
#else
swap(termLoc(Rslt_43), makePair(LAZ, 0, Rslt_43Args, glblFn1736));
#endif

// call  at /home/jim/toccata/interpreter/intrp-rdr.toc: 824
Term Rslt_44Args = makePair(APP, 0, Rslt_43, SUB);
Term Rslt_44 = newTerm(VAR, 0, portLoc(2, Rslt_44Args));
#ifdef STRICT
pushRedex(Rslt_44Args, seq_6);
#else
swap(termLoc(Rslt_44), makePair(LAZ, 0, Rslt_44Args, seq_6));
#endif

// link args to body
swap(r_seq_2, Rslt_44);

Term Rslt_44V = get(termLoc(Rslt_44));
apps = take(portLoc(1, Rslt_44V));
lams = take(portLoc(2, Rslt_44V));

vLoc = portLoc(2, apps);
swap(termLoc(Rslt_44), makePair(LAZ, 0, apps, lams));
pushRedex(args, seq_2);
return;
}
Term glblVal1819 = newRef(glblCFn1874);
Term glblProto1468;
Term glblval_kind2628;
void glblval_kind2627(Term ref, Term args) {
Term apps, lams, V;
Location vLoc;
Term seq_5 = makePair(LAM, 0, SUB, NUL);
Location r_seq_5 = portLoc(2, seq_5);
Term arg_4 = newTerm(VAR, 0, portLoc(1, seq_5));
// allocate args at interpreter/xns-probe.toc: 7
swap(termLoc(arg_4), ERA);
// link args to body
swap(r_seq_5, newI60(44));

V = get(r_seq_5);
apps = makePair(APP, 0, V, SUB);
lams = makePair(LAM, 0, SUB, NUL);
swap(portLoc(2, lams),newTerm(VAR, 0, portLoc(1, lams)));
vLoc = portLoc(2, apps);
swap(vLoc, makePair(LAZ, 0, apps, lams));
swap(r_seq_5, newTerm(VAR, 0, vLoc));
pushRedex(args, seq_5);
return;
}
Term glblval_kind2628 = newRef(glblval_kind2627);
struct {TYPE_SIZE type;
        REFS_SIZE refs;
        int64_t *hash;
        Value *parent;
        int64_t len;
        char buffer[4];
} str55 = {StringBufferType, REFS_STATIC, 0, 0, 3, "vk:"};
Term glblStr2645 = newTerm_(VAL, (Term)&str55);
struct {TYPE_SIZE type;
        REFS_SIZE refs;
        int64_t *hash;
        Value *parent;
        int64_t len;
        char buffer[26];
} str56 = {StringBufferType, REFS_STATIC, 0, 0, 25, "interpreter/xns-probe.toc"};
Term glblStr2647 = newTerm_(VAL, (Term)&str56);
void glblProto1468_2651(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(10), args);
  args = makePair(APP, 0, glblStr2647, args);
  interact(args, glblProto1468);
}
}
struct {TYPE_SIZE type;
        REFS_SIZE refs;
        int64_t *hash;
        Value *parent;
        int64_t len;
        char buffer[3];
} str57 = {StringBufferType, REFS_STATIC, 0, 0, 2, "72"};
Term glblStr2650 = newTerm_(VAL, (Term)&str57);
struct {TYPE_SIZE type;
        REFS_SIZE refs;
        int64_t *hash;
        Value *parent;
        int64_t len;
        char buffer[11];
} str58 = {StringBufferType, REFS_STATIC, 0, 0, 10, "sample.toc"};
Term glblStr2649 = newTerm_(VAL, (Term)&str58);
void glblProto1562_2653(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(13), args);
  args = makePair(APP, 0, glblStr2647, args);
  interact(args, glblProto1562);
}
}
void glblProto1468_2655(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 1, &arityArgs);
  if (arityArgs.count == 1) {
  swap(portLoc(1, args), arityArgs.args[0]);
  args = makePair(APP, 0, newI60(14), args);
  args = makePair(APP, 0, glblStr2647, args);
  interact(args, glblProto1468);
}
}
Term glblFn1471;
void glblCFn1470(Term ref, Term args) {
Term apps, lams, V;
Location vLoc;
Term seq_2 = makePair(LAM, 0, SUB, NUL);
Location r_seq_2 = portLoc(2, seq_2);
Term arg_1 = newTerm(VAR, 0, portLoc(1, seq_2));
// allocate args at interpreter/xns-probe.toc: 9
swap(termLoc(arg_1), ERA);
Term glblVal1489_2;
glblVal1489 = dupeArg(glblVal1489, &glblVal1489_2, 0);

Term glblVal1569_2;
glblVal1569 = dupeArg(glblVal1569, &glblVal1569_2, 0);

Term glblVal175_74;
glblVal175 = dupeArg(glblVal175, &glblVal175_74, 0);

// call vect-conj at interpreter/xns-probe.toc: 10
Term Rslt_4Args = makePair(APP, 0, glblStr2645, SUB);
Term Rslt_4 = newTerm(VAR, 0, portLoc(2, Rslt_4Args));
Rslt_4Args = makePair(APP, 0, glblVal175_74, Rslt_4Args);
#ifdef STRICT
pushRedex(Rslt_4Args, glblFn178);
#else
swap(termLoc(Rslt_4), makePair(LAZ, 0, Rslt_4Args, glblFn178));
#endif

// call ast/Location at interpreter/xns-probe.toc: 10
Term Rslt_5Args = makePair(APP, 0, newI60(10), SUB);
Term Rslt_5 = newTerm(VAR, 0, portLoc(2, Rslt_5Args));
Rslt_5Args = makePair(APP, 0, glblStr2647, Rslt_5Args);
#ifdef STRICT
pushRedex(Rslt_5Args, glblVal1489_2);
#else
swap(termLoc(Rslt_5), makePair(LAZ, 0, Rslt_5Args, glblVal1489_2));
#endif

// call ast/IntegerLit at interpreter/xns-probe.toc: 10
Term Rslt_6Args = makePair(APP, 0, Rslt_5, SUB);
Term Rslt_6 = newTerm(VAR, 0, portLoc(2, Rslt_6Args));
Rslt_6Args = makePair(APP, 0, newI60(77), Rslt_6Args);
#ifdef STRICT
pushRedex(Rslt_6Args, glblVal1569_2);
#else
swap(termLoc(Rslt_6), makePair(LAZ, 0, Rslt_6Args, glblVal1569_2));
#endif

// call val-kind at interpreter/xns-probe.toc: 10
Term Rslt_7Args = makePair(APP, 0, Rslt_6, SUB);
Term Rslt_7 = newTerm(VAR, 0, portLoc(2, Rslt_7Args));
#ifdef STRICT
pushRedex(Rslt_7Args, newRef(glblProto1468_2651));
#else
swap(termLoc(Rslt_7), makePair(LAZ, 0, Rslt_7Args, newRef(glblProto1468_2651)));
#endif
// call vect-conj at interpreter/xns-probe.toc: 10
Term Rslt_8Args = makePair(APP, 0, Rslt_7, SUB);
Term Rslt_8 = newTerm(VAR, 0, portLoc(2, Rslt_8Args));
Rslt_8Args = makePair(APP, 0, Rslt_4, Rslt_8Args);
#ifdef STRICT
pushRedex(Rslt_8Args, glblFn178);
#else
swap(termLoc(Rslt_8), makePair(LAZ, 0, Rslt_8Args, glblFn178));
#endif

// call println* at interpreter/xns-probe.toc: 10
Term Rslt_9Args = makePair(APP, 0, Rslt_8, SUB);
Term Rslt_9 = newTerm(VAR, 0, portLoc(2, Rslt_9Args));
#ifdef STRICT
pushRedex(Rslt_9Args, glblFn208);
#else
swap(termLoc(Rslt_9), makePair(LAZ, 0, Rslt_9Args, glblFn208));
#endif

// call rdr/make-state at interpreter/xns-probe.toc: 11
Term Rslt_10Args = makePair(APP, 0, newI60(1), SUB);
Term Rslt_10 = newTerm(VAR, 0, portLoc(2, Rslt_10Args));
Rslt_10Args = makePair(APP, 0, glblStr2649, Rslt_10Args);
Rslt_10Args = makePair(APP, 0, glblStr2650, Rslt_10Args);
#ifdef STRICT
pushRedex(Rslt_10Args, glblFn1716);
#else
swap(termLoc(Rslt_10), makePair(LAZ, 0, Rslt_10Args, glblFn1716));
#endif

// call rdr/parse-expr at interpreter/xns-probe.toc: 12
Term Rslt_11Args = makePair(APP, 0, Rslt_10, SUB);
Term Rslt_11 = newTerm(VAR, 0, portLoc(2, Rslt_11Args));
#ifdef STRICT
pushRedex(Rslt_11Args, glblVal1819);
#else
swap(termLoc(Rslt_11), makePair(LAZ, 0, Rslt_11Args, glblVal1819));
#endif

// call .value at interpreter/xns-probe.toc: 13
Term Rslt_12Args = makePair(APP, 0, Rslt_11, SUB);
Term Rslt_12 = newTerm(VAR, 0, portLoc(2, Rslt_12Args));
#ifdef STRICT
pushRedex(Rslt_12Args, newRef(glblProto1562_2653));
#else
swap(termLoc(Rslt_12), makePair(LAZ, 0, Rslt_12Args, newRef(glblProto1562_2653)));
#endif
// call val-kind at interpreter/xns-probe.toc: 14
Term Rslt_13Args = makePair(APP, 0, Rslt_12, SUB);
Term Rslt_13 = newTerm(VAR, 0, portLoc(2, Rslt_13Args));
#ifdef STRICT
pushRedex(Rslt_13Args, newRef(glblProto1468_2655));
#else
swap(termLoc(Rslt_13), makePair(LAZ, 0, Rslt_13Args, newRef(glblProto1468_2655)));
#endif
// link args to body
swap(r_seq_2, Rslt_13);

Term Rslt_13V = get(termLoc(Rslt_13));
apps = take(portLoc(1, Rslt_13V));
lams = take(portLoc(2, Rslt_13V));

vLoc = portLoc(2, apps);
#ifndef STRICT
{
Term discArgs = makePair(APP, 0, Rslt_9, ERA);
pushRedex(discArgs, discard);
}
#endif
swap(termLoc(Rslt_13), makePair(LAZ, 0, apps, lams));
pushRedex(args, seq_2);
return;
}
Term glblFn1471 = newRef(glblCFn1470);
Term glblProto54;
void glblCFn55(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 3, &arityArgs);
  if (arityArgs.count == 3) {
    String *fileStr = (String *)arityArgs.args[0];
    Value *dispVal = (Value *)arityArgs.args[2];
    swap(termLoc(args), (Term)dispVal);
    if (termTag((Term)dispVal) == I60) {
fprintf(stderr, "No implementation of 'wrap' found for integers called from %.*s: %ld\n", (int)fileStr->len, fileStr->buffer, getI60(arityArgs.args[1]));
BOOM("Dispatch failed");
} else {
switch(dispVal->type) {
case 8:
pushRedex(args, glblwrap706);
break;
default: {
fprintf(stderr, "No implementation of 'wrap' found for type %s (%ld) called from %.*s: %ld\n", typeName(dispVal->type), dispVal->type, (int)fileStr->len, fileStr->buffer, getI60(arityArgs.args[1]));
BOOM("Dispatch failed");
}
}
}
}
return;
}
Term glblProto54 = newRef(glblCFn55);
Term glblProto1670;
void glblFldFn1671(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 3, &arityArgs);
  if (arityArgs.count == 3) {
    String *fileStr = (String *)arityArgs.args[0];
    Value *dispVal = (Value *)arityArgs.args[2];
    swap(termLoc(args), (Term)dispVal);
    if (termTag((Term)dispVal) == I60) {
fprintf(stderr, "No implementation of '.constructors' found for integers called from %.*s: %ld\n", (int)fileStr->len, fileStr->buffer, getI60(arityArgs.args[1]));
BOOM("Dispatch failed");
} else {
switch(dispVal->type) {
case 67:
pushRedex(args, glblFld1676);
break;
default: {
fprintf(stderr, "No implementation of '.constructors' found for type %s (%ld) called from %.*s: %ld\n", typeName(dispVal->type), dispVal->type, (int)fileStr->len, fileStr->buffer, getI60(arityArgs.args[1]));
BOOM("Dispatch failed");
}
}
}
}
return;
}
Term glblProto1670 = newRef(glblFldFn1671);
Term glblProto72;
void glblCFn73(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 3, &arityArgs);
  if (arityArgs.count == 3) {
    String *fileStr = (String *)arityArgs.args[0];
    Value *dispVal = (Value *)arityArgs.args[2];
    swap(termLoc(args), (Term)dispVal);
    if (termTag((Term)dispVal) == I60) {
fprintf(stderr, "No implementation of 'reduce' found for integers called from %.*s: %ld\n", (int)fileStr->len, fileStr->buffer, getI60(arityArgs.args[1]));
BOOM("Dispatch failed");
} else {
switch(dispVal->type) {
case 8:
pushRedex(args, glblreduce712);
break;
default: {
fprintf(stderr, "No implementation of 'reduce' found for type %s (%ld) called from %.*s: %ld\n", typeName(dispVal->type), dispVal->type, (int)fileStr->len, fileStr->buffer, getI60(arityArgs.args[1]));
BOOM("Dispatch failed");
}
}
}
}
return;
}
Term glblProto72 = newRef(glblCFn73);
Term glblProto125;
void glblCFn126(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 3, &arityArgs);
  if (arityArgs.count == 3) {
    String *fileStr = (String *)arityArgs.args[0];
    Value *dispVal = (Value *)arityArgs.args[2];
    swap(termLoc(args), (Term)dispVal);
    if (termTag((Term)dispVal) == I60) {
fprintf(stderr, "No implementation of 'vals' found for integers called from %.*s: %ld\n", (int)fileStr->len, fileStr->buffer, getI60(arityArgs.args[1]));
BOOM("Dispatch failed");
} else {
switch(dispVal->type) {
case 12:
pushRedex(args, glblvals1233);
break;
case 13:
pushRedex(args, glblvals1265);
break;
case 11:
pushRedex(args, glblvals1201);
break;
default: {
fprintf(stderr, "No implementation of 'vals' found for type %s (%ld) called from %.*s: %ld\n", typeName(dispVal->type), dispVal->type, (int)fileStr->len, fileStr->buffer, getI60(arityArgs.args[1]));
BOOM("Dispatch failed");
}
}
}
}
return;
}
Term glblProto125 = newRef(glblCFn126);
Term glblProto66;
void glblCFn67(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 3, &arityArgs);
  if (arityArgs.count == 3) {
    String *fileStr = (String *)arityArgs.args[0];
    Value *dispVal = (Value *)arityArgs.args[2];
    swap(termLoc(args), (Term)dispVal);
    if (termTag((Term)dispVal) == I60) {
fprintf(stderr, "No implementation of 'empty' found for integers called from %.*s: %ld\n", (int)fileStr->len, fileStr->buffer, getI60(arityArgs.args[1]));
BOOM("Dispatch failed");
} else {
switch(dispVal->type) {
case 2:
pushRedex(args, glblempty481);
break;
case 8:
pushRedex(args, glblempty728);
break;
case 44:
pushRedex(args, glblempty478);
break;
default: {
fprintf(stderr, "No implementation of 'empty' found for type %s (%ld) called from %.*s: %ld\n", typeName(dispVal->type), dispVal->type, (int)fileStr->len, fileStr->buffer, getI60(arityArgs.args[1]));
BOOM("Dispatch failed");
}
}
}
}
return;
}
Term glblProto66 = newRef(glblCFn67);
Term glblProto84;
void glblCFn85(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 3, &arityArgs);
  if (arityArgs.count == 3) {
    String *fileStr = (String *)arityArgs.args[0];
    Value *dispVal = (Value *)arityArgs.args[2];
    swap(termLoc(args), (Term)dispVal);
    if (termTag((Term)dispVal) == I60) {
fprintf(stderr, "No implementation of 'split' found for integers called from %.*s: %ld\n", (int)fileStr->len, fileStr->buffer, getI60(arityArgs.args[1]));
BOOM("Dispatch failed");
} else {
switch(dispVal->type) {
default: {
fprintf(stderr, "No implementation of 'split' found for type %s (%ld) called from %.*s: %ld\n", typeName(dispVal->type), dispVal->type, (int)fileStr->len, fileStr->buffer, getI60(arityArgs.args[1]));
BOOM("Dispatch failed");
}
}
}
}
return;
}
Term glblProto84 = newRef(glblCFn85);
Term glblProto92;
void glblCFn93(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 3, &arityArgs);
  if (arityArgs.count == 3) {
    String *fileStr = (String *)arityArgs.args[0];
    Value *dispVal = (Value *)arityArgs.args[2];
    swap(termLoc(args), (Term)dispVal);
    if (termTag((Term)dispVal) == I60) {
fprintf(stderr, "No implementation of 'drop-while' found for integers called from %.*s: %ld\n", (int)fileStr->len, fileStr->buffer, getI60(arityArgs.args[1]));
BOOM("Dispatch failed");
} else {
switch(dispVal->type) {
default: {
fprintf(stderr, "No implementation of 'drop-while' found for type %s (%ld) called from %.*s: %ld\n", typeName(dispVal->type), dispVal->type, (int)fileStr->len, fileStr->buffer, getI60(arityArgs.args[1]));
BOOM("Dispatch failed");
}
}
}
}
return;
}
Term glblProto92 = newRef(glblCFn93);
Term glblProto40;
void glblCFn41(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 3, &arityArgs);
  if (arityArgs.count == 3) {
    String *fileStr = (String *)arityArgs.args[0];
    Value *dispVal = (Value *)arityArgs.args[2];
    swap(termLoc(args), (Term)dispVal);
    if (termTag((Term)dispVal) == I60) {
fprintf(stderr, "No implementation of 'recurse' found for integers called from %.*s: %ld\n", (int)fileStr->len, fileStr->buffer, getI60(arityArgs.args[1]));
BOOM("Dispatch failed");
} else {
switch(dispVal->type) {
case 2:
pushRedex(args, glblrecurse471);
break;
case 46:
pushRedex(args, glblrecurse1369);
break;
case 43:
pushRedex(args, glblrecurse245);
break;
case 44:
pushRedex(args, glblrecurse469);
break;
case 42:
pushRedex(args, glblrecurse191);
break;
case 47:
pushRedex(args, glblrecurse1323);
break;
default: {
fprintf(stderr, "No implementation of 'recurse' found for type %s (%ld) called from %.*s: %ld\n", typeName(dispVal->type), dispVal->type, (int)fileStr->len, fileStr->buffer, getI60(arityArgs.args[1]));
BOOM("Dispatch failed");
}
}
}
}
return;
}
Term glblProto40 = newRef(glblCFn41);
Term glblProto1472;
void glblFldFn1473(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 3, &arityArgs);
  if (arityArgs.count == 3) {
    String *fileStr = (String *)arityArgs.args[0];
    Value *dispVal = (Value *)arityArgs.args[2];
    swap(termLoc(args), (Term)dispVal);
    if (termTag((Term)dispVal) == I60) {
fprintf(stderr, "No implementation of '.file' found for integers called from %.*s: %ld\n", (int)fileStr->len, fileStr->buffer, getI60(arityArgs.args[1]));
BOOM("Dispatch failed");
} else {
switch(dispVal->type) {
case 48:
pushRedex(args, glblFld1483);
break;
default: {
fprintf(stderr, "No implementation of '.file' found for type %s (%ld) called from %.*s: %ld\n", typeName(dispVal->type), dispVal->type, (int)fileStr->len, fileStr->buffer, getI60(arityArgs.args[1]));
BOOM("Dispatch failed");
}
}
}
}
return;
}
Term glblProto1472 = newRef(glblFldFn1473);
Term glblProto382;
void glblFldFn383(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 3, &arityArgs);
  if (arityArgs.count == 3) {
    String *fileStr = (String *)arityArgs.args[0];
    Value *dispVal = (Value *)arityArgs.args[2];
    swap(termLoc(args), (Term)dispVal);
    if (termTag((Term)dispVal) == I60) {
fprintf(stderr, "No implementation of '.s' found for integers called from %.*s: %ld\n", (int)fileStr->len, fileStr->buffer, getI60(arityArgs.args[1]));
BOOM("Dispatch failed");
} else {
switch(dispVal->type) {
case 44:
pushRedex(args, glblFld396);
break;
default: {
fprintf(stderr, "No implementation of '.s' found for type %s (%ld) called from %.*s: %ld\n", typeName(dispVal->type), dispVal->type, (int)fileStr->len, fileStr->buffer, getI60(arityArgs.args[1]));
BOOM("Dispatch failed");
}
}
}
}
return;
}
Term glblProto382 = newRef(glblFldFn383);
Term glblProto1686;
void glblFldFn1687(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 3, &arityArgs);
  if (arityArgs.count == 3) {
    String *fileStr = (String *)arityArgs.args[0];
    Value *dispVal = (Value *)arityArgs.args[2];
    swap(termLoc(args), (Term)dispVal);
    if (termTag((Term)dispVal) == I60) {
fprintf(stderr, "No implementation of '.methods' found for integers called from %.*s: %ld\n", (int)fileStr->len, fileStr->buffer, getI60(arityArgs.args[1]));
BOOM("Dispatch failed");
} else {
switch(dispVal->type) {
case 65:
pushRedex(args, glblFld1692);
break;
default: {
fprintf(stderr, "No implementation of '.methods' found for type %s (%ld) called from %.*s: %ld\n", typeName(dispVal->type), dispVal->type, (int)fileStr->len, fileStr->buffer, getI60(arityArgs.args[1]));
BOOM("Dispatch failed");
}
}
}
}
return;
}
Term glblProto1686 = newRef(glblFldFn1687);
Term glblProto1358;
void glblFldFn1360(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 3, &arityArgs);
  if (arityArgs.count == 3) {
    String *fileStr = (String *)arityArgs.args[0];
    Value *dispVal = (Value *)arityArgs.args[2];
    swap(termLoc(args), (Term)dispVal);
    if (termTag((Term)dispVal) == I60) {
fprintf(stderr, "No implementation of '.head' found for integers called from %.*s: %ld\n", (int)fileStr->len, fileStr->buffer, getI60(arityArgs.args[1]));
BOOM("Dispatch failed");
} else {
switch(dispVal->type) {
case 46:
pushRedex(args, glblFld1379);
break;
default: {
fprintf(stderr, "No implementation of '.head' found for type %s (%ld) called from %.*s: %ld\n", typeName(dispVal->type), dispVal->type, (int)fileStr->len, fileStr->buffer, getI60(arityArgs.args[1]));
BOOM("Dispatch failed");
}
}
}
}
return;
}
Term glblProto1358 = newRef(glblFldFn1360);
Term glblProto1594;
void glblFldFn1595(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 3, &arityArgs);
  if (arityArgs.count == 3) {
    String *fileStr = (String *)arityArgs.args[0];
    Value *dispVal = (Value *)arityArgs.args[2];
    swap(termLoc(args), (Term)dispVal);
    if (termTag((Term)dispVal) == I60) {
fprintf(stderr, "No implementation of '.body' found for integers called from %.*s: %ld\n", (int)fileStr->len, fileStr->buffer, getI60(arityArgs.args[1]));
BOOM("Dispatch failed");
} else {
switch(dispVal->type) {
case 63:
pushRedex(args, glblFld1708);
break;
case 66:
pushRedex(args, glblFld1684);
break;
case 57:
pushRedex(args, glblFld1602);
break;
default: {
fprintf(stderr, "No implementation of '.body' found for type %s (%ld) called from %.*s: %ld\n", typeName(dispVal->type), dispVal->type, (int)fileStr->len, fileStr->buffer, getI60(arityArgs.args[1]));
BOOM("Dispatch failed");
}
}
}
}
return;
}
Term glblProto1594 = newRef(glblFldFn1595);
Term glblProto32;
void glblCFn33(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 3, &arityArgs);
  if (arityArgs.count == 3) {
    String *fileStr = (String *)arityArgs.args[0];
    Value *dispVal = (Value *)arityArgs.args[2];
    swap(termLoc(args), (Term)dispVal);
    if (termTag((Term)dispVal) == I60) {
fprintf(stderr, "No implementation of 'either' found for integers called from %.*s: %ld\n", (int)fileStr->len, fileStr->buffer, getI60(arityArgs.args[1]));
BOOM("Dispatch failed");
} else {
switch(dispVal->type) {
case 43:
pushRedex(args, glbleither243);
break;
case 42:
pushRedex(args, glbleither189);
break;
default: {
fprintf(stderr, "No implementation of 'either' found for type %s (%ld) called from %.*s: %ld\n", typeName(dispVal->type), dispVal->type, (int)fileStr->len, fileStr->buffer, getI60(arityArgs.args[1]));
BOOM("Dispatch failed");
}
}
}
}
return;
}
Term glblProto32 = newRef(glblCFn33);
Term glblProto1876;
void glblFldFn1877(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 3, &arityArgs);
  if (arityArgs.count == 3) {
    String *fileStr = (String *)arityArgs.args[0];
    Value *dispVal = (Value *)arityArgs.args[2];
    swap(termLoc(args), (Term)dispVal);
    if (termTag((Term)dispVal) == I60) {
fprintf(stderr, "No implementation of '.input' found for integers called from %.*s: %ld\n", (int)fileStr->len, fileStr->buffer, getI60(arityArgs.args[1]));
BOOM("Dispatch failed");
} else {
switch(dispVal->type) {
case 70:
pushRedex(args, glblFld1882);
break;
default: {
fprintf(stderr, "No implementation of '.input' found for type %s (%ld) called from %.*s: %ld\n", typeName(dispVal->type), dispVal->type, (int)fileStr->len, fileStr->buffer, getI60(arityArgs.args[1]));
BOOM("Dispatch failed");
}
}
}
}
return;
}
Term glblProto1876 = newRef(glblFldFn1877);
Term glblProto96;
void glblCFn97(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 3, &arityArgs);
  if (arityArgs.count == 3) {
    String *fileStr = (String *)arityArgs.args[0];
    Value *dispVal = (Value *)arityArgs.args[2];
    swap(termLoc(args), (Term)dispVal);
    if (termTag((Term)dispVal) == I60) {
fprintf(stderr, "No implementation of 'reverse' found for integers called from %.*s: %ld\n", (int)fileStr->len, fileStr->buffer, getI60(arityArgs.args[1]));
BOOM("Dispatch failed");
} else {
switch(dispVal->type) {
case 8:
pushRedex(args, glblreverse710);
break;
default: {
fprintf(stderr, "No implementation of 'reverse' found for type %s (%ld) called from %.*s: %ld\n", typeName(dispVal->type), dispVal->type, (int)fileStr->len, fileStr->buffer, getI60(arityArgs.args[1]));
BOOM("Dispatch failed");
}
}
}
}
return;
}
Term glblProto96 = newRef(glblCFn97);
Term glblProto20;
void glblCFn21(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 3, &arityArgs);
  if (arityArgs.count == 3) {
    String *fileStr = (String *)arityArgs.args[0];
    Value *dispVal = (Value *)arityArgs.args[2];
    swap(termLoc(args), (Term)dispVal);
    if (termTag((Term)dispVal) == I60) {
pushRedex(args, glbl_EQ_315);
} else {
switch(dispVal->type) {
case 2:
pushRedex(args, glbl_EQ_463);
break;
case 43:
pushRedex(args, glbl_EQ_249);
break;
case 8:
pushRedex(args, glbl_EQ_742);
break;
case 12:
pushRedex(args, glbl_EQ_1305);
break;
case 44:
pushRedex(args, glbl_EQ_461);
break;
case 13:
pushRedex(args, glbl_EQ_1307);
break;
case 45:
pushRedex(args, glbl_EQ_1177);
break;
case 42:
pushRedex(args, glbl_EQ_193);
break;
case 11:
pushRedex(args, glbl_EQ_1303);
break;
default: {
fprintf(stderr, "No implementation of '=' found for type %s (%ld) called from %.*s: %ld\n", typeName(dispVal->type), dispVal->type, (int)fileStr->len, fileStr->buffer, getI60(arityArgs.args[1]));
BOOM("Dispatch failed");
}
}
}
}
return;
}
Term glblProto20 = newRef(glblCFn21);
Term glblProto1622;
void glblFldFn1623(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 3, &arityArgs);
  if (arityArgs.count == 3) {
    String *fileStr = (String *)arityArgs.args[0];
    Value *dispVal = (Value *)arityArgs.args[2];
    swap(termLoc(args), (Term)dispVal);
    if (termTag((Term)dispVal) == I60) {
fprintf(stderr, "No implementation of '.field-name' found for integers called from %.*s: %ld\n", (int)fileStr->len, fileStr->buffer, getI60(arityArgs.args[1]));
BOOM("Dispatch failed");
} else {
switch(dispVal->type) {
case 54:
pushRedex(args, glblFld1626);
break;
default: {
fprintf(stderr, "No implementation of '.field-name' found for type %s (%ld) called from %.*s: %ld\n", typeName(dispVal->type), dispVal->type, (int)fileStr->len, fileStr->buffer, getI60(arityArgs.args[1]));
BOOM("Dispatch failed");
}
}
}
}
return;
}
Term glblProto1622 = newRef(glblFldFn1623);
Term glblProto1545;
void glblFldFn1546(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 3, &arityArgs);
  if (arityArgs.count == 3) {
    String *fileStr = (String *)arityArgs.args[0];
    Value *dispVal = (Value *)arityArgs.args[2];
    swap(termLoc(args), (Term)dispVal);
    if (termTag((Term)dispVal) == I60) {
fprintf(stderr, "No implementation of '.ns' found for integers called from %.*s: %ld\n", (int)fileStr->len, fileStr->buffer, getI60(arityArgs.args[1]));
BOOM("Dispatch failed");
} else {
switch(dispVal->type) {
case 62:
pushRedex(args, glblFld1554);
break;
default: {
fprintf(stderr, "No implementation of '.ns' found for type %s (%ld) called from %.*s: %ld\n", typeName(dispVal->type), dispVal->type, (int)fileStr->len, fileStr->buffer, getI60(arityArgs.args[1]));
BOOM("Dispatch failed");
}
}
}
}
return;
}
Term glblProto1545 = newRef(glblFldFn1546);
Term glblProto98;
void glblCFn99(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 3, &arityArgs);
  if (arityArgs.count == 3) {
    String *fileStr = (String *)arityArgs.args[0];
    Value *dispVal = (Value *)arityArgs.args[2];
    swap(termLoc(args), (Term)dispVal);
    if (termTag((Term)dispVal) == I60) {
fprintf(stderr, "No implementation of 'nth' found for integers called from %.*s: %ld\n", (int)fileStr->len, fileStr->buffer, getI60(arityArgs.args[1]));
BOOM("Dispatch failed");
} else {
switch(dispVal->type) {
case 2:
pushRedex(args, glblnth435);
break;
case 8:
pushRedex(args, glblnth732);
break;
case 44:
pushRedex(args, glblnth433);
break;
default: {
fprintf(stderr, "No implementation of 'nth' found for type %s (%ld) called from %.*s: %ld\n", typeName(dispVal->type), dispVal->type, (int)fileStr->len, fileStr->buffer, getI60(arityArgs.args[1]));
BOOM("Dispatch failed");
}
}
}
}
return;
}
Term glblProto98 = newRef(glblCFn99);
Term glblProto111;
void glblCFn112(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 3, &arityArgs);
  if (arityArgs.count == 3) {
    String *fileStr = (String *)arityArgs.args[0];
    Value *dispVal = (Value *)arityArgs.args[2];
    swap(termLoc(args), (Term)dispVal);
    if (termTag((Term)dispVal) == I60) {
fprintf(stderr, "No implementation of 'assoc*' found for integers called from %.*s: %ld\n", (int)fileStr->len, fileStr->buffer, getI60(arityArgs.args[1]));
BOOM("Dispatch failed");
} else {
switch(dispVal->type) {
case 12:
pushRedex(args, glblassoc_STAR_1229);
break;
case 13:
pushRedex(args, glblassoc_STAR_1261);
break;
case 11:
pushRedex(args, glblassoc_STAR_1197);
break;
default: {
fprintf(stderr, "No implementation of 'assoc*' found for type %s (%ld) called from %.*s: %ld\n", typeName(dispVal->type), dispVal->type, (int)fileStr->len, fileStr->buffer, getI60(arityArgs.args[1]));
BOOM("Dispatch failed");
}
}
}
}
return;
}
Term glblProto111 = newRef(glblCFn112);
Term glblProto1632;
void glblFldFn1633(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 3, &arityArgs);
  if (arityArgs.count == 3) {
    String *fileStr = (String *)arityArgs.args[0];
    Value *dispVal = (Value *)arityArgs.args[2];
    swap(termLoc(args), (Term)dispVal);
    if (termTag((Term)dispVal) == I60) {
fprintf(stderr, "No implementation of '.cases' found for integers called from %.*s: %ld\n", (int)fileStr->len, fileStr->buffer, getI60(arityArgs.args[1]));
BOOM("Dispatch failed");
} else {
switch(dispVal->type) {
case 53:
pushRedex(args, glblFld1640);
break;
default: {
fprintf(stderr, "No implementation of '.cases' found for type %s (%ld) called from %.*s: %ld\n", typeName(dispVal->type), dispVal->type, (int)fileStr->len, fileStr->buffer, getI60(arityArgs.args[1]));
BOOM("Dispatch failed");
}
}
}
}
return;
}
Term glblProto1632 = newRef(glblFldFn1633);
Term glblProto6;
void glblCFn7(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 3, &arityArgs);
  if (arityArgs.count == 3) {
    String *fileStr = (String *)arityArgs.args[0];
    Value *dispVal = (Value *)arityArgs.args[2];
    swap(termLoc(args), (Term)dispVal);
    if (termTag((Term)dispVal) == I60) {
pushRedex(args, glbltype_name319);
} else {
switch(dispVal->type) {
case 2:
pushRedex(args, glbltype_name485);
break;
case 8:
pushRedex(args, glbltype_name740);
break;
case 44:
pushRedex(args, glbltype_name483);
break;
default: {
pushRedex(args, glbltype_name9);
}
}
}
}
return;
}
Term glblProto6 = newRef(glblCFn7);
Term glblProto1515;
void glblFldFn1517(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 3, &arityArgs);
  if (arityArgs.count == 3) {
    String *fileStr = (String *)arityArgs.args[0];
    Value *dispVal = (Value *)arityArgs.args[2];
    swap(termLoc(args), (Term)dispVal);
    if (termTag((Term)dispVal) == I60) {
fprintf(stderr, "No implementation of '.implementations' found for integers called from %.*s: %ld\n", (int)fileStr->len, fileStr->buffer, getI60(arityArgs.args[1]));
BOOM("Dispatch failed");
} else {
switch(dispVal->type) {
case 50:
pushRedex(args, glblFld1529);
break;
default: {
fprintf(stderr, "No implementation of '.implementations' found for type %s (%ld) called from %.*s: %ld\n", typeName(dispVal->type), dispVal->type, (int)fileStr->len, fileStr->buffer, getI60(arityArgs.args[1]));
BOOM("Dispatch failed");
}
}
}
}
return;
}
Term glblProto1515 = newRef(glblFldFn1517);
Term glblProto102;
void glblCFn103(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 3, &arityArgs);
  if (arityArgs.count == 3) {
    String *fileStr = (String *)arityArgs.args[0];
    Value *dispVal = (Value *)arityArgs.args[2];
    swap(termLoc(args), (Term)dispVal);
    if (termTag((Term)dispVal) == I60) {
fprintf(stderr, "No implementation of 'sha1-update' found for integers called from %.*s: %ld\n", (int)fileStr->len, fileStr->buffer, getI60(arityArgs.args[1]));
BOOM("Dispatch failed");
} else {
switch(dispVal->type) {
default: {
fprintf(stderr, "No implementation of 'sha1-update' found for type %s (%ld) called from %.*s: %ld\n", typeName(dispVal->type), dispVal->type, (int)fileStr->len, fileStr->buffer, getI60(arityArgs.args[1]));
BOOM("Dispatch failed");
}
}
}
}
return;
}
Term glblProto102 = newRef(glblCFn103);
Term glblProto1634;
void glblFldFn1635(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 3, &arityArgs);
  if (arityArgs.count == 3) {
    String *fileStr = (String *)arityArgs.args[0];
    Value *dispVal = (Value *)arityArgs.args[2];
    swap(termLoc(args), (Term)dispVal);
    if (termTag((Term)dispVal) == I60) {
fprintf(stderr, "No implementation of '.default-case' found for integers called from %.*s: %ld\n", (int)fileStr->len, fileStr->buffer, getI60(arityArgs.args[1]));
BOOM("Dispatch failed");
} else {
switch(dispVal->type) {
case 53:
pushRedex(args, glblFld1642);
break;
default: {
fprintf(stderr, "No implementation of '.default-case' found for type %s (%ld) called from %.*s: %ld\n", typeName(dispVal->type), dispVal->type, (int)fileStr->len, fileStr->buffer, getI60(arityArgs.args[1]));
BOOM("Dispatch failed");
}
}
}
}
return;
}
Term glblProto1634 = newRef(glblFldFn1635);
Term glblProto28;
void glblCFn29(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 3, &arityArgs);
  if (arityArgs.count == 3) {
    String *fileStr = (String *)arityArgs.args[0];
    Value *dispVal = (Value *)arityArgs.args[2];
    swap(termLoc(args), (Term)dispVal);
    if (termTag((Term)dispVal) == I60) {
fprintf(stderr, "No implementation of 'and' found for integers called from %.*s: %ld\n", (int)fileStr->len, fileStr->buffer, getI60(arityArgs.args[1]));
BOOM("Dispatch failed");
} else {
switch(dispVal->type) {
case 43:
pushRedex(args, glbland239);
break;
case 42:
pushRedex(args, glbland185);
break;
default: {
fprintf(stderr, "No implementation of 'and' found for type %s (%ld) called from %.*s: %ld\n", typeName(dispVal->type), dispVal->type, (int)fileStr->len, fileStr->buffer, getI60(arityArgs.args[1]));
BOOM("Dispatch failed");
}
}
}
}
return;
}
Term glblProto28 = newRef(glblCFn29);
Term glblProto1476;
void glblFldFn1478(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 3, &arityArgs);
  if (arityArgs.count == 3) {
    String *fileStr = (String *)arityArgs.args[0];
    Value *dispVal = (Value *)arityArgs.args[2];
    swap(termLoc(args), (Term)dispVal);
    if (termTag((Term)dispVal) == I60) {
fprintf(stderr, "No implementation of '.line' found for integers called from %.*s: %ld\n", (int)fileStr->len, fileStr->buffer, getI60(arityArgs.args[1]));
BOOM("Dispatch failed");
} else {
switch(dispVal->type) {
case 48:
pushRedex(args, glblFld1486);
break;
default: {
fprintf(stderr, "No implementation of '.line' found for type %s (%ld) called from %.*s: %ld\n", typeName(dispVal->type), dispVal->type, (int)fileStr->len, fileStr->buffer, getI60(arityArgs.args[1]));
BOOM("Dispatch failed");
}
}
}
}
return;
}
Term glblProto1476 = newRef(glblFldFn1478);
Term glblProto1694;
void glblFldFn1695(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 3, &arityArgs);
  if (arityArgs.count == 3) {
    String *fileStr = (String *)arityArgs.args[0];
    Value *dispVal = (Value *)arityArgs.args[2];
    swap(termLoc(args), (Term)dispVal);
    if (termTag((Term)dispVal) == I60) {
fprintf(stderr, "No implementation of '.namespace' found for integers called from %.*s: %ld\n", (int)fileStr->len, fileStr->buffer, getI60(arityArgs.args[1]));
BOOM("Dispatch failed");
} else {
switch(dispVal->type) {
case 64:
pushRedex(args, glblFld1700);
break;
default: {
fprintf(stderr, "No implementation of '.namespace' found for type %s (%ld) called from %.*s: %ld\n", typeName(dispVal->type), dispVal->type, (int)fileStr->len, fileStr->buffer, getI60(arityArgs.args[1]));
BOOM("Dispatch failed");
}
}
}
}
return;
}
Term glblProto1694 = newRef(glblFldFn1695);
Term glblProto380;
void glblCFn381(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 3, &arityArgs);
  if (arityArgs.count == 3) {
    String *fileStr = (String *)arityArgs.args[0];
    Value *dispVal = (Value *)arityArgs.args[2];
    swap(termLoc(args), (Term)dispVal);
    if (termTag((Term)dispVal) == I60) {
fprintf(stderr, "No implementation of 'subs' found for integers called from %.*s: %ld\n", (int)fileStr->len, fileStr->buffer, getI60(arityArgs.args[1]));
BOOM("Dispatch failed");
} else {
switch(dispVal->type) {
case 2:
pushRedex(args, glblsubs409);
break;
case 44:
pushRedex(args, glblsubs393);
break;
default: {
fprintf(stderr, "No implementation of 'subs' found for type %s (%ld) called from %.*s: %ld\n", typeName(dispVal->type), dispVal->type, (int)fileStr->len, fileStr->buffer, getI60(arityArgs.args[1]));
BOOM("Dispatch failed");
}
}
}
}
return;
}
Term glblProto380 = newRef(glblCFn381);
Term glblProto1362;
void glblFldFn1363(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 3, &arityArgs);
  if (arityArgs.count == 3) {
    String *fileStr = (String *)arityArgs.args[0];
    Value *dispVal = (Value *)arityArgs.args[2];
    swap(termLoc(args), (Term)dispVal);
    if (termTag((Term)dispVal) == I60) {
fprintf(stderr, "No implementation of '.tail' found for integers called from %.*s: %ld\n", (int)fileStr->len, fileStr->buffer, getI60(arityArgs.args[1]));
BOOM("Dispatch failed");
} else {
switch(dispVal->type) {
case 46:
pushRedex(args, glblFld1382);
break;
default: {
fprintf(stderr, "No implementation of '.tail' found for type %s (%ld) called from %.*s: %ld\n", typeName(dispVal->type), dispVal->type, (int)fileStr->len, fileStr->buffer, getI60(arityArgs.args[1]));
BOOM("Dispatch failed");
}
}
}
}
return;
}
Term glblProto1362 = newRef(glblFldFn1363);
Term glblProto94;
void glblCFn95(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 3, &arityArgs);
  if (arityArgs.count == 3) {
    String *fileStr = (String *)arityArgs.args[0];
    Value *dispVal = (Value *)arityArgs.args[2];
    swap(termLoc(args), (Term)dispVal);
    if (termTag((Term)dispVal) == I60) {
fprintf(stderr, "No implementation of 'take-while' found for integers called from %.*s: %ld\n", (int)fileStr->len, fileStr->buffer, getI60(arityArgs.args[1]));
BOOM("Dispatch failed");
} else {
switch(dispVal->type) {
default: {
fprintf(stderr, "No implementation of 'take-while' found for type %s (%ld) called from %.*s: %ld\n", typeName(dispVal->type), dispVal->type, (int)fileStr->len, fileStr->buffer, getI60(arityArgs.args[1]));
BOOM("Dispatch failed");
}
}
}
}
return;
}
Term glblProto94 = newRef(glblCFn95);
Term glblProto498;
void glblCFn499(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 3, &arityArgs);
  if (arityArgs.count == 3) {
    String *fileStr = (String *)arityArgs.args[0];
    Value *dispVal = (Value *)arityArgs.args[2];
    swap(termLoc(args), (Term)dispVal);
    if (termTag((Term)dispVal) == I60) {
fprintf(stderr, "No implementation of 'vect-reverse' found for integers called from %.*s: %ld\n", (int)fileStr->len, fileStr->buffer, getI60(arityArgs.args[1]));
BOOM("Dispatch failed");
} else {
switch(dispVal->type) {
case 43:
pushRedex(args, glblvect_reverse619);
break;
case 42:
pushRedex(args, glblvect_reverse616);
break;
default: {
fprintf(stderr, "No implementation of 'vect-reverse' found for type %s (%ld) called from %.*s: %ld\n", typeName(dispVal->type), dispVal->type, (int)fileStr->len, fileStr->buffer, getI60(arityArgs.args[1]));
BOOM("Dispatch failed");
}
}
}
}
return;
}
Term glblProto498 = newRef(glblCFn499);
Term glblProto1584;
void glblFldFn1585(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 3, &arityArgs);
  if (arityArgs.count == 3) {
    String *fileStr = (String *)arityArgs.args[0];
    Value *dispVal = (Value *)arityArgs.args[2];
    swap(termLoc(args), (Term)dispVal);
    if (termTag((Term)dispVal) == I60) {
fprintf(stderr, "No implementation of '.operands' found for integers called from %.*s: %ld\n", (int)fileStr->len, fileStr->buffer, getI60(arityArgs.args[1]));
BOOM("Dispatch failed");
} else {
switch(dispVal->type) {
case 58:
pushRedex(args, glblFld1590);
break;
default: {
fprintf(stderr, "No implementation of '.operands' found for type %s (%ld) called from %.*s: %ld\n", typeName(dispVal->type), dispVal->type, (int)fileStr->len, fileStr->buffer, getI60(arityArgs.args[1]));
BOOM("Dispatch failed");
}
}
}
}
return;
}
Term glblProto1584 = newRef(glblFldFn1585);
Term glblProto1630;
void glblFldFn1631(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 3, &arityArgs);
  if (arityArgs.count == 3) {
    String *fileStr = (String *)arityArgs.args[0];
    Value *dispVal = (Value *)arityArgs.args[2];
    swap(termLoc(args), (Term)dispVal);
    if (termTag((Term)dispVal) == I60) {
fprintf(stderr, "No implementation of '.expr' found for integers called from %.*s: %ld\n", (int)fileStr->len, fileStr->buffer, getI60(arityArgs.args[1]));
BOOM("Dispatch failed");
} else {
switch(dispVal->type) {
case 53:
pushRedex(args, glblFld1638);
break;
default: {
fprintf(stderr, "No implementation of '.expr' found for type %s (%ld) called from %.*s: %ld\n", typeName(dispVal->type), dispVal->type, (int)fileStr->len, fileStr->buffer, getI60(arityArgs.args[1]));
BOOM("Dispatch failed");
}
}
}
}
return;
}
Term glblProto1630 = newRef(glblFldFn1631);
Term glblProto1548;
void glblFldFn1549(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 3, &arityArgs);
  if (arityArgs.count == 3) {
    String *fileStr = (String *)arityArgs.args[0];
    Value *dispVal = (Value *)arityArgs.args[2];
    swap(termLoc(args), (Term)dispVal);
    if (termTag((Term)dispVal) == I60) {
fprintf(stderr, "No implementation of '.loc' found for integers called from %.*s: %ld\n", (int)fileStr->len, fileStr->buffer, getI60(arityArgs.args[1]));
BOOM("Dispatch failed");
} else {
switch(dispVal->type) {
case 61:
pushRedex(args, glblFld1568);
break;
case 63:
pushRedex(args, glblFld1710);
break;
case 54:
pushRedex(args, glblFld1628);
break;
case 59:
pushRedex(args, glblFld1580);
break;
case 62:
pushRedex(args, glblFld1559);
break;
case 68:
pushRedex(args, glblFld1666);
break;
case 60:
pushRedex(args, glblFld1574);
break;
case 51:
pushRedex(args, glblFld1658);
break;
case 57:
pushRedex(args, glblFld1604);
break;
default: {
fprintf(stderr, "No implementation of '.loc' found for type %s (%ld) called from %.*s: %ld\n", typeName(dispVal->type), dispVal->type, (int)fileStr->len, fileStr->buffer, getI60(arityArgs.args[1]));
BOOM("Dispatch failed");
}
}
}
}
return;
}
Term glblProto1548 = newRef(glblFldFn1549);
Term glblProto1668;
void glblFldFn1669(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 3, &arityArgs);
  if (arityArgs.count == 3) {
    String *fileStr = (String *)arityArgs.args[0];
    Value *dispVal = (Value *)arityArgs.args[2];
    swap(termLoc(args), (Term)dispVal);
    if (termTag((Term)dispVal) == I60) {
fprintf(stderr, "No implementation of '.type-name' found for integers called from %.*s: %ld\n", (int)fileStr->len, fileStr->buffer, getI60(arityArgs.args[1]));
BOOM("Dispatch failed");
} else {
switch(dispVal->type) {
case 65:
pushRedex(args, glblFld1690);
break;
case 67:
pushRedex(args, glblFld1674);
break;
default: {
fprintf(stderr, "No implementation of '.type-name' found for type %s (%ld) called from %.*s: %ld\n", typeName(dispVal->type), dispVal->type, (int)fileStr->len, fileStr->buffer, getI60(arityArgs.args[1]));
BOOM("Dispatch failed");
}
}
}
}
return;
}
Term glblProto1668 = newRef(glblFldFn1669);
Term glblProto48;
void glblCFn49(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 3, &arityArgs);
  if (arityArgs.count == 3) {
    String *fileStr = (String *)arityArgs.args[0];
    Value *dispVal = (Value *)arityArgs.args[2];
    swap(termLoc(args), (Term)dispVal);
    if (termTag((Term)dispVal) == I60) {
fprintf(stderr, "No implementation of 'flatten' found for integers called from %.*s: %ld\n", (int)fileStr->len, fileStr->buffer, getI60(arityArgs.args[1]));
BOOM("Dispatch failed");
} else {
switch(dispVal->type) {
case 8:
pushRedex(args, glblflatten702);
break;
default: {
fprintf(stderr, "No implementation of 'flatten' found for type %s (%ld) called from %.*s: %ld\n", typeName(dispVal->type), dispVal->type, (int)fileStr->len, fileStr->buffer, getI60(arityArgs.args[1]));
BOOM("Dispatch failed");
}
}
}
}
return;
}
Term glblProto48 = newRef(glblCFn49);
Term glblProto26;
void glblCFn27(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 3, &arityArgs);
  if (arityArgs.count == 3) {
    String *fileStr = (String *)arityArgs.args[0];
    Value *dispVal = (Value *)arityArgs.args[2];
    swap(termLoc(args), (Term)dispVal);
    if (termTag((Term)dispVal) == I60) {
pushRedex(args, glbl_LT_313);
} else {
switch(dispVal->type) {
case 2:
pushRedex(args, glbl_LT_459);
break;
case 44:
pushRedex(args, glbl_LT_457);
break;
default: {
fprintf(stderr, "No implementation of '<' found for type %s (%ld) called from %.*s: %ld\n", typeName(dispVal->type), dispVal->type, (int)fileStr->len, fileStr->buffer, getI60(arityArgs.args[1]));
BOOM("Dispatch failed");
}
}
}
}
return;
}
Term glblProto26 = newRef(glblCFn27);
Term glblProto121;
void glblCFn122(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 3, &arityArgs);
  if (arityArgs.count == 3) {
    String *fileStr = (String *)arityArgs.args[0];
    Value *dispVal = (Value *)arityArgs.args[2];
    swap(termLoc(args), (Term)dispVal);
    if (termTag((Term)dispVal) == I60) {
fprintf(stderr, "No implementation of 'hash-vec' found for integers called from %.*s: %ld\n", (int)fileStr->len, fileStr->buffer, getI60(arityArgs.args[1]));
BOOM("Dispatch failed");
} else {
switch(dispVal->type) {
case 12:
pushRedex(args, glblhash_vec1235);
break;
case 13:
pushRedex(args, glblhash_vec1267);
break;
case 11:
pushRedex(args, glblhash_vec1203);
break;
default: {
fprintf(stderr, "No implementation of 'hash-vec' found for type %s (%ld) called from %.*s: %ld\n", typeName(dispVal->type), dispVal->type, (int)fileStr->len, fileStr->buffer, getI60(arityArgs.args[1]));
BOOM("Dispatch failed");
}
}
}
}
return;
}
Term glblProto121 = newRef(glblCFn122);
Term glblProto90;
void glblCFn91(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 3, &arityArgs);
  if (arityArgs.count == 3) {
    String *fileStr = (String *)arityArgs.args[0];
    Value *dispVal = (Value *)arityArgs.args[2];
    swap(termLoc(args), (Term)dispVal);
    if (termTag((Term)dispVal) == I60) {
fprintf(stderr, "No implementation of 'drop' found for integers called from %.*s: %ld\n", (int)fileStr->len, fileStr->buffer, getI60(arityArgs.args[1]));
BOOM("Dispatch failed");
} else {
switch(dispVal->type) {
default: {
fprintf(stderr, "No implementation of 'drop' found for type %s (%ld) called from %.*s: %ld\n", typeName(dispVal->type), dispVal->type, (int)fileStr->len, fileStr->buffer, getI60(arityArgs.args[1]));
BOOM("Dispatch failed");
}
}
}
}
return;
}
Term glblProto90 = newRef(glblCFn91);
Term glblProto42;
void glblCFn43(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 3, &arityArgs);
  if (arityArgs.count == 3) {
    String *fileStr = (String *)arityArgs.args[0];
    Value *dispVal = (Value *)arityArgs.args[2];
    swap(termLoc(args), (Term)dispVal);
    if (termTag((Term)dispVal) == I60) {
fprintf(stderr, "No implementation of 'map' found for integers called from %.*s: %ld\n", (int)fileStr->len, fileStr->buffer, getI60(arityArgs.args[1]));
BOOM("Dispatch failed");
} else {
switch(dispVal->type) {
case 43:
pushRedex(args, glblmap235);
break;
case 8:
pushRedex(args, glblmap708);
break;
case 42:
pushRedex(args, glblmap181);
break;
default: {
fprintf(stderr, "No implementation of 'map' found for type %s (%ld) called from %.*s: %ld\n", typeName(dispVal->type), dispVal->type, (int)fileStr->len, fileStr->buffer, getI60(arityArgs.args[1]));
BOOM("Dispatch failed");
}
}
}
}
return;
}
Term glblProto42 = newRef(glblCFn43);
Term glblProto115;
void glblCFn116(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 3, &arityArgs);
  if (arityArgs.count == 3) {
    String *fileStr = (String *)arityArgs.args[0];
    Value *dispVal = (Value *)arityArgs.args[2];
    swap(termLoc(args), (Term)dispVal);
    if (termTag((Term)dispVal) == I60) {
fprintf(stderr, "No implementation of 'dissoc*' found for integers called from %.*s: %ld\n", (int)fileStr->len, fileStr->buffer, getI60(arityArgs.args[1]));
BOOM("Dispatch failed");
} else {
switch(dispVal->type) {
case 12:
pushRedex(args, glbldissoc_STAR_1225);
break;
case 13:
pushRedex(args, glbldissoc_STAR_1257);
break;
case 11:
pushRedex(args, glbldissoc_STAR_1193);
break;
default: {
fprintf(stderr, "No implementation of 'dissoc*' found for type %s (%ld) called from %.*s: %ld\n", typeName(dispVal->type), dispVal->type, (int)fileStr->len, fileStr->buffer, getI60(arityArgs.args[1]));
BOOM("Dispatch failed");
}
}
}
}
return;
}
Term glblProto115 = newRef(glblCFn116);
Term glblProto1901;
void glblFldFn1902(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 3, &arityArgs);
  if (arityArgs.count == 3) {
    String *fileStr = (String *)arityArgs.args[0];
    Value *dispVal = (Value *)arityArgs.args[2];
    swap(termLoc(args), (Term)dispVal);
    if (termTag((Term)dispVal) == I60) {
fprintf(stderr, "No implementation of '.msg' found for integers called from %.*s: %ld\n", (int)fileStr->len, fileStr->buffer, getI60(arityArgs.args[1]));
BOOM("Dispatch failed");
} else {
switch(dispVal->type) {
case 71:
pushRedex(args, glblFld1905);
break;
default: {
fprintf(stderr, "No implementation of '.msg' found for type %s (%ld) called from %.*s: %ld\n", typeName(dispVal->type), dispVal->type, (int)fileStr->len, fileStr->buffer, getI60(arityArgs.args[1]));
BOOM("Dispatch failed");
}
}
}
}
return;
}
Term glblProto1901 = newRef(glblFldFn1902);
Term glblProto44;
void glblCFn45(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 3, &arityArgs);
  if (arityArgs.count == 3) {
    String *fileStr = (String *)arityArgs.args[0];
    Value *dispVal = (Value *)arityArgs.args[2];
    swap(termLoc(args), (Term)dispVal);
    if (termTag((Term)dispVal) == I60) {
fprintf(stderr, "No implementation of 'map!' found for integers called from %.*s: %ld\n", (int)fileStr->len, fileStr->buffer, getI60(arityArgs.args[1]));
BOOM("Dispatch failed");
} else {
switch(dispVal->type) {
default: {
fprintf(stderr, "No implementation of 'map!' found for type %s (%ld) called from %.*s: %ld\n", typeName(dispVal->type), dispVal->type, (int)fileStr->len, fileStr->buffer, getI60(arityArgs.args[1]));
BOOM("Dispatch failed");
}
}
}
}
return;
}
Term glblProto44 = newRef(glblCFn45);
Term glblProto68;
void glblCFn69(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 3, &arityArgs);
  if (arityArgs.count == 3) {
    String *fileStr = (String *)arityArgs.args[0];
    Value *dispVal = (Value *)arityArgs.args[2];
    swap(termLoc(args), (Term)dispVal);
    if (termTag((Term)dispVal) == I60) {
fprintf(stderr, "No implementation of 'conj' found for integers called from %.*s: %ld\n", (int)fileStr->len, fileStr->buffer, getI60(arityArgs.args[1]));
BOOM("Dispatch failed");
} else {
switch(dispVal->type) {
case 8:
pushRedex(args, glblconj716);
break;
default: {
fprintf(stderr, "No implementation of 'conj' found for type %s (%ld) called from %.*s: %ld\n", typeName(dispVal->type), dispVal->type, (int)fileStr->len, fileStr->buffer, getI60(arityArgs.args[1]));
BOOM("Dispatch failed");
}
}
}
}
return;
}
Term glblProto68 = newRef(glblCFn69);
Term glblProto76;
void glblCFn77(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 3, &arityArgs);
  if (arityArgs.count == 3) {
    String *fileStr = (String *)arityArgs.args[0];
    Value *dispVal = (Value *)arityArgs.args[2];
    swap(termLoc(args), (Term)dispVal);
    if (termTag((Term)dispVal) == I60) {
fprintf(stderr, "No implementation of 'first' found for integers called from %.*s: %ld\n", (int)fileStr->len, fileStr->buffer, getI60(arityArgs.args[1]));
BOOM("Dispatch failed");
} else {
switch(dispVal->type) {
case 2:
pushRedex(args, glblfirst455);
break;
case 8:
pushRedex(args, glblfirst724);
break;
case 44:
pushRedex(args, glblfirst453);
break;
default: {
fprintf(stderr, "No implementation of 'first' found for type %s (%ld) called from %.*s: %ld\n", typeName(dispVal->type), dispVal->type, (int)fileStr->len, fileStr->buffer, getI60(arityArgs.args[1]));
BOOM("Dispatch failed");
}
}
}
}
return;
}
Term glblProto76 = newRef(glblCFn77);
Term glblProto1889;
void glblFldFn1890(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 3, &arityArgs);
  if (arityArgs.count == 3) {
    String *fileStr = (String *)arityArgs.args[0];
    Value *dispVal = (Value *)arityArgs.args[2];
    swap(termLoc(args), (Term)dispVal);
    if (termTag((Term)dispVal) == I60) {
fprintf(stderr, "No implementation of '.state' found for integers called from %.*s: %ld\n", (int)fileStr->len, fileStr->buffer, getI60(arityArgs.args[1]));
BOOM("Dispatch failed");
} else {
switch(dispVal->type) {
case 72:
pushRedex(args, glblFld1899);
break;
case 71:
pushRedex(args, glblFld1907);
break;
case 73:
pushRedex(args, glblFld1895);
break;
default: {
fprintf(stderr, "No implementation of '.state' found for type %s (%ld) called from %.*s: %ld\n", typeName(dispVal->type), dispVal->type, (int)fileStr->len, fileStr->buffer, getI60(arityArgs.args[1]));
BOOM("Dispatch failed");
}
}
}
}
return;
}
Term glblProto1889 = newRef(glblFldFn1890);
Term glblProto82;
void glblCFn83(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 3, &arityArgs);
  if (arityArgs.count == 3) {
    String *fileStr = (String *)arityArgs.args[0];
    Value *dispVal = (Value *)arityArgs.args[2];
    swap(termLoc(args), (Term)dispVal);
    if (termTag((Term)dispVal) == I60) {
fprintf(stderr, "No implementation of 'butlast' found for integers called from %.*s: %ld\n", (int)fileStr->len, fileStr->buffer, getI60(arityArgs.args[1]));
BOOM("Dispatch failed");
} else {
switch(dispVal->type) {
case 2:
pushRedex(args, glblbutlast447);
break;
case 44:
pushRedex(args, glblbutlast445);
break;
default: {
fprintf(stderr, "No implementation of 'butlast' found for type %s (%ld) called from %.*s: %ld\n", typeName(dispVal->type), dispVal->type, (int)fileStr->len, fileStr->buffer, getI60(arityArgs.args[1]));
BOOM("Dispatch failed");
}
}
}
}
return;
}
Term glblProto82 = newRef(glblCFn83);
Term glblProto1495;
void glblFldFn1496(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 3, &arityArgs);
  if (arityArgs.count == 3) {
    String *fileStr = (String *)arityArgs.args[0];
    Value *dispVal = (Value *)arityArgs.args[2];
    swap(termLoc(args), (Term)dispVal);
    if (termTag((Term)dispVal) == I60) {
fprintf(stderr, "No implementation of '.path' found for integers called from %.*s: %ld\n", (int)fileStr->len, fileStr->buffer, getI60(arityArgs.args[1]));
BOOM("Dispatch failed");
} else {
switch(dispVal->type) {
case 49:
pushRedex(args, glblFld1503);
break;
default: {
fprintf(stderr, "No implementation of '.path' found for type %s (%ld) called from %.*s: %ld\n", typeName(dispVal->type), dispVal->type, (int)fileStr->len, fileStr->buffer, getI60(arityArgs.args[1]));
BOOM("Dispatch failed");
}
}
}
}
return;
}
Term glblProto1495 = newRef(glblFldFn1496);
Term glblProto496;
void glblCFn497(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 3, &arityArgs);
  if (arityArgs.count == 3) {
    String *fileStr = (String *)arityArgs.args[0];
    Value *dispVal = (Value *)arityArgs.args[2];
    swap(termLoc(args), (Term)dispVal);
    if (termTag((Term)dispVal) == I60) {
fprintf(stderr, "No implementation of 'vect-reduce' found for integers called from %.*s: %ld\n", (int)fileStr->len, fileStr->buffer, getI60(arityArgs.args[1]));
BOOM("Dispatch failed");
} else {
switch(dispVal->type) {
case 43:
pushRedex(args, glblvect_reduce579);
break;
case 42:
pushRedex(args, glblvect_reduce559);
break;
default: {
fprintf(stderr, "No implementation of 'vect-reduce' found for type %s (%ld) called from %.*s: %ld\n", typeName(dispVal->type), dispVal->type, (int)fileStr->len, fileStr->buffer, getI60(arityArgs.args[1]));
BOOM("Dispatch failed");
}
}
}
}
return;
}
Term glblProto496 = newRef(glblCFn497);
Term glblProto36;
void glblCFn37(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 3, &arityArgs);
  if (arityArgs.count == 3) {
    String *fileStr = (String *)arityArgs.args[0];
    Value *dispVal = (Value *)arityArgs.args[2];
    swap(termLoc(args), (Term)dispVal);
    if (termTag((Term)dispVal) == I60) {
fprintf(stderr, "No implementation of 'zero' found for integers called from %.*s: %ld\n", (int)fileStr->len, fileStr->buffer, getI60(arityArgs.args[1]));
BOOM("Dispatch failed");
} else {
switch(dispVal->type) {
default: {
fprintf(stderr, "No implementation of 'zero' found for type %s (%ld) called from %.*s: %ld\n", typeName(dispVal->type), dispVal->type, (int)fileStr->len, fileStr->buffer, getI60(arityArgs.args[1]));
BOOM("Dispatch failed");
}
}
}
}
return;
}
Term glblProto36 = newRef(glblCFn37);
Term glblProto46;
void glblCFn47(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 3, &arityArgs);
  if (arityArgs.count == 3) {
    String *fileStr = (String *)arityArgs.args[0];
    Value *dispVal = (Value *)arityArgs.args[2];
    swap(termLoc(args), (Term)dispVal);
    if (termTag((Term)dispVal) == I60) {
fprintf(stderr, "No implementation of 'flat-map' found for integers called from %.*s: %ld\n", (int)fileStr->len, fileStr->buffer, getI60(arityArgs.args[1]));
BOOM("Dispatch failed");
} else {
switch(dispVal->type) {
case 43:
pushRedex(args, glblflat_map233);
break;
case 8:
pushRedex(args, glblflat_map704);
break;
default: {
fprintf(stderr, "No implementation of 'flat-map' found for type %s (%ld) called from %.*s: %ld\n", typeName(dispVal->type), dispVal->type, (int)fileStr->len, fileStr->buffer, getI60(arityArgs.args[1]));
BOOM("Dispatch failed");
}
}
}
}
return;
}
Term glblProto46 = newRef(glblCFn47);
Term glblProto74;
void glblCFn75(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 3, &arityArgs);
  if (arityArgs.count == 3) {
    String *fileStr = (String *)arityArgs.args[0];
    Value *dispVal = (Value *)arityArgs.args[2];
    swap(termLoc(args), (Term)dispVal);
    if (termTag((Term)dispVal) == I60) {
fprintf(stderr, "No implementation of 'vec' found for integers called from %.*s: %ld\n", (int)fileStr->len, fileStr->buffer, getI60(arityArgs.args[1]));
BOOM("Dispatch failed");
} else {
switch(dispVal->type) {
case 2:
pushRedex(args, glblvec439);
break;
case 12:
pushRedex(args, glblvec1237);
break;
case 44:
pushRedex(args, glblvec437);
break;
case 13:
pushRedex(args, glblvec1269);
break;
case 11:
pushRedex(args, glblvec1205);
break;
default: {
fprintf(stderr, "No implementation of 'vec' found for type %s (%ld) called from %.*s: %ld\n", typeName(dispVal->type), dispVal->type, (int)fileStr->len, fileStr->buffer, getI60(arityArgs.args[1]));
BOOM("Dispatch failed");
}
}
}
}
return;
}
Term glblProto74 = newRef(glblCFn75);
Term glblProto113;
void glblCFn114(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 3, &arityArgs);
  if (arityArgs.count == 3) {
    String *fileStr = (String *)arityArgs.args[0];
    Value *dispVal = (Value *)arityArgs.args[2];
    swap(termLoc(args), (Term)dispVal);
    if (termTag((Term)dispVal) == I60) {
fprintf(stderr, "No implementation of 'get*' found for integers called from %.*s: %ld\n", (int)fileStr->len, fileStr->buffer, getI60(arityArgs.args[1]));
BOOM("Dispatch failed");
} else {
switch(dispVal->type) {
case 12:
pushRedex(args, glblget_STAR_1227);
break;
case 13:
pushRedex(args, glblget_STAR_1259);
break;
case 11:
pushRedex(args, glblget_STAR_1195);
break;
default: {
fprintf(stderr, "No implementation of 'get*' found for type %s (%ld) called from %.*s: %ld\n", typeName(dispVal->type), dispVal->type, (int)fileStr->len, fileStr->buffer, getI60(arityArgs.args[1]));
BOOM("Dispatch failed");
}
}
}
}
return;
}
Term glblProto113 = newRef(glblCFn114);
Term glblProto384;
void glblFldFn385(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 3, &arityArgs);
  if (arityArgs.count == 3) {
    String *fileStr = (String *)arityArgs.args[0];
    Value *dispVal = (Value *)arityArgs.args[2];
    swap(termLoc(args), (Term)dispVal);
    if (termTag((Term)dispVal) == I60) {
fprintf(stderr, "No implementation of '.start' found for integers called from %.*s: %ld\n", (int)fileStr->len, fileStr->buffer, getI60(arityArgs.args[1]));
BOOM("Dispatch failed");
} else {
switch(dispVal->type) {
case 44:
pushRedex(args, glblFld398);
break;
default: {
fprintf(stderr, "No implementation of '.start' found for type %s (%ld) called from %.*s: %ld\n", typeName(dispVal->type), dispVal->type, (int)fileStr->len, fileStr->buffer, getI60(arityArgs.args[1]));
BOOM("Dispatch failed");
}
}
}
}
return;
}
Term glblProto384 = newRef(glblFldFn385);
Term glblProto552;
void glblCFn554(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 3, &arityArgs);
  if (arityArgs.count == 3) {
    String *fileStr = (String *)arityArgs.args[0];
    Value *dispVal = (Value *)arityArgs.args[2];
    swap(termLoc(args), (Term)dispVal);
    if (termTag((Term)dispVal) == I60) {
fprintf(stderr, "No implementation of 'vect-sub' found for integers called from %.*s: %ld\n", (int)fileStr->len, fileStr->buffer, getI60(arityArgs.args[1]));
BOOM("Dispatch failed");
} else {
switch(dispVal->type) {
case 43:
pushRedex(args, glblvect_sub635);
break;
case 42:
pushRedex(args, glblvect_sub631);
break;
default: {
fprintf(stderr, "No implementation of 'vect-sub' found for type %s (%ld) called from %.*s: %ld\n", typeName(dispVal->type), dispVal->type, (int)fileStr->len, fileStr->buffer, getI60(arityArgs.args[1]));
BOOM("Dispatch failed");
}
}
}
}
return;
}
Term glblProto552 = newRef(glblCFn554);
Term glblProto78;
void glblCFn79(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 3, &arityArgs);
  if (arityArgs.count == 3) {
    String *fileStr = (String *)arityArgs.args[0];
    Value *dispVal = (Value *)arityArgs.args[2];
    swap(termLoc(args), (Term)dispVal);
    if (termTag((Term)dispVal) == I60) {
fprintf(stderr, "No implementation of 'rest' found for integers called from %.*s: %ld\n", (int)fileStr->len, fileStr->buffer, getI60(arityArgs.args[1]));
BOOM("Dispatch failed");
} else {
switch(dispVal->type) {
case 2:
pushRedex(args, glblrest451);
break;
case 8:
pushRedex(args, glblrest719);
break;
case 44:
pushRedex(args, glblrest449);
break;
default: {
fprintf(stderr, "No implementation of 'rest' found for type %s (%ld) called from %.*s: %ld\n", typeName(dispVal->type), dispVal->type, (int)fileStr->len, fileStr->buffer, getI60(arityArgs.args[1]));
BOOM("Dispatch failed");
}
}
}
}
return;
}
Term glblProto78 = newRef(glblCFn79);
Term glblProto50;
void glblCFn51(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 3, &arityArgs);
  if (arityArgs.count == 3) {
    String *fileStr = (String *)arityArgs.args[0];
    Value *dispVal = (Value *)arityArgs.args[2];
    swap(termLoc(args), (Term)dispVal);
    if (termTag((Term)dispVal) == I60) {
fprintf(stderr, "No implementation of 'extend' found for integers called from %.*s: %ld\n", (int)fileStr->len, fileStr->buffer, getI60(arityArgs.args[1]));
BOOM("Dispatch failed");
} else {
switch(dispVal->type) {
default: {
fprintf(stderr, "No implementation of 'extend' found for type %s (%ld) called from %.*s: %ld\n", typeName(dispVal->type), dispVal->type, (int)fileStr->len, fileStr->buffer, getI60(arityArgs.args[1]));
BOOM("Dispatch failed");
}
}
}
}
return;
}
Term glblProto50 = newRef(glblCFn51);
Term glblProto86;
void glblCFn87(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 3, &arityArgs);
  if (arityArgs.count == 3) {
    String *fileStr = (String *)arityArgs.args[0];
    Value *dispVal = (Value *)arityArgs.args[2];
    swap(termLoc(args), (Term)dispVal);
    if (termTag((Term)dispVal) == I60) {
fprintf(stderr, "No implementation of 'split-with' found for integers called from %.*s: %ld\n", (int)fileStr->len, fileStr->buffer, getI60(arityArgs.args[1]));
BOOM("Dispatch failed");
} else {
switch(dispVal->type) {
default: {
fprintf(stderr, "No implementation of 'split-with' found for type %s (%ld) called from %.*s: %ld\n", typeName(dispVal->type), dispVal->type, (int)fileStr->len, fileStr->buffer, getI60(arityArgs.args[1]));
BOOM("Dispatch failed");
}
}
}
}
return;
}
Term glblProto86 = newRef(glblCFn87);
Term glblProto1582;
void glblFldFn1583(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 3, &arityArgs);
  if (arityArgs.count == 3) {
    String *fileStr = (String *)arityArgs.args[0];
    Value *dispVal = (Value *)arityArgs.args[2];
    swap(termLoc(args), (Term)dispVal);
    if (termTag((Term)dispVal) == I60) {
fprintf(stderr, "No implementation of '.operator' found for integers called from %.*s: %ld\n", (int)fileStr->len, fileStr->buffer, getI60(arityArgs.args[1]));
BOOM("Dispatch failed");
} else {
switch(dispVal->type) {
case 58:
pushRedex(args, glblFld1588);
break;
default: {
fprintf(stderr, "No implementation of '.operator' found for type %s (%ld) called from %.*s: %ld\n", typeName(dispVal->type), dispVal->type, (int)fileStr->len, fileStr->buffer, getI60(arityArgs.args[1]));
BOOM("Dispatch failed");
}
}
}
}
return;
}
Term glblProto1582 = newRef(glblFldFn1583);
Term glblProto62;
void glblCFn63(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 3, &arityArgs);
  if (arityArgs.count == 3) {
    String *fileStr = (String *)arityArgs.args[0];
    Value *dispVal = (Value *)arityArgs.args[2];
    swap(termLoc(args), (Term)dispVal);
    if (termTag((Term)dispVal) == I60) {
fprintf(stderr, "No implementation of 'empty?' found for integers called from %.*s: %ld\n", (int)fileStr->len, fileStr->buffer, getI60(arityArgs.args[1]));
BOOM("Dispatch failed");
} else {
switch(dispVal->type) {
case 2:
pushRedex(args, glblempty_QM_467);
break;
case 8:
pushRedex(args, glblempty_QM_730);
break;
case 12:
pushRedex(args, glblempty_QM_1311);
break;
case 44:
pushRedex(args, glblempty_QM_465);
break;
case 13:
pushRedex(args, glblempty_QM_1313);
break;
case 11:
pushRedex(args, glblempty_QM_1309);
break;
default: {
fprintf(stderr, "No implementation of 'empty?' found for type %s (%ld) called from %.*s: %ld\n", typeName(dispVal->type), dispVal->type, (int)fileStr->len, fileStr->buffer, getI60(arityArgs.args[1]));
BOOM("Dispatch failed");
}
}
}
}
return;
}
Term glblProto62 = newRef(glblCFn63);
Term glblProto70;
void glblCFn71(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 3, &arityArgs);
  if (arityArgs.count == 3) {
    String *fileStr = (String *)arityArgs.args[0];
    Value *dispVal = (Value *)arityArgs.args[2];
    swap(termLoc(args), (Term)dispVal);
    if (termTag((Term)dispVal) == I60) {
fprintf(stderr, "No implementation of 'filter' found for integers called from %.*s: %ld\n", (int)fileStr->len, fileStr->buffer, getI60(arityArgs.args[1]));
BOOM("Dispatch failed");
} else {
switch(dispVal->type) {
case 8:
pushRedex(args, glblfilter700);
break;
default: {
fprintf(stderr, "No implementation of 'filter' found for type %s (%ld) called from %.*s: %ld\n", typeName(dispVal->type), dispVal->type, (int)fileStr->len, fileStr->buffer, getI60(arityArgs.args[1]));
BOOM("Dispatch failed");
}
}
}
}
return;
}
Term glblProto70 = newRef(glblCFn71);
Term glblProto100;
void glblCFn101(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 3, &arityArgs);
  if (arityArgs.count == 3) {
    String *fileStr = (String *)arityArgs.args[0];
    Value *dispVal = (Value *)arityArgs.args[2];
    swap(termLoc(args), (Term)dispVal);
    if (termTag((Term)dispVal) == I60) {
fprintf(stderr, "No implementation of 'store' found for integers called from %.*s: %ld\n", (int)fileStr->len, fileStr->buffer, getI60(arityArgs.args[1]));
BOOM("Dispatch failed");
} else {
switch(dispVal->type) {
case 8:
pushRedex(args, glblstore714);
break;
default: {
fprintf(stderr, "No implementation of 'store' found for type %s (%ld) called from %.*s: %ld\n", typeName(dispVal->type), dispVal->type, (int)fileStr->len, fileStr->buffer, getI60(arityArgs.args[1]));
BOOM("Dispatch failed");
}
}
}
}
return;
}
Term glblProto100 = newRef(glblCFn101);
Term glblProto1878;
void glblFldFn1879(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 3, &arityArgs);
  if (arityArgs.count == 3) {
    String *fileStr = (String *)arityArgs.args[0];
    Value *dispVal = (Value *)arityArgs.args[2];
    swap(termLoc(args), (Term)dispVal);
    if (termTag((Term)dispVal) == I60) {
fprintf(stderr, "No implementation of '.values' found for integers called from %.*s: %ld\n", (int)fileStr->len, fileStr->buffer, getI60(arityArgs.args[1]));
BOOM("Dispatch failed");
} else {
switch(dispVal->type) {
case 70:
pushRedex(args, glblFld1884);
break;
default: {
fprintf(stderr, "No implementation of '.values' found for type %s (%ld) called from %.*s: %ld\n", typeName(dispVal->type), dispVal->type, (int)fileStr->len, fileStr->buffer, getI60(arityArgs.args[1]));
BOOM("Dispatch failed");
}
}
}
}
return;
}
Term glblProto1878 = newRef(glblFldFn1879);
Term glblProto1592;
void glblFldFn1593(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 3, &arityArgs);
  if (arityArgs.count == 3) {
    String *fileStr = (String *)arityArgs.args[0];
    Value *dispVal = (Value *)arityArgs.args[2];
    swap(termLoc(args), (Term)dispVal);
    if (termTag((Term)dispVal) == I60) {
fprintf(stderr, "No implementation of '.parameter-list' found for integers called from %.*s: %ld\n", (int)fileStr->len, fileStr->buffer, getI60(arityArgs.args[1]));
BOOM("Dispatch failed");
} else {
switch(dispVal->type) {
case 63:
pushRedex(args, glblFld1706);
break;
case 66:
pushRedex(args, glblFld1682);
break;
case 57:
pushRedex(args, glblFld1600);
break;
default: {
fprintf(stderr, "No implementation of '.parameter-list' found for type %s (%ld) called from %.*s: %ld\n", typeName(dispVal->type), dispVal->type, (int)fileStr->len, fileStr->buffer, getI60(arityArgs.args[1]));
BOOM("Dispatch failed");
}
}
}
}
return;
}
Term glblProto1592 = newRef(glblFldFn1593);
Term glblProto88;
void glblCFn89(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 3, &arityArgs);
  if (arityArgs.count == 3) {
    String *fileStr = (String *)arityArgs.args[0];
    Value *dispVal = (Value *)arityArgs.args[2];
    swap(termLoc(args), (Term)dispVal);
    if (termTag((Term)dispVal) == I60) {
fprintf(stderr, "No implementation of 'take' found for integers called from %.*s: %ld\n", (int)fileStr->len, fileStr->buffer, getI60(arityArgs.args[1]));
BOOM("Dispatch failed");
} else {
switch(dispVal->type) {
default: {
fprintf(stderr, "No implementation of 'take' found for type %s (%ld) called from %.*s: %ld\n", typeName(dispVal->type), dispVal->type, (int)fileStr->len, fileStr->buffer, getI60(arityArgs.args[1]));
BOOM("Dispatch failed");
}
}
}
}
return;
}
Term glblProto88 = newRef(glblCFn89);
Term glblProto64;
void glblCFn65(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 3, &arityArgs);
  if (arityArgs.count == 3) {
    String *fileStr = (String *)arityArgs.args[0];
    Value *dispVal = (Value *)arityArgs.args[2];
    swap(termLoc(args), (Term)dispVal);
    if (termTag((Term)dispVal) == I60) {
fprintf(stderr, "No implementation of 'count' found for integers called from %.*s: %ld\n", (int)fileStr->len, fileStr->buffer, getI60(arityArgs.args[1]));
BOOM("Dispatch failed");
} else {
switch(dispVal->type) {
case 2:
pushRedex(args, glblcount411);
break;
case 46:
pushRedex(args, glblcount1373);
break;
case 8:
pushRedex(args, glblcount736);
break;
case 12:
pushRedex(args, glblcount1239);
break;
case 44:
pushRedex(args, glblcount391);
break;
case 13:
pushRedex(args, glblcount1271);
break;
case 11:
pushRedex(args, glblcount1207);
break;
case 47:
pushRedex(args, glblcount1327);
break;
default: {
fprintf(stderr, "No implementation of 'count' found for type %s (%ld) called from %.*s: %ld\n", typeName(dispVal->type), dispVal->type, (int)fileStr->len, fileStr->buffer, getI60(arityArgs.args[1]));
BOOM("Dispatch failed");
}
}
}
}
return;
}
Term glblProto64 = newRef(glblCFn65);
Term glblProto18;
void glblFldFn19(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 3, &arityArgs);
  if (arityArgs.count == 3) {
    String *fileStr = (String *)arityArgs.args[0];
    Value *dispVal = (Value *)arityArgs.args[2];
    swap(termLoc(args), (Term)dispVal);
    if (termTag((Term)dispVal) == I60) {
fprintf(stderr, "No implementation of '.x' found for integers called from %.*s: %ld\n", (int)fileStr->len, fileStr->buffer, getI60(arityArgs.args[1]));
BOOM("Dispatch failed");
} else {
switch(dispVal->type) {
case 43:
pushRedex(args, glblFld24);
break;
default: {
fprintf(stderr, "No implementation of '.x' found for type %s (%ld) called from %.*s: %ld\n", typeName(dispVal->type), dispVal->type, (int)fileStr->len, fileStr->buffer, getI60(arityArgs.args[1]));
BOOM("Dispatch failed");
}
}
}
}
return;
}
Term glblProto18 = newRef(glblFldFn19);
Term glblProto1562;
void glblFldFn1563(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 3, &arityArgs);
  if (arityArgs.count == 3) {
    String *fileStr = (String *)arityArgs.args[0];
    Value *dispVal = (Value *)arityArgs.args[2];
    swap(termLoc(args), (Term)dispVal);
    if (termTag((Term)dispVal) == I60) {
fprintf(stderr, "No implementation of '.value' found for integers called from %.*s: %ld\n", (int)fileStr->len, fileStr->buffer, getI60(arityArgs.args[1]));
BOOM("Dispatch failed");
} else {
switch(dispVal->type) {
case 61:
pushRedex(args, glblFld1566);
break;
case 59:
pushRedex(args, glblFld1578);
break;
case 68:
pushRedex(args, glblFld1664);
break;
case 60:
pushRedex(args, glblFld1572);
break;
case 73:
pushRedex(args, glblFld1893);
break;
default: {
fprintf(stderr, "No implementation of '.value' found for type %s (%ld) called from %.*s: %ld\n", typeName(dispVal->type), dispVal->type, (int)fileStr->len, fileStr->buffer, getI60(arityArgs.args[1]));
BOOM("Dispatch failed");
}
}
}
}
return;
}
Term glblProto1562 = newRef(glblFldFn1563);
Term glblProto1696;
void glblFldFn1697(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 3, &arityArgs);
  if (arityArgs.count == 3) {
    String *fileStr = (String *)arityArgs.args[0];
    Value *dispVal = (Value *)arityArgs.args[2];
    swap(termLoc(args), (Term)dispVal);
    if (termTag((Term)dispVal) == I60) {
fprintf(stderr, "No implementation of '.module-path' found for integers called from %.*s: %ld\n", (int)fileStr->len, fileStr->buffer, getI60(arityArgs.args[1]));
BOOM("Dispatch failed");
} else {
switch(dispVal->type) {
case 64:
pushRedex(args, glblFld1702);
break;
default: {
fprintf(stderr, "No implementation of '.module-path' found for type %s (%ld) called from %.*s: %ld\n", typeName(dispVal->type), dispVal->type, (int)fileStr->len, fileStr->buffer, getI60(arityArgs.args[1]));
BOOM("Dispatch failed");
}
}
}
}
return;
}
Term glblProto1696 = newRef(glblFldFn1697);
Term glblProto127;
void glblCFn128(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 3, &arityArgs);
  if (arityArgs.count == 3) {
    String *fileStr = (String *)arityArgs.args[0];
    Value *dispVal = (Value *)arityArgs.args[2];
    swap(termLoc(args), (Term)dispVal);
    if (termTag((Term)dispVal) == I60) {
pushRedex(args, glblstr_vect317);
} else {
switch(dispVal->type) {
case 2:
pushRedex(args, glblstr_vect475);
break;
case 43:
pushRedex(args, glblstr_vect251);
break;
case 8:
pushRedex(args, glblstr_vect738);
break;
case 44:
pushRedex(args, glblstr_vect473);
break;
case 42:
pushRedex(args, glblstr_vect195);
break;
default: {
fprintf(stderr, "No implementation of 'str-vect' found for type %s (%ld) called from %.*s: %ld\n", typeName(dispVal->type), dispVal->type, (int)fileStr->len, fileStr->buffer, getI60(arityArgs.args[1]));
BOOM("Dispatch failed");
}
}
}
}
return;
}
Term glblProto127 = newRef(glblCFn128);
Term glblProto1512;
void glblFldFn1513(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 3, &arityArgs);
  if (arityArgs.count == 3) {
    String *fileStr = (String *)arityArgs.args[0];
    Value *dispVal = (Value *)arityArgs.args[2];
    swap(termLoc(args), (Term)dispVal);
    if (termTag((Term)dispVal) == I60) {
fprintf(stderr, "No implementation of '.field-list' found for integers called from %.*s: %ld\n", (int)fileStr->len, fileStr->buffer, getI60(arityArgs.args[1]));
BOOM("Dispatch failed");
} else {
switch(dispVal->type) {
case 50:
pushRedex(args, glblFld1526);
break;
default: {
fprintf(stderr, "No implementation of '.field-list' found for type %s (%ld) called from %.*s: %ld\n", typeName(dispVal->type), dispVal->type, (int)fileStr->len, fileStr->buffer, getI60(arityArgs.args[1]));
BOOM("Dispatch failed");
}
}
}
}
return;
}
Term glblProto1512 = newRef(glblFldFn1513);
Term glblProto60;
void glblCFn61(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 3, &arityArgs);
  if (arityArgs.count == 3) {
    String *fileStr = (String *)arityArgs.args[0];
    Value *dispVal = (Value *)arityArgs.args[2];
    swap(termLoc(args), (Term)dispVal);
    if (termTag((Term)dispVal) == I60) {
fprintf(stderr, "No implementation of 'interpose' found for integers called from %.*s: %ld\n", (int)fileStr->len, fileStr->buffer, getI60(arityArgs.args[1]));
BOOM("Dispatch failed");
} else {
switch(dispVal->type) {
case 8:
pushRedex(args, glblinterpose698);
break;
default: {
fprintf(stderr, "No implementation of 'interpose' found for type %s (%ld) called from %.*s: %ld\n", typeName(dispVal->type), dispVal->type, (int)fileStr->len, fileStr->buffer, getI60(arityArgs.args[1]));
BOOM("Dispatch failed");
}
}
}
}
return;
}
Term glblProto60 = newRef(glblCFn61);
Term glblProto1612;
void glblFldFn1613(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 3, &arityArgs);
  if (arityArgs.count == 3) {
    String *fileStr = (String *)arityArgs.args[0];
    Value *dispVal = (Value *)arityArgs.args[2];
    swap(termLoc(args), (Term)dispVal);
    if (termTag((Term)dispVal) == I60) {
fprintf(stderr, "No implementation of '.symbol' found for integers called from %.*s: %ld\n", (int)fileStr->len, fileStr->buffer, getI60(arityArgs.args[1]));
BOOM("Dispatch failed");
} else {
switch(dispVal->type) {
case 55:
pushRedex(args, glblFld1618);
break;
default: {
fprintf(stderr, "No implementation of '.symbol' found for type %s (%ld) called from %.*s: %ld\n", typeName(dispVal->type), dispVal->type, (int)fileStr->len, fileStr->buffer, getI60(arityArgs.args[1]));
BOOM("Dispatch failed");
}
}
}
}
return;
}
Term glblProto1612 = newRef(glblFldFn1613);
Term glblProto1650;
void glblFldFn1651(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 3, &arityArgs);
  if (arityArgs.count == 3) {
    String *fileStr = (String *)arityArgs.args[0];
    Value *dispVal = (Value *)arityArgs.args[2];
    swap(termLoc(args), (Term)dispVal);
    if (termTag((Term)dispVal) == I60) {
fprintf(stderr, "No implementation of '.c-code' found for integers called from %.*s: %ld\n", (int)fileStr->len, fileStr->buffer, getI60(arityArgs.args[1]));
BOOM("Dispatch failed");
} else {
switch(dispVal->type) {
case 51:
pushRedex(args, glblFld1656);
break;
default: {
fprintf(stderr, "No implementation of '.c-code' found for type %s (%ld) called from %.*s: %ld\n", typeName(dispVal->type), dispVal->type, (int)fileStr->len, fileStr->buffer, getI60(arityArgs.args[1]));
BOOM("Dispatch failed");
}
}
}
}
return;
}
Term glblProto1650 = newRef(glblFldFn1651);
Term glblProto38;
void glblCFn39(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 3, &arityArgs);
  if (arityArgs.count == 3) {
    String *fileStr = (String *)arityArgs.args[0];
    Value *dispVal = (Value *)arityArgs.args[2];
    swap(termLoc(args), (Term)dispVal);
    if (termTag((Term)dispVal) == I60) {
fprintf(stderr, "No implementation of 'comp' found for integers called from %.*s: %ld\n", (int)fileStr->len, fileStr->buffer, getI60(arityArgs.args[1]));
BOOM("Dispatch failed");
} else {
switch(dispVal->type) {
case 8:
pushRedex(args, glblcomp726);
break;
default: {
fprintf(stderr, "No implementation of 'comp' found for type %s (%ld) called from %.*s: %ld\n", typeName(dispVal->type), dispVal->type, (int)fileStr->len, fileStr->buffer, getI60(arityArgs.args[1]));
BOOM("Dispatch failed");
}
}
}
}
return;
}
Term glblProto38 = newRef(glblCFn39);
Term glblProto1614;
void glblFldFn1615(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 3, &arityArgs);
  if (arityArgs.count == 3) {
    String *fileStr = (String *)arityArgs.args[0];
    Value *dispVal = (Value *)arityArgs.args[2];
    swap(termLoc(args), (Term)dispVal);
    if (termTag((Term)dispVal) == I60) {
fprintf(stderr, "No implementation of '.type-expr' found for integers called from %.*s: %ld\n", (int)fileStr->len, fileStr->buffer, getI60(arityArgs.args[1]));
BOOM("Dispatch failed");
} else {
switch(dispVal->type) {
case 55:
pushRedex(args, glblFld1620);
break;
case 51:
pushRedex(args, glblFld1654);
break;
default: {
fprintf(stderr, "No implementation of '.type-expr' found for type %s (%ld) called from %.*s: %ld\n", typeName(dispVal->type), dispVal->type, (int)fileStr->len, fileStr->buffer, getI60(arityArgs.args[1]));
BOOM("Dispatch failed");
}
}
}
}
return;
}
Term glblProto1614 = newRef(glblFldFn1615);
Term glblProto123;
void glblCFn124(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 3, &arityArgs);
  if (arityArgs.count == 3) {
    String *fileStr = (String *)arityArgs.args[0];
    Value *dispVal = (Value *)arityArgs.args[2];
    swap(termLoc(args), (Term)dispVal);
    if (termTag((Term)dispVal) == I60) {
fprintf(stderr, "No implementation of 'keys' found for integers called from %.*s: %ld\n", (int)fileStr->len, fileStr->buffer, getI60(arityArgs.args[1]));
BOOM("Dispatch failed");
} else {
switch(dispVal->type) {
default: {
fprintf(stderr, "No implementation of 'keys' found for type %s (%ld) called from %.*s: %ld\n", typeName(dispVal->type), dispVal->type, (int)fileStr->len, fileStr->buffer, getI60(arityArgs.args[1]));
BOOM("Dispatch failed");
}
}
}
}
return;
}
Term glblProto123 = newRef(glblCFn124);
Term glblProto58;
void glblCFn59(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 3, &arityArgs);
  if (arityArgs.count == 3) {
    String *fileStr = (String *)arityArgs.args[0];
    Value *dispVal = (Value *)arityArgs.args[2];
    swap(termLoc(args), (Term)dispVal);
    if (termTag((Term)dispVal) == I60) {
fprintf(stderr, "No implementation of 'apply' found for integers called from %.*s: %ld\n", (int)fileStr->len, fileStr->buffer, getI60(arityArgs.args[1]));
BOOM("Dispatch failed");
} else {
switch(dispVal->type) {
default: {
fprintf(stderr, "No implementation of 'apply' found for type %s (%ld) called from %.*s: %ld\n", typeName(dispVal->type), dispVal->type, (int)fileStr->len, fileStr->buffer, getI60(arityArgs.args[1]));
BOOM("Dispatch failed");
}
}
}
}
return;
}
Term glblProto58 = newRef(glblCFn59);
Term glblProto30;
void glblCFn31(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 3, &arityArgs);
  if (arityArgs.count == 3) {
    String *fileStr = (String *)arityArgs.args[0];
    Value *dispVal = (Value *)arityArgs.args[2];
    swap(termLoc(args), (Term)dispVal);
    if (termTag((Term)dispVal) == I60) {
fprintf(stderr, "No implementation of 'or' found for integers called from %.*s: %ld\n", (int)fileStr->len, fileStr->buffer, getI60(arityArgs.args[1]));
BOOM("Dispatch failed");
} else {
switch(dispVal->type) {
case 43:
pushRedex(args, glblor237);
break;
case 42:
pushRedex(args, glblor183);
break;
default: {
fprintf(stderr, "No implementation of 'or' found for type %s (%ld) called from %.*s: %ld\n", typeName(dispVal->type), dispVal->type, (int)fileStr->len, fileStr->buffer, getI60(arityArgs.args[1]));
BOOM("Dispatch failed");
}
}
}
}
return;
}
Term glblProto30 = newRef(glblCFn31);
Term glblProto1606;
void glblFldFn1607(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 3, &arityArgs);
  if (arityArgs.count == 3) {
    String *fileStr = (String *)arityArgs.args[0];
    Value *dispVal = (Value *)arityArgs.args[2];
    swap(termLoc(args), (Term)dispVal);
    if (termTag((Term)dispVal) == I60) {
fprintf(stderr, "No implementation of '.alternatives' found for integers called from %.*s: %ld\n", (int)fileStr->len, fileStr->buffer, getI60(arityArgs.args[1]));
BOOM("Dispatch failed");
} else {
switch(dispVal->type) {
case 56:
pushRedex(args, glblFld1610);
break;
default: {
fprintf(stderr, "No implementation of '.alternatives' found for type %s (%ld) called from %.*s: %ld\n", typeName(dispVal->type), dispVal->type, (int)fileStr->len, fileStr->buffer, getI60(arityArgs.args[1]));
BOOM("Dispatch failed");
}
}
}
}
return;
}
Term glblProto1606 = newRef(glblFldFn1607);
Term glblProto34;
void glblCFn35(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 3, &arityArgs);
  if (arityArgs.count == 3) {
    String *fileStr = (String *)arityArgs.args[0];
    Value *dispVal = (Value *)arityArgs.args[2];
    swap(termLoc(args), (Term)dispVal);
    if (termTag((Term)dispVal) == I60) {
pushRedex(args, glblcond311);
} else {
switch(dispVal->type) {
case 43:
pushRedex(args, glblcond241);
break;
case 42:
pushRedex(args, glblcond187);
break;
default: {
fprintf(stderr, "No implementation of 'cond' found for type %s (%ld) called from %.*s: %ld\n", typeName(dispVal->type), dispVal->type, (int)fileStr->len, fileStr->buffer, getI60(arityArgs.args[1]));
BOOM("Dispatch failed");
}
}
}
}
return;
}
Term glblProto34 = newRef(glblCFn35);
Term glblProto1644;
void glblFldFn1645(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 3, &arityArgs);
  if (arityArgs.count == 3) {
    String *fileStr = (String *)arityArgs.args[0];
    Value *dispVal = (Value *)arityArgs.args[2];
    swap(termLoc(args), (Term)dispVal);
    if (termTag((Term)dispVal) == I60) {
fprintf(stderr, "No implementation of '.lines' found for integers called from %.*s: %ld\n", (int)fileStr->len, fileStr->buffer, getI60(arityArgs.args[1]));
BOOM("Dispatch failed");
} else {
switch(dispVal->type) {
case 52:
pushRedex(args, glblFld1648);
break;
default: {
fprintf(stderr, "No implementation of '.lines' found for type %s (%ld) called from %.*s: %ld\n", typeName(dispVal->type), dispVal->type, (int)fileStr->len, fileStr->buffer, getI60(arityArgs.args[1]));
BOOM("Dispatch failed");
}
}
}
}
return;
}
Term glblProto1644 = newRef(glblFldFn1645);
Term glblProto80;
void glblCFn81(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 3, &arityArgs);
  if (arityArgs.count == 3) {
    String *fileStr = (String *)arityArgs.args[0];
    Value *dispVal = (Value *)arityArgs.args[2];
    swap(termLoc(args), (Term)dispVal);
    if (termTag((Term)dispVal) == I60) {
fprintf(stderr, "No implementation of 'last' found for integers called from %.*s: %ld\n", (int)fileStr->len, fileStr->buffer, getI60(arityArgs.args[1]));
BOOM("Dispatch failed");
} else {
switch(dispVal->type) {
case 2:
pushRedex(args, glbllast443);
break;
case 8:
pushRedex(args, glbllast722);
break;
case 44:
pushRedex(args, glbllast441);
break;
default: {
fprintf(stderr, "No implementation of 'last' found for type %s (%ld) called from %.*s: %ld\n", typeName(dispVal->type), dispVal->type, (int)fileStr->len, fileStr->buffer, getI60(arityArgs.args[1]));
BOOM("Dispatch failed");
}
}
}
}
return;
}
Term glblProto80 = newRef(glblCFn81);
Term glblProto104;
void glblCFn105(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 3, &arityArgs);
  if (arityArgs.count == 3) {
    String *fileStr = (String *)arityArgs.args[0];
    Value *dispVal = (Value *)arityArgs.args[2];
    swap(termLoc(args), (Term)dispVal);
    if (termTag((Term)dispVal) == I60) {
pushRedex(args, glblsha1309);
} else {
switch(dispVal->type) {
case 2:
pushRedex(args, glblsha1431);
break;
case 44:
pushRedex(args, glblsha1429);
break;
default: {
fprintf(stderr, "No implementation of 'sha1' found for type %s (%ld) called from %.*s: %ld\n", typeName(dispVal->type), dispVal->type, (int)fileStr->len, fileStr->buffer, getI60(arityArgs.args[1]));
BOOM("Dispatch failed");
}
}
}
}
return;
}
Term glblProto104 = newRef(glblCFn105);
Term glblProto117;
void glblCFn118(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 3, &arityArgs);
  if (arityArgs.count == 3) {
    String *fileStr = (String *)arityArgs.args[0];
    Value *dispVal = (Value *)arityArgs.args[2];
    swap(termLoc(args), (Term)dispVal);
    if (termTag((Term)dispVal) == I60) {
fprintf(stderr, "No implementation of 'dissoc' found for integers called from %.*s: %ld\n", (int)fileStr->len, fileStr->buffer, getI60(arityArgs.args[1]));
BOOM("Dispatch failed");
} else {
switch(dispVal->type) {
case 12:
pushRedex(args, glbldissoc1293);
break;
case 13:
pushRedex(args, glbldissoc1295);
break;
case 11:
pushRedex(args, glbldissoc1291);
break;
default: {
fprintf(stderr, "No implementation of 'dissoc' found for type %s (%ld) called from %.*s: %ld\n", typeName(dispVal->type), dispVal->type, (int)fileStr->len, fileStr->buffer, getI60(arityArgs.args[1]));
BOOM("Dispatch failed");
}
}
}
}
return;
}
Term glblProto117 = newRef(glblCFn118);
Term glblProto386;
void glblFldFn387(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 3, &arityArgs);
  if (arityArgs.count == 3) {
    String *fileStr = (String *)arityArgs.args[0];
    Value *dispVal = (Value *)arityArgs.args[2];
    swap(termLoc(args), (Term)dispVal);
    if (termTag((Term)dispVal) == I60) {
fprintf(stderr, "No implementation of '.len' found for integers called from %.*s: %ld\n", (int)fileStr->len, fileStr->buffer, getI60(arityArgs.args[1]));
BOOM("Dispatch failed");
} else {
switch(dispVal->type) {
case 46:
pushRedex(args, glblFld1384);
break;
case 44:
pushRedex(args, glblFld400);
break;
default: {
fprintf(stderr, "No implementation of '.len' found for type %s (%ld) called from %.*s: %ld\n", typeName(dispVal->type), dispVal->type, (int)fileStr->len, fileStr->buffer, getI60(arityArgs.args[1]));
BOOM("Dispatch failed");
}
}
}
}
return;
}
Term glblProto386 = newRef(glblFldFn387);
Term glblProto107;
void glblCFn108(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 3, &arityArgs);
  if (arityArgs.count == 3) {
    String *fileStr = (String *)arityArgs.args[0];
    Value *dispVal = (Value *)arityArgs.args[2];
    swap(termLoc(args), (Term)dispVal);
    if (termTag((Term)dispVal) == I60) {
fprintf(stderr, "No implementation of 'assoc' found for integers called from %.*s: %ld\n", (int)fileStr->len, fileStr->buffer, getI60(arityArgs.args[1]));
BOOM("Dispatch failed");
} else {
switch(dispVal->type) {
case 12:
pushRedex(args, glblassoc1299);
break;
case 13:
pushRedex(args, glblassoc1301);
break;
case 11:
pushRedex(args, glblassoc1297);
break;
default: {
fprintf(stderr, "No implementation of 'assoc' found for type %s (%ld) called from %.*s: %ld\n", typeName(dispVal->type), dispVal->type, (int)fileStr->len, fileStr->buffer, getI60(arityArgs.args[1]));
BOOM("Dispatch failed");
}
}
}
}
return;
}
Term glblProto107 = newRef(glblCFn108);
Term glblProto56;
void glblCFn57(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 3, &arityArgs);
  if (arityArgs.count == 3) {
    String *fileStr = (String *)arityArgs.args[0];
    Value *dispVal = (Value *)arityArgs.args[2];
    swap(termLoc(args), (Term)dispVal);
    if (termTag((Term)dispVal) == I60) {
fprintf(stderr, "No implementation of 'extract' found for integers called from %.*s: %ld\n", (int)fileStr->len, fileStr->buffer, getI60(arityArgs.args[1]));
BOOM("Dispatch failed");
} else {
switch(dispVal->type) {
case 43:
pushRedex(args, glblextract247);
break;
default: {
fprintf(stderr, "No implementation of 'extract' found for type %s (%ld) called from %.*s: %ld\n", typeName(dispVal->type), dispVal->type, (int)fileStr->len, fileStr->buffer, getI60(arityArgs.args[1]));
BOOM("Dispatch failed");
}
}
}
}
return;
}
Term glblProto56 = newRef(glblCFn57);
Term glblProto52;
void glblCFn53(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 3, &arityArgs);
  if (arityArgs.count == 3) {
    String *fileStr = (String *)arityArgs.args[0];
    Value *dispVal = (Value *)arityArgs.args[2];
    swap(termLoc(args), (Term)dispVal);
    if (termTag((Term)dispVal) == I60) {
fprintf(stderr, "No implementation of 'duplicate' found for integers called from %.*s: %ld\n", (int)fileStr->len, fileStr->buffer, getI60(arityArgs.args[1]));
BOOM("Dispatch failed");
} else {
switch(dispVal->type) {
default: {
fprintf(stderr, "No implementation of 'duplicate' found for type %s (%ld) called from %.*s: %ld\n", typeName(dispVal->type), dispVal->type, (int)fileStr->len, fileStr->buffer, getI60(arityArgs.args[1]));
BOOM("Dispatch failed");
}
}
}
}
return;
}
Term glblProto52 = newRef(glblCFn53);
Term glblProto109;
void glblCFn110(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 3, &arityArgs);
  if (arityArgs.count == 3) {
    String *fileStr = (String *)arityArgs.args[0];
    Value *dispVal = (Value *)arityArgs.args[2];
    swap(termLoc(args), (Term)dispVal);
    if (termTag((Term)dispVal) == I60) {
fprintf(stderr, "No implementation of 'copyAssoc' found for integers called from %.*s: %ld\n", (int)fileStr->len, fileStr->buffer, getI60(arityArgs.args[1]));
BOOM("Dispatch failed");
} else {
switch(dispVal->type) {
case 12:
pushRedex(args, glblcopyAssoc1231);
break;
case 13:
pushRedex(args, glblcopyAssoc1263);
break;
case 11:
pushRedex(args, glblcopyAssoc1199);
break;
default: {
fprintf(stderr, "No implementation of 'copyAssoc' found for type %s (%ld) called from %.*s: %ld\n", typeName(dispVal->type), dispVal->type, (int)fileStr->len, fileStr->buffer, getI60(arityArgs.args[1]));
BOOM("Dispatch failed");
}
}
}
}
return;
}
Term glblProto109 = newRef(glblCFn110);
Term glblProto1510;
void glblFldFn1511(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 3, &arityArgs);
  if (arityArgs.count == 3) {
    String *fileStr = (String *)arityArgs.args[0];
    Value *dispVal = (Value *)arityArgs.args[2];
    swap(termLoc(args), (Term)dispVal);
    if (termTag((Term)dispVal) == I60) {
fprintf(stderr, "No implementation of '.name' found for integers called from %.*s: %ld\n", (int)fileStr->len, fileStr->buffer, getI60(arityArgs.args[1]));
BOOM("Dispatch failed");
} else {
switch(dispVal->type) {
case 66:
pushRedex(args, glblFld1680);
break;
case 62:
pushRedex(args, glblFld1556);
break;
case 50:
pushRedex(args, glblFld1523);
break;
case 68:
pushRedex(args, glblFld1662);
break;
case 57:
pushRedex(args, glblFld1598);
break;
default: {
fprintf(stderr, "No implementation of '.name' found for type %s (%ld) called from %.*s: %ld\n", typeName(dispVal->type), dispVal->type, (int)fileStr->len, fileStr->buffer, getI60(arityArgs.args[1]));
BOOM("Dispatch failed");
}
}
}
}
return;
}
Term glblProto1510 = newRef(glblFldFn1511);
Term glblProto129;
void glblCFn130(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 3, &arityArgs);
  if (arityArgs.count == 3) {
    String *fileStr = (String *)arityArgs.args[0];
    Value *dispVal = (Value *)arityArgs.args[2];
    swap(termLoc(args), (Term)dispVal);
    if (termTag((Term)dispVal) == I60) {
fprintf(stderr, "No implementation of 'free-resource' found for integers called from %.*s: %ld\n", (int)fileStr->len, fileStr->buffer, getI60(arityArgs.args[1]));
BOOM("Dispatch failed");
} else {
switch(dispVal->type) {
default: {
fprintf(stderr, "No implementation of 'free-resource' found for type %s (%ld) called from %.*s: %ld\n", typeName(dispVal->type), dispVal->type, (int)fileStr->len, fileStr->buffer, getI60(arityArgs.args[1]));
BOOM("Dispatch failed");
}
}
}
}
return;
}
Term glblProto129 = newRef(glblCFn130);
Term glblProto119;
void glblCFn120(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 3, &arityArgs);
  if (arityArgs.count == 3) {
    String *fileStr = (String *)arityArgs.args[0];
    Value *dispVal = (Value *)arityArgs.args[2];
    swap(termLoc(args), (Term)dispVal);
    if (termTag((Term)dispVal) == I60) {
fprintf(stderr, "No implementation of 'get' found for integers called from %.*s: %ld\n", (int)fileStr->len, fileStr->buffer, getI60(arityArgs.args[1]));
BOOM("Dispatch failed");
} else {
switch(dispVal->type) {
case 8:
pushRedex(args, glblget734);
break;
case 12:
pushRedex(args, glblget1287);
break;
case 13:
pushRedex(args, glblget1289);
break;
case 11:
pushRedex(args, glblget1285);
break;
default: {
fprintf(stderr, "No implementation of 'get' found for type %s (%ld) called from %.*s: %ld\n", typeName(dispVal->type), dispVal->type, (int)fileStr->len, fileStr->buffer, getI60(arityArgs.args[1]));
BOOM("Dispatch failed");
}
}
}
}
return;
}
Term glblProto119 = newRef(glblCFn120);
Term glblProto1757;
void glblCFn1758(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 3, &arityArgs);
  if (arityArgs.count == 3) {
    String *fileStr = (String *)arityArgs.args[0];
    Value *dispVal = (Value *)arityArgs.args[2];
    swap(termLoc(args), (Term)dispVal);
    if (termTag((Term)dispVal) == I60) {
fprintf(stderr, "No implementation of 'result-kind' found for integers called from %.*s: %ld\n", (int)fileStr->len, fileStr->buffer, getI60(arityArgs.args[1]));
BOOM("Dispatch failed");
} else {
switch(dispVal->type) {
case 72:
pushRedex(args, glblresult_kind2154);
break;
case 71:
pushRedex(args, glblresult_kind2156);
break;
case 73:
pushRedex(args, glblresult_kind2150);
break;
case 74:
pushRedex(args, glblresult_kind2160);
break;
default: {
fprintf(stderr, "No implementation of 'result-kind' found for type %s (%ld) called from %.*s: %ld\n", typeName(dispVal->type), dispVal->type, (int)fileStr->len, fileStr->buffer, getI60(arityArgs.args[1]));
BOOM("Dispatch failed");
}
}
}
}
return;
}
Term glblProto1757 = newRef(glblCFn1758);
Term glblProto1759;
void glblCFn1760(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 3, &arityArgs);
  if (arityArgs.count == 3) {
    String *fileStr = (String *)arityArgs.args[0];
    Value *dispVal = (Value *)arityArgs.args[2];
    swap(termLoc(args), (Term)dispVal);
    if (termTag((Term)dispVal) == I60) {
fprintf(stderr, "No implementation of 'expr-kind' found for integers called from %.*s: %ld\n", (int)fileStr->len, fileStr->buffer, getI60(arityArgs.args[1]));
BOOM("Dispatch failed");
} else {
switch(dispVal->type) {
case 54:
pushRedex(args, glblexpr_kind2168);
break;
case 62:
pushRedex(args, glblexpr_kind2166);
break;
case 58:
pushRedex(args, glblexpr_kind2163);
break;
default: {
fprintf(stderr, "No implementation of 'expr-kind' found for type %s (%ld) called from %.*s: %ld\n", typeName(dispVal->type), dispVal->type, (int)fileStr->len, fileStr->buffer, getI60(arityArgs.args[1]));
BOOM("Dispatch failed");
}
}
}
}
return;
}
Term glblProto1759 = newRef(glblCFn1760);
Term glblProto1468;
void glblCFn1469(Term ref, Term args) {
  NativeArgs arityArgs = {0, {}};
  args = strictArgs(ref, args, 3, &arityArgs);
  if (arityArgs.count == 3) {
    String *fileStr = (String *)arityArgs.args[0];
    Value *dispVal = (Value *)arityArgs.args[2];
    swap(termLoc(args), (Term)dispVal);
    if (termTag((Term)dispVal) == I60) {
fprintf(stderr, "No implementation of 'val-kind' found for integers called from %.*s: %ld\n", (int)fileStr->len, fileStr->buffer, getI60(arityArgs.args[1]));
BOOM("Dispatch failed");
} else {
switch(dispVal->type) {
case 61:
pushRedex(args, glblval_kind2628);
break;
default: {
fprintf(stderr, "No implementation of 'val-kind' found for type %s (%ld) called from %.*s: %ld\n", typeName(dispVal->type), dispVal->type, (int)fileStr->len, fileStr->buffer, getI60(arityArgs.args[1]));
BOOM("Dispatch failed");
}
}
}
}
return;
}
Term glblProto1468 = newRef(glblCFn1469);
unsigned refsCount = 524;
unsigned refNameCount = 524;
refMap refNames[524] = {{intCond, "intCond"},
{vectMap, "vectMap"},
{vectConjFn, "vectConj"},
{graphFn, "graph"},
{discardFn, "discard"},
{accessFieldFn, "accessField"},
{hvmVectFn, "hvmVect"},
{constructFn, "construct"},
{glblCFn134, "pr*"},
{glblCFn55, "wrap"},
{glblwrap705, "wrap 8"},
{glblCFn755, "bmiCount"},
{glblCFn252, "int-="},
{glblCFn884, "collisionKey"},
{glblCFn1048, "bmiGet"},
{glblCFn866, "bmiSlotCount"},
{glblFldFn1671, ".constructors"},
{glblFldFn1675, ".constructors 67"},
{glblCFn73, "reduce"},
{glblreduce711, "reduce 8"},
{glblCFn901, "collisionVec"},
{glblCFn126, "vals"},
{glblvals1232, "vals 12"},
{glblvals1264, "vals 13"},
{glblvals1200, "vals 11"},
{glblCFn67, "empty"},
{glblempty480, "empty 2"},
{glblempty727, "empty 8"},
{glblempty476, "empty 44"},
{glblCFn773, "bmiCopyChild"},
{glblCFn849, "bmiVal"},
{glblCFn85, "split"},
{glblCFn93, "drop-while"},
{glblCFn655, "to-str"},
{glblCFn41, "recurse"},
{glblrecurse470, "recurse 2"},
{glblrecurse1367, "recurse 46"},
{glblrecurse244, "recurse 43"},
{glblrecurse468, "recurse 44"},
{glblrecurse190, "recurse 42"},
{glblrecurse1322, "recurse 47"},
{glblc211, "core: 466"},
{glblCFn263, "int-sha1"},
{glblFldFn1473, ".file"},
{glblFldFn1482, ".file 48"},
{glblFldFn383, ".s"},
{glblFldFn395, ".s 44"},
{glblCFn149, "*"},
{glblFldFn1687, ".methods"},
{glblFldFn1691, ".methods 65"},
{glblFldFn1360, ".head"},
{glblFldFn1378, ".head 46"},
{glblFldFn1595, ".body"},
{glblFldFn1707, ".body 63"},
{glblFldFn1683, ".body 66"},
{glblFldFn1601, ".body 57"},
{glblCFn33, "either"},
{glbleither242, "either 43"},
{glbleither188, "either 42"},
{glblFldFn1877, ".input"},
{glblFldFn1881, ".input 70"},
{glblCFn795, "bmiMutateChild"},
{glblCFn955, "bmiHashSeq-bm"},
{glblCFn414, "string-vec"},
{glblCFn743, "arrayNodeCopyChild"},
{glblCFn260, "<="},
{glblCFn161, "bit-or"},
{glblCFn97, "reverse"},
{glblreverse709, "reverse 8"},
{glblCFn751, "arrayNodeMutateChild"},
{glblCFn921, "bmiVec"},
{glblCFn21, "="},
{glbl_EQ_462, "= 2"},
{glbl_EQ_248, "= 43"},
{glbl_EQ_741, "= 8"},
{glbl_EQ_1304, "= 12"},
{glbl_EQ_460, "= 44"},
{glbl_EQ_1306, "= 13"},
{glbl_EQ_1176, "= 45"},
{glbl_EQ_192, "= 42"},
{glbl_EQ_314, "= 1"},
{glbl_EQ_1302, "= 11"},
{glblCFn0, "prefs"},
{glblCFn324, "vect-count"},
{glblFldFn1623, ".field-name"},
{glblFldFn1625, ".field-name 54"},
{glblFldFn1546, ".ns"},
{glblFldFn1553, ".ns 62"},
{glblCons1375, "Cons"},
{glblCFn99, "nth"},
{glblnth434, "nth 2"},
{glblnth731, "nth 8"},
{glblnth432, "nth 44"},
{glblCFn112, "assoc*"},
{glblassoc_STAR_1228, "assoc* 12"},
{glblassoc_STAR_1260, "assoc* 13"},
{glblassoc_STAR_1196, "assoc* 11"},
{glblCFn957, "bmiHashSeq"},
{glblCFn718, "emptyArrayNode"},
{glblCFn137, "+"},
{glblCFn321, "min"},
{glblCFn937, "arrayNodeVec-i"},
{glblFldFn1633, ".cases"},
{glblFldFn1639, ".cases 53"},
{glblCFn365, "str<"},
{glblCFn7, "type-name"},
{glbltype_name484, "type-name 2"},
{glbltype_name739, "type-name 8"},
{glbltype_name482, "type-name 44"},
{glbltype_name318, "type-name 1"},
{glblglbl8, "type-name 0"},
{glblFldFn1517, ".implementations"},
{glblFldFn1527, ".implementations 50"},
{glblCFn1070, "bmiDissoc"},
{glblCFn477, "vect-get"},
{glblCFn103, "sha1-update"},
{glblFldFn1635, ".default-case"},
{glblFldFn1641, ".default-case 53"},
{glblCFn1316, "str*"},
{glblCFn1037, "arrayNodeMutateAssoc"},
{glblCFn1168, "refs-count"},
{glblCFn29, "and"},
{glbland238, "and 43"},
{glbland184, "and 42"},
{glblFldFn1478, ".line"},
{glblFldFn1484, ".line 48"},
{glblCFn769, "bmiUpdate"},
{glblCFn371, "escape-chars"},
{glblFldFn1695, ".namespace"},
{glblFldFn1699, ".namespace 64"},
{glblCFn1026, "arrayNodeCopyAssoc"},
{glblCFn1314, "cons"},
{glblCFn381, "subs"},
{glblsubs408, "subs 2"},
{glblsubs392, "subs 44"},
{glblFldFn1363, ".tail"},
{glblFldFn1381, ".tail 46"},
{glblCFn492, "range*"},
{glblCFn916, "set-hash-val"},
{glblCFn95, "take-while"},
{glblCFn494, "range"},
{glblCFn499, "vect-reverse"},
{glblvect_reverse617, "vect-reverse 43"},
{glblvect_reverse614, "vect-reverse 42"},
{glblCFn1115, "collisionGet"},
{glblCFn173, "bit-shift-right"},
{glblCFn170, "bit-shift-left"},
{glblFldFn1585, ".operands"},
{glblFldFn1589, ".operands 58"},
{glblCFn10, "type-num"},
{glblFldFn1631, ".expr"},
{glblFldFn1637, ".expr 53"},
{glblFldFn1549, ".loc"},
{glblFldFn1567, ".loc 61"},
{glblFldFn1709, ".loc 63"},
{glblFldFn1627, ".loc 54"},
{glblFldFn1579, ".loc 59"},
{glblFldFn1558, ".loc 62"},
{glblFldFn1665, ".loc 68"},
{glblFldFn1573, ".loc 60"},
{glblFldFn1657, ".loc 51"},
{glblFldFn1603, ".loc 57"},
{glblCFn374, "char"},
{glblCFn131, "abort"},
{glblFldFn1669, ".type-name"},
{glblFldFn1689, ".type-name 65"},
{glblFldFn1673, ".type-name 67"},
{glblCFn152, "div"},
{glblCFn49, "flatten"},
{glblflatten701, "flatten 8"},
{glblCFn27, "<"},
{glbl_LT_458, "< 2"},
{glbl_LT_456, "< 44"},
{glbl_LT_312, "< 1"},
{glblCFn122, "hash-vec"},
{glblhash_vec1234, "hash-vec 12"},
{glblhash_vec1266, "hash-vec 13"},
{glblhash_vec1202, "hash-vec 11"},
{glblCFn989, "pair-val"},
{glblCFn164, "bit-xor"},
{glblCFn759, "bmiHashVec"},
{glblCFn898, "collisionShrink"},
{glblCFn377, "char-code"},
{glblCFn919, "bmiVec-bm"},
{glblCFn158, "bit-and"},
{glblCFn91, "drop"},
{glblCFn362, "str="},
{glblCFn973, "arrayNodeHashSeq-i"},
{glblCFn43, "map"},
{glblmap234, "map 43"},
{glblmap707, "map 8"},
{glblmap180, "map 42"},
{glblCFn1104, "collisionGet-i"},
{glblCFn913, "bmiSetChild"},
{glblCFn340, "fold"},
{glblCFn910, "bmiSetKV"},
{glblCFn116, "dissoc*"},
{glbldissoc_STAR_1224, "dissoc* 12"},
{glbldissoc_STAR_1256, "dissoc* 13"},
{glbldissoc_STAR_1192, "dissoc* 11"},
{glblFldFn1902, ".msg"},
{glblFldFn1904, ".msg 71"},
{glblCFn879, "collisionCount"},
{glblCFn45, "map!"},
{glblCFn69, "conj"},
{glblconj715, "conj 8"},
{glblCFn77, "first"},
{glblfirst454, "first 2"},
{glblfirst723, "first 8"},
{glblfirst452, "first 44"},
{glblFldFn1890, ".state"},
{glblFldFn1898, ".state 72"},
{glblFldFn1906, ".state 71"},
{glblFldFn1894, ".state 73"},
{glblCFn83, "butlast"},
{glblbutlast446, "butlast 2"},
{glblbutlast444, "butlast 44"},
{glblFldFn1496, ".path"},
{glblFldFn1502, ".path 49"},
{glblCFn889, "collisionVal"},
{glblCFn356, "str-malloc"},
{glblCFn497, "vect-reduce"},
{glblvect_reduce576, "vect-reduce 43"},
{glblvect_reduce557, "vect-reduce 42"},
{glblCFn996, "bmiCopyAssoc"},
{glblCFn763, "bmiBitMap"},
{glblCFn359, "str-append"},
{glblCFn155, "mod"},
{glblCFn37, "zero"},
{glblCFn47, "flat-map"},
{glblflat_map232, "flat-map 43"},
{glblflat_map703, "flat-map 8"},
{glblCFn75, "vec"},
{glblvec438, "vec 2"},
{glblvec1236, "vec 12"},
{glblvec436, "vec 44"},
{glblvec1268, "vec 13"},
{glblvec1204, "vec 11"},
{glblCFn114, "get*"},
{glblget_STAR_1226, "get* 12"},
{glblget_STAR_1258, "get* 13"},
{glblget_STAR_1194, "get* 11"},
{glblFldFn385, ".start"},
{glblFldFn397, ".start 44"},
{glblCFn554, "vect-sub"},
{glblvect_sub633, "vect-sub 43"},
{glblvect_sub630, "vect-sub 42"},
{glblCFn258, "number-str"},
{glblCFn345, "unfold"},
{glblc213, "core: 464"},
{glblCFn753, "arrayNodeSetSlot"},
{glblCFn79, "rest"},
{glblrest450, "rest 2"},
{glblrest717, "rest 8"},
{glblrest448, "rest 44"},
{glblCFn51, "extend"},
{glblCFn1140, "collisionAssoc"},
{glblCFn207, "println*"},
{glblCFn1129, "collisionAssoc-promote-i"},
{glblCFn675, "vect-="},
{glblCFn765, "bitpos"},
{glblCFn87, "split-with"},
{glblFldFn1583, ".operator"},
{glblFldFn1587, ".operator 58"},
{glblCFn368, "str<="},
{glblCFn63, "empty?"},
{glblempty_QM_466, "empty? 2"},
{glblempty_QM_729, "empty? 8"},
{glblempty_QM_1310, "empty? 12"},
{glblempty_QM_464, "empty? 44"},
{glblempty_QM_1312, "empty? 13"},
{glblempty_QM_1308, "empty? 11"},
{glblCFn71, "filter"},
{glblfilter699, "filter 8"},
{glblCFn677, "vect-store"},
{glblCFn101, "store"},
{glblstore713, "store 8"},
{glblCFn767, "bmiClone"},
{glblFldFn1879, ".values"},
{glblFldFn1883, ".values 70"},
{glblFldFn1593, ".parameter-list"},
{glblFldFn1705, ".parameter-list 63"},
{glblFldFn1681, ".parameter-list 66"},
{glblFldFn1599, ".parameter-list 57"},
{glblCFn89, "take"},
{glblCFn1095, "arrayNodeDissoc"},
{glblCFn1155, "collisionDissoc-find"},
{glblCFn939, "arrayNodeVec"},
{glblCFn1013, "bmiMutateAssoc"},
{glblc657, "core: 881"},
{glblCFn892, "collisionUpdate"},
{glblCFn167, "bit-not"},
{glblCFn65, "count"},
{glblcount410, "count 2"},
{glblcount1371, "count 46"},
{glblcount735, "count 8"},
{glblcount1238, "count 12"},
{glblcount390, "count 44"},
{glblcount1270, "count 13"},
{glblcount1206, "count 11"},
{glblcount1325, "count 47"},
{glblFldFn19, ".x"},
{glblFldFn23, ".x 43"},
{glblCFn1061, "arrayNodeGet"},
{glblFldFn1563, ".value"},
{glblFldFn1565, ".value 61"},
{glblFldFn1577, ".value 59"},
{glblFldFn1663, ".value 68"},
{glblFldFn1571, ".value 60"},
{glblFldFn1892, ".value 73"},
{glblFldFn1697, ".module-path"},
{glblFldFn1701, ".module-path 64"},
{glblSome22, "Some"},
{glblCFn128, "str-vect"},
{glblstr_vect474, "str-vect 2"},
{glblstr_vect250, "str-vect 43"},
{glblstr_vect737, "str-vect 8"},
{glblstr_vect472, "str-vect 44"},
{glblstr_vect194, "str-vect 42"},
{glblstr_vect316, "str-vect 1"},
{glblFldFn1513, ".field-list"},
{glblFldFn1525, ".field-list 50"},
{glblCFn140, "inc"},
{glblCFn61, "interpose"},
{glblinterpose697, "interpose 8"},
{glblCFn143, "-"},
{glblCFn895, "collisionAppend"},
{glblFldFn1613, ".symbol"},
{glblFldFn1617, ".symbol 55"},
{glblCFn761, "addCopiedBMI"},
{glblCFn649, "subvec"},
{glblFldFn1651, ".c-code"},
{glblFldFn1655, ".c-code 51"},
{glblCFn146, "dec"},
{glblCFn177, "vect-conj"},
{glblCFn39, "comp"},
{glblcomp725, "comp 8"},
{glblFldFn1615, ".type-expr"},
{glblFldFn1619, ".type-expr 55"},
{glblFldFn1653, ".type-expr 51"},
{glblCFn2, "default-type-name"},
{glblCFn124, "keys"},
{glblCFn59, "apply"},
{glblCFn31, "or"},
{glblor236, "or 43"},
{glblor182, "or 42"},
{glblCFn904, "bmiReplaceCopied"},
{glblFldFn1607, ".alternatives"},
{glblFldFn1609, ".alternatives 56"},
{glblCFn871, "bmiShrink"},
{glblCFn875, "emptyCollisionNode"},
{glblCFn977, "arrayNodeHashSeq"},
{glblCFn35, "cond"},
{glblcond240, "cond 43"},
{glblcond186, "cond 42"},
{glblcond310, "cond 1"},
{glblFldFn1645, ".lines"},
{glblFldFn1647, ".lines 52"},
{glblCFn81, "last"},
{glbllast442, "last 2"},
{glbllast721, "last 8"},
{glbllast440, "last 44"},
{glblCFn757, "arrayNodeCount"},
{glblCFn105, "sha1"},
{glblsha1430, "sha1 2"},
{glblsha1428, "sha1 44"},
{glblsha1308, "sha1 1"},
{glblCFn118, "dissoc"},
{glbldissoc1292, "dissoc 12"},
{glbldissoc1294, "dissoc 13"},
{glbldissoc1290, "dissoc 11"},
{glblCFn1118, "collisionAssoc-find"},
{glblFldFn387, ".len"},
{glblFldFn1383, ".len 46"},
{glblFldFn399, ".len 44"},
{glblCFn1166, "collisionDissoc"},
{glblCFn108, "assoc"},
{glblassoc1298, "assoc 12"},
{glblassoc1300, "assoc 13"},
{glblassoc1296, "assoc 11"},
{glblCFn347, "str-count"},
{glblCFn907, "bmiReplaceMutate"},
{glblCFn57, "extract"},
{glblextract246, "extract 43"},
{glblCFn53, "duplicate"},
{glblCFn334, "identity"},
{glblCFn820, "bmiKey"},
{glblSubString394, "SubString"},
{glblCFn110, "copyAssoc"},
{glblcopyAssoc1230, "copyAssoc 12"},
{glblcopyAssoc1262, "copyAssoc 13"},
{glblcopyAssoc1198, "copyAssoc 11"},
{glblFldFn1511, ".name"},
{glblFldFn1679, ".name 66"},
{glblFldFn1555, ".name 62"},
{glblFldFn1521, ".name 50"},
{glblFldFn1661, ".name 68"},
{glblFldFn1597, ".name 57"},
{glblCFn130, "free-resource"},
{glblCFn416, "str-sha1"},
{glblCFn120, "get"},
{glblget733, "get 8"},
{glblget1286, "get 12"},
{glblget1288, "get 13"},
{glblget1284, "get 11"},
{glblCFn256, "int-<"},
{glblCFn749, "arrayNodeUpdate"},
{glblSymbol1551, "Symbol"},
{glblDefType1672, "DefType"},
{glblMain1704, "Main"},
{glblStringLit1576, "StringLit"},
{glblDefp1678, "Defp"},
{glblFieldGetter1624, "FieldGetter"},
{glblIntegerLit1564, "IntegerLit"},
{glblConstructor1520, "Constructor"},
{glblSuperposition1608, "Superposition"},
{glblDefinition1660, "Definition"},
{glblTypeConstraint1616, "TypeConstraint"},
{glblExtendType1688, "ExtendType"},
{glblLocation1480, "Location"},
{glblFloatLit1570, "FloatLit"},
{glblBlockComment1646, "BlockComment"},
{glblFn1596, "Fn"},
{glblInline1652, "Inline"},
{glblAddNs1698, "AddNs"},
{glblMatch1636, "Match"},
{glblCall1586, "Call"},
{glblModule1500, "Module"},
{glblCFn1789, "and-or-desugar-rec"},
{glblCFn1758, "result-kind"},
{glblresult_kind2152, "result-kind 72"},
{glblresult_kind2155, "result-kind 71"},
{glblresult_kind2147, "result-kind 73"},
{glblresult_kind2158, "result-kind 74"},
{glblCFn1801, "let-desugar-rec"},
{glblCFn1771, "text-to-int-acc"},
{glblCFn1725, "symbol-continue?"},
{glblCFn1791, "and-or-desugar"},
{glblCFn1737, "run-length"},
{glblCFn1735, "skip-whitespace"},
{glblCFn1862, "dispatch-special-2"},
{glblCFn1846, "parse-cond"},
{glblCFn1848, "parse-and-or"},
{glblParserError1903, "ParserError"},
{glblCFn1765, "read-full-symbol"},
{glblCFn1811, "parse-number"},
{glblCFn1747, "read-string"},
{glblCFn1717, "state-line"},
{glblCFn1866, "parse-fn"},
{glblCFn1783, "cond-inner"},
{glblCFn1719, "is-digit"},
{glblCFn1836, "parse-threading-steps-acc"},
{glblCFn1844, "parse-hash"},
{glblCFn1826, "parse-body"},
{glblCFn1803, "let-desugar"},
{glblParserIgnore1897, "ParserIgnore"},
{glblCFn1795, "thread-step"},
{glblCFn1832, "read-hash-pairs-acc"},
{glblCFn1850, "parse-and"},
{glblCFn1817, "parse-symbol"},
{glblCFn1809, "parse-type-constraint"},
{glblCFn1874, "parse-expr"},
{glblCFn1828, "read-let-bindings-acc"},
{glblCFn1807, "read-param-list"},
{glblCFn1842, "parse-vector"},
{glblCFn1870, "dispatch-special"},
{glblCFn1805, "read-param-list-acc"},
{glblCFn1773, "text-to-int"},
{glblCFn1755, "elt2"},
{glblCFn1785, "cond-desugar-rec"},
{glblCFn1872, "parse-call"},
{glblCFn1824, "parse-body-acc"},
{glblCFn1745, "read-string-content"},
{glblCFn1838, "parse-threading-steps"},
{glblCFn1779, "hash-fold-acc"},
{glblCFn1856, "parse-str"},
{glblCFn1834, "read-hash-pairs"},
{glblCFn1760, "expr-kind"},
{glblexpr_kind2167, "expr-kind 54"},
{glblexpr_kind2164, "expr-kind 62"},
{glblexpr_kind2162, "expr-kind 58"},
{glblCFn1813, "parse-string-lit"},
{glblCFn1830, "read-let-bindings"},
{glblCFn1769, "contains-dot"},
{glblCFn1822, "parse-expr-list"},
{glblCFn1723, "symbol-start?"},
{glblCFn1749, "read-symbol"},
{glblCFn1727, "float-char?"},
{glblCFn1840, "parse-call-args"},
{glblCFn1787, "cond-desugar"},
{glblCFn1751, "read-int"},
{glblCFn1815, "parse-quoted-sym"},
{glblCFn1854, "parse-str-println"},
{glblParserMatch1891, "ParserMatch"},
{glblCFn1721, "is-alpha"},
{glblCFn1775, "fold-vec-acc"},
{glblCFn1741, "elt0"},
{glblCFn1799, "threading"},
{glblCFn1820, "parse-expr-list-acc"},
{glblCFn1860, "parse-threading"},
{glblCFn1868, "parse-let"},
{glblCFn1715, "make-state"},
{glblCFn1733, "skip-comment"},
{glblCFn1781, "hash-fold"},
{glblCFn1761, "make-loc"},
{glblCFn1858, "parse-println"},
{glblCFn1777, "fold-vec"},
{glblCFn1753, "read-float"},
{glblCFn1729, "peek-char"},
{glblCFn1793, "str-println-desugar"},
{glblCFn1739, "read-run"},
{glblCFn1763, "vect-concat"},
{glblCFn1743, "elt1"},
{glblParserState1880, "ParserState"},
{glblCFn1797, "threading-rec"},
{glblCFn1767, "read-type-expr"},
{glblCFn1864, "parse-fn-body"},
{glblCFn1731, "take-char"},
{glblCFn1852, "parse-or"},
{glblCFn1469, "val-kind"},
{glblval_kind2627, "val-kind 61"},
{glblCFn1470, "main"}};
typeNameMap typeNames[49] = {{1, "Integer"},
{2, "String"},
{8, "Vector"},
{9, "VectorNode"},
{11, "HashMap"},
{12, "HashMap"},
{13, "HashMap"},
{14, "HashMap"}
,
{11, "BitmapIndexedNode"},
{46, "Cons"},
{18, "Opaque"},
{3, "FnArity"},
{45, "GetSentinelVal"},
{12, "ArrayNode"},
{43, "Some"},
{13, "HashCollisionNode"},
{42, "None"},
{47, "EndOfList"},
{44, "Leaf"},
{44, "SubString"},
{69, "returns"},
{62, "Symbol"},
{67, "DefType"},
{63, "Main"},
{59, "StringLit"},
{66, "Defp"},
{54, "FieldGetter"},
{61, "IntegerLit"},
{50, "Constructor"},
{56, "Superposition"},
{68, "Definition"},
{55, "TypeConstraint"},
{65, "ExtendType"},
{48, "Location"},
{60, "FloatLit"},
{52, "BlockComment"},
{57, "Fn"},
{51, "Inline"},
{64, "AddNs"},
{53, "Match"},
{58, "Call"},
{49, "Module"},
{71, "ParserError"},
{72, "ParserIgnore"},
{74, "ParserFail"},
{73, "ParserMatch"},
{70, "ParserState"}};
void normGlobals(){
Term apps, lams, V;
Location vLoc;
{
Term glblVal17_1;
glblVal17 = dupeArg(glblVal17, &glblVal17_1, 0);

// call None at core: 97
Term glblRslt16Args = makePair(APP, 0, NUL, SUB);
glblRslt16 = newTerm(VAR, 0, portLoc(2, glblRslt16Args));
pushRedex(glblRslt16Args, glblVal17_1);
}
{
Term glblVal351_1;
glblVal351 = dupeArg(glblVal351, &glblVal351_1, 0);

// call Leaf at core: 588
Term glblRslt350Args = makePair(APP, 0, NUL, SUB);
glblRslt350 = newTerm(VAR, 0, portLoc(2, glblRslt350Args));
pushRedex(glblRslt350Args, glblVal351_1);
}
{
Term glblVal1182_1;
glblVal1182 = dupeArg(glblVal1182, &glblVal1182_1, 0);

// call GetSentinelVal at core: 1867
Term glblRslt1181Args = makePair(APP, 0, NUL, SUB);
glblRslt1181 = newTerm(VAR, 0, portLoc(2, glblRslt1181Args));
pushRedex(glblRslt1181Args, glblVal1182_1);
}
{
Term glblVal1332_1;
glblVal1332 = dupeArg(glblVal1332, &glblVal1332_1, 0);

// call EndOfList at core: 2003
Term glblRslt1330Args = makePair(APP, 0, NUL, SUB);
glblRslt1330 = newTerm(VAR, 0, portLoc(2, glblRslt1330Args));
pushRedex(glblRslt1330Args, glblVal1332_1);
}
{
Term glblVal1714_1;
glblVal1714 = dupeArg(glblVal1714, &glblVal1714_1, 0);

// call returns at /home/jim/toccata/interpreter/intrp-ast.toc: 100
Term glblRslt1713Args = makePair(APP, 0, NUL, SUB);
glblRslt1713 = newTerm(VAR, 0, portLoc(2, glblRslt1713Args));
pushRedex(glblRslt1713Args, glblVal1714_1);
}
{
Term glblVal1888_1;
glblVal1888 = dupeArg(glblVal1888, &glblVal1888_1, 0);

// call ParserFail at /home/jim/toccata/interpreter/intrp-rdr.toc: 30
Term glblRslt1887Args = makePair(APP, 0, NUL, SUB);
glblRslt1887 = newTerm(VAR, 0, portLoc(2, glblRslt1887Args));
pushRedex(glblRslt1887Args, glblVal1888_1);
}
  normalize(NULL);
  if (termTag(glblVal1389) == VAR)
    glblVal1389 = take(termLoc(glblVal1389));
  if (termTag(glblRslt1330) == VAR)
    glblRslt1330 = take(termLoc(glblRslt1330));
  if (termTag(glblVal1332) == VAR)
    glblVal1332 = take(termLoc(glblVal1332));
  if (termTag(glblRslt1181) == VAR)
    glblRslt1181 = take(termLoc(glblRslt1181));
  if (termTag(glblVal1182) == VAR)
    glblVal1182 = take(termLoc(glblVal1182));
  if (termTag(glblVal651) == VAR)
    glblVal651 = take(termLoc(glblVal651));
  if (termTag(glblVal401) == VAR)
    glblVal401 = take(termLoc(glblVal401));
  if (termTag(glblRslt350) == VAR)
    glblRslt350 = take(termLoc(glblRslt350));
  if (termTag(glblVal351) == VAR)
    glblVal351 = take(termLoc(glblVal351));
  if (termTag(glblVal262) == VAR)
    glblVal262 = take(termLoc(glblVal262));
  if (termTag(glblVal106) == VAR)
    glblVal106 = take(termLoc(glblVal106));
  if (termTag(glblVal25) == VAR)
    glblVal25 = take(termLoc(glblVal25));
  if (termTag(glblRslt16) == VAR)
    glblRslt16 = take(termLoc(glblRslt16));
  if (termTag(glblVal17) == VAR)
    glblVal17 = take(termLoc(glblVal17));
  if (termTag(glblVal13) == VAR)
    glblVal13 = take(termLoc(glblVal13));
  if (termTag(glblRslt1713) == VAR)
    glblRslt1713 = take(termLoc(glblRslt1713));
  if (termTag(glblVal1714) == VAR)
    glblVal1714 = take(termLoc(glblVal1714));
  if (termTag(glblVal1711) == VAR)
    glblVal1711 = take(termLoc(glblVal1711));
  if (termTag(glblVal1703) == VAR)
    glblVal1703 = take(termLoc(glblVal1703));
  if (termTag(glblVal1693) == VAR)
    glblVal1693 = take(termLoc(glblVal1693));
  if (termTag(glblVal1685) == VAR)
    glblVal1685 = take(termLoc(glblVal1685));
  if (termTag(glblVal1677) == VAR)
    glblVal1677 = take(termLoc(glblVal1677));
  if (termTag(glblVal1667) == VAR)
    glblVal1667 = take(termLoc(glblVal1667));
  if (termTag(glblVal1659) == VAR)
    glblVal1659 = take(termLoc(glblVal1659));
  if (termTag(glblVal1649) == VAR)
    glblVal1649 = take(termLoc(glblVal1649));
  if (termTag(glblVal1643) == VAR)
    glblVal1643 = take(termLoc(glblVal1643));
  if (termTag(glblVal1629) == VAR)
    glblVal1629 = take(termLoc(glblVal1629));
  if (termTag(glblVal1621) == VAR)
    glblVal1621 = take(termLoc(glblVal1621));
  if (termTag(glblVal1611) == VAR)
    glblVal1611 = take(termLoc(glblVal1611));
  if (termTag(glblVal1605) == VAR)
    glblVal1605 = take(termLoc(glblVal1605));
  if (termTag(glblVal1591) == VAR)
    glblVal1591 = take(termLoc(glblVal1591));
  if (termTag(glblVal1581) == VAR)
    glblVal1581 = take(termLoc(glblVal1581));
  if (termTag(glblVal1575) == VAR)
    glblVal1575 = take(termLoc(glblVal1575));
  if (termTag(glblVal1569) == VAR)
    glblVal1569 = take(termLoc(glblVal1569));
  if (termTag(glblVal1560) == VAR)
    glblVal1560 = take(termLoc(glblVal1560));
  if (termTag(glblVal1531) == VAR)
    glblVal1531 = take(termLoc(glblVal1531));
  if (termTag(glblVal1504) == VAR)
    glblVal1504 = take(termLoc(glblVal1504));
  if (termTag(glblVal1489) == VAR)
    glblVal1489 = take(termLoc(glblVal1489));
  if (termTag(glblVal1908) == VAR)
    glblVal1908 = take(termLoc(glblVal1908));
  if (termTag(glblVal1900) == VAR)
    glblVal1900 = take(termLoc(glblVal1900));
  if (termTag(glblVal1896) == VAR)
    glblVal1896 = take(termLoc(glblVal1896));
  if (termTag(glblRslt1887) == VAR)
    glblRslt1887 = take(termLoc(glblRslt1887));
  if (termTag(glblVal1888) == VAR)
    glblVal1888 = take(termLoc(glblVal1888));
  if (termTag(glblVal1885) == VAR)
    glblVal1885 = take(termLoc(glblVal1885));
}
void freeGlobals(){
  freeGlobal(glblVal1389);
  freeGlobal(glblRslt1330);
  freeGlobal(glblVal1332);
  freeGlobal(glblRslt1181);
  freeGlobal(glblVal1182);
  freeGlobal(glblVal651);
  freeGlobal(glblVal401);
  freeGlobal(glblRslt350);
  freeGlobal(glblVal351);
  freeGlobal(glblVal262);
  freeGlobal(glblVal106);
  freeGlobal(glblVal25);
  freeGlobal(glblRslt16);
  freeGlobal(glblVal17);
  freeGlobal(glblVal13);
  freeGlobal(glblRslt1713);
  freeGlobal(glblVal1714);
  freeGlobal(glblVal1711);
  freeGlobal(glblVal1703);
  freeGlobal(glblVal1693);
  freeGlobal(glblVal1685);
  freeGlobal(glblVal1677);
  freeGlobal(glblVal1667);
  freeGlobal(glblVal1659);
  freeGlobal(glblVal1649);
  freeGlobal(glblVal1643);
  freeGlobal(glblVal1629);
  freeGlobal(glblVal1621);
  freeGlobal(glblVal1611);
  freeGlobal(glblVal1605);
  freeGlobal(glblVal1591);
  freeGlobal(glblVal1581);
  freeGlobal(glblVal1575);
  freeGlobal(glblVal1569);
  freeGlobal(glblVal1560);
  freeGlobal(glblVal1531);
  freeGlobal(glblVal1504);
  freeGlobal(glblVal1489);
  freeGlobal(glblVal1908);
  freeGlobal(glblVal1900);
  freeGlobal(glblVal1896);
  freeGlobal(glblRslt1887);
  freeGlobal(glblVal1888);
  freeGlobal(glblVal1885);
  normalize(NULL);
}
Term mainFn = newRef(glblCFn1470);
int typeCount = 75;
