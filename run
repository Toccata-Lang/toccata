#!/bin/bash

rm $1.out $1.tmp
$TOCCATA_DIR/toccata $1 > $1.tmp &&
awk '/^#$/ { printf "#line %d \"%s\"\n", NR+1, "m.c"; next; } { print; }' $1.tmp > m.c &&
$CC -g -fno-objc-arc -o $1.out -I$TOCCATA_DIR -std=c99 -DCHECK_MEM_LEAK=1 $TOCCATA_DIR/core.c m.c -lpthread &&
./$1.out "${@:2}"

