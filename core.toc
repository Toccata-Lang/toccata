
(defn pr* [str]
  ;; Send a string to the current output stream
  ;; (assert (instance? String str))
  (inline C Number "return(pr_STAR(arg0));"))

(defn number-str [n]
  (inline C String "return(integer_str(arg0));"))
