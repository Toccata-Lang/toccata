
(main [_]
  (let [cs [1 2]]
    (cond (Some cs (partial = 3))
          (abort)

          (every cs (partial instance? Integer))
          (print-err 'yay)

          (some cs (partial instance? Integer))
          (abort)

          (abort))))
