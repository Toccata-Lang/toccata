
(defn f1 [d]
  (or (map (get d 'x)
           (fn [x]
             (vector (str (inc 9)))))
      ;; this comment used to cause an incRef error
      )
  (or (map (get d 'x)
           (fn [x]
             (vector (str (inc 9)))))
      ;; this comment used to cause an incRef error
      ))

(defn f2 [x]
  (and (and
        ;; just a comment
        None)

       ;; try a comment here
       (Some 99)))

(main [_]
  (f1 {})
  (f2 'flyer))
